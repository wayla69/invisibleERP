# UAT — Cycle 03: Procure-to-Pay (Expenditure / AP)

**Status: DRAFT v1.5 · 2026-07-03** · *v1.5: one-tap goods receipt — web รับครบ + LINE `receive <PO no>` (UAT-P2P-094).* · *v1.4: PR→PO conversion + item reconcile/new-code (UAT-P2P-093).* · *v1.3: LP-2 copilot expense/advance drafts (UAT-P2P-092).* · *v1.2: EXP-10 AP invoice intake — new cases UAT-P2P-086..091 (scan → PO auto-map → matched-at-posting, ambiguity → NeedsReview, duplicate refusal, cumulative one-PO-one-bill guard, blocked-then-released by the scheduled auto re-match, direct image/PDF upload with type/size gates). Cross-ref adds EXP-10 + `compliance.ts` EXP-10 ToE.* · *v1.1: LC-5 confirm-first AI PR drafts (UAT-P2P-085).* · *v1.0: LC-2 petty-cash chat raise (UAT-P2P-084).* · *v0.9: LC-1 one-tap chat approvals (docs/30) — UAT-P2P-083 (postback confirm flow, replay safety, SoD over buttons).* · *v0.8: PO attachment evidence (0228) — UAT-P2P-081..082 (web + LINE chat photo attach, evidence-integrity delete rules).* · Cross-ref: process narrative `02-procure-to-pay.md` (EXP-01..07, EXP-09, R02–R04/R07/R13), `07-cash-treasury.md` (EXP-07), harness `tools/cutover/src/match.ts`, `compliance.ts` (EXP-06 + EXP-01/EXP-09 ToE), `e2e.ts`, `basics.ts`, `line-crm.ts` (LINE chat → PR). · *v0.7: LINE chat phase 2 (0228) — new cases UAT-P2P-076..080 (workflow LINE notifications, approve/reject via chat with engine SoD, my prs/find/cancel/stock self-service).* · *v0.6: LINE chat → PR channel (narrative §7 step 2) — new cases UAT-P2P-070..075 (link-code binding, chat PR into the same approval workflow, unlinked/permission/dedup/unlink negatives).* · *v0.5 (PwC Capital Markets): the 3-way-match payment gate (EXP-01/EXP-09 — the panel's "EXP-03") now has an explicit Test-of-Effectiveness in the formal ICFR harness `compliance.ts`, not only `match.ts`; see `compliance/CONTROL_STATUS_HONEST.md` for the panel↔RCM ID crosswalk.* · *v0.4 (WS2.3): formalized the AP 3-way-match payment gate as control **EXP-09**; retagged the payment-gate cases (P2P-006 variance-blocks-pay, P2P-011 non-PO-pays-gate-fails-open) to EXP-09.*

**v0.4 — access redesign (PR/PO/GR + AP/Payment split by user group):** PR moved to `/requisitions` (perm `pr_raise`, company-wide); PO stays `/procurement` (`procurement`); GR moved to `/receiving` and tightened to `wh_receive`; AP disbursement approval moved to the finance-owned `/disbursements` screen (`approvals`/`gl_close`). New cases UAT-P2P-060..062 + UAT-UI-P2P-ACC-01 below.

Result legend: Pass / Fail / Blocked / N/A / Not Run. Error codes/amounts are exact.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-P2P-001 | Create purchase order | Buyer/Procurement | Item X, approved vendor V1 | 1. `POST /api/procurement/pos`. | `{vendor_id: V1, items:[{item_id:X, order_qty:100, unit_price:10}]}` | 201; `po_no` matches `PO-YYYYMMDD-###`; `total_amount`=1000. | High | Positive | EXP-01 | Not Run | e2e.ts/match.ts |
| UAT-P2P-002 | Approve PO (maker≠checker) | Procurement | PO created by Buyer | 1. `PATCH /api/procurement/pos/{po}/approve` as a different approver. | `{approve: true}` | 200; PO Approved. | High | Control | EXP-01, R02 | Not Run | match.ts |
| UAT-P2P-003 | Goods receipt against PO | WarehouseOperator | PO approved | 1. `POST /api/procurement/grs`. | `{po_no, items:[{item_id:X, received_qty:100}]}` | GR created; `po_items.received_qty`=100. | High | Positive | EXP-02 | Not Run | match.ts |
| UAT-P2P-004 | 3-way match success → payable | ApClerk | PO 100@10 + GR 100; AP txn 1000 | 1. `POST /api/finance/ap/transactions`. 2. `POST /api/procurement/match/run`. | lines `[{item_id:X, qty:100, unit_price:10}]` | `match_status: matched`, `payable: true`. | High | Positive | EXP-03 | Not Run | match.ts |
| UAT-P2P-005 | Matched invoice → request + approve posts GL | ApClerk + approver | Matched txn (P2P-004) | 1. `PATCH .../ap/transactions/{txn}/pay` amount 1000 (maker). 2. `POST .../ap/payments/{no}/approve` (different user). | `{amount: 1000}` | Request 200 `PendingApproval` (no GL yet); approve → bill `Paid`; PAY-AP GL Dr2000=1000 / Cr1000=1000. | High | Positive | EXP-06, GL-01 | Not Run | match.ts |
| UAT-P2P-006 | Price variance blocks payment (AP-pay gate) | ApClerk | PO @10, invoice @12 (+20%) | 1. `POST .../match/run` qty100 @12. 2. `PATCH .../pay`. | `[{item_id:X, qty:100, unit_price:12}]` | `match_status: price_variance`, payable false, `price_var_pct`=20; the pay request is refused at the gate (`assertPayable`) → 409 `MATCH_BLOCKED`. | High | Control | EXP-09, EXP-01 | Pass | match.ts, compliance.ts |
| UAT-P2P-007 | Quantity over-invoice blocks payment | ApClerk | GR 100, invoice qty 120 | 1. `POST .../match/run` qty120 @10. 2. `PATCH .../pay`. | `[{item_id:X, qty:120, unit_price:10}]` | `match_status: over_invoiced`; pay → 409 (blocked). | High | Control | EXP-03 | Not Run | match.ts |
| UAT-P2P-008 | Tolerance honoured | ApClerk/PricingManager | tolerance set | 1. `PUT .../match/tolerance` price_pct 2. 2. match @10.15. 3. set price_pct 0, re-match. | unit_price 10.15 | At 2% → `matched` payable; at 0% → `price_variance`. | Med | Control | EXP-03 | Not Run | match.ts |
| UAT-P2P-009 | Override unblocks blocked invoice (independent overrider) | FinancialController (≠ the matcher) | price_variance invoice (P2P-006), matched by another user | 1. `POST /api/procurement/match/{txn}/override` with reason. 2. request pay. 3. approve (different user). | `{reason: manager approved variance}` | `override: true`, `override_by` ≠ matcher; request accepted; approve → bill `Paid`; PAY-AP Dr2000=1200 posted. | High | Control | EXP-01, EXP-06, R04 | Pass | match.ts |
| UAT-P2P-009b | Matcher cannot override their own match (SoD) | the user who RAN the match | price_variance invoice they matched | 1. `POST /api/procurement/match/{txn}/override` as the matcher. | `{reason: self-override}` | `403 SOD_VIOLATION` (binds even Admin) — override needs a different user. | High | Control | EXP-01 | Pass | match.ts |
| UAT-P2P-010 | Override cleared by re-match | ApClerk | overridden invoice re-matched still failing | 1. Override. 2. Re-run match (still @12). 3. `PATCH .../pay`. | unit_price 12 | Pay → 409 `MATCH_BLOCKED` (stale override reset). | High | Control | EXP-03 | Not Run | match.ts |
| UAT-P2P-011 | Non-PO bill pays (gate fails open) | ApClerk + approver | AP txn with no match row | 1. `POST .../ap/transactions` 500. 2. request pay 500. 3. approve. | `{amount: 500}` | `assertPayable` returns (no match row) so the request is accepted (only PO-matched invoices are match-gated); approve → bill `Paid`. | Med | Positive | EXP-09, EXP-06 | Not Run | match.ts |
| UAT-P2P-012 | Blocklisted vendor cannot transact | Buyer | Vendor V1 blocklisted | 1. `PATCH .../suppliers/{V1}/status` blocklisted. 2. `POST .../pos`. 3. submit RFQ quote for V1. | `{blocklisted: true, reason: คุณภาพต่ำ}` | createPo → 422 `SUPPLIER_BLOCKED`; quote → 422. | High | Control | EXP-04, R13 | Not Run | match.ts |
| UAT-P2P-013 | Un-blocklist restores ordering | MasterDataAdmin | V1 blocklisted | 1. `PATCH .../suppliers/{V1}/status` blocklisted false, approved. 2. `POST .../pos`. | `{blocklisted: false, approval_status: approved}` | 201; `po_no` PO- created. | Med | Positive | EXP-04 | Not Run | match.ts |
| UAT-P2P-014 | RFQ → quote → award builds PO | Buyer | Item X | 1. `POST .../rfqs`. 2. `POST .../rfqs/{no}/quotes`. 3. `POST .../rfqs/{no}/award`. | quote 50@9.50 | RFQ-/PO- created; awarded PO line 50 @9.50. | Med | Positive | EXP-01 | Not Run | match.ts |
| UAT-P2P-015 | Re-run match idempotent | ApClerk | matched txn | 1. Re-run match twice with same lines. | qty100 @10 | Single result row, same `match_no`. | Med | Positive | EXP-03 | Not Run | match.ts |
| UAT-P2P-016 | RLS — cannot mutate another tenant's vendor | Procurement (T1) | T2 owns vendor VB | 1. `PATCH .../suppliers/{VB}/status` as T1 user. | `{blocklisted: true}` | 404; VB row untouched (no cross-tenant DoS). | High | Control | ITGC-AC (RLS) | Not Run | match.ts |
| UAT-P2P-017 | AP sub-ledger ↔ GL reconciles | FinancialController | gated pays done | 1. `GET /api/finance/reconciliation`. | — | AP balanced (`ap.balanced`/`ap_balanced` true). | Med | Detective | REC-01 | Not Run | match.ts |
| UAT-P2P-018 | Supplier portal: vendor sees only own POs (D3) | Vendor | vendor linked (user_name); POs for V1 & V2 | 1. `GET /api/supplier/purchase-orders`. 2. `GET /api/supplier/purchase-orders/<V2 po>`. | bearer vendor | Lists only V1's PO; V2's PO → 404. | High | Control | Feature (supplier portal) | Not Run | supplier.ts |
| UAT-P2P-019 | Supplier acknowledge + submit invoice | Vendor | own PO | 1. `POST .../:po/acknowledge`. 2. `POST /api/supplier/invoices {po_no,invoice_no,amount}`. | as left | `acknowledged_at` set; AP- pending (Unpaid) created + listed. | High | Positive | Feature (supplier portal) | Not Run | supplier.ts |
| UAT-P2P-020 | Supplier cannot invoice another vendor's PO | Vendor | V2 PO exists | 1. `POST /api/supplier/invoices {po_no:<V2 po>}`. | — | 400 `PO_NOT_YOURS`. | High | Control | Feature (supplier portal) | Not Run | supplier.ts |
| UAT-P2P-021 | Supplier portal unlinked user refused | user w/ `vendor_portal`, no vendor | — | 1. `GET /api/supplier/purchase-orders`. | — | 403 `VENDOR_NOT_LINKED`. | Med | Control | Feature (supplier portal) | Not Run | supplier.ts |
| UAT-P2P-022 | AP bill idempotency (W3) | ApClerk | — | 1. `POST /api/finance/ap/transactions {amount, idempotency_key:k}`. 2. Retry same key. | as left | Same `txn_no`; 2nd `idempotent:true`; exactly 1 ap_transactions row + 1 AP GL entry (no duplicate payable). | High | Control | EXP-03 / GL-01 | Not Run | match.ts |
| UAT-P2P-023 | AP payment-request idempotency (W3) | ApClerk + approver | matched bill | 1. request pay `{amount, idempotency_key:k}`. 2. Retry same key. 3. approve once. | as left | One pending request (2nd `idempotent:true`); approve → paid once; exactly 1 PAY-AP GL entry (no double cash-out). | High | Control | EXP-06 / GL-01 | Not Run | match.ts |
| UAT-P2P-029 | Pre-paid bill creation blocked | ApClerk | — | 1. `POST .../ap/transactions {amount, paid_amount>0}`. | `{amount: 500, paid_amount: 500}` | 400 `AP_PREPAID_BLOCKED` (cannot disburse without approval). | High | Control | EXP-06 | Not Run | compliance.ts |
| UAT-P2P-030 | Payment request creates no GL/paid effect | ApClerk | Unpaid bill | 1. request pay. 2. inspect bill + PAY-AP GL. | `{amount: 1000}` | `PendingApproval`; bill `paid_amount`=0, status Unpaid; no PAY-AP entry; listed in pending queue. | High | Control | EXP-06 | Not Run | compliance.ts |
| UAT-P2P-031 | Requester self-approval blocked (SoD) | dual creditors+approvals; Admin | own pending request | 1. requester (also holds approvals) approves own request. 2. Admin approves Admin's own request. | — | Both → 403 `SOD_VIOLATION` (binds even Admin). | High | Control | EXP-06, R03/R07 | Not Run | compliance.ts |
| UAT-P2P-032 | Maker without approval authority blocked | ApClerk (creditors only) | pending request | 1. `POST .../ap/payments/{no}/approve` as the maker. | — | 403 (lacks `approvals`/`gl_close`). | Med | Control | EXP-06 | Not Run | compliance.ts |
| UAT-P2P-033 | Reject leaves bill unpaid | approver | pending request | 1. `POST .../ap/payments/{no}/reject {reason}`. | `{reason: duplicate}` | 200 `Rejected`; bill `paid_amount`=0; no GL. | Med | Control | EXP-06 | Not Run | compliance.ts |
| UAT-P2P-024 | Dimension routing — PO routes by vendor | Buyer | PO workflow: step1 vendor=ACME→Planner, step2 vendor=OTHER→Procurement | 1. Create PO vendor ACME. 2. Create PO vendor OTHER. | as left | ACME PO engages step 1 (Planner); OTHER PO engages step 2 (Procurement). | High | Positive | EXP-03 (workflow) | Not Run | workflow.ts |
| UAT-P2P-025 | PO approval routes through the engine | Planner | ACME PO at step 1 | 1. `PATCH /api/procurement/pos/{no}/approve {approve:true}`. | bearer planner | Engine clears step 1 → PO `Approved`. | High | Positive | EXP-03 (workflow) | Not Run | workflow.ts |
| UAT-P2P-026 | SLA escalation sweep flags + reminds | Admin | PR workflow sla_hours, instance overdue | 1. force `due_at` past. 2. `POST /api/workflow/run-escalations`. | as left | Instance `escalated`; reminder notification written; sweep returns escalated≥1. | High | Control | EXP-03 (workflow) | Not Run | workflow.ts |
| UAT-P2P-027 | Escalation fallback approver can act | Planner (escalate-to) | escalated instance (step approver = Procurement) | 1. `POST /api/workflow/instances/{id}/act {approve}` as Planner. | as left | Approved — the SLA-fallback approver may act once escalated. | High | Control | EXP-03 (workflow) | Not Run | workflow.ts |
| UAT-P2P-028 | No-code builder replaces steps | MasterDataAdmin | a definition with 2 steps | 1. `PUT /api/workflow/definitions/{id} {steps:[1 step]}`. | as left | Definition now has 1 step. | Med | Positive | Feature (workflow builder) | Not Run | workflow.ts |
| UAT-P2P-029 | Vendor statement of account | ApClerk | Vendor with bills + approved payments | 1. `GET /api/finance/ap/statement?vendor=&from=&to=`. | — | Opening balance + bills (charges) and payments in date order with a running balance; closing balance ties to AP open. | Med | Detective | EXP-06 | Not Run | basics.ts |
| UAT-P2P-030 | Petty cash: issue an advance | ApClerk | — | 1. `POST /api/finance/advances {payee, amount:1000}`. | — | `ADV-…` open; posts **Dr 1180 / Cr 1000**; 1180 float = 1000. | High | Control | EXP-07 | Not Run | basics.ts |
| UAT-P2P-031 | Petty cash: settle reconciles or rejects | ApClerk | Open advance 1000 | 1. settle 700+200 (≠1000). 2. settle 700+300. | — | Step 1 → `400 SETTLE_MISMATCH`; step 2 clears the float (Dr 5100 700 + Dr 1000 300 / Cr 1180 1000), advance `settled`. | High | Control | EXP-07 | Not Run | basics.ts |
| UAT-UI-SUP-01 | Supplier portal screen (vendor self-service) | Vendor (perm `vendor_portal`) | vendor user mapped to a vendor; a PO issued to them | 1. Sidebar → **จัดซื้อ → พอร์ทัลซัพพลายเออร์ (Supplier)** (`/supplier`). 2. **ใบสั่งซื้อ (PO)** tab → click the PO → **ยืนยันรับทราบ PO**. 3. **ใบแจ้งหนี้** tab → submit an invoice. | invoice 1000 + VAT 70 | Vendor sees ONLY their own POs; acknowledge flips the row to **รับทราบแล้ว**; invoice submit creates an **Unpaid AP txn** (toast `txn_no`); another vendor's PO is never visible (self-scoped). | Med | UI | Vendor portal (EXP-01..04 downstream) | Not Run | manual / e2e |
| UAT-P2P-040 | Capital PO line → GR eligible (not stocked) | Buyer + WarehouseOperator | — | 1. `POST .../pos` with a line `is_capital:true`. 2. approve PO. 3. `POST .../grs` receive that line. 4. `GET /api/assets/registrations/eligible?gr_no=`. | PO line 2 @ 25000, `is_capital:true` | GR created (PO auto-closes); the capital line is **not** capitalised to inventory (1200); eligible list shows 1 line, `suggested_cost`=50000. | High | Positive | FA-10 | Not Run | basics.ts |
| UAT-P2P-041 | Register asset from GR → PendingApproval, no GL | FaAccountant (maker) | eligible GR line (P2P-040) | 1. `POST /api/assets/registrations {gr_no, gr_item_id, name, useful_life_months}`. 2. inspect 1500. | life 36 | 201 `FAR-…` `PendingApproval`; `acquire_cost`=50000; Dr 1500 unchanged (no GL yet). | High | Control | FA-10 | Not Run | basics.ts / compliance.ts |
| UAT-P2P-042 | Capitalization self-approval blocked (SoD) | FaAccountant (maker); Admin | own pending FAR- | 1. preparer approves own request. 2. Admin approves own request. | — | Both → 403 `SOD_VIOLATION` (binds even Admin). | High | Control | FA-10, R07 | Not Run | basics.ts / compliance.ts |
| UAT-P2P-043 | Independent approval creates asset + posts GL | Controller (checker ≠ maker) | pending FAR- | 1. `POST /api/assets/registrations/{reg}/approve`. 2. `GET /api/assets`. | — | 201 `Posted`; `FA-…` created; acquisition JE **Dr 1500 / Cr 2000 +50000**; asset on register with `source_gr_no`/`source_po_no` (PR→PO→GR→FA traceability). | High | Control | FA-10, GL-01 | Not Run | basics.ts / compliance.ts |
| UAT-P2P-044 | GR line cannot be capitalised twice | FaAccountant | line already registered/posted | 1. `POST /api/assets/registrations` again for the same `gr_item_id`. | — | 400 `ALREADY_REGISTERED`. | Med | Control | FA-10 | Not Run | basics.ts |
| UAT-UI-CAP-01 | Capitalize-from-GR screen | FaAccountant + Controller | a GR with a capital line | 1. `/assets` → **ตั้งทรัพย์สินจาก GR** → enter GR no → **ค้นหา**. 2. **ตั้งทรัพย์สิน** on a line → name + life → **ส่งคำขอ**. 3. a **different** user opens the **รออนุมัติ** queue → **อนุมัติ**. | life 60 | Eligible line listed with suggested cost; request shows in the pending queue; approve creates the asset (toast `FA-…`) and it appears on the register with source GR/PO; self-approve is refused. | Med | UI | FA-10 | Not Run | manual / e2e |
| UAT-P2P-050 | Establish petty-cash fund within float (วงเงิน) | ApClerk | — | 1. `POST /api/finance/petty-cash/funds {fund_code, float_limit:5000, initial_amount:5000}`. 2. establish with `initial_amount` > float. | as left | Fund created, balance 5000, **Dr 1015 / Cr 1000**; over-float initial → 400 `OVER_FLOAT`. | High | Control | EXP-08 | Not Run | basics.ts |
| UAT-P2P-051 | Expense request → PendingApproval, no GL | ApClerk (maker) | fund PCF-1 | 1. `POST /api/finance/petty-cash/requests {fund_code, kind:expense, amount:1200, doc_ref}`. 2. inspect 5100/1015. | as left | 201 `PEX-…` `PendingApproval`; no GL posted; request carries the document ref. | High | Control | EXP-08 | Not Run | basics.ts, compliance.ts |
| UAT-P2P-052 | Disbursement self-approval blocked (SoD) | ApClerk (maker); Admin | own pending PEX- | 1. preparer approves own request. 2. Admin approves own request. | — | Both → 403 `SOD_VIOLATION` (binds even Admin). | High | Control | EXP-08, R07 | Not Run | basics.ts, compliance.ts |
| UAT-P2P-053 | Independent approval posts GL + decrements fund | Controller (checker ≠ maker) | pending expense PEX- | 1. `POST …/requests/{req}/approve`. | — | 200 `Approved`; **Dr 5100 / Cr 1015** (1200); fund balance 3800. | High | Control | EXP-08, GL-01 | Not Run | basics.ts, compliance.ts |
| UAT-P2P-054 | Draw beyond the fund balance blocked | ApClerk | fund balance 1800 | 1. request expense 5000. | — | 422 `INSUFFICIENT_FLOAT`. | High | Control | EXP-08 | Not Run | basics.ts, compliance.ts |
| UAT-P2P-055 | Advance approve → disburse → settle | ApClerk + Controller | fund PCF-1 | 1. request advance 2000 → approve (**Dr 1180 / Cr 1015**). 2. settle 1500 spend + 500 returned. | — | Advance disbursed; settle posts **Dr 5100 + Dr 1015 / Cr 1180**, returns 500 to the fund, status `Settled`; mismatch → `SETTLE_MISMATCH`. | High | Control | EXP-08, GL-01 | Not Run | basics.ts |
| UAT-P2P-056 | Replenish capped at the float limit | ApClerk | fund balance 2300, float 5000 | 1. replenish 4000. 2. replenish 2000. | — | Step 1 → 422 `OVER_FLOAT`; step 2 → balance 4300 (**Dr 1015 / Cr 1000**). | Med | Control | EXP-08 | Not Run | basics.ts |
| UAT-P2P-057 | Pending petty-cash requests in GOV-01 monitor | FinancialController | a pending PEX- | 1. `GET /api/finance/approvals/pending`. | — | The pending request appears with `control:'EXP-08'`. | Med | Detective | EXP-08, GOV-01 | Not Run | basics.ts |
| UAT-UI-PCX-01 | Petty-cash fund + expense screen | ApClerk + Controller | — | 1. `/petty-cash` → **กองทุน** create fund. 2. **เปิดค่าใช้จ่าย** raise expense/advance. 3. a **different** user **อนุมัติ**. | float 5000 | Fund shows balance/limit/available; request posts only on independent approval; self-approve refused; advance settle returns cash to fund. | Med | UI | EXP-08 | Not Run | manual / e2e |
| UAT-P2P-060 | PR raised by any employee (company-wide `pr_raise`) | Cashier (holds `pr_raise`, **not** `procurement`) | Item X | 1. Login as a non-procurement staff user. 2. `POST /api/procurement/prs`. | `{items:[{item_id:X, request_qty:5}]}` | 201; PR created (status Pending) — `pr_raise` alone suffices; raising a requisition is not restricted to Procurement. | High | Positive | R03 (access design) | Not Run | manual |
| UAT-P2P-061 | GR refused for a procurement-only user (R04 at permission layer) | Procurement (`procurement`, **no** `warehouse`/`wh_receive`) vs WarehouseOperator | PO approved | 1. `POST /api/procurement/grs` as the procurement-only user. 2. retry as a `wh_receive` user. | `{po_no, items:[{item_id:X, received_qty:100}]}` | Step 1 → **403** (buyer cannot confirm receipt — needs `wh_receive`); step 2 → GR created. Enforces SoD R04 at the API. | High | Control | R04 | Not Run | manual |
| UAT-P2P-062 | AP disbursement approved on the finance Disbursements screen | Finance (`approvals`/`gl_close`) vs the requesting AP Clerk | Pending AP payment request (booked by Accounting on `/finance`) | 1. `GET /api/finance/ap/payments/pending` (the `/disbursements` queue). 2. `POST .../ap/payments/{no}/approve` as Finance (≠ requester). 3. requester (creditors) attempts the same approve. | `{amount: 1000}` | Request visible on `/disbursements`; Finance approve → bill `Paid` + PAY-AP GL (Dr2000/Cr1000); requester approve → 403 `SOD_VIOLATION`. Accounting books/requests, Finance releases. | High | Control | EXP-06, R07 | Not Run | compliance.ts |
| UAT-UI-P2P-ACC-01 | Procurement & AP screens split by user group | mixed roles | users with each permission | 1. `pr_raise`-only user opens `/requisitions`. 2. `procurement` user opens `/procurement`. 3. `wh_receive` user opens `/receiving`. 4. `creditors` user opens `/finance` (AP); `approvals`/`gl_close` user opens `/disbursements`. | — | Each user sees **only** their own step's screen in the sidebar; PR/PO/GR/AP-book/AP-release are five separate routes; a screen a user lacks the permission for is hidden (nav) and its API returns 403. | Med | UI | R03/R04/R07 | Not Run | manual / e2e |
| UAT-P2P-063 | Multi-currency PO — USD fx rate stored | Buyer | — | 1. `POST /api/procurement/pos {vendor_id, currency:'USD', fx_rate:34, items:[...]}`. 2. approve + GR. | — | PO created with `currency=USD`, `fx_rate=34`; GR inherits `currency=USD`, `fx_rate=34`. | High | Positive | C1 | Not Run | basics.ts |
| UAT-P2P-064 | GR inherits PO currency / rate | WarehouseOperator | PO-USD above | 1. `POST /api/procurement/grs {po_no:...}`. 2. inspect GR. | — | GR `currency=USD`, `fx_rate=34` matches the PO. | High | Positive | C1 | Not Run | basics.ts |
| UAT-P2P-065 | JPY vendor statement rounds to 0dp | ApClerk | JPY AP invoice | 1. `GET /api/finance/ap/statement?vendor=&currency=JPY`. | charge ¥10000 | `total_charges=10000` (integer); `closing_balance` an integer; no fractional yen. | High | Control | C1, EXP-06 | Not Run | basics.ts |
| UAT-P2P-066 | Vendor PII encrypted at rest; ghost-vendor detector still fires | Admin + DBA (raw SQL) | two vendors sharing one tax ID | 1. Create 2 vendors with the same `tax_id` + a `bank_account`. 2. Raw SQL `SELECT tax_id, bank_account FROM vendors`. 3. `POST /api/controls/scan` → `GET /api/controls/findings`. | tax ID `9999999999999` | (2) stored values are ciphertext `v1:…` — never the plaintext; (3) a `ghost_vendor` finding is raised naming the shared (decrypted) tax ID — the detector groups decrypted values in app code. | High | Control | ITGC-AC-19, EXP-02 | Not Run | ext.ts |
| UAT-P2P-070 | LINE chat: staff links their LINE account with a one-time code | Cashier (`pr_raise`) | Tenant LINE OA webhook configured (`/api/line/webhook/<shop>`) | 1. `POST /api/line/link-code` (or the **สร้างรหัสเชื่อม LINE** card on `/requisitions`). 2. In the OA chat send `link <code>`. 3. `GET /api/line/link`. | 6-char code, TTL 10 min | (1) 200 `{code, expires_at, linked:false}`; (2) bot replies **เชื่อมบัญชีสำเร็จ ✔**; (3) `{linked:true}`. `users.line_user_id` bound (DB-unique). | High | Positive | EXP-03 (entry integrity) | Pass | line-crm.ts |
| UAT-P2P-071 | LINE chat: `pr` command raises a real PR into the approval workflow | linked Cashier | UAT-P2P-070 done | 1. Chat `pr A4-PAPER 10 กระดาษหมด, TONER-85A 2`. 2. Chat `status <PR no>`. | as left | (1) Bot replies the new `PR-YYYYMMDD-###`; PR exists status **Pending**, `requested_by` = the linked user, 2 lines with qty/reason; enters the same PR approval workflow as the web. (2) reply `รออนุมัติ`. | High | Positive | EXP-03 | Pass | line-crm.ts |
| UAT-P2P-072 | LINE chat: unlinked / expired-code identities are refused | any LINE user | — | 1. From an unlinked LINE account chat `pr GOLD-BAR 999`. 2. Send `link <wrong/expired code>`. | as left | (1) Guidance reply (ยังไม่ได้เชื่อมบัญชีพนักงาน); **no PR row created**. (2) rejected (รหัสเชื่อมไม่ถูกต้องหรือหมดอายุ); no identity bound. | High | Control | EXP-03 (entry integrity) | Pass | line-crm.ts |
| UAT-P2P-073 | LINE chat: linked user without `pr_raise` cannot raise; customer-portal role cannot issue a code | AccessAdmin (no `pr_raise`); Customer | AccessAdmin LINE-linked | 1. AccessAdmin chats `pr A4-PAPER 1`. 2. Customer-role user calls `POST /api/line/link-code`. | — | (1) refused (ไม่มีสิทธิ์ … `pr_raise`); no PR created — chat re-resolves effective permissions per command. (2) **403** — link codes are a `pr_raise` surface. | High | Control | EXP-03, R03 (access design) | Pass | line-crm.ts |
| UAT-P2P-074 | LINE chat: webhook redelivery cannot duplicate a PR; a LINE account binds to one user | linked Cashier | UAT-P2P-071 done | 1. Redeliver the SAME webhook message id (`pr …`). 2. A second user issues a code; the already-linked LINE account sends `link <code2>`. | as left | (1) deduped — PR count unchanged. (2) rejected (บัญชี LINE นี้ถูกเชื่อมกับผู้ใช้อื่นแล้ว); second user stays unlinked. | High | Control | EXP-03 (entry integrity) | Pass | line-crm.ts |
| UAT-P2P-075 | LINE chat: free customer chat is ignored; unlink stops the channel | linked Cashier; customer LINE user | — | 1. A customer chats `สวัสดีค่ะ ขอดูเมนูหน่อย` to the OA. 2. Staff `DELETE /api/line/link` (or **ยกเลิกการเชื่อมต่อ LINE**), then chats `pr A4-PAPER 1`. | — | (1) No reply, nothing logged — the OA stays a customer channel. (2) `{linked:false}`; subsequent `pr` gets the not-linked guidance (no PR). | Med | Control | EXP-03 (entry integrity) | Pass | line-crm.ts |
| UAT-P2P-076 | LINE notify: approver pushed on queue entry; requester pushed on decision | linked Cashier + linked Procurement | Active PR workflow (step 1 → role Procurement); both users LINE-linked | 1. Cashier chats `pr A4-PAPER 3 หมึกใกล้หมด`. 2. Procurement chats `approve <PR no>`. | as left | (1) The linked Procurement user receives a 🔔 LINE push naming the PR + the `approve <PR no>` hint (message_log campaign `wf_notify`). (2) PR flips **Approved** and the requester receives the ✅ decision push. | High | Positive | EXP-03 (workflow) | Pass | line-crm.ts |
| UAT-P2P-077 | LINE chat: approve routes through the engine — SoD binds (self-approve refused) | linked Procurement (maker) | Own chat-raised PR pending at their role's step | 1. Raise `pr TONER-85A 1` from the Procurement user's chat. 2. Same user chats `approve <that PR>`. | as left | Reply contains `SOD_VIOLATION`; PR stays **Pending** — the maker cannot approve their own document over chat any more than on the web. | High | Control | EXP-03, R07 | Pass | line-crm.ts |
| UAT-P2P-078 | LINE chat: approve without `procurement` refused; reject decides + notifies | linked Cashier (no `procurement`); linked Procurement | Pending PR | 1. Cashier chats `approve <PR no>`. 2. Procurement chats `reject <PR no> ซื้อรวมล็อตหน้า`. | as left | (1) Refused (ไม่มีสิทธิ์ … `procurement`); PR unchanged. (2) PR **Rejected**; requester receives the ❌ decision push. | High | Control | EXP-03 | Pass | line-crm.ts |
| UAT-P2P-079 | LINE chat: `my prs` / `find <keyword>` self-service lookups | linked Cashier | Items seeded | 1. Chat `my prs`. 2. Chat `find กระดาษ`. | as left | (1) Lists the caller's own recent PRs with Thai statuses (never another user's). (2) Returns matching item ids + descriptions from the item master. | Med | Positive | Feature (chat self-service) | Pass | line-crm.ts |
| UAT-P2P-080 | LINE chat: `cancel` own pending PR only; `stock` read-only on-hand | linked Cashier | Own pending PR; inv_balances row for the item | 1. Chat `cancel <own pending PR>`. 2. Chat `cancel <another user's PR>`. 3. Chat `stock <item id>`. | as left | (1) PR **Cancelled** and its pending workflow instance closed (`cancelled`). (2) Refused — `PR_NOT_YOURS` (ยกเลิกได้เฉพาะคำขอของตนเอง). (3) Replies the tenant-scoped on-hand total + per-location breakdown; no data is written. | High | Control | EXP-03 (entry integrity) | Pass | line-crm.ts |
| UAT-P2P-081 | PO attachments: web upload/list/fetch; delete = uploader-or-Admin | ApClerk (uploader) + Procurement + Admin | PO exists | 1. `POST /api/procurement/attachments {doc_type:PO, doc_no, data_url, kind}`. 2. `GET …/attachments?doc_type=PO&doc_no=`. 3. `GET …/attachments/:id`. 4. DELETE as a *different* perm-holder. 5. DELETE as the uploader. 6. Upload at unknown PO; upload a non-data-URL. | photo ≤2MB | (2) lists **metadata only** (no base64 payloads); (3) returns the `data_url`; (4) **403 `NOT_UPLOADER`** — evidence integrity; (5) deleted; (6) 404 / 400 `BAD_IMAGE`. | High | Control | EXP-01 (evidence) | Pass | line-crm.ts |
| UAT-P2P-084 | LINE chat: petty-cash expense/advance raise + EXP-08 notifications (LC-2) | linked ApClerk (maker) + second linked ApClerk; linked Cashier | Active fund with balance; users LINE-linked | 1. Cashier chats `expense <fund> 100`. 2. ApClerk chats `expense <fund> 300 ค่าน้ำแข็ง`. 3. ApClerk chats `expense <fund> 99999`. 4. ApClerk chats `advance <fund> 200`; second ApClerk approves one on `/petty-cash` and rejects the other with a reason. | as left | (1) refused (needs `creditors`/`exec`). (2) PEX- **PendingApproval, no GL**; the OTHER creditors/exec holders get the 🔔 push — the maker is excluded. (3) `INSUFFICIENT_FLOAT` reply, no request. (4) approve → requester ✅ push (amount); reject → ❌ push (reason). Chat can raise, never decide (approval stays on `/petty-cash`). | High | Control | EXP-08, R07 | Pass | line-crm.ts |
| UAT-P2P-085 | LINE copilot: AI-drafted PR is confirm-first (LC-5) | linked staff (`pr_raise`) | Staff LINE-linked | 1. Chat `บอท ขอซื้อ A4-PAPER 4 ใกล้หมดแล้ว`. 2. Inspect PRs BEFORE confirming. 3. Tap [ยืนยันสร้าง PR]. 4. Chat `bot วันนี้อากาศดีจัง`. | as left | (1) Flex draft card echoing the parsed item/qty/reason — **no PR row exists yet**. (3) PR created through the normal path (same numbering, `requested_by` = linked staff, enters the workflow); reply tagged `chat_ai_confirm`. (4) honest "ยังไม่เข้าใจ" + usage — no guessing, no action. | High | Control | EXP-03 (entry integrity; AI drafts) | Pass | line-crm.ts |
| UAT-P2P-092 | LINE copilot: AI-drafted petty-cash expense/advance is confirm-first (LP-2) | linked staff (`creditors`) | Staff LINE-linked; petty-cash fund exists | 1. Chat `บอท ขอเบิก 250 จาก <fund> ค่าน้ำแข็ง`. 2. Inspect expense requests BEFORE confirming. 3. Tap [ยืนยันเบิกเงิน]. | as left | (1) Flex draft card (fund/amount/reason echoed) — **no PEX row exists yet**. (3) PEX created **PendingApproval** through the SAME chat `expense` path (maker = linked staff, no GL, EXP-07/08 guards bind); decision stays on `/petty-cash`. | High | Control | EXP-07/08 (AI drafts) | Pass | line-crm.ts |
| UAT-P2P-093 | Convert an approved PR → PO with item reconcile + new-code opening | Procurement (`procurement`) | an Approved PR whose line is a free-text name | 1. `GET /api/procurement/items/search?q=<name>`. 2. `POST /api/procurement/prs/<pr>/to-po` with one matched existing item + one `create_item:true` new code, vendor + prices. 3. Re-read the PR + the new item + PO. 4. Try converting a non-approved PR. | as left | (1) master matches returned; (2) PO raised via the normal path (vendor screening + approval); the new item-master code is opened; (3) PR marked **Converted**, every PR line stamped with the PO no (`pr_items.po_no`); (4) **422 `PR_NOT_APPROVED`** (or 404). | High | Positive+Control | EXP (PR→PO link) | Pass | line-crm.ts |
| UAT-P2P-094 | One-tap รับครบ (receive-all) — web button + LINE `receive <PO no>`; approval + SoD gates hold | WarehouseOperator / linked staff (`wh_receive`/`warehouse`/`procurement`) | An Approved PO with outstanding qty; a still-Pending PO; a fully-received PO | 1. On `/receiving`, confirm the **รับครบ** button shows only for the Approved / part-received PO (not Pending/Closed). 2. Click it (or chat `receive <PO no>`). 3. Chat `receive <PO no>` for a still-Pending PO. 4. Chat `receive <PO no>` again for the now-closed PO. | as left | (1) Button visible only for receivable POs. (2) `POST /api/procurement/pos/:poNo/receive-all` → GR created, every line fully received, PO auto-**Closed**; the receiver sees the GR no. (3) refused — **PO สถานะ … ต้องได้รับอนุมัติก่อน** (EXP-03 gate; un-approved PO cannot be received). (4) **`NOTHING_TO_RECEIVE`** (รับของครบแล้ว). | High | Positive+Control | EXP-02, EXP-03, R04 | Pass | line-crm.ts / receiving-receive-all.spec.ts |
| UAT-P2P-083 | LINE chat: one-tap postback approve with confirm step (LC-1) | linked Procurement; linked Cashier (requester) | Active PR workflow; both linked | 1. Requester raises a PR in chat — approver's push is a flex card with [อนุมัติ]/[ปฏิเสธ]. 2. Approver taps [อนุมัติ]. 3. Taps [ยืนยัน]. 4. Replays the same confirm postback. 5. Approver taps approve on their OWN PR and confirms. | as left | (2) Confirm card only — PR still **Pending** (no confirm = no action). (3) PR **Approved** via the engine; requester ✅ push. (4) Expiry reply — state consumed before acting, replay cannot double-act. (5) `SOD_VIOLATION` — buttons are the same control path as typed commands. `my prs` replies a flex carousel. | High | Control | EXP-03, R07 | Pass | line-crm.ts |
| UAT-P2P-082 | LINE chat: `attach <PO no>` + photo → attachment; negatives | linked Procurement; linked Cashier | PO exists; tenant LINE token set | 1. Chat `attach <PO no> receipt` then send a photo (≤10 min). 2. Replay the same photo webhook. 3. Photo with no pending state. 4. `attach <unknown PO>`. 5. `attach` from a user without procurement/creditors/wh_receive. | as left | (1) Photo fetched from the LINE content API and stored (`source:line`, kind receipt, uploader = linked staff); bot replies the attachment count. (2) No duplicate (state consumed). (3) Ignored silently — customer images unaffected. (4) ไม่พบเอกสาร. (5) refused (ไม่มีสิทธิ์). | High | Control | EXP-01 (evidence) | Pass | line-crm.ts |
| UAT-P2P-086 | AP intake: scan with a PO number auto-maps + books + matches in one step | ApClerk (`creditors`) | Approved PO fully received (e.g. 40@25 = 1,000); invoice text carries the PO number | 1. `/procurement/ap-intake` → paste the scan text (vendor, `Invoice# IV-9001`, `PO Number: <PO>`, total 1,000). 2. Press **อัตโนมัติทั้งหมด** (`POST /api/procurement/ap-intake/auto`). 3. Request + approve payment as usual. | as left | (2) Intake **Posted**: mapped `po_number` (100%), AP bill created, 3-way match run **in the same flow** → `matched`, **พร้อมจ่าย**. (3) Payment request passes the gate; approval pays (EXP-09/EXP-06 unchanged). | High | Positive | **EXP-10**, EXP-01 | Pass | match.ts, compliance.ts |
| UAT-P2P-087 | AP intake: no PO number → vendor+amount auto-map only when unambiguous; ties → NeedsReview | ApClerk | One 600-baht received PO for the vendor; separately two 1,000-baht POs for the same vendor | 1. Scan a 600-baht invoice (no PO reference) → `POST /api/procurement/ap-intake`. 2. Scan a 1,000-baht invoice (no PO reference). 3. On (2) pick a candidate / `PUT …/map`, then post. | as left | (1) **Mapped** `vendor_amount` to the single 600-baht PO; post → `matched`. (2) **NeedsReview** with ≥2 scored candidates — the mapper never guesses between tied POs. (3) Manual map + post runs the match normally. | High | Control | **EXP-10** | Pass | match.ts |
| UAT-P2P-088 | AP intake: duplicate invoice number is never auto-booked; explicit post refused | ApClerk | UAT-P2P-086 done (IV-9001 booked) | 1. Re-scan the same IV-9001 text via `/auto`. 2. `POST …/:intake/post` on the new intake. | as left | (1) `auto_posted: false`, status **NeedsReview**, `dup_of` = the earlier document — no second bill, no GL. (2) **409 `DUPLICATE_INVOICE`** (a genuine re-bill needs the deliberate `allow_duplicate` flag). | High | Control | **EXP-10** | Pass | match.ts, compliance.ts |
| UAT-P2P-089 | AP intake: cumulative guard — one PO cannot be billed twice under two invoice numbers | ApClerk | PO from UAT-P2P-086 already fully invoiced (1,000/1,000) | 1. Scan a NEW invoice number for the same vendor, amount 1,000; map it to the same PO. 2. Post. 3. Attempt payment. | as left | (2) Match `over_invoiced` (invoices matched to a PO consume its received value; remaining = 0), **ระงับ**. (3) Payment request → **409 `MATCH_BLOCKED`**. | High | Control | **EXP-10**, EXP-01, EXP-09 | Pass | match.ts |
| UAT-P2P-090 | AP intake: invoice ahead of goods is blocked, then released by the scheduled auto re-match; non-PO scan fails open | ApClerk + scheduler | Approved PO 20@50, only 10 received; unknown-vendor utility scan | 1. Scan a 1,000-baht invoice carrying the PO number → `/auto`; attempt payment. 2. Receive the remaining 10; schedule + run `ap_automatch_rerun` (`POST /api/bi/subscriptions` + `/run`). 3. Re-check the match; pay. 4. Scan an unmappable utility invoice; post it without a PO. | as left | (1) Posted but `over_invoiced` → pay **409 MATCH_BLOCKED**. (2) Sweep reports `released n of m`; the invoice re-verdicts **matched** from current GR state (overrides untouched). (3) Payment now passes → Paid. (4) **NeedsReview**, no candidates → posts as a **non-PO bill**, payable (EXP-09 fail-open); payment still maker-checked. | High | Control | **EXP-10**, EXP-09 | Pass | match.ts |
| UAT-P2P-091 | AP intake upload: direct image/PDF — PDF text layer auto-posts; keyless image queues with the file; type/size gates | ApClerk | Approved PO fully received (25@40 = 1,000); no `ANTHROPIC_API_KEY` (deterministic path) | 1. `/procurement/ap-intake` → **แนบรูป / PDF**, attach a digital PDF invoice carrying the PO number → **อัตโนมัติทั้งหมด** (`POST /api/procurement/ap-intake/upload/auto`). 2. `GET …/:no/file`. 3. Upload a photo (PNG) with AI unconfigured, then post it. 4. Upload a `.txt` data URL; upload an oversized (>~5 MB) image. | as left | (1) Text layer extracted deterministically (`extract_source: rules`) → auto-mapped `po_number`, bill posted, match `matched`, **พร้อมจ่าย**; file stored on the intake. (2) Returns the stored document (object-store URL, or the inline `data:` URL fallback). (3) **NeedsReview**, `extract_source: none`, file attached for review; post → **400 `INTAKE_AMOUNT_REQUIRED`** (a blank bill can never book). (4) **400 `UNSUPPORTED_FILE_TYPE`**; **400 `FILE_TOO_LARGE`**. | High | Control | **EXP-10** | Pass | match.ts |
