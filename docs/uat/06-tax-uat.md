# UAT — Cycle 06: Tax Compliance (VAT / WHT / e-Tax)

**Status: DRAFT v0.5 · 2026-07-06** · *v0.5 (maker-checker gap G16, Phase P3 — detective, no new control): new detective **voided-tax-invoice exception report** `GET /api/tax-invoices/exceptions/voided` (`exec`/`ar`/`fin_report`, optional `from`/`to` on issue date) lists every voided invoice (doc no./type/issue date/source/total/reason/created_by + count & total) for independent periodic review; the void itself stays single-user (RD requirement, numbers never reused); added UAT-TAX-041, harness `taxdocs.ts`.* · *v0.4: ใบลดหนี้ (ม.86/10) / ใบเพิ่มหนี้ (ม.86/9) — credit/debit note with maker-checker + signed output-VAT adjustment (new control TAX-07, migration 0248) — UAT-TAX-035..038.* · Cross-ref: process narrative `06-tax-compliance.md` (TAX-01..07, REV-10, PAY-02), harness `tools/cutover/src/etax.ts`, `etax-sign.ts`, `taxdocs.ts` (TAX-07 ToE), `worldclass.ts`, `pos-p1.ts`, `payroll.ts`, `basics.ts` (TC-C2-01..08).

Result legend: Pass / Fail / Blocked / N/A / Not Run. VAT = 7%. Error codes/amounts are exact.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-TAX-001 | VAT 7% on a sale | Admin/GlAccountant | — | 1. `GET /api/tax/calc?net=100&country=TH`. | net=100 | `tax`=7. | High | Positive | TAX-01 | Not Run | worldclass.ts |
| UAT-TAX-002 | VAT flows through portal sale | Customer | item A=50 | 1. `POST /api/portal/pos/sales` qty 2. | `{items:[{item_id:A, qty:2, unit_price:50}]}` | `total`=107, `vat`=7. | High | Positive | TAX-01, REV-03 | Not Run | worldclass.ts |
| UAT-TAX-003 | Currency-aware rounding (0-decimal JPY) | Admin | — | 1. `GET /api/tax/calc?net=105&country=TH&currency=JPY`. | net=105, JPY | `tax`=7 (not 7.35 — rounds to 0 minor units). | Med | Control | TAX-01 | Not Run | worldclass.ts |
| UAT-TAX-004 | Supported currencies listed | Admin | — | 1. `GET /api/tax/currencies`. | — | Includes THB and USD. | Low | Positive | TAX-01 | Not Run | worldclass.ts |
| UAT-TAX-005 | e-Tax invoice XML generated (UBL 2.1) | MasterDataAdmin/Admin | Tax invoice TIV-202606-0007 + 2 lines (sub 300, VAT 21, grand 321) | 1. `GET /api/tax-invoices/{docNo}/etax-xml`. | docNo=TIV-202606-0007 | 200; `application/xml`; attachment filename `{docNo}.xml`; starts `<?xml`, root `<Invoice>`, `UBLVersionID 2.1`. | High | Positive | TAX-02 | Not Run | etax.ts |
| UAT-TAX-006 | e-Tax XML header fields correct | Admin | XML from TAX-005 | 1. Inspect XML header. | — | `<cbc:ID>{docNo}`, IssueDate 2026-06-22, InvoiceTypeCode 388, DocumentCurrencyCode THB. | Med | Positive | TAX-02 | Not Run | etax.ts |
| UAT-TAX-007 | e-Tax XML parties & tax IDs | Admin | XML | 1. Inspect PartyTaxScheme. | — | Seller CompanyID 0105551234567 and buyer 0992001234567 present. | Med | Positive | TAX-02 | Not Run | etax.ts |
| UAT-TAX-008 | e-Tax XML tax & totals | Admin | XML | 1. Inspect tax/totals. | — | Percent 7.00; TaxableAmount 300.00; TaxAmount 21.00; PayableAmount 321.00. | High | Positive | TAX-02 | Not Run | etax.ts |
| UAT-TAX-009 | e-Tax XML lines + special-char escaping | Admin | XML | 1. Inspect invoice lines. | — | 2 `<cac:InvoiceLine>`; `<` → `&lt;`, `&` → `&amp;`; no raw angle brackets/ampersand leak. | Med | Control | TAX-02 | Not Run | etax.ts |
| UAT-TAX-010 | e-Tax submission accepted | MasterDataAdmin | Tax invoice exists | 1. `POST /api/tax/etax/submit/{docNo}`. 2. `GET /api/tax/etax/status/{docNo}`. | docNo=TIV-202606-0007 | submit → status `Accepted`, `provider_ref` `mock-*`, `signed:false` (no cert); status read back `Accepted`. | Med | Positive | TAX-02 | Not Run | etax.ts |
| UAT-TAX-011 | e-Tax resubmit idempotent | MasterDataAdmin | Already submitted | 1. `POST /api/tax/etax/submit/{docNo}` again. | same docNo | `idempotent: true`, same `provider_ref`. | Low | Control | TAX-02 | Not Run | etax.ts |
| UAT-TAX-011a | e-Tax XML XAdES-signed | Admin | Signing cert configured (`ETAX_SIGNING_*`) | 1. `GET /api/tax-invoices/{docNo}/etax-xml?signed=1`. 2. Inspect XML. | docNo + test cert | Doc contains `<ds:Signature>` with `<xades:QualifyingProperties>` (SigningTime + SigningCertificate digest) and an embedded `<ds:X509Certificate>`; verifies (digest + RSA + props OK). | High | Positive | TAX-02 | Not Run | etax-sign.ts |
| UAT-TAX-011b | e-Tax signature tamper-evident | Admin | Signed XML from 011a | 1. Alter an amount in the signed XML. 2. Re-verify. | — | Verification fails (`docDigestOk=false`); altering the SignatureValue fails (`signatureOk=false`); a different certificate fails to verify. | High | Control | TAX-02 | Not Run | etax-sign.ts |
| UAT-TAX-011c | e-Tax unsigned fallback (no cert) | Admin | No signing cert | 1. `GET /api/tax-invoices/{docNo}/etax-xml?signed=1`. | docNo=TIV-202606-0007 | 200; valid UBL with **no** `<ds:Signature>`; filename `{docNo}.xml` (no `-signed`). | Med | Control | TAX-02 | Not Run | etax.ts |
| UAT-TAX-012 | Tax-invoice numbering sequential | Admin | Issue 2+ tax invoices | 1. Issue tax invoices. 2. Inspect doc numbers. | — | Monotonic, gapless, tenant-scoped numbering (no duplicates). | Med | Control | TAX-03 | Not Run | tax-docs |
| UAT-TAX-013 | WHT withheld on payroll | GlAccountant | Payroll run 2026-06 | 1. `POST /api/payroll/runs?period=2026-06`. 2. `GET /api/payroll/pnd1?period=2026-06`. | gross 42000 | `wht_total`=170.83; ภ.ง.ด.1 total_wht 170.83; GL 2360 WHT payable cr 170.83. | High | Positive | PAY-02, TAX | Not Run | payroll.ts |
| UAT-TAX-014 | File ภ.พ.30 → DRAFT snapshot (idempotent) | Tax | Output VAT 38.5, input VAT 70 for 2026-06 | 1. `POST /api/tax-reports/filings` `{filing_type:PP30,month:6,year:2026}`. 2. Re-file the same period. | — | (1) `status:DRAFT`, `output_vat`=38.5, `input_vat`=70, `net_vat`=−31.5. (2) same `id` (idempotent), `already_filed:false`. | High | Positive | TAX-05 | Not Run | taxdocs.ts |
| UAT-TAX-015 | Submit requires a reference; lifecycle to ACCEPTED | Tax | DRAFT PP30 from TAX-014 | 1. `POST …/filings/{id}/submit` (no ref). 2. Submit with `submission_ref`. 3. `POST …/{id}/accept`. 4. Re-file the period. | — | (1) **400** (reference required). (2) `status:SUBMITTED`, `submitted_at` set. (3) `status:ACCEPTED`. (4) `already_filed:true`, not overwritten. | High | Control | TAX-05 | Not Run | taxdocs.ts |
| UAT-TAX-016 | Remittance calendar shows due/filed with deadlines | Tax | PP30 ACCEPTED + PND53 filed for 2026-06 | 1. `GET /api/tax-reports/remittance-calendar?year=2026`. | — | June PP30 row `status:ACCEPTED`, `deadline:2026-07-15`; PND rows carry the 7th-of-next-month deadline; unfiled months show `NOT_FILED`. | High | Positive | TAX-05 | Not Run | taxdocs.ts |
| UAT-TAX-017 | Deferred tax run computes DTA from the AR allowance | Tax / FinancialController | A posted AR allowance of 1100 for the tenant; CIT 20% | 1. `POST /api/ledger/deferred-tax/run` `{period:'2026-12', as_of_date:'2026-12-31', tenant_id:N}`. | — | HTTP 200; `status:'Open'`; `dta`=220 (1100×20%), `net_deferred`=220, `delta_posted`=220; `temp_differences` lists the allowance (deductible). | High | Positive | TAX-06 | Not Run | deferred-tax.service.ts runDeferredTax; basics.ts |
| UAT-TAX-018 | Deferred tax post is maker-checker and idempotent | Tax / FinancialController | An Open deferred tax run (UAT-TAX-017) | 1. `POST /api/ledger/deferred-tax/:id/post` **as the runner**. 2. As a **different** user. 3. Post again. | — | (1) 403 `SELF_POST`. (2) HTTP 200; JE posted **Dr 1700 / Cr 5950** 220. (3) 400 `ALREADY_POSTED`. | High | Control | TAX-06 | Not Run | deferred-tax.service.ts postDeferredTax; basics.ts |
| UAT-TAX-018a | Deferred tax web screen — run then maker-checker post | Tax / FinancialController | A posted AR allowance for the tenant; two users with `gl_close`/`gl_post` | 1. Open **`/deferred-tax`** → **คำนวณงวดใหม่** tab; enter period, press **คำนวณ**. 2. On **รายการที่คำนวณ / โพสต์** tab, as the **runner** press **โพสต์เข้า GL**. 3. As a **different** user, press **โพสต์เข้า GL**. | — | (1) run appears with DTA/DTL split, temporary-difference breakdown, `status` Open. (2) toast error `SELF_POST` (poster ≠ runner). (3) success toast with the JE number; row flips to **โพสต์แล้ว** and the Post button disappears. | High | Control | TAX-06 | Not Run | apps/web `/deferred-tax`; deferred-tax.controller.ts |
| UAT-TAX-019 | SG GST 9% provider — correct calc | Admin/exec | — | 1. `GET /api/tax/calc?country=SG&net=100&currency=SGD`. | net=100, SGD | `rate`=0.09, `tax`=9, `gross`=109, `label`='GST 9.00%'. | High | Positive | TAX-01 (C2) | Not Run | basics.ts TC-C2-01 |
| UAT-TAX-020 | MY SST 6% provider — standard rate | Admin/exec | — | 1. `GET /api/tax/calc?country=MY&net=100&currency=MYR`. | net=100, MYR | `rate`=0.06, `tax`=6, `gross`=106, `label`='SST 6.00%'. | High | Positive | TAX-01 (C2) | Not Run | basics.ts TC-C2-02 |
| UAT-TAX-020a | MY SST food exemption | Admin/exec | — | 1. `GET /api/tax/calc?country=MY&net=200&currency=MYR&category=food`. | category=food | `rate`=0, `tax`=0, `label`='SST Exempt'. | Med | Control | TAX-01 (C2) | Not Run | basics.ts TC-C2-03 |
| UAT-TAX-021 | EU VAT 20% generic provider | Admin/exec | — | 1. `GET /api/tax/calc?country=EU&net=100&currency=EUR`. | net=100, EUR | `rate`=0.20, `tax`=20, `gross`=120, `label`='VAT 20.00%'. | Med | Positive | TAX-01 (C2) | Not Run | basics.ts TC-C2-04 |
| UAT-TAX-022 | Tax providers list includes SG, MY, EU | Admin/exec | — | 1. `GET /api/tax/providers`. | — | `countries` array includes 'SG', 'MY', 'EU'. | Med | Positive | TAX-01 (C2) | Not Run | basics.ts TC-C2-05 |
| UAT-TAX-023 | MYR in currency catalogue | Admin/exec | — | 1. `GET /api/tax/currencies`. | — | `currencies` array includes entry with `code='MYR'`, `decimals=2`. | Low | Positive | TAX-01 (C2) | Not Run | basics.ts TC-C2-06 |
| UAT-TAX-024 | MY e-invoice (MyInvois UBL 2.1) accepted | Admin/exec | Provider set to `einvoice.my.myinvois` | 1. `PUT /api/einvoice/config` `{provider:'einvoice.my.myinvois'}`. 2. `POST /api/einvoice/submit` `{doc:{doc_ref:'MY-INV-001',seller:'...',buyer:'...',total:106,currency:'MYR'}}`. | — | `status`='accepted', `ref` starts 'EINV-', `provider`='einvoice.my.myinvois'. | High | Positive | TAX-02 (C2) | Not Run | basics.ts TC-C2-07 |
| UAT-TAX-025 | SG e-invoice (Peppol BIS3) accepted | Admin/exec | Provider set to `einvoice.sg.invoicenow` | 1. `PUT /api/einvoice/config` `{provider:'einvoice.sg.invoicenow'}`. 2. `POST /api/einvoice/submit` `{doc:{doc_ref:'SG-INV-001',seller:'...',buyer:'...',total:109,currency:'SGD'}}`. | — | `status`='accepted', `ref` starts 'EINV-', `provider`='einvoice.sg.invoicenow'. | High | Positive | TAX-02 (C2) | Not Run | basics.ts TC-C2-08 |
| UAT-TAX-026 | WHT withheld at AP payment → GL 2361 (maker-checker) | Creditors (maker) + Approver (checker) | Vendor service bill ฿1070 (1000 net + 70 VAT) | 1. `PATCH /api/finance/ap/transactions/{no}/pay` `{amount:1070, wht_income_type:'3tre-service', wht_rate:0.03}` (maker). 2. `POST /api/finance/ap/payments/{paymentNo}/approve` as a **different** user. 3. Request a payment with `wht_rate:0.5`. | — | (1) request accepted, `wht_rate`=0.03. (2) `wht_amount`=30 (3% of the ฿1000 pre-VAT base), `net_paid`=1040; GL **Dr 2000 / Cr 2361 30 / Cr 1000 1040**. (3) **400** (rate out of range, 0–30%). | High | Control | TAX-03 | Not Run | taxdocs.ts |
| UAT-TAX-027 | ภ.ง.ด.→GL tie-out reconciles vendor WHT | Tax / exec | One approved AP payment with ฿30 WHT in the month | 1. `GET /api/tax-reports/pnd-tieout?month=M&year=Y`. | — | `gl_net_movement`=30, `ap_wht_withheld`=30, `tied_gl_ap:true`; `cert_wht_issued` + `uncertificated_wht` reported (un-certificated WHT flagged). | High | Detective | TAX-03 | Not Run | taxdocs.ts |
| UAT-TAX-028 | Scheduled WHT-cert batch auto-issues the 50-ทวิ from the AP-payment WHT | Tax / exec | An approved AP payment (vendor with a 13-digit tax ID) that withheld ฿30, no certificate yet | 1. Create a BI subscription `report_type='tax_wht_cert_batch'`, `filters:{month:M,year:Y}`. 2. Run it (`POST /api/bi/subscriptions/{id}/run`). 3. `GET /api/wht/certificates`. | — | (2) summary "issued 1 of 1"; (3) a 50-ทวิ (PND53, ฿30) linked to the payment (`ap_txn_no`/`payment_no`). | High | Positive | TAX-03 | Not Run | taxdocs.ts |
| UAT-TAX-029 | WHT-cert batch is idempotent (no duplicate on re-run) | Tax / exec | The batch has already run once | 1. Run the batch again. 2. `GET /api/wht/certificates`. | — | (1) summary "issued 0 of 1" (the certificated payment skipped); (2) exactly one certificate for the payment. | High | Control | TAX-03 | Not Run | taxdocs.ts |
| UAT-TAX-030 | Scheduled PP30 filing-draft job registers the period return | Tax / exec | Period with VAT activity | 1. Create + run a `tax_pp30_draft` subscription for the period. | — | The job runs `success` and registers/refreshes the period's ภ.พ.30 as a DRAFT filing (idempotent per period). | Medium | Positive | TAX-05 | Not Run | taxdocs.ts |
| UAT-TAX-031 | AP bill `tax_code` routes input VAT to the code's account | Creditors | A VAT code `VAT7X` (7%, input account 2102) | 1. `POST /api/finance/ap/transactions` `{amount:1070, tax_code:'VAT7X'}`. 2. `GET /api/ledger/account-ledger?account=2102`. | — | Input VAT ฿70 posts to **2102** (not 2100); a bill with `tax_code:'NOPE'` → `UNKNOWN_TAX_CODE` (400). | High | Positive+Control | GL-21 | Not Run | taxdocs.ts |
| UAT-TAX-032 | AR output VAT routes to the item `vat_code` account | Tax / ar | `posting_determination` on; item `VATITEM` with `vat_code=VAT7X` (output 2101); an order for it | 1. `POST /api/finance/ar/sync`. 2. `GET /api/ledger/account-ledger?account=2101`. | — | Output VAT ฿70 posts to **2101** (item→vat_code); a mixed/un-opted order keeps 2100 (parity). | High | Positive | GL-21 | Not Run | taxdocs.ts |
| UAT-TAX-033 | PP30↔GL reconciliation spans the VAT-account set under routing | Tax / exec | VAT routed to 2101/2102 via a tax code | 1. `GET /api/tax-reports/pp30`. | — | `reconciliation.gl_account` = `2100+2101+2102` (the whole VAT-account set) so the tie stays exact. | High | Detective | TAX-04 | Not Run | taxdocs.ts |
| UAT-TAX-034 | WHT tax_code defaults the income type + rate on an AP payment | Creditors | An active WHT code `WHT3X` (3%, 3tre-service) | 1. `PATCH /api/finance/ap/transactions/{no}/pay` `{amount, wht_tax_code:'WHT3X'}` (no explicit rate). 2. Request with a VAT code as `wht_tax_code`. | — | (1) the payment captures `wht_rate=0.03` from the code; (2) `INVALID_WHT_TAX_CODE` (400). | High | Positive+Control | TAX-03 | Not Run | taxdocs.ts |
| UAT-TAX-035 | Issue a credit note (ใบลดหนี้ ม.86/10) against a tax invoice | AR/Sales (`ar`/`pos`) | An **Issued** full tax invoice (net 100, VAT 7) | 1. `POST /api/tax-invoices/credit-note` `{original_doc_no, reason:'สินค้าชำรุด', lines:[{description, amount:100}]}`. 2. Read the output-VAT report before approval. 3. Omit the reason; issue a credit of 5000. | as left | (1) 201; a `CN-YYYYMM-NNNN` is created with `status: PendingApproval`, `gl_status: Draft`, `subtotal 100 / vat 7 / total 107`, referencing the original. (2) the note is **excluded** from output VAT while pending. (3) missing reason → 400; a credit exceeding the original net → `CREDIT_EXCEEDS_ORIGINAL` (400). | High | Positive+Control | TAX-07 | Pass | taxdocs.ts |
| UAT-TAX-036 | Maker-checker on a credit/debit note (SoD) | Issuer (AR/Sales) + a different approver (`approvals`/`gl_close`/`exec`) | A PendingApproval credit note | 1. `POST /api/tax-invoices/{docNo}/approve-note` as the **issuer**. 2. Approve as a **different** user. | as left | (1) `SOD_VIOLATION` (403) — the issuer cannot approve their own note. (2) 200/201; the note flips to **Issued** and its Draft GL entry posts (`gl.status: Posted`). | High | Control | TAX-07, GL-05 | Pass | taxdocs.ts |
| UAT-TAX-037 | An approved credit/debit note adjusts output VAT in its period | AR + approver | An approved credit note (net 100) and debit note (net 50) | 1. Read the ภ.พ.30 output-VAT total before + after approving the credit note. 2. Approve a debit note and re-read. | as left | (1) the approved **credit note REDUCES** output VAT by 7 (signed − into the note's issue month). (2) the approved **debit note INCREASES** it by 3.5 (signed +). The GL-2100 movement matches (TAX-04 tie preserved). | High | Positive+Control | TAX-07, TAX-04 | Pass | taxdocs.ts |
| UAT-TAX-038 | Print a credit note as a ม.86/10 document | AR/Sales | An issued credit note | 1. `GET /api/tax-invoices/{CN-…}/pdf`. | — | 200; the PDF shows **ใบลดหนี้**, มาตรา 86/10, the **original tax-invoice reference**, the **reason**, the reduced value + VAT and the amount in baht text (HTML fallback when Chromium is absent). | Med | Positive | TAX-07 | Pass | taxdocs.ts |
| UAT-TAX-039 | Full tax invoice PDF applies the no-code document template — presentation only, ม.86/4 fiscal integrity preserved | Admin/exec (author) + AR/Sales (print) | An Issued full tax invoice; a tenant logo set | 1. In **Settings → Document templates**, author an active `tax_invoice_full` template: accent `#0F766E`, a header note, footer terms, **amount-in-words OFF**, and (illegally) **hide seller tax-id + address**. 2. `GET /api/tax-invoices/{TIV-…}/pdf`. | as left | (2) 200; the PDF **honours** the accent colour, logo, header note and footer terms, and **omits** the amount-in-words line. **Fiscal integrity:** the ม.86/4 seller name, **address and Tax ID still print** despite the hide toggles, and the "ใบกำกับภาษี" heading, VAT line and grand total are unchanged. No amounts change; nothing posts to the GL. | High | Positive+Control | TAX-01 | Pass | taxdocs.ts |
| UAT-TAX-040 | Abbreviated 80mm slip (ม.86/6) applies the no-code document template — presentation only, fiscal integrity preserved | Admin/exec (author) + POS/AR (print) | An issued abbreviated tax invoice (ATV-…) | 1. In **Settings → Document templates**, author an active `tax_invoice_abbreviated` template with a **header note** and **footer notes** (terms + an extra line); the live preview shows the real 80mm slip. 2. `GET /api/tax-invoices/{ATV-…}/pdf`. | as left | (2) 200; the slip **prints the header note and footer notes**. **Fiscal integrity:** the "ใบกำกับภาษีอย่างย่อ" title, the seller Tax ID and the VAT-inclusive total ("ราคารวมภาษีมูลค่าเพิ่มแล้ว") still print (ม.86/6). Accent/logo/signature knobs have no effect on the thermal slip. No amounts change; nothing posts to the GL. | High | Positive+Control | TAX-01 | Pass | taxdocs.ts |
| UAT-TAX-041 | Voided-tax-invoice exception report surfaces voided invoices for independent review (G16) | Reviewer (`exec`/`ar`/`fin_report`) | at least one issued tax invoice has been **voided** (with a reason) | 1. Void an issued tax invoice with a reason. 2. `GET /api/tax-invoices/exceptions/voided` (optionally `?from=&to=` on the **issue date**). | — | (2) the report lists **every voided tax invoice** — doc no., type, issue date, source, grand total, **void reason** and `created_by` — with a **count** and **total**, tenant-scoped. Read-only (nothing posts). The void from step 1 appears with its reason. The void itself stays single-user (RD requirement — numbers never reused); value-changing adjustments must instead route via the dual-controlled credit/debit note (TAX-07). | Med | Detective | Detective (no new control) | Not Run | taxdocs.ts |
