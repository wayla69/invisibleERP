# UAT — Cycle 02: Order-to-Cash (Revenue / AR)

**Status: DRAFT v0.24 · 2026-07-07** · *v0.24: customer match-merge / duplicate resolution (DQM, master-data audit Phase 5) — `GET /api/customer-master/duplicates` scores probable duplicates (exact tax-id/email/phone + app-side fuzzy name similarity; `pg_trgm` not enabled, see `common/text-similarity.ts`) and `POST /api/customer-master/:survivorNo/merge` merges a duplicate into a survivor (child-row repoint via `md_merge_repoint` migration `0273`, survivorship back-fill, soft-retire `status='merged'` + `merged_into`; `SELF_MERGE`/`ALREADY_MERGED`/`409 MERGE_CONFLICT` guards); no new control; added UAT-O2C-264..266, harness `customers.ts`.* · *v0.23: customer master gains full relational Party-model depth — multi-address (`customer_addresses`), multi-contact (`customer_contacts`), and a self-referencing parent-company pointer (`parent_customer_no`, migration `0272`, master-data audit Phase 4); no new control, no maker-checker (same reasoning as Phase 3); added UAT-O2C-261..263, harness `customers.ts`.* · *v0.22: customer master gains Oracle/NetSuite-grade fields (credit_terms/sales_rep/category/language/external_ref, migration `0271`) + a new `PATCH /api/customer-master/:customerNo` direct-edit endpoint + its first web CRUD screen `/customers` (search/create/edit + 360° panel — the fast-follow flagged in rev 0.10); no new control, no maker-checker (mirrors the vendor-profile direct-edit precedent — none of these fields carry payment-redirection risk); added UAT-O2C-260, harness `customers.ts`.* · *v0.21 (maker-checker gaps G14 + G10, Phase P3 — detective-first, no new RCM control): **G14** — new detective **void/refund exception report** `GET /api/payments/exceptions/voids-refunds` (`exec`/`ar`/`fin_report`, optional `from`/`to`) lists every void + refund in a window (no./amount/who/when + counts & totals) for independent periodic review; voids and sub-threshold refunds stay single-user by design (till speed), REV-16 still gates large refunds; added UAT-O2C-258, harness `refund-approval.ts`. **G10** — documentation: bank-statement import stays single-user by design and the imported lines are folded into the **reconciliation certifier's evidence** (REC-02, R06 self-certify block), so a bad import is caught at certification; added UAT-O2C-259, harness `bankrec.ts`.* · *v0.20 (maker-checker gaps G12 + G9, Phase P2 — no new RCM control): **G12** — accepting a **billable** CPQ quote (`POST /api/cpq/quotes/:id/accept`, revenue `Dr 1100 AR / Cr 4000`) now enforces the acceptor ≠ the quote's `createdBy`, else `403 SOD_VIOLATION` and no revenue posts; strengthens **R07/R10 / CPQ-03** (no migration — uses `quotes.createdBy`); added UAT-O2C-253/254 (author self-accept blocked, no revenue; a distinct exec accepts → AR/revenue 50000, TB balanced), harness `cpq-gl.ts`. **G9** — a **new bank account** is created `PendingApproval` + inactive and cannot bank cash (`400 BANK_NOT_APPROVED`) until a distinct approver activates it via `POST /api/bank/accounts/:id/approve` (`approvals`/`exec`; self-approve → `403 SOD_VIOLATION`); `.../reject` + queue `GET /api/bank/accounts/pending`; strengthens **REC-05** (migration 0264); added UAT-O2C-255/256/257 (new account pending + deposit blocked, self-approve blocked, distinct approver activates → deposit posts Dr bank / Cr 1000), harness `cash-banking.ts` / `bankrec.ts`.* · *v0.19 (maker-checker gap G6): a **price/promo rule change** is now two-person maker-checker — `upsertRule` stages `PendingApproval`/`active=false` (the discount engine reads only `active=true`, so a staged rule affects **no** quote or sale), a distinct approver (`exec`/`approvals`) activates it via `…/rules/:id/approve`, self-approve → `SOD_VIOLATION`; strengthens **R10** / **MKT-01** (no new control), migration 0262. Added UAT-O2C-250..252 (staged rule inactive in a quote, self-approve blocked, distinct approver activates → discount applies); combo prices not yet staged (follow-up). Harness `pricing.ts` (22).* · *v0.18 (maker-checker gap G7): a customer **credit-limit change** is now two-person maker-checker — it stages `PendingApproval` (ceiling unmoved), a distinct approver (`approvals`/`exec`) applies it, self-approve → `SOD_VIOLATION`; strengthens **REV-08** / SoD **R09** (no new control), migration 0261. Reworked UAT-O2C-148 (now staged, not applied) + added UAT-O2C-247..249 (self-approve blocked, distinct approver applies → 50000, credit-events audit old→new).* · *v0.17 (depth): retention release posts GL + `retention_due` action-center (Depth-1); output VAT + POC/rev-rec reconciliation on progress claims + subcontractor WHT (Depth-2/3) — folded into UAT-O2C-243/244/245, no new control.* · *v0.16: tender / estimating → award — build a priced estimate (cost build-up + markup), track estimating→submitted→won/lost, and on a win award it (proj_tender) which seeds a project + a draft BoQ from the winning bid, whose maker-checker approve sets the controlled budget baseline (UAT-O2C-246, docs/35 P3, control PROJ-18).* · *v0.15: subcontractor management — a subcontract reserves BoQ-line budget (docs/32 commitment); the subcontractor's cumulative valuations are certified maker-checker (proj_subcon vs proj_subcon_certify, SoD R18), posting AP + WIP + retention PAYABLE (2440) into the sub-ledger, with back-charges (UAT-O2C-245, docs/35 P2, control PROJ-17).* · *v0.14: progress billing / งวดงาน — BoQ-line cumulative valuation + maker-checker certification (proj_billing vs proj_billing_certify, SoD R17) that splits AR into net + retention receivable and withholds retention into the sub-ledger (UAT-O2C-244, docs/35 P1, control PROJ-16).* · *v0.13: retention (เงินประกันผลงาน) shared sub-ledger foundation — withhold/release/due + the 1170/2440 retention GL accounts (UAT-O2C-243, docs/35 Phase 0); foundation for the progress-billing (PROJ-16) & subcontract (PROJ-17) tracks, no new control in P0.* · *v0.12: AR document email defaults the recipient to the customer's email on file (`to_email` optional) + a recent-receipts list surface with print/email (UAT-O2C-241..242); presentation-only, no new control.* · *v0.11: printable + emailable AR documents — ใบแจ้งยอดบัญชี (Statement), ใบสำคัญรับเงิน (AR receipt voucher) และ หนังสือทวงถามหนี้ (dunning letter) render HTML→PDF (HTML fallback) + email a PDF attachment via the shared `DocEmailService` (UAT-O2C-238..240); presentation-only, no new control.* · *v0.10: printable + emailable outbound documents — ใบเสนอราคา (Quotation), ใบส่งของ (Delivery Note) และ ใบแจ้งหนี้/ใบวางบิล (AR billing invoice) render HTML→PDF (HTML fallback) and email a PDF attachment via the shared `DocEmailService` (UAT-O2C-235..237); presentation-only, no new control.* · *v0.9: raise site cash from the project workspace — web (docs/32 FU4, UAT-O2C-234); see the team walkthrough `UAT-DOCS32-material-control-walkthrough.md`.* · *v0.8: customer AR card screen `/finance/customers` (UAT-O2C-228).* · Cross-ref: process narrative `01-order-to-cash.md` (REV-01..18, GL-01, R07–R10/R12), harness `e2e.ts`, `worldclass.ts`, `returns.ts`, `compliance.ts`, `pos-p1.ts`, `basics.ts`, `pricing.ts`. · *v0.7 (B4 retail pricing wiring): added portal service-charge + rounding cases UAT-O2C-200..201.* · *v0.6 (B2 UX finish): added POS Favourites + Returns create-flow cases UAT-O2C-198..199 (star toggle persists via user-prefs; create-return dialog → RTN).* · *v0.5 (WS2.3): added AR allowance for doubtful accounts (ECL) cases UAT-O2C-194..197 (aging compute, compute-vs-post SoD, delta post Dr 5720 / Cr 1190, no double-post + register), control REV-18.* · *v0.4: added the touch **register** (`/pos/register`) cases UAT-O2C-151..155 (menu-grid sell, modifier pricing, PromptPay QR, hold/recall, customer-display mirror).*

Result legend: Pass / Fail / Blocked / N/A / Not Run. VAT = 7%. Error codes/amounts are exact.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-O2C-001 | Create POS sales order (happy path) | Sales | Item A seeded, customer T1 | 1. `POST /api/pos/orders` with one line. 2. `GET /api/pos/orders`. | `{customer_name: T1, items:[{item_id:A, order_qty:2, unit_price:10}]}` | 201; `order_no` matches `SO-YYYYMMDD-####`; appears in list. | High | Positive | REV-01 | Not Run | e2e.ts |
| UAT-O2C-002 | Order within credit limit allowed | CreditManager/Sales | Customer creditLimit 1000, open AR 800 | 1. `POST /api/pos/orders` amount 150 (800+150≤1000). | `{customer_name: TCR, items:[{item_id:X, order_qty:1, unit_price:150}]}` | 201; `order_no` returned. | High | Positive | REV-08 | Not Run | compliance.ts |
| UAT-O2C-003 | Order breaching credit limit blocked | Sales | Same customer, open AR 800 | 1. `POST /api/pos/orders` amount 300 (800+300=1100>1000). | `{..., unit_price:300}` | 409 `CREDIT_LIMIT`; no order created. | High | Control | REV-08 | Not Run | compliance.ts |
| UAT-O2C-004 | Customer on credit hold cannot order | Sales | Customer `creditHold=true` | 1. `POST /api/pos/orders` any amount. | `{..., unit_price:1}` | 409 `CREDIT_HOLD`. | High | Control | REV-08 | Not Run | compliance.ts |
| UAT-O2C-005 | Order status transition | Sales | Order created | 1. Advance order status (e.g. to Shipped/Completed) via order endpoint. 2. `GET /api/pos/orders`. | order from O2C-001 | Status updates; transition logged. | Med | Positive | REV-02 | Not Run | |
| UAT-O2C-006 | Generate AR invoice (INV-) from completed order | ArClerk | Order Shipped/Completed, no invoice yet | 1. `POST /api/finance/ar/sync`. 2. List AR. | — | Invoice `INV-<order_no>` created, status Unpaid. | High | Positive | REV-04 | Not Run | finance.service |
| UAT-O2C-007 | Record AR receipt (RCP-) | ArClerk | Unpaid invoice exists | 1. `POST /api/finance/ar/receipts`. | `{invoice_no: INV-..., amount: <full>, method: Cash}` | 201; `receipt_no` `RCP-...`; invoice paid_amount updated, status Paid. | High | Positive | REV-05 | Not Run | finance.service |
| UAT-O2C-008 | Customer-portal sale applies VAT 7% + loyalty | Customer | Loyalty enabled (1pt/baht), item A=50 | 1. `POST /api/portal/pos/sales` qty 2. | `{items:[{item_id:A, qty:2, unit_price:50}]}` | 201; `total`=107, `vat`=7; `payment_no` PAY-, `journal_no` JE-; loyalty points accrued. | High | Positive | REV-03, GL-01 | Not Run | worldclass.ts |
| UAT-O2C-009 | Capture tender / payment | Cashier | Sale exists | 1. `POST /api/payments`. | `{sale_no, method: Cash, amount: 100, currency: THB}` | 201; `payment_no` PAY-; status `Captured`. | High | Positive | REV-06 | Not Run | worldclass.ts |
| UAT-O2C-009a | Card tender makes a real PSP charge | Cashier | `OPN_SECRET_KEY` set; card token from terminal | 1. `POST /api/payments` with `gateway:opn`, card `token`. | `{sale_no, method:Card, amount:107.5, gateway:opn, token}` | Real Omise charge (amount in satang, secret-key auth); status `Captured`; `gateway_ref` is the PSP charge id. | High | Positive | REV-03 | Not Run | payments-gateway.ts |
| UAT-O2C-009b | No-token / declined card never books funds | Cashier | `OPN_SECRET_KEY` set | 1. Tender `gateway:opn` with **no** token. 2. Tender with a token the PSP declines. | as left | (1) status `Pending`, no PSP call; (2) status `Failed` with an `error`; a **durable `Failed`** tender row persists (gateway_ref carries `declined:…`); **no `Captured`** for the sale (money not booked). | High | Control | REV-03 | Not Run | payments-gateway.ts |
| UAT-O2C-009c | Card tender idempotency = one charge | Cashier | `OPN_SECRET_KEY` set | 1. `POST /api/payments` twice with the same `idempotency_key`. | as left | Same `payment_no`; 2nd `replayed:true`; exactly one PSP charge. | High | Control | REV-02 | Not Run | payments-gateway.ts |
| UAT-O2C-010 | Full refund flips payment to Refunded | ReturnsClerk | Captured payment | 1. `POST /api/payments/refunds` full amount. | `{payment_no, amount: 100, reason: test}` | 201; `refund_no` REF-; `fully_refunded: true`. | High | Positive | REV-09 | Not Run | worldclass.ts |
| UAT-O2C-011 | Over-refund rejected | ReturnsClerk | Payment 100, already refunded 60 | 1. `POST /api/payments/refunds` amount 50 (60+50>100). | `{payment_no, amount: 50}` | 400 (over-refund rejected). | High | Control | REV-09 | Not Run | worldclass.ts |
| UAT-O2C-012 | Item-level POS return → restock + GL reversal | ReturnsClerk | Portal sale 2×A (total 214, VAT 14), stock 8 | 1. `POST /api/pos/returns` full qty. 2. Check stock + GL. | `{sale_no, items:[{item_id:A, qty:2}], reason: ชำรุด}` | RTN-/REF- issued; `total_returned`=214; restocked to 10; JE Dr4000=200, Dr2100=14, Cr1000=214. | High | Control | REV-09, GL-01 | Not Run | returns.ts |
| UAT-O2C-013 | Partial return computes pro-rata VAT | ReturnsClerk | Sale 2×A | 1. `POST /api/pos/returns` qty 1. | `{sale_no, items:[{item_id:A, qty:1}]}` | `total_returned`=107, subtotal 100, vat 7; stock +1; payment stays `Captured`. | Med | Positive | REV-09 | Not Run | returns.ts |
| UAT-O2C-014 | Over-return blocked | ReturnsClerk | Line qty 2 | 1. `POST /api/pos/returns` qty 3. | `{items:[{item_id:A, qty:3}]}` | 400 `OVER_RETURN`. | High | Control | REV-09 | Not Run | returns.ts |
| UAT-O2C-015 | Return on comp sale (no captured payment) blocked | ReturnsClerk | Sale with no captured payment | 1. `POST /api/pos/returns`. | `{sale_no: COMP-1, items:[{item_id:A, qty:1}]}` | 400 `NO_CAPTURED_PAYMENT`. | Med | Control | REV-09 | Not Run | returns.ts |
| UAT-O2C-016 | Till open → sale links → close Z-report variance | PosSupervisor | — | 1. `POST /api/payments/till/open` float 100. 2. Portal cash sale 53.5. 3. Full refund of that sale. 4. `POST /api/payments/till/close` count 100. | open float=100; sale 1×B@50 | Close: `expected_cash`=100, `variance`=0 (refund nets to 0). | High | Control | REV-11, REC-02 | Not Run | worldclass.ts |
| UAT-O2C-017 | Blind drawer close (variance computed server-side) | Cashier | Till open float 1000 | 1. `POST /api/payments/till/close` closing_count 1000. | `{closing_count: 1000}` | 200; `expected_cash`=1000, `variance`=0; counted−expected. | Med | Control | REV-11 | Not Run | pos-p1.ts |
| UAT-O2C-018 | PromptPay async settlement | Cashier | — | 1. `POST /api/payments` QR/promptpay. 2. `PATCH /api/payments/{no}/settle`. | `{method: QR, amount: 50, gateway: promptpay}` | Initial status `Pending`; after settle `Captured`. | Med | Positive | REV-06 | Not Run | worldclass.ts |
| UAT-O2C-019 | RLS — staff cannot return another tenant's sale | Sales (T2) | T1 sale exists | 1. `POST /api/pos/returns` against T1 sale as T2 user. | bearer sales2 | 404 NOT_FOUND. | High | Control | ITGC-AC (RLS) | Not Run | returns.ts |
| UAT-O2C-020 | POS home (store overview) loads today's KPIs | Sales | Sales seeded today | 1. Open `/pos-home`. 2. `GET /api/pos/summary?start_date=<today>&end_date=<today>`. | bearer sales | 200; shows today's `total_sales`, `total_orders`, `avg_order_value`, top items, by-payment, open tills, recent bills. | Med | Positive | Feature (POS dashboard) | Not Run | pos.controller.ts |
| UAT-O2C-021 | Cashier can view own shift KPIs (read-only) | Cashier | Cashier holds `pos_sell` only | 1. As Cashier, `GET /api/pos/summary` + `/api/pos/sessions` + `/api/pos/orders`. | bearer cashier | 200 on all three (read-only shift KPIs now visible to `pos_sell`/`pos_till`). | Med | Control | RBAC (least-privilege read) | Not Run | pos.controller.ts |
| UAT-O2C-022 | Workspace switcher filters the menu (ERP ↔ POS) | Sales/Admin | Logged in to back office | 1. Toggle **POS** → sidebar shows POS groups + `/pos-home`. 2. Toggle **ERP** → shows ERP groups + `/dashboard`. 3. ⌘K search. | UI | Menu filters per workspace; switching navigates to that workspace's home; ⌘K still searches all permitted screens. | Med | Positive | Feature (ERP/POS workspaces) | Not Run | app-shell.tsx; automated: apps/web/e2e/workspace-split.spec.ts |
| UAT-O2C-023 | Pricing rules apply at dine-in checkout (B4) | Sales | Item A=100 + active item rule "20% off" | 1. Create dine-in order 1×A. 2. `POST /api/restaurant/orders/:no/checkout` with `apply_pricing_rules:true`. | `{apply_pricing_rules:true}` | `subtotal`=80, `total`=85.60; `applied_rules` lists the rule; GL balanced, Cr4000=80. | High | Positive | REV-01, GL-01 | Not Run | pricing.ts |
| UAT-O2C-024 | Service charge (4400) + satang rounding (4900) post & balance | Sales | Item D=200, order rule 5%, party 6 | 1. Checkout 1×D `apply_pricing_rules:true, party_size:6, service_charge_pct:10, service_min_party:6, rounding:1`. | as left | `service_charge`=19, `total`=224, `rounding_adjustment`=0.37; GL Cr4400=19, Cr4900=0.37, balanced. | High | Control | GL-01 | Not Run | pricing.ts |
| UAT-O2C-025 | Pricing rules NOT applied unless opted in (backward-compat) | Sales | Item A=100 + active "20% off" rule | 1. Checkout 1×A with no `apply_pricing_rules`. | `{}` | Full price: `subtotal`=100, `total`=107, `applied_rules` empty. | Med | Control | REV-01 | Not Run | pricing.ts |
| UAT-O2C-250 | New price/promo rule is STAGED (PendingApproval) and does NOT apply | PricingManager (author A, `pricelist`) | Item A=100, no active rule on A | 1. `POST /api/pricing/rules {type:"percent", scope:"item", target:A, value:20, ...}`. 2. `POST /api/pricing/quote` for 1×A (or a checkout with `apply_pricing_rules`). | as left | Create returns `{id, status:"PendingApproval", pending:true}` and the rule is `active=false`; the quote/checkout charges **full price** (`total`=107 with VAT / `applied_rules` empty) — the discount engine reads only `active=true`, so a staged rule affects no sale (maker-checker G6, migration 0262). | High | Control | REV-01, R10 | Not Run | pricing.ts |
| UAT-O2C-251 | Rule author cannot self-approve (SoD) | PricingManager (author A) | The rule from UAT-O2C-250 is PendingApproval, created by user **A** | 1. User **A** `POST /api/pricing/rules/:id/approve`. | — | `403 SOD_VIOLATION` (`createdBy === approver`); the rule stays `PendingApproval`, `active=false`; still no discount applies. | High | Control | R10 | Not Run | pricing.ts |
| UAT-O2C-252 | A distinct approver activates the rule → the discount then applies | Approver B (`exec`/`approvals`) ≠ author | The same pending rule; approver **B** ≠ A | 1. Approver **B** `POST /api/pricing/rules/:id/approve`. 2. `POST /api/pricing/quote` for 1×A (or checkout with `apply_pricing_rules`). | — | Approve sets `active=true`, `status="Active"` (`approved_by`/`approved_at` stamped); **only now** the 20% discount applies (`subtotal`=80). A reject path (`…/rules/:id/reject`) and the pending queue (`GET …/rules/pending`) also exist. NB combo prices (`PUT …/combos/:sku`) are not yet staged (follow-up). | High | Control | REV-01, R10 | Not Run | pricing.ts |
| UAT-O2C-026 | Cashier-speed quick-tender & change (UI) | Cashier | On `/pos/new`, total ฿85.60 | 1. Tap **฿100**. 2. Read change. 3. Press **F2**/**F9**. | UI | Cash received=100, **change**=฿14.40; F2 adds a line; F9 confirms. | Low | Positive | Feature (cashier speed) | Not Run | apps/web .../pos/new/page.tsx |
| UAT-O2C-027 | AR receipt idempotency (W3) | ArClerk | open invoice | 1. `POST /api/finance/ar/receipts {amount, idempotency_key:k}`. 2. Retry same key. | as left | Same `receipt_no`; 2nd `idempotent:true`; cash collected once (`paid_amount` applied once); exactly 1 RCP GL entry. | High | Control | REC-01 / GL-01 | Not Run | match.ts |
| UAT-O2C-028 | Diner pulls the menu via QR session token | Diner (public) | Table open; item `QR01` available | 1. `GET /api/qr/t/{token}/menu`. | public token | 200; menu lists `QR01` with `is_available:true` and its modifier groups. | High | Positive | REST-08 | Not Run | restaurant.ts |
| UAT-O2C-029 | Diner self-order auto-fires to KDS | Diner (public) | As above | 1. `POST /api/qr/t/{token}/order`. 2. `GET /api/restaurant/kds/feed`. | `{items:[{sku:QR01, qty:2}]}` | 201; returned order item `kds_status:queued`; the item appears on the KDS feed under its station. | High | Positive | REST-08 | Not Run | restaurant.ts |
| UAT-O2C-030 | Second submit appends to the same open order | Diner (public) | One self-order already placed | 1. `POST /api/qr/t/{token}/order` again. | `{items:[{sku:QR01, qty:1}]}` | 201; same `order_no`; order now has 2 lines. | Med | Positive | REST-08 | Not Run | restaurant.ts |
| UAT-O2C-031 | Diner cannot self-order a freeform/priced line | Diner (public) | Table open | 1. `POST /api/qr/t/{token}/order` with a freeform line. | `{items:[{name:"ของแถมฟรี", unit_price:0, qty:1}]}` | 400; rejected — only menu items (`sku`/`menu_item_id`) may be self-ordered (no price tampering). | High | Control | REST-08 | Not Run | restaurant.ts |
| UAT-O2C-032 | Diner self-order of an 86'd item blocked | Diner (public) | `QR01` set unavailable | 1. `PATCH /api/menu/items/QR01/availability {available:false}`. 2. `POST /api/qr/t/{token}/order`. | `{items:[{sku:QR01, qty:1}]}` | 400 `ITEM_UNAVAILABLE`. | High | Control | REST-08 | Not Run | restaurant.ts |
| UAT-O2C-033 | Menu/order on an ended session rejected | Diner (public) | Session already closed | 1. `GET /api/qr/t/{closed-token}/menu`. | closed token | 401 `SESSION_ENDED`. | High | Control | REST-08 | Not Run | restaurant.ts |
| UAT-O2C-034 | Admin creates a buffet tier | MasterData | Menu item `BF01` exists | 1. `POST /api/restaurant/buffet/packages`. | `{code:STD, name:..., price_per_pax:299, time_limit_min:90, overtime_fee_per_pax:100, item_skus:[BF01]}` | 201; tier returned with `price_per_pax`=299 and `item_skus` incl. `BF01`. | High | Positive | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-035 | Diner starts buffet → per-pax charge + window | Diner (public) | Table open; tier `STD` (299/pax, 90 min) | 1. `GET /api/qr/t/{token}/buffet/tiers`. 2. `POST .../buffet/start`. | `{package_id, pax:4}` | `order_mode`=buffet; bill `subtotal`=1196 (299×4); `buffet.minutes_left`>0. | High | Positive | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-036 | Buffet food orders at ฿0 but hits KDS | Diner (public) | Buffet started | 1. `POST .../order` `{sku:BF01, qty:3}`. 2. `GET /api/restaurant/kds/feed`. | as left | Line `amount`=0, `is_buffet`=true, `kds_status`=queued; appears on KDS; bill still 1196; charge line NOT on KDS feed. | High | Positive/Control | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-037 | Off-tier buffet item rejected | Diner (public) | Buffet `STD` excludes `BF02` | 1. `POST .../order` `{sku:BF02, qty:1}`. | as left | 400 `NOT_IN_PACKAGE`. | High | Control | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-038 | One mode per session (no mixing) | Diner (public) | À la carte order already placed | 1. `POST .../buffet/start`. | `{package_id, pax:2}` | 400 `MODE_LOCKED`. | High | Control | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-039 | Ordering after the buffet window blocked | Diner (public) | Buffet window elapsed | 1. `POST .../order` `{sku:BF01, qty:1}`. | as left | 400 `BUFFET_EXPIRED`. | High | Control | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-040 | Overtime surcharge billed past the window | Diner (public) | Window elapsed; tier overtime 100/pax, pax 4 | 1. `POST .../bill`. 2. `GET /api/qr/t/{token}`. | as left | Overtime charge line `amount`=400; bill `subtotal`=1596 (1196+400). | High | Control | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-041 | KDS flags ticket origin (diner / buffet) | Kitchen | Diner + buffet orders on the feed | 1. `GET /api/restaurant/kds/feed`. | bearer sales | Diner-placed lines `from_diner`=true; buffet food `is_buffet`=true; staff lines `from_diner`=false; per-pax charge absent from feed. | Med | Positive | REST-08, REST-09 | Not Run | restaurant.ts |
| UAT-O2C-042 | Buffet behaviour analytics per tier | Manager | Buffet tier `STD`: 1 session, 4 pax, BF01×3, overtime once | 1. `GET /api/restaurant/buffet/analytics`. | bearer sales | Tier `STD`: `sessions`=1, `covers`=4, `top_items[0]`=หมูสไลด์ qty 3, `items_per_head`=0.75, `revenue`=1596, `avg_bill_per_session`=1596, `overtime_rate_pct`=100. | Med | Positive | Feature (buffet analytics) | Not Run | restaurant.ts |
| UAT-O2C-043 | Printed table-QR sticker | Sales | Table exists with `qr_token` | 1. `GET /api/restaurant/tables/{id}/qr?base=https://app.example`. | bearer sales | 200; `url`=`https://app.example/qr/start/{qr_token}`; `qr_image` is a `data:image…` QR. | Med | Positive | Feature (printed QR) | Not Run | restaurant.ts |
| UAT-O2C-044 | Scan printed QR opens/joins session | Diner (public) | Printed `qr_token` | 1. `POST /api/qr/start/{qr_token}`. 2. repeat. | qr_token | Returns a `public_token`; re-scan re-joins the same live session (idempotent). | High | Positive | REST-04 | Not Run | restaurant.ts |
| UAT-O2C-045 | PromptPay pay returns scannable QR (real mode) | Diner (public) | Webhook secret configured | 1. `POST /api/qr/t/{token}/pay`. | — | `qr_image` is a `data:image…` QR; `mock_settle`=false; tender `Pending`. | High | Positive | REST-04 | Not Run | restaurant.ts |
| UAT-O2C-046 | PromptPay settlement webhook (auth + finalize) | PSP | Pending tender exists | 1. `POST /api/qr/webhook/promptpay` wrong secret. 2. correct secret. | `{payment_no}` + `x-webhook-secret` | Wrong/missing → 401 `BAD_WEBHOOK_SIG`; correct → settles, builds `SALE-`/invoice, `paid:true`; session closed. | High | Control | REST-04 | Not Run | restaurant.ts |
| UAT-O2C-047 | Webhook idempotent + payment-status poll | Diner/PSP | Already settled | 1. `GET /api/qr/t/{token}/payment-status`. 2. re-deliver webhook. | — | `payment-status` `settled`=true (closed session tolerated); re-delivery is a no-op (no double-post). | High | Control | REST-04 | Not Run | restaurant.ts |
| UAT-O2C-048 | Staff starts buffet from the POS | Sales | Tier `STD`; available table | 1. `POST /api/restaurant/tables/{id}/buffet` `{package_id, pax:2}`. | as left | Session opens in buffet mode; per-pax charge (299×2) order is open on the table. | Med | Positive | REST-09 | Not Run | restaurant.ts |
| UAT-O2C-049 | Public diner endpoint rate-limited | Diner (public) | Live session | 1. `POST /api/qr/t/{token}/order` >15×/min. | menu items | After the burst cap, further calls return 429 `RATE_LIMITED`. | Med | Control | Anti-abuse | Not Run | restaurant.ts |
| UAT-O2C-050 | Diner self-order UI smoke (Playwright) | Diner (public) | Stubbed public QR API | 1. Load `/qr/{token}`. 2. open เมนู, tap item. 3. open cart, ส่งออเดอร์เข้าครัว. | UI (mocked) | Item adds to cart; submit posts the order; dish shows on the status tab as รอคิว. | Low | Positive | Feature (diner UI) | Not Run | apps/web/e2e/qr-self-order.spec.ts |
| UAT-O2C-051 | Move a live tab to a free table | Sales | Table A10 open with an order; A11 free | 1. `POST /api/restaurant/tables/{A10}/move` `{to_table_id:A11}`. 2. `GET /api/restaurant/tables/status`. | as left | A11 `occupied` carrying the order; A10 `available`; session unchanged. | Med | Positive | Feature (table ops) | Not Run | restaurant.ts |
| UAT-O2C-052 | Move onto an occupied table blocked | Sales | A11 occupied; A12 occupied | 1. `POST /api/restaurant/tables/{A11}/move` `{to_table_id:A12}`. | as left | 400 `TABLE_BUSY` (use merge). | Med | Control | Feature (table ops) | Not Run | restaurant.ts |
| UAT-O2C-053 | Transfer line items between tables | Sales | A13 order (สเต๊ก 200 + สลัด 80); A14 seated | 1. `POST /api/restaurant/orders/{A13ord}/transfer-items` `{item_ids:[steak], to_table_id:A14}`. | as left | `moved`=1; A13 keeps สลัด (subtotal 80); A14 order carries สเต๊ก (total 214). | Med | Positive | Feature (table ops) | Not Run | restaurant.ts |
| UAT-O2C-054 | Merge two tabs into a combined bill | Sales | A15 (ข้าวผัด 100); A16 (น้ำเปล่า 50) | 1. `POST /api/restaurant/tables/{A15}/merge` `{from_table_id:A16}`. | as left | Both lines on A15's order (subtotal 150); A16 session closed + table `available`. | Med | Positive | Feature (table ops) | Not Run | restaurant.ts |
| UAT-O2C-095 | Reposition a table on the floor plan | Sales | Table `F1` at (0,0) | 1. `PATCH /api/restaurant/tables/{F1}` `{pos_x:240, pos_y:130}`. 2. `GET /api/restaurant/tables/status`. | as left | 200; table returns `pos_x`=240, `pos_y`=130; the status board reflects the new position. | Low | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-096 | Delete a free table (soft-delete) | Sales | Free table `F2` | 1. `DELETE /api/restaurant/tables/{F2}`. 2. `GET /api/restaurant/tables`. 3. `DELETE` again. | as left | `deleted:true`; `F2` drops off the list (past orders + fiscal trail preserved); deleting an already-removed table → 404. | Med | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-097 | Delete a seated table blocked | Sales | Table `F3` with a live session | 1. `POST /api/restaurant/tables/{F3}/open`. 2. `DELETE /api/restaurant/tables/{F3}`. | as left | 400 `TABLE_BUSY` — a seated table can't be deleted (clear/checkout first). | High | Control | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-098 | Create a VIP room (zone) | Sales | — | 1. `POST /api/restaurant/zones` `{name:"ห้อง VIP", color:"#caa53d", pos_x:40, pos_y:24, width:300, height:180}`. | as left | 201; room returned with the name, accent colour and geometry (width 300). | Med | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-099 | Move/resize/rename a room persists | Sales | Room exists | 1. `PATCH /api/restaurant/zones/{id}` `{pos_x:120, width:360, name:"ห้อง VIP 1"}`. 2. `GET /api/restaurant/zones`. | as left | 200; geometry + name persisted; the zone list reflects them. | Low | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-100 | Assign / un-assign a table to a room | Sales | Room + free table `V1` | 1. `PATCH /api/restaurant/tables/{V1}` `{zone_id:room}`. 2. `{zone_id:null}`. | as left | Table `zone_id` set to the room, then cleared to `null`. | Med | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-101 | Delete a room keeps its tables | Sales | Room with `V1` assigned | 1. `DELETE /api/restaurant/zones/{id}`. 2. `GET /api/restaurant/tables`. 3. `DELETE` again. | as left | `deleted:true`; room drops off the list; `V1` survives with `zone_id:null`; second delete → 404. | Med | Positive/Control | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-102 | Rooms are tenant-isolated | Sales (T2) | T1 created a room | 1. `GET /api/restaurant/zones` as T2. | bearer sales2 | T1's room is not visible (RLS). | High | Control | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-103 | Set table shape / size / rotation / seats | Sales | Table `S1` | 1. `PATCH /api/restaurant/tables/{S1}` `{shape:"circle", width:90, height:90, rotation:45, seats:8}`. 2. `GET /api/restaurant/tables/status`. | as left | 200; persisted; the status board returns `shape:circle`, `rotation:45`, height 90, seats 8. | Low | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-104 | Invalid shape / rotation rejected | Sales | Table `S1` | 1. `PATCH …{shape:"triangle"}`. 2. `PATCH …{rotation:400}`. | as left | Both 400 — `shape` is a validated enum (round/rectangle/square); `rotation` must be 0–359. | Med | Control | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-105 | Optimistic-locked table update (rev) | Sales | New table `C9` (rev 0) | 1. `PATCH /tables/{C9}` `{pos_x:50, rev:0}`. 2. `PATCH` again `{pos_x:80, rev:0}` (now-stale rev). | as left | First applies + `rev`→1; the second is rejected `409 STALE_WRITE` (someone else changed it). | Med | Control | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-106 | Unconditional update / undo (no rev) | Sales | Table `C9` | 1. `PATCH /tables/{C9}` `{pos_x:80}` (no `rev`). | as left | Applies last-write-wins (the path the editor's **undo** uses); `rev` still bumps. | Low | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-107 | Create with full initial appearance | Sales | — | 1. `POST /api/restaurant/tables` `{shape:"square", rotation:90, width:70, height:70, seats:6}`. | as left | 201; table persisted with the given shape/rotation/size/seats (the editor's **ทำซ้ำ / duplicate** copies these). | Low | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-108 | Floor-plan editor UI smoke (Playwright) | Sales | Stubbed restaurant API | 1. Load `/tables`. 2. open **ผังร้าน** → **แก้ไขผัง**. 3. add a table + a room. | UI (mocked) | The seeded table renders; **แก้ไขผัง** reveals the room toolbar; **เพิ่มโต๊ะ** / **เพิ่มห้อง** POST to the API. | Low | Positive | Feature (floor-plan UI) | Not Run | apps/web/e2e/floor-plan.spec.ts |
| UAT-O2C-109 | Revenue attributed by room | Sales | A room with a settled dine-in sale (฿300 + VAT) | 1. `GET /api/restaurant/zones/revenue` (defaults to today). | bearer sales | The room shows `sales`=1, `revenue`= the sale's gross total, `avg_sale`=revenue; the grand total reconciles rooms + unzoned. | Med | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-110 | Room revenue is tenant-isolated | Sales (T2) | T1 has room revenue today | 1. `GET /api/restaurant/zones/revenue` as T2. | bearer sales2 | T1's room is not present in T2's report (RLS). | High | Control | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-111 | Revenue snapshot survives a table move | Sales | A room sale settled; table then moved to another room | 1. `PATCH /tables/{id}` `{zone_id: other}`. 2. `GET /api/restaurant/zones/revenue`. | as left | The sale stays under the **original** room; the new room shows 0 (room snapshotted at checkout). | Med | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-112 | Deleted room keeps its past takings | Sales | A room with revenue, then deleted | 1. `DELETE /api/restaurant/zones/{id}`. 2. `GET /api/restaurant/zones/revenue`. | as left | The room still appears (flagged **inactive**) with its revenue; the grand total still reconciles rooms + unzoned. | Med | Positive | Feature (floor-plan layout) | Not Run | restaurant.ts |
| UAT-O2C-113 | Service charge persisted + itemised on the receipt | Sales | Dine-in checkout, party 6, `service_charge_pct:10`, `service_min_party:6`, `apply_pricing_rules:true` | 1. Checkout. 2. `GET /api/print/receipt/{saleNo}/data`. 3. `GET /api/print/receipt/{saleNo}`. | as left | `cust_pos_sales.service_charge` > 0; receipt data carries `service_charge`; HTML/ESC-POS slip shows a **ค่าบริการ** line. | High | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-114 | Large-party receipt ties out incl. service charge | Sales | The UAT-O2C-113 sale | 1. `GET /api/print/tie-out/{saleNo}`. | bearer sales | `matched:true`; reconciles Σ line − discount + **service charge** + VAT + tip = total (service-charge leg included). | High | Control | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-115 | Send receipt via LINE channel | Sales | Settled sale; back-office Receipts screen | 1. Pick channel **LINE**, enter a LINE User ID. 2. `POST /api/print/receipt/{saleNo}/send` `{channel:line, to:...}`. | as left | 201; delivery `status:sent`; `message_log` row with `channel:line` and the provider (`line` when token set, else `mock`). | Med | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-116 | Enrol/link member via LINE (idempotent) | Sales | LINE login (dev `mock:<userId>` token) | 1. `POST /api/loyalty/members/enroll-line {id_token}`. 2. repeat. 3. `POST /api/loyalty/members/:id/link-line` on a phone member. | as left | (1) member created with `line_user_id`; (2) same member (`created:false`); (3) phone member gains the LINE id. | High | Positive | Feature (LINE CRM) | Not Run | line-crm.ts |
| UAT-O2C-117 | One LINE account = one member | Sales | A LINE id already linked to member A | 1. `POST /api/loyalty/members/:B/link-line` with A's LINE id. 2. `GET /api/loyalty/members/lookup?line_user_id=`. | as left | (1) 409 `LINE_ALREADY_LINKED`; (2) lookup resolves the original member. | High | Control | Feature (LINE CRM) | Not Run | line-crm.ts |
| UAT-O2C-118 | LINE push addresses the LINE userId, consent enforced | Sales | Member with `line_user_id`; one without; one opted-out | 1. `POST /api/messaging/send {member_id, channel:line}`. | as left | Linked member → `sent`, pushed to the **`line_user_id`** (Bearer token, not phone); unlinked → `failed` (no recipient); opted-out → `skipped`. | High | Control | Feature (LINE CRM), MKT-04 | Not Run | line-crm.ts |
| UAT-O2C-119 | Aggregator menu push (real adapter) | order_mgt | `CHANNEL_API_URL_GRAB` set; grab adapter store GRAB-001 | 1. `POST /api/channels/grab/menu-sync`. | as left | `pushed:true`, `provider:grab`; partner receives a POST to `…/menu` with **Bearer token** + the available items + `store_ref`. | High | Positive | Feature (aggregator adapter) | Not Run | channel-adapter.ts |
| UAT-O2C-120 | Accept/reject routes to KDS + notifies platform | order_mgt | A received order (auto_accept off) | 1. `POST /api/channels/orders/:no/accept`. 2. on a 2nd order, `…/reject {reason}`. | as left | Accept → lines move `new`→`queued` on the KDS, order `sent_to_kitchen`, platform `/accept` called (`post_ok`); reject → order `cancelled`, platform `/reject` called. | High | Positive | Feature (aggregator adapter) | Not Run | channel-adapter.ts |
| UAT-O2C-121 | Status callback + mock fallback | order_mgt | configured grab; unconfigured lineman | 1. `POST /api/channels/orders/:no/status {status}`. 2. `POST /api/channels/lineman/menu-sync`. | as left | (1) partner `/status` POSTed, `post_ok:true`; (2) unconfigured platform → `provider:mock`, **no** partner HTTP call. | Med | Control | Feature (aggregator adapter) | Not Run | channel-adapter.ts |
| UAT-O2C-122 | Multi-terminal realtime KDS event | pos/order_mgt (2 tenants) | A fired KDS item for T1 | 1. `PATCH /api/restaurant/kds/items/:id {action:start}`. 2. `GET /api/pos/scale/events/recent` as T1. 3. as T2. | as left | A `kds_item` event (item_id, `kds_status:preparing`, order_id) appears on **T1**'s feed; a 2nd transition pushes another; **T2 never sees T1's events** (tenant-scoped). Web KDS/tables consume the SSE stream live. | High | Control | Feature (multi-terminal SSE) | Not Run | realtime-kds.ts |
| UAT-O2C-123 | LINE marketing automation — closed loop | marketing | Members: lapsed+LINE / active / opted-out / no-LINE / at-risk | 1. `POST /api/marketing/automation/campaigns {trigger:lapsed, discount 100}`. 2. redeem the pushed coupon. 3. again. 4. `GET …/campaigns/:id`. | as left | 3 targeted → **1 sent** (opted-in LINE), **1 skipped** (opt-out), **1 failed** (no LINE); coupon pushed to the LINE userId; redeem → value 100 attributed; **re-redeem idempotent** (already_redeemed); report 100% redemption, ฿100 attributed; unknown coupon → 404. | High | Control | Feature (LINE automation), MKT-04 | Not Run | line-automation.ts |
| UAT-O2C-055 | Fire one course (hold the rest) | Sales | Order with course 1 + course 2 items | 1. `POST /api/restaurant/orders/{no}/fire?course=1`. 2. `GET /api/restaurant/kds/feed`. | as left | Course-1 lines `queued` on the feed (tagged course 1); course-2 lines stay `new` and off the feed. | Med | Positive | Feature (course firing) | Not Run | restaurant.ts |
| UAT-O2C-056 | Fire the next course / empty course | Sales | Above, course 1 fired | 1. `fire?course=2`. 2. `fire?course=5`. | as left | Course 2 queues; firing an empty course → 400 `NO_COURSE_ITEMS`. | Med | Control | Feature (course firing) | Not Run | restaurant.ts |
| UAT-O2C-057 | Day-parting: menu flags availability | MasterData/Diner | Items with time-window / day-mask / no schedule | 1. Create items w/ windows. 2. `GET /api/menu`. | Bangkok now | `available_now`=false for out-of-window + excluded-day items; true for always-available. | Med | Positive | Feature (day-parting) | Not Run | restaurant.ts |
| UAT-O2C-058 | Day-parting: order outside window blocked | Diner (public) | Item out of its time window / day | 1. `POST /api/qr/t/{token}/order` an out-of-window item. | as left | 400 `OUTSIDE_HOURS`; always-available item still orders. | Med | Control | Feature (day-parting) | Not Run | restaurant.ts |
| UAT-O2C-059 | CRM: enrol member w/ birthday + consent; messaging | Sales | — | 1. `POST /api/loyalty/members {birthday, marketing_opt_in:true}`. 2. `POST /api/messaging/send {member_id, channel:sms}`. | as left | Member created; send logged `sent` via `mock` provider. | Med | Positive | MKT-04 | Not Run | restaurant.ts |
| UAT-O2C-060 | CRM: consent enforced on messaging | Sales | Opted-out member | 1. `POST /api/messaging/send` to opted-out member. 2. `POST /api/messaging/blast {audience:birthdays_today}`. | as left | Opted-out send → `skipped`; birthday blast sends to opted-in only, skips opted-out; all in `/api/messaging/log`. | High | Control | MKT-04 | Not Run | restaurant.ts |
| UAT-O2C-061 | Food-cost: per-menu margin from recipe | Sales | Item FC01 price 100, recipe cost 30 | 1. `GET /api/menu/food-cost`. | bearer sales | FC01: cost 30, margin 70, margin_pct 70, food_cost_pct 30, has_recipe; summary reports avg food-cost % + costed count. | Med | Positive | Feature (food-cost) | Not Run | restaurant.ts |
| UAT-O2C-062 | Food-cost: ingredient cost contribution | Sales | Recipe with NOODLE @ 30/serving | 1. `GET /api/menu/ingredient-cost`. | bearer sales | NOODLE listed with cost 30/serving and recipes_using ≥ 1, sorted by cost. | Low | Positive | Feature (food-cost) | Not Run | restaurant.ts |
| UAT-O2C-063 | Checkout auto-queues a receipt print job | Sales | A dine-in sale just settled | 1. `GET /api/print/jobs`. | bearer sales | A `receipt` job for the sale exists with `status:queued`. | Med | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-064 | Receipt ties out to the fiscal sale | Sales | Settled sale (total 181.90, VAT 11.90) | 1. `GET /api/print/tie-out/{saleNo}`. | bearer sales | `matched:true`; total reconciles (Σ line − discount + service charge + VAT + tip = total). | High | Control | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-065 | Receipt renders seller header + VAT + items | Sales | VAT-registered seller | 1. `GET /api/print/receipt/{saleNo}/data`. | bearer sales | `seller.vat_registered:true`; 2 item lines; VAT line = 11.90. | Med | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-066 | HTML receipt document served | Sales | Settled sale | 1. `GET /api/print/receipt/{saleNo}`. | bearer sales | 200 `text/html`; body contains the sale number and “ใบเสร็จ”. | Low | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-067 | Agent pulls + acks the next job | Agent (pos) | A queued receipt job | 1. `GET /api/print/jobs/next`. 2. `POST /api/print/jobs/{id}/ack` `{ok:true}`. | bearer sales | Pull returns the job (`format:escpos`, payload present, → `sent`); ack → `printed`. | Med | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-068 | Reprint is flagged a COPY (สำเนา) | Sales | Original receipt already issued | 1. `POST /api/print/reprint/{saleNo}`. 2. `GET /api/print/receipt/{saleNo}/data`. | bearer sales | New job enqueued; re-render `is_copy:true`. | Med | Control | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-069 | Send receipt out-of-band (email) | Sales | Settled sale | 1. `POST /api/print/receipt/{saleNo}/send` `{channel:email, to:...}`. | as left | 201; delivery `status:sent` via the messaging gateway. | Low | Positive | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-070 | Print jobs are tenant-isolated | Sales (T2) | T1 has queued jobs | 1. `GET /api/print/jobs` as T2. | bearer sales2 | None of T1's jobs are visible (RLS). | High | Control | REST-10 | Not Run | restaurant.ts |
| UAT-O2C-071 | Register a cash-drawer device | Sales | — | 1. `POST /api/peripherals/devices` `{device_code:DRW1, kind:cash_drawer, terminal:T01, printer_id:PRN1}`. | as left | 201; device created; listed in `GET /devices`. | Med | Positive | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-072 | Cash checkout auto-pops the drawer | Sales | Cash sale just settled | 1. `GET /api/peripherals/drawer/events`. | bearer sales | A `sale` open exists for the sale (auto-logged at checkout). | High | Control | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-073 | No-sale open kicks + audits the drawer | Sales | Drawer device registered | 1. `POST /api/peripherals/drawer/kick` `{terminal:T01, reason:no_sale}`. 2. `GET /api/print/jobs`. | as left | `kicked:true` with a `drawer` (escpos) print job; audit row reason `no_sale`. | High | Control | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-074 | Drawer reconciliation by reason | Manager | ≥1 sale + ≥1 no-sale open | 1. `GET /api/peripherals/drawer/reconciliation`. | bearer sales | `total_opens`≥2; `no_sale_opens`≥1; breakdown by reason. | High | Control | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-075 | Set + poll customer display | Sales | Terminal T01 | 1. `POST /api/peripherals/display/T01` (total/change/lines). 2. `GET /api/peripherals/display/T01`. | as left | Polled state echoes total, change and line items. | Low | Positive | Feature (display) | Not Run | restaurant.ts |
| UAT-O2C-076 | Mark an item sold-by-weight | MasterData | Item WGH1 (price 180/kg) | 1. `PATCH /api/peripherals/scale/items/WGH1` `{sold_by_weight:true, weight_unit:kg}`. | as left | `sold_by_weight:true`, `weight_unit:kg`. | Med | Positive | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-077 | Scale computes price server-side | Sales | WGH1 weighed @ 180/kg | 1. `POST /api/peripherals/scale/read` `{sku:WGH1, gross_weight:1.25, tare_weight:0.05}`. | as left | net 1.2 kg; amount 216; ready line `unit_price`=216 (priced from catalog). | High | Control | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-078 | Scale read on a non-weighed item rejected | Sales | AL01 not sold-by-weight | 1. `POST /api/peripherals/scale/read` `{sku:AL01, gross_weight:1}`. | as left | 400 `NOT_WEIGHED`. | Med | Control | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-079 | Peripheral registry is tenant-isolated | Sales (T2) | T1 registered DRW1 | 1. `GET /api/peripherals/devices` as T2. | bearer sales2 | DRW1 not visible (RLS). | High | Control | REST-11 | Not Run | restaurant.ts |
| UAT-O2C-080 | Take a customer deposit | Sales | — | 1. `POST /api/payments/deposits` `{amount:500}`. | as left | 201; `DEP-…`; balanced JE (Dr 1000 / Cr 2210). | Med | Positive | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-081 | Apply a deposit (recognise revenue) | Sales | Deposit 500 open | 1. `POST /deposits/{no}/apply` `{amount:200}`. | as left | remaining 300; JE Dr 2210 / Cr 4000+2100. | Med | Positive | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-082 | Deposit over-apply rejected | Sales | 300 remaining | 1. `POST /deposits/{no}/apply` `{amount:400}`. | as left | 400 `OVER_APPLY`. | High | Control | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-083 | Refund the unused deposit | Sales | 300 remaining | 1. `POST /deposits/{no}/refund` `{}`. | as left | refunded 300; status `closed`; JE Dr 2210 / Cr 1000. | Med | Positive | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-084 | Open a house account + charge within limit | Manager/Sales | — | 1. `POST /house-accounts` `{credit_limit:1000}`. 2. `POST /:no/charge` `{amount:600}`. | as left | `HA-…`; balance 600; JE Dr 1100 / Cr 4000+2100. | Med | Positive | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-085 | Charge over the credit limit rejected | Sales | balance 600, limit 1000 | 1. `POST /:no/charge` `{amount:600}`. | as left | 400 `CREDIT_LIMIT_EXCEEDED`. | High | Control | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-086 | Settle a house account (THB) | Sales | balance 600 | 1. `POST /:no/settle` `{amount:600}`. | as left | balance 0; fx 0; JE Dr 1000 / Cr 1100. | Med | Positive | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-087 | FX settlement books realised FX gain | Sales | balance 340 | 1. `POST /:no/settle` `{amount:340, currency:USD, fx_rate:34, foreign_tendered:10.5}`. | as left | received 357; FX gain +17 (→5410); balance 0. | High | Control | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-088 | Statement reconciles + over-settle rejected | Sales | settled account | 1. `GET /:no/statement`. 2. `POST /:no/settle` `{amount:100}`. | as left | statement balance 0, 4 entries, available_credit 1000; over-settle → 400 `OVER_SETTLE`. | High | Control | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-089 | Card surcharge quote + charge | Sales | surcharge Card 3% | 1. `GET /surcharges/quote?method=Card&amount=1000`. 2. `POST /surcharges/charge`. | as left | quote 30 / total 1030; charge posts Dr 1000 / Cr 4500+2100 (net 28.04, VAT 1.96). | Med | Positive | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-090 | Payments-depth tenant isolation | Sales (T2) | T1 has deposits + accounts | 1. `GET /house-accounts` + `GET /deposits` as T2. | bearer sales2 | None of T1's visible (RLS). | High | Control | REST-12 | Not Run | restaurant.ts |
| UAT-O2C-091 | Receipt defaults to tenant language | Sales | Settled sale; tenant default TH | 1. `GET /api/print/receipt/{saleNo}/data`. | bearer sales | `data.lang` = `th`. | Low | Positive | Feature (i18n) | Not Run | restaurant.ts |
| UAT-O2C-092 | Receipt renders in English | Sales | Settled sale | 1. `GET /api/print/receipt/{saleNo}?lang=en`. | bearer sales | HTML shows Subtotal/Total/Thank you; no Thai total label. | Med | Positive | Feature (i18n) | Not Run | restaurant.ts |
| UAT-O2C-093 | Bilingual (TH/EN) receipt | Sales | Settled sale | 1. `GET /api/print/receipt/{saleNo}?lang=both`. | bearer sales | Labels render as "TH / EN" (e.g. `รวมสุทธิ / Total`). | Low | Positive | Feature (i18n) | Not Run | restaurant.ts |
| UAT-O2C-094 | Per-tenant default language persists | Admin | — | 1. `PATCH /api/tenant/profile {default_language:en}`. 2. `GET /api/tenant/profile`. | bearer admin | Saved + returned `default_language` = `en`. | Med | Positive | Feature (i18n) | Not Run | restaurant.ts |
| UAT-O2C-130 | Collections worklist lists open overdue AR, excludes paid | ArClerk | INV-A (40d overdue), INV-B (100d), INV-C (paid) | 1. `GET /api/finance/ar/collections`. | — | INV-A and INV-B listed (oldest first: INV-B top); INV-C (Paid) excluded. | High | Positive | REV-12 | Not Run | basics.ts |
| UAT-O2C-131 | Dunning stage recommended by aging | ArClerk | As O2C-116 | 1. Read worklist rows. | — | INV-A (40d) → `recommended_stage: second_notice`; INV-B (100d) → `legal`. | High | Positive | REV-12 | Not Run | basics.ts |
| UAT-O2C-132 | Record a dunning action advances the stage | ArClerk | INV-A overdue | 1. `POST /api/finance/ar/collections/INV-A/dunning {stage:second_notice, channel:email}`. 2. Re-read worklist + history. | as left | `DUN-…` issued; INV-A `current_stage:second_notice`, `escalate:false`; history count = 1. | High | Control | REV-12 | Not Run | basics.ts |
| UAT-O2C-133 | Dunning on a paid invoice rejected | ArClerk | INV-C is Paid | 1. `POST /api/finance/ar/collections/INV-C/dunning {stage:reminder}`. | as left | 400 `ALREADY_PAID`. | Med | Control | REV-12 | Not Run | basics.ts |
| UAT-O2C-134 | Credit status flags over-limit + serious overdue | ArClerk | Customer limit 2500, exposure 3000, INV 100d overdue | 1. `GET /api/finance/ar/credit-status?tenant_id=<cust>`. | — | `exposure`≈3000; `over_limit:true`; `serious_overdue:true` (`max_overdue_days`≥100); `on_hold:true`. | High | Detective | REV-12, R09 | Not Run | basics.ts |
| UAT-O2C-135 | Credit check denies further credit to a held customer | Sales/ArClerk | Customer on_hold | 1. `POST /api/finance/ar/credit-check {tenant_id:<cust>, amount:100}`. | as left | `approved:false`; `reason` ∈ {`CREDIT_LIMIT_EXCEEDED`,`SERIOUS_OVERDUE`}. | High | Control | REV-12, R09 | Not Run | basics.ts |
| UAT-O2C-136 | Order entry blocks a 90+ day defaulter (within limit) | Sales | Customer limit 100k, one invoice 120d overdue | 1. `POST /api/pos/orders {customer_name:<cust2>, items:[…]}`. | small order | 409 `CREDIT_OVERDUE` — blocked even under limit (POS + portal share this gate). | High | Control | REV-12 | Not Run | basics.ts |
| UAT-O2C-137 | Order entry — over-limit still blocked (parity) | Sales | Customer over credit limit | 1. `POST /api/pos/orders {customer_name:<cust>, items:[…]}`. | small order | 409 `CREDIT_LIMIT` (existing REV-08 check unchanged). | High | Control | REV-08 | Not Run | basics.ts |
| UAT-O2C-138 | Order entry — good-standing customer can order | Sales | Customer under limit, only 20d overdue | 1. `POST /api/pos/orders {customer_name:<cust3>, items:[…]}`. | small order | 201; `order_no` `SO-…` (no over-block). | High | Positive | REV-12 | Not Run | basics.ts |
| UAT-O2C-139 | Automated dunning sweep advances overdue invoices | ArClerk (cron) | Overdue invoices past their current stage | 1. `POST /api/finance/ar/collections/sweep`. | — | `advanced` = count of escalated invoices; each gets a `DUN-` at its recommended stage (channel `auto`, system-actioned); invoices already at-stage skipped. | Med | Positive | REV-12 | Not Run | basics.ts |
| UAT-O2C-140 | Dunning sweep is idempotent | ArClerk (cron) | Sweep already run | 1. Re-run `POST /api/finance/ar/collections/sweep`. | — | `advanced` = 0 (no re-escalation until aging advances the recommended stage). | Med | Control | REV-12 | Not Run | basics.ts |
| UAT-O2C-141 | Schedule daily automated dunning | Controller | — | 1. `POST /api/bi/subscriptions {report_type:'ar_collections_dunning', frequency:'daily'}`. 2. `GET /api/bi/report-types`. | — | Subscription created with `frequency:daily` + `next_run_at`; `ar_collections_dunning` listed in the schedulable job catalog. | Med | Positive | REV-12 | Not Run | basics.ts |
| UAT-O2C-142 | Scheduler tick fires the dunning job | Controller (cron) | A due daily dunning subscription + overdue AR | 1. `POST /api/bi/subscriptions/run`. | — | The job runs: `status:success`, summary "advanced N of M overdue invoices", a run is recorded; the same sweep state is shared with the direct endpoint (idempotent). | Med | Control | REV-12 | Not Run | basics.ts |
| UAT-O2C-143 | Dunning action dispatches a notice to the customer | ArClerk | Customer with email on master | 1. `POST /api/finance/ar/collections/INV-A/dunning {stage:second_notice, channel:email}`. 2. `GET /api/messaging/log`. | — | Response `message_status:sent`, `recipient` = customer email; a `message_log` row exists with `campaign:dunning:second_notice`, channel email, per-stage body naming the invoice. | High | Positive | REV-12 | Not Run | basics.ts |
| UAT-O2C-144 | Sweep dispatches notices, channel auto-picked from contact | Controller (cron) | Customers with email and phone-only | 1. Run the dunning sweep/scheduled job. 2. `GET /api/messaging/log`. | — | Notices sent for each advanced invoice — email where the customer has email, **SMS** where only a phone exists; `notices_sent` reflects the count. | High | Control | REV-12 | Not Run | basics.ts |
| UAT-O2C-145 | Credit Manager places a manual hold | CreditManager | Customer in good standing (under limit, not overdue) | 1. `POST /api/finance/ar/credit-hold {tenant_id:<cust>, reason:"dispute"}`. 2. `GET /api/finance/ar/credit-status?tenant_id=<cust>`. | — | Hold set; status `manual_hold:true`, `on_hold:true`, `hold_reason:"dispute"` even though limit/aging are fine. | High | Control | REV-08, R09 | Not Run | basics.ts |
| UAT-O2C-146 | Credit check denies a manually-held customer | Sales/ArClerk | Customer manually on hold | 1. `POST /api/finance/ar/credit-check {tenant_id:<cust>, amount:1}`. | — | `approved:false`; `reason: CREDIT_HOLD` (manual hold takes precedence over limit/aging reasons). | High | Control | REV-08 | Not Run | basics.ts |
| UAT-O2C-147 | Self-release blocked, second person releases (SoD) | CreditManager / Approver | Hold placed by user A | 1. User **A** `POST /api/finance/ar/credit-release {tenant_id:<cust>}` → rejected. 2. Approver **B** releases. 3. `GET /api/finance/ar/credit-status`. | — | Step 1 → 409 `SOD_SELF_RELEASE`; step 2 succeeds; status `manual_hold:false`, `on_hold:false`. Releasing a non-held account → `NOT_ON_HOLD`. | High | Control | REV-08, R09 | Not Run | basics.ts |
| UAT-O2C-148 | Credit-limit change is STAGED for approval (ceiling not moved) | CreditManager (requester) | Customer limit 100000 | 1. `POST /api/finance/ar/credit-limit {tenant_id:<cust>, new_limit:50000, reason:"risk"}`. 2. Re-read the customer's limit / credit-status. | — | Response `{req_no, status:"PendingApproval", pending:true, old_limit:100000, new_limit:50000, requested_by:<user>}`; the customer's **effective creditLimit is still 100000** — a single user cannot change it (maker-checker G7, migration 0261). | High | Control | REV-08, R09 | Not Run | basics.ts |
| UAT-O2C-149 | Customer statement of account (running balance) | ArClerk | Customer with 2 invoices (1000+500) + 1 receipt (400) | 1. `GET /api/finance/ar/statement?tenant_id=<cust>&from=&to=`. | — | `opening_balance` struck before window; `total_charges:1500`, `total_payments:400`, `closing_balance:1100`; lines carry a **running balance** ending 1100. | Med | Detective | REV-12 | Not Run | basics.ts |
| UAT-O2C-150 | Customer statement — multi-currency (base + filter) | ArClerk | Customer with a THB invoice (1000) + a USD invoice (100 @ fx 34) | 1. `GET …/ar/statement` (no currency). 2. `GET …/ar/statement?currency=USD`. | — | (1) `reporting_currency:THB`, charges = 1000 + 100×34 = **4400** (USD converted at fx). (2) `reporting_currency:USD`, only USD docs, charges **100** in their own units. | Med | Detective | REV-12 | Not Run | basics.ts |
| UAT-O2C-151 | Register quick sale (tap menu → cash → receipt + drawer) | Cashier | Menu seeded; on `/pos/register` | 1. Search/tap a menu item to add it. 2. Adjust qty with **+**. 3. **ชำระเงิน** → **เงินสด** → tap **฿100** → **ยืนยันชำระเงิน**. | UI; item A=50, qty 1 | A sale settles (`SALE-…`); success screen shows **เงินทอน**=฿46.50 (50+VAT=53.50); receipt prints (driver/USB); for cash the drawer pops; sale appears in `รายการออเดอร์`. | High | Positive | Feature (POS register) | Not Run | apps/web .../pos/register/page.tsx |
| UAT-O2C-152 | Register modifier line priced from the catalog | Cashier | Item with a modifier group (e.g. +ไข่ดาว +10) | 1. Tap an item showing the **ตัวเลือก** badge. 2. Pick an option. 3. **เพิ่มลงตะกร้า**. | UI | The picker price reflects base + option (`POST /api/menu/resolve`); the cart line carries the modifier label + the resolved `unit_price`; price can't be hand-edited. | High | Control | Feature (POS register) | Not Run | menu.resolve / modifier-dialog.tsx |
| UAT-O2C-153 | Register PromptPay tender shows a scannable QR | Cashier | Business PromptPay ID configured | 1. **ชำระเงิน** → **QR พร้อมเพย์**. | total ฿107 | `GET /api/payments/promptpay-qr?amount=107` returns `qr_image` (data-URL PNG); the QR renders on screen for the exact amount; with no PromptPay ID set the screen explains it isn't configured. | Med | Positive | Feature (POS register) | Not Run | payments.service (qr_image) |
| UAT-O2C-154 | Register hold → recall round-trips the cart | Cashier | A cart with ≥1 line | 1. **พักบิล**. 2. **บิลที่พักไว้** → **เรียกคืน**. | UI | Hold posts the cart JSON (`POST /api/pos/hold`, `HOLD-…`) and clears the screen; recall (`/recall`) restores the same lines/mode/table; **ทิ้ง** discards a held bill. | Med | Positive | Feature (POS register) | Not Run | pos-control.service / register page |
| UAT-O2C-155 | Register mirrors the cart to the customer display | Cashier | Terminal `T01`; `/display/T01` open | 1. Add/adjust items on `/pos/register`. 2. Watch `/display/T01`. 3. Settle a cash sale. | UI | The register pushes `POST /api/peripherals/display/T01` as the cart changes; the second screen shows the live lines + total, then the **change** after settling. | Low | Positive | REST-11 | Not Run | terminal.ts / display page |
| UAT-O2C-156 | Immaterial cash short posts over/short to GL | PosSupervisor | Till open, expected cash 907 | 1. `POST /api/payments/till/close` `{closing_count:900}` (variance −7, < THB 100). | `{closing_count:900}` | 200; `variance`=−7, `variance_status`=`NotRequired`, `variance_journal_no` = `JE-…`; GL **Dr 5830 Cash Over/Short 7 / Cr 1000 7**; trial balance balanced. | High | Control | REV-13 | Not Run | cashreport.ts |
| UAT-O2C-157 | Material cash short → maker-checker (SoD) | PosSupervisor + Manager | Till open, expected cash 107, cashier `sales1` | 1. Close `{closing_count:0}` (variance −107 ≥ 100). 2. `POST …/till/variance/{session}/approve` as **sales1** (the closer). 3. Approve as **admin** (different user). | as left | (1) `variance_status`=`PendingApproval`, the over/short JE is **Draft** (excluded from balances). (2) **403 `SOD_VIOLATION`** — closer cannot self-approve. (3) 200; JE → **Posted**, `variance_status`=`Approved`; re-approve → 400 `NOT_PENDING`. | High | Control | REV-13 | Not Run | cashreport.ts |
| UAT-O2C-158 | AR bad-debt write-off → Draft (maker-checker) | ArClerk (`admin`) | AR exists | 1. `POST /api/finance/ar/write-off` `{amount:500, reason, customer_name}`. 2. `GET /api/ledger/trial-balance`. | `{amount:500, reason:'ลูกค้าปิดกิจการ'}` | (1) `pending:true`, `entry_no:JE-…`; (2) account **5720** unchanged — the Draft is excluded from balances. | High | Control | REV-14, GL-05 | Not Run | basics.ts |
| UAT-O2C-159 | Write-off: maker cannot self-approve; different user approves | `admin` (maker) + `mgr` | write-off Draft from UAT-O2C-158 | 1. `POST /api/ledger/journal/{entryNo}/approve` as **admin**. 2. Approve as **mgr**. 3. `GET /api/finance/ar/write-offs`. | — | (1) **403 `SOD_VIOLATION`** (binds even Admin). (2) 200; **Dr 5720 +500 / Cr 1100** now effective. (3) register lists the write-off `state:approved`, `total_written_off`=500. | High | Control | REV-14, GL-05 | Not Run | basics.ts |
| UAT-O2C-160 | Write-off input guards | ArClerk | — | 1. `POST .../ar/write-off` `{amount:0, reason:'x'}`. 2. `{amount:100}` (no reason). | — | (1) **400** (non-positive amount); (2) **400** (reason required). No JE posted. | Med | Control | REV-14 | Not Run | basics.ts |
| UAT-O2C-161 | Register sells offline then auto-syncs on reconnect | Cashier | On `/pos/register`, menu seeded; quick mode | 1. Add a menu item. 2. Go **offline**. 3. **ชำระเงิน → เงินสด → พอดี → ยืนยันชำระเงิน**. 4. Go back **online**. | UI (offline) | (2) badge shows **ออฟไลน์ — บันทึกในเครื่อง**. (3) success shows **บันทึกออฟไลน์แล้ว** (no server `sale_no`/receipt); a **รอซิงค์ 1** badge appears. (4) the register auto-replays to `POST /api/restaurant/offline-sync` and the badge clears. | High | Positive | BRANCH-03 | Not Run | e2e/register-offline.spec.ts |
| UAT-O2C-162 | Register offline replay is idempotent (exactly-once) | System | Sales queued offline | 1. `POST /api/restaurant/offline-sync` a batch. 2. Re-send the **same** batch (same `client_uuid`s). | sku lines + `client_uuid` | (1) all `synced` with distinct server-minted `SALE-`; sale + GL (Dr1000=Cr4000+Cr2100) posted. (2) all `duplicate`, same `sale_no`, **no** second sale/GL; a bad op fails (retryable tombstone) while neighbours sync; dedup is tenant-scoped. | High | Control | BRANCH-03 | Not Run | restaurant-offline.ts |
| UAT-O2C-163 | Reserve a table for a future time | Host | Table `R1` available | 1. `POST /api/restaurant/reservations` `{table_id, reserved_for, party_size:4, customer_phone}`. 2. `GET /api/restaurant/tables`. | `kind` omitted (reservation) | 201; `status:booked`; the assigned table is held **`reserved`**. A reservation with no `reserved_for` → **400**. | High | Positive | Feature (reservations) | Not Run | reservations.ts |
| UAT-O2C-164 | Walk-in waitlist → notify when ready | Host | — | 1. `POST .../reservations` `{kind:waitlist, party_size:2, customer_phone, quoted_wait_min:20}`. 2. `POST .../reservations/:id/notify`. | as left | (1) `status:waiting`, no table held. (2) `status:ready`; a `message_log` row is written (`campaign='reservation_ready'`, SMS/LINE). | High | Positive | Feature (waitlist) | Not Run | reservations.ts |
| UAT-O2C-165 | Seat a reservation → table occupied | Host | Booked reservation on `R1` | 1. `POST .../reservations/:id/seat`. 2. Re-`seat`. | — | (1) `status:seated`; table `R1` → **occupied**. (2) **400 `BAD_STATUS`** (already seated). | High | Control | Feature (reservations) | Not Run | reservations.ts |
| UAT-O2C-166 | Cancel releases the held table; RLS isolates tenants | Host (T1) + staff (T2) | Booked reservation holding `R2` | 1. `POST .../reservations/:id/cancel`. 2. As **T2**, `GET .../reservations` + `seat` a T1 id. | bearer sales2 | (1) `status:cancelled`; `R2` → **available**. (2) T2 sees **0** rows and `seat` a T1 reservation → **404**. | High | Control | Feature · ITGC-AC (RLS) | Not Run | reservations.ts |
| UAT-O2C-167 | Tips accrue to 2300; cashier cannot distribute | Cashier + Manager | Sales rung with tips (Σ 100) | 1. `GET /api/restaurant/tips/pool?from=&to=`. 2. As a **Cashier** (`pos_sell`), `POST .../tips/distribute`. | — | (1) `collected:100`, `available:100`, GL **2300 = Cr 100**. (2) **403** — distributing is `order_mgt`/`exec` only (cashier can't pay tips to self). | High | Control | TIP-01 | Not Run | tips.ts |
| UAT-O2C-168 | Manager distributes the pool by hours | Manager (`order_mgt`) | Pool 100 available | 1. `POST .../tips/distribute` `{method:hours, staff:[{staff:A,hours:6},{staff:B,hours:2}]}`. | as left | A=฿75, B=฿25 (Σ=100); JE **Dr 2300 100 / Cr 1000 100**; 2300 clears to **0**; TB balanced. | High | Control | TIP-01 | Not Run | tips.ts |
| UAT-O2C-169 | Cannot over-distribute the tip pool | Manager | Pool already paid out (available 0) | 1. `POST .../tips/distribute` `{amount:50, staff:[{staff:A}]}`. | as left | **400 `TIP_OVER_DISTRIBUTE`** (or `NO_POOL`); no JE; 2300 stays 0. | High | Control | TIP-01 | Not Run | tips.ts |
| UAT-O2C-170 | Kiosk takeaway returns a public track link | Cashier | Till open | 1. `POST /api/restaurant/kiosk/checkout` `{fulfillment_type:takeaway, items, method:Cash}`. 2. `GET /api/order/t/{track_token}`. | as left | (1) response carries `track_url` = `/track/{token}`. (2) the public endpoint (no login) returns the order with `fulfillment_status:received`. | High | Positive | Feature (order tracker) | Not Run | channel.ts |
| UAT-O2C-171 | Customer tracks a takeaway order & pays (public) | Diner (public) | Stubbed public tracker API | 1. Open `/track/{token}`. 2. Watch the timeline. 3. **ชำระเงินออนไลน์ (PromptPay)** → **ฉันชำระเงินแล้ว**. | UI (mocked) | The fulfillment timeline shows **กำลังเตรียม** active + the bill total; pay renders a PromptPay QR; confirm shows **ชำระเงินแล้ว**. | Med | Positive | Feature (order tracker) | Not Run | e2e/order-tracker.spec.ts |
| UAT-O2C-180 | Create a customer-of-record linking B2C + B2B | CRM/AR | a B2B account tenant + a loyalty member | 1. `POST /api/customer-master {name, kind:company, account_code, member_id}`. | as left | 201; `customer_no` = `CUS-YYYYMMDD-###`; the record links the member + the B2B account. | High | Positive | REV-15 | Not Run | basics.ts |
| UAT-O2C-181 | 360 view ties AR to the sub-ledger + shows loyalty | CRM/AR | customer linked (O2C-180); account has 3000 open AR; member Gold/250 pts | 1. `GET /api/customer-master/{no}/360`. | — | `summary.ar_outstanding`=3000 (== the AR sub-ledger / `/api/customers/{account}` balance); `loyalty.tier`=Gold, `points_balance`=250; `summary.has_loyalty`/`has_account` true. | High | Control | REV-15 | Not Run | basics.ts |
| UAT-O2C-182 | Customer master register search | CRM | customers created | 1. `GET /api/customer-master?search=Acme`. | — | The matching customer-of-record is listed. | Med | Positive | REV-15 | Not Run | basics.ts |
| UAT-O2C-183 | Small refund runs immediately; large refund needs approval | Cashier (`pos_sell`) + Supervisor (`pos_refund`) | captured payments 500 and 2000 | 1. `POST /api/payments/refunds` `{payment_no:P500, amount:500}`. 2. `{payment_no:P2000, amount:2000}`. 3. `GET /api/finance/approvals/pending`. | as left | (1) `status:Refunded`, `REF-…`. (2) **`status:PendingApproval`** + `request_id`; **no** `payment_refunds` row yet. (3) the request appears in the GOV-01 monitor (`type:refund`, `REV-16`). | High | Control | REV-16 | Not Run | refund-approval.ts |
| UAT-O2C-184 | Refund SoD: requester can't approve; different user can | Requester + Manager | refund request from O2C-183 | 1. `POST /api/payments/refund-requests/{id}/approve` as the **requester**. 2. As a **different** user. 3. Re-approve. | — | (1) **403 `SOD_VIOLATION`**. (2) `status:Approved`, `REF-…`, exactly one `payment_refunds` row. (3) **400 `NOT_PENDING`**. A **reject** by a different user moves no money. | High | Control | REV-16 | Not Run | refund-approval.ts |
| UAT-O2C-185 | Lead → convert → customer-of-record + opportunity | CRM/Sales | — | 1. `POST /api/crm/pipeline/leads`. 2. `…/leads/{no}/qualify`. 3. `…/leads/{no}/convert {amount}`. 4. re-convert. | as left | LEAD- created; convert → 201 `converted` + `customer_no` CUS- + `opp_no` OPP-; re-convert → 400 `LEAD_CONVERTED`. | High | Control | REV-17 | Not Run | basics.ts |
| UAT-O2C-186 | Opportunity stage machine (won terminal) | CRM/Sales | open opportunity | 1. `PATCH …/opportunities/{no}/stage {stage:proposal}`. 2. `{stage:won}`. 3. `{stage:negotiation}`. | as left | proposal → 200; won → `stage:won`, `probability:100`; re-stage a won deal → 400 `OPP_CLOSED`. | High | Control | REV-17 | Not Run | basics.ts |
| UAT-O2C-187 | Marking lost requires a reason | CRM/Sales | open opportunity | 1. `PATCH …/opportunities/{no}/stage {stage:lost}` (no reason). | — | 400 `LOST_REASON_REQUIRED`. | High | Control | REV-17 | Not Run | basics.ts |
| UAT-O2C-188 | Weighted pipeline forecast + win-rate | CRM/Controller | opps across stages | 1. `GET /api/crm/pipeline/summary`. | — | `weighted_forecast` = Σ open amount×probability; `won_amount` reflects won deals; `by_stage` counts; `win_rate` computed. | Med | Detective | REV-17 | Not Run | basics.ts |
| UAT-O2C-189 | Log + list an activity | CRM/Sales | an opportunity | 1. `POST /api/crm/pipeline/activities {entity_type:opportunity, entity_no, type:call}`. 2. `GET …/activities?entity_no=`. | — | The activity is logged and listed against the opportunity. | Med | Positive | REV-17 | Not Run | basics.ts |
| UAT-O2C-190 | Bank the safe cash → Dr bank / Cr cash; cashier can't | Cashier (`pos_till`) + Treasury (`ar`) | bank account (1010); till with 2 drops (300+200) | 1. `GET /api/bank/deposits/undeposited-drops`. 2. `POST /api/bank/deposits` as the **cashier**. 3. As **treasury**. | `{bank_account_id}` | (1) `cash_in_safe`=500, 2 drops. (2) **403** — cashier (`pos_till`) can't bank (SoD). (3) `BDEP-…` banks 500, GL **Dr 1010 500 / Cr 1000 500**; cash-in-safe → **0**; TB balanced. | High | Control | REC-05 | Not Run | cash-banking.ts |
| UAT-O2C-191 | Reconcile the deposit to the statement | Treasury | deposit from O2C-190 | 1. `POST /api/bank/deposits/{id}/reconcile`. 2. Re-reconcile. | — | (1) `status:Reconciled`. (2) **400 `ALREADY_RECONCILED`**. Re-banking with no drops → **400 `NO_DROPS`**. | High | Control | REC-05 | Not Run | cash-banking.ts |
| UAT-O2C-255 | New bank account is PendingApproval + inactive; a deposit into it is rejected `BANK_NOT_APPROVED` (G9) | Treasury/Finance (`exec`/`ar`) | cash in the safe (undeposited drops) available to bank | 1. Create a new bank account (account no + GL mapping + opening balance). 2. `GET /api/bank/accounts/pending`. 3. `POST /api/bank/deposits` into the newly created (pending) account. | `{bank_account_id}` | (1) account created `status:PendingApproval`, **inactive** (`requested_by` stamped; migration 0264). (2) it appears on the pending queue. (3) **400 `BANK_NOT_APPROVED`** — a non-approved account cannot bank cash; **no** deposit row and nothing posts to the GL. | High | Control | REC-05 | Not Run | cash-banking.ts |
| UAT-O2C-256 | Requester cannot self-approve the new bank account (`SOD_VIOLATION`) (G9) | Requester **A** (`approvals`/`exec`) | the pending bank account from O2C-255, `requested_by` = user **A** | 1. User **A** `POST /api/bank/accounts/:id/approve`. | — | **403 `SOD_VIOLATION`** (approver = `requested_by`); the account stays `PendingApproval`/inactive and still can't bank cash (`BANK_NOT_APPROVED`) — activation needs a **different** person. | High | Control | REC-05 | Not Run | cash-banking.ts |
| UAT-O2C-257 | A distinct approver activates the account → banking proceeds (Dr bank / Cr 1000) (G9) | Approver **B** (`approvals`/`exec`) ≠ requester | the same pending account; approver **B** ≠ A; undeposited drops in the safe | 1. Approver **B** `POST /api/bank/accounts/:id/approve`. 2. `POST /api/bank/deposits` into the now-approved account. 3. `GET /api/ledger/trial-balance`. | — | (1) account `status:Approved`, **active** (`approved_by`=B, `approved_at` stamped); a `.../reject` (discard) path also exists. (2) the deposit now banks — GL **Dr <bank account GL> / Cr 1000 Cash**, cash-in-safe drops to 0. (3) **TB balanced**. | High | Control | REC-05 | Not Run | cash-banking.ts / bankrec.ts |
| UAT-O2C-258 | Void / refund exception report surfaces void + refund activity for independent review (G14) | Reviewer (`exec`/`ar`/`fin_report`) ≠ the till operator | at least one voided payment and one executed refund exist in the period | 1. `GET /api/payments/exceptions/voids-refunds` (optionally `?from=&to=` `YYYY-MM-DD`). | — | Lists **every voided payment and every refund** in the window — each with its number, amount, by-whom and when — plus per-type **counts** and **totals**, tenant-scoped. Read-only (nothing posts). The executed refund from the refund flow appears; voids and sub-threshold refunds (single-user by design) are visible for after-the-fact review. | Med | Detective | R08/R12 (detective; no new control) | Not Run | refund-approval.ts |
| UAT-O2C-259 | Bank-statement import is single-user; the imported lines are reviewed at certification (G10, documentation) | Certifier (`approvals`) ≠ preparer | a reconciliation period with imported statement lines (matched + unmatched) | 1. Preparer imports the statement + runs auto-match. 2. Certifier reviews the matched/unmatched statement lines. 3. Certifier `POST /api/recon/periods/:id/certify`. | — | (1) statement import succeeds single-user (no second-person gate — by design). (2) the certifier's evidence review covers the imported lines. (3) certification succeeds only for a **different** user than the preparer (`SOD_VIOLATION` on self-certify, R06) — so an erroneous/unauthorised import is caught at certification (compensating detective control, **REC-02**). No new endpoint. | Med | Detective | REC-02 (compensating; no new control) | Not Run | bankrec.ts |
| UAT-O2C-260 | Customer master direct-edit + Oracle/NetSuite-grade fields (master-data audit Phase 3, no new control) | `crm`/`ar`/`exec` holder | — | 1. `POST /api/customer-master` with `credit_terms`/`sales_rep`/`category`/`language`/`external_ref`. 2. `GET /api/customer-master/{no}`. 3. `PATCH /api/customer-master/{no}` with updated fields + `status:'inactive'`. 4. Same endpoint with an empty body. 5. `GET /api/customer-master?search=` finds the record. | credit_terms=Net 30→Net 60, category=Wholesale→Key Account | (1) 200/201; fields saved. (2) GET projects all the new fields. (3) 200; direct-edit saves immediately (no maker-checker — none of these fields carry payment-redirection risk, mirrors the vendor-profile precedent). (4) 400 `NO_FIELDS`. (5) search finds the record with the updated fields. | Med | Positive | (no new control) | Pass | customers.ts |
| UAT-O2C-261 | Customer gains multiple addresses; only the newest marked primary keeps the flag (master-data audit Phase 4, no new control) | `crm`/`ar`/`exec` holder | a customer master record exists | 1. `POST …/{no}/addresses` (billing, `is_primary:true`). 2. `POST …/{no}/addresses` (shipping, `is_primary:true`). 3. `GET …/{no}/addresses`. 4. `DELETE …/{no}/addresses/{id}` the first (now non-primary) address. 5. `DELETE …/{no}/addresses/999999` (non-existent). | address_type billing → shipping | (1)/(2) 200/201, each returns the new row. (3) both addresses listed, only the shipping one `is_primary:true` (adding a new primary demotes the old one). (4) 200 `{deleted:true}`. (5) 404 `ADDRESS_NOT_FOUND`. | Med | Positive | (no new control) | Pass | customers.ts |
| UAT-O2C-262 | Customer gains multiple contacts (master-data audit Phase 4, no new control) | `crm`/`ar`/`exec` holder | a customer master record exists | 1. `POST …/{no}/contacts` (name/title/phone, `is_primary:true`). 2. `GET …/{no}/contacts`. 3. `DELETE …/{no}/contacts/999999` (non-existent). | name=คุณสมหญิง, title=ผู้จัดการฝ่ายจัดซื้อ | (1) 200/201, returns the new contact. (2) contact listed with the saved fields. (3) 404 `CONTACT_NOT_FOUND`. | Med | Positive | (no new control) | Pass | customers.ts |
| UAT-O2C-263 | Customer links to a parent company; self-parent blocked; 360° surfaces addresses/contacts/parent together (master-data audit Phase 4, no new control) | `crm`/`ar`/`exec` holder | a second customer master record exists to act as the parent | 1. `PATCH …/{no}/parent {parent_customer_no: no}` (itself). 2. `PATCH …/{no}/parent {parent_customer_no: parentNo}`. 3. `GET …/{no}/360`. | — | (1) 400 `SELF_PARENT` — a customer cannot be its own parent. (2) 200; `parent_customer_no` set to the parent's customer number. (3) the 360° response includes `addresses`, `contacts`, and `parent:{customer_no,name}` together in one call. | Med | Positive | (no new control) | Pass | customers.ts |
| UAT-O2C-264 | Duplicate-customer detection surfaces probable duplicates for steward review (DQM, Phase 5, no new control) | `crm`/`ar`/`exec` holder | two customers that share a phone and have similar names, plus an address+contact on one of them | 1. `GET /api/customer-master/duplicates`. | survivor "บริษัท สมาร์ทโซลูชั่น จำกัด"; duplicate "สมาร์ทโซลูชั่น", same phone | A group is returned containing both records; the duplicate carries `reasons` including **`phone`** and **`name`** (exact phone match + fuzzy name similarity ≥ 0.6) and a `score`. Read-only. | Med | Positive | (no new control) | Pass | customers.ts |
| UAT-O2C-265 | Merging a duplicate repoints its child rows + survivorship + soft-retire (DQM, Phase 5, no new control) | `crm`/`exec`/`masterdata` holder | the pair from O2C-264; the duplicate has an address + contact + an email the survivor lacks | 1. `POST /api/customer-master/{survivorNo}/merge {duplicate_customer_no}`. 2. `GET …/{duplicateNo}`. 3. `GET …/{survivorNo}/addresses` + `…/contacts`. 4. `GET …/{survivorNo}`. | — | (1) 200 `{merged:true}`. (2) the duplicate is **soft-retired**: `status='merged'`, `merged_into` = survivor id (record preserved, not deleted). (3) the duplicate's address + contact are now listed under the **survivor** (child rows repointed via `md_merge_repoint`). (4) the survivor's previously-blank `email` is back-filled from the duplicate (survivorship). | High | Positive | (no new control) | Pass | customers.ts |
| UAT-O2C-266 | Merge guards — self-merge, already-merged, and merged rows leave the review queue (DQM, Phase 5, no new control) | `crm`/`exec`/`masterdata` holder | the merged pair from O2C-265 | 1. `POST …/{survivorNo}/merge {duplicate_customer_no: survivorNo}`. 2. `POST …/{survivorNo}/merge {duplicate_customer_no: <already-merged>}`. 3. `GET /api/customer-master/duplicates`. | — | (1) 400 `SELF_MERGE`. (2) 400 `ALREADY_MERGED`. (3) the merged duplicate no longer appears in the review queue. (A survivor+duplicate that both own a row with the same unique key would roll back with `409 MERGE_CONFLICT` for manual resolution.) | Med | Control | (no new control) | Pass | customers.ts |
| UAT-O2C-192 | Sign Z-report → immutable signed archive | PosSupervisor | Till closed (counted 900, denoms 500×1/100×4, variance −7) | 1. `POST /api/payments/till/{session}/z-report/sign`. 2. Re-sign. 3. `GET /api/payments/xz-reports/{id}`. | — | (1) `status:SIGNED`, 64-hex `content_hash`, totals + denominations snapshotted (`cash_counted`=900, `variance`=−7). (2) `already:true`, same `content_hash` (idempotent). (3) `hash_valid:true`. | High | Control | POS-07 | Not Run | cashreport.ts |
| UAT-O2C-193 | Tamper detection + sign guards | PosSupervisor + Cashier | signed Z from O2C-192; an open till | 1. Mutate a persisted total then `GET …/xz-reports/{id}`. 2. Sign an **open** till. 3. Sign as a sell-only **Cashier**. | — | (1) `hash_valid:false` (re-computed hash ≠ stored). (2) **400 `TILL_NOT_CLOSED`**. (3) **403** — `pos_close` required (manager attestation). | High | Control | POS-07 | Not Run | cashreport.ts |
| UAT-O2C-194 | AR allowance: aging compute | AR/Controller (`admin`) | open AR aged: 1000 @170d, 2000 @50d, 5000 not-due | 1. `POST /api/finance/ar-allowance/compute {tenant_id}`. | default bucket rates | 200; `total_ar`=8000; `allowance`=**1100** (1000×100% + 2000×5% + 5000×0%); buckets returned; `posted:false`. | High | Positive | REV-18 | Not Run | basics.ts |
| UAT-O2C-195 | AR allowance: computer cannot post own (SoD) | `admin` (computer) | allowance from O2C-194 | 1. `POST /api/finance/ar-allowance/{id}/post` as **admin**. | — | **403 `SOD_SELF_POST`** (binds even Admin). | High | Control | REV-18 | Not Run | basics.ts |
| UAT-O2C-196 | AR allowance: independent post books the delta | `mgr` (poster ≠ computer) | allowance from O2C-194 | 1. `POST …/ar-allowance/{id}/post` as **mgr**. 2. `GET /api/ledger/trial-balance`. | — | (1) 200; `posted_amount`=1100, `entry_no:JE-…`. (2) **Dr 5720 +1100 / Cr 1190 +1100**; gross AR (1100) untouched. | High | Control | REV-18 | Not Run | basics.ts |
| UAT-O2C-197 | AR allowance: no double-post; register | AR/Controller | posted allowance from O2C-196 | 1. Re-post `POST …/ar-allowance/{id}/post`. 2. `GET /api/finance/ar-allowance`. | — | (1) **400 `ALREADY_POSTED`**. (2) register lists the computation `posted:true`, `allowance`=1100. | Med | Control | REV-18 | Not Run | basics.ts |
| UAT-O2C-198 | POS Favourites star toggle persists via user-prefs | Cashier | register loaded, item A id=5 | 1. `PUT /api/user-prefs {pos_fav:[5]}`. 2. `GET /api/user-prefs`. 3. `PUT /api/user-prefs {pos_fav:[]}` (unstar). 4. `GET`. | bearer cashier | (1) 200. (2) `pos_fav:[5]`. (3) 200. (4) `pos_fav:[]`. | Med | Positive | Feature (B2 favourites) | Not Run | manual |
| UAT-O2C-199 | Returns create-flow: sale search → RTN | ReturnsClerk | sale S-001 2×item-A (total 214) | 1. `GET /api/pos/orders/S-001`. 2. `POST /api/pos/returns {sale_no:S-001, items:[{sale_item_id, qty:2}], refund_method:Cash}`. 3. `GET /api/pos/returns?sale_no=S-001`. | bearer returns-clerk | (1) sale + items returned. (2) `return_no` RTN-…, `total_returned`=214, `refund_method:Cash`. (3) the RTN appears in the sale's returns list. | High | Positive | Feature (B2 returns UI) | Not Run | manual |
| UAT-O2C-200 | Retail portal: service charge fires for large party | Cashier/Admin | item B at unit_price 200, no active item-level rules | `POST /api/portal/pos/sales {apply_pricing:true, items:[{item_id:"B", qty:1, unit_price:200}], party_size:6, service_charge_pct:10, service_min_party:6, rounding:1}` | bearer admin | 200; `service_charge`=20, `total`=235, `rounding_adjustment`=-0.40; GL Cr 4400=20, Cr 4000=200, Cr 2100≈15.40, Dr 1000=235, Dr/Cr 4900=0.40 (balanced). | High | Positive | Feature (B4 portal pricing) | Not Run | pos-wiring harness |
| UAT-O2C-201 | Retail portal: service charge skipped below party threshold | Cashier/Admin | same item B | `POST /api/portal/pos/sales {apply_pricing:true, items:[{item_id:"B", qty:1, unit_price:200}], party_size:3, service_charge_pct:10, service_min_party:6}` | bearer admin | 200; `service_charge`=0, `total`=214, no 4400 GL line. | Med | Control | Feature (B4 portal pricing) | Not Run | pos-wiring harness |
| UAT-O2C-202 | Won opportunity → project (won-only, traceable, idempotent) | ProjectController | a won opportunity (`amount` set) | 1. `POST /api/crm/pipeline/opportunities {amount:250000}`. 2. `PATCH …/opportunities/{no}/stage {stage:won}`. 3. `POST /api/projects/from-opportunity/{no} {billing_type:Fixed}`. 4. re-convert the same opp. 5. convert an open opp. 6. convert `OPP-NOPE`. | as left | (3) 201 project `contract_amount`=250000, `crm_opp_no`={no}, `customer_no` stamped, `status:Open`; (4) `already:true`, same `project_code` (no duplicate); (5) **400 `OPP_NOT_WON`**; (6) **404 `OPP_NOT_FOUND`**. | High | Control | CRM-WL | Not Run | projects.ts |
| UAT-O2C-203 | Project WBS — planned-hours-weighted % complete roll-up | ProjectPlanner | a project (`PRJ-WBS`) | 1. `POST /api/projects/PRJ-WBS/tasks {name,planned_hours:10,pct_complete:50}`. 2. `…/tasks {name,planned_hours:30,pct_complete:0}`. 3. `PATCH /api/projects/tasks/{id} {status:done}`. 4. `GET /api/projects/PRJ-WBS`. 5. `PATCH /api/projects/tasks/999999`. | as left | (2) `pct_complete`=12.5, `count`=2; (3) roll-up=87.5 (10×50+30×100)/40; (4) detail `pct_complete`=87.5, `task_count`=2; (5) **404 `TASK_NOT_FOUND`**. | High | Positive | Feature (P1 WBS); 16 §7 (7) | Not Run | projects.ts |
| UAT-O2C-204 | Milestone completion raises the Fixed-price progress bill | ProjectController | Fixed project (`PRJ-MS`, contract 100000) | 1. `POST /api/projects/PRJ-MS/milestones {name,billing_percent:40}`. 2. `POST /api/projects/milestones/{id}/reach`. 3. re-reach. 4. reach a non-billing milestone. 5. reach `999999`. | as left | (2) `status:reached`, `billing.revenue`=40000, JE `JE-…` (Dr 1100/Cr 4200, WIP relief, contract-capped — PROJ-02); (3) **400 `MILESTONE_REACHED`** (no double bill); (4) `reached`, `billing:null`; (5) **404 `MILESTONE_NOT_FOUND`**. | High | Control | PROJ-02; 16 §7 (8) | Not Run | projects.ts |
| UAT-O2C-205 | Resource assignment snapshots the authorized rate card | ProjectManager | rate card `Senior Dev` cost 1000/bill 2000 from 2026-01-01 | 1. `POST /api/projects/rate-cards {role:"Senior Dev",cost_rate:1000,bill_rate:2000,effective_from:"2026-01-01"}`. 2. `POST /api/projects/PRJ-RES/resources {resource_name:"Alice",role:"Senior Dev",alloc_pct:60,period_start:"2026-02-01"}`. 3. assign `{resource_name:"Bob",role:"Unknown"}`. 4. assign `{resource_name:"Carol",alloc_pct:0}`. | as left | (2) resource `cost_rate`=1000, `bill_rate`=2000, `alloc_pct`=60; (3) no rate card → `cost_rate`=0 (not guessed); (4) **400 `BAD_ALLOC`**. | High | Control | PROJ-05; 16 §7 (9) | Not Run | projects.ts |
| UAT-O2C-206 | Capacity utilization flags over-allocation | ProjectManager | Alice 60% on `PRJ-RES` + 60% on `PRJ-RES2` | 1. `GET /api/projects/resources/utilization`. | — | Alice `allocated_pct`=120, `over_allocated:true`; `over_allocated_count`≥1. | Med | Detective | PROJ-05; 16 §7 (9) | Not Run | projects.ts |
| UAT-O2C-207 | Timesheet → project labor maker-checker posts to WIP | Timekeeper (submit) + ProjectManager (approve) | employee `hourly_rate`=200; project `PRJ-TS` | 1. `POST /api/hcm/timesheets {emp_code,regular_hours:8,project_code:"PRJ-TS",billable:true}` as **admin**. 2. `POST /api/hcm/timesheets/{id}/approve` as **admin** (submitter). 3. approve as **mgr** (independent). 4. `GET /api/projects/PRJ-TS`. 5. re-approve as mgr. 6. approve `999999`. | as left | (1) `status:Pending`; (2) **403 `SOD_SELF_APPROVAL`** (binds Admin); (3) `Approved`, `labor_cost`=1600 (8×200), `entry_no:JE-…` posted via PRJ-COST; (4) `cost_to_date`=1600, `wip`=1600; (5) `already:true` (no double-post); (6) **404 `TIMESHEET_NOT_FOUND`**. | High | Control | PROJ-04; 16 §7 (10) | Not Run | projects.ts |
| UAT-O2C-208 | Earned-value metrics + task dependency guard | ProjectManager | project `PRJ-EVM`; task A 1000@100% end 2026-01-31, task B 1000@0% end 2099-12-31 depends on A; actual cost 900 | 1. `POST /api/projects/PRJ-EVM/tasks` ×2 (B `depends_on:[A]`). 2. `POST /api/projects/PRJ-EVM/cost {amount:900}`. 3. `GET /api/projects/PRJ-EVM/evm`. 4. `PATCH /api/projects/tasks/{A} {depends_on:[A]}`. | as left | (1) B `depends_on` includes A; (3) `bac`=2000, `ev`=1000, `pv`=1000, `ac`=900, `cpi`≈1.1111, `spi`=1.0, `cost_variance`=100, `schedule_variance`=0, `eac`≈1800; (4) **400 `BAD_DEPENDENCY`**. | High | Detective | PROJ-06; 16 §7 (11) | Not Run | projects.ts |
| UAT-O2C-209 | Critical-path schedule (CPM) + EVM S-curve | ProjectManager | project `PRJ-CPM`: A(2d)→B(3d), A→C(1d), B,C→D(2d) | 1. `GET /api/projects/PRJ-CPM/schedule`. 2. `GET /api/projects/PRJ-SC/evm/series`. | as left | (1) `project_duration_days`=7; A,B,D `on_critical_path:true`, C `false` (slack 2); (2) S-curve `series` cumulative 1000→2000 over 2 months, `bac`=2000. | Med | Detective | PROJ-06; 16 §7 (11) | Not Run | projects.ts |
| UAT-O2C-210 | Win/loss analytics for the pipeline dashboard | Sales/Controller | a lost opportunity (50000, reason set) | 1. `PATCH …/opportunities/{no}/stage {stage:lost, lost_reason}`. 2. `GET /api/crm/pipeline/win-loss`. | — | `loss_reasons[]` includes the reason (amount 50000); `by_owner[]` lists owners with `win_rate`; `summary.win_rate` present; `monthly[]` trend returned. | Med | Detective | REV-17; 16 §7 (11) | Not Run | projects.ts |
| UAT-O2C-211 | Schedulable BI report types generate | Exec | projects + a closed opportunity exist | 1. `POST /api/bi/subscriptions {report_type:project_evm}` → `…/{id}/run`. 2. `POST /api/bi/subscriptions {report_type:crm_win_loss}` → `…/{id}/run`. 3. `GET /api/bi/report-types`. | — | (1) run `status:success`, summary "Portfolio EVM…"; (2) run `status:success`, summary "Win/loss…"; (3) catalog includes `project_evm` + `crm_win_loss`. | Low | Positive | PROJ-06/REV-17; 16 §7 (11) | Not Run | projects.ts |
| UAT-O2C-212 | Portfolio command center rollup | Exec/ProjectController | ≥1 project + a won + converted opportunity | 1. `GET /api/projects/portfolio`. | — | `count`>0; `totals` (BAC/EV/AC/EAC/CPI); `health` (on_track/at_risk/no_data); `financials` (contract/billed/wip/margin); `capacity.over_allocated_count`; `funnel` (open/won/converted); `at_risk[]`. | Med | Detective | PROJ-06; 16 §7 (11) | Not Run | projects.ts |
| UAT-O2C-213 | Change-controlled baselines + variance | ProjectController/PMO | project `PRJ-BL` with 2 tasks (cost 1000 each) | 1. `POST /api/projects/PRJ-BL/baseline`. 2. add a 500 task → `GET …/baseline`. 3. `POST …/baseline` (no reason). 4. `POST …/baseline {reason}`. | as left | (1) baseline `baseline_bac`=2000, `status:active`, history 1; (2) `variance.bac_delta`=500, `bac_pct`=25; (3) **400 `BASELINE_REASON_REQUIRED`**; (4) new active `baseline_bac`=2500, variance 0, history 2 (prior superseded). | High | Control | PROJ-07; 16 §7 (12) | Not Run | projects.ts |
| UAT-O2C-214 | Project templates: author + one-step apply | ProjectController/PMO | template `IMPL-STD` (3 tasks + 1 milestone; seq deps + parent + offsets); project `PRJ-TPL` start `2026-03-01` | 1. `POST /api/projects/templates`. 2. re-`POST` same code. 3. `POST /api/projects/PRJ-TPL/apply-template/IMPL-STD`. 4. `GET …/milestones`. 5. apply again. | as left | (1) template has 4 items (1 milestone); (2) **400 `TEMPLATE_EXISTS`**; (3) 3 tasks + 1 milestone scaffolded, Kickoff dated `2026-03-01`→`2026-03-02`, Build depends on Kickoff, config `parent_id`=Build; (4) Go-live due `2026-03-13`, billing 50%; (5) **400 `PROJECT_HAS_TASKS`**. | Medium | Functional | 16 §7 (13) | Not Run | projects.ts |
| UAT-O2C-215 | RACI accountability + "my tasks" | ProjectController/PMO | project `PRJ-RACI`; users `admin`, `mgr` | 1. add task RACI-A {A:admin, R:[mgr,dev1], C:[arch], I:[exec]}. 2. add RACI-B {A:mgr, R:[admin]}, RACI-C {R:[dev1]} (no A). 3. `PATCH …/tasks/{A}` {consulted:[arch,qa]}. 4. `GET …/raci`. 5. `GET /api/projects/my-tasks` as admin then mgr. | as left | (1) task stores A/R/C/I (lists deduped); (3) consulted=[arch,qa]; (4) matrix 3 tasks, admin A×1+R×1, `missing_accountable`=[RACI-C], `complete:false`; (5) admin → RACI-A (accountable)+RACI-B (responsible); mgr → RACI-A (responsible)+RACI-B (accountable), not RACI-C. | Medium | Control | PROJ-04 (SoD note); 16 §7 (14) | Not Run | projects.ts |
| UAT-O2C-216 | Risk & issue register + portfolio top-risks | ProjectController/PMO | project `PRJ-RISK` | 1. `POST …/risks` {prob 5, impact 5} no mitigation. 2. `POST …/risks` {kind issue, impact 4, +mitigation} + a low risk. 3. `PATCH /api/projects/risks/{high}` {+mitigation}. 4. `GET /api/projects/risks/top`. 5. `PATCH …/{issue}` {status closed}. 6. `PATCH …/999999`. | as left | (1) score 25, rag red, `unmitigated_high`=1; (2) issue score 20/red, low risk green, summary risks 2/issues 1; (3) `unmitigated_high`→0 (high_open still 2); (4) top ranks score-25 first, `high_count`=2, `unmitigated_high_count`=0; (5) closed 1, `closed_at` stamped; (6) **404 `RISK_NOT_FOUND`**. | High | Control | PROJ-08; 16 §7 (15) | Not Run | projects.ts |
| UAT-O2C-217 | POC over-time revenue recognition | ProjectController/Controller | POC project `PRJ-POC` (Fixed, contract 100000, `rev_method:poc`, `estimated_cost` 60000) | 1. cost 30000 → `POST …/recognize`. 2. `GET …` (read). 3. re-`recognize`. 4. `POST …/bill` 40000. 5. cost +30000 → `recognize`. 6. bill-ahead on a 2nd POC project then recognize. 7. `recognize` a billing-method project. | as left | (1) `poc_pct`=50, revenue 50000 / cost 30000 / margin 20000 (Dr 1265/Cr 4200, Dr 5800/Cr 1260); (2) `recognized_revenue`=50000, `contract_asset`=50000; (3) **already** (no double revenue); (4) invoice clears asset — revenue 0, `contract_asset_cleared`=40000; (5) `poc_pct`=100, recognised-to-date 100000, GL balanced; (6) bill-ahead → `billings_in_excess`; recognise releases 2410; (7) **400 `NOT_POC`**. | High | Control | PROJ-09; 16 §7 (16) | Not Run | projects.ts |
| UAT-O2C-218 | Change order — maker-checker contract variation | ProjectController/PMO; two users | project `PRJ-CO` (Fixed, contract 100000, budget 60000) | 1. `POST …/change-orders` no deltas. 2. `POST …/change-orders` {contract +20000, budget +12000}. 3. `GET …`. 4. approve **as requester**. 5. approve **as a different user**. 6. `GET …/change-orders`. 7. re-approve. | as left | (1) **400 `EMPTY_CHANGE_ORDER`**; (2) `pending`=1; (3) contract still 100000 (posts nothing); (4) **400 `SOD_SELF_APPROVAL`**; (5) `approved`, contract 120000 / budget 72000, a new **baseline captured**; (6) `approved`=1, `approved_contract_delta`=20000; (7) **400 `CHANGE_ORDER_DECIDED`**. | High | Control | PROJ-10; 16 §7 (17) | Not Run | projects.ts |
| UAT-O2C-219 | Time-phased resource capacity calendar | ProjectController/PMO | a resource booked 60% Jul–Aug and 60% Aug–Sep | 1. `POST …/resources` ×2 (overlapping windows). 2. `GET /api/projects/resources/capacity?from=2026-07&months=3`. | as left | Per-resource months Jul 60 / **Aug 120 (over_allocated)** / Sep 60; `peak_pct`=120, `over_months`=1; the August monthly rollup flags ≥1 `resources_over`. | Med | Positive | PROJ-05 (operational); 16 §7 (9) | Not Run | projects.ts |
| UAT-O2C-220 | Project health history (EVM/RAG trend) | ProjectController/PMO | project `PRJ-EVM` (CPI 1.11 / SPI 1.0) | 1. `POST …/health` {as_of A}. 2. `POST …/health` {as_of B} ×2. 3. `GET …/health`. 4. subscribe+run BI `project_health_capture`. | as left | (1) snapshot `rag:green`, cpi 1.11; (2/3) history = **2** dated snapshots ascending, same-day re-capture idempotent, all green; (4) run `status:success`, summary "captured N of N". | Med | Detective | PROJ-06 (operational); 16 §7 (18) | Not Run | projects.ts |
| UAT-O2C-221 | PMO action center / exception inbox | PMO; two users | project `PRJ-ACT` seeded with one of every exception (over budget, red EVM, pending change-order, pending timesheet, overdue milestone, unmitigated-high risk, no baseline; health just snapshotted) | 1. `GET /api/projects/action-center`. 2. inspect the items for `PRJ-ACT`. 3. `GET /api/bi/live/recent`. 4. approve the change order **as a different user**, re-`GET action-center`. 5. `GET …/action-center?stale_days=1`. | as left | (1/2) the worklist surfaces **all 7** seeded kinds at the right severity (over_budget / project_red / risk_unmitigated_high = **High**; first item overall High), `stale_health` **absent** (just snapshotted), each item deep-links to `/projects/PRJ-ACT?tab=…`, `summary.total` = item count; (3) a `project_action` SSE event for **both** `project_red` and `risk_unmitigated_high`; (4) `change_order_pending` **and** `no_baseline` clear from the list; (5) `stale_days` echoed = 1. | High | Detective | PROJ-11; 16 §7 (19) | Not Run | projects.ts |
| UAT-O2C-222 | Pipeline-weighted forward resource & cash forecast | PMO | Fixed project `PRJ-FC` (contract 100000) + a pending 25%-billing milestone due 2026-08-15; an OPEN opportunity 200000 @ 50% expected-close 2026-09-10 | 1. `GET /api/projects/forecast?from=2026-07&months=6`. 2. read `billing.monthly` for Aug / Sep. 3. read `billing.*_total` + `pipeline.weighted_forecast`. 4. read `resourcing.monthly`. | as left | (2) **Aug `committed_billing`=25000** (25% of contract), **Sep `weighted_pipeline`=100000** (200000 × 50%); (3) `committed_total`=25000, `weighted_pipeline_total`=100000, `expected_total`=125000, `weighted_forecast` ≥ 100000; (4) 6-month resourcing band, each with a `committed_demand_pct` + an `over_allocated_count`. | Med | Positive | PROJ-05/PROJ-06 (operational); 16 §7 (20) | Not Run | projects.ts |
| UAT-O2C-223 | Period governance / status pack | PMO | green project `PRJ-EVM` (CPI 1.11, ≥2 health snapshots); portfolio incl. a red project | 1. `GET /api/projects/PRJ-EVM/governance-pack`. 2. `GET /api/projects/governance-pack`. 3. subscribe+run BI `project_governance_pack`. | as left | (1) `scope:project`, `rag:green`, `evm.cpi`≈1.11, **all sections present** (`health_trend` ≥2, `baseline`, `risks.summary`, `milestones`, `change_orders`); (2) `scope:portfolio`, RAG-ranked rows (**red first**), roll-up `summary` with ≥1 red + unmitigated-high + overdue-milestone; (3) run `status:success`, summary "Governance pack …". | Med | Detective | PROJ-06 (operational); 16 §7 (21) | Not Run | projects.ts |
| UAT-O2C-224 | Program (cross-project) critical path | PMO | program `PG-1` of 4 projects (A=10d, B=5d, C=3d, D=2d), deps A→B→C and A→D | 1. `PATCH …/program` to set `program_code`+`depends_on_projects` on each. 2. `GET /api/projects/program-critical-path?program=PG-1`. 3. `GET /api/projects/programs`. 4. set a self-dependency. 5. set a dependency on a missing project. | as left | (2) `program_duration_days`=18, `critical_path`=[A,B,C], A.es=0/B.es=10/C.es=15, **D off-path** with `slack`=6; (3) PG-1 listed, `member_count`=4, duration 18; (4) **400 `BAD_DEPENDENCY`**; (5) **400 `DEP_PROJECT_NOT_FOUND`**. | Med | Detective | PROJ-06 (operational); 16 §7 (22) | Not Run | projects.ts |
| UAT-O2C-225 | Pipeline → FTE resourcing forecast | PMO | open opportunity 200000 @ 50% expected-close 2026-09 (→ 100000 weighted in Sep) | 1. `GET /api/projects/forecast?from=2026-07&months=6` (default rate). 2. read `resourcing.monthly` for Sep. 3. `GET …&rev_per_fte_month=100000`. | as left | (1/2) `rev_per_fte_month`=200000, Sep `pipeline_demand_fte`=0.5 (100000÷200000), `total_demand_fte`=`committed_demand_fte`+0.5, `peak_total_demand_fte` present; (3) `rev_per_fte_month`=100000 → Sep `pipeline_demand_fte`=1.0 (doubles). | Med | Positive | PROJ-05/PROJ-06 (operational); 16 §7 (20) | Not Run | projects.ts |
| UAT-O2C-226 | Project period-end close review — maker-checker (via `/projects/close`) | exec preparer + a **different** exec approver | tenant with open projects carrying WIP | 1. `POST /api/projects/close-review?period=2026-06`. 2. `POST …/close-review/2026-06/approve` **as the preparer**. 3. approve **as a different exec**. 4. `POST …/close-review/2026-05` then `…/reject` {reason}. 5. `POST …/close-review?period=bad`. 6. `GET …/close-reviews`. | as left | (1) status **`Prepared`**, `wip_total`/`clearing_balance`/`open_projects` snapshotted, `prepared_by` stamped; (2) **403 `SOD_VIOLATION`** (approver ≠ preparer); (3) status **`Approved`**, `approved_by` = the 2nd user; (4) status **`Rejected`**, `rejection_reason` stored; (5) **400 `BAD_PERIOD`** (must be YYYY-MM); (6) both periods listed newest-first. | High | Control | PROJ-03; 16 §7, §9 | Not Run | projects.ts |
| UAT-O2C-227 | Timesheet project allocation + approval surfaced in the list (`/hcm`) | HCM user + a different approver | employee `EMP-TS`, project `PRJ-TS` with a task | 1. `POST /api/hcm/timesheets` {emp, 8h, `project_code:PRJ-TS`, `task_id`, `billable:true`}. 2. `GET /api/hcm/timesheets`. 3. `POST …/timesheets/{id}/approve` as a different user. 4. re-`GET`. | as left | (1) row created **Pending**; (2) list row exposes `id`, `project_code`=`PRJ-TS`, `task_id`, `billable:true`, `status:Pending`; (3) posts labour to project WIP once, returns `entry_no`; (4) row now `status:Approved` with `entry_no` shown (self-approval still → `SOD_SELF_APPROVAL`). | Med | Control | PROJ-04; 16 §7 (10), §9 | Not Run | hcm.ts / projects.ts |
| UAT-O2C-228 | Customer AR card — list → statement of account | AR clerk (`ar`/`exec`) | a customer with open AR (invoices + a receipt) | 1. Open **การ์ดลูกหนี้** (`/finance/customers`). 2. Confirm the list shows customers with an open balance (exposure, worst days-overdue, *ระงับเครดิต* badge). 3. Click a customer → statement. 4. Set the date range; read opening/charges/payments/closing + the running balance; export CSV. | — | List renders from `/api/finance/ar/credit-positions` (per-customer, carries `tenant_id`); clicking loads `/api/finance/ar/statement?tenant_id=…&from=&to=` — opening balance struck before *from*, every invoice (charge) + receipt (payment) in date order with a running `balance`, closing = opening+charges−payments; CSV downloads. Read-only (no posting). | Med | Positive | REV-01 / collections | Not Run | web UI (statement endpoint pre-tested) |
| UAT-O2C-229 | Bill of Quantities (BoQ) + project-dimensioned procurement (M0, docs/32) | project controller (author) + a **different** approver | project `PRJ-A` | 1. `POST /api/projects/PRJ-A/boq` {2 lines: material 100×150, labor 20×500}. 2. `POST …/boq/{id}/lines` {subcon budget_amount 5000}. 3. `POST …/boq/{id}/approve` **as the author**. 4. approve **as a different user**. 5. `GET /api/projects/PRJ-A`. 6. `POST …/boq/{id}/lines` again. 7. `POST …/boq/lines/{matLineId}/remeasure` {110}. 8. `POST …/boq/{id}/lock`, then re-measure. 9. `POST /api/procurement/prs` {`project_code:PRJ-A`, line `boq_line_id`}. 10. `POST /api/procurement/prs` {`project_code:PRJ-NOPE`}. | as left | (1) BoQ **draft**, 2 lines, `budget_total`=25000; (2) 3 lines, total 30000; (3) **400 `SOD_SELF_APPROVAL`**; (4) status **approved**, project `budget_amount` **synced to 30000**; (5) `boq.status`=approved, `boq.budget_total`=30000; (6) **400 `BOQ_NOT_DRAFT`**; (7) `remeasured_qty`=110, `remeasure_variance_qty`=+10; (8) locked, re-measure → **400 `BOQ_LOCKED`**; (9) PR persists `project_id`(PRJ-A)+`boq_line_id`; (10) **404 `PROJECT_NOT_FOUND`**. | High | Control | maker-checker (BoQ approve); PROJ-12 (M1); 16 §7 (23–24) | Not Run | projects.ts |
| UAT-O2C-230 | Material-budget commitment enforcement (M1, docs/32, PROJ-12) | procurement (`procurement`/Admin) | project `PRJ-A` with an approved BoQ line (CEMENT, budget 15000, 0 committed) | 1. `POST /api/procurement/pos` {`project_code:PRJ-A`, line CEMENT 50×150=7500, `boq_line_id`}. 2. `GET /api/projects/PRJ-A/boq`. 3. `POST …/pos` {CEMENT 60×150=9000, same line}. 4. `POST …/pos` {CEMENT 50×150=7500, same line}. 5. `PATCH /api/procurement/pos/{po1}/cancel` {reason}. 6. `GET /api/projects/PRJ-A/commitments`. | as left | (1) PO created (encumbers 7500); (2) line `committed`=7500, `remaining`=7500; (3) **400 `BUDGET_EXCEEDED`** and **no PO row created** (draw rolled back atomically — the FOR UPDATE lock serialises concurrent draws so they can't jointly overrun); (4) PO created → line `remaining`=0; (5) cancel releases the encumbrance → `remaining` restored to 7500; (6) ledger summary `open`=7500 (PO#4), `released`=7500 (PO#1), `committed`=7500. | High | Control (preventive) | PROJ-12; 16 §7 (25) | Not Run | projects.ts |
| UAT-O2C-231 | Material requisition + over-budget LINE approval (M2, docs/32, PROJ-13) | requester (`pr_raise`) + a **different** authoriser (`procurement`/`exec`) | project `PRJ-A`, BoQ line CEMENT (remaining 7500) | 1. `POST /api/pmr` {`project_code:PRJ-A`, line CEMENT 10×100=1000}. 2. `POST /api/pmr` {CEMENT 100×100=10000}. 3. `GET /api/projects/action-center`. 4. `POST /api/pmr/{pmrNo}/approve` **as the requester**. 5. approve **as a different authoriser** (or one-tap in LINE). 6. `GET /api/projects/PRJ-A/boq`. 7. re-`GET` action-center. | as left | (1) within budget → status **routed**, a project-tagged **PR** raised (`linked_doc_no` `PR-…`); (2) over budget → status **pending**, `over_amount`=2500, no PO yet; (3) worklist shows **`pmr_over_budget`** (high) for the PMR; (4) **400 `SOD_SELF_APPROVAL`**; (5) status **approved**, a **Draft** project-tagged PO auto-drafted (`linked_doc_no` `PO-…`); (6) the BoQ line is now `committed`=17500 / `remaining`=**−2500** (authorised overage); (7) `pmr_over_budget` cleared. | High | Control | PROJ-13; 16 §7 (26) | Not Run | projects.ts |
| UAT-O2C-232 | Stock reservation → issue-to-project (M3, docs/32, INV-13) | warehouse (`wh_custody`/`warehouse`) | project `PRJ-A`; item `STEEL` on hand 100 @ ฿50 at WH-MAIN | 1. `GET /api/reservations/available?item_id=STEEL`. 2. `POST /api/reservations` {`project_code:PRJ-A`, STEEL 30}. 3. re-`GET` available. 4. `POST /api/reservations` {STEEL 80}. 5. `POST /api/reservations/{id}/issue`. 6. `GET /api/ledger/trial-balance`. 7. re-`GET` available. 8. reserve 20 then `POST …/{id2}/release`. | as left | (1) `on_hand`=100, `held`=0, `available`=100; (2) reservation **held**, `available_after`=70; (3) `held`=30, `available`=70; (4) **400 `INSUFFICIENT_STOCK`** (30 held + 80 > 100 — no double-allocation); (5) reservation **consumed**, `value`=1500, GL **Dr 1260 project WIP (`project_id`) / Cr 1200 Inventory**; (6) TB balanced, 1260 +1500 / 1200 −1500; (7) `on_hand`=70, `available`=70; (8) release → the hold is **freed**, `available` back to 70. | High | Control (preventive) | INV-13; 16 §7 (27) | Not Run | projects.ts |
| UAT-O2C-233 | Project-linked advances & reimbursements — site cash (M4, docs/32, PROJ-14) | finance (`creditors`/`exec`) | project `PRJ-A` | 1. `POST /api/finance/advances` {payee, amount 2000, `project_code:PRJ-A`}. 2. inspect the GL (journal_lines). 3. `POST /api/finance/advances/{advanceNo}/settle` {settled_expense 2000}. 4. inspect the GL. 5. `GET /api/projects/PRJ-A/site-cash`. 6. `POST /api/finance/advances` {`project_code:PRJ-NOPE`}. | as left | (1) advance issued, `project_id`=PRJ-A; (2) **Dr 1180 line carries `project_id`** (Cr 1000); (3) settled; (4) the settlement **Dr 5100 expense line carries `project_id`** (clears 1180); (5) rollup lists the advance, `totals.advances`=2000; (6) **404 `PROJECT_NOT_FOUND`**. | Med | Control | PROJ-14; 16 §7 (28) | Not Run | projects.ts |
| UAT-O2C-234 | Raise site cash from the project workspace — web (FU4, docs/32, PROJ-14) | project user (web) + finance (`creditors`/`exec`) for advance; petty-cash approver for the request | project `PRJ-A` with an approved BoQ line; one established petty-cash fund | 1. Open `/projects/PRJ-A?tab=sitecash`. 2. **ออกเงินทดรอง** → payee, amount 2000, link BoQ line CEMENT → **ออกเงินทดรอง** (`POST /api/finance/advances` {`project_code:PRJ-A`, `boq_line_id`}). 3. **ขอเงินสดย่อย** → pick fund, kind `expense`, amount 500, link CEMENT → **ส่งคำขอ** (`POST /api/finance/petty-cash/requests`). 4. settle the advance / approve the petty-cash request in finance. 5. re-read the **BoQ** + **เงินสดหน้างาน** tabs. | as left | (2) advance appears in the Advances table, `totals.advances`+2000, GL advance line carries `project_id`; (3) request lands **pending** in petty-cash (maker-checker), tagged to the project; (4) settlement/approval posts a **consumed** commitment on the CEMENT line; (5) the BoQ line **คงเหลือ** drops by the settled/approved amount — site cash consumes the BoQ ceiling (FU1). | Med | Usability + Control | PROJ-14; FU4; 16 §7 (28) | Not Run | walkthrough (manual) |
| UAT-O2C-235 | Print / email a ใบเสนอราคา (Quotation) | Sales (`exec`) | a quote `QT-…` (e.g. total ฿58,000) for a customer | 1. `GET /api/cpq/quotes/{id}/pdf`. 2. inspect the document. 3. `POST /api/cpq/quotes/{id}/send-email {to_email}` (with SMTP configured). 4. (no-SMTP env) repeat step 3. | as left | (1) 200; `application/pdf` (or `text/html` in the Chromium-absent fallback); shows **ใบเสนอราคา**, the customer, the priced lines + net total ฿58,000 and the baht-text total. (3) emailed to the customer as a PDF attachment; the quote moves **Draft→Sent**. (4) with no `SMTP_*`/`MAIL_FROM` the endpoint returns **`EMAIL_NOT_CONFIGURED` (503)** — the render+mail path is wired, it only needs a mail account. Presentation only, no GL. | Med | Positive | CPQ-01 (presentation; no new control) | Pass | cpq.ts |
| UAT-O2C-253 | Quote author cannot self-accept a billable quote (SOD_VIOLATION); no revenue posts (G12) | Sales/Exec (quote author **A**, `exec`) | a **billable** quote `QT-…` (`total`=50000) created by user **A**, status Sent; the ledger is wired | 1. User **A** `POST /api/cpq/quotes/:id/accept`. 2. `GET /api/ledger/trial-balance`. | — | (1) **403 `SOD_VIOLATION`** — the acceptor equals the quote's `createdBy`, so the author cannot accept their own quote; the quote is **not** accepted and **no** `CPQ-WIN` revenue JE posts. (2) no AR (1100) / revenue (4000) movement from the quote. Uses the existing `quotes.createdBy` — no migration; the ledger-less standalone quote pipeline (no ledger) is a pure status transition and is unaffected. | High | Control | R07, R10 / CPQ-03 | Not Run | cpq-gl.ts |
| UAT-O2C-254 | A distinct exec accepts the billable quote → Dr 1100 AR / Cr 4000 posts 50000, TB balanced (G12) | Exec (acceptor **B**) ≠ author | the same billable quote (`total`=50000), still Sent, author **A**; acceptor **B** ≠ A | 1. User **B** `POST /api/cpq/quotes/:id/accept`. 2. `GET /api/ledger/trial-balance`. | — | (1) 200; the quote moves **Sent → Accepted** and a balanced `CPQ-WIN` JE posts **Dr 1100 AR 50000 / Cr 4000 Sales Revenue 50000** (ref = quote no; idempotent on re-accept). (2) **TB balanced**; revenue is recognised by a second person (distinct-actor guard satisfied). | High | Control | CPQ-03, GL-01 / R07, R10 | Not Run | cpq-gl.ts |
| UAT-O2C-236 | Print / email a ใบส่งของ (Delivery Note) | Logistics (`delivery`) | a delivery order `DO-…` with item lines | 1. `GET /api/delivery/{doNo}/pdf`. 2. inspect. 3. `POST /api/delivery/{doNo}/send-email {to_email}`. | as left | (1) 200; shows **ใบส่งของ**, ship-to address, driver/vehicle, the item lines (no prices — a goods-movement doc) and a receiver-signature block; PDF or HTML fallback. (3) emailed to the customer as a PDF attachment (`EMAIL_NOT_CONFIGURED` 503 until SMTP set). Read-only, no GL. | Med | Positive | REV (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-237 | Print / email a ใบแจ้งหนี้/ใบวางบิล (AR billing invoice) | AR clerk (`ar`/`exec`) | an AR invoice `INV-…` (e.g. ฿1,000, part-paid) | 1. `GET /api/finance/ar/invoices/{invoiceNo}/pdf`. 2. inspect. 3. `POST /api/finance/ar/invoices/{invoiceNo}/send-email {to_email}`. | as left | (1) 200; shows **ใบแจ้งหนี้/ใบวางบิล** (distinct from the statutory ใบกำกับภาษี), the customer + tax ID, the lines drawn from the linked sales order, the amount billed, **ชำระแล้ว** and **คงเหลือค้างชำระ**; PDF or HTML fallback. (3) emailed as a PDF attachment (`EMAIL_NOT_CONFIGURED` 503 until SMTP set). Read-only, no GL; the ใบกำกับภาษี path is unchanged. | Med | Positive | REV-01 (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-238 | Print / email a ใบแจ้งยอดบัญชี (Statement of account) | AR clerk (`ar`/`exec`); AP uses `creditors`/`exec` | a customer with invoices + a receipt over a date range | 1. `GET /api/finance/ar/statement/pdf?tenant_id=&from=&to=`. 2. inspect. 3. `POST /api/finance/ar/statement/send-email?tenant_id=&from=&to= {to_email}`. 4. (AP) `GET /api/finance/ap/statement/pdf?vendor=`. | as left | (1) 200; shows **ใบแจ้งยอดบัญชี**, opening balance, every charge/payment line in date order with a running balance, and the closing balance (baht text for THB); PDF or HTML fallback. (3) emailed as a PDF attachment (`EMAIL_NOT_CONFIGURED` 503 until SMTP set). (4) the AP/vendor statement renders the same way. Read-only, no GL. | Med | Positive | REV-01 (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-239 | Print / email a ใบสำคัญรับเงิน (AR receipt voucher) | AR clerk (`ar`/`exec`) | an `ar_receipts` row `RCP-…` (e.g. ฿400) | 1. `GET /api/finance/ar/receipts/{receiptNo}/pdf`. 2. inspect. 3. `POST /api/finance/ar/receipts/{receiptNo}/send-email {to_email}`. | as left | (1) 200; shows **ใบสำคัญรับเงิน**, the payer (customer), the referenced invoice, the amount received ฿400 (baht text) and the payment method; PDF or HTML fallback. (3) emailed as a PDF attachment. Distinct from the POS receipt. Read-only, no GL. | Med | Positive | REV-01 (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-240 | Print / email a หนังสือทวงถามหนี้ (Dunning / collection letter) | AR clerk (`ar`/`exec`) | an overdue invoice `INV-…` with at least one recorded dunning action | 1. `GET /api/finance/ar/collections/{invoiceNo}/dunning-letter/pdf`. 2. inspect. 3. `POST /api/finance/ar/collections/{invoiceNo}/dunning-letter/send-email {to_email}`. | as left | (1) 200; renders a formal **หนังสือทวงถามหนี้** from the latest `ar_dunning_log` action — the customer, the invoice, the amount overdue (baht text), days overdue and the stage-appropriate wording (legal-stage escalation for the `legal` rung); PDF or HTML fallback. (3) emailed as a PDF attachment. **Recording** a dunning action is unchanged (`recordDunning`); this only prints/sends the letter. Read-only, no GL. | Med | Positive | REV-08 (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-241 | AR document email defaults the recipient to the customer's email on file | AR clerk (`ar`/`exec`) | a customer with an email on file (customer master) + a statement over a date range | 1. `POST /api/finance/ar/statement/send-email?tenant_id=&from=&to=` with **no** `to_email`. 2. same call with an explicit `{to_email}`. 3. (Negative) a party with **no** email on file, no `to_email`. | as left | (1) the recipient **defaults to the customer's email on file** → the send reaches the (unconfigured) transport `EMAIL_NOT_CONFIGURED` (503) — NOT a validation error, proving `to_email` is optional and the master default resolved. (2) the explicit address overrides the default. (3) `400 NO_RECIPIENT`. Same behaviour on the ใบแจ้งหนี้ / receipt / dunning email endpoints. | Med | Control | REV-01 (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-242 | Recent AR receipts list surface (print / email each ใบสำคัญรับเงิน) | AR clerk (`ar`/`exec`) | ≥1 receipt recorded (`RCP-…`) | 1. `GET /api/finance/ar/receipts`. 2. On the finance `/` **รายรับ (AR)** tab, use **พิมพ์** / **ส่งอีเมล** on a receipt row (leave the email prompt blank). | — | (1) 200; a list of recent receipts (`receipt_no`, date, invoice, amount, method), tenant-scoped. (2) print opens the ใบสำคัญรับเงิน PDF; email sends it as a PDF — a blank recipient defaults to the customer's email on file. Read-only, no GL. | Med | Positive | REV-01 (presentation; no new control) | Pass | basics.ts |
| UAT-O2C-246 | Tender / estimating → award — seed a project + draft BoQ from the winning bid (docs/35 P3, PROJ-18) | Estimator (`proj_tender`/`marketing`) + BoQ approver (`gl_close`/`exec`) | — | 1. `POST /api/tenders {title, project_code:PRJ-TND, markup_pct:20, lines:[{qty:10,unit_cost:1000},{qty:5,unit_cost:2000}]}`. 2. `POST …/lines {qty:2, unit_cost:3000, markup_pct:0}`. 3. `POST …/submit`. 4. `POST …/award` (before won). 5. `POST …/outcome {outcome:won}`; `POST …/award`. 6. `GET /api/projects/PRJ-TND` + `…/boq`. 7. approve the BoQ (different user). 8. re-award. 9. `POST …/outcome {won}` again. 10. a 2nd tender marked lost without a reason; then with a reason; `GET /api/tenders`. | as left | (1) estimating; estimated_cost 20000, bid_price 24000 (20% markup). (2) bid_price 30000 (line bid_rate 3000, per-line markup 0). (3) submitted. (4) `400 TENDER_NOT_WON`. (5) won → award seeds project **PRJ-TND** (Fixed, contract 30000) + a **DRAFT** BoQ (rate = bid_rate). (6) the project + a 3-line draft BoQ exist (L1 rate 1200). (7) approve → project budget baseline 30000 (controlled). (8) `already:true` — no duplicate project. (9) `400 TENDER_DECIDED`. (10) `400 LOSS_REASON_REQUIRED` then lost; register win-rate 50%. No GL — estimating is a modelling surface. | High | Control | PROJ-18 (new) | Pass | projects.ts |
| UAT-O2C-245 | Subcontractor management — subcontract vs BoQ budget, valuation certify, retention payable (docs/35 P2, PROJ-17) | Preparer (`proj_subcon`/`procurement`) + Certifier (`proj_subcon_certify`/`gl_close`) | a project `PRJ-SUB` with an approved BoQ (line 60000) | 1. `POST /api/subcontracts {project_code:PRJ-SUB, retention_pct:10, scope:[{boq_line_id:L1, amount:40000}]}`. 2. `GET /api/projects/PRJ-SUB/commitments`. 3. (Negative) a 2nd subcontract for 30000 on the same line (would exceed the 60000 budget). 4. `POST /api/subcontracts/{subNo}/valuations {pct_complete:50}`. 5. preparer certifies own valuation. 6. certifier `POST …/valuations/{valNo}/certify`. 7. `GET /api/retention/project/PRJ-SUB`. 8. a 2nd valuation pct 80 with back_charge 1000; certify. 9. a valuation with an excessive back-charge. 10. `GET /api/subcontracts/project/PRJ-SUB`. | as left | (1) contract_value 40000, retention 10%, active. (2) committed 40000 on the BoQ line (source SUBCON). (3) `400 BUDGET_EXCEEDED` (subcontract rolled back). (4) draft valuation: gross 20000, retention 2000, net 18000. (5) `400 SOD_SELF_APPROVAL`. (6) certified — JE **Dr 1260 20000 / Cr 2000 18000 / Cr 2440 2000**; wip_cost 20000. (7) retention 2000 withheld as a subcontractor **payable** (outstanding 2000). (8) gross **12000** (32000 to-date − 20000 prior); net 9800 (12000 − 1200 retention − 1000 back-charge); certify → wip_cost 11000. (9) `400 BAD_BACK_CHARGE`. (10) subcontract_value 40000, certified_to_date 32000, retention_payable 3200. *(Depth-2/P5: a subcontract also takes `wht_pct` + `vat_pct` → the certified valuation withholds Thai WHT (ภ.ง.ด.53, Cr 2361) and books recoverable input VAT (Dr 1300) so AP = net − WHT + VAT.)* | High | Control | PROJ-17 (new; SoD R18) | Pass | projects.ts |
| UAT-O2C-244 | Progress billing / งวดงาน — BoQ-line valuation, maker-checker certify, retention (docs/35 P1, PROJ-16) | Preparer (`proj_billing`) + Certifier (`proj_billing_certify`/`gl_close`) | a Fixed project `PRJ-PB` (contract 90000) with an approved 2-line BoQ (60000 + 40000) and 20000 WIP | 1. `POST /api/progress-billing {project_code:PRJ-PB, retention_pct:10, lines:[{boq_line_id:L1, pct_complete_to_date:50}, {L2:0}]}`. 2. (Negative) preparer certifies own claim. 3. certifier `POST /api/progress-billing/{claimNo}/certify`. 4. re-certify. 5. `GET /api/retention/project/PRJ-PB`. 6. a 2nd claim L1@80%; certify. 7. re-claim L1@80%. 8. a claim taking the contract over 90000; certify. 9. `GET /api/progress-billing/project/PRJ-PB`. | as left | (1) draft claim: gross 30000 (50% of 60000), retention 3000, net 27000. (2) `400 SOD_SELF_APPROVAL`. (3) certified — JE **Dr 1100 27000 / Dr 1170 3000 / Cr 4200 30000**, WIP relieved **Dr 5800 20000 / Cr 1260 20000**; cost_recognized 20000. (4) `400 CLAIM_NOT_DRAFT`. (5) retention 3000 withheld into the sub-ledger (receivable outstanding 3000). (6) claim 2 gross **18000** (48000 to-date − 30000 previously certified) — the cumulative model nets off the prior claim; certify → net 16200, retention 1800. (7) `400 NOTHING_TO_BILL`. (8) `400 BILL_EXCEEDS_CONTRACT`. (9) certified_to_date 48000, retention_withheld 4800. *(Depth-2/3: a claim also takes a `vat_pct` → output VAT (Cr 2100), AR = net + VAT; and on a `rev_method=poc` project the claim only bills — Cr 1265/2410, no 4200 revenue — so it never double-recognises with `recognizePoc`.)* | High | Control | PROJ-16 (new; SoD R17) | Pass | projects.ts |
| UAT-O2C-247 | Credit-limit change — requester cannot self-approve (SoD, G7) | CreditManager (requester A) | A pending credit-limit change staged by user A (UAT-O2C-148) | 1. User **A** `POST /api/finance/ar/credit-limit/:reqNo/approve`. | — | `403 SOD_VIOLATION`; the request stays `PendingApproval`; the limit is still unchanged. | High | Control | REV-08, R09 | Not Run | basics.ts |
| UAT-O2C-248 | Credit-limit change — a distinct approver applies it (→ 50000) | Approver B (`approvals`/`exec`) ≠ requester | The same pending change; approver **B** = `mgr` | 1. Approver **B** `POST /api/finance/ar/credit-limit/:reqNo/approve`. 2. Re-read the customer's limit. | — | 200/201 with `{status:"Approved", new_limit:50000, approved_by:"mgr"}`; **only now** does the customer's `creditLimit` become **50000** (`approvedAt` stamped). A reject path (`…/:reqNo/reject`) and a `…/credit-limit/pending` queue also exist. | High | Control | REV-08, R09 | Not Run | basics.ts |
| UAT-O2C-249 | Credit-change audit shows the limit_change old→new | ArClerk / CreditManager | Change from UAT-O2C-148 approved via UAT-O2C-248 | 1. `GET /api/finance/ar/credit-events?tenant_id=<cust>`. | — | The credit-events trail contains a `limit_change` event with `old_limit:100000 → new_limit:50000` (now Approved) alongside the prior `hold`/`release` events, each stamped with the acting/approving user. | High | Detective | REV-08, R09 | Not Run | basics.ts |
| UAT-O2C-243 | Retention (เงินประกันผลงาน) shared sub-ledger — withhold / release / due (docs/35 Phase 0) | Controller (`gl_close`/`exec`) | a project `PRJ-A`; retention GL accounts seeded | 1. `GET /api/ledger/accounts` — confirm **1170** (Asset) and **2440** (Liability). 2. `POST /api/retention/withhold {party_type:customer, project_code:PRJ-A, source_doc_no:CLAIM-1, amount:500}` then a subcontractor withhold `{party_type:subcontractor, amount:1000, schedule:[{due_basis:date, due_date:2020-01-01, pct:50},{due_basis:dlp_end, pct:50}]}`. 3. `GET /api/retention/project/PRJ-A`. 4. `POST /api/retention/{custId}/release {amount:200}`. 5. (Negative) `POST /api/retention/{custId}/release {amount:5000}`. 6. `GET /api/retention/due`, then release that tranche by `tranche_id`. 7. post + approve a JE Dr 1170 / Cr 2440 and `GET /api/ledger/cash-flow`. | as left | (1) both accounts present with the right type. (2) withhold returns gl_account 1170 / 2440 and the withheld amount. (3) receivable outstanding 500, payable outstanding 1000. (4) `partially_released`, released 200, **outstanding 300**. (5) `400 RETENTION_OVER_RELEASE` (cannot exceed outstanding — `FOR UPDATE` row-lock). *(Depth-1: a successful release posts a balanced JE — customer Dr 1100 / Cr 1170; subcontractor Dr 2440 / Cr 2000 — and an overdue tranche surfaces on the action center as `retention_due`.)* (6) the overdue date-tranche (500) is **due** (the `dlp_end` tranche is excluded); releasing it drops it off the worklist and reduces payable outstanding to 500. (7) 1170 & 2440 classify under **operating** working capital in the indirect SCF (not `unclassified`). Sub-ledger records balances only — no GL is posted by withhold/release. | High | Control | Foundation for PROJ-16 / PROJ-17 (docs/35; no new control in P0) | Pass | projects.ts |
