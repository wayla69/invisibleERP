# UAT — Cycle 18: Supply Chain Planning — Demand Forecasting & Replenishment (SCM-01 / SCM-02 / SCM-03)

**Status: DRAFT v0.13 · 2026-07-23** · *v0.13: adds §18 optimal (MinT) reconciliation + coherent probabilistic-paths cases (UAT-SCM-068/069) for docs/58 Track C · C3/C4 — NO new control, contract unchanged (`SCM_ENGINE_CONTRACT_VERSION` stays '2'; the `method`/`covariance` enums already carried `mint` + `ols`/`wls_struct`/`wls_var`/`shrink`), NO migration. The engine's `reconcile.py` gains real **`mint`** minimum-trace reconciliation (`G=(SᵀW⁻¹S)⁻¹SᵀW⁻¹`, oblique projector `P=S·G`); `service.run_forecast` forecasts each aggregate node's summed leaf history INDEPENDENTLY so MinT differs from bottom-up and beats it at the aggregate on a known-truth hierarchy; **C4** applies `P` per scenario/day to the sample paths (clip-at-0 + renormalize each root subtree). `W` is estimated from the base forecasts' predictive dispersion — no new query path. MinT requires the Python engine (in-process/stub engines do BU/TD only), so this is engine-capability + engine-pytest coverage (14 reconcile tests / 101 full suite); the harness keeps pinning coherence via bottom-up, and the production API keeps sending `bottom_up` (the MinT flip is a deferred one-line policy step). PN-34 §7.11b, manual ch.21.* · *v0.12: adds §17 scheduled batch retrain + forecast-source cases (UAT-SCM-066/067) for docs/59 Track D · D1 — NO new control, contract unchanged (`SCM_ENGINE_CONTRACT_VERSION` stays '2'). A schedulable **`scm_batch_retrain`** job forecasts every planning-enabled series (`executePlanRun(scope='retrain')`) and **persists the reconciled sample paths** (migration **0477** — additive `scm_demand_forecasts.sample_paths` jsonb + the `uq_scm_retrain_run` partial-unique index, both on existing tables → no RLS loop/grant); a **nightly** plan run then reuses a recent retrain's persisted paths **all-or-nothing per branch** (only when a fresh `scope='retrain'` forecast newer than `SCM_FORECAST_STALENESS_HOURS` (default 24, new env) covers the whole branch — already reconciled, so no partial mix/re-reconciliation) via `loadFreshMenuPaths` (`scm-forecast-source.ts`), a miss falling through to a full fresh forecast; idempotent per (tenant, run_date) so a duplicate retrain is a no-op. NO GL, no golden-master path. ToE `scm` +3 D1 checks (60/60, `scm-mfg` shard). PN-34 §7.19, manual ch.21 §2.* · *v0.11: adds §16 cold-start / analog forecasting cases (UAT-SCM-064/065) for docs/56 Track A · A4 — a brand-new SKU below the 56-day Prophet floor borrows the pooled, normalized demand shape of **established same-branch siblings** (≥ 90 days, ≤ 5 donors) via the already-reserved `analog_of` per-series input, and the engine (two-phase fan-out — donors first — `forecasting.py _analog_paths`) rescales it to the new item's own baseline seed; flagged **`analog`** in `attribution.regressors_used` (persisted to `scm_demand_forecasts.regressorsUsed`) so a reviewer sees it is borrowed, not observed. The analog branch fires only when `analog_of` is set AND own history < 56 days AND ≥1 donor present; else normal routing (no-donor fallback), and a run with no cold-start series is byte-identical to pre-A4. Contract UNCHANGED — no `SCM_ENGINE_CONTRACT_VERSION` bump (stays '2'). NO migration, NO new control (governed by SCM-01/02/03). ToE `scm` new-item case + engine pytest (borrow+flag / no-donor fallback / ignored-when-sufficient / two-phase). PN-34 §7.17, manual ch.21 §1. (docs/59 Track D · D3 — shared fail-open Redis engine `ResultCache` — is infrastructure with no user flow: ops-verified via a load run, not a functional UAT gate.)* · *v0.10: adds §15 warm-start / model-registry cases (UAT-SCM-061..063) for docs/59 Track D · D2 — `scm_model_cache` (migration 0475, canonical RLS + leading `(tenant_id, branch_id, item_id)` index + `coalesce(branch_id,0)` unique) caches each series' serialized Prophet fit; a run whose training window is unchanged AND within `refit_cadence_days` (new additive `scm_settings` column, default 14, range 1–90) ships the cached fit as an OPTIONAL `warm_start` and the engine reports a warm hit — reusing the fit and skipping BOTH the primary fit and the backtest refit, byte-identically to a cold fit (determinism); two fail-safe staleness guards (`fit_hash` mismatch + cadence age) force a refit; a warm hit carries `fit_wape` forward; a corrupt cache fails closed to a refit; cross-tenant → 0 rows. Contract is ADDITIVE — no `SCM_ENGINE_CONTRACT_VERSION` bump (stays '2'). NO new control (SCM-07 belongs to D4). ToE `scm` +4 D2 checks (57/57, `scm-mfg` shard) + engine pytest (reproducibility / refit-on-window-change / corrupt-cache). PN-34 §7.16, manual ch.21 §2.* · *v0.9: adds §14 multi-echelon network-plan cases (UAT-SCM-057..060) for docs/57 Track B · B2 — control **SCM-05** (network-plan maker-checker): `POST /api/scm-network/plans/run {item_code}` builds a Draft two-echelon plan pooling safety stock at the DC (via the B2a `/v1/optimize-network` engine route, or the in-process fallback `B=μ·(L+R)+z·σ·√(L+R)` with DC echelon = Σ of branches, no pooling; every engine quantity zod-validated + clamped); submit → approve by a DIFFERENT `scm_approve` holder (self-approval → `403 SOD_SELF_APPROVAL`, maker bound to the SUBMITTER; SoD **R24** reused — no new rule) → convert rolls the DC order up to a PR via the existing `ProcurementService.createPr` seam (reason `SCM-NET`, idempotent by `pr_no`); persisted to `scm_network_plans`/`scm_network_plan_lines` (migration 0474, canonical RLS + tenant-leading index); cross-tenant read → 404. No GL, no golden-master money path. PN-34 §7.15, manual ch.21 §2. ToE `scm` +8 B2 checks (`scm-mfg` shard).* · *v0.8: adds §13 cross-price-elasticity (cannibalization/halo) cases (UAT-SCM-055/056) for docs/56 Track A · A3 — category-scoped γ estimated API-side (log-log, same identifiability floor as A2) and persisted to `scm_cross_elasticity` (`GET /api/scm-planning/cross-elasticity`); the scenario what-if composes own ε + Σ sibling γ, applied ONLY to same-category items whose price moved in the scenario; tenant-scoped; no new control. Estimator proven in apps/api vitest. PN-34 §7.14, manual ch.21 §7.* · *v0.7: adds §12 own-price-elasticity cases (UAT-SCM-052/053/054) for docs/56 Track A · A2 — a governed promo price cut identifies ε (engine OLS log-log with an identifiability floor; ε=null when not identified), the run persists it to `scm_price_elasticity` (`GET /api/scm-planning/elasticity`), and the scenario what-if `price_multiplier` applies `demand × (price)^ε` (advisory); tenant-scoped; no new control. PN-34 §7.13, manual ch.21 §7.* · *v0.6: adds §11 supply-network master-data cases (UAT-SCM-049/050/051) for docs/57 Track B · B1 — declare `supply_nodes`/`supply_lanes` topology, `GET /api/scm-network/topology` validation (legal two-echelon DAG, echelon-skipping lane → `LANE_ENDPOINTS_INVALID`, unwired branch → `UNREACHABLE_BRANCH`, node-with-lanes delete → `NODE_HAS_LANES`), and the cross-tenant boundary; no control change (B1 adds none — SCM-05/06 arrive with B2/B3). PN-34 §7.12, manual ch.21.* · *v0.5: adds §10 forecast-reconciliation case (UAT-SCM-048) for docs/58 Track C · C2 — bottom-up/top-down reconciliation flows coherently end to end (aggregate == Σ leaves; malformed→degrade-to-base); no control change (Track C adds none). PN-34 §7.11.* · *v0.4: adds §9 promo/price-demand cases (UAT-SCM-045/046/047) for docs/56 Track A · A1 — control **SCM-04** (server-derived promo regressors from approved promotions; a fabricated promo cannot inflate a forecast; tenant-scoped). PN-34 §7.10, manual ch.21.* · *v0.3: adds §8 forecast-reconciliation-hierarchy cases (UAT-SCM-043/044) for docs/58 Track C · C1 — declare/synthesize aggregation structures via `/api/scm-planning/hierarchy`, cyclic-forest rejection (`SCM_HIERARCHY_INVALID`), and the cross-tenant boundary; no control change (Track C adds none). Manual ch.21 §2, PN-34 §7.9.* · *v0.2: adds §7 planner-workspace screen cases (UAT-SCM-033..042) for the docs/54 Phase 3 tabs on `/demand` — branch plans with the uncertainty band and the untagged-demand warning, order-plan edit/submit/approve/convert, the approve controls hidden without `scm_approve`, the advisory scenario tool that persists nothing, the spike feed, and full th/en coverage from the `scm.*` catalog. Manual chapter 21; no control or endpoint change.* · *v0.1: docs/54 Phase 2 — per-(branch, item) probabilistic demand planning + perishable-aware order optimization. New controls **SCM-01** (order-plan maker-checker: submit → approve by a DIFFERENT user holding `scm_approve` → convert to a purchase requisition, idempotent by `pr_no`), **SCM-02** (planning-job monitoring + database-enforced idempotency: one nightly run per business day, watermarked spike scan, per-day spike dedupe + cooldown), **SCM-03** (auditable demand-driven order sizing: channel-partitioned demand so a dine-in dish is counted ONCE, closed/stockout-day exclusion, per-scenario BoM explosion, shelf-life cap that holds even with the external engine disabled). Migration `0459`; new duties `scm_plan` / `scm_approve` with SoD **R24**. Cases UAT-SCM-001..020; ToE `scm` (20 checks). Cross-ref: process narrative `34-supply-chain-planning.md`, plan `docs/54-dynamic-scm-forecasting-plan.md`, engine `services/forecast-engine`.*

Result legend: Pass / Fail / Blocked / N/A / Not Run. All amounts THB. Error codes are exact and surface as `json.error.code`.

New duties: `scm_plan` (maker — maintain planning settings/policies, run planning, edit + submit plans, convert an approved plan) · `scm_approve` (checker — approve/reject order plans; a SUB-permission, so no coarse module key confers it). Role: **Planner** is seeded with `scm_plan` only. SoD **R24**: `scm_plan` vs `scm_approve`.

## §1 — Settings, policies and item shelf life (SCM-03)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-001 | Planning settings round-trip | Planner | — | 1. `PUT /api/scm-planning/settings`. 2. `GET /api/scm-planning/settings`. | `{horizon_days:7, service_level:0.95, sample_paths:20, closures:[{date:'<+3d>', reason:'ปิดปรับปรุง'}], dine_in_branch_id:<branch>, auto_replan:true}` | (1) 200. (2) The horizon, the closure and the dine-in branch attribution persist. | High | Positive | SCM-03 | Not Run | scm.ts |
| UAT-SCM-002 | Settings validation rejects out-of-range values | Planner | — | 1. `PUT .../settings` `{horizon_days:99}`. 2. `PUT .../settings` `{service_level:0.2}`. | — | Both **400** `VALIDATION_ERROR` (horizon 1–56; service level 0.5–0.9999). | Med | Negative | SCM-03 | Not Run | — |
| UAT-SCM-003 | Per-(branch,item) policy upsert + tenant isolation | Planner (T1) / Planner (T2) | Two tenants | 1. T1 `POST .../policies` `{item_id:'ING-CHK', shelf_life_days:3, service_level:0.9}`. 2. T1 `GET .../policies`. 3. T2 `GET .../policies`. | — | (1) 201. (2) 1 policy. (3) **0 rows** — no cross-tenant visibility (RLS). | High | Control | SCM-03 | Not Run | scm.ts |
| UAT-SCM-004 | Shelf life suggested from goods-receipt history | Planner | 3 receipts where expiry − receipt date = 3 days | 1. `GET .../items/shelf-life-suggestions`. 2. `POST .../items/shelf-life` `{item_id:'ING-CHK', days:3}`. | — | (1) `ING-CHK` suggested **3** days with `samples:3` and the current master value. (2) 201; `items.shelf_life_days` = 3 (shared master, deliberate gated write). | Med | Positive | SCM-03 | Not Run | scm.ts |
| UAT-SCM-005 | Shelf-life write rejects an implausible value | Planner | — | 1. `POST .../items/shelf-life` `{item_id:'ING-CHK', days:0}`. 2. `{days:99999}`. | — | Both **400** (`VALIDATION_ERROR` / `BAD_SHELF_LIFE`) — 1–3650 days only. | Low | Negative | SCM-03 | Not Run | — |

## §2 — Demand extraction and the channel partition (SCM-03)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-006 | **Buffet dedupe** — a dine-in dish is counted ONCE | Planner | A business day with a 10-dish retail sale AND a 4-dish dine-in order whose ฿0 buffet lines were copied into `cust_pos_items` at checkout | 1. Run a plan. 2. Compare the extracted demand for that day against a naive union of the POS and kitchen tables. | — | Extracted demand for the day = **14** (10 retail + 4 dine-in). A naive union yields **18** — the dine-in dish double-counted. The retail leg must exclude `payment_method IN ('Dine-in','Split')`. | **High** | **Control** | SCM-03 | Not Run | scm.ts — pins the rule so a checkout refactor that renames the settlement literals fails CI |
| UAT-SCM-007 | Non-dish lines never enter the demand signal | Planner | A service-charge / split-bill synthetic line exists | 1. Run a plan. 2. Inspect the menu-level forecasts. | — | Only SKUs carrying a recipe are forecast; charge refs and synthetic lines are absent. | Med | Control | SCM-03 | Not Run | — |
| UAT-SCM-008 | Closed days are excluded and forecast to zero | Planner | A closure configured inside the horizon | 1. Run a plan. 2. Inspect the forecast for the closed day. | — | The closed day's forecast demand is **0** and the day is excluded from the fitted history (it must not read as a demand collapse). | Med | Control | SCM-03 | Not Run | scm.ts |
| UAT-SCM-009 | Stockout-censored days do not suppress the forecast | Planner | 10 recent days flagged `stockout` with 0 sales | 1. Run a plan. | — | The forecast level stays near the true demand — censored days are excluded from the fit, so a stockout cannot teach the model to order less and prolong itself. | High | Control | SCM-03 | Not Run | engine pytest `test_stockout_days_are_excluded_from_the_fit` |
| UAT-SCM-010 | Dine-in attribution gap is visible, not silent | Planner | `dine_in_branch_id` unset | 1. Run a plan. 2. Read the run's metrics. | — | Restaurant demand pools in the untagged (NULL-branch) unit and `branch_null_share` reports the share, so the misconfiguration surfaces. | Med | Detective | SCM-03 | Not Run | — |

## §3 — Forecasting, order sizing and the engine boundary (SCM-03)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-011 | Planning run against the external engine | Planner | `SCM_ENGINE_URL` + `SCM_ENGINE_SECRET` set | 1. `POST /api/scm-planning/run`. | `{}` | 201; run `status:Completed`, `engine:external`; forecasts persisted per (branch, item); ≥1 Draft plan with ingredient lines. | High | Positive | SCM-03 | Not Run | scm.ts |
| UAT-SCM-012 | **Engine requests are signed; a wrong secret is refused** | Planner | Engine stub verifying HMAC | 1. Run with the correct secret. 2. Change `SCM_ENGINE_SECRET` and re-run. | — | (1) The engine receives a VALID `x-engine-signature` over `${unixSeconds}.${rawBody}` and answers. (2) The engine returns **401 `BAD_SIGNATURE`** and the run fails — the boundary is real, not decorative. | **High** | **Control/Security** | SCM-03 | Not Run | scm.ts |
| UAT-SCM-013 | **Fallback plans when the engine is unavailable, and the shelf-life cap still holds** | Planner | Engine env unset; stock drained; `ING-CHK` shelf life 3 days | 1. `POST .../run`. 2. Inspect the chicken line. | — | 201; run `engine:fallback`; lines are still produced; the chicken quantity ≤ **shelf_life × mean daily demand** with `detail.clamped` recording `shelf_life`. The perishable guard never depends on an optional service. | **High** | **Control** | SCM-03 | Not Run | scm.ts |
| UAT-SCM-014 | Engine quantities are clamped before storage | Planner | Engine returns an absurd quantity | 1. Run a plan. 2. Inspect the line. | — | The stored quantity is capped (≤ 2× max stock, never negative) and `detail.clamped` is set — a faulty engine cannot place an absurd number in front of an approver. | High | Control/Security | SCM-01 | Not Run | — |
| UAT-SCM-015 | Every line carries its rationale | Planner | A completed run | 1. `GET .../plans/:id`. | — | Each line records the model/method, expected fill rate, waste and lost sales, solver status, per-day order-up-to and safety stock, and any clamp — the quantity is reviewable. | Med | Positive | SCM-03 | Not Run | scm.ts |
| UAT-SCM-016 | Scenario what-if is advisory and writes nothing | Planner | — | 1. Count planning runs. 2. `POST .../scenario` `{branch_id, item_ids:['ING-CHK','ING-RICE'], horizon_days:7, demand_multiplier:2}`. 3. Re-count. | — | 201 with suggested lines returned synchronously; **the run count is unchanged** — a scenario persists nothing. Bounded to ≤25 items / ≤28 days. | Med | Positive | SCM-03 | Not Run | scm.ts |

## §4 — Order-plan maker-checker and hand-off to purchasing (SCM-01)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-017 | Draft edit → submit → GOV-01 queue | Planner (`scm_plan`) | A Draft plan exists | 1. `PUT .../plans/:id/lines/:lineId` `{final_qty:7}`. 2. `POST .../plans/:id/submit`. 3. `GET /api/finance/approvals/pending` (as a user holding `exec`/`approvals`). | — | (1) 200, quantity and plan total updated. (2) 201, status `PendingApproval`. (3) The plan appears with `type:'scm_order_plan'`, `control:'SCM-01'`, its value and age. | High | Positive | SCM-01, GOV-01 | Not Run | scm.ts |
| UAT-SCM-018 | **Self-approval is refused; an independent approver succeeds** | Planner (submitter, granted `scm_approve`) / second approver | Plan submitted by plannerA | 1. plannerA `POST .../plans/:id/approve`. 2. approverA (holds `scm_approve`) `POST .../plans/:id/approve`. | — | (1) **403 `SOD_SELF_APPROVAL`** — even holding the checker duty, the submitter cannot approve their own plan. (2) 201, status `Approved`, `approved_by` = approverA. Maker binds to the SUBMITTER (nightly plans are created by the scheduler). | **High** | **Control** | SCM-01, R24 | Not Run | scm.ts |
| UAT-SCM-019 | Approval without the checker duty is refused at the guard | Planner (no `scm_approve`) | Plan submitted | 1. `POST .../plans/:id/approve`. | — | **403** — the request never reaches the service. `scm_approve` is a sub-permission, so no coarse module key grants it. | High | Control | SCM-01, R24 | Not Run | scm.ts |
| UAT-SCM-020 | **Approved plan converts to a real requisition, idempotently** | Planner | Plan Approved | 1. `POST .../plans/:id/convert-to-pr`. 2. Verify the requisition. 3. Call convert again. | — | (1) 201 with `pr_no`. (2) A real `purchase_requests` row exists with line items — raised through the purchasing API, never a direct PO insert; no GL is posted by this module. (3) Returns the **same** `pr_no` (`idempotent:true`) — no second requisition. | **High** | **Control** | SCM-01, EXP-01 | Not Run | scm.ts |
| UAT-SCM-021 | Lifecycle guards | Planner / approver | — | 1. Edit a line on a `PendingApproval` plan. 2. Approve a `Draft` plan. 3. Convert a plan that is not Approved. 4. Submit a plan whose quantities are all zero. 5. Reject with no reason. | — | (1) **409 `PLAN_NOT_DRAFT`**. (2) **409 `PLAN_NOT_PENDING`**. (3) **409 `PLAN_NOT_APPROVED`**. (4) **400 `PLAN_EMPTY`**. (5) **400 `REASON_REQUIRED`**. | Med | Negative | SCM-01 | Not Run | — |
| UAT-SCM-022 | Rejection returns the plan to the planner | Approver / Planner | Plan `PendingApproval` | 1. `POST .../plans/:id/reject` `{reason:'ลดจำนวนไก่'}`. 2. Planner edits a line. | — | (1) 201, status `Rejected` with the reason recorded. (2) Editing re-opens it to `Draft` so it can be resubmitted — the reviewer's feedback is actionable. | Med | Positive | SCM-01 | Not Run | — |
| UAT-SCM-023 | Cross-tenant isolation on plans | Planner (T2) | HQ plan exists | 1. T2 `GET .../plans`. 2. T2 `GET .../plans/:hqPlanId`. 3. T2 approve the HQ plan. | — | (1) **0 rows**. (2) **404**. (3) **404/403** — no cross-tenant read or approval. | **High** | **Control** | SCM-01 | Not Run | scm.ts |
| UAT-SCM-024 | Every persisted row carries the tenant | — | A completed run | 1. Inspect `scm_order_plans` + `scm_order_plan_lines`. | — | Every row carries `tenant_id` (the RLS `WITH CHECK` would reject otherwise) — no tenantless planning data. | High | Control | SCM-01 | Not Run | scm.ts |

## §5 — Demand spikes, scheduling and idempotency (SCM-02)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-025 | **Spike detected once; a re-scan adds no duplicate** | Planner | ~90 days of ~10/day history, then a 50-unit day | 1. `POST .../spikes/scan`. 2. `POST .../spikes/scan` again immediately. | — | (1) **1** new spike event (judged against the baseline as it stood BEFORE that day — scoring after folding it in would let the spike hide itself). (2) **0** new events — the per-day unique key plus the watermark make any cadence idempotent. | **High** | **Control** | SCM-02 | Not Run | scm.ts |
| UAT-SCM-026 | Auto-replan queues ONE job per branch | Planner | `auto_replan:true`; spikes on several items at one branch | 1. Scan. 2. Drain the job queue. | — | Exactly **one** `scm_replan` job for the branch (batching its items, not one job per item); draining it produces a `replan` run and flips the events to `Replanned` with the run id. | High | Control | SCM-02 | Not Run | scm.ts |
| UAT-SCM-027 | A replan still requires independent approval | Planner / approver | A replan run produced a Draft plan | 1. Submit and approve it. | — | The replan plan follows the same SCM-01 lifecycle — a spike never auto-orders. | High | Control | SCM-01, SCM-02 | Not Run | — |
| UAT-SCM-028 | **Nightly job is idempotent** | — | — | 1. Enqueue `scm_nightly_plan` twice for the same business day. 2. Drain. | — | Exactly **one** nightly run exists; the second job returns `skipped:true`. Enforced by a partial unique index, not by convention, so a duplicate scheduler tick on a second replica cannot double-plan. | **High** | **Control** | SCM-02 | Not Run | scm.ts |
| UAT-SCM-029 | Low-volume noise does not fire | Planner | An item moving 2→7 units | 1. Scan. | — | No spike — the minimum-volume floor (`spike_min_qty`) suppresses small-number noise, while the cumulative-deviation detector still accumulates so a genuine slow burn is not lost. | Med | Negative | SCM-02 | Not Run | — |
| UAT-SCM-030 | Cooldown suppresses repeat firing | Planner | A spike fired within the cooldown window | 1. Seed another spiking day. 2. Scan. | — | No second event until `spike_cooldown_hours` has elapsed — one viral evening yields one event, not forty. | Med | Control | SCM-02 | Not Run | — |
| UAT-SCM-031 | A failed run records a diagnosable reason | IT Ops | Engine returns an invalid response | 1. Run a plan. 2. Inspect the run row and the ops alert. | — | The run is `Failed` with the **full underlying cause chain** recorded (the pg SQLSTATE is nested under `.cause`); the queue retries then dead-letters with `job_dead_letter` and an administrator notification (ITGC-OP-04). | High | Control | SCM-02, ITGC-OP-04 | Not Run | — |
| UAT-SCM-032 | Misconfigured engine degrades rather than fails | IT Ops | Only ONE of `SCM_ENGINE_URL` / `SCM_ENGINE_SECRET` set | 1. Run a plan. | — | The run completes in fallback and a one-time `scm_engine_misconfigured` ops alert is raised — a half-configured integration must not silently look healthy. | Med | Control | SCM-02 | Not Run | — |

## §7 — Planner workspace screens (docs/54 Phase 3)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-033 | Planning tabs are reachable from the demand workspace | Planner | — | 1. Open `/demand`. | — | The tabs **แผนสาขา**, **แผนสั่งซื้อ**, **จำลองสถานการณ์**, **ดีมานด์พุ่ง** appear alongside the existing forecast tabs. | Med | Positive | SCM-03 | Not Run | manual 21 |
| UAT-SCM-034 | Run a plan from the screen | Planner | — | 1. **แผนสาขา** → **วางแผนตอนนี้**. | — | A run appears with status Completed, showing series/branch counts and which engine produced it (เครื่องพยากรณ์ vs คำนวณในระบบ). | Med | Positive | SCM-03 | Not Run | manual 21 §3 |
| UAT-SCM-035 | Forecast chart shows the uncertainty band | Planner | An engine-backed run exists | 1. Click the run. | — | The per-day forecast chart renders; the caption reports the model, WAPE and that a p10–p90 band is available. | Med | Positive | SCM-03 | Not Run | manual 21 §3 |
| UAT-SCM-036 | Untagged-demand warning is visible | Planner | `dine_in_branch_id` unset and dine-in demand present | 1. Open **แผนสาขา**. | — | A warning explains that dine-in orders carry no branch and per-branch ordering will under-plan until the setting is made. | Med | Detective | SCM-03 | Not Run | manual 21 §2 |
| UAT-SCM-037 | Edit a draft line, then submit | Planner | A Draft plan exists | 1. **แผนสั่งซื้อ** → open a plan. 2. Change **สั่งจริง** on a line. 3. **ส่งอนุมัติ**. | — | The quantity and the plan total update; the plan moves to PendingApproval and the edit field becomes read-only. | High | Positive | SCM-01 | Not Run | manual 21 §4 |
| UAT-SCM-038 | Approve controls are hidden without the checker duty | Planner (no `scm_approve`) | A PendingApproval plan | 1. Open the plan. | — | **อนุมัติ**/**ปฏิเสธ** are not rendered (the server refuses regardless — this only avoids a dead button). | Med | Control | SCM-01, R24 | Not Run | manual 21 §5 |
| UAT-SCM-039 | Approve, then convert, from the screen | Approver / Planner | A PendingApproval plan submitted by someone else | 1. Approve. 2. **แปลงเป็นใบขอซื้อ**. | — | Status → Approved → Converted; the requisition number is shown on the plan. | High | Positive | SCM-01 | Not Run | manual 21 §5–6 |
| UAT-SCM-040 | Scenario tab computes without saving | Planner | — | 1. **จำลองสถานการณ์** → enter 2 item codes, multiplier 2 → **คำนวณ**. 2. Re-open **แผนสาขา**. | — | Quantities and an estimated cost are returned with an advisory note; **no new run or plan appears**. | Med | Positive | SCM-03 | Not Run | manual 21 §7 |
| UAT-SCM-041 | Spike feed lists and dismisses | Planner | An Open spike event | 1. **ดีมานด์พุ่ง**. 2. **ปิดเรื่อง** on a row. | — | Expected vs actual, the z-score and direction are shown; dismissing flips the row to Dismissed. | Med | Positive | SCM-02 | Not Run | manual 21 §8 |
| UAT-SCM-042 | Screens are fully bilingual | Planner | — | 1. Switch language th ↔ en on every planning tab. | — | Every label, column, button and message switches; no hardcoded Thai or English remains. | Med | Positive | — | Not Run | catalog `scm.*` |

## §8 — Forecast reconciliation hierarchy (docs/58 Track C · C1)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-043 | Declare / synthesize a reporting hierarchy | Planner (`scm_plan`) | ≥1 active branch, no hierarchy declared | 1. `GET /api/scm-planning/hierarchy/forest?axis=branch`. 2. `PUT /api/scm-planning/hierarchy` declaring `BKK01 → CENTRAL → บริษัท`. 3. Re-read the forest. 4. `PUT` a two-node set with each the other's parent. | branch axis nodes | Step 1 returns a **synthesized** forest (one *Total* root + one leaf per branch). Step 2 succeeds; the computed levels are leaf 0, region 1, total 2. Step 3 now returns source **declared** with 3 nodes. Step 4 (a cycle) is rejected `SCM_HIERARCHY_INVALID`. | High | Positive / Control | C1 | Not Run | scm harness · manual 21 §2 |
| UAT-SCM-044 | Hierarchy is tenant-isolated | Planner (tenant B) | Tenant A declared a branch hierarchy | 1. As B, `GET /api/scm-planning/hierarchy`. 2. As B, `GET …/hierarchy/forest?axis=branch`. | — | B's declared list returns **0** of A's nodes; B's synthesized forest contains none of A's branches. The mandatory cross-tenant boundary. | High | Cross-tenant | C1 | Not Run | scm harness |

## §9 — Promotion & price-effect demand (docs/56 Track A · A1, control SCM-04)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-045 | Governed promo lifts the forecast, with attribution | Planner (`scm_plan`) | An approved, active promotion covering the horizon | 1. Run a plan with no promotion → note a menu forecast. 2. Approve a promotion for the item's category covering the horizon. 3. Re-run. | promo 20%, horizon-covering | Step 1's forecast carries **no** promo attribution. After the governed promo, the menu forecast carries `regressors_used` including **promo** and a populated `promo_uplift_pct`; the promo-day quantity is materially above the un-promoted baseline. | High | Positive | SCM-04 | Not Run | scm harness · PN-34 §7.10 |
| UAT-SCM-046 | A fabricated promo cannot inflate a forecast | Planner | — | 1. Attempt to influence a production run's promo signal via the request body. | run body with a promo hint | The run body has **no** promo field — the promo signal is server-derived from approved `promotions` only. A run with no governed promo forecasts the baseline (no uplift, no `promo` in `regressors_used`). The control (SCM-04) holds by construction. | High | Control | SCM-04 | Not Run | server-derived only |
| UAT-SCM-047 | Promo signal is tenant-scoped | Planner (tenant B) | Tenant A has an approved promotion | 1. As B, run/extract. | — | Tenant A's promotion never enters B's forecast signal (RLS); B sees 0 of A's promotions. The mandatory cross-tenant boundary. | High | Cross-tenant | SCM-04 | Not Run | scm harness |

## §10 — Coherent forecast reconciliation (docs/58 Track C · C2)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-048 | Reconciliation flows coherently end to end | Planner (`scm_plan`) | Engine-backed run | 1. Run a plan. 2. Inspect the engine request/response and the persisted menu forecasts. | — | The run sends the engine an aggregation forest and the engine returns a **reconciled** block whose aggregate node equals the sum of its leaves (coherent). The API explodes the reconciled leaf paths; with **bottom-up**, branch order quantities are **unchanged** vs no reconciliation (a coherence property, not an order change). A malformed hierarchy degrades to the base forecast rather than persisting an incoherent one. | High | Positive / Control | C2 | Not Run | scm harness · engine pytest (coherence/projection/determinism) · PN-34 §7.11 |

## §11 — Multi-echelon supply-network master data (docs/57 Track B · B1)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-049 | Declare a supplier→DC→branch topology and validate it | Planner (`scm_plan`) | ≥1 active branch | 1. `POST /api/scm-network/nodes` for a `supplier`, a `central_kitchen`, and a `branch` (linking `branch_id`). 2. `POST /api/scm-network/lanes` supplier→DC and DC→branch. 3. `GET /api/scm-network/topology`. 4. Attempt a `branch` node with no `branch_id`. | node/lane payloads | Nodes get echelon 0/1/2 by kind. Topology `validation.ok = true` with the branch in `reachableBranches`. The `branch` without `branch_id` is rejected `BRANCH_NODE_NEEDS_BRANCH`. | High | Positive / Control | B1 | Not Run | scm harness · manual 21 §2 |
| UAT-SCM-050 | Malformed topology is rejected by the validator | Planner (`scm_plan`) | The valid topology of UAT-SCM-049 | 1. Add an echelon-skipping lane supplier→branch; re-read topology. 2. Add an unwired second branch; re-read. 3. Try to delete a node that still has lanes. | — | Step 1: `validation.ok = false`, issues include `LANE_ENDPOINTS_INVALID`. Step 2: issues include `UNREACHABLE_BRANCH`. Step 3: rejected `NODE_HAS_LANES` (409). | High | Control | B1 | Not Run | scm harness |
| UAT-SCM-051 | Supply network is tenant-isolated | Planner (tenant B) | Tenant A declared a network | 1. As B, `GET /api/scm-network/nodes`. 2. As B, `POST /api/scm-network/lanes` referencing A's node IDs. | — | B sees **0** of A's nodes; a lane onto A's node IDs is rejected `LANE_ENDPOINTS_INVALID` (both endpoints must be the caller's own nodes). The mandatory cross-tenant boundary. | High | Cross-tenant | B1 | Not Run | scm harness |

## §12 — Own-price elasticity (docs/56 Track A · A2)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-052 | A governed price cut identifies an own-price elasticity | Planner (`scm_plan`) | An approved promotion (discount) covering part of the menu item's history | 1. Run a plan. 2. Inspect the menu forecast attribution. 3. `GET /api/scm-planning/elasticity`. | promo 20% over part of history | The promotion cuts the effective price on some history days → the engine's log-log fit identifies **ε < 0**, returned in `attribution` (`price_elasticity`, `elasticity_r2`, `elasticity_n_obs`) and carried by the menu forecast with `price` in `regressors_used`. The run persists it; `GET …/elasticity` lists the item with its ε (server-derived only — never from the request body). | High | Positive | A2 | Not Run | scm harness · engine pytest (recovery/floor) · PN-34 §7.13 |
| UAT-SCM-053 | The scenario what-if applies the elasticity | Planner (`scm_plan`) | An identified ε on file for the item | 1. `POST /api/scm-planning/scenario` with `price_multiplier: 1.5`. 2. Again with `price_multiplier: 0.5`. | — | A price **rise** shrinks the demand response (`< 1`) and a price **cut** grows it (`> 1`) — `demand × (price_multiplier)^ε` — so the suggested quantity never rises with price. **Advisory only:** no run or plan is created. An item with no identified ε gets a unit response (unchanged). | High | Positive | A2 | Not Run | scm harness |
| UAT-SCM-054 | Elasticities are tenant-isolated | Planner (tenant B) | Tenant A has persisted elasticities | 1. As B, `GET /api/scm-planning/elasticity`. | — | B sees **0** of A's elasticities (RLS). The mandatory cross-tenant boundary. | High | Cross-tenant | A2 | Not Run | scm harness |

## §13 — Cannibalization & halo: cross-price elasticity (docs/56 Track A · A3)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-055 | The scenario composes own + category-scoped cross elasticity | Planner (`scm_plan`) | A persisted cross pair γ between two same-category items | 1. `GET /api/scm-planning/cross-elasticity`. 2. `POST /api/scm-planning/scenario` with `price_multiplier` and **both** sibling items in `item_ids`. 3. Again with only **one** of the pair in `item_ids`. | γ=0.5 (substitutes) | The pair is listed. With the sibling in the what-if, the item's `cross_elasticity_sum` includes γ and its demand response reflects `ε + γ` (cannibalization offsets the price-rise drop). With the sibling absent, `cross_elasticity_sum = 0` — the cross term is applied **only** to items whose price actually moved (category-scoped). **Advisory only.** | High | Positive / Control | A3 | Not Run | scm harness · apps/api vitest (estimator) · PN-34 §7.14 |
| UAT-SCM-056 | Cross-elasticities are tenant-isolated | Planner (tenant B) | Tenant A has persisted cross pairs | 1. As B, `GET /api/scm-planning/cross-elasticity`. | — | B sees **0** of A's cross pairs (RLS). The mandatory cross-tenant boundary. | High | Cross-tenant | A3 | Not Run | scm harness |

## §14 — Multi-echelon network replenishment plans (docs/57 Track B · B2, control SCM-05)

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-057 | Run a two-echelon network plan → Draft echelon plans | Planner (`scm_plan`) | A valid supplier→DC→branch topology (§11) with demand history; engine env unset (fallback) | 1. `POST /api/scm-network/plans/run` `{item_code:'ING-CHK'}`. 2. `GET /api/scm-network/plans/:id`. | `{item_code:'ING-CHK'}` | 201; plan `status:Draft` with per-node lines at **both** echelons (branch installation base-stock + a DC echelon base-stock). The **DC echelon base-stock ≥ the sum of its branches' installation base-stock** (coherence). Fallback is `B=μ·(L+R)+z·σ·√(L+R)` per branch, DC = Σ (no pooling); with the engine on, a positive pooling benefit is reported. Every engine quantity is zod-validated + clamped before storage. | High | Positive | SCM-05 | Not Run | scm harness · PN-34 §7.15 · manual 21 §2 |
| UAT-SCM-058 | **Submit; self-approval refused; an independent approver succeeds** | Planner (submitter, granted `scm_approve`) / second approver | A Draft network plan | 1. `POST /api/scm-network/plans/:id/submit`. 2. The submitter `POST .../plans/:id/approve`. 3. A second holder of `scm_approve` `POST .../plans/:id/approve`. | — | (1) 201, status `PendingApproval` (surfaced in GOV-01). (2) **403 `SOD_SELF_APPROVAL`** — even holding the checker duty, the submitter cannot approve their own plan; the maker binds to the SUBMITTER, not `created_by`. (3) 201, status `Approved`. SoD **R24** (`scm_plan` vs `scm_approve`) is reused — B2 adds no new rule. | **High** | **Control** | SCM-05, R24 | Not Run | scm harness |
| UAT-SCM-059 | **Approved network plan converts to a real requisition, idempotently** | Planner | Plan `Approved` | 1. `POST /api/scm-network/plans/:id/convert`. 2. Verify the requisition. 3. Convert again. | — | (1) 201 with `pr_no` — the DC's supplier-facing order rolls up to a **real** `purchase_requests` row through the `ProcurementService.createPr` seam (reason `SCM-NET`), never a direct PO insert; no GL is posted by this module. (3) Returns the **same** `pr_no` (idempotent) — no second requisition. | **High** | **Control** | SCM-05, EXP-01 | Not Run | scm harness |
| UAT-SCM-060 | Cross-tenant isolation on network plans | Planner (tenant B) | A tenant-A network plan exists | 1. B `GET /api/scm-network/plans`. 2. B `GET .../plans/:aPlanId`. 3. B attempt to approve the tenant-A plan. | — | (1) **0 rows**. (2) **404**. (3) **404/403** — no cross-tenant read or approval (RLS). The mandatory cross-tenant boundary. | **High** | **Cross-tenant** | SCM-05 | Not Run | scm harness |

## §15 — Warm-start / model registry (docs/59 Track D · D2)

*Compute-only — no new control. `scm_model_cache` (migration 0475) caches each series' fitted Prophet model; a run whose training window is unchanged and within the refit cadence reuses the cached fit (skipping the refit) and only samples.*

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-061 | **Warm-start reuse is reproducible; the fitted model persists** | Planner (`scm_plan`) | An engine-backed run for a series | 1. `POST /api/scm-planning/run`. 2. Inspect `scm_model_cache` for the series. 3. Re-run with the training window **unchanged** (same business day, no history change). 4. Compare the reused-fit forecast to the cold-fit forecast. | — | (1) 201; a fitted model persists to `scm_model_cache` with a `fit_hash` and serialized `fit_params`. (3) The engine ships the cached fit as `warm_start` and reports a **warm hit** — it reuses the serialized fit and skips BOTH the primary fit and the backtest refit. (4) The warm-reuse forecast is **byte-identical** to a cold fit (Prophet MAP + reseeded sampling ⇒ deterministic). The persisted `fit_wape` is carried forward so the forecast WAPE stays meaningful across the reuse run. A corrupt cache entry fails closed to a refit. | High | Positive / Control | Track D · D2 | Not Run | scm harness · engine pytest (warm-start reproducibility / corrupt-cache fallback) · PN-34 §7.16 |
| UAT-SCM-062 | Cadence guard force-refits a stale fit | Planner (`scm_plan`) | A cached fit whose `fitted_at` is older than `refit_cadence_days` | 1. Set `refit_cadence_days` (e.g. 14). 2. Age a cached fit past the cadence with the training window otherwise unchanged. 3. Re-run. | — | The cached fit is **NOT reused** — a fit older than `refit_cadence_days` is force-refit even though the `fit_hash` window is unchanged (the second of the two independent, fail-safe staleness guards; the first is the `fit_hash` mismatch of UAT-SCM-061). `scm_model_cache` records a fresh `fitted_at`. Out-of-range cadence (`<1` or `>90`) is rejected `VALIDATION_ERROR` at `PUT .../settings`. | High | Control | Track D · D2 | Not Run | scm harness · PN-34 §7.16 |
| UAT-SCM-063 | Model cache is tenant-isolated | Planner (tenant B) | Tenant A (HQ) has cached fits in `scm_model_cache` | 1. As B, read/own `scm_model_cache` rows. | — | Tenant B owns **0** of tenant A's `scm_model_cache` rows (RLS). No cross-tenant fit is ever reused. The mandatory cross-tenant boundary. | High | Cross-tenant | Track D · D2 | Not Run | scm harness |

## §16 — Cold-start / analog forecasting for new items (docs/56 Track A · A4)

*Forecast-quality only — no new control, no migration. A brand-new SKU with too little history to fit borrows the pooled demand shape of donor series (established same-branch siblings) and rescales it to its own baseline seed; the forecast is flagged `analog` in `attribution.regressors_used` so a reviewer sees it is borrowed, not observed. Contract UNCHANGED — `analog_of` and the `analog` flag were already reserved (no `SCM_ENGINE_CONTRACT_VERSION` bump, stays '2').*

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-064 | **A cold-start new item borrows an analog and is flagged `analog`** | Planner (`scm_plan`) | ≥1 **established** item at the branch with ≥ 90 days of history, and a **brand-new** item at the same branch whose dense history is below the 56-day Prophet floor | 1. Run an engine-backed plan (`POST /api/scm-planning/run`). 2. Inspect the new item's persisted menu forecast + its attribution. 3. Inspect an established item's forecast. | new SKU < 56 days; ≥1 sibling ≥ 90 days | The new item cannot be fitted directly, so the run selects **established same-branch siblings** (≤ 5) as donors and passes them on the already-reserved `analog_of` per-series input; the engine (two-phase fan-out: donors first) pools the donors' **normalized** shape and rescales it to the new item's own baseline seed. The new item's forecast carries **`analog`** in `attribution.regressors_used` (persisted to `scm_demand_forecasts.regressorsUsed`), so a reviewer sees the number is **borrowed, not observed**; the established item's forecast is unaffected (no `analog`). No new order authority — SCM-01/02/03 still govern the run. | High | Positive | A4 | Not Run | scm harness · engine pytest (borrow+flag / two-phase) · PN-34 §7.17 · manual 21 §1 |
| UAT-SCM-065 | A new item with no eligible donor falls back to its own short-history baseline | Planner (`scm_plan`) | A brand-new item at a branch with **no** established same-branch sibling (no item ≥ 90 days) | 1. Run an engine-backed plan. 2. Inspect the new item's forecast + attribution. | new SKU, no donor | With no eligible donor, the analog branch does **not** fire (it requires `analog_of` set **AND** own history < 56 days **AND** ≥1 donor present) — the series routes normally on its own short history, and its forecast carries **no** `analog` flag. A run with no cold-start series at all is byte-identical to pre-A4 behaviour (the donor pre-pass is a no-op). Forecast-quality fallback only; no error, no control. | High | Negative / Control | A4 | Not Run | scm harness · engine pytest (no-donor fallback / ignored-when-history-sufficient) · PN-34 §7.17 |

## §17 — Scheduled batch retrain + forecast-source seam (docs/59 Track D · D1)

*Compute/scheduling only — no new control, contract unchanged (`SCM_ENGINE_CONTRACT_VERSION` stays '2'). A schedulable **`scm_batch_retrain`** job forecasts every planning-enabled series (`executePlanRun(scope='retrain')`) and **persists the reconciled sample paths** (migration 0477 — additive `scm_demand_forecasts.sample_paths` jsonb + the `uq_scm_retrain_run` partial-unique index, both on existing tables). A **nightly** plan run then reuses a recent retrain's persisted paths **all-or-nothing per branch** (only when a fresh retrain covered the whole branch — the paths are already reconciled) within `SCM_FORECAST_STALENESS_HOURS` (default 24) via `loadFreshMenuPaths`; a miss falls through to a full fresh forecast (unchanged). Idempotent per (tenant, run_date).*

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-066 | **A scheduled batch retrain persists reconciled forecasts and the nightly plan reuses them without re-forecasting** | Planner (`scm_plan`) | An engine-backed setup with planning-enabled series across a branch | 1. Run the `scm_batch_retrain` job (`scope='retrain'`). 2. Inspect the branch's `scm_demand_forecasts` rows. 3. Run a **nightly** plan for the same day, with the engine instrumented to record whether it is called for those series. | — | (1) The retrain Completes and persists the branch's series with the **reconciled `sample_paths`** populated (jsonb K×H, migration 0477). (3) The nightly run **reuses the persisted paths for the whole branch and does NOT call the engine** for those series (the forecast-source seam — reuse is all-or-nothing per branch, only when a fresh `scope='retrain'` forecast newer than `SCM_FORECAST_STALENESS_HOURS` covers every series). Order quantities are unchanged vs a fresh-forecast nightly run (reuse is a compute optimization, not a plan change). A missing/stale retrain would fall through to a full fresh forecast. | High | Positive | Track D · D1 | Not Run | scm harness (retrain persists sample_paths / nightly reuses without re-calling the engine) · PN-34 §7.19 · manual 21 §2 |
| UAT-SCM-067 | A duplicate retrain the same day is a no-op | Planner (`scm_plan`) | A completed `scm_batch_retrain` for the business day | 1. Enqueue / run `scm_batch_retrain` again for the **same** (tenant, run_date). 2. Inspect `scm_plan_runs`. | — | The second retrain is a **no-op** — the partial unique index `uq_scm_retrain_run` on `scm_plan_runs` plus the generalized `executePlanRun` run guard short-circuit it (mirrors the nightly-plan idempotency of §5), so exactly **one** retrain run exists for the day and no duplicate forecasts are written. A duplicate scheduler tick on a second replica cannot double-retrain. | High | Negative / Control | Track D · D1 | Not Run | scm harness (duplicate retrain is a no-op) · PN-34 §7.19 |

## §18 — Optimal (MinT) reconciliation & coherent probabilistic paths (docs/58 Track C · C3/C4)

*Forecast-quality only — no new control, contract unchanged (`SCM_ENGINE_CONTRACT_VERSION` stays '2'; the `method`/`covariance` enums already carried `mint` + the four covariances), no migration. The engine's `reconcile.py` gains real **`mint`** minimum-trace reconciliation (`G=(SᵀW⁻¹S)⁻¹SᵀW⁻¹`, projector `P=S·G`) with `ols`/`wls_struct`/`wls_var`/`shrink` covariance; `service.run_forecast` forecasts each aggregate node's summed leaf history INDEPENDENTLY so MinT ≠ bottom-up, and **C4** applies `P` per scenario/day to the sample paths (clip-at-0 + renormalize). MinT requires the Python engine; the in-process/stub engines do BU/TD only, so this is engine-capability + engine-pytest coverage (the harness continues to pin coherence via bottom-up). Verified in `services/forecast-engine/tests/test_reconcile.py`.*

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SCM-068 | **MinT reconciliation is coherent and improves the aggregate over bottom-up** | Planner (`scm_plan`) / engine operator | The Python forecast engine deployed; a two-level hierarchy (branch leaves → TOTAL) with ≥ the backtest-min history | 1. Forecast with `reconciliation.method='mint'`, `covariance='shrink'` (or `wls_struct`). 2. Inspect the `reconciled` block. 3. Compare the reconciled TOTAL to a pure bottom-up run against a known truth. | A synthetic hierarchy whose aggregate carries an independent signal | Every parent node's reconciled forecast **equals the sum of its children** scenario-by-scenario (coherence), and the reconciled leaves have **moved off** the pure bottom-up leaves (the independent aggregate forecast pulled them). On a hierarchy with known truth, MinT's reconciled aggregate error is **≤** the base (independent) aggregate error — the whole justification for the method. All four covariances (`ols`/`wls_struct`/`wls_var`/`shrink`) produce coherent output; `shrink` stays invertible even when n_obs < number of nodes. | High | Positive | C3 | Not Run | engine pytest (coherence / moves-off-BU-and-toward-truth / closed-form G / shrink n_obs<m / all four covariances) · PN-34 §7.11b |
| UAT-SCM-069 | **Coherent probabilistic paths: the projector preserves an already-coherent forecast** | Planner (`scm_plan`) / engine operator | As UAT-SCM-068 | 1. Feed MinT an aggregate base forecast equal to the exact sum of the leaves (an already-coherent input). 2. Inspect the reconciled sample paths. | Aggregate base = Σ leaf base | Because `P=S·G` is an oblique projector onto the coherent subspace (`P·S=S`), an already-coherent input is a **fixed point**: the reconciled leaf sample paths equal the base leaf paths unchanged (the §1.3 projection identity), scenario-by-scenario. A projected path that dips below zero is clipped at 0 and the leaves renormalized to the reconciled total, so the output is always non-negative and coherent. | High | Positive | C4 | Not Run | engine pytest (projection identity on a coherent input / clip+renormalize non-negativity) · PN-34 §7.11b |

## §6 — Traceability summary

| Control | Cases |
|---|---|
| **SCM-01** (order-plan maker-checker) | UAT-SCM-014, 017–024, 027, 037–039 |
| **SCM-02** (job monitoring + idempotency) | UAT-SCM-025–032, 041 |
| **SCM-03** (auditable demand-driven sizing) | UAT-SCM-001–016, 033–036, 040 |
| SoD **R24** (`scm_plan` vs `scm_approve`) | UAT-SCM-018, 019, 038 |
| GOV-01 (pending approvals) | UAT-SCM-017 |
| EXP-01 (requisition hand-off) | UAT-SCM-020 |
| ITGC-OP-04 (job failure alerting) | UAT-SCM-031 |
| Track C · C1 (forecast hierarchy definition) | UAT-SCM-043, 044 |
| Track C · C2 (forecast reconciliation) | UAT-SCM-048 |
| Track C · C3/C4 (optimal MinT + coherent probabilistic paths — no control) | UAT-SCM-068, 069 |
| **SCM-04** (promo-forecast governance, docs/56 A1) | UAT-SCM-045, 046, 047 |
| Track B · B1 (supply-network master data) | UAT-SCM-049, 050, 051 |
| Track A · A2 (own-price elasticity) | UAT-SCM-052, 053, 054 |
| Track A · A3 (cross-price elasticity / cannibalization) | UAT-SCM-055, 056 |
| **SCM-05** (multi-echelon network-plan maker-checker, docs/57 B2) | UAT-SCM-057, 058, 059, 060 |
| Track D · D2 (warm-start / model registry — no control) | UAT-SCM-061, 062, 063 |
| Track A · A4 (cold-start / analog forecasting — no control) | UAT-SCM-064, 065 |
| Track D · D1 (scheduled batch retrain + forecast-source seam — no control) | UAT-SCM-066, 067 |
