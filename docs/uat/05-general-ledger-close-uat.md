# UAT — Cycle 05: General Ledger & Financial Close

**Status: DRAFT v0.63 · 2026-07-18** · *v0.63: added UAT-GL-212..216 — chart expansion + statement-section binding (GL-11, migration 0438; no new control): sub-account create with `PARENT_TYPE_MISMATCH`/`PARENT_SELF` guards + 4–6-digit codes; the canonical chart ships ~34 more everyday accounts (now 142); accounts BIND to งบดุล (`bs_group`) and งบกำไรขาดทุน (`is_group`) sections mirroring `cf_bucket` — `balanceSheet` returns grouped `sections`, `incomeStatement` a structured `groups`+`summary` (gross/operating/pre-tax/net income), own-column → canonical-default → type resolution; additive (golden re-pinned 588). ToE `coa-change.test.ts`, `statement-sections.test.ts`, `basics.ts` 460; PN-04 rev 2.38–2.40; manual 06 v0.27.* · *v0.62: added UAT-GL-209..211 — Close Manager v2b (G3; extends GL-15/GL-19/REC-01, no new control/migration): `bank_rec` / `subledger_tieout` now auto-complete from the REC-01 register — every workspace the tenant OPENED for the period on the step's account set (cash = the canonical CASH_ACCOUNTS; tie-out = 1100/2000/1200/1500) must be CERTIFIED; fail-closed both ways (zero workspaces = no evidence; one un-certified blocks); the human act is the certification itself (preparer ≠ certifier, or B4 provably-safe auto-certify) and the tick pins the per-account certifiers in the evidence. ToE `basics.ts` 454→458; PN-04 rev 2.41; manual 06 v0.25.* · *v0.61: added UAT-GL-206..208 — Close Manager v2 (docs/50 follow-up; closes the rev-2.37 deferrals; extends GL-15/GL-19/GOV-01, no new control/migration): evidence-driven `close/auto-complete` (recurring ← nothing left due; fx_reval / deferred_tax ← a Posted run for the period; depreciation ← a Posted DEP JE) with "(auto)" attribution + pinned evidence, human sign-offs never auto-completed, idempotent; overdue close tasks age in the GOV-01 pending-approvals center (`close_task_overdue`, GL-15) until signed off. ToE `basics.ts` 446→454; PN-04 rev 2.40; manual 06 v0.24.* · *v0.60: added UAT-GL-202..204 — JE anomaly & control-exception analytics (docs/50 Wave 5 B5; NEW control **GL-28**, migration `0424`): five deterministic rules (duplicate_je HIGH w/ peers · round_amount Manual ≥ ฿10,000 whole-฿1,000 · backdated > 7 days · after_hours outside 06:00–22:00 Asia/Bangkok · unusual_pair Manual cash↔revenue HIGH) over Posted journal_entries/journal_lines/gl_audit_log into the idempotent `je_exceptions` register (per tenant × rule × entry); dismissal reason-mandatory + `EXCEPTION_DISMISSED` audit row; Close-Cockpit pillar (red = HIGH open) + schedulable `je_exceptions` sweep. ToE `basics.ts` 431→446; PN-04 rev 2.39; manual 06 v0.23 / 09 v0.10; RCM 297 (294/3/0).* · *v0.59: added UAT-GL-199..200 — recon workspace depth (docs/50 Wave 4 B4; extends REC-01/GL-19, migration `0422`): roll-forward (opening/activity/closing tie to the posted GL), risk rating + unmatched-item aging, safe-class auto-certification (LOW risk + zero roll-forward only; flagged + '(auto)'-attributed; NOT_ZERO skipped), REC-01 SoD untouched for everything else; GL-19 gains the advisory `recon_completeness` check. ToE `recon-profitability.ts` 13→24; PN-04 rev 2.38; manual 06 v0.22.* · *v0.58: added UAT-GL-196..197 — Close Manager (docs/50 Wave 3 B1; extends GL-15/16, migration `0421`): per-tenant `close_task_templates` (custom tasks + standard-step overrides, owner role, due-day offset, predecessor dependency); `startClose` composes standard + templates (none ⇒ byte-identical); dependent task blocked `DEPENDENCY_NOT_DONE`; custom REQUIRED task gates the lock (`STEPS_INCOMPLETE`); `SELF_DEPENDENCY`/`UNKNOWN_DEPENDENCY` fail-closed; GL-16 maker-checker lock unchanged. ToE `basics.ts` 422→431; PN-04 rev 2.37; manual 06 v0.21.* · *v0.57: added UAT-GL-194..194 — schedulable period-end staging (docs/50 Wave 2 B3; no new control/migration): action jobs `gl_fx_reval_run` (stages/refreshes the period's Open GL-18 run idempotently; ALREADY_POSTED → graceful no-op) and `consolidation_run` (per-group fault-isolated staging via `runConsolidation`; IC_RECON_NOT_APPROVED/NO_ENTITIES/ALREADY_POSTED recorded, never thrown at the sweep) with filters `{period?}` (default = just-ended business month; consolidation also `{group_id?}`); auto-Draft only — GL-18 SELF_POST / CON-03 posting maker-checker unchanged. ToE `fxreval.ts` 15→22 + `consolidation.ts` 42→47; PN-04 §7(10) rev 2.36; PN-11 rev 0.9.* · *v0.56: added UAT-GL-190..192 — auto-reversing accruals (docs/50 Wave 1 B2; extends GL-08/GL-17, migration `0419`, no new control): `recurring_journals.auto_reverse` (monthly-only — daily/weekly rejected `AUTO_REVERSE_MONTHLY_ONLY`); the `gl_recurring_journals` sweep's first run in the next business month posts the prior month's accrual back out (lines flipped, dated the 1st, Draft through GL-05 maker-checker, run response `reversals`), idempotent per `REC-<id>-<lastRun>-REV`; `/gl-schedules` create form gains the กลับรายการอัตโนมัติ checkbox + list badge. ToE `basics.ts` 414→422; PN-04 §7(11) rev 2.35; manual 06 v0.19.* · *v0.55: added UAT-GL-189 — pentest P4: the JE approval path (`approveEntry`) now enforces the hard-close (`PERIOD_LOCKED`) gate, not just `PERIOD_CLOSED`, so a Draft prepared while a period was open cannot be approved into a since-hard-Locked period and rewrite its balances (rejects before any write; entry stays Draft, no APPROVE audit). No new control (reinforces GL-05/GL-15/GL-19); ToE `ledger-posting-write.test.ts` (mirrors the existing PERIOD_CLOSED unit case); PN-04 rev 2.34; `docs/security/2026-07-16-penetration-test-report.md`.* · *v0.54: added TC-REV-27-01..06 — Track D Wave 4 (FINAL) significant financing component + revenue disclosure pack (new control **REV-27**, migration 0367; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120): a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method so Σ interest = face − PV; the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense with the contract liability accreting or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650) with the contract asset accreting, idempotent; the §120 contract-liability rollforward (opening → billings → recognized → closing) reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. ToE `revrec-disclosure.ts` (23); PN-12 §5f rev 0.14; RCM 267.* · *v0.53: added UAT-GL-188 — COA-D2: effective_from/effective_to and require_dimension are enforced in the postEntry account guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING, only when declared); PARENT_NOT_FOUND on create; dialogs expose both. ToE basics.ts 414 + ledger-posting.test.ts 18; PN-04 rev 2.32.* · *v0.52: added UAT-GL-187 — COA-D1: GL-24/GL-27 (+ staged masterdata imports MDM-03 and sensitive field changes MDM-01) surface in the GOV-01 pending-approvals center with per-item/batch approve routed through their own maker-checker endpoints; /chart-of-accounts GL-27 history register. ToE compliance.ts 179; PN-04 rev 2.31.* · *v0.51: added UAT-GL-186 — GL-27 canonical CoA maker-checker (migration 0362): a canonical create/edit/deactivate stages a coa_change_requests row (fail-closed request-time validation, one pending per code); creator self-approve → SOD_VIOLATION; a DIFFERENT Admin approves (applies) or rejects (chart untouched); single-Admin exception AutoApplied. ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts; PN-04 rev 2.30.* · *v0.50: added TC-REV-26-01..06 — Track D Wave 3 contract modifications (new control **REV-26**, migration 0360; TFRS 15 / IFRS 15 / ASC 606 §18-21): the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a new independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. ToE `revrec-modifications.ts` (28); PN-12 §5e rev 0.13; RCM 265.* · *v0.49: added TC-REV-25-01..06 — Track D Wave 2 variable consideration + the constraint (new control **REV-25**, migration 0359; TFRS 15 / IFRS 15 / ASC 606 §50-59): estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price — a `constrained_amount` above the gross estimate is rejected (`CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`; approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. ToE `revrec-variable.ts` (25); PN-12 §5d rev 0.12; RCM 264.* · *v0.48: added TC-REV-24-01..06 — Track D Wave 1 contract-asset / contract-liability split + independent billing schedule (new control **REV-24**, migration 0358; TFRS 15 / IFRS 15 / ASC 606 §105-107): recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300); a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410; `/position` tie-out (contract_asset = max(0, Σrecognized − Σbilled)); over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`); REV-19 up-front-billing back-compat (recognition still releases 2410); tenant RLS. ToE `revrec-billing.ts` (24); PN-12 §5c rev 0.11; RCM 263.* · *v0.47: added TC-REV-24-01..06 — Track D Wave 1 contract-asset / contract-liability split + independent billing schedule (new control **REV-24**, migration 0358; TFRS 15 / IFRS 15 / ASC 606 §105-107): recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300); a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410; `/position` tie-out (contract_asset = max(0, Σrecognized − Σbilled)); over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`); REV-19 up-front-billing back-compat (recognition still releases 2410); tenant RLS. ToE `revrec-billing.ts` (24); PN-12 §5c rev 0.11; RCM 263.* · *v0.46: added UAT-GL-184 — COA follow-up A (web-only): the /chart-of-accounts edit dialog gains cf_bucket + is_current (backfill an existing account; อัตโนมัติ = clear to the fallback chain); the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` linking to the chart. Manual 06 v0.14; PN-04 rev 2.28. Header consolidated: 3 stacked Status lines merged (CLS-02 retagged v0.41→v0.44b — v0.41 is the historic PR-6 note).* · *v0.45: added UAT-GL-179..183 — CLS-01/GL-25 flux / variance analysis with forced explanation + independent sign-off (new control, migration 0348): generate flags the lines breaching abs+pct thresholds (revenue +30k/+30% and marketing +15k/+300% breach; COGS +1k below abs threshold does not); sign-off refused while a breached line is unexplained (UNEXPLAINED_LINES); a non-breached line can't be explained (LINE_NOT_BREACHED); the preparer's self-review is blocked (SOD_SELF_APPROVAL) and a distinct reviewer certifies (ALREADY_CERTIFIED after); prior_year/budget comparatives + BS-basis filter (BUDGET_PL_ONLY) + RLS. Read-only — posts nothing to the GL. ToE flux-analysis.ts (18); PN-04 rev 2.24.* · *v0.44b: added UAT-GL-175..178 — CLS-02/GL-26 disclosure / close-package checklist (governed close binder): open seeds the standard TFRS/SEC items (Draft); review blocked while any item Open → ITEMS_INCOMPLETE; maker-checker (preparer self-review → SOD_SELF_APPROVAL, a distinct reviewer reviews then issues, Issued binder immutable → NOT_DRAFT); DISC support evidence pins to doc_attachments (assertDocExists) + tenant RLS. ToE disclosure-checklist.ts 13; PN-04 rev 2.24, migration 0347, RCM 251.* · *v0.44: added UAT-GL-174 — docs/43 PR-9 (FINAL): the /setup/posting-rules registry workspace — ทะเบียนเหตุการณ์ grid (event/role/side/default/tier + current override), cross-event คิวรออนุมัติ, append-only ประวัติการแก้ไข viewer, ปิดใช้ on approved rules; web-only over the PR-1 endpoints, GL-24 unchanged; PN-04 rev 2.25; docs/43 SERIES COMPLETE.* · *v0.43: added UAT-GL-173 — docs/43 PR-8: accounts.cf_bucket/cf_label/is_current (0346) drive the indirect SCF + BS metrics (column → map/list → fallback); /chart-of-accounts captures them; bulk IO for the canonical CoA (Admin-only, sensitive → staged) and posting-rule overrides (every row through GL-24 upsertRule → PendingApproval). ToE basics.ts 407 + ext.ts 299; PN-04 rev 2.24.* · *v0.42: added UAT-GL-172 — docs/43 PR-7 tie-out widening: PAY-02 payables (2350/2360/2370), vendor WHT (2361) and SBT (2130) roles flip widen→free; the PAY-02 schedule/remit and the PND/ภ.ธ.40 tie-outs read the widened sets (postingAccountSet); VAT sets + 2410 stay widen; REC-04 pinned permanently. ToE payroll.ts 46 (4 PR-7 checks); PN-05 rev 0.7, PN-06 rev 0.27.* · *v0.41: added UAT-GL-171 — docs/43 PR-6 POS hot-path override wiring (SALE.FOOD/SVC.CHARGE/POS.ROUNDING batched via postingOverridesMany in dine-in + portal checkouts; SALE.DELIVERY on channel fees; CPQ-win + house-charge share SALE.FOOD.revenue; cash/gift/tips/AR pinned, VAT widen-gated). ToE restaurant.ts 186 (3 PR-6 checks); PN-01 rev 0.32, PN-20 rev 3.10.* · *v0.40: added UAT-GL-170 — docs/43 PR-5 manufacturing/inventory override wiring (MFG.WO_ISSUE applied, MFG.WO_COMPLETE variance, QA.SCRAP incl. NCR, COSTING.PPV incl. the COST-02 STDREV variance, COSTING.ISSUE COGS, INV.ADJUST, WASTE.WRITEOFF; posting-rule layer inside the inventory account funnel between determination and the control literal; 1250/1210/1200/2000 pinned). ToE manufacturing.ts 14 (3 PR-5 checks); PN-15 rev 0.9, PN-03 rev 1.5.* · *v0.39: added UAT-GL-169 — docs/43 PR-4 projects/RE/IC override wiring (PROJECT.COST applied/COGS legs, PROJECT.REVENUE revenue/COGS across bill/POC/progress-claim/RE transfer, REALESTATE.BOOK deposit, IC per-side map legs; 1260/1265/2410/1100/1150/2150 pinned; GL-24-governed). ToE projects.ts 259 (3 PR-4 checks); PN-16 rev 0.45.* · *v0.38: added UAT-GL-168 — docs/43 PR-3 assets & leases: asset-category-grain account determination (Q2 — category asset/accum/dep accounts drive acquisition + the depreciation run under posting_determination, GL-21 fail-closed at category save, INVALID_POSTING_ACCOUNT), ASSET.DISPOSE/ASSET.REVALUE/LEASE.MODIFY/LESSOR/PREPAID override wiring (GL-24-governed, pinned controls unchanged); ToE basics.ts 405 (5 PR-3 checks); PN-09 rev 1.7, PN-04 rev 2.23.* · *v0.37: added UAT-GL-167 — docs/43 PR-2 finance/treasury override wiring: 23 registry events now consult the tenant posting-rule (Approved-only) before their registry default at the real posting sites (write-off/advance-settle/reverse-charge/AP-WHT/AP-discount, bank interest/fee, petty-cash expense, FX unrealized+realized, returns, CN/DN revenue+VAT, membership defer/recognize, loyalty accrue, till variance/paid-in-out incl. the hub replay sharing TILL.VARIANCE, deposits take/apply/refund, card surcharge); pinned control legs (1100/1200/2200/1000…) stay literal; the write-off register keys on the debit leg (not a hard-coded 5720); no new control/migration — GL-24 governs the rule lifecycle. ToE `basics.ts` (8 PR-2 checks: default path, pending-rule inert, distinct-approver, re-route, register). Also repaired the UAT-GL id collision from the PR #676 merge race: the six docs/42+GL-24 rows are (re)numbered 159..164 as this header already stated (they briefly duplicated SVC-3's 151..157).* · *v0.36: added UAT-GL-166 — per-period intercompany reconciliation sign-off (REC-03) now driven from the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI-only surfacing of existing `api/ic-reconciliation/{prepare,approve,reject,list}` endpoints, no control/GL change): prepare & sign, maker-checker approve (`SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject with reason; ToE unchanged `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v0.35: added UAT-GL-165 — period-end FX revaluation run→post (maker-checker, GL-18) now driven from the new `/fx → ปรับปรุงสิ้นงวด` tab (UI-only surfacing of existing `api/ledger/fx-reval/{run,list,get,post}` endpoints, no control/GL change): stage an Open run, drill the per-doc detail, and post with the `SELF_POST`/`ALREADY_POSTED` maker-checker gate; ToE unchanged `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v0.34: added UAT-GL-159..164 — docs/42 CoA usability cases restored (postEntry account-universe guard, /chart-of-accounts manage surface, posting_determination switch — deferred from PR #665 to escape a shared-file merge race) + docs/43 PR-1 **GL-24 posting-rule change governance** (fail-closed registry validation OVERRIDE_ROLE_PINNED/UNKNOWN_POSTING_ROLE/POSTING_SIDE_MISMATCH/INVALID_POSTING_ACCOUNT; PendingApproval → distinct-approver SOD_VIOLATION; Approved-only resolver with bust-on-approve cache; append-only audit; migration 0327; RCM 233); ToE compliance.ts 173 + payroll.ts 42; PN-04 rev 2.18.* · *v0.33: added UAT-FIN4-08 — the FIN-4 statutory pack (statement of changes in equity, DBD e-Filing XBRL export, and the definition-driven statement/notes viewer) now driven from the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI-only surfacing of existing `api/reports/fs/{changes-in-equity,dbd-export,definitions,render,notes}` endpoints, no control/GL change); ToE unchanged `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v0.32: added UAT-GL-158 — lessor-side lease accounting (IFRS-16 lessor, LSE-02) now driven from the new `/leases → ผู้ให้เช่า` tab (UI-only surfacing of existing `api/lessor-leases/{classify,create,approve,run,receivable-reconciliation}` endpoints, no control/GL change): preview finance/operating classification, create-pending, maker-checker approve (`SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation banner; ToE unchanged `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v0.31: added UAT-GL-151..157 — SVC-3 Service Contract Renewal & Expiry management (new control SVC-02, migration 0331): within-threshold renewal auto-approves + creates the successor contract (new_value = base × (1+uplift)); over-threshold / auto-renew-price-rise routes to pending; self-approval blocked (SOD_SELF_APPROVAL); a distinct approver creates the successor + marks the old contract renewed; reject leaves the old contract (declined); expiry worklist + RLS isolation; ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8.* · *v0.30: added UAT-GL-150 — consolidation elimination rules, IFRS-8 segment definitions/report (CON-04) and the post-elimination consolidated statement of cash flows (CON-05) now driven from the `/consolidation → ตัดรายการ & ส่วนงาน` tab + the run cash-flow card (UI-only surfacing of existing `api/consolidation/{rules,segments,segment-report,runs/:id/cash-flow}` endpoints, no control/GL change); ToE unchanged `consolidation.ts` (42); PN-11 rev 0.6.* · *v0.29: added UAT-GL-149 — TFRS 15 five-step contract engine (REV-19) now driven from the `/revenue → สัญญา TFRS 15` screen (UI-only surfacing of existing `api/revenue/contracts/*` endpoints, no control/GL change): create contract + performance obligations, per-contract allocate/schedule/activate, then recognise on the deferred tab; ToE unchanged `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v0.28: added UAT-GL-148 — consolidation group setup (create-group + add/remove-entity) and CON-03 post-to-GL now driven from the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*` endpoints, no control change); maker-checker `SELF_POST`/`ALREADY_POSTED` unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.5; manual 06-general-ledger.md.* · *v0.27: added UAT-GL-141..147 — FIN-10 lessor-side lease accounting (IFRS 16 / TFRS 16 lessor, control LSE-02, migration 0309): classification finance vs operating with the boundary flip at 75% of economic life / 90% of fair value; classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation (Dr 5200 / Cr 1590); net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610; harness `basics.ts` (12 lessor checks); PN-04 rev 2.15.* · *v0.26: added UAT-GL-137..140 — FIN-7b GL allocation engine (GL-23, migration 0307): an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; zero-basis → NO_BASIS; the scheduled run (`gl_allocation_run`) posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); ratio 3:1 → 7,500/2,500, driver headcount 2:1 → 600/300; harness `basics.ts`; PN-04 rev 2.16.* · *v0.25: added UAT-GL-132..133 — FIN-7a multi-dimensional financial reports: additive `?project_id=&dept_id=&branch_id=` filters on the trial balance / account ledger / income statement (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical), `GET /api/ledger/dimensions` dropdown helper, junk id → 400 BAD_QUERY; web dropdowns on `/accounting` + `/financial-statements`; no control change; harness `basics.ts`; PN-04 rev 2.14.* · *v0.24: added UAT-GL-131 — bank-statement FILE import (`statements/import-file`): fuzzy Thai/English header detection, BE→CE dates, derived opening/closing, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN, optional immediate auto-match; `/bank` upload button; harness bankrec.ts 16→21; PN-07 rev 1.6.* · *v0.23: added UAT-GL-128..130 — master-data audit Phase 2 field exposure: item-master fields (barcode/UOM/stock thresholds/MRP lot-sizing) on `/setup/items` (GL-21 setup, no control change); warehouse master fields (name/zone/type/capacity/temperature/notes, previously partly read-only or unexposed) on `/setup/warehouses`; asset register columns (location/department/serial_no/assigned_to, already returned by the API but never rendered) on `/assets`; harnesses `basics.ts`, `module-qr.ts`.* · *v0.22: added UAT-GL-065..068 — gift-card issuance maker-checker (audit gap G1): face > 5,000 THB issues `PendingApproval` (no GL, not redeemable), issuer self-approve → SOD_VIOLATION, a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates, ≤ 5,000 THB still auto-issues; strengthens GC-01/GL-01 (SoD R14), migration 0252; harness `giftcards.ts`.* · *v0.21: added UAT-GL-060..064 — GL-05 dual-control gap closures (opening-balance batch now Draft/maker-checker, G4; manual reversal now requires a distinct reverser, G2); harnesses `opening-balances.ts`, `basics.ts`.* · *v0.20: added UAT-GL-126..127 — FA-12 asset verification-exception + audit BI reports (harness `module-qr.ts`).* · *v0.19: added UAT-GL-121..125 — FA-11 asset custody-change maker-checker + audit-by-scan (harness `module-qr.ts`).* · Cross-ref: process narrative `04-general-ledger-close.md` (GL-01..17, LSE-01, REC-01..04, R05/R06), `09-fixed-assets-depreciation.md` (FA-07, FA-08, FA-09, FA-11), `11-intercompany-consolidation.md` (CON-03 eliminations/maker-checker, CON-04 segment reporting — WS3.3), `12-revenue-recognition-billing.md` (REV-19 TFRS 15 — WS3.4), harnesses `tools/cutover/src/compliance.ts`, `worldclass.ts`, `basics.ts`, `giftcards.ts`, `consolidation.ts`, `revrec.ts`.

Result legend: Pass / Fail / Blocked / N/A / Not Run. Error codes/amounts are exact.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-GL-001 | Chart of accounts seeded | Admin | COA seeded | 1. `GET /api/ledger/accounts`. | — | ≥9 accounts; includes 3100 Retained Earnings, 1500/1510/1590/5200. | Med | Positive | GL-01 | Not Run | worldclass.ts |
| UAT-GL-002 | Balanced manual JE posts as Draft | GlAccountant | Logged in | 1. `POST /api/ledger/journal` balanced. | `lines:[{1000, debit:1234},{4000, credit:1234}]` | 201; `entry_no` JE-; `status: Draft`, `pending: true`. | High | Control | GL-05 | Not Run | compliance.ts |
| UAT-GL-003 | Draft JE excluded from balances | GlAccountant/Admin | Draft JE from GL-002 | 1. `GET /api/ledger/trial-balance?period=<cur>`. | — | Account 4000 credit = 0 (no impact until approved). | High | Control | GL-05 | Not Run | compliance.ts |
| UAT-GL-004 | Draft JE in pending-approval queue | FinancialController | Draft JE exists | 1. `GET /api/ledger/journal/pending`. | — | The Draft `entry_no` is listed. | Med | Positive | GL-05 | Not Run | compliance.ts |
| UAT-GL-005 | Preparer self-approval blocked (incl. dual-duty) | exec/dual-duty | Self-prepared Draft JE | 1. `POST /api/ledger/journal/{no}/approve` as the preparer. | bearer = preparer | 403 `SOD_VIOLATION`. | High | Control | GL-05, R05 | Not Run | compliance.ts |
| UAT-GL-006 | Independent approver posts JE | FinancialController | Draft JE by GlAccountant | 1. `POST .../{no}/approve` as different user. 2. Re-read trial balance. | bearer fincon | 200 `Posted`; `approved_by: fincon`; 4000 credit now = 1234. | High | Control | GL-05 | Not Run | compliance.ts |
| UAT-GL-007 | Reject JE → Voided, no balance impact | FinancialController | Draft JE | 1. `POST .../{no}/reject` with reason. 2. Check balance. | `{reason: unsupported}` | 200 `Voided`; account credit stays 0. | High | Control | GL-05 | Not Run | compliance.ts |
| UAT-GL-008 | Maker-checker binds even Admin | Admin | Admin-prepared Draft JE | 1. `POST .../{no}/approve` as same Admin. | bearer admin | 403 `SOD_VIOLATION` (no self-approve override). | High | Control | GL-05, R05 | Not Run | compliance.ts |
| UAT-GL-060 | Opening-balance batch posts as Draft, excluded from the trial balance (G4) | Admin (`gl_post`) | COA seeded | 1. `POST /api/ledger/opening-balances` `{batch_ref:'OB-2026', rows:[{1000,debit:5000},{1200,debit:3000},{2000,credit:2000}]}`. 2. `GET /api/ledger/trial-balance`. | net difference auto-balances to 3000 | (1) 201; `entry_no` JE-; `status:'Draft'`, `pending:true`, `balanced:true`, `lines_posted:4` (incl. a 3000 Opening-Balance Equity leg of 6000 cr). (2) 3000 credit = 0 — the Draft batch is **excluded** from the TB until approved. | High | Control | GL-05, R05 | Pass | opening-balances.ts |
| UAT-GL-061 | Preparer self-approving the opening batch blocked (SoD) | Admin (preparer) | Draft opening batch from UAT-GL-060 | 1. `POST /api/ledger/journal/{entryNo}/approve` as the **preparer**. | bearer = preparer | 403 `SOD_VIOLATION`; batch stays Draft and excluded from balances. Binds **even Admin**. | High | Control | GL-05, R05 | Pass | opening-balances.ts |
| UAT-GL-062 | Distinct approver posts the opening batch → TB then balances | different Admin (≠ preparer) | Draft opening batch pending | 1. `POST /api/ledger/journal/{entryNo}/approve` as a **different** user. 2. `GET /api/ledger/trial-balance`. | bearer = approver | (1) 200 `Posted`. (2) `totals.balanced:true`; 3000 Opening-Balance Equity now 6000 cr (the batch now affects the ledger). | High | Control | GL-05 | Pass | opening-balances.ts |
| UAT-GL-063 | Preparer cannot reverse their own posted entry (G2) | GlAccountant/Admin (preparer) | a Posted entry the caller prepared (e.g. Dr 1280 777 / Cr 2400 777) | 1. `POST /api/ledger/journal/{id}/reverse` `{reason:'self-reverse'}` as the original preparer. | bearer = preparer | 403 `SOD_VIOLATION` (*"Maker-checker: you cannot reverse a journal entry you prepared"*); **no** contra entry posted; the original is **not** flagged `is_reversed` (the GL-05 second-person check cannot be silently undone). | High | Control | GL-17, GL-05, R05 | Pass | basics.ts |
| UAT-GL-064 | Distinct user reverses the posted entry successfully | different user (≠ preparer, `gl_post`) | the same Posted entry (UAT-GL-063) | 1. `POST /api/ledger/journal/{id}/reverse` `{reason:'duplicate posting'}` as a **different** user. 2. Inspect the reversal entry + trial balance. | bearer ≠ preparer | 200; returns `{reversalId, originalId}`; the contra is **Posted** with every line's Dr/Cr swapped (1280 now a 777 credit), the original flagged `is_reversed`, and the two net to **zero** on each account. (Automated system reversals — e.g. FX reval — do not set the distinct-reverser flag.) | High | Control | GL-17, GL-05 | Pass | basics.ts |
| UAT-GL-065 | High-value gift-card issue (> 5,000 THB) → PendingApproval, no GL, not redeemable (maker-checker gap G1) | Cashier / POS (`pos`) | COA seeded (2200 Customer Deposits) | 1. `POST /api/pos/gift-cards/issue` `{amount:8000}`. 2. `POST /api/pos/gift-cards/redeem` `{card_no, amount:100, sale_no:'S-NOPE'}`. 3. Query `journal_lines` for `source='GCISSUE', source_ref=card_no`. | face 8,000 (> 5,000 `ISSUE_APPROVAL_THRESHOLD`) | (1) 201; `status:'PendingApproval'`, `pending:true`, `journal_no:null`; **no** GCISSUE JE posted. (2) 400 `GIFT_CARD_INACTIVE` — a pending card is **not** redeemable. (3) zero GCISSUE lines. | High | Control | GC-01, R14 | Pass | giftcards.ts |
| UAT-GL-066 | Issuer cannot self-approve the issuance (SoD) | issuer (`pos`) | PendingApproval card from UAT-GL-065, issued by `sales1` | 1. `POST /api/pos/gift-cards/{card_no}/approve` as the **issuer**. | bearer = issuer | 403 `SOD_VIOLATION` (*"you cannot approve a gift card you issued"*); card stays `PendingApproval`, still no GL. | High | Control | GC-01, R14 | Pass | giftcards.ts |
| UAT-GL-067 | Distinct finance approver activates the card + posts the GL | different user (`creditors`/`exec`, ≠ issuer) | the same PendingApproval card | 1. `POST /api/pos/gift-cards/{card_no}/approve` as a **different** user (admin/finance). 2. Query `GCISSUE` `journal_lines`. 3. `GET /api/pos/gift-cards/{card_no}/balance`. | bearer = approver (admin) | (1) 201; `status:'Active'`, `issued_by` preserved (= `sales1`, ≠ approver). (2) GCISSUE JE posts **Dr 1000 = 8000 / Cr 2200 = 8000**, balanced (GL posts **only** on approval). (3) card `Active`, balance 8,000, now redeemable. | High | Control | GC-01, GL-01, R14 | Pass | giftcards.ts |
| UAT-GL-068 | Small-value gift-card issue (≤ 5,000 THB) still auto-issues Active | Cashier / POS (`pos`) | COA seeded | 1. `POST /api/pos/gift-cards/issue` `{amount:500}`. 2. Query `GCISSUE` `journal_lines`. 3. `GET /api/pos/gift-cards/{card_no}/balance`. | face 500 (≤ 5,000) | (1) 201; `card_no` `GC-…`, `status:'Active'`, `journal_no` `JE-…` (till stays fast). (2) GCISSUE posts **Dr 1000 = 500 / Cr 2200 = 500**, balanced, immediately. (3) balance 500, `Active`, redeemable. | High | Positive | GC-01, GL-01 | Pass | giftcards.ts |
| UAT-GL-009 | Unbalanced JE rejected | GlAccountant | Logged in | 1. `POST /api/ledger/journal` debit≠credit. | `[{1000, debit:100},{4000, credit:90}]` | 400 `UNBALANCED`. | High | Control | GL-02 | Not Run | worldclass.ts |
| UAT-GL-010 | Trial balance debits = credits | Admin | Postings exist | 1. `GET /api/ledger/trial-balance`. | — | `totals.balanced: true` (debit total = credit total). | Med | Detective | REC-01 | Not Run | worldclass.ts |
| UAT-GL-011 | Close period locks posting | FinancialController | Period to close | 1. `POST /api/ledger/periods/{period}/close`. 2. `POST /api/ledger/journal` into that period. | period `2020-01` | close 200; post → 400 `PERIOD_CLOSED`. | High | Control | GL-04, R06 | Not Run | compliance.ts |
| UAT-GL-012 | Re-open period allows posting | FinancialController | Closed period | 1. `POST /api/ledger/periods/{period}/open`. 2. Post a balanced JE. | — | open ok; subsequent post 200/201. | Med | Positive | GL-04 | Not Run | worldclass.ts |
| UAT-GL-013 | Year-end close moves P&L to retained earnings | FinancialController | FY2025 JE Posted (approved) | 1. `POST /api/ledger/close-year?fiscal_year=2025&tenant_id=T1`. | — | `net_income`≈1000 → JE- to 3100; FY2025 P&L net≈0 after. | High | Positive | GL-06 | Not Run | worldclass.ts |
| UAT-GL-014 | Balance sheet balanced + RE updated | Admin | After year-end close | 1. `GET /api/ledger/balance-sheet?as_of=2025-12-31`. | — | `balanced: true`; `retained_earnings`≈1000. | High | Detective | GL-06 | Not Run | worldclass.ts |
| UAT-GL-015 | Year-end close idempotent | FinancialController | FY2025 already closed | 1. Re-run `close-year` FY2025. | — | `already: true` (no double posting). | Med | Control | GL-06 | Not Run | worldclass.ts |
| UAT-GL-016 | Sub-ledger ↔ GL reconciliation | FinancialController | AR/AP activity | 1. `GET /api/finance/reconciliation`. | — | `ar.reconciled: true` (and AP balanced). | Med | Detective | REC-01 | Not Run | worldclass.ts |
| UAT-GL-045 | Control-account reconciliation pack (period-end) | Controller (`exec`/`ar`/`creditors`) | sub-ledger + GL activity | 1. `GET /api/finance/reconciliation/controls`. | — | 5 lines (1100/2000/1200/2200/2400), each `{sub_ledger, gl_control, variance, reconciled}`; gift-card 2200 & inventory 1200 sub-ledgers tie to GL; `all_reconciled` / `exceptions` roll-up; a known difference surfaces as an exception. | High | Detective | REC-04 | Pass | giftcards.ts, compliance.ts |
| UAT-GL-046 | Pending-approvals monitor (maker-checker backlog) | Controller (`exec`/`approvals`/`creditors`) | a Draft JE (and/or other pending items) exist | 1. Post a manual JE (Draft). 2. `GET /api/finance/approvals/pending`. | — | The Draft JE appears with `type:journal`, `control:GL-05`, its amount, `requested_by`, and `age_days`; the response rolls up `count`, `oldest_age_days`, `overdue` (≥ threshold) and `total_amount` across GL-05/BANK-02/EXP-06/PAY-03/FA-08/FA-09/INV-07/FX-04/BUD-01. | High | Detective | GOV-01 | Pass | compliance.ts |
| UAT-GL-046b | Batch approve / reject clears several items in one action, SoD still per-item | Controller (`approvals`) + a second maker | ≥2 pending items exist, at least one **created by the approver** | 1. Open **Pending Approvals** (`/approvals`). 2. Tick two batchable items (one you created, one someone else created) — or **Select all**. 3. Click **Approve selected**. | — | Each ticked item fires its **own** maker-checker endpoint (`Promise.allSettled`): the item created by a *different* maker is **approved** and its underlying action posts; the item **you** created is refused `SOD_VIOLATION` and stays pending; the summary reports **1 succeeded · 1 failed** with the SoD error. Reject-selected prompts once for a shared reason. **No new endpoint / no new control** — batching is UX over the per-item routes. | Med | Control | GOV-01 | Not Run | web UI (per-item routes) |
| UAT-GL-047 | Signup with industry provisions a curated chart | New company (self-serve) | plans seeded | 1. `POST /api/auth/signup {…, industry:'restaurant'}`. 2. Log in as the new admin. 3. `GET /api/ledger/accounts`. | `industry:'restaurant'` | Signup 201 echoes `industry:'restaurant'`; the chart is **overlay-scoped** (`source:'overlay'`) and **industry-named** — `4000` reads *Food & Beverage Sales*, and restaurant accounts (`5300` Recipe COGS, `2300` Tips, `4400` Service Charge) are present. | High | Positive | GL-10 | Pass | basics.ts, compliance.ts |
| UAT-GL-048 | Overlay curates the picker but never gates postings | New restaurant company | UAT-GL-047 | 1. `GET /api/ledger/accounts` (default). 2. `GET /api/ledger/accounts?all=true`. | — | Default chart **omits** non-industry accounts (no `4300` Service / `4200` Project revenue); `?all=true` returns `source:'canonical'` with the **full** universe (incl. `4300`) and a strictly larger count — so a user can still post to any account. | High | Control | GL-10 | Pass | basics.ts, compliance.ts |
| UAT-GL-049 | Signup without industry defaults to the full chart | New company (self-serve) | plans seeded | 1. `POST /api/auth/signup {…}` (no `industry`). 2. Log in. 3. `GET /api/ledger/accounts`. | no `industry` | Signup echoes `industry:'general'`; the tenant's chart equals the **full canonical chart** (includes both `4300` and `5300`); count = the `?all=true` count. | Med | Positive | GL-10 | Pass | basics.ts |
| UAT-GL-050 | Industry template can't drift from the engine's codes | Platform (boot) | — | 1. App boot runs `seedChartOfAccounts → assertTemplatesSubsetOf`. | a template code not in canonical COA | Boot **fails fast** (throws) if any template references an account code that does not exist in the canonical chart, or declares a code twice — a drifted template can never reach a tenant. | High | Control | GL-10 | Pass | assertTemplatesSubsetOf (boot guard) |
| UAT-GL-052 | Dedicated Chart-of-Accounts reference screen | GlAccountant/Controller (`gl_coa`/`gl_post`/`exec`) | industry-scoped tenant, logged in | 1. Open **ผังบัญชี** (`/chart-of-accounts`) from *Ledger & GL*. 2. Confirm accounts group by type (สินทรัพย์/หนี้สิน/ส่วนของเจ้าของ/รายได้/ค่าใช้จ่าย) and show ดุลปกติ + control/postable/dimension badges. 3. Search a code (e.g. `1100`) and use the type-filter chips. 4. Toggle **แสดงบัญชีทั้งหมด** ↔ **เฉพาะบัญชีของธุรกิจ**. 5. Click **ส่งออก CSV**. | code `1100` | Chart renders grouped/enriched; `1100` (AR) shows *เดบิต (Dr)* + a **ควบคุม · AR** badge; search + filter narrow the list and the count; the toggle switches curated ↔ full canonical (`?all=true`); CSV downloads the filtered rows. Screen is **read-only** (no create/edit affordances). | Med | Positive | GL-10 | Not Run | web UI (read-only) |
| UAT-GL-055 | Recurring & prepaid GL-automation screens | Accountant (`gl_post`/`exec`) | logged in | 1. Open **รายการบัญชีตั้งเวลา** (`/gl-schedules`). 2. **รายการตั้งเวลา**: create a balanced monthly template; confirm an unbalanced one is refused; **พัก/เปิดใช้** toggles; **ลงรายการที่ถึงกำหนด**. 3. **ค่าใช้จ่ายจ่ายล่วงหน้า**: create a schedule (total/months/expense acct, optional capitalize); **ตัดจ่ายงวดที่ถึงกำหนด**. | template Dr 5100 / Cr 2100; prepaid 12,000 / 12m | (2) balanced template saved; unbalanced → `UNBALANCED`; **ลงรายการที่ถึงกำหนด** posts each due template as a **Draft** JE (maker-checker → Pending), idempotent on re-run; toggle pauses/activates. (3) schedule created with a straight-line monthly slice + progress bar; **ตัดจ่าย** posts Dr expense / Cr 1280, idempotent per period. | High | Positive | GL-08 / GL-09 | Not Run | web UI + basics.ts (engines) |
| UAT-GL-054 | Account ledger (GL detail) drill-down | Accountant (`gl_post`/`exec`/`ar`) | posted + reversed activity on an account (e.g. 1280) | 1. `GET /api/ledger/account-ledger?account=1280`. 2. On `/accounting` → **แยกประเภทรายบัญชี**, pick the account + a date range. 3. `GET …?account=ZZZZ`. | account `1280` | (1) returns `opening_balance`, dated `lines` (each with debit/credit + a running `balance`), `total_debit`/`total_credit`, and `closing_balance` = Σ debit−credit = the account's trial-balance balance; the last line's running `balance` = `closing_balance`. (2) the tab renders opening → movements → closing with the running balance. (3) **404 `ACCOUNT_NOT_FOUND`**. | High | Detective | GL-01 / REC-01 | Not Run | basics.ts |
| UAT-GL-053 | Dedicated Financial Statements screen (detail + export) | Controller (`fin_report`/`exec`/`ar`/`creditors`) | posted GL activity | 1. Open **งบการเงิน** (`/financial-statements`). 2. **งบดุล**: set an as-of date → assets/liabilities/equity list **per account** with subtotals; net profit shown under equity; Assets = Liab+Equity balance badge. 3. `GET /api/ledger/balance-sheet?as_of=<d>` and confirm the response now carries a `lines[]` array (Asset/Liability/Equity, signed `balance`). 4. **งบกำไรขาดทุน**: revenue/expense lines + net; toggle **แยกตามสาขา**. 5. **งบกระแสเงินสด**: toggle ทางอ้อม/ทางตรง/พยากรณ์. 6. Switch the ledger selector; **ส่งออก CSV**. | — | Statements render account-level detail with correct subtotals; BS `lines[]` present and section totals equal the summary `assets`/`liabilities`/`equity`; balance badge = *สมดุล*; P&L by-branch splits net per branch; cash-flow methods reconcile (`reconciled: true`); forecast shows 8 weekly buckets + projected balance; ledger switch re-runs; CSV downloads. | High | Positive | GL-07 | Not Run | web UI + basics/worldclass (BS `lines`) |
| UAT-GL-017 | Reconciliation prepare → certify | FinancialController | reconciliation available | 1. Prepare reconciliation. 2. Certify (different/authorized user). | `<<period>>` | Prepared then certified; sign-off recorded. | Med | Control | REC-02/03 | Not Run | reconciliation |
| UAT-GL-018 | RLS — tenant cannot see another's GL | Procurement (T2) | T1 JEs exist | 1. `GET /api/ledger/journal?limit=100` as T2. | bearer finT2 | No T1 entries; no T1 4000 credit in trial balance. | High | Control | ITGC-AC (RLS) | Not Run | compliance.ts |
| UAT-GL-019 | Revenue recognition is tenant-scoped (W2) | HQ/Admin (no tenant) | due rev-rec lines in two tenants, same period | 1. `POST /api/revenue/recognize?period=P` (no tenant_id). 2. Retry with `&tenant_id=<T1>`. | — | (1) 400 `TENANT_REQUIRED`; (2) recognizes ONLY T1's line — T2's stays unrecognized. | High | Control | ITGC-AC-03 / REVREC-03 | Not Run | revrec.ts |
| UAT-GL-020 | Bank reconciliation is tenant-scoped (W2) | Admin | T1 bank account on 1010; a T2 movement on 1010 | 1. `GET /api/bank/accounts/<T1>/reconciliation` as Admin. | — | T2's 1010 movement excluded; `gl_balance` reflects T1 only (no cross-tenant leak). | High | Control | ITGC-AC-03 / REC-02 | Not Run | bankrec.ts |
| UAT-GL-048 | Bank adjustment maker-checker (request → Draft) | Controller (`exec`/`ar`) | an unmatched statement fee line | 1. `POST /api/bank/lines/<id>/adjustment` `{kind:'fee'}`. 2. `GET …/reconciliation`. 3. `GET /api/bank/adjustments/pending`. | — | (1) Draft JE, `status:'PendingApproval'`; (2) reconciliation `difference` unchanged (Draft excluded from GL); (3) the line appears in the pending queue. | High | Control | BANK-02 | Pass | bankrec.ts |
| UAT-GL-049 | Bank adjustment — self-approve blocked, independent approve closes diff | requester then a different approver (`approvals`/`gl_close`) | a pending bank adjustment | 1. Approve as the requester. 2. Approve as a different user. 3. `GET …/reconciliation`. | — | (1) `403 SOD_VIOLATION` (binds Admin); (2) `status:'Posted'`; (3) reconciliation `difference`→0. | High | Control | BANK-02 | Pass | bankrec.ts |
| UAT-GL-131 | Bank-statement FILE import — Thai bank CSV/XLSX, BE dates, fail-closed parse | Treasury (`exec`/`ar`) | an approved bank account; GL cash movements | 1. `POST /api/bank/accounts/:id/statements/import-file` with a KBank-style Thai CSV (headers วันที่/รายการ/ถอนเงิน/ฝากเงิน/คงเหลือ, dd/mm/พ.ศ. dates, a trailing summary row) + `auto_match:true`. 2. `GET …/reconciliation`. 3. Upload a file with no date column. 4. Web `/bank` → นำเข้าไฟล์ Statement. | as left | (1) `BANKSTMT-` created; `line_count` = transaction rows only, `skipped_rows` counts the summary row; opening/closing derived from the running-balance column; BE dates land as CE; `auto_match.matched` reported. (2) reconciliation ties (difference 0 when the GL agrees). (3) `400 NO_DATE_COLUMN` — nothing partially imported. (4) upload button imports + toasts imported/matched counts. | High | Positive + negative | REC-02 / BANK-02 (same import spine — no new control) | Pass | bankrec.ts |
| UAT-GL-132 | Dimension-filtered trial balance — project slice shows only that project's lines | Accountant (`exec`/`ar`) | a posted JE tagged `project_id`/`dept_id`/`branch_id` on its lines (Dr 5100 250 / Cr 1000 250), among other untagged activity | 1. `GET /api/ledger/trial-balance?project_id=<P>`. 2. `GET /api/ledger/trial-balance?dept_id=<D>`. 3. `GET /api/ledger/trial-balance?branch_id=<unused>`. 4. `GET /api/ledger/account-ledger?account=5100&project_id=<P>`. 5. `GET /api/ledger/income-statement?from=&to=&project_id=<P>`. 6. Web `/accounting` → งบทดลอง → เลือกโครงการ. | — | (1) rows = ONLY 5100 (`balance` +250) and 1000 (`balance` −250); totals debit=credit=250, `balanced:true`; response echoes `project_id`. (2) same two rows for the department slice. (3) an unused branch id → empty `rows`, still 200. (4) `count:1`, `closing_balance:250` — ties to the filtered TB. (5) `expense:250`, `net_income:-250`. (6) the table narrows to the slice. | High | Positive | GL-13 (dimension review — no new control) | Pass | basics.ts |
| UAT-GL-133 | Unfiltered TB unchanged + junk dimension id fails closed | Accountant (`exec`/`ar`) | as UAT-GL-132 | 1. `GET /api/ledger/trial-balance` (no dimension params) before and after posting the tagged JE. 2. `GET /api/ledger/trial-balance?project_id=abc`. 3. `GET /api/ledger/dimensions`. | — | (1) response carries NO `project_id`/`dept_id`/`branch_id` keys (snapshot path unchanged); totals move only by the tagged JE's 250/250 and stay `balanced:true`. (2) `400` `BAD_QUERY` (error envelope `error.code`). (3) lists the in-use project/department/branch with master code+name labels (feeds the dropdowns). | High | Negative/Control | GL-13 (no new control) | Pass | basics.ts |
| UAT-GL-050 | FX rate maker-checker — manual rate not usable until approved | Treasury (`exec`/`ar`) then approver | open foreign AR/AP | 1. `POST /api/fx/rates` manual rate. 2. `POST /api/fx/revalue` for that ccy. 3. self-approve `POST /api/fx/rates/approve`. | — | (1) `status:'PendingApproval'`; (2) `400 NO_RATE` (pending rate excluded); (3) `403 SOD_VIOLATION` (binds Admin). | High | Control | FX-04 | Pass | fxreval.ts |
| UAT-GL-051 | FX rate approved by a different user → revaluation works | a different approver (`approvals`/`gl_close`) | a pending FX rate | 1. `POST /api/fx/rates/approve` (different user). 2. `POST /api/fx/revalue`. | — | (1) `status:'Approved'`; (2) revaluation posts the FXREVAL JE (rate now usable). | High | Control | FX-04 | Pass | fxreval.ts |
| UAT-GL-021 | Statement of Cash Flows reconstructed from GL | Controller | Posted period activity (sale, cash expense, depreciation, inventory-on-credit, receipt) | 1. `GET /api/ledger/cash-flow?from=2026-03-01&to=2026-03-31`. | net income 500; dep 300; AR +600; inv +500; AP +500 | `operating.net_income`≈500; depreciation add-back (1590)≈300; `operating.net`≈200; `investing.net`=0; `financing.net`=0. | High | Positive | GL-07 | Not Run | basics.ts |
| UAT-GL-022 | Cash flow reconciles to change in cash | Controller | As GL-021 | 1. Read same response. | beginning cash 10000 | `net_change_in_cash`≈200; `cash_beginning`≈10000; `cash_ending`≈10200; `reconciled: true`; `unclassified_accounts` empty. | High | Detective | GL-07 | Not Run | basics.ts |
| UAT-GL-023 | Year-end close excluded from cash flow | Controller | A `CLOSE` JE dated in the window | 1. Post a year-end CLOSE entry in the window. 2. Re-read cash-flow. | CLOSE zeroes P&L to 3100 | `operating.net_income` unchanged (≈500); `reconciled: true` (closing reclassification ignored). | High | Control | GL-07 | Not Run | basics.ts |
| UAT-GL-024 | Direct-method cash flow by receipt/payment nature | Controller | As GL-021 (posted period activity) | 1. `GET /api/ledger/cash-flow-direct?from=&to=`. | receipt 400 from customer, supplier payment 200 | `receipts_from_customers`≈400; `payments_to_suppliers`≈-200; each cash entry attributed to its contra nature. | High | Positive | GL-07 | Not Run | basics.ts |
| UAT-GL-025 | Direct method ties to operating + Δcash | Controller | As GL-024 | 1. Read same response. | — | Σ direct operating buckets ≈ indirect `operating.net`; whole report `reconciled: true` to the change in cash. | High | Detective | GL-07 | Not Run | basics.ts |
| UAT-GL-026 | Cash-flow forecast projects open AR/AP by due date | Controller | Open AR (due in 2w) + open AP (due in 3w); overdue items | 1. `GET /api/ledger/cash-flow-forecast?weeks=6`. | opening = today's cash | 7 weekly buckets (week 0..6); week 0 holds overdue/due-now; each week shows inflow/outflow/net and `projected_balance` running from today's cash. | High | Positive | GL-07 | Not Run | basics.ts |
| UAT-EAM-001 | Raise a corrective maintenance work order | Maintenance | Registered asset FA-EAM1 | 1. `POST /api/eam/work-orders {asset_no, type:corrective, vendor_name, cost_estimate}`. | — | 201; `wo_no` `MWO-…`, status `open`. Unknown asset → `ASSET_NOT_FOUND`. | High | Positive | FA-06 | Not Run | basics.ts |
| UAT-EAM-002 | Complete WO → maintenance cost to AP (5710) | Maintenance/AP | Open WO with vendor | 1. `PATCH /api/eam/work-orders/{woNo}/status {status:completed, actual_cost:1000, vendor_name, vat_treatment:exempt}`. | — | status `completed`, `ap_txn_no` `AP-…`; GL `Dr 5710=1000 / Cr 2000=1000`; payable in AP. | High | Control | EXP-05, GL-01 | Not Run | basics.ts |
| UAT-EAM-003 | Illegal WO transition rejected | Maintenance | Completed WO | 1. `PATCH …/status {status:in_progress}`. | — | 400 `BAD_TRANSITION`. | Med | Control | FA-06 | Not Run | basics.ts |
| UAT-EAM-004 | PM sweep raises due preventive WOs (time + meter) | Maintenance (cron) | Time PM (due) + meter PM (over interval) + a reading | 1. Run `eam_pm_generate` (or `POST /api/eam/pm/run`). | meter 150 ≥ interval 100 | 2 preventive WOs raised (one time-due, one meter-due); schedules rolled forward. | High | Positive | FA-06 | Not Run | basics.ts |
| UAT-EAM-005 | PM generation is idempotent | Maintenance (cron) | Sweep already run | 1. Re-run the PM sweep. | — | `generated` = 0 (schedule with an open generated WO + advanced due date is skipped). | Med | Control | FA-06 | Not Run | basics.ts |
| UAT-EAM-006 | WO cost lines roll up to actual cost | Maintenance | Open WO | 1. `POST …/lines {kind:labor, hours:10, unit_cost:150}`. 2. `POST …/lines {kind:part, quantity:5, unit_cost:100}`. 3. `GET …/lines`. | — | labor line = 1500, part line = 500; `labor_total:1500`, `parts_total:500`, `total:2000`; WO `actual_cost` = 2000 (rolled up from lines). | High | Control | FA-06 | Not Run | basics.ts |
| UAT-EAM-007 | Completion posts the rolled-up actual cost to AP | Maintenance/AP | WO with cost lines (total 2000), vendor | 1. Complete WO **without** an explicit `actual_cost`. | — | AP payable posts for **2000** (the line roll-up), not the estimate; `Dr 5710=2000 / Cr 2000=2000`. | High | Control | EXP-05, FA-06 | Not Run | basics.ts |
| UAT-EAM-008 | Per-asset reliability & cost KPIs | Maintenance | Asset with ≥2 corrective WOs + preventive history | 1. `GET /api/eam/assets/{assetNo}/reliability`. | — | Reports `corrective_failures`≥2, `preventive`, `open`, `total_downtime_hours`, `mtbf_days` (computed, non-null), `total_maintenance_cost`. | Med | Detective | FA-06 | Not Run | basics.ts |
| UAT-GL-027 | Unbalanced recurring template rejected | Controller | — | 1. `POST /api/ledger/recurring {frequency:daily, lines:[Dr 5710 1000, Cr 2100 500]}`. | — | 400 `UNBALANCED`; no template saved. | High | Control | GL-08 | Not Run | basics.ts |
| UAT-GL-028 | Recurring run posts a Draft JE via maker-checker | Controller (cron) | Balanced daily template due today | 1. `POST /api/ledger/recurring` (balanced). 2. `POST /api/ledger/recurring/run`. 3. `GET /api/ledger/trial-balance` + `GET /api/ledger/journal/pending`. | Dr 5710 1000 / Cr 2100 1000 | `posted:1`, `JE-…` raised; entry is **Draft** (excluded from trial balance) and appears in the pending queue (GL-05). | High | Control | GL-08, R05 | Not Run | basics.ts |
| UAT-GL-029 | Recurring run idempotent; second person approves → hits GL | Controller (cron) / approver | As GL-028 | 1. Re-run `POST /api/ledger/recurring/run`. 2. A **different** user approves the draft JE. 3. Re-read trial balance. | — | Re-run `posted:0` (idempotent — next_run advanced + dedup). After approval the accrual posts (5710 +1000); approver ≠ preparer enforced (`SOD_VIOLATION` on self-approval). | High | Control | GL-08, GL-05, R05 | Not Run | basics.ts |
| UAT-GL-030 | Register a prepaid schedule + capitalize | Controller | — | 1. `POST /api/ledger/prepaid {total_amount:1200, months:12, capitalize:true}`. | — | `monthly_amount:100`; capitalize posts **Dr 1280 1200 / Cr 1000**. | High | Positive | GL-09 | Not Run | basics.ts |
| UAT-GL-031 | Prepaid amortization run (straight-line) + idempotent | Controller (cron) | Active schedule due today | 1. `POST /api/ledger/prepaid/run`. 2. Re-run. | — | First run `posted:1`, slice **100** to expense (**Dr 5100 / Cr 1280**); re-run `posted:0` (idempotent). | High | Control | GL-09 | Not Run | basics.ts |
| UAT-GL-137 | Allocation cycle with zero total basis rejected | Controller (`gl_post`/`exec`) | — | 1. `POST /api/ledger/allocation {method:ratio, frequency:daily, pool_amount:1000, source_account:'5000', targets:[{target_account:'5710', basis:0}]}`. | — | 400 `NO_BASIS`; no cycle saved (also `NO_TARGETS` when targets empty, `BAD_AMOUNT` for a non-positive pool). | High | Control | GL-23 | Not Run | basics.ts |
| UAT-GL-138 | Allocation run posts ONE balanced Draft JE via maker-checker | Controller (cron) | A ratio cycle due today: pool 10,000 from 5000, targets 5710 basis 3 / 5720 basis 1 | 1. `POST /api/ledger/allocation` (ratio). 2. `POST /api/ledger/allocation/run`. 3. `GET /api/ledger/trial-balance` + `GET /api/ledger/journal/pending`. | pool 10,000; 3:1 | `posted:1`, one `JE-…` raised; it is **Draft** (excluded from the trial balance — 5710/5000 unchanged) and appears in the pending queue (GL-05). | High | Control | GL-23, R05 | Not Run | basics.ts |
| UAT-GL-139 | Allocation run idempotent; second person approves → balanced split hits GL | Controller (cron) / approver | As GL-138 | 1. Re-run `POST /api/ledger/allocation/run`. 2. A **different** user approves the draft JE. 3. Re-read trial balance. | — | Re-run `posted:0` (idempotent — next_run advanced + `ALC-<id>-<date>` dedup). After approval the split posts **balanced**: 5710 **+7,500** / 5720 **+2,500** / 5000 **−10,000**; approver ≠ preparer enforced (`SOD_VIOLATION` on self-approval). | High | Control | GL-23, GL-05, R05 | Not Run | basics.ts |
| UAT-GL-140 | Driver / statistical-key allocation splits the pool by the driver | Controller (cron) / approver | A driver cycle: pool 900 from 5000, targets 5710 basis 2 (headcount) / 5720 basis 1 | 1. `POST /api/ledger/allocation {method:driver, …}`. 2. `POST /api/ledger/allocation/run`. 3. Approve as a different user; re-read TB. | pool 900; headcount 2:1 | Cycle `method:driver`; run posts a balanced Draft JE; after approval 5710 **+600** / 5720 **+300** (pool × basis ÷ Σbasis, remainder to the last target). | High | Positive | GL-23 | Not Run | basics.ts |
| UAT-GL-032 | Lease commencement recognises ROU + liability at PV | Controller | — | 1. `POST /api/leases {term_months:12, monthly_payment:1000, annual_rate_pct:12}`. | — | `initial_liability ≈ 11255` (PV); posts **Dr 1600 / Cr 2600** = liability. | High | Control | LSE-01 | Not Run | basics.ts |
| UAT-GL-033 | Lease periodic run posts interest + payment + ROU depreciation | Controller (cron) | Active lease due | 1. `POST /api/leases/run`. 2. `GET /api/leases`. | — | `posted:1`; entry has `interest + principal = 1000` (payment) + ROU `depreciation`; liability & ROU NBV **reduced**. | High | Control | LSE-01 | Not Run | basics.ts |
| UAT-GL-034 | Asset upward revaluation request → Draft, deferred (FA-08) | FaAccountant | Asset with NBV | 1. `POST /api/assets/{assetNo}/revalue {new_value: NBV+10000}`. 2. `GET /api/ledger/trial-balance`. | — | `kind:revaluation`, `delta:10000`, `status:PendingApproval`; the JE is **Draft** → **3200 surplus NOT affected** and NBV unchanged until approved. | High | Control | FA-07, FA-08 | Not Run | basics.ts; compliance.ts |
| UAT-GL-035 | Asset impairment (after approval) + no-change guard + audit | FaAccountant | Revalued asset (approved) | 1. `POST .../revalue {new_value: lower}` then approve. 2. Repeat same value. 3. `GET .../revaluations`. | — | `kind:impairment` posts **Dr 5820 / Cr 1500** once approved; same-value → `400 NO_CHANGE`; audit lists both events, status `Posted`. | High | Control | FA-07, FA-08 | Not Run | basics.ts |
| UAT-GL-040 | Asset revaluation: preparer cannot self-approve (SoD) | FaAccountant (preparer) | revaluation `PendingApproval` | 1. `POST .../revalue/approve` as the same user who requested it. | — | `403 SOD_VIOLATION`; status stays `PendingApproval`, JE stays Draft. Binds **even Admin**. | High | Control | FA-08, R07 | Not Run | basics.ts; compliance.ts |
| UAT-GL-041 | Asset revaluation: independent approver makes it effective | Different user (≠ preparer) | revaluation `PendingApproval` | 1. `POST .../revalue/approve` as a different user. 2. `GET /api/ledger/trial-balance`. | — | `status:Posted`, `approved_by` ≠ `prepared_by`; the surplus now hits **3200** and the carrying value moves. | High | Control | FA-08, GL-01 | Not Run | basics.ts; compliance.ts |
| UAT-GL-036 | Lease modification remeasures liability + ROU | Controller | Active lease (1 period posted) | 1. `POST /api/leases/{leaseNo}/modify {new_monthly_payment: higher}`. 2. Re-modify with no change. | — | Liability remeasured at revised PV; ROU steps up by the same delta (**Dr 1600 / Cr 2600**); `liability_delta` > 0; a no-op modify → `400 NO_CHANGE`. | High | Control | LSE-01 | Not Run | basics.ts |
| UAT-GL-037 | Disposal (approved) recycles revaluation surplus to RE | Approver (≠ requester) | Revalued asset, surplus 10000 in 3200, disposal pending | 1. `POST /api/assets/{assetNo}/dispose/approve`. | — | status `disposed`; `revaluation_surplus_recycled:10000`; posts **Dr 3200 / Cr 3100** (surplus → retained earnings, not P&L); 3200 surplus cleared. | High | Control | FA-07, FA-09 | Not Run | basics.ts |
| UAT-GL-042 | Disposal request → Draft, pending (not yet disposed) | FaAccountant | Active asset | 1. `PATCH /api/assets/{assetNo}/dispose {proceeds}`. 2. `GET /api/assets`. | — | `status:pending_disposal`, JE- returned; asset **not** yet `disposed` and excluded from depreciation; JE is Draft (no balance impact). | High | Control | FA-09 | Not Run | basics.ts; worldclass.ts; compliance.ts |
| UAT-GL-043 | Disposal: requester cannot self-approve (SoD) | FaAccountant (requester) | disposal `pending_disposal` | 1. `POST /api/assets/{assetNo}/dispose/approve` as the same user. | — | `403 SOD_VIOLATION`; asset stays pending, JE stays Draft. Binds **even Admin**. | High | Control | FA-09, R07 | Not Run | basics.ts; compliance.ts |
| UAT-GL-044 | Disposal: independent approver makes it effective | Different user (≠ requester) | disposal `pending_disposal` | 1. `POST /api/assets/{assetNo}/dispose/approve` as a different user. | — | `status:disposed`, `approved_by` ≠ `prepared_by`; the disposal JE posts (Dr 1590/1000, Cr 1500, ±1510) and balances. | High | Control | FA-09, GL-01 | Not Run | basics.ts; worldclass.ts; compliance.ts |
| UAT-GL-038 | Working-capital health score | exec | Cash 100k (GL Dr 1000); AR 40k (10k overdue); AP 180k; POS ฿2,000/day | 1. `GET /api/finance/health`. | — | `cash_on_hand` 100k, AR 40k / AP 180k, `overdue_ar_pct` 25; `days_cash_on_hand` ≈14.9, `current_ratio` ≈0.78; **score 45 / grade D**; drivers liquidity 25 + receivables 75. | High | Positive | Feature (financial-health score) | Not Run | financial-health.ts |
| UAT-UI-LSE-01 | Lease screen reachable + create + run | exec / GlAccountant | logged in (perm `exec`/`gl_post`) | 1. Sidebar → **การเงิน ▸ สมุดบัญชี & แยกประเภท → สัญญาเช่า (IFRS 16)** (`/leases`). 2. Fill **สร้างสัญญาเช่าใหม่** (term 12, payment 1000, rate 12) → submit. 3. Press **ลงรายการงวดที่ครบกำหนดเดี๋ยวนี้**. 4. Click the lease row → **ปรับปรุงสัญญา**. | — | Screen loads; success toast shows `lease_no` + ROU/liability; new row shows `liability_balance` & `rou_nbv`; "run-due" posts a period (periods_posted +1); modify panel opens and remeasures. | Med | UI | LSE-01 | Not Run | manual / e2e |
| UAT-GL-045 | Lease-liability reconciliation ties GL 2600 to the schedule | exec / GlAccountant | ≥1 active lease, after run + remeasurement | 1. `GET /api/leases/liability-reconciliation`. | — | `gl_liability` (GL 2600 net) == `schedule_liability` (Σ remaining schedule liability); `difference` 0; `reconciled` true; `leases[]` lists each lease's `liability_balance`. | High | Detective | LSE-01 | Not Run | basics.ts |
| UAT-UI-LSE-02 | Lease tie-out banner | exec / GlAccountant | on `/leases` | 1. Open `/leases`. | — | A reconciliation banner shows **GL 2600** vs **ตารางสัญญา** with a **กระทบยอดตรง ✓** badge when reconciled (red **พบผลต่าง ⚠** + difference when not). | Med | UI | LSE-01 | Not Run | manual / e2e |
| UAT-GL-141 | Lessor classification boundary flips finance↔operating at 75% of economic life | Controller (`gl_post`/`exec`) | — | 1. `POST /api/lessor-leases/classify {term_months:71, monthly_payment:100, asset_cost:10000, fair_value:10000, economic_life_months:100}`. 2. Same with `term_months:75`. | — | term 71/100 (71%) + PV 71% of FV → `classification:operating`, `reasons:[]`; term 75/100 (75%) → `classification:finance`, `reasons` includes `major_part_of_economic_life`. | High | Control | LSE-02 | Not Run | basics.ts |
| UAT-GL-142 | Lessor finance lease: create classifies FINANCE + net investment at PV, PENDING (no GL) | Controller | — | 1. `POST /api/lessor-leases {term_months:12, monthly_payment:1000, annual_rate_pct:12, asset_cost:11000, fair_value:12000, transfer_ownership:true}`. 2. `GET /api/ledger/trial-balance`. | — | `classification:finance`, `net_investment ≈ 11255` (PV), `status:pending`; **no GL yet** (1610 unchanged). | High | Control | LSE-02 | Not Run | basics.ts |
| UAT-GL-143 | Lessor: classifier cannot approve their own lease (SoD) | Controller (classifier) | finance lease `pending` | 1. `POST /api/lessor-leases/{leaseNo}/approve` as the same user who created it. | — | `403 SOD_SELF_APPROVAL`; lease stays `pending`, no GL. Binds even Admin. | High | Control | LSE-02, R05 | Not Run | basics.ts |
| UAT-GL-144 | Lessor finance: independent approver books commencement (derecognise asset + net investment) | Different user (≠ classifier) | finance lease `pending` | 1. `POST /api/lessor-leases/{leaseNo}/approve` as a different user. 2. `GET /api/ledger/trial-balance`. | — | `status:active`; posts **Dr 1610 (net investment = PV) / Cr 1500 (asset derecognised)**, selling profit/loss to 1510. | High | Control | LSE-02, GL-01 | Not Run | basics.ts |
| UAT-GL-145 | Lessor finance: periodic run recognises interest income + collects cash | Controller (cron) | active finance lease due | 1. `POST /api/lessor-leases/run`. 2. `GET /api/lessor-leases`. | — | entry has `interest_income + principal = 1000` (payment), `interest_income > 0` (Cr 4600, Dr 1000); receivable reduced by the principal. | High | Control | LSE-02 | Not Run | basics.ts |
| UAT-GL-146 | Lessor operating: straight-line rental income + continued depreciation | Controller (cron) | active operating lease due (asset stays on books) | 1. `POST /api/lessor-leases` (operating: short term, PV < FV). 2. approve (different user). 3. `POST /api/lessor-leases/run`. | — | approval books **no commencement GL** (`net_investment:0`); run posts **Dr 1000 / Cr 4610** (rental) + **Dr 5200 / Cr 1590** (depreciation). | High | Control | LSE-02 | Not Run | basics.ts |
| UAT-GL-147 | Lessor net-investment reconciliation ties GL 1610 to the finance-lease schedule | exec / GlAccountant | ≥1 active finance lease | 1. `GET /api/lessor-leases/receivable-reconciliation`. | — | `gl_receivable` (GL 1610 net) == `schedule_receivable` (Σ finance-lease receivable); `difference` 0; `reconciled` true. | High | Detective | LSE-02 | Not Run | basics.ts |
| UAT-GL-148 | Consolidation group setup + post-to-GL from the `/consolidation` screen (CON-03) | HQ Admin (runner) + a different HQ Admin (poster) | two tenants with posted GL + an in-group IC pair | 1. On **/consolidation → กลุ่มบริษัท**: **สร้างกลุ่มบริษัท** (name/fiscal year), select it, **เพิ่มบริษัทในกลุ่ม** ×2 (entity tenant id + ownership), and **นำออก** one then re-add. 2. **รวมงบ** tab → run the period. 3. As the **runner**, press **โพสต์เข้า GL**. 4. As a **different** admin, press **โพสต์เข้า GL**. | as left | (1) `POST /groups` then `POST/DELETE .../entities` add/remove members (list refreshes); (2) the run appears **Final**; (3) **403 `SELF_POST`** — the runner cannot post their own run (maker-checker); (4) **Posted** (`posted_by` ≠ `run_by`); re-pressing on the frozen period → `ALREADY_POSTED`. | High | Positive / Control | CON-03 | Not Run | consolidation.ts |
| UAT-GL-149 | TFRS 15 five-step contract driven from the `/revenue` screen (REV-19) | Revenue Accountant (`exec`/`ar`) | logged in (tenant scope) | 1. On **/revenue → สัญญา TFRS 15**: **สร้างสัญญา TFRS 15** — price 1000, two obligations (Impl SSP 800 *over_time* 2040-01-01→04-30; License SSP 400 *point_in_time*). 2. Click the new contract row → **จัดสรรตาม SSP**. 3. **สร้างตารางรับรู้**. 4. **เปิดใช้**. 5. Go to **รายได้รอตัดบัญชี** → **รับรู้รายได้งวดนี้** for 2040-01. | as left | (1) contract `REVC-…` created **Draft**, 2 POs listed; (2) allocation Impl≈666.6667 / License≈333.3333, Σ==1000 (panel shows allocated column); (3) schedule rows appear (Impl monthly + License single); (4) status **Active**, toast shows deferred 1000 (Dr 1100 / Cr 2410); (5) period recognises (Dr 2410 / Cr 4300), obligation **% ที่ปฏิบัติแล้ว** advances; **เปิดใช้** disabled once Active (`ALREADY_ACTIVE`). | High | UI / Control | REV-19 | Not Run | revrec.ts (backend, 34); manual 06 |
| UAT-GL-150 | Eliminations, segments & consolidated cash flow driven from the `/consolidation` screen (CON-04 / CON-05) | Group/Financial Controller (HQ Admin, `exec`) | a consolidation group with entities; posted P&L lines dimension-tagged; a completed consolidation run | 1. On **/consolidation → ตัดรายการ & ส่วนงาน**: **เพิ่มกฎ** an elimination rule (name + type `ic_balance` + pattern) for the group → the rules table lists it. 2. **เพิ่มส่วนงาน** — code `NORTH`, dimension `สาขา`, members `1, 2`. 3. **รันรายงาน** the segment report for the period/dimension. 4. On **การรวมงบ** tab, click a run row → the **งบกระแสเงินสดรวม** card renders. | as left | (1) `POST /rules` then `GET /rules?group_id=` lists the new rule (active); (2) `POST /segments` then `GET /segments` lists `NORTH` (members 1,2); (3) `GET /segment-report?period=&dimension=` renders revenue/expense/net per segment (unmapped → own/Unassigned bucket); (4) `GET /runs/:id/cash-flow` renders operating (net income + add-backs + working capital) / investing / financing / FX-effect sections with a **✓ ยอดกระทบกับการเคลื่อนไหวเงินสด** badge when `reconciled:true`. UI-only surfacing of existing `api/consolidation/*` endpoints; no backend/control/GL change. | High | UI / Detective | CON-04, CON-05 | Not Run | consolidation.ts (backend, 42) |
| UAT-GL-158 | Lessor-side lease accounting driven from the `/leases → ผู้ให้เช่า` tab (LSE-02) | Controller (`gl_post`/`exec`) as classifier + a different user as approver | logged in (tenant scope) | 1. On **/leases**, open the **ผู้ให้เช่า (Lessor)** tab. 2. Fill the form (name, monthly payment, asset cost, term; optionally fair value / economic life / transfer-ownership / bargain) and click **ดูการจัดประเภท**. 3. **สร้าง (รออนุมัติ)**. 4. As the **same** user, click **อนุมัติ** on the new row. 5. As a **different** user, click **อนุมัติ**. 6. Click **เดินงวด**. | as left | (2) `POST /classify` returns finance/operating + reasons + PV/fair-value & term/life ratios (preview panel, no row created); (3) `POST /` creates a **pending** row (`LSR-…`, classification badge, no GL); (4) `POST /:leaseNo/approve` → **403 `SOD_SELF_APPROVAL`** (classifier can't approve own lease); (5) a different approver → **active** (finance books Dr 1610 / Cr 1500 commencement JE, operating keeps the asset); (6) `POST /run` recognises the due period; the **net-investment reconciliation** banner ties GL 1610 to the finance-lease receivable schedule (`reconciled: true`). UI-only surfacing of existing `api/lessor-leases/*` endpoints; no backend/control/GL change. | High | UI / Control | LSE-02 | Not Run | basics.ts (backend, lessor checks) |
| UAT-GL-159 | Account-universe guard: a posting line naming a non-existent (or non-postable) account is rejected fail-closed | GlAccountant (`gl_post`) | canonical chart seeded | 1. `POST /api/ledger/journal` with lines `[{account_code:'6666', debit:100},{account_code:'4000', credit:100}]`. 2. Deactivate an unused account (Admin) then post a JE naming it. | 6666 not in the chart | (1) **400 `INVALID_POSTING_ACCOUNT`** — nothing posts (previously the line landed silently and vanished from typed reports). (2) **400 `INVALID_POSTING_ACCOUNT`** (deactivated ⇒ non-postable); `viaSubledger` never bypasses existence/postability. | High | Control | GL-21, GL-11 | Pass | compliance.ts |
| UAT-GL-160 | /chart-of-accounts manage surface: canonical create/edit/deactivate (Admin) + per-tenant overlay show/hide (gl_coa) | Platform Admin + tenant FinancialController (`gl_coa`) | an overlay (industry-pack) tenant | 1. As Admin press **เพิ่มบัญชี**, enter a 3-digit code → then a free 4-digit code + name/type, save. 2. Row **แก้ไข** rename; row **ปิดใช้งาน** on the new (zero-balance) account. 3. As the tenant `gl_coa` user, press the row **eye** to hide/show an account on the company chart. 4. As the tenant user try a canonical write. | — | (1) 3-digit code blocked in-form (Zod `4 digits`); the 4-digit create succeeds → appears in the full chart. (2) Rename saves; deactivate succeeds (balance 0). (3) Overlay toggle updates ONLY this company's chart (other tenants unaffected; posting never blocked). (4) **403 `COA_ADMIN_ONLY`**. | High | Positive+Control | GL-11 | Not Run | manual |
| UAT-GL-161 | posting_determination master switch on /setup/item-categories | md_config (or exec) + a viewer without md_config | — | 1. Open `/setup/item-categories` as md_config — read the status card. 2. Press **เปิดใช้งาน**; re-read. 3. As a user without md_config/exec open the same page. | flag default OFF | (1) Card shows **ปิดอยู่ (ใช้บัญชีมาตรฐาน)**. (2) Toast + badge flips to **เปิดใช้งานอยู่**; item/category account profiles now drive postings (GL-21 precedence). (3) The toggle button is absent. | Med | Positive | GL-21 | Not Run | manual |
| UAT-GL-162 | GL-24 posting-rule validation is fail-closed at save | Admin (`gl_posting_rules`) | registry seeded (0331) | 1. `POST /api/ledger/posting-rules` for a **pinned** role (`PAYROLL.GROSS`/`net_pay_cash`). 2. Same with role `bogus_role`. 3. Same with `wages_expense` but side `CR`. 4. Same with `wages_expense`/`DR` but account `6666`. | — | (1) **400 `OVERRIDE_ROLE_PINNED`** (sub-ledger control / structural legs can never be re-mapped; incl. the five permanently-pinned REC-04 accounts). (2) **400 `UNKNOWN_POSTING_ROLE`**. (3) **400 `POSTING_SIDE_MISMATCH`**. (4) **400 `INVALID_POSTING_ACCOUNT`**. | High | Control | GL-24 | Pass | compliance.ts |
| UAT-GL-163 | GL-24 maker-checker: a pending rule never posts; a DIFFERENT user activates it | Admin (creator) + a different Admin (approver) | a postable override account exists | 1. `POST /api/ledger/posting-rules` `{PAYROLL.GROSS, wages_expense, DR, <acct>}`. 2. Run + approve a payroll period. 3. `POST /api/ledger/posting-rules/:id/approve` as the **creator**. 4. `/approve` as a **different** user. 5. Run + approve the next period. | — | (1) `status:PendingApproval`. (2) Salaries land on the **default 5600** (Δ = gross) — the pending rule is ignored. (3) **403 `SOD_VIOLATION`** (binds even Admin). (4) `Approved`, `approvedBy` stamped. (5) Salaries land on the **override** account (Δ = gross). Re-editing the approved rule demotes it back to PendingApproval. | High | Control | GL-24, PAY-03 | Pass | payroll.ts |
| UAT-GL-164 | GL-24 audit trail + deactivate fallback | Admin | ≥1 rule created/approved | 1. `GET /api/ledger/posting-rules/audit`. 2. `POST /api/ledger/posting-rules/:id/deactivate`. 3. Run the affected posting flow. | — | (1) Append-only rows carry **CREATE** and **APPROVE** actions (actor + detail). (2) `active:false` + a DEACTIVATE audit row. (3) The flow posts to the **registry default** again (cache busted immediately). | Med | Detective | GL-24 | Pass | compliance.ts |
| UAT-GL-167 | docs/43 PR-2: an approved posting-rule override re-routes a wired finance posting; unapproved never applies | Admin (`gl_posting_rules`) + a different approver | registry seeded (0331); a postable override account (e.g. `5721`) exists | 1. `POST /api/finance/ar/write-off` with NO rule. 2. `POST /api/ledger/posting-rules` `{BADDEBT.WRITEOFF, bad_debt_exp, DR, 5721}`. 3. Write off again (rule still pending). 4. Approve the rule as a DIFFERENT user. 5. Write off again. 6. `GET /api/finance/ar/write-offs`. | amounts 111.25 / 50 / 250 | (1) JE debits the registry default **5720**. (2) `status:PendingApproval`. (3) still **5720** — a pending rule never posts. (4) `Approved`. (5) JE debits **5721**; the AR control leg stays **1100** (pinned). (6) The register lists ALL three write-offs (it keys on the debit leg, not a hard-coded account). | High | Control | GL-24, REV-14 | Pass | basics.ts |
| UAT-GL-168 | docs/43 PR-3: category-grain asset accounts + governed dispose override | Admin (`gl_posting_rules`) + a different approver | posting_determination OFF initially; a postable 5201 exists | 1. `POST /api/assets/categories` with `dep_expense_account: '9999'`. 2. Create category `dep_expense_account: '5201'` + acquire an asset in it. 3. Enable `posting_determination`. 4. Run depreciation for a fresh period. 5. Approve an `ASSET.DISPOSE`/`gain_loss` rule (distinct approver) and dispose the asset at a gain. | cost 120,000 / life 60 → 2,000/mo | (1) **400 `INVALID_POSTING_ACCOUNT`** — category save is fail-closed. (2-4) The run's per-tenant JE debits **5201** for the categorized asset and **5200** for uncategorized ones (split pairs, entry balanced). (5) The gain credits the **override** account; **1500/1590 stay pinned**. | High | Control | GL-24, GL-21, FA-02, FA-03 | Pass | basics.ts |
| UAT-GL-169 | docs/43 PR-4: an approved PROJECT.REVENUE override re-routes a project bill's revenue leg | Admin (`gl_posting_rules`) + a different approver | a postable 4210 exists; a T&M project | 1. `POST /api/ledger/posting-rules` `{PROJECT.REVENUE, project_revenue, CR, 4210}`. 2. Approve as a DIFFERENT user. 3. Bill a new project. | bill 4,444 | (1) `PendingApproval`. (2) `Approved`. (3) The bill JE credits **4210** and debits **1100** (AR control pinned); before approval the default 4200 path is unchanged (asserted by the harness's earlier GL checks). | High | Control | GL-24, PROJ-02 | Pass | projects.ts |
| UAT-GL-170 | docs/43 PR-5: an approved MFG.WO_COMPLETE override re-routes a work order's yield-variance leg | Admin (`gl_posting_rules`) + a different approver | a postable 5811 exists; a BOM-costed WO flow | 1. `POST /api/ledger/posting-rules` `{MFG.WO_COMPLETE, yield_variance, DR, 5811}`. 2. Approve as a DIFFERENT user. 3. Issue + complete a NEW work order at 15/20 yield. | std total 420 → loss 105 | (1) `PendingApproval`. (2) `Approved`. (3) The yield loss (105) debits **5811**; the pre-rule 5810 balance is unchanged; FG (1210) and WIP (1250) controls stay pinned; trial balance balanced. | High | Control | GL-24, MFG-01 | Pass | manufacturing.ts |
| UAT-GL-175 | CLS-02 open a disclosure checklist seeds the standard TFRS/SEC items (Draft) | Controller (`gl_close`) | — | 1. `POST /api/close/disclosure` `{period:'2026-06'}`. 2. `POST` the same period again. | — | (1) 201, `status:'Draft'`, `checklist_no` `DISC-…`, ≥10 items each `status:'Open'` with a `standard_ref` + `owner` (related parties/revenue/leases/tax/segments/subsequent-events/disclosure-controls). (2) idempotent — the same checklist id is returned. | High | Positive | GL-26 | Pass | disclosure-checklist.ts |
| UAT-GL-176 | CLS-02 review is blocked while any item is still Open | Controller (reviewer, `gl_close`) | a Draft checklist with ≥1 Open item | 1. `POST /api/close/disclosure/:id/review`. | — | **400 `ITEMS_INCOMPLETE`** — the binder stays Draft; the open items are listed. | High | Control | GL-26 | Pass | disclosure-checklist.ts |
| UAT-GL-177 | CLS-02 maker-checker: preparer cannot self-review; a distinct reviewer reviews then issues | preparer (`gl_close`) + a DIFFERENT reviewer (`gl_close`) | every item set Complete/NA (one carries a `support_doc_ref`) | 1. `PUT …/items/:itemId` `{status:'Complete', support_doc_ref}` for each item (records `completed_by`). 2. `POST …/review` **as the preparer**. 3. `POST …/review` as a **different** user. 4. `POST …/issue`. 5. `POST …/review` again. | — | (2) **403 `SOD_SELF_APPROVAL`** (preparer ≠ reviewer). (3) `status:'Reviewed'`, `reviewed_by` stamped. (4) `status:'Issued'`, `issued_by` stamped. (5) **400 `NOT_DRAFT`** (an Issued binder is immutable). | High | Control | GL-26, R05 | Pass | disclosure-checklist.ts |
| UAT-GL-178 | CLS-02 support evidence pins to doc_attachments (docType DISC) + tenant RLS | Controller (`gl_close`+`procurement`); a different-tenant user | an open checklist (`DISC-…`) exists in T1 | 1. `POST /api/procurement/attachments` `{doc_type:'DISC', doc_no:<checklist_no>, data_url}`. 2. Same with `doc_no:'DISC-NOPE-999'`. 3. As a T2 user: `GET /api/close/disclosure`. | small PNG | (1) 201, `doc_type:'DISC'` (evidence pinned via `assertDocExists`). (2) **404 `NOT_FOUND`** (a bogus checklist can't receive evidence). (3) `checklists:[]` — T2 sees none of T1's binders (RLS). | High | Control | GL-26, ITGC-AC-03 | Pass | disclosure-checklist.ts |
| UAT-GL-171 | docs/43 PR-6: an approved SALE.FOOD override re-routes a dine-in checkout's revenue leg | tenant Admin maker + a DIFFERENT tenant Admin approver | a postable 4010 exists; an open table with a billed order | 1. `POST /api/ledger/posting-rules` `{SALE.FOOD, revenue, CR, 4010}` (maker). 2. Approve as a DIFFERENT user. 3. Checkout a NEW dine-in order (Cash). | order 100 THB | (1) `PendingApproval`. (2) `Approved`. (3) The sale JE credits **4010** (no 4000 line); cash (1000) and output VAT (2100) stay pinned/literal. | High | Control | GL-24, REV-01 | Pass | restaurant.ts |
| UAT-GL-172 | docs/43 PR-7: an overridden PAY-02 payable still reconciles and remits (widened set) | Admin (`gl_posting_rules`) + a different approver | a postable 2351 exists; ≥1 posted payrun | 1. `POST /api/ledger/posting-rules` `{PAYROLL.SSO, sso_payable, CR, 2351}` (previously 400 OVERRIDE_ROLE_PINNED as widen). 2. Approve as a DIFFERENT user. 3. Run + approve a payroll period. 4. `GET /api/payroll/liabilities`. 5. Remit the 2351 balance. | — | (1) `PendingApproval` (accepted). (3) SSO payable credits **2351**. (4) The SSO line's `account_set` = `[2350, 2351]`, `reconciled: true` against the payrun accrual. (5) Remit accepts 2351 and `outstanding_after` = 0 for that account. | High | Control | GL-24, PAY-02 | Pass | payroll.ts |
| UAT-GL-173 | docs/43 PR-8: SCF self-classification + governed bulk IO | Admin (HQ) + a masterdata-duty non-Admin | registry seeded; 0346 applied | 1. `POST /api/ledger/accounts` `{2650, Liability, cfBucket: financing, isCurrent: false}` + post/approve a loan drawdown JE. 2. `GET /api/ledger/cash-flow` over the window. 3. Import a posting_rules CSV (1 valid + 1 bogus role, skip_errors). 4. Import an accounts CSV (Type/CF set) as Admin. 5. Repeat (4) as a non-Admin with the masterdata duty. | loan 250,000 | (1-2) The 2650 movement lands in **FINANCING** (not unclassified/operating). (3) `status: PendingApproval`, imported 1, per-row `UNKNOWN_POSTING_ROLE`; the DB row is **PendingApproval** — an import can never bypass GL-24. (4) STAGED (`req_no`, nothing written) for a distinct approver. (5) **403 `COA_ADMIN_ONLY`**. | High | Control | GL-07, GL-11, GL-24 | Pass | basics.ts, ext.ts |
| UAT-GL-174 | docs/43 PR-9: the posting-rules registry workspace (UI) | Admin (`gl_posting_rules`) + a DIFFERENT approver | logged in; ≥1 pending override exists | 1. Open **กฎการลงบัญชี** (`/setup/posting-rules`) → **ทะเบียนเหตุการณ์** tab. 2. Search an event (e.g. `BANK.FEE`), filter ระดับ = ปรับได้ (free). 3. Press **ตั้งค่า** on a free role. 4. Save an override; open **คิวรออนุมัติ** as the maker, then as a DIFFERENT user press **อนุมัติ**. 5. Open **ประวัติการแก้ไข**. 6. On ตั้งค่า & ทดลอง press **ปิดใช้** on the approved rule. | — | (1) Four tabs render; summary cards show events/wired/overrides/pending counts. (2) The card shows each role's side, default account and tier badge; pinned roles have no ตั้งค่า button. (3) Jumps to **ตั้งค่า & ทดลอง** with event/role/side pre-filled. (4) The queue lists the pending row across ALL events; maker's own อนุมัติ fails (`SOD_VIOLATION` toast); the different user's succeeds and the registry tab now shows the override with อนุมัติแล้ว. (5) CREATE/APPROVE rows appear newest-first with actor + detail. (6) Rule retires; registry override column reverts to —. | Med | UI | GL-24 | Not Run | manual / e2e |
| UAT-GL-179 | CLS-01 flux generate flags the lines breaching abs+pct thresholds | Preparer (`gl_close`/`fin_report`) | `gl_period_balances` seeded for 2025-05 & 2025-06 (revenue 100k→130k, COGS 60k→61k, marketing 5k→20k) | 1. `POST /api/close/flux/generate` `{period:'2025-06', basis:'PL', comparative:'prior_period', threshold_abs:10000, threshold_pct:10}`. | as left | Draft analysis, `comparative_period:'2025-05'`, 3 lines; revenue Δ +30,000 / +30% **breached**, marketing Δ +15,000 / +300% **breached**, COGS Δ +1,000 (< abs 10,000) **not** breached; `breached_count:2`. Posts nothing to the GL. | High | Positive | GL-25 | Pass | flux-analysis.ts |
| UAT-GL-180 | CLS-01 sign-off is refused while a breached line is unexplained | Reviewer (`gl_close`) | the UAT-GL-179 Draft analysis | 1. `POST /api/close/flux/:id/review`. 2. Explain ONE breached line via `PUT …/lines/:lineId/explain`. 3. `POST …/review` again. | explanation text | (1) **400 `UNEXPLAINED_LINES`**. (2) status stays **Draft** (`explained_count:1`). (3) still **400 `UNEXPLAINED_LINES`** — every breaching line must be explained. | High | Control | GL-25 | Pass | flux-analysis.ts |
| UAT-GL-181 | CLS-01 non-breached line cannot be explained | Preparer | the UAT-GL-179 analysis | 1. `PUT …/lines/:lineId/explain` on the COGS (non-breached) line. | any text | **400 `LINE_NOT_BREACHED`** — only threshold-breaching lines take an explanation. | Med | Control | GL-25 | Pass | flux-analysis.ts |
| UAT-GL-182 | CLS-01 forced explanation → maker-checker sign-off (reviewer ≠ preparer) | Preparer + a DIFFERENT reviewer | both breached lines explained → status Explained | 1. As the **preparer**, `POST …/review`. 2. As a **different** reviewer, `POST …/review` `{note}`. 3. `PUT …/lines/:lineId/explain` after certification. | note optional | (1) **403 `SOD_SELF_APPROVAL`** (preparer can't sign off own analysis). (2) status **Certified**, `reviewed_by` = the reviewer. (3) **400 `ALREADY_CERTIFIED`** — a certified analysis is locked. | High | Control | GL-25, R05 | Pass | flux-analysis.ts |
| UAT-GL-183 | CLS-01 prior_year / budget comparatives + BS-basis filter + RLS | Preparer; a different-tenant user | prior-year 2024-06 revenue 90k; approved 2025-06 budget for 4100 = 110k; a T2 tenant | 1. Generate `comparative:'prior_year'`. 2. Generate `comparative:'budget'`. 3. Generate `basis:'BS', comparative:'budget'`. 4. Generate `basis:'BS'`. 5. As a T2 user, `GET /api/close/flux`. | as left | (1) `comparative_period:'2024-06'`, revenue Δ +40,000 **breached**. (2) `comparative_period:'budget'`, revenue vs 110,000 → Δ +20,000 **breached**. (3) **400 `BUDGET_PL_ONLY`** (budget compares only on P&L). (4) BS basis lists only balance-sheet accounts (no P&L accounts). (5) T2 sees **none** of T1's analyses (RLS). | High | Positive+Control | GL-25, ITGC-AC-03 | Pass | flux-analysis.ts |
| UAT-GL-184 | COA follow-up A: backfill CF classification on an existing account + unclassified banner | Admin (HQ) | a balance-sheet account with no declared cf_bucket outside the built-in map has movement in the window | 1. **Accounting → Cash Flow** (indirect) over the window. 2. Note the warning banner listing the unclassified code(s); follow its link to **ผังบัญชี**. 3. **แก้ไข** the listed account → กระแสเงินสด = จัดหาเงิน + ไม่หมุนเวียน. 4. Re-run the indirect SCF. 5. Edit again → both fields back to อัตโนมัติ. | — | (1-2) Banner lists the code(s); the statement still reconciles (`reconciled: true`, bucketed by type meanwhile). (3) The edit dialog shows both fields for Asset/Liability/Equity and PATCHes them. (4) The account lands in FINANCING; the banner no longer lists it. (5) อัตโนมัติ sends null — the declaration clears and the fallback chain applies again. | Med | UI | GL-07, GL-11 | Not Run | manual / e2e |
| UAT-GL-185 | COA follow-up B: where-used report warns before deactivating a referenced account | Admin (HQ) | an account is referenced by ≥1 config master (e.g. an item category's COGS account) | 1. `GET /api/ledger/accounts/{code}/where-used` (or open **ผังบัญชี** → **ปิดใช้งาน** on that account). 2. Review the dialog. 3. Repeat for an unreferenced zero-balance account. 4. `GET …/6666/where-used`. | — | (1-2) The dialog lists each referencing source with its count (posting rules / item categories / tax codes / items / warehouses / asset categories / bank accounts / recurring JEs / prepaid / rev-rec) + the fail-closed warning; the ปิดใช้งาน button stays available (warn-only). (3) "ไม่พบการตั้งค่าใดอ้างอิงบัญชีนี้" and deactivation proceeds. (4) 404 `ACCOUNT_NOT_FOUND`. | Med | Control-adjacent | GL-11, GL-21 | Pass | basics.ts |
| UAT-GL-186 | GL-27: canonical CoA change is a two-person action (single-Admin exception AutoApplied) | two platform Admins | ≥2 active Admins exist | 1. Admin A creates account 9990 on **ผังบัญชี** (or `POST /api/ledger/accounts`). 2. Admin A presses อนุมัติ on the queued request. 3. Admin B presses อนุมัติ. 4. Admin A requests another change for 9990 while one is pending (repeat step 1 twice). 5. Admin A stages 9993; Admin B ปฏิเสธ. 6. (unit) With exactly ONE active Admin, repeat step 1. | — | (1) Toast "รอผู้ดูแลอีกคนอนุมัติ (GL-27)"; the account does NOT appear in the chart; the queue lists the request. (2) 403 `SOD_VIOLATION`. (3) 200 — the account NOW exists (D-normal, postable). (4) 400 `CHANGE_ALREADY_PENDING`. (5) request `Rejected`; 9993 never exists (`ACCOUNT_NOT_FOUND` on where-used). (6) applied immediately; the request row reads `AutoApplied` with the exception reason. | High | Control | GL-27, GL-11 | Pass | compliance.ts, basics.ts, coa-change.test.ts |
| UAT-GL-187 | COA-D1: governance queues in the pending-approvals center | Admin maker + a different Admin approver | a GL-24 override and a GL-27 chart change are staged | 1. Stage a posting-rule override and a canonical account create. 2. Open **รายการรออนุมัติ** (`/approvals`). 3. As the MAKER tick both and กดอนุมัติที่เลือก. 4. As a DIFFERENT Admin approve both from the same screen. 5. On **ผังบัญชี** press แสดง on ประวัติคำขอ. | — | (2) Both appear as `กฎการลงบัญชี (override)` (GL-24) and `แก้ไขผังบัญชีกลาง` (GL-27) with requester + age. (3) Each item fails per-item with `SOD_VIOLATION` (batch reports the failures; nothing applied). (4) Both succeed — the override activates and the account exists. (5) The history register lists the approved requests with approver + status (AutoApplied rows flagged มีผลทันที Admin เดียว). | Med | Control | GOV-01, GL-24, GL-27 | Pass | compliance.ts |
| UAT-GL-188 | COA-D2: effective window + required dimensions bind at posting | Admin (HQ) ×2 | GL-27 flow available | 1. Create account 5910 with มีผลถึง = last month (stage → different Admin approves). 2. Post a JE to 5910 dated today. 3. Create 5920 with มิติที่ต้องระบุ = ศูนย์ต้นทุน (stage → approve). 4. JE to 5920 without a cost centre; 5. repeat WITH cost centre. 6. Create an account naming parent 9876. | — | (2) 400 `ACCOUNT_NOT_EFFECTIVE` (window shown). (4) 400 `REQUIRED_DIMENSION_MISSING`. (5) Posts (entry_no returned). (6) 400 `PARENT_NOT_FOUND` at request time — nothing staged. Existing accounts (no attributes) post exactly as before. | High | Control | GL-21, GL-27 | Pass | basics.ts, ledger-posting.test.ts |
| UAT-GL-189 | Pentest P4 — a Draft cannot be APPROVED into a hard-Locked period | Preparer (`gl_post`) + distinct approver (`gl_close`) | a Draft JE prepared in an OPEN period; that period is then hard-**Locked** (close run → lock by a different user) | 1. Prepare a manual JE (posts as Draft, GL-05). 2. Hard-lock its period. 3. Approve the Draft via `POST /api/ledger/journal/:entryNo/approve` as a distinct approver. 4. Inspect the entry + the period balances. | — | (3) **400 `PERIOD_LOCKED`** — the approval path re-checks the hard-close gate (mirroring `PERIOD_CLOSED`) and rejects **before** any write; (4) the entry stays **Draft** (never Posted), no `APPROVE` audit row, the locked period's balances unchanged. A `Closed` (soft) period likewise → `PERIOD_CLOSED`. | High | Control | GL-05, GL-15, GL-19 | Pass | ledger-posting-write.test.ts |
| UAT-GL-190 | AutoRev: non-monthly template rejected | GlAccountant | — | 1. `POST /api/ledger/recurring` frequency `daily`, `auto_reverse: true`, balanced lines. | — | 400 `AUTO_REVERSE_MONTHLY_ONLY`; nothing saved. | Med | Negative | GL-08 | Not Run | basics.ts |
| UAT-GL-191 | AutoRev: monthly accrual posts; no same-month reversal | GlAccountant + approver | — | 1. Create monthly template `auto_reverse: true` (Dr 5720 700 / Cr 2100 700). 2. `POST /api/ledger/recurring/run`. 3. Approve the Draft as a different user. | — | Run posts the accrual Draft, `reversals: []`; after approval GL +700. | High | Control | GL-08, GL-05 | Not Run | basics.ts |
| UAT-GL-192 | AutoRev: first sweep of the next month posts the flipped reversal as Draft | GlAccountant + approver | Template's `last_run_date` in the prior business month | 1. `POST /api/ledger/recurring/run`. 2. Check `/api/ledger/journal/pending`. 3. Approve as a different user. | — | Run response `reversals[0]` = `JE-…` with `reversed_run` = prior run date; reversal is Draft (GL unchanged until approval); after approval the account's net balance returns to its pre-accrual value (lines flipped, dated the 1st). | High | Control | GL-08, GL-17, GL-05 | Not Run | basics.ts |
| UAT-GL-193 | AutoRev: re-run posts no second reversal (idempotent) | GlAccountant | The reversal from UAT-GL-192 exists | 1. `POST /api/ledger/recurring/run` again. | — | `reversals: []`, `posted: 0` (source_ref `REC-<id>-<lastRun>-REV` dedupes). | Med | Control | GL-08, GL-04 | Not Run | basics.ts |
| UAT-GL-194 | B3: scheduled FX-reval staging — Open run staged, idempotent, posted period no-ops | GlAccountant + approver | Approved FX rates + open foreign AR/AP | 1. `POST /api/bi/subscriptions` `{report_type: gl_fx_reval_run, filters: {period}}`. 2. `POST …/:id/run` ×2. 3. A different user posts the run. 4. Run the job again. | — | (2) exactly ONE Open run for the period (re-run refreshes it). (3) Posted (SELF_POST maker-checker binds). (4) HTTP 200, "already posted — no-op", still one run. | High | Control | GL-18 | Not Run | fxreval.ts |
| UAT-GL-195 | B3: scheduled consolidation staging — gates bind per group, cleared gate stages Draft | Admin (HQ) ×2 | A group with a POSTED period + a fresh period with no IC sign-off | 1. Run `consolidation_run` for the posted period. 2. Run it for the fresh period. 3. Approve the fresh period's IC recon (different user) and run again. | — | (1) HTTP 200, group outcome `ALREADY_POSTED`, no new run. (2) HTTP 200, outcome `IC_RECON_NOT_APPROVED`, no run. (3) A Draft/Final (NOT Posted) run staged — CON-03 posting still maker-checker. | High | Control | CON-01, REC-03 | Not Run | consolidation.ts |
| UAT-GL-196 | B1: template guards — self/unknown dependency rejected | FinancialController | — | 1. `PUT /api/ledger/close/task-templates` with `depends_on_key` = own key. 2. With an unknown key. | — | (1) 400 `SELF_DEPENDENCY`. (2) 400 `UNKNOWN_DEPENDENCY`. | Med | Negative | GL-15 | Not Run | basics.ts |
| UAT-GL-197 | B1: templates compose into the close run (custom task + override; owner/due/dependency) | FinancialController | Templates saved: custom `insurance_review` (required, owner, +3d, after `bank_rec`) + `bank_rec` title override | 1. `POST /api/ledger/close/start`. | — | ≥10 steps; the custom step carries owner/due (= period end + 3d)/dependency; `bank_rec` shows the overridden title; sign-off of the dependent before its predecessor → 400 `DEPENDENCY_NOT_DONE`. | High | Control | GL-15 | Not Run | basics.ts |
| UAT-GL-198 | B1: custom REQUIRED task gates the lock; maker-checker lock unchanged | FinancialController ×2 | All standard required steps Done, custom task pending | 1. Lock as a different user. 2. Complete the custom task. 3. Lock again. | — | (1) 400 `STEPS_INCOMPLETE` naming `insurance_review`. (2) Run `ReadyToLock`. (3) Locked (locker ≠ starter — GL-16 unchanged). | High | Control | GL-15, GL-16 | Not Run | basics.ts |
| UAT-GL-199 | B4: roll-forward + aging on the recon summary | GlAccountant | Account with posted activity | 1. Open the recon period. 2. `GET …/summary`. | — | `opening/activity/closing` tie to the posted GL (closing = opening + activity = gl_balance cumulative); `aging` buckets present. | Med | Positive | REC-01 | Not Run | recon-profitability.ts |
| UAT-GL-200 | B4: auto-certify fires ONLY on the safe class; attribution logged | Reviewer (`approvals`) | A LOW-risk zero-activity account + a LOW-risk NON-zero account, both open | 1. `POST /api/recon/periods/auto-certify` `{period}`. | — | Zero account → Certified, `auto_certified=true`, certified_by "…(auto)"; non-zero account skipped `NOT_ZERO`; its preparer still cannot self-certify (403 — REC-01 unchanged). | High | Control | REC-01 | Not Run | recon-profitability.ts |
| UAT-GL-201 | B4: GL-19 recon_completeness — warns while uncertified, clears when done | GlAccountant | Recons opened for the period | 1. `GET /api/ledger/close/validate?period=`. 2. Certify all. 3. Validate again. | — | (1) `recon_completeness` in `warnings` (never `blockers`), uncertified accounts listed. (3) Warning gone. | Med | Control | GL-19, REC-01 | Not Run | recon-profitability.ts |
| UAT-GL-202 | B5: JE-exception scan flags every rule deterministically | Controller | Seed: two identical Posted JEs (same date/total/accounts); a Manual ฿50,000 even JE; a JE dated 30 days before capture; an approval at 03:00 Asia/Bangkok; a Manual Dr cash 1000 / Cr revenue 4000 | 1. `POST /api/ledger/je-exceptions/scan`. 2. `GET /api/ledger/je-exceptions?status=open`. | — | Scan reports all five rules in `by_rule`; both duplicates flagged HIGH each naming its peer; round total 50,000; backdated `lag_days` ≈ 30; after-hours carries `bkk_hour` 3; the cash↔revenue pair flagged HIGH. | High | Control | GL-28 | Not Run | basics.ts |
| UAT-GL-203 | B5: re-scan is idempotent; dismissed items never re-raise | Controller | The register from UAT-GL-202 | 1. Re-run the scan. 2. Dismiss one exception, re-run again. | — | (1) Same findings, `new: 0`. (2) Still `new: 0` — the dismissed row stays dismissed. | High | Control | GL-28 | Not Run | basics.ts |
| UAT-GL-204 | B5: dismissal is reason-mandatory and audit-logged | Controller | An open exception | 1. Dismiss with no reason. 2. Dismiss with a reason. 3. Dismiss the same one again. | — | (1) 400. (2) `dismissed` + who/when stamped; a `gl_audit_log` `EXCEPTION_DISMISSED` row carries the rule + reason. (3) 400 `ALREADY_DISMISSED`. | High | Negative/Control | GL-28, GL-17 | Not Run | basics.ts |
| UAT-GL-205 | B5: cockpit pillar + schedulable sweep | Controller | Open HIGH exceptions exist | 1. `GET /api/finance/metrics/close/status`. 2. Create + run a `je_exceptions` BI subscription. | — | (1) The `je_exceptions` pillar reports open/high counts, RAG **red** (HIGH open). (2) The sweep runs `success` with the finding counts in its summary; a re-run inserts nothing new. | Med | Positive | GL-28, GL-22 | Not Run | basics.ts |
| UAT-GL-206 | F1: auto-complete flips ONLY system-verifiable steps, with evidence | Controller | A close run; no evidence yet, then: no due recurring/prepaid, a Posted FX-reval run, a Posted deferred-tax run, a Posted DEP entry in the period | 1. `POST /api/ledger/close/auto-complete` before any evidence. 2. Create the four evidence states. 3. Auto-complete again. | — | (1) 0 completed — all four candidates `evidence_not_met`. (3) recurring / fx_reval / deferred_tax / depreciation flip Done with `completed_by "<user> (auto)"` + `detail.evidence` pinned. | High | Control | GL-15, GL-19 | Not Run | basics.ts |
| UAT-GL-207 | F1: human sign-offs never auto-complete; re-run idempotent | Controller | The run from UAT-GL-206 | 1. Inspect trial_balance_review / flux_review after auto-complete. 2. Auto-complete a third time. | — | (1) Human steps stay Pending; the run does NOT advance to ReadyToLock on auto-evidence alone. (2) 0 completed, each candidate `already_done`. | High | Negative/Control | GL-15 | Not Run | basics.ts |
| UAT-GL-208 | F1: an overdue close task ages in the GOV-01 center until signed off | Controller | A not-Done step with a past `due_date` in an ACTIVE run | 1. `GET /api/finance/approvals/pending`. 2. Sign the step off. 3. Re-read the worklist. | — | (1) `close_task_overdue` item `CLOSE-<period>:<step>` with control GL-15 and age = days overdue. (3) The item is gone. | Med | Control | GL-15, GOV-01 | Not Run | basics.ts |
| UAT-GL-209 | G3: bank_rec/subledger_tieout never tick from absence — no recon workspace = no evidence | Controller | A close run with ZERO recon workspaces for the period | 1. `POST /api/ledger/close/auto-complete`. | — | Both steps skipped `evidence_not_met`; nothing completes. | High | Control | GL-15, GL-19, REC-01 | Not Run | basics.ts |
| UAT-GL-210 | G3: one un-certified workspace on the account set blocks the step | Controller | A cash recon Reconciled (not certified); tie-out 1100 Certified but 2000 Open | 1. Auto-complete. | — | `bank_rec` and `subledger_tieout` both still `evidence_not_met` — a single un-certified workspace fails the whole set. | High | Control | GL-15, REC-01 | Not Run | basics.ts |
| UAT-GL-211 | G3: once every opened workspace is Certified the steps flip Done, pinning the certifiers | Controller | All period workspaces on the sets Certified (REC-01 maker-checker) | 1. Auto-complete. 2. Read the run's steps. | — | Both steps Done with `completed_by "<user> (auto)"` and `detail.evidence.certifications` naming each account's certifier — the tick reflects the recorded human sign-off, it never replaces it. | High | Control | GL-15, REC-01 | Not Run | basics.ts |
| UAT-GL-212 | Sub-account creation, type + self-parent guards, 4–6-digit codes | Admin/HQ (`gl_coa`) | Canonical account 5110 ค่าเดินทาง exists (header) | 1. `POST /api/ledger/accounts {code:'515001', type:'Expense', parentCode:'5110'}`. 2. `POST … {code:'515002', type:'Asset', parentCode:'5110'}`. 3. `POST … {code:'5110', parentCode:'5110'}`. | as steps | (1) accepted (a 5–6-digit sub-account whose type matches its Expense parent stages/creates via GL-27); (2) 400 `PARENT_TYPE_MISMATCH`; (3) 400 `PARENT_SELF`. | High | Control | GL-11, GL-27 | Pass | coa-change.test.ts |
| UAT-GL-213 | Expanded canonical chart ships the everyday SME accounts | exec / `gl_coa` | — | 1. `GET /api/ledger/accounts?all=true`. | — | ≥142 accounts; includes 5110 Travel & Transport, 1310 Withholding Tax Receivable, 1400 Undue Input VAT, 1540 Buildings, 1800 Intangible Assets, 3300 Legal Reserve, 4800 Other Income. | Med | Positive | GL-11 | Pass | basics.ts |
| UAT-GL-214 | Balance sheet binds accounts to งบดุล sections | exec / `gl_post` | Posted balances exist | 1. `GET /api/ledger/balance-sheet?as_of=<d>`. | — | `sections` present, grouping current_asset / noncurrent_asset / current_liability / noncurrent_liability / equity; Σ(current+non-current asset section totals) = `assets`; existing top-level totals unchanged. | High | Positive | GL-11, GL-07 | Pass | basics.ts |
| UAT-GL-215 | Income statement binds accounts to งบกำไรขาดทุน sections | exec / `gl_post` | Posted revenue + expense exist | 1. `GET /api/ledger/income-statement?from=&to=`. | — | `groups` + `summary` present; `summary.net_income` = `net_income` (= revenue − expense); `summary.gross_profit` = revenue − cogs; operating_profit / profit_before_tax subtotals present. | High | Positive | GL-11 | Pass | basics.ts |
| UAT-GL-216 | Per-account statement-section binding overrides the default | Admin/HQ (`gl_coa`) | — | 1. Resolve a section with the account's own bs_group/is_group column set. 2. Resolve one with no column. 3. Resolve an unmapped code. | as steps | Own column wins (a re-bind takes effect); no column → canonical default (5000→cogs, 1500→noncurrent_asset); unmapped → type fallback (Revenue→revenue, Expense→selling_admin); a garbage column value is ignored. | High | Control | GL-11 | Pass | statement-sections.test.ts |
| UAT-UI-EAM-01 | EAM screen reachable + WO lifecycle | Maintenance (exec/warehouse) | logged in; asset registered | 1. Sidebar → **การผลิต → ซ่อมบำรุงสินทรัพย์ (EAM)** (`/eam`). 2. **ใบสั่งงานซ่อม** tab → create WO (asset, corrective). 3. Use the row **เปลี่ยนสถานะ** select → `completed`. 4. **แผนบำรุงรักษา (PM)** tab → **เดินแผน PM…**. 5. **ความน่าเชื่อถือ** tab → enter asset → ดูข้อมูล. | — | 3 tabs render; WO created (toast `MWO-…`); status select advances (completing with a vendor shows `ตั้งเจ้าหนี้ AP-…`); PM sweep toast reports scanned/generated; reliability KPIs (MTBF, downtime, spend) show. | Med | UI | FA-06 | Not Run | manual / e2e |
| UAT-UI-COA-01 | Pick business type at signup | New visitor | on `/signup` | 1. Open `/signup`. 2. Choose **ประเภทธุรกิจ** (e.g. *ร้านอาหาร*). 3. Fill the rest and submit. | industry `restaurant` | The **ประเภทธุรกิจ** dropdown shows restaurant/retail/distribution/services/general; on submit the company is created and lands on the dashboard with an industry-tailored chart. | High | UI | GL-10 | Not Run | manual / e2e |
| UAT-UI-COA-02 | View the industry chart of accounts | exec / GlAccountant | logged in; company has an industry | 1. Sidebar → **บัญชีแยกประเภท** (`/accounting`). 2. Open the **ผังบัญชี** tab. 3. Press **แสดงบัญชีทั้งหมด**, then toggle back. | — | The tab lists the curated chart with a *ผังบัญชีตามประเภทธุรกิจ* badge + count; **แสดงบัญชีทั้งหมด** reveals the full universe and switches the badge to *ผังบัญชีเต็ม*; toggling back restores the curated list. | Med | UI | GL-10 | Not Run | manual / e2e |
| UAT-UI-COA-03 | Curate the chart in the UI (rename / group / active / re-order) | FinancialController (`gl_coa`) vs GlAccountant (`gl_post`) | logged in; company has an industry chart | 1. As `gl_coa`: `/accounting` → **ผังบัญชี** tab — the curation note + per-row controls show. 2. Row **pencil** → set **ชื่อบัญชี (ไทย)** + **กลุ่ม** → **บันทึก**. 3. Row **power** icon → turn off, then on. 4. Row **↑/↓** to move it. 5. Re-open as a `gl_post`-only *GlAccountant*. | rename e.g. 4000 → *รายได้ อาหารและเครื่องดื่ม*, group *รายได้จากการขาย* | (2) success toast; Thai name + group persist and the list refreshes (`PATCH /api/ledger/accounts/:code/overlay`). (3) off → row struck-through with *ปิดใช้งาน* and hidden from pickers (list requested with `include_inactive=true`); on → restored. (4) order changes and persists. (5) the `gl_post`-only user sees the chart **read-only** — no edit/toggle/re-order controls; a hint notes master-code changes are HQ-only. | High | UI | GL-11 | Not Run | manual / e2e; API path compliance.ts GL-11 (overlay curation + include_inactive) |
| TC-GL-11-01 | Create a canonical account via API as platform Admin/HQ | Admin (HQ, holds `gl_coa`) | Logged in as Admin; account code `9990` does not exist | 1. `POST /api/ledger/accounts {code:'9990', name:'Test Custom', type:'Expense'}`. 2. `GET /api/ledger/accounts?all=true`. | `{code:'9990', name:'Test Custom', type:'Expense'}` | 201; new account returned with `code:'9990'`, `normalBalance:'D'`, `isPostable:true`; account appears in the canonical list. Re-`POST` the same code → 400 `DUPLICATE_ACCOUNT`. | High | Positive | GL-11 | Not Run | compliance.ts; coa.service.ts |
| TC-GL-11-04 | Tenant gl_coa holder blocked from a canonical (shared) CoA change | FinancialController (`gl_coa`, non-Admin) | Logged in as a tenant Financial Controller | 1. `POST /api/ledger/accounts {code:'9991', name:'x', type:'Expense'}`. | — | 403 `COA_ADMIN_ONLY`; no account created — the shared canonical universe is Admin/HQ-only. | High | Control | GL-11 | Not Run | compliance.ts |
| TC-GL-11-05 | Per-tenant chart curation via the overlay is reflected and RLS-scoped | FinancialController (`gl_coa`) | Tenant has an industry chart; account `4000` exists | 1. `PATCH /api/ledger/accounts/4000/overlay {display_name:'Curated F&B Sales', sort_order:3}`. 2. `GET /api/ledger/accounts` (same tenant). 3. `GET /api/ledger/accounts` (a DIFFERENT tenant). | — | 200; step 2 shows `4000` named *Curated F&B Sales*; step 3 (other tenant) still shows `4000`'s canonical name — curation never leaks cross-tenant. | High | Control | GL-11 | Not Run | compliance.ts |
| TC-GL-11-06 | Overlay curation requires gl_coa | GlAccountant (`gl_post`, no `gl_coa`) | Logged in | 1. `PATCH /api/ledger/accounts/5100/overlay {active:false}`. | — | 403 FORBIDDEN — chart curation is a `gl_coa` duty (SoD-separated from posting). | High | Control | GL-11 | Not Run | compliance.ts |
| TC-GL-11-02 | Deactivate account with non-zero balance → blocked | Admin (HQ, holds `gl_coa`) | An account (e.g. `9990`) carries a non-zero net balance from a posted line | 1. `POST /api/ledger/accounts/9990/deactivate`. | — | 400 `ACCOUNT_HAS_BALANCE`; account remains active and postable. | High | Control | GL-11 | Not Run | compliance.ts; coa.service.ts |
| TC-GL-11-03 | Direct post to AR control account (1100) → blocked | GlAccountant (`gl_post`) | Logged in; account 1100 is flagged `is_control=true, control_subledger='AR'` | 1. `POST /api/ledger/journal {source:'Manual', lines:[{account_code:'1100', debit:100},{account_code:'4000', credit:100}], createdBy:'test'}`. | — | 400 `CONTROL_ACCOUNT`; message cites account 1100 as AR control; no JE created. | High | Control | GL-11 | Not Run | ledger.service.ts control-account guard |
| TC-GL-13-01 | Smoke test: by-branch endpoint returns branches key | exec / ar / fin_report | API running, user has `exec` or `ar` permission | 1. `GET /api/ledger/income-statement/by-branch?from=<today>&to=<today>` | — | HTTP 200; response body contains `{ "branches": {...} }` | High | Positive | GL-13 | Not Run | ledger.service.ts incomeStatementByBranch |
| TC-GL-13-02 | Dimension branching: two branch_ids produce two keys | exec / ar / fin_report | Two Posted JEs exist with `branch_id=1` and `branch_id=2` on their Revenue lines | 1. Insert JE with Revenue line branchId=1. 2. Insert JE with Revenue line branchId=2. 3. `GET /api/ledger/income-statement/by-branch?from=<date>&to=<date>` | — | HTTP 200; `branches["1"]` and `branches["2"]` both present with non-zero revenue. | High | Positive | GL-13 | Not Run | ledger.service.ts incomeStatementByBranch |
| TC-GL-13-03 | Unassigned bucket: lines without branch_id | exec / ar / fin_report | A Posted JE exists with an Expense line and no `branch_id` | 1. Insert JE with Expense line and no branchId. 2. `GET /api/ledger/income-statement/by-branch?from=<date>&to=<date>` | — | HTTP 200; `branches["unassigned"]` is present with non-zero expense. | High | Control | GL-13 | Not Run | ledger.service.ts incomeStatementByBranch |
| TC-GL-13-04 | Cost-centre master — create & list (web) | exec / masterdata | Logged in with `exec` or `masterdata` | 1. Open **`/cost-centers`** → **ศูนย์ต้นทุน (Master)** tab; enter code + name, pick a type, press **เพิ่มศูนย์ต้นทุน**. 2. Re-submit the same code. | code `SALES-01`, type department | (1) success toast; new row appears in the list, count increments. (2) error toast `CC_EXISTS`; no duplicate row. | Med | Positive | GL-13 | Not Run | apps/web `/cost-centers`; cost-centers.controller.ts |
| TC-GL-13-05 | Dimensional P&L view (web) | exec / masterdata | ≥1 cost centre exists with Posted revenue/expense lines tagged to it | 1. On the **กำไร-ขาดทุนตามมิติ** tab, pick the cost centre and a from/to range. | — | HTTP 200 `GET /api/ledger/cost-centers/{code}/pl`; StatCards show revenue / expense / net income; the per-account lines table lists only lines tagged with that cost centre. | High | Positive | GL-13 | Not Run | apps/web `/cost-centers`; ledger.service.ts incomeStatement (cost_center) |
| TC-GL-14-01 | Run AR sub-ledger tie-out | gl_close / gl_post | Posted AR activity exists | 1. `POST /api/ledger/tie-out/run` `{ "subledger": "AR" }`. | `{ subledger: "AR" }` | HTTP 200/201; body has numeric `glBalance`, `subledgerBalance`, `variance`; `status` is `Matched` or `Variance`; `control_account` = `1100`. | High | Positive | GL-14 | Not Run | subledger-tieout.service.ts runTieOut; basics.ts |
| TC-GL-14-02 | Self-certify blocked (maker-checker SoD) | gl_close | A tie-out run created by the current user (the runner) | 1. `POST /api/ledger/tie-out/:id/certify` **as the same user who ran it**. | — | HTTP 400; `error.code = SELF_CERTIFY`; status stays un-certified. | High | Control | GL-14 | Not Run | subledger-tieout.service.ts certify; basics.ts |
| TC-GL-14-03 | Certify by a different user | gl_close | A tie-out run exists, run by a different user | 1. `POST /api/ledger/tie-out/:id/certify` as a **different** `gl_close` user, body `{ "note": "Reviewed — ties out" }`. | — | HTTP 200; `status = Certified`; `certified_by` = the certifier; `certified_at` set. | High | Positive | GL-14 | Not Run | subledger-tieout.service.ts certify; basics.ts |
| TC-GL-15-01 | Start hard close seeds the checklist | gl_close | A period not yet closed | 1. `POST /api/ledger/close/start` `{ "period": "2020-01" }`. | `{ period: "2020-01" }` | HTTP 200/201; `status = InProgress`; `steps` is an array of ≥4 seeded steps (`subledger_tieout`, `bank_rec`, `depreciation`, `recurring`, `fx_reval`, `trial_balance_review`). | High | Positive | GL-15 | Not Run | close.service.ts startClose; basics.ts |
| TC-GL-15-02 | Lock blocked while steps incomplete | gl_close | A close run exists with required steps still Pending | 1. `POST /api/ledger/close/lock` `{ "close_run_id": N }`. | — | HTTP 400; `error.code = STEPS_INCOMPLETE`; response lists pending required `step_key`s; period stays unlocked. | High | Control | GL-15 | Not Run | close.service.ts lockPeriod; basics.ts |
| TC-GL-15-03 | Posting into a locked period blocked | gl_post | A period has been Locked | 1. `POST /api/ledger/journal` with `date` inside the locked period (e.g. `2020-01-15`), a balanced 2-line entry. | `{ date: "2020-01-15", lines: [Dr 1000 10, Cr 4000 10] }` | HTTP 400; `error.code = PERIOD_LOCKED`; no JE posted (gate fires regardless of `allowClosedPeriod`; only `source='CLOSE'` is exempt). | High | Control | GL-15 | Not Run | ledger.service.ts postEntry PERIOD_LOCKED gate; basics.ts |
| TC-GL-16-01 | Self-lock blocked (maker-checker SoD) | gl_close | A close run is ReadyToLock, started by the current user | 1. Complete all required steps. 2. `POST /api/ledger/close/lock` **as the same user who started the run**. | — | HTTP 400; `error.code = SELF_LOCK`; period stays unlocked. | High | Control | GL-16 | Not Run | close.service.ts lockPeriod SELF_LOCK; basics.ts |
| TC-GL-16-02 | Lock by a different user → Locked | gl_close | A close run is ReadyToLock, started by a different user | 1. `POST /api/ledger/close/lock` `{ "close_run_id": N }` as a **different** `gl_close` user. | — | HTTP 200; `status = Locked`; `locked_by` = the locker; `locked_at` set; `fiscal_periods.status = Locked`. | High | Positive | GL-16 | Not Run | close.service.ts lockPeriod; basics.ts |
| TC-GL-19-01 | Pre-lock validation of a clean period → ready | gl_close / gl_post / exec | a past period with no drafts and balanced postings | 1. `GET /api/ledger/close/validate?period=YYYY-MM`. | period `2020-01` | HTTP 200; `ready=true`; `blockers=[]`; checks `unposted_drafts.ok=true`, `period_balanced.ok=true`, `unbalanced_entries.ok=true`. | High | Detective | GL-19 | Not Run | close.service.ts validate; basics.ts |
| TC-GL-19-02 | An unposted Draft JE blocks readiness | gl_close | a manual JE (posts as Draft, GL-05) dated in the period | 1. `POST /api/ledger/journal` dated in `YYYY-MM`. 2. `GET /api/ledger/close/validate?period=YYYY-MM`. | period `2019-06` | `ready=false`; `blockers` includes `unposted_drafts`; `checks.unposted_drafts.count ≥ 1`. | High | Detective | GL-19 | Not Run | close.service.ts validate; basics.ts |
| TC-GL-121 | Close Cockpit composes readiness into one RAG board (GL-22) | exec / fin_report / gl_close | a period with GL activity; AP/inventory sub-ledger not tying to GL control | 1. `GET /api/finance/metrics/close/status?period=YYYY-MM`. 2. Post the matching AP + inventory sub-ledgers. 3. Re-fetch. | — | (1) 200 with `tie_out`/`readiness`/`approvals`/`rag`; `rag.tie_out='red'`, `tie_out.exceptions≥1`, `rag.overall='red'`, `rag.readiness='green'`, `close_run` null, `days_to_close` numeric. (3) `rag.tie_out='green'`, `rag.overall='green'`, `tie_out.exceptions=0`. A warehouse-only user → 403. | High | Detective | GL-22 | Pass | finance-metrics.service.ts closeStatus; finance-kpi.ts |
| TC-GL-17-01 | Posted entry is immutable (void blocked) | gl_post | A journal entry has been posted (Posted) — id known | 1. `POST /api/ledger/journal/:id/attempt-void`. 2. `GET /api/ledger/audit?entryId=:id`. | — | Step 1: HTTP 400; `error.code = GL_IMMUTABLE`; the entry is unchanged. Step 2: a `MUTATE_BLOCKED` row is present in the GL audit trail for the entry. | High | Control | GL-17 | Not Run | ledger.service.ts attemptVoidPosted + DB trigger gl_block_posted_mutation; basics.ts |
| TC-GL-17-02 | Correct a posted entry via reversal | gl_post | A posted entry (e.g. Dr 1280 777 / Cr 2400 777), not yet reversed | 1. `POST /api/ledger/journal/:id/reverse` `{ "reason": "duplicate posting" }`. 2. Inspect the reversal entry + trial balance + `GET /api/ledger/audit?entryId=:id`. | `{ reason: "duplicate posting" }` | HTTP 200; returns `{ reversalId, originalId }`. The reversal entry is **Posted**, `reversal_of` = original id, with every line's Dr/Cr swapped (1280 now a 777 credit); original + reversal net to **zero** on each account; the original is flagged `is_reversed = true`; a `REVERSE` audit row is recorded. | High | Positive | GL-17 | Not Run | ledger.service.ts reverseEntry; basics.ts |
| TC-GL-17-03 | An entry cannot be reversed twice | gl_post | An entry already reversed (TC-GL-17-02) | 1. `POST /api/ledger/journal/:id/reverse` `{ "reason": "again" }`. | `{ reason: "again" }` | HTTP 400; `error.code = ALREADY_REVERSED`; no second reversal entry is created. | High | Control | GL-17 | Not Run | ledger.service.ts reverseEntry ALREADY_REVERSED; basics.ts |
| TC-GL-18-01 | FX revaluation run computes the unrealized gain/loss | gl_close / gl_post | An open foreign-currency AR (e.g. USD 1,000 @ booked 34) and AP (USD 400 @ 34) exist; closing rate 36 | 1. `POST /api/ledger/fx-reval/run` `{ "period":"2026-12", "as_of_date":"2026-12-31", "rates":{"USD":36}, "tenant_id":N }`. | `{ period, rates:{USD:36} }` | HTTP 200; `status='Open'`; `ar_delta=2000`, `ap_delta=800`, `net=1200` (net gain); `detail` lists each doc (booked→closing→delta). | High | Positive | GL-18 | Not Run | fx-reval.service.ts runReval; basics.ts |
| TC-GL-18-02 | FX revaluation post is maker-checker and idempotent | gl_close / gl_post | An Open FX reval run (TC-GL-18-01) | 1. `POST /api/ledger/fx-reval/:id/post` **as the runner**. 2. `POST /api/ledger/fx-reval/:id/post` as a **different** user. 3. Post again. | — | Step 1: HTTP 403 `SELF_POST`. Step 2: HTTP 200; JE posted; **5400** +1200 credit, **1100** +2000 debit, **2000** +800 credit. Step 3: HTTP 400 `ALREADY_POSTED`. | High | Control | GL-18 | Not Run | fx-reval.service.ts postReval; basics.ts |
| UAT-GL-165 | Period-end FX revaluation run→post driven from the `/fx → ปรับปรุงสิ้นงวด` tab (GL-18) | Controller (`gl_close`/`gl_post`) as runner + a different user as poster | open foreign-currency AR/AP exist | 1. On **/fx**, open the **ปรับปรุงสิ้นงวด (Period-end, GL-18)** tab. 2. Set period → **คำนวณ**. 3. Click the run row to see the detail. 4. As the **same** user, **โพสต์เข้า GL**. 5. As a **different** user, **โพสต์เข้า GL**. | as left | (2) `POST /fx-reval/run` stages an **Open** run (net delta shown); (3) `GET /fx-reval/:id` lists each open AR/AP doc (scope, booked→closing rate, delta) + gain/loss/net totals; (4) `POST /:id/post` → **403 `SELF_POST`** (runner can't post own run); (5) a different poster → **Posted** (AR/AP control restatement + net to 5400, JE shown); re-post → `ALREADY_POSTED`. UI-only surfacing of existing `api/ledger/fx-reval/*` endpoints; no backend/control/GL change. | High | UI / Control | GL-18 | Not Run | basics.ts (backend, TC-GL-18-01/02) |
| TC-CON-02-01 | Consolidation run eliminates IC and keeps the group TB balanced | Group/Financial Controller (HQ Admin, `approvals`) | A group with ≥2 member entities, each with posted GL and an in-group IC pair (1150/2150) | 1. `POST /api/consolidation/groups/{id}/run` `{ "period":"2026-01" }`. 2. `GET /api/consolidation/runs/{runId}/lines`. | — | HTTP 200; `status:'Final'`, `balanced:true`, `elimination_net≈0`, `tb_net≈0`; run lines include reciprocal **Elimination** lines for **1150** (−amt) and **2150** (+amt) that net to ~0; consolidated revenue ties to Σ entity revenue. | High | Control | CON-03 | Pass | consolidation.ts |
| TC-CON-02-02 | Consolidation run→post maker-checker (self-post blocked, idempotent) | runner then a different HQ Admin (`approvals`) | A Final consolidation run from TC-CON-02-01 | 1. `POST /api/consolidation/runs/{runId}/post` **as the runner**. 2. Post as a **different** user. 3. Re-run the same period. | — | Step 1: HTTP 403 `SELF_POST`. Step 2: HTTP 200; `status:'Posted'`, `posted_by` ≠ `run_by`. Step 3: HTTP 400 `ALREADY_POSTED` (the period is frozen). | High | Control | CON-03 | Pass | consolidation.ts |
| TC-CON-02-03 | Unbalanced eliminations are rejected | Group/Financial Controller (HQ Admin) | Eliminations that do not net to zero (mismatched 1150/2150) | 1. `POST /api/consolidation/groups/{id}/run` `{ period }`. | — | HTTP 400 `CONSOL_UNBALANCED`; the run is rolled back (no Draft/Final left behind). *(Covered by the balanced-TB assertion; the harness asserts the balanced path passes — `balanced:true` — and the assertion fires when residual nets are non-zero.)* | High | Control | CON-03 | Pass | consolidation.ts (balanced assertion) |
| TC-CON-03-01 | Segment report (IFRS 8) groups P&L by dimension | Controller (HQ Admin, `exec`) | Posted P&L lines tagged with `branch_id`; a `segment_definitions` row mapping those branches into a segment | 1. `POST /api/consolidation/segments` `{ code:'NORTH', dimension:'branch', member_keys:[1,2] }`. 2. `GET /api/consolidation/segment-report?period=2026-02&dimension=branch`. | — | HTTP 200; `segments[]` contains `NORTH` with `revenue`/`expense`/`net` equal to the sum of the mapped branches' posted revenue/expense (e.g. rev 9000, exp 3000, net 6000); unmapped values surface as their own/Unassigned bucket. | High | Detective | CON-04 | Pass | consolidation.ts |
| TC-CON-05-01 | Dual-rate FX translation parks CTA in OCI (IAS 21) | Group/Financial Controller (HQ Admin, `approvals`) | A foreign (non-THB) member entity with posted GL (e.g. USD: Dr Cash 1000 / Cr Revenue 1000); two Approved USD rates in the period month (average 31, closing 32) | 1. `POST /api/consolidation/groups/{id}/run` `{ "period":"2026-03" }`. 2. `GET /api/consolidation/runs/{runId}/lines`. | — | HTTP 200; `balanced:true`; the **revenue** entity line has `rate_type:'average'`, `fx_rate:31` (translated −31000), the **cash** line `rate_type:'closing'`, `fx_rate:32` (translated 32000); an **FX_CTA** line on **3400** with `rate_type:'cta'` equals the translation difference (−1000) and `cta_total≈−1000`; the consolidated TB still balances. | High | Control | CON-05 | Pass | consolidation.ts |
| TC-CON-05-02 | Consolidated statement of cash flows reconciles (post-elimination, IAS 7) | Controller (HQ Admin, `exec`) | The consolidation run from TC-CON-05-01 | 1. `GET /api/consolidation/runs/{runId}/cash-flow`. | — | HTTP 200; `method:'indirect'`, `post_elimination:true`; `operating.net_income≈31000`, `fx_effect.net≈1000` (CTA shown as effect of exchange-rate changes on cash), `net_change_in_cash≈32000` = `consolidated_cash_movement`, `reconciled:true`. | High | Detective | CON-05 | Pass | consolidation.ts |

| TC-REV-19-01 | Create TFRS 15 contract + allocate by SSP (Sum == total) | Revenue Accountant (`exec`/`ar`/`fin_report`) | Logged in (tenant scope) | 1. `POST /api/revenue/contracts` `{ total_price:1000, obligations:[{name:'Impl',ssp:800,method:'over_time',start_date:'2040-01-01',end_date:'2040-04-30'},{name:'License',ssp:400,method:'point_in_time',start_date:'2040-01-15'}] }`. 2. `POST /api/revenue/contracts/:id/allocate`. | total 1000; SSP 800/400 | 201; contract `REVC-...`, status `Draft`, 2 POs. Allocate: Impl approx 666.6667 (gets the rounding residual), License approx 333.3333; `sum_allocated == 1000` exactly. | High | Positive | REV-19 | Pass | revrec.service.ts allocateBySSP; revrec.ts |
| TC-REV-19-02 | Activate raises deferred revenue (Dr 1100 / Cr 2410) | Revenue Accountant | An allocated contract (TC-REV-19-01) | 1. `POST /api/revenue/contracts/:id/activate`. 2. Inspect the `REVREC-INV` JE + trial balance. | - | 200; status `Active`, `deferred_revenue:1000`. JE: Dr 1100 = 1000 / Cr 2410 = 1000; the 2410 deferred-revenue liability appears on the balance sheet. | High | Positive | REV-19 | Pass | revrec.service.ts activate; revrec.ts |
| TC-REV-19-03 | Recognize a period releases 2410 to revenue (Dr 2410 / Cr 4300) | Revenue Accountant | Active contract with a built schedule (`POST /:id/schedule`) | 1. `POST /api/revenue/contracts/recognize` `{ contract_id, period:'2040-01' }`. 2. Inspect trial balance. | period 2040-01 | 200; `recognized_count:2`, `total_recognized:500` (one month of Impl 166.6667 + License point-in-time 333.3333). 2410 falls by 500, 4300 recognized revenue rises by 500; trial balance stays balanced. | High | Positive | REV-19 | Pass | revrec.service.ts recognize; revrec.ts |
| TC-REV-19-04 | Recognizing the same period again is blocked (no double post) | Revenue Accountant | The period in TC-REV-19-03 already recognized | 1. `POST /api/revenue/contracts/recognize` `{ contract_id, period:'2040-01' }` again. | - | 200; `recognized_count:0`; no new JE (already-recognized rows are skipped - TFRS 15 idempotency). | High | Control | REV-19 | Pass | revrec.service.ts recognize idempotency; revrec.ts |
| TC-REV-19-05 | Refund liability accrual (Dr 4300 contra / Cr 2420) | Revenue Accountant | A contract with recognized revenue (TC-REV-19-03) | 1. `POST /api/revenue/contracts/:id/refund-liability` `{ expected_refund_rate:0.10, as_of_date:'2040-01-31' }`. 2. Inspect the `REVREC-REF` JE. | rate 10% | 200; `expected_refund_amount:50` (10% of recognized 500), `posted_delta:50`. JE: Dr 4300 = 50 (contra revenue) / Cr 2420 = 50 Refund Liability. | Med | Control | REV-19 | Pass | revrec.service.ts accrueRefundLiability; revrec.ts |
| TC-REV-24-01 | Recognize AHEAD of billing builds a contract asset (Dr 1265 / Cr 4300) — TFRS 15 §105-107 | Revenue Accountant (`exec`/`ar`/`fin_report`) | Logged in (tenant scope) | 1. `POST /api/revenue/contracts` `{ total_price:1200, obligations:[{name:'Managed service',ssp:1200,method:'over_time',start_date:'2050-01-01',end_date:'2050-12-31'}] }` → allocate. 2. `POST /:id/activate` `{ bill_upfront:false }`. 3. `POST /:id/schedule`. 4. `POST /api/revenue/contracts/recognize` `{ contract_id, period:'2050-03' }`. 5. `GET /:id/position`. | 3 months recognized ahead of any billing | 200; activate returns `bill_upfront:false`, `billed:0` (no `REVREC-INV` JE). Recognize 300: JE **Dr 1265 = 300 / Cr 4300 = 300** (no 2410 movement). `/position` → `recognized_revenue:300`, `billed_to_date:0`, `contract_asset:300`, `contract_liability:0`. | High | Positive | REV-24 | Not Run | rev-billing.service.ts; revrec.service.ts recognize; revrec-billing.ts |
| TC-REV-24-02 | Bill reclasses the contract asset 1265 → 1100 AR (maker-checker) | Billing user (`ar`) ≠ the schedule maker | Contract from TC-REV-24-01 (asset 300) | 1. `POST /:id/billing-schedule` `{ milestones:[{period:'2050-03',amount:200},{period:'2050-06',amount:400}] }` **as maker**. 2. `POST /:id/bill` `{ billing_schedule_id:<ms1> }` **as the maker**. 3. `POST /:id/bill` `{ billing_schedule_id:<ms1> }` **as a different user**. | milestone 200 within earned asset 300 | (1) 2 `Planned` milestones tagged `created_by`=maker. (2) **403 `SOD_SELF_BILLING`** — maker may not bill own milestone. (3) a different user bills 200: JE **Dr 1100 = 200 / Cr 1265 = 200** (`contract_asset_cleared:200`, `billings_in_excess:0`); `/position` contract_asset falls to 100. | High | Control | REV-24 | Not Run | rev-billing.service.ts bill; revrec-billing.ts |
| TC-REV-24-03 | Over-bill (ahead of recognition) parks a contract liability 2410 | Billing user ≠ maker | TC-REV-24-02 (asset 100 remaining, recognized 300, billed 200) | 1. `POST /:id/bill` `{ billing_schedule_id:<ms2> }` (400) as a different user. 2. `GET /:id/position`. | milestone 400 > remaining earned asset 100 | (1) JE Dr 1100 = 400 / **Cr 1265 = 100** (clears remaining asset) / **Cr 2410 = 300** (billings in excess); `contract_asset_cleared:100`, `billings_in_excess:300`. (2) `/position` → `recognized_revenue:300`, `billed_to_date:600`, `contract_asset:0`, `contract_liability:300` — the tie-out holds (asset = max(0, recognized − billed)). | High | Control | REV-24 | Not Run | rev-billing.service.ts bill; revrec-billing.ts |
| TC-REV-24-04 (negative) | Billing beyond the contract price is rejected | Maker / biller | TC-REV-24-03 (billed 600 of a 1200 contract) | 1. `POST /:id/billing-schedule` `{ milestones:[{period:'2050-09',amount:700}] }` (would plan cumulative 1300 > 1200). | over-contract milestone | **400 `SCHEDULE_EXCEEDS_CONTRACT`** — planned billing may not exceed the contract price; nothing is scheduled. (Cumulative billing over the price is likewise blocked at `/bill` with **400 `BILL_EXCEEDS_CONTRACT`**.) | High | Negative / Control | REV-24 | Not Run | rev-billing.service.ts defineBillingSchedule; revrec-billing.ts |
| TC-REV-24-05 | REV-19 back-compat: up-front billing still releases 2410 on recognition | Revenue Accountant | Logged in (tenant scope) | 1. Create + allocate a 600 over_time contract. 2. `POST /:id/activate` (default, no `bill_upfront`). 3. schedule → `POST /recognize` `{period}` for 3 months. 4. `GET /:id/position`. | default activation | (2) `bill_upfront:true`, up-front JE Dr 1100 = 600 / Cr 2410 = 600. (3) recognition **releases 2410** (Dr 2410 / Cr 4300), **1265 untouched** — identical to REV-19. (4) `/position` → `billed_to_date:600`, `recognized_revenue:300`, `contract_liability:300`, `contract_asset:0`. | High | Control | REV-24, REV-19 | Not Run | revrec.service.ts activate/recognize; revrec-billing.ts |
| TC-REV-24-06 | Tenant RLS isolation on the billing schedule | Tenant-2 user; a Tenant-1 user | a T2 contract with a billing schedule | 1. As a T1 user `GET /api/revenue/contracts/<t2_id>/position`. 2. As the T2 owner `GET /<t2_id>/billing-schedule`. | cross-tenant read | (1) **404 `CONTRACT_NOT_FOUND`** (RLS hides the T2 contract). (2) the T2 owner sees its own schedule (1 milestone). | High | Control | REV-24, ITGC-AC-03 | Not Run | rev-billing.service.ts; revrec-billing.ts |
| TC-REV-25-01 | Estimate variable consideration — expected value vs most-likely (TFRS 15 §50-53) | Revenue Accountant (`exec`/`ar`/`fin_report`) | An Active contract | 1. `POST /:id/variable-consideration` `{ method:'expected_value', scenarios:[{amount:200,probability:0.5},{amount:0,probability:0.5}], constrained_amount:80 }`. 2. `POST /:id/variable-consideration` `{ method:'most_likely', scenarios:[{amount:100,probability:0.3},{amount:300,probability:0.7}], constrained_amount:100 }`. | two estimation methods | (1) `gross_estimate:100` (Σ prob×amount), `constrained_amount:80`, `status:'Pending'`. (2) `gross_estimate:300` (the max-probability outcome). | High | Positive | REV-25 | Not Run | rev-variable.service.ts recordEstimate; revrec-variable.ts |
| TC-REV-25-02 (negative) | The constraint caps the recognizable price — over-recognition blocked | Revenue Accountant | An Active contract | 1. `POST /:id/variable-consideration` `{ method:'most_likely', most_likely_amount:100, constrained_amount:150 }`. 2. `POST /:id/variable-consideration` `{ method:'expected_value', scenarios:[{amount:100,probability:0.5}], constrained_amount:10 }`. | constrained > estimate; probabilities ≠ 1 | (1) **400 `CONSTRAINT_EXCEEDS_ESTIMATE`** — the constraint may only *reduce* the recognizable amount (§56-58). (2) **400 `INVALID_PROBABILITIES`** — scenario probabilities must sum to 1. | High | Negative / Control | REV-25 | Not Run | rev-variable.service.ts recordEstimate; revrec-variable.ts |
| TC-REV-25-03 | Estimate maker-checker — approval mandatory before it drives revenue | Estimator (maker); a different approver (checker) | A Pending estimate (from TC-REV-25-01) | 1. `POST /:id/reestimate` while the estimate is still Pending. 2. `POST /:id/variable-consideration/:vcId/approve` **as the estimator**. 3. `…/approve` **as a different user**. | self-approval blocked | (1) no-op `applied:false`, `catch_up_delta:0` (a Pending estimate does not drive revenue). (2) **403 `SOD_SELF_APPROVAL`** — the estimator may not approve their own estimate. (3) `status:'Approved'`, `approved_by` = the checker. | High | Control | REV-25 | Not Run | rev-variable.service.ts approveEstimate/reestimate; revrec-variable.ts |
| TC-REV-25-04 | Re-estimate trues-up already-recognized revenue (catch-up) building a contract asset | Revenue Accountant | A 1000 fixed-price contract, decoupled (bill_upfront:false), 400 recognized; an Approved estimate constrained to 80 | 1. `POST /:id/reestimate`. 2. `GET /:id/position`. | upward re-estimate | (1) `applied:true`, `new_total_price:1080` (fixed 1000 + **constrained 80**, NOT gross 100), `catch_up_delta:32` (400 × 8% uplift): JE **Dr 1265 = 32 / Cr 4300 = 32**. (2) `/position` → `recognized_revenue:432`, `billed_to_date:0`, `contract_asset:432`. | High | Positive | REV-25 | Not Run | rev-variable.service.ts reestimate; revrec-variable.ts |
| TC-REV-25-05 | Re-estimate is idempotent; up-front-billed catch-up releases 2410 | Revenue Accountant | Contract from TC-REV-25-04; and a 500 up-front-billed contract with 200 recognized + an Approved estimate constrained to 100 | 1. `POST /:id/reestimate` a second time (no new approved estimate). 2. On the up-front-billed contract `POST /:id/reestimate`. | idempotency + back-compat GL path | (1) no-op `applied:false`, `catch_up_delta:0`; 4300 unchanged (no duplicate post). (2) `new_total_price:600`, `catch_up_delta:40`: catch-up **releases 2410 = 40** (billed up-front), **1265 untouched** — REV-19 back-compat path. | High | Control | REV-25, REV-19 | Not Run | rev-variable.service.ts reestimate; revrec-variable.ts |
| TC-REV-25-06 | Tenant RLS isolation on variable-consideration estimates | Tenant-2 user; a Tenant-1 user | a T2 contract with an estimate | 1. As a T1 user `GET /api/revenue/contracts/<t2_id>/variable-consideration`. 2. As the T2 owner `GET /<t2_id>/variable-consideration`. | cross-tenant read | (1) **404 `CONTRACT_NOT_FOUND`** (RLS hides the T2 contract). (2) the T2 owner sees its own estimate (1 row). | High | Control | REV-25, ITGC-AC-03 | Not Run | rev-variable.service.ts; revrec-variable.ts |
| TC-REV-26-01 | Classify a contract modification — the three TFRS 15 §18-21 outcomes | Revenue Accountant (`exec`/`ar`/`fin_report`) | An Active contract with 400 recognized of 1000 | 1. `POST /:id/modify` `{ added_price:200, distinct_flag:false, at_ssp_flag:false }`. 2. `POST /:id/modify` `{ added_price:300, distinct_flag:true, at_ssp_flag:false, obligations:[…] }`. 3. `POST /:id/modify` `{ added_price:500, distinct_flag:true, at_ssp_flag:true, obligations:[…] }`. | the classifier | (1) `type:'cumulative_catchup'` (§21b), `status:'Pending'`, `preview_effect:80`. (2) `type:'prospective'` (§21a), `preview_effect:900`. (3) `type:'separate_contract'` (§20). | High | Positive | REV-26 | Not Run | rev-modification.service.ts modify/classify; revrec-modifications.ts |
| TC-REV-26-02 | Cumulative catch-up (§21b) — a not-distinct modification catches up the satisfied portion | Revenue Accountant | Contract from TC-REV-26-01, cumulative modification approved | 1. Approve the cumulative modification (distinct approver). 2. `GET /:id/position`. | catch-up on satisfied revenue | (1) `status:'Applied'`, `new_total_price:1200`, `catch_up_delta:80`: JE **Dr 1265 = 80 / Cr 4300 = 80** (decoupled). (2) `/position` → `recognized_revenue:480`, `contract_asset:480`. | High | Positive | REV-26 | Not Run | rev-modification.service.ts applyCumulative; revrec-modifications.ts |
| TC-REV-26-03 | Prospective (§21a) — distinct not at SSP re-allocates the remaining price with NO catch-up | Revenue Accountant | A 1000 contract, 400 recognized (delivered PO), an approved prospective modification adding a distinct PO (added 300, ssp 400) | 1. Approve the prospective modification. 2. `GET /:id`. | re-allocation, no catch-up | (1) `new_total_price:1300`, `catch_up_delta:0`; recognized revenue 4300 **unchanged**. (2) remaining price re-allocated over remaining POs (Support 600→540, Add-on 360), the satisfied PO frozen at 400; Σ allocated = 1300. | High | Positive / Control | REV-26 | Not Run | rev-modification.service.ts applyProspective; revrec-modifications.ts |
| TC-REV-26-04 | Separate contract (§20) — distinct at SSP creates a new contract, original untouched | Revenue Accountant | A 1000 contract, 400 recognized; an approved separate-contract modification (added 500 = ssp 500) | 1. Approve the separate-contract modification. 2. `GET` the new contract. 3. `GET` the original. | new independent contract | (1) `status:'Applied'`, `new_contract_id` set, no revenue posted (4300 unchanged). (2) new contract total 500, 1 PO allocated 500. (3) original **untouched** — total 1000, 1 PO, recognized 400. | High | Positive / Control | REV-26 | Not Run | rev-modification.service.ts applySeparate; revrec-modifications.ts |
| TC-REV-26-05 | Modification maker-checker — approval mandatory before it drives revenue | Recorder (maker); a different approver (checker) | A Pending modification | 1. `GET /:id` after recording (before approval). 2. `…/modifications/:modId/approve` **as the recorder**. 3. `…/approve` **as a different user**, then again. | self-approval blocked; drives-nothing-until-approved | (1) contract unchanged (a Pending modification drives nothing). (2) **403 `SOD_SELF_APPROVAL`**. (3) `status:'Applied'`; a re-approve → **400 `MODIFICATION_NOT_PENDING`**. | High | Control | REV-26 | Not Run | rev-modification.service.ts approve; revrec-modifications.ts |
| TC-REV-26-06 | Tenant RLS isolation on contract modifications | Tenant-2 user; a Tenant-1 user | a T2 contract with a modification | 1. As a T1 user `GET /api/revenue/contracts/<t2_id>/modifications`. 2. As the T2 owner `GET /<t2_id>/modifications`. | cross-tenant read | (1) **404 `CONTRACT_NOT_FOUND`** (RLS hides the T2 contract). (2) the T2 owner sees its own modification (1 row). | High | Control | REV-26, ITGC-AC-03 | Not Run | rev-modification.service.ts; revrec-modifications.ts |
| TC-REV-27-01 | Significant financing component — discount to PV + EIR interest unwind ties to the face (§60-65) | Revenue Accountant (`exec`/`ar`/`fin_report`) | An Active contract, face 1200, billed up-front (2410 = 1200) | 1. `POST /:id/financing-component` `{ discount_rate_pct:12, periods:12, direction:'advance' }`. 2. Inspect the returned schedule. | discount price to PV + schedule the unwind | (1) `present_value` = 1200/1.01^12 ≈ 1064.94, `financing_total` ≈ 135.06 = face − PV, `status:'Pending'`. (2) 12 schedule rows, Σ `interest_amount` = 135.06, last `closing_balance` = 1200 (nominal). | High | Positive | REV-27 | Not Run | rev-financing.service.ts setFinancingComponent; revrec-disclosure.ts |
| TC-REV-27-02 | Financing-component discount-rate maker-checker; un-approved cannot post | Rate-setter (maker); a different approver (checker) | The Pending financing component from TC-REV-27-01 | 1. `…/financing-component/approve` **as the maker**. 2. `…/run-financing` while still Pending. 3. `…/approve` **as a different user**. | self-approval blocked; drives-nothing-until-approved | (1) **403 `SOD_SELF_APPROVAL`**. (2) **400 `FINANCING_NOT_APPROVED`** (a Pending component posts no interest). (3) `status:'Approved'`, `approved_by` = the checker. | High | Control | REV-27 | Not Run | rev-financing.service.ts approveFinancingComponent; revrec-disclosure.ts |
| TC-REV-27-03 | Advance (prepay → entity borrows) run posts financing interest EXPENSE; the contract liability accretes; idempotent | Revenue Accountant | The Approved financing component (advance) from TC-REV-27-02 | 1. `…/run-financing` `{ period:'YYYY-12' }`. 2. `…/run-financing` again. | interest unwind (advance) | (1) `posted_count:12`, `total_interest` = 135.06: JE per period **Dr 5900 / Cr 2410** (IFRS 15 Ex 29 — the entity borrowed from the customer); GL 5900 debit = 135.06, GL 2410 credit accretes +135.06. (2) `posted_count:0` (idempotent — no duplicate post). | High | Positive | REV-27 | Not Run | rev-financing.service.ts runFinancing; revrec-disclosure.ts |
| TC-REV-27-04 | Arrears (deferred payment → entity lends) run accretes the contract asset against interest INCOME | Revenue Accountant | A contract, face 1000, an Approved `direction:'arrears'` component (12%/yr over 6 months) | 1. `…/run-financing`. 2. Inspect GL 1265 / 4650. | interest unwind (arrears) | (1) `posted_count:6`, `total_interest` = 1000 − 1000/1.01^6 ≈ 57.95: JE per period **Dr 1265 / Cr 4650** (IFRS 15 Ex 26 — the entity finances the customer); GL 1265 Dr +57.95, GL 4650 Cr 57.95. A second financing component on the contract → **400 `FINANCING_ALREADY_SET`**. | High | Positive / Control | REV-27 | Not Run | rev-financing.service.ts runFinancing; revrec-disclosure.ts |
| TC-REV-27-05 | Contract-liability rollforward reconciles to GL (§120(b)) | Revenue Accountant | A decoupled contract (10×100), bill 300 in period P, recognize month 1 (100) in P | 1. `GET /api/revenue/disclosure/contract-liability-rollforward?period=P`. | §120 rollforward tie-out | opening 0 + billings 300 − recognized 100 = closing 200 = `gl_closing` 200; `reconciled:true` (opening + additions − reductions = closing = GL 2410). | High | Positive / Detective | REV-27 | Not Run | rev-disclosure.service.ts contractLiabilityRollforward; revrec-disclosure.ts |
| TC-REV-27-06 | RPO / backlog = Σ unrecognized allocated price, banded + tenant RLS (§120(a)) | Revenue Accountant; a Tenant-1 vs Tenant-2 user | The 10×100 contract with month 1 recognized (RPO 900); a T2 contract with a financing component | 1. `GET /api/revenue/disclosure/rpo?as_of=…`. 2. As a T1 user `GET /<t2_id>/financing-component`. 3. Compare T1 vs T2 RPO. | backlog + cross-tenant isolation | (1) the contract's `rpo` = 900, `within_12m + beyond_12m = rpo`. (2) **404** (RLS hides the T2 financing component). (3) T1 RPO excludes the T2 contract; the T2 owner sees its own. | High | Positive / Control | REV-27, ITGC-AC-03 | Not Run | rev-disclosure.service.ts rpo; revrec-disclosure.ts |
| UAT-GL-101 | GlAccountant cannot see "รออนุมัติ (JE)" tab on /accounting | GlAccountant (`gl_post` only) | user with `gl_post` and no `approvals`/`gl_close` | 1. Login as GlAccountant. 2. Go to `/accounting`. | — | "รออนุมัติ (JE)" tab is absent; Journal, TB, COA, financial-statement tabs visible. | High | Control | R05, GL-05 | Not Run | manual |
| UAT-GL-102 | FinancialController sees "รออนุมัติ (JE)" tab | FinancialController (`gl_close`) | user with `gl_close` | 1. Login as FinancialController. 2. Go to `/accounting`. | — | "รออนุมัติ (JE)" tab visible alongside all other tabs. | High | Control | R05, GL-05 | Not Run | manual |
| UAT-GL-103 | GlAccountant can reach /accounting in nav | GlAccountant (`gl_post`) | user with `gl_post` only | 1. Login as GlAccountant. 2. Check ERP nav under สมุดบัญชี & แยกประเภท. | — | "บัญชีแยกประเภท" nav item is visible and `/accounting` loads successfully. | High | Control | R05 | Not Run | manual |
| UAT-GL-104 | GlAccountant cannot see /finance/period-close in nav | GlAccountant (`gl_post` only) | user with `gl_post` and no `gl_close` | 1. Login as GlAccountant. 2. Check ERP nav. | — | "ปิดงวดบัญชี" nav item absent; direct `/finance/period-close` returns 403. | High | Control | R05, GL-15 | Not Run | manual |

| UAT-GL-105 | GlAccountant can reach /reconciliation in nav | GlAccountant (`recon_prep`) | user with `recon_prep` only | 1. Login as GlAccountant. 2. Check ERP nav under ธนาคาร & กระทบยอด. | — | "กระทบยอด" nav item visible; `/reconciliation` loads. | High | Control | R06, REC-01 | Not Run | manual |
| UAT-GL-106 | GlAccountant cannot see certify button on /reconciliation | GlAccountant (`recon_prep` only) | an open recon period | 1. Login as GlAccountant. 2. Open a recon period. | — | "รับรองงวด" button absent; open/import/auto-match buttons visible. | High | Control | R06, REC-03 | Not Run | manual |
| UAT-GL-107 | FinancialController sees and can click certify | FinancialController (`approvals`) | a matched recon period | 1. Login as FinancialController. 2. Open the period. 3. Click รับรองงวด. | — | Period certified; `certified_by` stamped. | High | Control | R06, REC-03 | Not Run | manual |
| UAT-GL-108 | API certify blocked for recon_prep-only token | GlAccountant (`recon_prep`) | matched period | 1. `POST /api/recon/periods/:id/certify` with `recon_prep`-only JWT. | — | 403 FORBIDDEN — `approvals` required. | High | Control | R06, REC-03 | Not Run | compliance.ts |
| UAT-GL-109 | Consolidation blocked until IC reconciliation approved | Admin (HQ) | A group with member entities + IC balances for the period | 1. `POST /api/consolidation/groups/:id/run` `{period}` **before** any IC sign-off. | — | **400 `IC_RECON_NOT_APPROVED`** — consolidation refuses to eliminate until the period's IC reconciliation is approved. | High | Control | REC-03 | Not Run | consolidation.ts |
| UAT-GL-110 | IC reconciliation prepare + independent approve (SoD) | Admin (preparer) + Admin2 (approver) | IC Due-From 1150 = Due-To 2150 for the period | 1. `POST /api/ic-reconciliation/groups/:id/prepare` `{period}`. 2. `/approve` as the **preparer**. 3. `/approve` as a **different** admin. | — | (1) `status:Prepared`, `total_due_from`=`total_due_to`, `eliminates:true`. (2) **403 `SOD_VIOLATION`**. (3) `status:Approved`, `approved_by` set → consolidation may now run. | High | Control | REC-03 | Not Run | consolidation.ts |
| UAT-GL-111 | Approval blocked when IC balances don't eliminate | Admin (approver) | Prepared period where Due-From ≠ Due-To | 1. `POST /api/ic-reconciliation/groups/:id/approve` `{period}`. | — | **400 `IC_NOT_ELIMINATED`** — cannot sign off until intra-group receivables/payables agree. | High | Control | REC-03 | Not Run | ic-recon.service.ts |
| UAT-GL-166 | IC reconciliation prepare→approve/reject driven from the `/consolidation → กระทบยอดระหว่างกัน` tab (REC-03) | HQ Admin (preparer) + a different HQ Admin (approver) | a group with member entities + IC balances for the period | 1. On **/consolidation → กระทบยอดระหว่างกัน**: pick the group + period → **จัดทำ & ลงนาม**. 2. As the **same** user, **อนุมัติ** on the Prepared row. 3. As a **different** admin, **อนุมัติ**. 4. On a non-eliminating period, **อนุมัติ**. 5. **ปฏิเสธ** a Prepared period with a reason. | as left | (1) `POST …/prepare` → row **Prepared** with Due-From/Due-To + the `eliminates` badge; (2) `POST …/approve` → **403 `SOD_VIOLATION`** (approver = preparer); (3) a different approver → **Approved** (consolidation may now run); (4) **400 `IC_NOT_ELIMINATED`** when Due-From ≠ Due-To; (5) `POST …/reject` → **Rejected** (reason stored). UI-only surfacing of existing `api/ic-reconciliation/*` endpoints; no backend/control/GL change. | High | UI / Control | REC-03 | Not Run | consolidation.ts (backend, REC-03 checks) |
| UAT-GL-112 | Project-close WIP/clearing review + sign-off (maker-checker) | Preparer + independent approver ('exec') | Projects with billable WIP (Dr 1260 / Cr 2390) | 1. `POST /api/projects/close-review?period=YYYY-MM`. 2. `/close-review/:period/approve` as the **preparer**. 3. `/approve` as a **different** user. 4. `GET /api/projects/close-reviews`. | — | (1) `status:Prepared` capturing `wip_total` (1260), `clearing_balance` (2390), `open_projects`; (2) **403 `SOD_VIOLATION`**; (3) `status:Approved`, `approved_by` set; (4) the review is in the register. | High | Control | PROJ-03 | Pass | projects.ts |
| UAT-GL-113 | Snapshot reconciliation blocks a drifted close (GL-20) | FinancialController + DBA | a clean past period | 1. `GET /api/ledger/close/validate?period=P` → note `gl_snapshot_drift` ok. 2. Raw SQL: insert a bogus `gl_period_balances` row for P. 3. Re-validate. 4. Resync (re-run the 0219 backfill recompute). 5. Re-validate. | bogus row: account 1000, debit 123.45 | (1) ok=true; (3) `ready=false`, blocker `gl_snapshot_drift`, the account named with raw-vs-snapshot amounts; (5) drift cleared, period ready. | High | Control | GL-20 | Not Run | basics.ts |
| UAT-GL-114 | Trial balance reads the maintained snapshot (post → TB reflects instantly) | GLAccountant | — | 1. Post a JE (via approval). 2. `GET /api/ledger/trial-balance?period=P`. | Dr 1000 / Cr 4000, 100 | TB reflects the new entry immediately (snapshot bumped in the posting transaction); totals balanced. | High | Positive | GL-01, GL-20 | Not Run | basics.ts; worldclass.ts |
| UAT-GL-115 | Item-posting determination OFF by default → item account override ignored (parity) | FinancialController | an item with a COGS-account override, `posting_determination` **off** | 1. Receive + issue the item. 2. `GET /api/ledger/account-ledger?account=<override>`. | item DETITEM, override 5001 | The override account is untouched (closing 0); COGS posted to the standard 5000 — behaviour identical to before. | High | Control | GL-21 | Not Run | basics.ts |
| UAT-GL-116 | Determination ON → goods issue routes COGS to the item's override account; sub-ledger still ties | FinancialController | `posting_determination` **on** for the company | 1. Enable the flag. 2. Issue 4 @ 10 of the item. 3. `GET /api/ledger/account-ledger?account=5001`. 4. `GET /api/inventory/reconciliation`. | override 5001 | (3) 5001 debited 40 (COGS routed to the override); (4) inventory sub-ledger still reconciles to the GL inventory account set. | High | Positive | GL-21 | Not Run | basics.ts |
| UAT-GL-117 | Determination fail-closed → override at a non-existent/non-postable account is rejected | FinancialController | `posting_determination` **on**; an item whose override account doesn't exist | 1. Receive that item. | override 9999 (not in CoA) | Rejected with `INVALID_POSTING_ACCOUNT` (400); nothing posts. | High | Control | GL-21 | Not Run | basics.ts |
| UAT-GL-118 | Item-setup screens: create a category, link an item, category-level account drives posting | MasterDataAdmin + FinancialController | `posting_determination` on | 1. `POST /api/item-setup/categories` a category with a COGS account. 2. `PATCH /api/item-setup/items/<id>` to link the item to it. 3. Receive + issue the item. 4. Check the category's COGS account balance. 5. Try a category with a non-postable account. | category BEV → 5001; item CATITEM | (3-4) COGS posts to the category's 5001 (item→category→account); (5) rejected `INVALID_POSTING_ACCOUNT`. | High | Positive+Control | GL-21 | Not Run | basics.ts |
| UAT-GL-119 | Warehouse account default drives inventory posting (lowest tier) | MasterDataAdmin + FinancialController | `posting_determination` on; a warehouse; an item with no item/category inventory override | 1. `PATCH /api/item-setup/warehouses/<id>` `{inventory_account:'1201'}`. 2. Receive 10@10 + issue 3 of the item at that warehouse. 3. `GET /api/ledger/account-ledger?account=1201`. 4. `GET /api/inventory/reconciliation`. 5. Try a warehouse with a non-postable account. | WH-DET → 1201; item WHITEM | (3) 1201 = 100−30 = 70 (item→category→**warehouse**); (4) sub-ledger still reconciles; (5) `INVALID_POSTING_ACCOUNT`. | High | Positive+Control | GL-21 | Not Run | basics.ts |
| UAT-GL-120 | Item default_location_id + AR revenue account drive posting | MasterDataAdmin + FinancialController | `posting_determination` on | 1. `PATCH /api/item-setup/items/<id>` `{default_location_id:'WH-COLD'}`; receive it with **no** location. 2. Set an item's `revenue_account`; ar/sync an order for it; `GET account-ledger?account=<rev>`. | DETLOC → WH-COLD; VATITEM → 4001 | (1) valuation shows the item at **WH-COLD** (not WH-MAIN); (2) AR revenue posts to **4001** (Cr 1000 net). | Medium | Positive | GL-21 | Not Run | basics.ts; taxdocs.ts |
| UAT-GL-121 | FA-11 custody change → maker-checker (register moves only on approval) | Custodian (requester) + a different user (approver) | asset FA-TEST at 'Kitchen' | 1. `POST /api/assets/scan-update` `{code:'ASSET_ID:FA-TEST', location:'Room B'}`. 2. `POST /api/assets/custody/<reqNo>/approve` as the **requester**. 3. `/approve` as a **different** user. | as left | (1) `status:'pending'`, `request_no` returned, **register NOT moved** (still 'Kitchen'), no `Scan Update` movement. (2) **403 `SOD_VIOLATION`** (binds even Admin). (3) register → 'Room B', a `Scan Update` movement logged with `by_user`=approver. | High | Control | FA-11, R07 | Pass | module-qr.ts |
| UAT-GL-122 | Confirming the current location = verification (no approval) | Custodian | FA-TEST at 'Room B' | 1. `POST /api/assets/scan-update` `{code:'ASSET_ID:FA-TEST', location:'Room B'}`. | as left | `status:'verified'`; a `Scan Verify` movement is logged immediately; **no** custody request raised. | High | Control | FA-11, FA-04 | Pass | module-qr.ts |
| UAT-GL-123 | Custody reject leaves the asset in place | Approver | a pending custody request | 1. `POST /api/assets/custody/<reqNo>/reject` `{reason}`. | as left | `status:'rejected'`; register unchanged; request no longer pending. | Med | Control | FA-11 | Pass | module-qr.ts |
| UAT-GL-124 | Audit-by-scan reconciles + offline replay is idempotent | Counter | FA-TEST at 'Room B' | 1. `POST /api/assets/audits` `{location:'Room B'}`. 2. `…/scan` FA-TEST `{client_uuid:'u1'}` ×2. 3. `…/scan` an unknown code. 4. `GET /api/assets/audits/<no>`. | as left | (1) `expected_count`=1. (2) first → `result:'Found'`; second (same `client_uuid`) → `deduped:true` (no double count). (3) → `result:'Unknown'`. (4) `summary` = found 1 / missing 0 / misplaced 0 / unknown 1. | High | Control | FA-11 | Pass | module-qr.ts |
| UAT-GL-125 | Audit finds a misplaced asset → closing raises a custody request | Counter + Approver | FA-TEST register location 'Room B' | 1. `POST /api/assets/audits` `{location:'Room C'}`. 2. `…/scan` FA-TEST. 3. `…/close`. 4. `GET /api/assets/custody?status=PendingApproval`. | as left | (2) `result:'Misplaced'`, `register_location:'Room B'`. (3) `custody_requests_raised`=1. (4) a pending request for FA-TEST with `source:'audit'`, `to_location:'Room C'` — approvable on the Custody approvals tab. | High | Control | FA-11 | Pass | module-qr.ts |
| UAT-GL-126 | FA-12 verification-exception report flags stale/never-verified assets | FaAccountant / Controller | an active asset acquired long ago and never scan-verified (FA-OLD), a recently-scanned asset (FA-TEST) | 1. `GET /api/assets/unverified?days=90`. 2. Schedule + run it: `POST /api/bi/subscriptions` `{report_type:'asset_verification_exceptions', frequency:'monthly', filters:{days:90}}` → `POST /api/bi/subscriptions/:id/run`. | as left | (1) `exceptions` includes FA-OLD with `ever_verified:false`; the recently-scanned FA-TEST is **not** listed. (2) run `status:'success'`, summary "N of M active assets not verified in 90 days". | High | Control | FA-12 | Pass | module-qr.ts |
| UAT-GL-127 | Audit results surfaced as a BI report | FaAccountant / Controller | audits run + a pending custody request exist | 1. `GET /api/assets/audit-report`. 2. `GET /api/bi/report-types`. | — | (1) returns `audits[]` with found/missing/misplaced/unknown tallies + `custody_exceptions[]` (the pending requests) + `totals.pending_custody`≥1. (2) `report_types` includes `asset_audit` and `asset_verification_exceptions` (schedulable). | Med | Positive | FA-12, FA-11 | Pass | module-qr.ts |
| UAT-GL-128 | Item-master fields editable on `/setup/items` (master-data audit Phase 2) | InventoryManager (`md_item`/`md_config`) | an item exists | 1. `PATCH /api/item-setup/items/{itemId}` with `barcode`/`uom`/`base_uom`/`conversion_factor`/`unit_price`/`temperature_type`/`bu_id`/`min_stock`/`max_stock`/`avg_daily_usage`/`lead_time_days`/`min_order_qty`/`order_multiple`/`order_cost`/`holding_cost`/`is_fixed_asset`. 2. `GET /api/item-setup/items/{itemId}`. | barcode=8850000000012, uom=BOX, conversion_factor=12, min_stock=10, min_order_qty=24 | (1) 200; response echoes all the saved fields (previously only the GL-21 posting profile was accepted/returned). (2) GET round-trips the same values. | Med | Positive | GL-21 (no control change) | Pass | basics.ts |
| UAT-GL-129 | Warehouse master fields fully editable on `/setup/warehouses` (master-data audit Phase 2) | InventoryManager (`md_item`/`md_config`) | a warehouse (`locations` row) exists | 1. `PATCH /api/item-setup/warehouses/{locationId}` with `location_name`/`zone`/`type`/`capacity`/`temperature`/`active`/`notes` (in addition to the existing `inventory_account`/`adjustment_account`). 2. `GET /api/item-setup/warehouses`. | zone=B, type=ColdStorage, capacity=500, temperature=Chilled | (1) 200; all fields saved — previously `location_name`/`zone`/`active` were **read-only** and `type`/`capacity`/`temperature`/`notes` weren't returned at all. (2) the warehouse list projects the new fields. | Med | Positive | GL-21 (no control change) | Pass | basics.ts |
| UAT-GL-130 | Asset register shows location/department/serial/assigned-to; capitalize form collects them (master-data audit Phase 2) | FaAccountant / Controller (`exec`/`creditors`) | an eligible capital GR line exists | 1. `GET /api/assets` — inspect an existing asset's `location`/`department`/`serial_no`/`assigned_to`. 2. `POST /api/assets/registrations` with `location`/`department`/`serial_no` set. 3. Approve the request as a distinct user; inspect the resulting asset. | location=Warehouse A, department=Kitchen, serial_no=SN-001 | (1) fields already present in the API response (were never rendered in the Register table). (2)-(3) the FA-10 maker-checker request carries the fields through unchanged; the resulting asset's `location`/`department`/`serial_no` match what was submitted. | Low | Positive | FA-10 (no control change) | Pass | basics.ts |
| UAT-GL-131 | FIN-6a parallel tax book: acquire seeds it, tax-depreciation run applies the initial allowance + accelerated life (memo, no GL) | FaAccountant (`exec`/`creditors`) | tenant with no other assets | 1. `POST /api/assets` `{cost 120000, useful_life_months 60, tax_useful_life_months 24, tax_initial_allowance_pct 40}`. 2. `GET /api/assets` — inspect the tax book. 3. `POST /api/assets/depreciation/run` `{2026-01}`. 4. `POST /api/assets/tax-depreciation/run` `{2026-01}`. | cost 120000; book life 60m; tax life 24m; IA 40% | (1)-(2) asset carries `tax_net_book_value`=120000, `tax_useful_life_months`=24. (3) book NBV 118000 (Dr 5200/Cr 1590). (4) tax dep = initial allowance 48000 + 5000 = **53000**, tax NBV **67000**, and **nothing posts to the GL**. | High | Positive | FIN-6a | Pass | basics.ts |
| UAT-GL-132 | FIN-6a deferred tax consumes the actual tax NBV → DTL, maker-checker post | FaAccountant (runner) + a different user (poster) | asset with a tax book (from UAT-GL-131), book NBV 118000 / tax NBV 67000 | 1. `POST /api/ledger/deferred-tax/run` `{2026-01, tenant_id}`. 2. `POST /api/ledger/deferred-tax/:id/post` as the **runner**. 3. `/post` as a **different** user. | tax rate 20% | (1) `dtl`=**10200** (51000 × 20%), `net_deferred`=−10200, `delta_posted`=−10200 (NOT the ~200 the old factor gave). (2) **403 `SELF_POST`**. (3) posts the charge **Dr 5950 / Cr 1700** (Δ −10200). | High | Positive+Control | FIN-6a, TAX-06 | Pass | basics.ts |
| UAT-GL-133 | FIN-6a tax-depreciation run is idempotent per period | FaAccountant | tax book already run for 2026-01 | 1. `POST /api/assets/tax-depreciation/run` `{2026-01}` again. | — | `asset_count`=0 — the already-depreciated asset is skipped; no double-count. | Med | Control | FIN-6a | Pass | basics.ts |
| UAT-GL-134 | FA-13 CIP/AUC: accumulate cost into 1520 (not depreciated, not in 1500) | FaAccountant (`exec`/`creditors`) | — | 1. `POST /api/assets/cip` `{name}`. 2. `…/cip/:cipNo/cost` `{amount 30000, pay_source credit}`. 3. `…/cip/:cipNo/cost` `{amount 20000, pay_source cash}`. 4. Check the 1520 / 1500 balances. | 30000 credit + 20000 cash | (1) `status:Open`, cip_no `CIP-…`. (2) Dr 1520 / Cr 2000, accumulated 30000. (3) Dr 1520 / Cr 1000, accumulated 50000. (4) 1520 balance +50000; **1500 unchanged** (CIP is not yet a fixed asset, not depreciated). | High | Positive | FA-13 | Pass | basics.ts |
| UAT-GL-135 | FA-13 settlement maker-checker → reclass Dr 1500 / Cr 1520 only on independent approval | FaAccountant (requester) + a different user (approver) | an Open CIP with 50000 accumulated cost | 1. `…/cip/:cipNo/settle` `{useful_life_months, reason}`. 2. `…/cip/:cipNo/settle/approve` as the **requester**. 3. `/settle/approve` as a **different** user. 4. `GET /api/assets` — inspect the new asset. | reason mandatory | (1) `status:PendingSettlement`, **1500 unchanged** (no GL). (2) **403 `SOD_VIOLATION`** (binds even Admin). (3) fixed asset created, reclass **Dr 1500 +50000 / Cr 1520 −50000**. (4) the asset carries `source_cip_no` and (if tax params given) its own tax book. | High | Control | FA-13, R07 | Pass | basics.ts |
| UAT-GL-136 | FA-13 CIP guards: no cost after capitalize; no settle without cost | FaAccountant | a Capitalized CIP + a fresh empty CIP | 1. `…/cip/:cipNo/cost` on a Capitalized CIP. 2. `POST /api/assets/cip` then `…/settle` immediately. | — | (1) **400 `CIP_NOT_OPEN`**. (2) **400 `CIP_NO_COST`** (nothing to capitalize). | Med | Control | FA-13 | Pass | basics.ts |
| UAT-FIN4-01 | Financial-report builder: custom P&L row-groups + computed subtotal tie to the income statement | Controller (`fin_report`/`exec`) | Posted P&L activity exists | 1. `POST /api/reports/fs/definitions` a `pl` layout: groups Revenue (types:['Revenue'], credit), Expenses (types:['Expense'], debit), Net profit (sumOf Revenue×1 + Expenses×−1). 2. `GET /api/reports/fs/render/PL-CUSTOM?as_of=<to>&from=<from>`. 3. `GET /api/ledger/income-statement?from=<from>&to=<to>`. | as left | (1) 201, `code:'PL-CUSTOM'`. (2) rendered Revenue group = `income-statement.revenue`, Expenses = `.expense`, Net-profit subtotal (`is_subtotal:true`) = `.net_income`. | High | Positive | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-02 | Comparative (prior-year) column | Controller (`fin_report`/`exec`) | a rendered P&L layout (UAT-FIN4-01) | 1. `GET /api/reports/fs/render/PL-CUSTOM?as_of=<to>&from=<from>&prior_as_of=<pto>&prior_from=<pfrom>`. | as left | `comparative:true`; every row carries a numeric `prior` beside `current` (Net-profit prior populated). | High | Positive | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-03 | Custom balance-sheet layout ties to the balance sheet | Controller (`fin_report`/`exec`) | Posted balances exist | 1. `POST /api/reports/fs/definitions` a `bs` layout (Assets/Liabilities/Equity by type). 2. `GET /api/reports/fs/render/BS-CUSTOM?as_of=<d>`. 3. `GET /api/ledger/balance-sheet?as_of=<d>`. | as left | Rendered Assets group = `balance-sheet.assets`; Equity group = `.equity`. | High | Positive | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-04 | Statement of changes in equity — roll-forward ties to the balance sheet | Controller (`fin_report`/`exec`) + JE approver | equity activity: a share issue (Dr 1010 / Cr 3000 100000) and a dividend (Dr 3100 / Cr 1010 40000), both approved (GL-05) | 1. `GET /api/reports/fs/changes-in-equity?from=<f>&to=<t>` before. 2. Post + approve the share issue and dividend. 3. Re-fetch SOCE. | as left | 3000 component `movements` rises by 100000; 3100 `movements` falls by 40000; every component rolls forward (opening + movements + profit = closing); profit flows entirely to 3100; `ties_to_balance_sheet:true` and `totals.closing == balance_sheet_equity` (equity + net income). | High | Positive | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-05 | Note schedule — account mapping + comparative + policy text | Controller (`fin_report`/`exec`) | a `notes` layout with an Equity note (types:['Equity']) + a policy-text note | 1. `POST /api/reports/fs/definitions` the notes layout. 2. `GET /api/reports/fs/notes/NOTES-STD?as_of=<d>&prior_as_of=<d>&basis=bs`. | as left | Two notes returned; the Equity note total = `balance-sheet.equity`; each note carries a `prior_total` (comparative) and the first note renders its `policy_text`. | Med | Positive | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-06 | DBD e-Filing export — balanced S-form facts + XBRL instance | Controller (`fin_report`/`exec`) | a fiscal year with GL activity | 1. `GET /api/reports/fs/dbd-export?fiscal_year=<Y>&taxpayer_name=HQ%20Co&taxpayer_id=0105500000001`. | as left | `format:'DBD-XBRL'`, 6 S-form facts (TotalAssets/TotalLiabilities/TotalEquity/Revenue/Expenses/NetProfit) each with current+prior, `balanced:true` (A = L + E incl. net profit), `xml` contains `<xbrl`, `dbd:TotalAssets`, `dbd:NetProfit`; TotalAssets current = year-end `balance-sheet.assets`. | High | Positive | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-07 | Validation / control-negative cases | Controller (`fin_report`/`exec`) | the layouts above exist | 1. `GET …/render/NOTES-STD?as_of=<d>` (render a notes layout). 2. `GET …/render/PL-CUSTOM?from=<f>` (no `as_of`). 3. `GET …/definitions/NOPE`. 4. `POST …/definitions {statement_type:'zzz'}`. | as left | (1) 400 `FS_NOT_RENDERABLE`; (2) 400 `FS_ASOF_REQUIRED`; (3) 404 `FS_DEF_NOT_FOUND`; (4) 400 (invalid `statement_type`). | High | Control | FIN-4 (no control) | Pass | basics.ts |
| UAT-FIN4-08 | Statutory pack (SOCE + DBD e-Filing + custom statement/notes viewer) driven from the `/financial-statements → งบตามกฎหมาย & DBD` tab (FIN-4) | Controller (`fin_report`/`exec`) | posted GL + (optional) an fs definition | 1. Open **/financial-statements**, tab **งบตามกฎหมาย & DBD**. 2. **งบแสดงการเปลี่ยนแปลงส่วนของเจ้าของ**: set from/to → **ดูรายงาน**. 3. **DBD e-Filing**: set fiscal year (+ optional taxpayer name/id) → **ดูรายงาน** → **ดาวน์โหลด XBRL**. 4. **งบตามนิยาม**: pick a definition → **ดูรายงาน**. | as left | (2) `GET /changes-in-equity` renders opening/movements/profit/closing per equity component + a totals row and the **✓ กระทบยอดกับส่วนของเจ้าของในงบดุล** badge when `ties_to_balance_sheet:true`; CSV downloads. (3) `GET /dbd-export` renders the six S-form facts (current/prior) + the **✓ A = L + E** badge; the XBRL file downloads (`dbd-sform-<Y>.xbrl`, contains `<xbrl`). (4) `GET /definitions` lists layouts; a pl/bs pick renders grouped rows (comparative when ticked); a notes pick renders per-note lines + total + policy text; empty list shows the API-managed hint. UI-only surfacing of existing `api/reports/fs/*` endpoints; no backend/control/GL change. | High | UI | FIN-4 (no control) | Not Run | basics.ts (backend, FIN-4 checks) |
| UAT-GL-151 | SVC-3 within-threshold renewal auto-approves + creates the successor contract | Service Manager (`masterdata`/`exec`) | an Active Gold contract (monthly 10000) ending soon; threshold 5% | 1. `POST /api/service/contracts/:id/renew` `{uplift_pct:3, proposed_start, proposed_end}`. 2. `GET /api/service/contracts`. | uplift 3% | (1) `auto_approved:true`, `requires_approval:false`, `renewal.new_value`=**10300** (10000×1.03), a `successor_contract` (SVC-…, monthly 10300) is created. (2) the old contract shows `renewal_status:'renewed'` + `renewed_to_contract_id`. | High | Positive | SVC-02 | Pass | contract-renewal.ts |
| UAT-GL-152 | SVC-3 over-threshold renewal routes to pending (no successor yet) | Service Manager (`masterdata`/`exec`) | an Active contract (monthly 20000); threshold 5% | 1. `POST /api/service/contracts/:id/renew` `{uplift_pct:20}`. | uplift 20% | `requires_approval:true`, `auto_approved:false`, `renewal.status:'pending'`, **no** `successor_contract` returned. | High | Control | SVC-02 | Pass | contract-renewal.ts |
| UAT-GL-153 | SVC-3 self-approval blocked (maker-checker) | the proposer of a pending renewal | UAT-GL-152 pending renewal | 1. `POST /api/service/renewals/:id/approve` **as the proposer**. | — | **403 `SOD_SELF_APPROVAL`**; the renewal stays pending; no successor created. | High | Control | SVC-02 | Pass | contract-renewal.ts |
| UAT-GL-154 | SVC-3 distinct approver approves → successor created + old contract renewed | a DIFFERENT approver (`approvals`/`exec`) | UAT-GL-152 pending renewal | 1. `POST /api/service/renewals/:id/approve` as a different user. 2. `GET /api/service/contracts`. | — | (1) `renewal.status:'approved'`, `successor_contract` (monthly **24000**) created. (2) old contract `renewal_status:'renewed'` + `renewed_to_contract_id`. | High | Positive | SVC-02 | Pass | contract-renewal.ts |
| UAT-GL-155 | SVC-3 reject leaves the old contract untouched | a different approver | a pending over-threshold renewal | 1. `POST /api/service/renewals/:id/reject` `{reason}`. 2. `GET /api/service/contracts`. | reason given | (1) `renewal.status:'rejected'`, `declined:true`. (2) old contract `renewal_status:'declined'`, **no** `renewed_to_contract_id`. | Med | Control | SVC-02 | Pass | contract-renewal.ts |
| UAT-GL-156 | SVC-3 auto-renew that raises price is gated even within threshold | Service Manager | an Active contract; threshold 5% | 1. `POST /api/service/contracts/:id/renew` `{uplift_pct:2, auto_renew:true}`. | uplift 2%, auto-renew | `requires_approval:true` — an auto-renew price rise always routes to maker-checker, regardless of the ceiling. | Med | Control | SVC-02 | Pass | contract-renewal.ts |
| UAT-GL-157 | SVC-3 expiry worklist + RLS isolation | Service Manager (`exec`/`marketing`); a different-tenant user | contracts near `end_date`, some renewed; a T1 tenant | 1. `GET /api/service/contracts/expiring?days=90&as_of=<d>`. 2. As a T1 user: `GET /api/service/renewals`; `POST /api/service/renewals/<hq_id>/approve`. | — | (1) lists near-expiry contracts with **no renewal in flight**; excludes `renewed`/`pending` ones. (2) T1 sees `count:0`; approving an HQ renewal → **404 `RENEWAL_NOT_FOUND`** (RLS). | High | Control | SVC-02, ITGC-AC-03 | Pass | contract-renewal.ts |
