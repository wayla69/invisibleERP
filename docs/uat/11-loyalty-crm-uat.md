# UAT — Loyalty & CRM (Members & Points)

**Status: DRAFT v0.1 · 2026-06-24** · Cross-ref: process narrative `19-marketing-pricing-loyalty.md`
(MKT-04/05/06, R10), design `docs/14-crm-members-points-worldclass-design.md`, harness
`tools/cutover/src/compliance.ts`.

Covers CRM Phase 1: the member directory & 360 view, the PDPA per-purpose consent register, and the
points-liability tie-out (control account 2250). Result legend: `Pass` / `Fail` / `Blocked` / `N/A` /
`Not Run` (default).

| Test ID | Scenario | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-LOY-001 | Member directory lists & searches | Sales | ≥1 member enrolled (e.g. `M-000001`, phone `0810000001`) | 1. `GET /api/loyalty/members?q=0810000001` | q=`0810000001` | 200; `members[]` contains the member; each row has `member_code`, `balance`, `segment` | High | Positive | Feature (member directory); 19 §7 (8a) | Not Run | |
| UAT-LOY-002 | Directory blocked without loyalty/marketing/crm | Warehouse | Warehouse role lacks `loyalty`/`marketing`/`crm` | 1. `GET /api/loyalty/members` as Warehouse | — | 403 `FORBIDDEN`; no member data returned | High | Control | ITGC-AC-02/07; 19 §6 | Not Run | RBAC gate |
| UAT-LOY-003 | Member 360 — balance & points history | Sales | Member with ≥1 earn ledger row | 1. `GET /api/loyalty/members/{id}` 2. `GET /api/loyalty/members/{id}/history` | id=member | 200; balance returned; `history[]` lists Earn/Redeem rows with `balance_after` + `ref_doc` | Medium | Positive | Feature (CRM 360); 19 §7 | Not Run | |
| UAT-LOY-004 | Withdraw marketing consent stops sends | Sales | Member opted in; `marketing_opt_in=true` | 1. `POST /api/loyalty/members/{id}/consents` `{purpose:"marketing",granted:false}` 2. `GET /api/loyalty/members/{id}` 3. `POST /api/messaging/blast` `{audience:"all",channel:"sms",body:"x"}` | as left | 200; member `marketing_opt_in=false`; blast result counts this member under `skipped`, never `sent` | High | Control | MKT-04, MKT-05; 19 §7 (11), §9 | Not Run | PDPA opt-out enforced via the synced flag |
| UAT-LOY-005 | Consent register persists per purpose | Sales | Member exists | 1. `POST /api/loyalty/members/{id}/consents` `{purpose:"line",granted:true}` 2. `GET /api/loyalty/members/{id}/consents` | purpose=`line` | 200; `consents[]` includes `{purpose:"line",granted:true}` with a `granted_at` timestamp | Medium | Positive | MKT-05; 19 §7 (11) | Not Run | |
| UAT-LOY-006 | Points-liability ties to outstanding × fair value | ExecutiveViewer | Loyalty config `baht_per_point` set; ≥1 member with a positive balance | 1. `GET /api/loyalty/liability` | — | 200; `control_account="2250"`; `liability_value == round(outstanding_points × fair_value_per_point)`; `movements.earned_points` ≥ `redeemed_points` | High | Control | MKT-06; 19 §7 (12), §9 | Not Run | TFRS 15 sub-ledger tie-out |
| UAT-LOY-007 | Members are tenant-isolated (RLS) | Sales (T2) | Member exists under tenant T1 | 1. `GET /api/loyalty/members` as a T2 user | — | 200; the T1 member is **not** present in T2's list | High | Control | ITGC-AC (RLS); 08 §9 | Not Run | Cross-tenant isolation |
| UAT-LOY-008 | Liability accrual posts & ties out to GL 2250 | GlAccountant | `baht_per_point=0.1`; active members holding 1500 pts (⇒ ฿150) | 1. `POST /api/loyalty/liability/post` 2. `GET /api/ledger/trial-balance?period={current}` | — | 200; `posted=true`, `liability_delta=150`, JE `JE-…`; account `2250` credit = `150` (Dr 5700 / Cr 2250); trial balance balanced | High | Control | MKT-06; 19 §7 (13), §9 | Not Run | TFRS 15 provision accrual; LedgerService.postEntry |
| UAT-LOY-009 | Accrual run is idempotent | GlAccountant | UAT-LOY-008 already posted; no new points movement | 1. `POST /api/loyalty/liability/post` again 2. re-read 2250 credit | — | 200; `posted=false` (`reason=up_to_date`); account `2250` credit still `150` (no double posting) | High | Control | MKT-06; 19 §7 (13) | Not Run | Watermark + ux_je_idem |
| UAT-LOY-010 | Tie-out is tenant-scoped under Admin bypass; all-member basis | Admin / GlAccountant | T1 has ฿150 of points; Admin is HQ | 1. `GET /api/loyalty/liability` as Admin (HQ) 2. deactivate a balance-bearing T1 member (no ledger row) 3. `GET /api/loyalty/liability` as a T1 user; `POST /api/loyalty/liability/post` | — | (1) HQ admin sees HQ scope only — `outstanding_points=0`, **not** T1's 1500 (no cross-tenant commingling); (3) T1 liability still `1500`/`฿150` and re-run posts nothing (`2250` unchanged) — deactivation does not desync the GL | High | Control | MKT-06, ITGC-AC (RLS); 19 §7 (12) | Not Run | Admin RLS-bypass leak guard + all-member basis |
| UAT-LOY-011 | Aged points expire (breakage) & release the liability | Sales/Loyalty | `expiry_days=365`, `baht_per_point=0.1`; member A holds 1000 pts earned >365 days ago + member B holds 1000 recent | 1. `POST /api/loyalty/liability/post` (baseline ฿200) 2. `POST /api/loyalty/expire` 3. `POST /api/loyalty/liability/post` | — | (2) `expired_members=1`, `expired_points=1000`; an `Expire` row appears in A's history & A's balance → 0; (3) accrual `liability_delta=-100`, `posted_liability=100` (Dr 2250 / Cr 5700). Re-running expire expires 0 (idempotent) | High | Control | MKT-06; 19 §7 (14), §9 | Not Run | redeemable() aging model |
| UAT-LOY-012 | Period close auto-accrues the liability | FinancialController / Sales (gl_close) | A new points movement exists since the last accrual; the period is open | 1. `POST /api/ledger/periods/{period}/close` | — | 200; `status=Closed`; `loyalty_accrual.posted=true` with the expected `liability_delta`; the accrual JE is posted (dated within the period) **before** the period locks; year-end `closeYear` sweeps the `5700` expense to Retained Earnings | High | Control | MKT-06; 19 §7 (13), §9 | Not Run | best-effort; does not block close |
| UAT-LOY-013 | Scheduled maintenance sweep expires + re-accrues per tenant | Admin / exec | A tenant has an aged-earn member + a baseline accrual | 1. `POST /api/loyalty/maintenance/run` (optionally `{tenant_id}`) | — | 200; `results[]` per tenant; the target tenant shows `expired_points > 0` and the accrual released (`liability_delta < 0`); **best-effort** — a tenant whose period is closed is recorded with an `error` and does **not** abort the others; an Admin caller covers all tenants (RLS bypass), a tenant user only its own | High | Control | MKT-06; 19 §7 (15), §9 | Not Run | cron sweep — GitHub Actions `loyalty-maintenance.yml` |
| UAT-LOY-014 | Reward burn → single-use code → liability release → double-use blocked | Sales (marketing+pos) | `baht_per_point=0.1`; a member with ≥300 points | 1. `POST /api/loyalty/rewards` (300 pts, value ฿50) 2. `POST /api/loyalty/rewards/{id}/redeem` `{member_id}` 3. `POST /api/loyalty/liability/post` 4. `POST /api/loyalty/redemptions/{code}/use` `{sale_no}` 5. repeat step 4 | — | (2) `RDM-…` code issued, member balance −300; (3) accrual `liability_delta = −30` (Dr 2250 / Cr 5700); (4) `status=used`; (5) 409 `ALREADY_USED` | High | Control | MKT-07; 19 §7 (16–17), §9 | Not Run | LedgerService accrual release |
| UAT-LOY-015 | Reward eligibility guards | Sales | Rewards with high `point_cost` / `stock=0` / `per_member_limit=1` / high `tier_min` | 1. redeem a reward costing more than the balance 2. redeem an out-of-stock reward 3. redeem a per-member-limited reward twice 4. redeem above the member's tier | — | 409 with `INSUFFICIENT_POINTS` / `OUT_OF_STOCK` / `LIMIT_REACHED` / `TIER_TOO_LOW` respectively; no points burned on a rejected redeem | High | Control | MKT-07; 19 §7 (16) | Not Run | atomic guards |
| UAT-LOY-016 | Tier auto-recompute + journey | Sales/exec | Tier ladder seeded (Silver ≥ 500, Gold ≥ 1000); members with lifetime 500 / 1000 | 1. `POST /api/loyalty/tiers/recompute` 2. `GET /api/loyalty/members/{id}/tier` | — | members re-tiered to Silver / Gold; a `loyalty_tier_history` row recorded per change; the journey returns `current_tier`, `next_tier`, `to_next`, `progress_pct` | High | Control | MKT-08; 19 §7 (18), §9 | Not Run | also run by the maintenance sweep |
| UAT-LOY-017 | Mission claim grants reward once | Sales (config) + Sales/POS (progress/claim) | A member exists | 1. `POST /api/loyalty/missions` (goal 3, +50 pts) 2. `POST /api/loyalty/missions/{id}/progress` `{member_id, amount:3}` 3. `POST /api/loyalty/missions/{id}/claim` `{member_id}` 4. repeat step 3 | — | (2) `completed=true`; (3) reward `points=50`, member balance +50, `claimed`; (4) 409 `ALREADY_CLAIMED`; claiming an incomplete mission → 409 `MISSION_INCOMPLETE` | High | Control | MKT-08; 19 §7 (19), §9 | Not Run | single-claim under FOR UPDATE |
| UAT-LOY-018 | Referral rewards both once + anti-gaming + tenant-scoped | Sales | Two members (referrer + referred) | 1. `POST /api/loyalty/referrals` `{referrer_member_id, referred_member_id, referrer_points:50, referred_points:50}` 2. `POST /api/loyalty/referrals/{id}/reward` 3. repeat step 2 4. refer the same referred again 5. refer self 6. reward as an HQ admin | — | (1) `RFL-…` code; (2) both members +50, `status=rewarded`; (3) 409 `ALREADY_REWARDED`; (4) 409 `ALREADY_REFERRED`; (5) 400 `SELF_REFERRAL`; (6) 404 (HQ admin out of T1 scope) | High | Control | MKT-08, ITGC-AC (RLS); 19 §7 (20), §9 | Not Run | single-reward; refer-once partial unique |
| UAT-LOY-019 | Member self-service app: phone-OTP login, self-scoped, staff routes blocked | (none — member device) | A member with a phone on shop T1 | 1. `POST /api/member/auth/request-otp` `{phone, tenant_code:'T1'}` 2. `POST /api/member/auth/verify-otp` with a wrong code 3. verify with the correct code 4. `GET /api/member/me` with the member token 5. `GET /api/member/rewards` 6. `GET /api/loyalty/members` with the member token | — | (1) `{sent:true}` + (non-prod) `dev_otp` 6 digits; (2) 401 `OTP_INVALID`; (3) `{token}` minted; (4) the member's OWN card (`member_code`, balance); (5) the active rewards list; (6) **403** (member token has no staff permission) | High | Control | LYL-10, ITGC-AC; 19 §7 (21), control 21 | Not Run | new MEMBER principal; self-scoped |
| UAT-LOY-019b | Member OTP brute-force cap holds | (none — member device) | A member with a phone on shop T1 | 1. `POST /api/member/auth/request-otp` 2. `POST /api/member/auth/verify-otp` with a wrong code **5 times** 3. verify with the **correct** code | — | (2) each → 401 `OTP_INVALID`; (3) **401** `OTP_INVALID` — after 5 failed attempts the code is locked even though the code is correct (request a fresh OTP) | High | Control | LYL-10b; 19 control 21 | Not Run | attempt increment auto-commits (`@NoTx`); survives the 401 |
| UAT-LOY-020 | Spin-the-wheel — weighted draw, free→cost, stock cap | Sales (config + spin) | A member with a points balance | 1. `POST /api/loyalty/wheels` (cost 5, free/day 1, one points segment +10) 2. `POST /api/loyalty/wheels/{id}/spin` (first) 3. spin again 4. create a wheel with one segment, stock 1 5. spin it 6. spin it again | — | (2) `free=true`, prize `+10`, balance +10; (3) `free=false`, `cost_points=5`, net +5; (4–5) wins the prize, stock→0; (6) **409** `NO_PRIZES` (stock exhausted); outcome is server-chosen & recorded (`loyalty_spins`) | High | Control | MKT-09; 19 §7 (22), control 22 | Not Run | provably-fair; cost burn under member lock; atomic stock guard |
| UAT-LOY-021 | Campaign — segmented send respects opt-out, audited, idempotent | Sales (marketing) | ≥3 members in a tier/segment, one opted-out | 1. `POST /api/loyalty/campaigns` (audience=tier, body) 2. `POST /api/loyalty/campaigns/{id}/send` 3. repeat step 2 | — | (2) `targeted` = all in the audience, `sent` = opted-in count, `skipped` = opted-out count, `status=sent`; one `message_log` row per recipient (`campaign=CMP-…`); (3) 409 `ALREADY_SENT` | High | Control | MKT-10; 19 §7 (23), control 23 | Not Run | PDPA opt-out skipped; idempotent under FOR UPDATE; tenant-scoped audience |
| UAT-LOY-022 | Partner privilege — tier-gated single-use claim, limit, partner redeem | Sales | A partner + a privilege (tier_min, stock, limit) | 1. claim as a qualifying member 2. partner uses the code 3. use again 4. claim again (limit) 5. claim as a below-tier member | — | (1) `PRV-…` code; (2) `used`; (3) 409 `ALREADY_USED`; (4) 409 `LIMIT_REACHED`; (5) 409 `TIER_TOO_LOW` | High | Control | MKT-11; 19 §7 (24), control 24 | Not Run | tier/stock/limit under FOR UPDATE; single-use; tenant-scoped |
| UAT-LOY-023 | Loyalty analytics — tenant-scoped liability + funnel + churn | Sales (marketing) | ≥1 member with activity, incl. ≥1 dormant member (no activity ≥90d) holding a points balance | 1. `GET /api/loyalty/analytics` 2. `GET /api/loyalty/analytics/churn` | — | (1) **HTTP 200** (no serialization error) — members/tier_mix/points/redemption/liability/churn returned for the caller's tenant only; ratios are numbers (no divide-by-zero); the dormant member is counted in `members.at_risk` and `churn_rate_pct`; (2) at-risk list (dormant ≥90d, balance>0) and its rows match the `at_risk` count from (1) | Medium | Positive | LYL-15; 19 §7 (25) | Not Run | every aggregate tenant-scoped; HQ must pass tenant_id; the 90-day cutoff is bound as an encoded string (regression guard: a raw `Date` param crashed postgres-js with "Received an instance of Date") |
| UAT-LOY-024 | LINE login — linked account mints a member token; unlinked rejected | (none — member device) | A member with `line_user_id` linked | 1. `POST /api/member/auth/line` for the linked LINE id 2. with an unlinked id 3. (signed in) `POST /api/member/link-line` | — | (1) `{token}`, `/me` is that member, self-scoped; (2) 401 `LINE_NOT_LINKED`; (3) `linked:true`; dev id rejected when `NODE_ENV=production` | High | Control | ITGC-AC, LYL-16; 19 §7 (26), control 26 | Not Run | idToken server-verified (prod); dev bypass non-prod only; tenant+line_user_id resolution |
| UAT-LOY-025 | Receipt upload — submit → staff approve → points granted via earnInTx | (member device) submit; Sales/Loyalty (`crm_points_adjust`) approve | An enrolled member (member app token) | 1. (member) `POST /api/member/receipts` with a photo + amount 2. (member) attempt `POST /api/loyalty/receipts/{id}/approve` on it 3. (staff, `crm_points_adjust`/`loyalty`/`exec`) `POST /api/loyalty/receipts/{id}/approve` 4. (member) `GET /api/member/me` | receipt photo (data URL) + purchase_amount 200 | (1) `status=Pending`, no points yet; (2) **403** (member token has no approval permission — SoD); (3) `status=Approved`, `points_granted` = the config-computed points; (4) member balance increased by exactly `points_granted`, via the same `pos_member_ledger` Earn path POS checkout uses | High | Control | LYL-17; 19 §7 (27), control 27 | Not Run | member cannot self-approve; points post through the reviewed, shared earn path — no separate GL logic |
| UAT-LOY-026 | Receipt upload — reject leaves the balance untouched; a reviewed submission can't be re-reviewed | Sales/Loyalty | A pending receipt submission | 1. (staff) `POST /api/loyalty/receipts/{id}/reject` with a reason 2. (member) `GET /api/member/me` 3. (staff) `POST /api/loyalty/receipts/{id}/approve` on the same id | reason: "ไม่ชัดเจน" | (1) `status=Rejected`; (2) balance unchanged from before submission; (3) **409** `RECEIPT_ALREADY_REVIEWED` | Medium | Control | LYL-17; 19 §7 (27), control 27 | Not Run | rejection is final; no ledger entry, no points |
| UAT-LOY-027 | Receipt upload — duplicate claim (same member/date/amount) is blocked | (member device) | A pending or approved receipt for a given date + amount | 1. (member) submit a receipt with the same `purchase_date` + `purchase_amount` as an existing non-rejected submission | receipt photo + duplicate date/amount | **409** `DUPLICATE_RECEIPT` — no new row created; a *rejected* submission for the same date/amount does not block a corrected resubmission | High | Control | LYL-17; 19 §7 (27), control 27 | Not Run | partial-unique `(tenant_id, member_id, purchase_date, purchase_amount)` excluding rejected rows |
| UAT-LOY-028 | RFM segment distribution — tenant-scoped Customer Segmentation view | Sales (marketing) | ≥1 member with a refreshed CRM profile on shop T1; a member/profile on shop T2 | 1. `GET /api/loyalty/analytics/segments` as a T1 manager 2. the same call as a T2 manager | — | (1) **HTTP 200**; the five canonical segments (Champions/Loyal/At Risk/Lost/New) are all present (0-filled if empty), the member's segment shows `members ≥ 1`, `profiled_members ≥ 1`, and `avg_spend` is a number; (2) `profiled_members = 0` — a T2 caller never sees T1's members (tenant-scoped aggregate) | Medium | Positive | LYL-15; 19 §7 (25) | Not Run | read-only aggregate over `customer_profiles`; members with no profile group under `Unsegmented`; HQ/Admin must pass `tenant_id` |

| UAT-LOY-029 | Loyalty real-time tick — earn publishes a tenant-filtered live event | Sales (exec) | A member earns points (POS/online/receipt) on shop T1; an exec on shop T2 | 1. member earns points on T1 2. `GET /api/bi/live/recent` as a T1 exec 3. the same as a T2 exec 4. `GET /api/loyalty/analytics/live` as a T1 loyalty manager | — | (2) the feed contains a `loyalty_points` event with `kind='earn'`, the member's `member_id`, and `points>0`; (3) the T2 feed contains **no** such event (tenant-filtered); (4) the loyalty-scoped live feed (analytics **แต้มสด** card source) returns the same tick for T1, none for T2 | Low | Positive | 22 Phase B; 19 §7 rev 1.8, 1.15 | Not Run | best-effort in-memory bus; advisory only (never a control); ledger stays authoritative; loyalty feed perms `loyalty`/`marketing`/`exec` |

| UAT-LOY-036 | LINE follow/unfollow webhook — auto-enrol, signature-verified, unknown tenant rejected | (none — LINE server) | Tenant `T1` with LINE creds; a Channel secret set for the signed path | 1. `POST /api/line/webhook/T1` with a `follow` event for a new LINE userId 2. an `unfollow` event for the same userId 3. `POST /api/line/webhook/NOPE` 4. (secret set) a request with a bad `X-Line-Signature` | LINE webhook event JSON | (1) `received:true`, `followed:1` — a member is auto-enrolled keyed by the LINE userId (active, `M-` code); (2) `unfollowed:1`, an `oa_unfollow` row logged, the member is **kept** (not deactivated); (3) **401** `UNKNOWN_TENANT`; (4) **401** `BAD_WEBHOOK_SIGNATURE` | High | Control | 19 §7 rev 1.17; ITGC-AC | Not Run | HMAC-SHA256 over the raw body under the tenant Channel Secret; fail-closed in prod, dev/test accepts parsed body; @NoTx tenant-scoped |
| UAT-LOY-035 | LINE flex (rich) message — broadcast + targeted push | Marketing / CRM | LINE configured (else mock); a LINE-linked member | 1. `POST /api/messaging/broadcast-oa` with `{flex, alt_text}` 2. `POST /api/messaging/line/flex` `{member_id, alt_text, flex}` 3. flex push to an opted-out member | LINE flex bubble JSON | (1) broadcast sent as a `flex` message carrying the `alt_text`; (2) targeted push delivered as `flex` to the member's LINE userId; (3) **skipped** (marketing opt-out respected on targeted sends) | Medium | Positive | 19 §7 rev 1.16 | Not Run | rich card/carousel closes the plain-text gap; consent enforced on targeted (not on OA broadcast by design) |
| UAT-LOY-030 | LINE OA broadcast — sends to all followers, no consent filter, audit-logged | Marketing / Exec | `LINE_CHANNEL_TOKEN` configured (else mock) | 1. `POST /api/messaging/broadcast-oa` `{body, campaign}` 2. `GET /api/messaging/log` 3. attempt as a role without `marketing`/`exec` | — | (1) `status='sent'`, provider `line` (or `mock` if unset) — one call to LINE `/message/broadcast`, **no** recipient list; (2) a `message_log` row with recipient `oa:broadcast` + the campaign; (3) **403** | Medium | Control | 19 §7 rev 1.9 | Not Run | OA-follower audience (opt-out = unfollow); no per-member `marketing_opt_in` filter by design; operator-gated + audited |

| UAT-LOY-031 | CDP export — bulk member snapshot with consent, tenant-scoped | Marketing / Exec | ≥1 active member with a refreshed profile on T1; a member base on T2 | 1. `GET /api/crm/export?limit=100` as a T1 marketer 2. the same as a T2 marketer 3. as HQ/Admin with no `tenant_id` | — | (1) `total ≥ 1`; each row carries `member_code`, `rfm_segment`, and a `consent` object (`marketing`/`line`/`sms`/`email` booleans); (2) `total = 0` (never T1's members); (3) `TENANT_REQUIRED` error (HQ must scope) | Medium | Control | 19 §7 rev 1.10, 1.13 | Not Run | read-only PII egress; consent ships with each row; explicit tenant scope; DSAR is separate (PDPA); **each export writes an append-only `CRM.CDP_EXPORT` audit row** (actor + row count) |

| UAT-LOY-032 | Per-tenant messaging provider — own token overrides platform env; secret never returned | Admin (`users`/`exec`) | Platform `LINE_CHANNEL_TOKEN` set; a LINE-linked, opted-in member | 1. `PUT /api/messaging/providers/line` `{creds:{token:'X'}, enabled:true}` 2. `GET /api/messaging/providers` 3. send a LINE message to the member | — | (1) `configured:true`; (2) the `line` channel shows `configured:true`/`enabled:true` and the response body does **not** contain the token `X` (write-only); (3) the outbound push carries the **tenant** token `X`, not the platform env token | High | Control | 19 §7 rev 1.11 | Not Run | secret AES-256-GCM encrypted at rest (`config_enc`); resolver order per-tenant → env → mock; RLS tenant-scoped |

| UAT-LOY-034 | Messaging provider — configure own credentials + send test (admin screen) | Admin (`users`/`exec`) | A tenant with (or without) its own LINE/SMS/SMTP creds set | 1. `PUT /api/messaging/providers/line` with creds 2. from `/settings/messaging`, use **ส่งทดสอบ** → `POST /api/messaging/providers/line/test` `{to}` | — | (1) saved (`configured:true`, secret not echoed); (2) `status='sent'` via the tenant provider (or `mock` if unset) and a `provider_test` row appears in the message log | Low | Positive | 19 §7 rev 1.11, 1.14 | Not Run | send-test resolves per-tenant → env → mock; audit-logged like any send |
| UAT-LOY-033 | Receipt photo offloaded to object storage; inline fallback; erasure deletes the object | (member device) submit; Admin erase | `OBJECT_STORE_URL` configured (and a run with it unset) | 1. (configured) member submits a receipt photo 2. inspect the stored row + the shaped `receipt_image` 3. (unset) submit again 4. run a PDPA erasure for the member | receipt photo (data URL) | (1)+(2) the DB stores an `objstore:<key>` reference (not the base64 blob) and the API returns a resolvable URL; the object store received one PUT of the decoded bytes; (3) with storage unset the image stays inline (unchanged behaviour); (4) erasure deletes the stored object and redacts the row to `[erased]` | Medium | Control | 19 §7 rev 1.12 | Not Run | verified by the `object-storage` unit test (7); no schema change (reuses `receipt_image`); PDPA-complete erasure |

| UAT-LOY-037 | Scheduled CDP sync — pushes the member snapshot in batches (idempotent, consent-aware) | Exec | ≥1 active member on T1; `CDP_WEBHOOK_URL` set (and a run with it unset) | 1. create a `cdp_export_sync` subscription 2. run it 3. re-run it | — | (2) `status='success'`, summary "CDP sync: pushed N/total members to cdp" (or `mock` if unset); each row carries consent; (3) re-run pushes the same snapshot (idempotent — no duplicates on the CDP side, keyed by `member_code`) | Medium | Positive | 19 §7 rev 1.18 | Not Run | rides the BI scheduler; batch failure stops + alerts (ITGC-OP-04); tenant-scoped |

| UAT-LOY-038 | Loyalty write API (API key) — enrol/earn/redeem, scope-gated, fires webhooks | (integration — API key) | An API key with `loyalty:write`+`loyalty:read`; loyalty config enabled; a webhook subscribed to `loyalty.earned` | 1. `POST /api/v1/loyalty/enroll` 2. `POST /api/v1/loyalty/earn` `{member_id, net_spend:100}` 3. `POST /api/v1/loyalty/redeem` `{member_id, points:40}` 4. `GET /api/v1/loyalty/member?phone=…` 5. the same earn with a `catalog:read`-only key | — | (1) member enrolled (`M-` code); (2) `points_earned:100`, balance 100; (3) `points_redeemed:40`, balance 60; (4) balance 60; (5) **403** (missing `loyalty:write` scope); a `loyalty.earned` webhook delivery is recorded | High | Control | 19 §7 rev 1.19 | Not Run | API-key only (human JWT rejected), scope-gated, rate-limited; points move through the same locked/ledgered path as POS; webhooks post-commit best-effort |

| UAT-LOY-039 | Loyalty automation trigger — earn over a threshold fires a rule | Admin (rule) + integration (API key) | An automation rule "when `loyalty.earned` and `points_earned ≥ 100` then log/notification" | 1. create the rule 2. `POST /api/v1/loyalty/earn` `{member_id, net_spend:100}` 3. check automation executions 4. an earn below the threshold | — | (2) earn succeeds; (3) an execution row for `loyalty.earned` with status `executed`; (4) the same rule is `skipped` (condition not met) | Medium | Positive | 19 §7 rev 1.20 | Not Run | loyalty events in the automation catalog; tenant-scoped; best-effort (never rolls back points); no GL |

| UAT-LOY-040 | Saved custom segment — build, resolve, whitelist-safe, tenant-scoped | Marketing / Exec | A member on T1 with RFM segment 'New'; a member base on T2 | 1. `GET /api/loyalty/saved-segments/catalog` 2. `POST /api/loyalty/saved-segments` `{name, rules:[{field:'segment',op:'eq',value:'New'}]}` 3. `GET …/:id/members` 4. create a rule with an unknown field 5. resolve the T1 segment as a T2 user | — | (1) fields + operators returned; (2) segment saved with its rules; (3) the matching members (the 'New' member) + total; (4) **400** `BAD_FIELD` (whitelist — no SQLi); (5) **404** (tenant-scoped) | Medium | Control | 19 §7 rev 1.21 | Not Run | rule→SQL is whitelist-mapped + drizzle-bound; RLS on `saved_segments` (migration 0209) |

| UAT-LOY-041 | Member-login OTP delivers through the tenant's own SMS provider | Admin (`users`/`exec`) + a member device | Shop T1 has connected its own SMS provider (`PUT /api/messaging/providers/sms`); a member with a phone on T1 | 1. `POST /api/member/auth/request-otp` `{phone, tenant_code:'T1'}` 2. `POST /api/member/auth/verify-otp` with the returned dev code 3. inspect the message log / SMS provider | — | (1) `{sent:true}` (+ non-prod `dev_otp`); (2) `{token}` minted (login still single-use / attempt-bounded); (3) the OTP SMS went out via the **tenant's** SMS sender (not the shared platform account); with no tenant provider it falls back to env → mock | Medium | Positive | 19 §7 rev 1.22 | Not Run | OTP is transactional (never opt-out filtered); resolver order per-tenant → env → mock; no schema/GL/control change |
| UAT-LOY-042 | Delivery-status callback — correlate by provider_ref, token-guarded, tenant-scoped | (integration — messaging provider) | Shop T1's LINE provider has a `callbackToken` set; a message was sent (log row carries a `provider_ref`) | 1. send a message, note the `provider_ref` 2. `POST /api/messaging/delivery-callback/NOPE` 3. `POST /api/messaging/delivery-callback/T1` with a **wrong** `X-Callback-Token` 4. same with the **correct** token `{channel:'line', ref:<provider_ref>, status:'delivered'}` 5. a callback with an unrecognised status (`bounced`) | — | (1) send `sent`, log row has a non-empty `provider_ref`; (2) **401** `UNKNOWN_TENANT`; (3) **401** `BAD_CALLBACK_TOKEN` — the row stays `sent`; (4) `{updated:1, status:'delivered'}`, the row flips `sent → delivered`; (5) row set to `undelivered` (unknown status normalises) with the provider error captured | High | Control | 19 §7 rev 1.23 | Not Run | public `@NoTx`; token compared constant-time; fail-closed in prod when no token; correlated by `{tenant, channel, provider_ref}` (migration 0210) |

| UAT-LOY-043 | Segment-builder screen + saved segment as campaign/blast audience (consent + tenant-scope hold) | Marketing / Exec | A member matching a rule (RFM 'New'); a second matching member who has **opted out**; a T2 user | 1. on `/loyalty/segments` build a segment (e.g. `กลุ่ม RFM = New`), save 2. press **ดูสมาชิก** 3. `POST /api/messaging/blast` `{audience:'saved_segment', segment_id, channel:'sms', body}` 4. create + send a campaign with กลุ่มเป้าหมาย = เซกเมนต์ที่บันทึกไว้ 5. as a T2 user create a campaign with the T1 `saved_segment_id` | — | (1) segment saved with its rules; (2) live count + sample of matching members; (3) `targeted:2, sent:1, skipped:1` — the opted-out member is **skipped** (PDPA); (4) same tallies, per-recipient rows in the message log under the `CMP-…` code; (5) **404** `SEGMENT_NOT_FOUND` (tenant-scoped) | High | Control | 19 §7 rev 1.24; MKT-04/10 | Not Run | membership resolved fresh at send time via the whitelisted rule engine (`saved_segment_id`, migration 0211); claim-first idempotency unchanged |
| UAT-LOY-044 | Scheduled RFM re-profiling keeps segments fresh (stale profile re-bucketed; repeat run stable) | Marketing / Exec | A member whose stored RFM segment no longer matches their behaviour (stale) | 1. `POST /api/crm/profiles/refresh` 2. `GET /api/crm/profile/{id}` 3. create a `daily` BI subscription of type `crm_profile_refresh` 4. `POST /api/bi/subscriptions/{id}/run` twice | — | (1) `{profiled: N, segment_changes ≥ 1}`; (2) the member's `crm.rfm_segment` reflects current behaviour; (4) first run `success` with the change tally; the **repeat** run reports **0 segment change(s)** (idempotent — pure profile upsert) | Medium | Positive | 19 §7 rev 1.25; ITGC-OP-04 (job alerting) | Not Run | sweeps active members in id-keyed batches through the single reviewed refreshProfile path; HQ/Admin passes tenant_id |
| UAT-LOY-045 | Provider go-live readiness — resolved provider visible, mock→tenant transition, secrets never returned | Admin (`users`/`exec`) | Platform LINE env token set; no tenant SMS creds; a LINE send already logged | 1. `GET /api/messaging/providers` 2. `PUT /api/messaging/providers/sms` with own creds 3. `GET /api/messaging/providers` again 4. inspect `/settings/messaging` | — | (1) `line.resolved_provider` reflects its source (`tenant` if own creds else `env`), with `last_send_at/last_status/last_provider` populated and `callback_token_set` boolean; `sms.resolved_provider='mock'`; the response contains **no secret values**; (3) `sms.resolved_provider='tenant'` (go-live transition visible); (4) badges 🟢/🟡/⚪ + last delivery per channel; a ⚪ demo channel with past sends shows the silent-no-op warning | Medium | Control | 19 §7 rev 1.26; ITGC-AC (config hygiene) | Not Run | read-only; secrets decrypted internally for booleans only, write-only contract intact |
| UAT-LOY-046 | Lifecycle journey — at-most-once steps, consent skip, frequency cap, once-per-member, tenant-scoped | Marketing / Exec | An opted-in member + an opted-out member on T1; a T2 user | 1. create a 2-step journey (step1 wait 0, step2 wait 7) + activate 2. enrol the member twice 3. `POST /api/loyalty/journeys/run-due` twice 4. enrol the opted-out member, run 5. create a cap-1/7d journey with two zero-wait steps, enrol, run twice 6. as T2, list journeys + enrol into the T1 journey | — | (2) second enrol `enrolled:false` (once-per-member); (3) first run sends step 1 **once**, second run sends **nothing** (claim-first) and step 2 waits; (4) opted-out ⇒ `skipped` audited in the message log; (5) step 2 skipped with error `frequency cap` (audited); (6) empty list + **404** | High | Control | 19 §7 (28), control 28; MKT-12 | Not Run | `journeys`/`journey_steps`/`journey_enrollments` (migration 0212, RLS); runner claim-first `@NoTx`; BI job `journey_runner` |
| UAT-LOY-047 | Campaign A/B + holdout — deterministic assignment, holdout untouched, per-group lift report | Marketing | ≥30 opted-in LINE-linked members | 1. `POST /api/marketing/automation/campaigns` with `variant_b_body`, `split_b_pct:30`, `holdout_pct:30` 2. inspect `campaign_sends` 3. redeem one B coupon 4. `GET …/campaigns/:id` | — | (1) holdout members recorded `status='holdout'` with **no coupon, no recipient, no gateway call**; A and B bodies both delivered; (2) every row's `variant` re-derives from the `(campaign_id, member_id)` hash (0 mismatches — no RNG); (4) report shows per-group sent/redeemed/attributed for A/B + holdout count + a lift figure with the honest 0-baseline note; the B redemption lands in group B | Medium | Control | 19 §7 (12) rev 1.28; MKT-04 unchanged | Not Run | migration 0213; assignment before consent so groups are comparable; loyalty_campaigns has body-only A/B |
| UAT-LOY-048 | Predictive scoring — cadence-relative churn, versioned, segment-usable, at-risk value | Marketing / Exec | Member A: 2 orders 100/90 days ago (weekly-ish cadence, long quiet); member B: purchased today | 1. `POST /api/crm/profiles/refresh` 2. `GET /api/crm/profile/{A}` and `{B}` 3. create a saved segment `churn_risk ≥ 60` and resolve it 4. `GET /api/loyalty/analytics/segments` | — | (2) A's `crm.churn_risk` **high** (≥60) with `score_version='v1'` and a non-negative `predicted_ltv`; B's **low** (≤20); (3) the segment resolves to exactly member A (whitelisted field, bound value); (4) `at_risk_value` = member count + Σ predicted LTV at threshold 70, labelled an estimate | Medium | Positive | 19 §7 rev 1.29; docs/ops/predictive-scoring.md | Not Run | migration 0214; scores computed inside refreshProfile (F2 sweep keeps them fresh); null until ≥1 paid order; never posted to GL |

## Revision history

| Version | Date | Author | Summary |
|---|---|---|---|
| 0.1 | 2026-06-24 | Platform | Initial loyalty/CRM UAT — member directory, 360, PDPA consent, points-liability tie-out (7 cases, 4 control-type). |
| 0.2 | 2026-06-24 | Platform | Added UAT-LOY-008..010 — points-liability GL accrual (`POST /api/loyalty/liability/post`): ties out to acct 2250, idempotent re-run, tenant-scoped under Admin bypass + all-member basis (now 10 cases, 7 control-type). Corrected control account 2350 → **2250**. |
| 0.3 | 2026-06-24 | Platform | Added UAT-LOY-011 (points expiry/breakage releases the liability) and UAT-LOY-012 (period close auto-accrues) — now 12 cases, 9 control-type. |
| 0.4 | 2026-06-24 | Platform | Added UAT-LOY-013 (scheduled maintenance sweep) — now 13 cases, 10 control-type. |
| 0.5 | 2026-06-24 | Platform | **Phase 2:** added UAT-LOY-014 (reward burn → single-use code → liability release → double-use blocked) and UAT-LOY-015 (reward eligibility guards) — now 15 cases, 12 control-type. |
| 0.6 | 2026-06-24 | Platform | **Phase 3:** added UAT-LOY-016 (tier auto-recompute + journey) and UAT-LOY-017 (mission claim single-claim) — now 17 cases, 14 control-type. |
| 0.7 | 2026-06-24 | Platform | **Phase 4:** added UAT-LOY-018 (referral rewards both once + anti-gaming + tenant-scoped) — now 18 cases, 15 control-type. |
| 0.8 | 2026-06-24 | Platform | **Phase 4:** added UAT-LOY-019 (member self-service app — phone-OTP login, self-scoped, staff routes blocked) and UAT-LOY-019b (OTP brute-force cap holds — adversarial-review fix) — now 20 cases, 17 control-type. |
| 0.9 | 2026-06-24 | Platform | **Phase 4:** added UAT-LOY-020 (spin-the-wheel — weighted draw, free→cost accounting, per-prize stock cap) — now 21 cases, 18 control-type. |
| 1.0 | 2026-06-24 | Platform | **Phase 4:** added UAT-LOY-021 (campaign — segmented send respects opt-out, audited, idempotent) — now 22 cases, 19 control-type. |
| 1.1 | 2026-06-24 | Platform | **Phase 4 complete:** added UAT-LOY-022 (partner privilege — tier-gated single-use), UAT-LOY-023 (loyalty analytics — tenant-scoped), UAT-LOY-024 (LINE login) — now 25 cases, 21 control-type. |
| 1.2 | 2026-06-25 | Platform | Hardened UAT-LOY-023 into a regression guard: the analytics overview must return **200** (the 90-day churn cutoff is now bound as an encoded string; a raw `Date` param previously crashed the screen with "Received an instance of Date"), require a dormant member in the fixture, and assert the `at_risk` count matches the churn drill-down list. |
| 1.3 | 2026-07-01 | Platform | **CRM Phase 4 — receipt-upload-for-points:** added UAT-LOY-025 (submit → staff approve → points via the shared `earnInTx` path, member cannot self-approve), UAT-LOY-026 (reject leaves the balance untouched; re-review blocked), UAT-LOY-027 (duplicate member/date/amount claim blocked) — now 28 cases, 24 control-type. New control LYL-17. |
| 1.4 | 2026-07-01 | Platform | **Customer Segmentation view:** added UAT-LOY-028 (RFM segment distribution — canonical segments present, member counts/avg spend, tenant-scoped so T2 sees 0) covering the new `GET /api/loyalty/analytics/segments` endpoint + the analytics-page segments panel — now 29 cases. Verified by the `crm` harness (2 new checks). |
| 1.5 | 2026-07-01 | Platform | **Loyalty real-time signal:** added UAT-LOY-029 (earn publishes a `loyalty_points` tick to the BiLive live feed; T2 tenant-filtered) — now 30 cases. Verified by the `crm` harness (2 new checks). |
| 1.6 | 2026-07-01 | Platform | **LINE OA broadcast:** added UAT-LOY-030 (broadcast to all OA followers — no consent filter by design, audit-logged, operator-gated) — now 31 cases. Verified by the `line-crm` harness (2 new checks). |
| 1.7 | 2026-07-01 | Platform | **CDP export:** added UAT-LOY-031 (bulk member export with identity + RFM + consent flags; tenant-scoped; HQ must pass tenant_id) — now 32 cases. Verified by the `crm` harness (2 new checks). |
| 1.8 | 2026-07-01 | Platform | **Per-tenant messaging providers:** added UAT-LOY-032 (tenant's own LINE/SMS/SMTP creds override env; encrypted + write-only; tenant token used on a real push) covering `tenant_messaging_config` (migration 0208) + `GET`/`PUT /api/messaging/providers/:channel` — now 33 cases. Verified by the `line-crm` harness (3 new checks). |
| 1.16 | 2026-07-02 | Platform | **Saved custom segments (Phase D1):** added UAT-LOY-040 (build a rule-based segment, resolve to members, unknown field rejected, tenant-scoped) covering `saved_segments` (migration 0209) + `/api/loyalty/saved-segments` — now 41 cases. Verified by the `crm` harness (5 new checks). |
| 1.17 | 2026-07-02 | Platform | **Per-tenant OTP SMS (Phase E1):** added UAT-LOY-041 (member-login OTP delivers through the tenant's own SMS provider; falls back env → mock) — now 42 cases. Verified by the `compliance` harness (LYL-10/10b, OTP login + brute-force cap green with the per-tenant resolve). |
| 1.18 | 2026-07-02 | Platform | **Delivery-status callback (Phase E2):** added UAT-LOY-042 (provider callback correlates by `provider_ref` and flips `sent → delivered`/`undelivered`; unknown shop code + wrong token rejected, row untouched) covering `message_log.provider_ref` (migration 0210) + `POST /api/messaging/delivery-callback/:tenantCode` — now 43 cases. Verified by the `line-crm` harness (5 new checks). |
| 1.19 | 2026-07-02 | Platform | **Segment-builder UI + saved-segment audiences (Phase F1):** added UAT-LOY-043 (build a segment on `/loyalty/segments`, preview members, blast + campaign target it with PDPA skip, T2 cannot target a T1 segment) covering `loyalty_campaigns.saved_segment_id` (migration 0211) + blast `audience:'saved_segment'` — now 44 cases. Verified by the `crm` harness (3 new checks, 32/32). |
| 1.20 | 2026-07-02 | Platform | **Scheduled RFM re-profiling (Phase F2):** added UAT-LOY-044 (bulk refresh re-buckets a stale profile; `crm_profile_refresh` scheduled job repeat-run reports 0 changes) — now 45 cases. Verified by the `crm` harness (2 new checks, 34/34). |
| 1.21 | 2026-07-02 | Platform | **Provider go-live readiness (Phase F3):** added UAT-LOY-045 (resolved provider per channel + last delivery on `GET /api/messaging/providers`; mock→tenant transition; secrets never returned; demo-mode warning) — now 46 cases. Verified by the `line-crm` harness (2 new checks, 26/26). |
| 1.22 | 2026-07-02 | Platform | **Lifecycle journeys (Growth Engine G1):** added UAT-LOY-046 (at-most-once steps, consent skip, frequency cap, once-per-member, tenant isolation) covering `journeys`* (migration 0212) + `/api/loyalty/journeys` + `journey_runner` — now 47 cases. Verified by the `crm` harness (5 new checks, 39/39) + the **MKT-12** compliance check (115/115); RCM regenerated → 170 controls. |
| 1.23 | 2026-07-02 | Platform | **A/B + holdout (Growth Engine G2):** added UAT-LOY-047 (deterministic split, holdout no-touch baseline, per-group lift report) covering migration 0213 — now 48 cases. Verified by the `line-automation` harness (3 new checks, 10/10). |
| 1.24 | 2026-07-02 | Platform | **Predictive scoring (Growth Engine G3):** added UAT-LOY-048 (cadence-relative churn high/low split, versioned stamp, segment-builder field, analytics at-risk value) covering migration 0214 — now 49 cases. Verified by the `crm` harness (3 new checks, 42/42). |
| 1.15 | 2026-07-02 | Platform | **Loyalty automation triggers (Phase D2):** added UAT-LOY-039 (a `loyalty.earned ≥ threshold` rule fires an action; below-threshold is skipped) — now 40 cases. Verified by the `ext` harness (2 new checks). |
| 1.14 | 2026-07-01 | Platform | **Loyalty write API + webhooks (Phase C2):** added UAT-LOY-038 (enrol/earn/redeem/read via API key, `loyalty:write` scope enforced, `loyalty.earned` webhook fired) — now 39 cases. Verified by the `ext` harness (6 new checks). |
| 1.13 | 2026-07-01 | Platform | **Scheduled CDP sync (Phase C1):** added UAT-LOY-037 (`cdp_export_sync` pushes the member snapshot in batches, idempotent + consent-aware) — now 38 cases. Verified by the `crm` harness (1 new check). |
| 1.12 | 2026-07-01 | Platform | **LINE follow webhook (Phase B2):** added UAT-LOY-036 (follow auto-enrol, unfollow logged/member kept, unknown tenant + bad signature rejected) — now 37 cases. Verified by the `line-crm` harness (3 new checks). |
| 1.11 | 2026-07-01 | Platform | **LINE flex messages (Phase B1):** added UAT-LOY-035 (flex broadcast + targeted flex push; consent respected on targeted) — now 36 cases. Verified by the `line-crm` harness (2 new checks). |
| 1.10 | 2026-07-01 | Platform | **Messaging providers UI + test-send (Phase A1):** added UAT-LOY-034 (configure own provider + ส่งทดสอบ via `/settings/messaging` + `POST /api/messaging/providers/:channel/test`) — now 35 cases. Verified by the `line-crm` harness (1 new check). |
| 1.9 | 2026-07-01 | Platform | **Receipt object storage:** added UAT-LOY-033 (receipt photo offloads to an S3-compatible store as an `objstore:<key>` ref; inline fallback when unset; PDPA erasure deletes the object) — now 34 cases. Verified by the `object-storage` unit test (7 checks). |
