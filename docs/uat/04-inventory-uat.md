# UAT — Cycle 04: Inventory & Warehouse (WMS)

**Status: DRAFT v1.6 · 2026-07-11** · *v1.6: UAT-INV-097 — MRP **plan-to-PR** + **lot-sizing** toggle + **rough-cut capacity (RCCP)** surfaced on the `/production → MRP` tab (the run existed; the plan-to-PR button, lot-policy column, and work-centre load/overload table were UI-less). UI-only over the harness-tested engine; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v1.5: UAT-INV-096 — ATP / order-promising (INV-09) surfaced on the `/costing` **พร้อมส่งมอบ & จองสินค้า** tab (check ATP, reserve against a doc, release/fulfil the reservation register). UI-only over the harness-tested engine (`INSUFFICIENT_ATP` unchanged); ToE `costing.ts` (19); PN-03 rev; manual 04 §11.* · Cross-ref: process narratives `03-inventory-cogs.md` (INV-01..07, REV-07, R11) and `15-manufacturing-costing.md` §5a (MRP), §9 (costing/PPV), harnesses `tools/cutover/src/wms.ts` (branch replenishment INV-05, UAT-INV-027..029), `e2e.ts`, `mrp.ts`, `costing.ts`, the perpetual valued sub-ledger + FIFO/FEFO cost layers + the **INV-07 write-off maker-checker** (UAT-INV-030..042) in `tools/cutover/src/basics.ts` + `tools/cutover/src/compliance.ts`, and the stock-ops → sub-ledger bridge in `tools/cutover/src/stock-ops.ts`; work-order WIP costing + **yield variance (MFG-02, UAT-MFG-01..02)** in `tools/cutover/src/manufacturing.ts`. **v0.9:** UAT-INV-060..064 — R11 SoD screen split (stocktake counting vs stock-adjustment posting). **v1.0:** UAT-INV-066..070 — QR camera scanning + `/q` deep-link resolver (capture-only; no control change), harness `tools/cutover/src/module-qr.ts`. **v1.1:** UAT-INV-069b/069c/070b — cross-browser scanner (@zxing fallback) + 1D barcodes + continuous scan; `/q` page now covered by web e2e `apps/web/e2e/qr-resolver.spec.ts`. **v1.3 (2026-07-09):** UAT-INV-073 — doc-reference dropdowns on the manufacturing/goods-issue screens (WO/routing/BOM/quality-ref picked from existing list GETs; UI-only). **v1.2 (2026-07-09):** UAT-INV-071..072 — WMS pending-list reads (`GET /api/wms/wave-candidates`, `GET /api/wms/picks?status=`, `GET /api/wms/shipments?status=`) feeding the `/wms` Wave/Pack/Ship doc-reference dropdowns; read-only, no control change; ToE `wms.ts`. **v1.4 (2026-07-10):** UAT-INV-090..093 — waste-ledger closing-the-loop (POS-5a, **INV-15**): reason × disposition taxonomy + `by_disposition`, void-fired-item capture (recipe explosion → ingredient waste + one Dr 5810/Cr 1200 JE), theoretical-vs-actual usage variance; ToE `waste.ts`.

Result legend: Pass / Fail / Blocked / N/A / Not Run. Error codes/amounts are exact.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-INV-001 | Stock list + low-stock count | Admin | Items A=5, B=−2, C=0 seeded | 1. `GET /api/inventory/stock?limit=50`. | — | 200; `total`=3; `low_stock_count`=2. | Med | Positive | INV-01 | Not Run | e2e.ts |
| UAT-INV-002 | Create bins | WarehouseOperator | — | 1. `POST /api/wms/bins` storage. 2. `POST /api/wms/bins` quarantine. | `{bin_code: A-01-01, bin_type: storage}` | Bins created. | Med | Positive | INV-02 | Not Run | wms.ts |
| UAT-INV-003 | Putaway updates bin stock + movement | WarehouseOperator | Bin A-01-01, GR-T1 | 1. `POST /api/wms/putaway` qty 10. | `{gr_no: GR-T1, bin_code: A-01-01, item_id: A, qty: 10}` | bin_stock=10; one `Transfer` stock movement. | High | Positive | INV-02 | Not Run | wms.ts |
| UAT-INV-004 | Putaway idempotent | WarehouseOperator | Same GR already put away | 1. Repeat putaway same `gr_no`. | same payload | bin_stock stays 10 (not 20). | Med | Control | INV-02 | Not Run | wms.ts |
| UAT-INV-005 | Wave batches orders into pick lists | WarehouseOperator | 2 POS sales SALE-W1/W2 | 1. `POST /api/wms/waves` with 2 orders. | `{orders:[{POS, SALE-W1},{POS, SALE-W2}]}` | `pick_count`=2; 2 pick lists; A line bin resolved to A-01-01. | High | Positive | INV-03 | Not Run | wms.ts |
| UAT-INV-006 | Re-wave idempotent | WarehouseOperator | Wave already created | 1. Re-run same wave. | same orders | `pick_count`=0; total pick lists stays 2. | Med | Control | INV-03 | Not Run | wms.ts |
| UAT-INV-007 | Pick decrements bin stock | WarehouseOperator | Pick list, bin A-01-01=10 | 1. `POST /api/wms/picks/{pick}/pick` qty 4. | `{lines:[{pick_line_id, picked_qty:4}]}` | status `Picked`; bin 10→6; one `Issue` movement. | High | Positive | INV-03 | Not Run | wms.ts |
| UAT-INV-008 | Over-pick (last unit / no stock) blocked | WarehouseOperator | Item B has no bin stock | 1. `POST /api/wms/picks/{pick}/pick` qty 1. | `{picked_qty:1}` | 422 `PICK_SHORT`; no negative stock. | High | Control | INV-03, R11 | Not Run | wms.ts |
| UAT-INV-009 | Pack → shipment shell | WarehouseOperator | Pick Picked | 1. `POST /api/wms/picks/{pick}/pack`. | — | `shipment_no` created; status `Packed`. | Med | Positive | INV-03 | Not Run | wms.ts |
| UAT-INV-010 | Ship → tracking recorded | WarehouseOperator | Packed shipment | 1. `POST /api/wms/shipments/{no}/ship`. | `{carrier: Kerry, tracking_no: KX123}` | status `Shipped`; tracking + shipped_at set. | Med | Positive | INV-03 | Not Run | wms.ts |
| UAT-INV-011 | WMS execution posts ZERO GL | Admin | After putaway/pick/ship | 1. Query/inspect journal entries with source WMS/PICK/SHIP/PUTAWAY. | — | 0 journal entries (COGS booked at sale-issue, not WMS). | High | Control | INV-04 | Not Run | wms.ts |
| UAT-INV-012 | Min-max replenishment suggestion | Planner | R1 on_hand 3, reorder 10, qty 50 | 1. `POST /api/replenishment/suggest`. | — | R1 suggested_qty=50; urgency `warning`. | Med | Positive | INV-01 | Not Run | wms.ts |
| UAT-INV-013 | Auto-PR consolidates suggestions | Planner | suggestions exist | 1. `POST /api/replenishment/auto-pr`. | — | `pr_no` created; R1 status `PR_Created` with that pr_no. | Med | Positive | INV-01 | Not Run | wms.ts |
| UAT-INV-014 | RMA receive → restock to bin + credit | WarehouseOperator/ReturnsClerk | Portal sale captured | 1. `POST /api/rma`. 2. `/receive` restock disposition. 3. `/restock`. | `{sale_no, lines:[{item_id:A, qty:1}]}` | restock +1 to bin (6→7); ReturnsService `return_no`; status `Credited`. | Med | Positive | REV-07 | Not Run | wms.ts |
| UAT-INV-015 | RMA restock idempotent | WarehouseOperator | RMA already Credited | 1. Repeat `/restock`. | — | `duplicate: true` or 400; no double restock. | Low | Control | REV-07 | Not Run | wms.ts |
| UAT-INV-016 | Cycle-count variance review | StockCounter/InventoryController | Stock present | 1. Record a count differing from on-hand. 2. Review/approve the variance. | `<<count vs system>>` | Variance flagged for review; adjustment controlled (requires approval). | Med | Control | INV-01 | Not Run | stock-ops |
| UAT-INV-017 | RLS — planner sees only own tenant bins | Planner (T2) | T1 has bins + suggestions | 1. `GET /api/wms/bins`. 2. `GET /api/replenishment/suggestions`. | bearer plan2 | 0 T1 bins, 0 T1 suggestions. | High | Control | ITGC-AC (RLS) | Not Run | wms.ts |
| UAT-INV-018 | Trial balance balanced after inventory ops | Admin | After WMS + RMA | 1. `GET /api/ledger/trial-balance`. | — | `totals.balanced: true`. | Med | Detective | REC-01 | Not Run | wms.ts |
| UAT-INV-019 | Multi-level MRP explosion + netting (D3) | Planner | 2-level BOM (CAKE→SPONGE→FLOUR/EGG); on-hand SPONGE 4, FLOUR 5 | 1. `POST /api/mrp/run` demand 10 CAKE. | `{demand:[{item_id:BOM-CAKE,qty:10}]}` | Make CAKE(L0)+SPONGE(L1, net 6); buy FLOUR 7 (gross 12−5), EGG 18, FROSTING 10; `summary.max_level`=1. | High | Positive | Feature (MRP) | Not Run | mrp.ts |
| UAT-INV-020 | MRP plan-to-PR creates a real PR | Procurement/Planner | same BOM | 1. `POST /api/mrp/plan-to-pr` demand 10 CAKE. | as left | `pr_no` `PR-…`; PR has the 3 planned-buy lines (FLOUR/EGG/FROSTING). | High | Positive | Feature (MRP→PR) | Not Run | mrp.ts |
| UAT-INV-021 | Circular BOM rejected | Planner | BOM-X↔BOM-Y cycle | 1. `POST /api/mrp/run` demand BOM-X. | — | 400 `CIRCULAR_BOM`. | Med | Control | Feature (MRP guard) | Not Run | mrp.ts |
| UAT-INV-022 | MRP lot-sizing — min-order-qty / order-multiple / EOQ (D3) | Planner | item masters: FLOUR min_order_qty 20; EGG order_multiple 12; FROSTING avg_daily_usage 1, order_cost 40, holding_cost 5 | 1. `POST /api/mrp/run` demand 10 CAKE with `lot_sizing:true`. | `{demand:[{item_id:BOM-CAKE,qty:10}],lot_sizing:true}` | Buy FLOUR `ordered_qty`=20 (`lot_policy` min), EGG=24 (multiple, rounded from 18), FROSTING=77 (`eoq` √(2·365·40/5), policy eoq). | High | Positive | Feature (MRP lot-sizing) | Not Run | mrp.ts |
| UAT-INV-023 | Rough-cut capacity flags overloaded work-centre (D3) | Planner | routing RT-CAKE: MIX setup 30 + run 5/unit; BAKE run 10/unit; make qty 10 | 1. `POST /api/mrp/capacity` demand 10 CAKE, work_centers MIX=100, BAKE=60. | `{demand:[{item_id:BOM-CAKE,qty:10}],work_centers:[{code:MIX,available_minutes:100},{code:BAKE,available_minutes:60}]}` | MIX `load_minutes`=80 (30+5×10), `overloaded`=false; BAKE=100 (10×10) > 60 → `overloaded`=true; `summary.overloaded`=1. | High | Positive | Feature (RCCP) | Not Run | mrp.ts |
| UAT-INV-024 | STD goods-receipt PPV stays balanced under rounding (W4) | CostAccountant | item STD, standard_cost 3.3235 | 1. Receive qty 8.99 @ actual 0.0083. | as left | GRV JE posts (not `UNBALANCED`): Dr 1200 (std, rounded) + PPV 5500 plug + Cr 2000 (actual); Σdebit = Σcredit. | High | Control | MFG-03 / GL-01 | Not Run | costing.ts |
| UAT-INV-025 | Food-cost variance valued at ingredient cost | Manager | EOD count: NOODLE actual 12 vs theoretical 10 (cost ฿15); OIL 4 vs 5 (cost ฿50) | 1. `GET /api/menu/food-cost/variance?from=&to=`. | window covering the counts | NOODLE `variance_cost`=+30 (unfavourable), `theoretical_cost`=150, anomaly `High` (20%). | High | Positive | INV-04 (analytics) | Not Run | restaurant.ts |
| UAT-INV-026 | Variance summary nets favourable/unfavourable + tenant-isolated | Manager | as above; another tenant has its own counts | 1. `GET /api/menu/food-cost/variance` as tenant T1. | as left | `summary.variance_cost`=−20, `unfavorable_cost`=30, `favorable_cost`=−50, `items`=2; T2's rows excluded (RLS). | High | Control | INV-04 (analytics) | Not Run | restaurant.ts |
| UAT-INV-027 | Branch-aware replenishment splits transfer-before-buy | Planner | Branch BA holds X1 on_hand 30 (ROP 10); branch BB on_hand 2 (ROP 10, ROQ 50) | 1. `POST /api/replenishment/suggest`. 2. `GET /api/replenishment/suggestions`. | — | X1@BB yields a `transfer` row `transfer_qty`=20 from BA + a `buy` row `buy_qty`=30 (residual); both urgency `warning`. | High | Positive | INV-05 | Not Run | wms.ts |
| UAT-INV-028 | Auto-transfer moves branch stock + branch-attributed log; auto-PR raises residual | Warehouse (transfer) / Procurement (buy) | suggestions from UAT-INV-027 exist | 1. `POST /api/replenishment/auto-transfer` (wh_custody). 2. `POST /api/replenishment/auto-pr` (procurement). | — | Transfer: `doc_no` `TRF-…`, `transfers`=1, `branch_stock` BA 30→10 & BB 2→22, `Transfer-Out`+`Transfer-In` logged; transfer row `Transfer_Done`. Buy: `pr_no` `PR-…`, `lines`=1, residual row `PR_Created`. | High | Positive | INV-05, EXP-03 | Not Run | wms.ts |
| UAT-INV-029 | Authorization + RLS — no-custody user blocked; other tenant sees no branch suggestions | Customer (no warehouse/custody) / cross-tenant Planner | T3 has branch suggestions | 1. Call `auto-transfer` as a user with neither `warehouse` nor `wh_custody`. 2. `GET /api/replenishment/suggestions` as a different tenant's planner. | bearer of no-custody user / other-tenant planner | 403 on `auto-transfer` (transfer is a gated custody duty; the buy PR is independently approved under EXP-03); other tenant sees 0 of T3's branch suggestions (RLS). | High | Control | INV-05, EXP-03, ITGC-AC (RLS) | Not Run | wms.ts |
| UAT-INV-030 | Goods receipt at cost → moving-average + GL | WarehouseOperator (`wh_receive`) | Clean tenant | 1. `POST /api/inventory/receipts` 100 @ 10 (ref GRN-1). 2. `POST /api/inventory/receipts` 100 @ 12 (ref GRN-2). | `{item_id:SUGAR, qty, unit_cost, ref_type:GRN, ref_id}` | 201; 2nd: `balance_qty`=200, `avg_cost`=11; each returns `gl_entry_no` `JE-…` (Dr 1200 / Cr 2000). | High | Positive | INV-06 | Pass | basics.ts, compliance.ts |
| UAT-INV-031 | Duplicate goods-receipt is idempotent | WarehouseOperator | GRN-1 already posted | 1. Re-`POST /api/inventory/receipts` same `ref_id` GRN-1. | same payload | `deduped:true`; on-hand stays 200 (no double stock / no double JE). | High | Control | INV-02, INV-06 | Pass | basics.ts, compliance.ts |
| UAT-INV-032 | Goods issue at moving-average → COGS | WarehouseOperator (`wh_custody`) | 200 @ 11 on hand | 1. `POST /api/inventory/issues` qty 50. | `{item_id:SUGAR, qty:50}` | 201; `value`=550; `balance_qty`=150; Dr 5000 COGS / Cr 1200. | High | Positive | INV-06 | Pass | basics.ts |
| UAT-INV-033 | Over-issue beyond on-hand blocked | WarehouseOperator | 150 on hand | 1. `POST /api/inventory/issues` qty 1000. | `{item_id:SUGAR, qty:1000}` | 400 `NEG_STOCK`; no negative stock. | High | Control | INV-01, INV-06 | Pass | basics.ts, compliance.ts |
| UAT-INV-034 | Write-off is a request (pending) + reason required | InventoryController (`wh_adjust`) | 150 on hand | 1. `POST /api/inventory/adjustments` `qty_delta:-10` reason "Spoilage". 2. Repeat with blank reason. 3. `GET /api/inventory/valuation`. | `{item_id:SUGAR, qty_delta, reason}` | 1: `status:pending_approval`, `request_id` returned, **nothing posted** (on-hand still 150). 2: 400 `REASON_REQUIRED`. | High | Control | INV-04, INV-07, R11 | Pass | basics.ts, compliance.ts |
| UAT-INV-040 | Write-off: requester cannot self-approve (SoD) | `wh_adjust` requester | write-off `PendingApproval` | 1. `POST /api/inventory/writeoffs/{id}/approve` as the same user. | — | 403 `SOD_VIOLATION`; nothing posted, request stays pending. Binds **even Admin**. | High | Control | INV-07, R11 | Pass | basics.ts, compliance.ts |
| UAT-INV-041 | Write-off: independent approver applies it | Different `wh_adjust` holder | write-off `PendingApproval` | 1. `POST /api/inventory/writeoffs/{id}/approve` as a different user. 2. `GET /api/inventory/reconciliation`. | — | `status:Posted`, `approved_by` ≠ requester; now applied — `balance_qty`=140, `value`=−110 (Dr 5810 / Cr 1200); sub-ledger ties to GL 1200 at 1540. | High | Control | INV-07, INV-06 | Pass | basics.ts, compliance.ts |
| UAT-INV-042 | Stocktake variance still posts immediately (not gated) | StockCounter→bridge | tracked item, count variance | 1. Post a stocktake with a negative variance via stock-ops. | — | Posts immediately (the count is the authorizing document) — NOT routed through the INV-07 write-off maker-checker. | Med | Control | INV-06 | Pass | stock-ops.ts |
| UAT-MFG-01 | WO completion: full-yield (no variance) | WarehouseStaff | WO Released (planned 20, total cost 420) | 1. `POST /api/manufacturing/work-orders/{wo}/complete` `{qty_produced:20}`. 2. `GET /api/ledger/trial-balance`. | — | `fg_value`=420, `yield_variance`=0; **Dr 1210 420 / Cr 1250 420**; WIP nets 0; no 5810; TB balanced (unchanged behaviour). | High | Control | MFG-02, GL-01 | Pass | manufacturing.ts |
| UAT-MFG-02 | WO completion: short yield → yield variance to 5810 | WarehouseStaff | WO Released (planned 20, total cost 420) | 1. `complete {qty_produced:15}`. 2. `GET /api/ledger/trial-balance`. 3. `GET /api/manufacturing/work-orders`. | — | `fg_value`=315 (=21×15), `yield_variance`=105; **Dr 1210 315 + Dr 5810 105 / Cr 1250 420**; WIP nets 0; TB balanced; the WO register exposes `yield_variance`=105 (full-yield WO shows 0). | High | Control | MFG-02, GL-01 | Pass | manufacturing.ts |
| UAT-INV-035 | Sub-ledger reconciles to GL inventory account | Controller (`wh_count`) | After 030–034 | 1. `GET /api/inventory/reconciliation`. | — | `sub_ledger_value`=1540; `gl_inventory`=1540; `reconciled:true`. | High | Detective | INV-06 | Pass | basics.ts, compliance.ts |
| UAT-INV-036 | Bridge: stocktake on a tracked item posts a valued variance | StockCounter/InventoryController | Item B perpetual-tracked (valued receipt 100 @ 10) | 1. Stocktake B physical 95. 2. `POST /api/stocktake/{no}/post`. | `{item_id:B, physical_qty:95}` | post `valued_lines`=1; valued on-hand B=95 @ 10 = 950; variance Dr 5810 / Cr 1200 50; reconciled. | High | Control | INV-06 (bridge) | Pass | stock-ops.ts |
| UAT-INV-037 | Bridge: goods issue/transfer cost a tracked item; legacy item audit-only | WarehouseOperator (`wh_custody`) | B tracked; A snapshot-only | 1. `POST /api/inventory/issue` B 3 (`from WH-MAIN`). 2. `POST /api/inventory/transfer` B 2 WH-MAIN→WH-2. 3. Issue A 3. | `{item_id, qty, from_location}` | B: `valued_lines`=1 each; on-hand WH-MAIN 90 / WH-2 2; reconciled 920. A: `valued_lines`=0 (audit-only, `stock_movements` −3, no GL). | High | Control | INV-06 (bridge) | Pass | stock-ops.ts |
| UAT-INV-038 | FEFO cost layers: issue consumes soonest-expiry first | WarehouseOperator (`wh_receive`/`wh_custody`) | item MILK, `costing_method:fefo` | 1. Receive 10 @ 12 (lot L1, exp 2026-07-01). 2. Receive 10 @ 15 (lot L2, exp 2026-06-20). 3. Issue 12. 4. `GET /api/inventory/layers?item_id=MILK`. | `{costing_method:fefo, lot_no, expiry_date}` | Issue `value`(COGS)=174 (=10×15 + 2×12, **not** 162 at avg 13.5); balance 8 @ 12 = 96; one open layer L1 (8 @ 12); reconciled. | High | Positive | INV-06 (FIFO/FEFO) | Pass | basics.ts |
| UAT-INV-039 | Costing method fixed on first receipt; moving-avg default | WarehouseOperator | — | 1. Receive SUGAR with no `costing_method`. 2. `GET /api/inventory/valuation`. | — | SUGAR `costing_method`=`moving_avg`; no cost layers created; existing moving-average behaviour unchanged. | Med | Control | INV-06 (FIFO/FEFO) | Pass | basics.ts |
| UAT-INV-040 | Bin layout geometry + utilisation in warehouse map | WarehouseOperator | bin A-01-01 holds stock | 1. `PATCH /api/wms/bins/A-01-01/layout {pos_x,pos_y,pos_z,dim_*,capacity:1000}`. 2. `GET /api/wms/layout`. | as left | Bin carries `pos`/`dim`/`capacity`; layout returns `on_hand`>0 and `utilization`=on_hand÷capacity; `avg_utilization` + `over_capacity` rolled up. | Med | Positive | INV-08 | Not Run | wms.ts |
| UAT-INV-041 | Locate an item to its bin(s) | WarehouseOperator | item A in A-01-01 | 1. `GET /api/wms/locate?item_id=A`. | — | Returns the bin(s) holding A with qty + position; `total_qty` = sum across bins. | Med | Positive | INV-08 | Not Run | wms.ts |
| UAT-INV-042 | Bin capacity over-fill blocked on putaway (INV-08) | WarehouseOperator | bin CAP-01 capacity 5 | 1. putaway 4 to CAP-01. 2. putaway +3 to CAP-01. | `{bin_code:CAP-01, item_id:A, qty}` | Step 1 accepted (4≤5); step 2 → `422 BIN_CAPACITY_EXCEEDED`, bin stays 4 (no overflow). | High | Control | INV-08 | Not Run | wms.ts, compliance.ts |
| UAT-INV-043 | 3D warehouse view screen | WarehouseOperator | bins with X/Y/Z + capacity | 1. `/wms` → **ผังคลัง 3D**. 2. orbit/zoom; click a bin. 3. search an Item ID. | — | Bins render as boxes coloured by utilisation (over-capacity red); clicking shows bin contents; item search highlights holding bins in purple with total qty. | Med | UI | INV-08 | Not Run | manual / e2e |
| UAT-INV-044 | Reservation reduces ATP; idempotent re-allocate | Planner | WIDGET ATP 100 | 1. `POST /api/costing/allocate {item_id, qty:30, ref_doc:SO-2}`. 2. repeat same ref. 3. `GET /api/costing/atp`. | — | After 1: ATP 70. After 2: still ATP 70, one Open row (`adjusted:true`) — no leak. | High | Control | INV-09 | Not Run | costing.ts |
| UAT-INV-045 | Reserving beyond ATP blocked | Planner | WIDGET ATP 70 | 1. `POST /api/costing/allocate {qty:200, ref_doc:SO-3}`. | — | 422 `INSUFFICIENT_ATP`; no reservation created. | High | Control | INV-09 | Not Run | costing.ts |
| UAT-INV-046 | Release frees a cancelled reservation | Planner | open reservation SO-1 (20) | 1. `POST /api/costing/allocations/SO-1/release`. 2. `GET /api/costing/atp`. | — | `released_qty`=20; ATP rises by 20; SO-1 status Cancelled. | High | Control | INV-09 | Not Run | costing.ts |
| UAT-INV-047 | Fulfil is ATP-neutral vs on-hand drop | Planner/Warehouse | open reservation SO-2 (30); goods issued (on-hand −30) | 1. `POST /api/costing/allocations/SO-2/fulfill`. 2. `GET /api/costing/atp`. | — | `fulfilled_qty`=30; ATP unchanged (reservation retired as on-hand falls — not double-counted); register shows 0 open. | High | Control | INV-09 | Not Run | costing.ts |
| UAT-INV-048 | Costed waste posts to GL + decrements stock | Warehouse/Kitchen | `PORK` stock 100 | 1. `POST /api/inventory/waste` `{item_id:PORK, qty:5, reason_code:spoilage, unit_cost:80}`. 2. trial balance. | as left | `WASTE-…` + `JE-…`, `total_cost`=400; GL **Dr 5810 400 / Cr 1200 400**; stock 100→95; TB balanced. | High | Control | INV-10 | Not Run | waste.ts |
| UAT-INV-049 | Uncosted waste logs without GL; analytics by reason | Warehouse | after UAT-INV-048 | 1. `POST .../waste` `{item_id:PORK, qty:3, reason_code:prep_error}` (no `unit_cost`). 2. `GET /api/inventory/waste`. | as left | (1) logged, `journal_no`=null, stock 95→92. (2) `total_cost`=400; `by_reason` spoilage cost 400, prep_error cost 0. | High | Positive | INV-10 | Not Run | waste.ts |
| UAT-INV-050 | Perpetual item can't be wasted (boundary) | Warehouse | `WIDGET` has an `inv_balances` row | 1. `POST .../waste` `{item_id:WIDGET, qty:1, reason_code:damage, unit_cost:50}`. | as left | **400 `USE_WRITEOFF`** — must use the INV-07 maker-checker write-off; no waste row, no GL, no stock change. | High | Control | INV-10, INV-07 | Not Run | waste.ts |
| UAT-INV-090 | Waste disposition taxonomy — costed waste with a disposition + by_disposition roll-up | Warehouse/Kitchen | `PORK` stock | 1. `POST .../waste` `{item_id:PORK, qty:2, reason_code:expiry, unit_cost:80, disposition:'donate'}`. 2. `GET /api/inventory/waste`. 3. `GET /api/inventory/waste?disposition=donate`. | as left | (1) `201`, `disposition:'donate'`, `total_cost`=160. (2) `by_disposition` has `donate` cost 160. (3) filter returns only the donate row. | High | Positive | INV-15 | Not Run | waste.ts |
| UAT-INV-091 | Invalid disposition rejected | Warehouse | — | 1. `POST .../waste` `{item_id:PORK, qty:1, reason_code:damage, disposition:'nonsense'}`. | as left | **400 `BAD_DISPOSITION`** — disposition must be discard/compost/donate/staff_meal/rework/return_supplier. | Med | Control | INV-15 | Not Run | waste.ts |
| UAT-INV-092 | Void-fired-item capture — recipe explodes to ingredient waste + one JE | Kitchen | recipe `FRIEDRICE` = 0.2kg RICE @30 + 1 EGG @5; RICE/EGG in stock | 1. `POST /api/inventory/waste/void-fire` `{sku:'FRIEDRICE', qty:1, ref_doc:'TCKT-9'}`. 2. check RICE stock. 3. `POST .../void-fire` `{sku:'NOPE', qty:1}`. | as left | (1) `201`, 2 ingredient waste lines, `total_cost`=11.00, one `WASTE-…` + `JE-…` (Dr 5810 11 / Cr 1200 11), `source=void_fire`. (2) RICE 50→49.8. (3) **400 `NO_RECIPE`**. | High | Positive/Control | INV-15 | Not Run | waste.ts |
| UAT-INV-093 | Theoretical-vs-actual usage variance | F&B Controller | RICE recipe-COGS `Consume` 10kg + logged waste 0.2kg (void-fire) | 1. `GET /api/inventory/waste/variance`. | window covering both | RICE row: `theoretical_use`=10, `waste_use`=0.2, `actual_use`=10.2, `variance_cost`=6 (0.2×30), `variance_pct`=2; summary `variance_cost`>0. | High | Positive | INV-15 | Not Run | waste.ts |
| UAT-INV-094 | Stock-movement posting works for a normal (non-bypass) tenant user after the 0316 RLS | WarehouseOperator (`wh_custody`, tenant T1) | 0316 fail-closed RLS active on `stock_movements` | 1. `POST /api/wms/putaway` (A×10 → bin). 2. wave → pick a sale (issue movement). 3. post a goods receipt (GR movement); issue a work order. | as left | every posting **succeeds** and writes a `stock_movements` row scoped to **T1**. Before the fix these six sites inserted `tenant_id` NULL → RLS `WITH CHECK` violation → **500** for any non-bypass user. Cross-tenant read stays blocked (UAT-INV-074). | High | Control / Regression | ITGC-AC-18 · R11 | Not Run | wms.ts, stock-ops.ts, manufacturing.ts |
| UAT-INV-095 | WMS confirm-pick screen — the wave → **pick** → pack step is now reachable | WarehouseOperator (`wh_custody`) | a wave with an Open pick list for item A (bin A-01-01, requested 5, on-hand 10) | 1. On `/wms` **หยิบ (Pick)** tab: `GET /api/wms/picks?status=Open,Picking`. 2. select the pick → `GET /api/wms/picks/{pickNo}`. 3. submit counted qty per line → `POST /api/wms/picks/{pickNo}/pick`. 4. Pack the now-picked list. | as left | (1) the open pick is listed. (2) lines carry `pick_line_id`, `requested_qty` 5, suggested `bin_code` A-01-01. (3) bin stock decrements, line/list → `Picked`. (4) Pack now **succeeds** — before, a waved list stayed `Open` with no pick UI and could never be packed. | High | Positive | (no new control — surfaces the existing pick endpoint) | Not Run | wms.ts |
| UAT-INV-096 | ATP / order-promising surfaced on the `/costing` screen (INV-09) | Planner (`planner`/`pos`) | WIDGET: on-hand 50, allocated 20, safety 10, an open PO of 80 within the horizon (ATP 100) | 1. On `/costing → พร้อมส่งมอบ & จองสินค้า (ATP)`: enter WIDGET, qty 90, need-by → **ตรวจสอบ**. 2. enter a ref (SO-9), **จองสินค้า** 30. 3. re-check qty 90. 4. in **รายการจอง**, **ยกเลิกจอง** SO-9. 5. re-check; reserve SO-9 30 again then **ส่งมอบแล้ว**. | as left | (1) green **พร้อมส่งมอบได้**, ATP 100 with on-hand/allocated/safety + the scheduled PO listed; (2) reserved 30 (toast), the register lists SO-9 Open, open-qty rises; (3) ATP now 70; (4) release → SO-9 Cancelled, ATP back to 100; (5) fulfil → SO-9 Fulfilled, register shows 0 open for it. Reserving beyond ATP → `INSUFFICIENT_ATP`. | High | UI / Control | INV-09 | Not Run | costing.ts (backend, 19); manual 04 §11 |
| UAT-INV-097 | MRP plan-to-PR + lot-sizing + rough-cut capacity surfaced on the `/production → MRP` tab | Planner / Procurement (`planner`/`procurement`) | 2-level BOM (CAKE→SPONGE→FLOUR/EGG/FROSTING); item masters for lot-sizing; routing RT-CAKE (MIX setup 30 + 5/u, BAKE 10/u) | 1. On `/production → MRP`: enter demand 10 BOM-CAKE, tick **ใช้ขนาดล็อต**, **รัน MRP**. 2. **สร้างใบขอซื้อจากแผน (Plan → PR)**. 3. in **กำลังการผลิตคร่าว (RCCP)** add work centres MIX=100 / BAKE=60, **รันกำลังการผลิต**. | as left | (1) Make (CAKE/SPONGE) + Buy lists render; with lot-sizing the buy grid shows a **นโยบายล็อต** column (min/multiple/eoq) and `ordered_qty`; (2) a green **ใบขอซื้อ: PR-…** badge appears (consolidated PR from the planned buy; no buy → `mf.mrp_pr_none`); (3) the capacity table shows MIX load 80 / avail 100 **พอ**, BAKE load 100 / avail 60 **เกิน** (overloaded), summary 1 overloaded. | High | UI | Feature (MRP→PR / RCCP) | Not Run | mrp.ts (backend, 16); PN-15 §5a |
| UAT-INV-051 | Modifier COGS folds into the sold line's COGS | Manager | KP01 recipe cost 8.95 (post_cogs); modifier "หมูเพิ่ม" `cogs_delta`=12 attached | 1. `POST /api/menu/resolve` `{sku:KP01, qty:1, modifier_option_ids:[extraPork]}`. 2. `POST /api/portal/pos/sales` `{items:[{item_id:KP01, qty:2, unit_price:80, modifier_option_ids:[extraPork]}]}`. | as left | (1) `unit_price`=80, `modifier_cogs`=12. (2) `POS-COGS` entry `Dr 5300`=41.90 / `Cr 1200`=41.90 (8.95×2 + 12×2). | High | Positive | INV-03 | Not Run | recipe.ts |
| UAT-INV-052 | Modifier on a non-recipe item posts COGS for the modifier only | Manager | COLA non-recipe; modifier "ไข่ดาว" `cogs_delta`=4 | 1. `POST /api/portal/pos/sales` `{items:[{item_id:COLA, qty:1, unit_price:30, modifier_option_ids:[egg]}]}`. | as left | `POS-COGS` `Dr 5300`=4 / `Cr 1200`=4; base COLA contributes no recipe COGS. | Med | Control | INV-03 | Not Run | recipe.ts |
| UAT-INV-053 | BoM yield factor inflates gross consumption | Manager | YP01 recipe: PORK `qty_per`=0.10, `unit_cost`=50, `yield_factor`=0.5 | 1. `GET /api/menu/items/YP01/recipe`. 2. `POST /api/portal/pos/sales` `{items:[{item_id:YP01, qty:1, unit_price:40}]}`. | as left | (1) `recipe_cost`=10.00 (0.10/0.5×50), line `gross_qty`=0.20. (2) PORK stock drops by **0.20** (gross, not 0.10); `POS-COGS` `Dr 5300`=10.00. | High | Positive | INV-03 | Not Run | recipe.ts |
| UAT-INV-054 | Default yield (1.0) leaves existing recipes unchanged | Manager | KP01 recipe (no yield set) | 1. `GET /api/menu/items/KP01/recipe`. | — | `recipe_cost`=8.95 unchanged; each line `yield_factor`=1, `gross_qty`=`qty_per` (backward-compatible). | Med | Control | INV-03 | Not Run | recipe.ts |
| UAT-INV-055 | Food-cost variance rolls up by reason code | F&B Controller | EOD counts: NOODLE +2 (PORTIONING@Sauce, ฿15); OIL −1 (WASTE@Fry, ฿50) | 1. `GET /api/menu/food-cost/variance?from=&to=`. | window covering counts | `by_reason`: PORTIONING `variance_cost`=+30, WASTE=−50; per-item NOODLE +30 (High). | High | Positive | INV-11 | Not Run | restaurant.ts |
| UAT-INV-056 | Food-cost variance rolls up by station | F&B Controller | as above | 1. `GET …/variance`. | as left | `by_station`: Sauce `variance_cost`=+30, `variance_pct`=20; Fry −50. | High | Control | INV-11 | Not Run | restaurant.ts |
| UAT-INV-057 | Variance reason_code enum-validated on record | Customer (cust_variance) | — | 1. `POST /api/portal/variance` `{items:[{item_id, actual_use, reason_code:'NONSENSE'}]}`. | as left | **400** — `reason_code` must be one of WASTE/OVERSTOCK/SPOILAGE/PORTIONING/THEFT/OTHER (VarianceBody Zod enum). | Med | Control | INV-11 | Not Run | (schema-enforced) |
| UAT-INV-058 | Demand-driven par recommendation flags under-buffered branch | Planner | X1@BB: static reorder 10, lead 3d; ~10/day consumption logged | 1. `GET /api/replenishment/par-recommendations?branch_id=BB`. | — | X1: `avg_daily_usage`=10, `recommended_reorder_point`=38 (⌈10×3×1.25⌉), `under_buffered`=true, `gap`=28. | High | Positive | INV-12 | Not Run | wms.ts |
| UAT-INV-059 | Apply par recommendation writes the reorder point | Planner | recommendation from INV-058 | 1. `POST /api/replenishment/par-recommendations/apply` `{branch_id:BB, item_id:X1}`. | — | `applied:true`; `branch_stock.reorder_point` for X1@BB = **38** (was 10). | High | Control | INV-12 | Not Run | wms.ts |
| UAT-INV-060 | StockCounter cannot see /stock-adjustment in nav | StockCounter (`wh_count` only) | user with `wh_count` only | 1. Login as StockCounter. 2. Check sidebar under สินค้าคงคลัง. | — | "อนุมัติปรับสต๊อก" item is absent from the sidebar; direct navigation to `/stock-adjustment` returns 403. | High | Control | R11, INV-04 | Not Run | manual |
| UAT-INV-061 | StockCounter can save a count but not post it | StockCounter | items in stock | 1. `/stocktake` → add lines → บันทึกใบนับ. 2. Confirm UI shows "send to Inventory Controller" note — no Post button. | — | Count saved with status "Counted"; page shows link to `/stock-adjustment` with note; no "ลงบัญชีผลต่าง" button. | High | Control | R11, INV-04 | Not Run | manual |
| UAT-INV-062 | InventoryController can post a counted stocktake | InventoryController (`wh_adjust`) | count from UAT-INV-061 | 1. `/stock-adjustment` → "ใบนับรอลงบัญชี" tab. 2. Click ลงบัญชีผลต่าง on the count. | — | Count posted; variance JE created (Dr 5810 / Cr 1200); stocktake status changes to "Posted". | High | Control | R11, INV-04 | Not Run | manual |
| UAT-INV-063 | Posting via /api/stocktake/:id/post requires wh_adjust | StockCounter | count with status "Counted" | 1. `POST /api/stocktake/<stNo>/post` with StockCounter JWT. | — | **403 FORBIDDEN** — `wh_adjust` permission required; count remains "Counted". | High | Control | R11, INV-04 | Not Run | compliance.ts |
| UAT-INV-064 | InventoryController can approve pending write-off on /stock-adjustment | InventoryController | write-off request from a warehouse user | 1. `/stock-adjustment` → "ตัดสต๊อกรออนุมัติ" tab. 2. Click อนุมัติ. | — | Write-off approved; stock decremented; GL posted Dr 5810 / Cr 1200. | High | Control | R11, INV-07 | Not Run | manual |
| UAT-INV-065 | APS finite-capacity scheduling | Planner / Warehouse | work centres MIXER+OVEN (480 min/day); routing RT-CAKE (op10 MIXER 10+2·qty, op20 OVEN 20+3·qty); two WOs qty 20 (A far-due, B past-due) | 1. `POST /api/work-centers` ×2. 2. `POST /api/aps/schedule` `{horizon_start, work_orders:[A,B]}`. | as left | 4 ops scheduled, `unscheduled_no_routing`=0, `makespan_minutes`=210 (1 day); MIXER dispatch starts **[0, 50]** (the second op waits for the centre → finite capacity), load 100; `late` includes the past-due WO and not the far-due one (`summary.late`=1). | High | Positive | docs/22 Phase A (operational; no control) | Not Run | mfg-depth.ts |
| UAT-INV-066 | QR deep-link URL resolves the same as a raw payload | WarehouseOperator | item A + asset FA-TEST seeded | 1. `parseQrPayload('https://erp/q?d=' + encodeURIComponent('ASSET_ID:FA-TEST\|DESC:x'))`. 2. `POST /api/assets/scan-update {code: <that URL>, location:'Zone C'}`. | as left | (1) unwraps → `ASSET_ID`=`FA-TEST`. (2) 201, `location`='Zone C' — a pasted `/q?d=` deep link resolves the asset end-to-end (same as the raw `ASSET_ID:…` code). | High | Control | FA-04 (capture) | Pass | module-qr.ts |
| UAT-INV-067 | scanCodeId no longer drops an asset/bare tag | WarehouseOperator | — | 1. `scanCodeId('ASSET_ID:FA-9\|DESC:x')`. 2. `scanCodeId('P001')`. | — | (1) → `FA-9` (previously scan-to-fill read only `ITEM_ID` and silently dropped it). (2) → `P001` (bare code). | Med | Control | INV-04 (capture), FA-04 | Pass | module-qr.ts |
| UAT-INV-068 | Resolve endpoint identifies item / asset / unknown | Any scan-capable role (`mobile`/`warehouse`/`exec`/`creditors`/`dashboard`) | item A + asset FA-TEST | 1. `GET /api/scan/sessions/resolve?d=ASSET_ID:FA-TEST`. 2. `?d=A`. 3. `?d=NOPE-404`. | as left | (1) `{kind:'asset', id:'FA-TEST'}`. (2) `{kind:'item', id:'A'}`. (3) `{kind:'unknown', id:'NOPE-404'}`. Powers the `/q` resolver page. | High | Positive | FA-04 / INV-04 (capture) | Pass | module-qr.ts |
| UAT-INV-069 | In-app camera scanner (cross-browser) fills the scan box | WarehouseOperator | printed item QR label | 1. On `/stocktake` or `/goods-issue` tap **สแกน QR / Scan QR**. 2. Grant camera permission. 3. Point at the QR. | — | Camera opens on **any browser with a camera** (Chrome/Edge/**Safari-iPhone**/Firefox/Android — native detector or JS fallback); on read: **beep+vibrate**, code parsed, item field pre-filled, dialog closes. Torch button shows if the camera supports it. | Med | UI | INV-04 (capture) | Not Run | manual (page side: qr-resolver.spec.ts) |
| UAT-INV-069b | Continuous multi-scan auto-adds session lines | WarehouseOperator | open scan session on `/mobile-scan` | 1. Tap **สแกน QR**. 2. Scan several item tags in a row without closing. | — | Each distinct read auto-adds a line (qty 1), dup reads within ~1.5s ignored; running "Scanned N" count shown; **Done** closes. Lines commit on session close. | Med | UI | INV-04 (capture) | Not Run | manual |
| UAT-INV-069c | Scanner reads a 1D product barcode | WarehouseOperator | item tagged with an EAN/Code-128 barcode | 1. Tap **สแกน QR** and point at a 1D barcode. | — | The barcode value is decoded and treated as the Item ID (bare code → `scanCodeId`), filling the item field — not just QR. | Med | UI | INV-04 (capture) | Not Run | manual |
| UAT-INV-070 | Phone native camera opens the /q resolver (deep-link) | WarehouseOperator | `WEB_BASE_URL` set; printed deep-link QR | 1. Scan an item/asset tag with the phone's built-in camera app. 2. Open the offered link. | — | Browser opens `/q?d=…`; the page shows the item/asset identity; if logged in it shows live details + links into the workspace, else offers a login link that returns to `/q`. | Med | UI | FA-04 / INV-04 (capture) | Pass (page) | qr-resolver.spec.ts |
| UAT-INV-070b | /q resolver page identifies item / asset / empty (automated) | — (public page, no session) | production web build | 1. GET `/q?d=ITEM_ID:A\|DESC:Apple`. 2. GET `/q?d=ASSET_ID:FA-1\|DESC:Test Fridge`. 3. GET `/q` (no payload). | as left | (1) shows Item, id `A`, `Apple`, links to `/mobile-scan` + `/goods-issue`. (2) shows Asset, `FA-1`, `Test Fridge`, link to `/assets`. (3) "No data in this QR link". Server-side lookup falls back to the payload when unauthenticated. | High | UI | FA-04 / INV-04 (capture) | Pass | qr-resolver.spec.ts |
| UAT-INV-071 | Wave-candidates pending list — appears before, gone after waving | WarehouseOperator (`wh_custody`) | POS sales `SALE-W1`/`SALE-W2` not yet waved | 1. `GET /api/wms/wave-candidates`. 2. `POST /api/wms/waves` for both. 3. `GET /api/wms/wave-candidates` again. | — | (1) both sales listed (`source_type`, `source_ref`, status/amount info); voided sales and cancelled/closed dine-ins excluded. (3) both gone — the pick_lists `(source_type, source_ref)` unique key defines pending. The `/wms` Wave tab dropdown shows exactly this list. | Med | Positive | (no new control — read-only feed) | Pass | wms.ts |
| UAT-INV-072 | Picks / shipments status lists feed the Pack & Ship dropdowns | WarehouseOperator (`wh_custody`) | a wave with one pick fully picked, then packed | 1. `GET /api/wms/picks?status=Open` after waving. 2. pick fully → `GET /api/wms/picks?status=Picked`. 3. pack → `GET /api/wms/shipments?status=Packed`. | — | (1) both new picks listed. (2) the picked list contains the packable pick only (Pack tab lists only `Picked` — matching the `NOT_PICKED` service guard). (3) the packed shipment appears with its `source_ref` (Ship tab lists only `Packed`). Tenant-scoped, newest first, limit 100. | Med | Positive | (no new control — read-only feed) | Pass | wms.ts |
| UAT-INV-073 | Manufacturing doc-reference dropdowns (UI) | Production supervisor (`bom_master`/`warehouse`) | an open WO, a routing, a BOM and a GR exist | 1. `/production` → shop-floor WO + routing fields. 2. `/production` → quality ref-doc with the WO/GR toggle. 3. `/manufacturing` → BOM field. 4. `/goods-issue` → ref field. | — | Every field is a dropdown fed by the existing list GET (`/api/manufacturing/work-orders`, `/api/routings`, `/api/bom/master`, `/api/procurement/grs`); the quality ref list switches source with the WO/GR toggle (switching clears the picked ref); **พิมพ์เลขเอกสารเอง…** manual escape everywhere. No endpoint/control change. | Med | UI | (no new control — usability) | Not Run | manual |
| UAT-INV-074 | Stocktakes and stock movements are tenant-isolated (cross-tenant read/post blocked) | Warehouse (T1) + Warehouse (T2) | Two tenants, each with a warehouse user | 1. T1 creates a stocktake with a variance line. 2. As **T2**: `GET /api/stocktake`. 3. As T2: `GET /api/stocktake/{stNo}`. 4. As T2: `POST /api/stocktake/{stNo}/post`. 5. As T1: list. 6. As T2: `GET /api/inventory/movements`. | — | (2) T1’s sheet is **absent**. (3) **404**. (4) **404** — T2 cannot post T1’s variance movements. (5) T1 still sees its own sheet. (6) empty (movement history scoped). Before migration 0316 all of (2)–(4) succeeded. | High | Control | ITGC-AC-18 · R11 | Not Run | stock-ops.ts |
