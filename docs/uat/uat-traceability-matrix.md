# UAT Traceability Matrix — Invisible ERP V2

**Status: DRAFT v7.24 · *v7.24: SME-01 — SME single-user edition (docs/49, new RCM control **SME-01**, migration `0412`, RCM 290) — maps UAT-ADM-157..160 to SME-01: `common/control-profile.ts` `assertMakerChecker` is the single maker-checker seam; a `control_profile='sme'` tenant (god-only at creation, upgrade-only to enterprise — `PROFILE_DOWNGRADE_FORBIDDEN`) may self-approve WITH a mandatory logged reason (`SELF_APPROVAL_REASON_REQUIRED`), writing `self_approvals` evidence reviewed by the scheduled `sme_self_approval_review` report (external accountant + platform owner); enterprise behaviour byte-identical. ToE `sme.ts`; narrative PN-23 rev 0.5; manual 11 + FAQ.* · *v7.23: PROJ-26 — project phase-gate governance (docs/44 PPM Wave P4, new RCM control **PROJ-26**, migration `0411`, RCM 289) — maps UAT-O2C-464..470 to PROJ-26: `projects-gate.service.ts` gates a project's advance along the concept→planning→execution→closeout→closed ladder; `submitGate` is forward-only + single-pending (`BAD_PHASE`/`BAD_PHASE_ORDER`/`GATE_ALREADY_PENDING`); `decideGate` records GO/HOLD/KILL where decider ≠ submitter (`SOD_SELF_APPROVAL`/`GATE_ALREADY_DECIDED`); a GO advances the project (current phase = latest GO gate's target), HOLD/KILL record without advancing. ToE `projects.ts` 314→323; narrative PN-16 §7 (37) rev 0.53; manual 14 rev 2.33.* · *v7.22: PROJ-25 — portfolio selection scenarios (docs/44 PPM Wave P4, new RCM control **PROJ-25**, migration `0410`, RCM 288) — maps UAT-O2C-457..463 to PROJ-25: `projects-portfolio.service.ts` models candidate projects (include/exclude + priority) into a named scenario with a budget envelope; `analyze()` rolls up the included set's contract/budget/margin vs the envelope (`over_envelope`/`over_by`) read-only; `commitScenario` is maker-checker (committer ≠ created_by → `SOD_SELF_APPROVAL`) and blocked over the envelope (`OVER_ENVELOPE`) unless an exec overrides with a recorded reason (`OVERRIDE_REQUIRES_EXEC`), then locks the GO-set (`SCENARIO_LOCKED`/`SCENARIO_NOT_DRAFT`). No project row mutated. ToE `projects.ts` 304→314; narrative PN-16 §7 (36) rev 0.52; manual 14 rev 2.32.* · *v7.21: PROJ-24 — change-order impact simulation (docs/44 Wave PPM-A3, new RCM control **PROJ-24**, no migration, RCM 287) — maps UAT-O2C-452..456 to PROJ-24: `projects-evm.service.ts` `simulateChangeOrder` takes a **pending** change order and computes, writing nothing, the project's current vs projected contract/budget/estimated-cost, gross margin + margin %, formulaic EAC and budget headroom (signed deltas over the live figures, `evm()` for the EV/AC/CPI basis, `bac_basis` tasks|budget); the projection matches what `approveChangeOrder()` then persists (`CHANGE_ORDER_NOT_FOUND`/`CHANGE_ORDER_DECIDED` guards); read-only impersonation mutates nothing. ToE `projects.ts` 299→304; narrative PN-16 §7 (35) rev 0.51; manual 14 rev 2.31.* · *v7.20: CRM-17 — CRM data-quality: scoring + duplicate surveillance + merge audit (docs/44 Wave C4, new RCM control **CRM-16**, migration `0409`, RCM 286) — maps UAT-O2C-445..451 to CRM-16: CrmDqService scores each account on field completeness + validity (present-but-invalid scored not-ok), banded good/fair/poor, worst-first worklist + band counts + idempotent daily snapshot (`crm_dq_scores`) + `crm_dq_scan` BI report; `GET /api/crm/dq/duplicates` proactive near-duplicate surveillance (fuzzy name + exact tax/email/phone); every merge writes an append-only `crm_merge_log` inside the merge transaction. ToE `pipeline.ts`; narrative PN-18 §7.18 rev 0.21; manual 16 §16.4g rev 2.9.* · *v7.19: CRM-14 — CPQ pricebooks (docs/44 Wave C3, new RCM control **CRM-15**, migration `0408`, RCM 285) — maps UAT-O2C-439..444 to CRM-15: `cpq_pricebooks`(+entries) is a masterdata-gated, effective-dated price list; a quote created with `pricebook_id` prices each covered line from the book's entry (config + free-line paths), validated active + in-window at quote time (PRICEBOOK_NOT_FOUND/_INACTIVE/_NOT_EFFECTIVE); `quotes.pricebook_id` records the basis and the CPQ-01 floor still governs. ToE `cpq-approval.ts`; narrative PN-18 §7.17 rev 0.20; manual 01 rev 0.56.* · *v7.18: CRM-8 — unified activity timeline + collaboration feed (docs/44 Wave C1, new RCM control **CRM-14**, migration `0407`, RCM 284) — maps UAT-O2C-433..438 to CRM-14: `GET /api/crm/timeline` merges all-source activities + `crm_stage_history` + linked quotes + `crm_feed_posts` into one newest-first, tenant-scoped, BOLA-checked stream (an account rolls up its opportunities); `crm_feed_posts` is append-only (no update/delete); @mentions validated against active tenant users route a directed notification via new `notifications.target_username` (visible only to that user). ToE `pipeline.ts` 143→152; narrative PN-18 §7.16 rev 0.19; manual 16 §16.2 rev 2.8.* · DRAFT v7.17 · *v7.17: CRM-7 — pipeline kanban depth: stage playbooks (docs/44 Wave C1, new RCM control **CRM-13**, migration `0406`, RCM 283) — maps UAT-O2C-427..432 to CRM-13: `crm_stage_playbooks` (one row per stage) gates the single governed `setStage` path with per-stage required-field exit criteria (`STAGE_REQUIREMENTS_UNMET`, skipped for Lost) + a WIP limit (`WIP_LIMIT_EXCEEDED`, ignored for Won/Lost); `GET/PUT /api/crm/pipeline/playbooks[/:stageId]` (config gate crm/exec, whitelist-validated, tenant master data) + `POST …/opportunities/bulk-stage` (per-item, non-atomic). Web: WIP badges + required-field/guidance on the kanban columns, a supervisor playbook editor, list-view bulk move. ToE `pipeline.ts` 132→143; narrative PN-18 §7.15 rev 0.18; manual 16 §16.1b rev 2.7.* · *v7.16: force purge for the shared item master (no new control) — maps UAT-ADM-156: god-only `POST /api/admin/item-maintenance/force-purge` deletes products even when a company still references them (wiping the cross-tenant reference rows across all companies), gated by a mandatory blast-radius preview (`force-preview`) + the strong confirm `FORCE-PURGE-ITEMS`; ToE `onboarding.ts`; PN-17 §7 step 14; manual 11 §8.1.* · *v7.15: PROJ-23 — resource leveling: over-allocation vs the CPM schedule's slack (docs/44 PPM-A2, new RCM control **PROJ-23**, no migration, RCM 281) — maps UAT-O2C-421..426 to PROJ-23: `GET /api/projects/:code/resource-leveling` detects an over-allocated resource-month within a project's own assignments (calendar-aware ceiling, PROJ-20) and cross-references the CPM schedule's per-task slack (PROJ-06/21) to suggest shifting the contributing task with the most slack, or flags `NO_SLACK` when every contributor is on the critical path. Project-scoped, detective, suggests only. ToE `projects.ts` 292→299; narrative PN-16 §34 rev 0.50; manual 14 rev 2.30.* · *v7.14: docs/48 — Marketing Mix Modeling / MMM (new control **MKT-15**, Partial, migration `0405`) — maps UAT-RPT-058: staging→core→analytics pipeline, ingest → `computeMmm` (pure/deterministic attribution) → audited `mmm_model_runs`/`mmm_channel_results`, tenant-isolated + BOLA-safe getRun, BI `mmm_run`/`mmm_summary` + live `GET /api/bi/mmm-summary`.* · *v7.13: global item-master garbage collection (shared-master unused-item purge; no new control) — maps UAT-ADM-153..155: because `items` is a shared master (no `tenant_id`), factory-reset/purge leave a wiped company's products in the shared catalogue and they keep showing in every tenant's `/shop`; platform-owner-only preview `GET /api/admin/item-maintenance/unused-items` + purge `POST …/purge-unused-items` (`confirm: PURGE-UNUSED-ITEMS`) delete only items no tenant references any more (computed cross-tenant under the `@PlatformAdmin` bypass), keep still-referenced items, preserve merged rows, idempotent; ToE `onboarding.ts`; PN-17 §7 step 14; manual 11 §8.1.* · *v7.12: PROJ-22 — bottom-up cost-to-complete (ETC) vs the formulaic EAC (docs/44 PPM-B2, new RCM control **PROJ-22**, migration `0404`, RCM 279) — maps UAT-O2C-415..420 to PROJ-22: `project_etc` (per-task or project-level, append-only — latest row per bucket is current) drives `GET /api/projects/:code/eac-scenarios`'s bottom-up EAC vs the pre-existing formulaic EAC + their variance; `POST /api/projects/:code/etc` guards `TASK_NOT_FOUND`. ToE `projects.ts` 286→292; narrative PN-16 §33 rev 0.49; manual 14 rev 2.29.* · *v7.11: docs/47 — reputation & external analytics ingestion (new control **MKT-14**, migration `0402`) — maps UAT-UI-REP-01: Google Maps reviews (Business Profile OAuth2 poll, no webhook exists) + Google Analytics GA4 sync (Data API poll), encrypted OAuth token storage, tenant-scoped ingestion, dedicated `/reputation` screen.* · *v7.08: docs/45 UI gap closure (no new control) — maps UAT-UI-ROI-01/UAT-UI-AUD-01/UAT-UI-CHREF-01: marketing ROI dashboard tab, audience-export screen, channel-link staff/member screens (fixing a dead /portal/loyalty URL).* · *v7.06: weather overlay for demand forecasting (docs/45 residual; no new control) — maps UAT-RPT-057: opt-in `weather` candidate in demand-ml (Open-Meteo, DEMAND_WEATHER_ENABLED), byte-identical to dow_seasonal when off.* · *v7.05: G3c consent-withdrawal removal sync (extends PDPA-05, migration `0386`) — maps UAT-LOY-084: manifest-driven REMOVE ops (Meta DELETE / Google remove job / webhook), `rows_removed` register evidence, idempotent.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v6.98: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.10 · *v7.10: PROJ-21 — richer scheduling: dependency types/lag, SNET/FNLT constraints, opt-in working calendar (docs/44 PPM-B1, new RCM control **PROJ-21**, migration `0401`, RCM 278) — maps UAT-O2C-406..414 to PROJ-21: `project_task_dependencies` (per-edge type FS/SS/FF/SF + lag/lead) drives the CPM-with-lag forward/backward pass; per-task `constraint_type`/`constraint_offset_days` (SNET/FNLT) floors/caps es/lf; an opt-in per-tenant working calendar (`project_calendars`/`project_calendar_exceptions`) counts working days once enabled. ToE `projects.ts` 272→286; narrative PN-16 §32 rev 0.48; manual 14 rev 2.28.* · *v7.09: PROJ-20 — resource capacity heatmap governed by a real availability calendar + role/skill supply-vs-demand (docs/44 PPM-A1, new RCM control **PROJ-20**, migration `0400`, RCM 277) — maps UAT-O2C-398..405 to PROJ-20: `resource_skills` (named-vs-generic) + `resource_calendar` (per-resource per-month availability ceiling, default 100%) make the capacity heatmap's over-allocation flag calendar-aware, and `GET /api/projects/resources/role-demand` rolls demand vs qualified supply per role/month (`understaffed` flag). Two tenant tables, RLS 0232. Purely additive to PROJ-05 (existing callers unaffected). ToE `projects.ts` (8 PROJ-20 checks, 272 total); narrative PN-16 §7 (31) rev 0.47; manual 14 rev 2.27.* · *v7.07: CRM-14 — CPQ bundles, tiered discount authority & guided selling (docs/44, new RCM control **CRM-12**, migration `0399`, RCM 276) — maps UAT-O2C-391..397 to CRM-12: bundle master data (`cpq_bundles`/`cpq_bundle_items`) expands into ordinary instance-tagged `quote_lines` so the **unmodified** CPQ-01 margin-floor check governs a bundle's blended margin; a second, higher `exec_discount_pct` ceiling requires an `exec`-tier approver specifically (`quote_approvals.required_tier`, `403 TIER_APPROVAL_REQUIRED` on a tier mismatch); explainable co-purchase recommendations. Two tenant tables + 3 columns, RLS 0232. ToE `cpq-approval.ts` (13 CRM-14 checks, 30 total); narrative PN-18 §7.14 rev 0.17; manual 01-sales-and-pos.md v0.55.* · *v7.04: CRM-8 — sales sequences / cadences (docs/44, new RCM control **CRM-11**, migration `0392`, RCM 275) — maps UAT-O2C-384..390 to CRM-11: `crm_sequences`/`crm_sequence_steps` playbooks (channel email/line/sms/task + wait_days) + lead/deal `crm_sequence_enrollments`; advance sends via MessagingService + logs a `crm_activities` touch + schedules the next step + completes on the last; `run-due` sweep + BI report `crm_sequence_run` (module-owned BiReportSource, docs/46). New module `crm/sequences`, RLS 0232. ToE `pipeline.ts` (10 CRM-8 checks, 132 total); PN-18 §7.13 rev 0.16; manual 16 §16.4f rev 2.4.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.02: CRM-11 — persisted territory & quota management (docs/44, new RCM control **CRM-10**, migration `0385`, RCM 274) — maps UAT-O2C-378..383 to CRM-10: governed `crm_territories` (regions/segments/categories criteria + self-parent roll-up hierarchy + manager), rep membership (`crm_territory_members`), per-period owner/territory quotas (`crm_quotas`); `GET /api/crm/territory/attainment` reconciles won-in-period per owner vs owner quota + rolls a territory's SUBTREE up vs the territory quota. Three tenant tables, RLS 0232. New module `crm/territory`. ToE `pipeline.ts` (6 CRM-11 checks, 122 total); narrative PN-18 §7.12 rev 0.15; manual 16 §16.4e rev 2.3.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v7.00: PROJ-19 — earned schedule (docs/44 Wave P1, new RCM control **PROJ-19**, RCM 272, no migration) — maps UAT-O2C-378 to PROJ-19: `GET /api/projects/:code/earned-schedule` computes Lipke **ES** off the PV curve (plateau at exactly EV credited — no false alarm) → **SPI(t) = ES/AT**, **SV(t)**, **IEAC(t)** finish forecast (`NO_DATED_PLAN`/`PLAN_NOT_STARTED` when not computable), staying honest where the classic SPI converges to 1 on a late project; the PMO action center (PROJ-11) flags `schedule_slip_es` (medium) when SPI(t) < 0.9 on a not-already-red project; an earned-schedule card joins the classic EVM on the project overview. ToE `projects.ts` 259→264; narrative PN-16 §7 (30) rev 0.46; manual 14 rev 2.26.* · *v6.99: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.98: CRM-12 — sales forecasting depth (docs/44, new RCM control **CRM-09**, migration `0378`, RCM 271) — maps UAT-O2C-370..377 to CRM-09: a rep→manager OVERRIDE roll-up (`crm_forecast_submissions`, governed draft→submitted) reconciled against the system-weighted forecast with the VARIANCE (`GET /api/crm/forecast/depth`), a pipeline-COVERAGE ratio (open pipeline ÷ commit target, ≥3× healthy) + a commit→best-case→pipeline WATERFALL, and dated period SNAPSHOTS (`crm_forecast_snapshots`, idempotent per period/day, BI report `crm_forecast_snapshot`) capturing forecast + actual-won for the forecast-vs-actual ACCURACY trend. New module `crm/forecast`; BI constructor param appended at END (positional golden-master contract). ToE `pipeline.ts` (8 CRM-12 checks, 116 total); narrative PN-18 §7.11 rev 0.14; manual 16 §16.4d rev 2.2.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.09 · *v7.09: PROJ-20 — resource capacity heatmap governed by a real availability calendar + role/skill supply-vs-demand (docs/44 PPM-A1, new RCM control **PROJ-20**, migration `0400`, RCM 277) — maps UAT-O2C-398..405 to PROJ-20: `resource_skills` (named-vs-generic) + `resource_calendar` (per-resource per-month availability ceiling, default 100%) make the capacity heatmap's over-allocation flag calendar-aware, and `GET /api/projects/resources/role-demand` rolls demand vs qualified supply per role/month (`understaffed` flag). Two tenant tables, RLS 0232. Purely additive to PROJ-05 (existing callers unaffected). ToE `projects.ts` (8 PROJ-20 checks, 272 total); narrative PN-16 §7 (31) rev 0.47; manual 14 rev 2.27.* · *v7.07: CRM-14 — CPQ bundles, tiered discount authority & guided selling (docs/44, new RCM control **CRM-12**, migration `0399`, RCM 276) — maps UAT-O2C-391..397 to CRM-12: bundle master data (`cpq_bundles`/`cpq_bundle_items`) expands into ordinary instance-tagged `quote_lines` so the **unmodified** CPQ-01 margin-floor check governs a bundle's blended margin; a second, higher `exec_discount_pct` ceiling requires an `exec`-tier approver specifically (`quote_approvals.required_tier`, `403 TIER_APPROVAL_REQUIRED` on a tier mismatch); explainable co-purchase recommendations. Two tenant tables + 3 columns, RLS 0232. ToE `cpq-approval.ts` (13 CRM-14 checks, 30 total); narrative PN-18 §7.14 rev 0.17; manual 01-sales-and-pos.md v0.55.* · *v7.04: CRM-8 — sales sequences / cadences (docs/44, new RCM control **CRM-11**, migration `0392`, RCM 275) — maps UAT-O2C-384..390 to CRM-11: `crm_sequences`/`crm_sequence_steps` playbooks (channel email/line/sms/task + wait_days) + lead/deal `crm_sequence_enrollments`; advance sends via MessagingService + logs a `crm_activities` touch + schedules the next step + completes on the last; `run-due` sweep + BI report `crm_sequence_run` (module-owned BiReportSource, docs/46). New module `crm/sequences`, RLS 0232. ToE `pipeline.ts` (10 CRM-8 checks, 132 total); PN-18 §7.13 rev 0.16; manual 16 §16.4f rev 2.4.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.02: CRM-11 — persisted territory & quota management (docs/44, new RCM control **CRM-10**, migration `0385`, RCM 274) — maps UAT-O2C-378..383 to CRM-10: governed `crm_territories` (regions/segments/categories criteria + self-parent roll-up hierarchy + manager), rep membership (`crm_territory_members`), per-period owner/territory quotas (`crm_quotas`); `GET /api/crm/territory/attainment` reconciles won-in-period per owner vs owner quota + rolls a territory's SUBTREE up vs the territory quota. Three tenant tables, RLS 0232. New module `crm/territory`. ToE `pipeline.ts` (6 CRM-11 checks, 122 total); narrative PN-18 §7.12 rev 0.15; manual 16 §16.4e rev 2.3.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v7.00: PROJ-19 — earned schedule (docs/44 Wave P1, new RCM control **PROJ-19**, RCM 272, no migration) — maps UAT-O2C-378 to PROJ-19: `GET /api/projects/:code/earned-schedule` computes Lipke **ES** off the PV curve (plateau at exactly EV credited — no false alarm) → **SPI(t) = ES/AT**, **SV(t)**, **IEAC(t)** finish forecast (`NO_DATED_PLAN`/`PLAN_NOT_STARTED` when not computable), staying honest where the classic SPI converges to 1 on a late project; the PMO action center (PROJ-11) flags `schedule_slip_es` (medium) when SPI(t) < 0.9 on a not-already-red project; an earned-schedule card joins the classic EVM on the project overview. ToE `projects.ts` 259→264; narrative PN-16 §7 (30) rev 0.46; manual 14 rev 2.26.* · *v6.99: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.98: CRM-12 — sales forecasting depth (docs/44, new RCM control **CRM-09**, migration `0378`, RCM 271) — maps UAT-O2C-370..377 to CRM-09: a rep→manager OVERRIDE roll-up (`crm_forecast_submissions`, governed draft→submitted) reconciled against the system-weighted forecast with the VARIANCE (`GET /api/crm/forecast/depth`), a pipeline-COVERAGE ratio (open pipeline ÷ commit target, ≥3× healthy) + a commit→best-case→pipeline WATERFALL, and dated period SNAPSHOTS (`crm_forecast_snapshots`, idempotent per period/day, BI report `crm_forecast_snapshot`) capturing forecast + actual-won for the forecast-vs-actual ACCURACY trend. New module `crm/forecast`; BI constructor param appended at END (positional golden-master contract). ToE `pipeline.ts` (8 CRM-12 checks, 116 total); narrative PN-18 §7.11 rev 0.14; manual 16 §16.4d rev 2.2.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.08 · *v7.08: docs/45 UI gap closure (no new control) — maps UAT-UI-ROI-01/UAT-UI-AUD-01/UAT-UI-CHREF-01: marketing ROI dashboard tab, audience-export screen, channel-link staff/member screens (fixing a dead /portal/loyalty URL).* · *v7.06: weather overlay for demand forecasting (docs/45 residual; no new control) — maps UAT-RPT-057: opt-in `weather` candidate in demand-ml (Open-Meteo, DEMAND_WEATHER_ENABLED), byte-identical to dow_seasonal when off.* · *v7.05: G3c consent-withdrawal removal sync (extends PDPA-05, migration `0386`) — maps UAT-LOY-084: manifest-driven REMOVE ops (Meta DELETE / Google remove job / webhook), `rows_removed` register evidence, idempotent.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v6.98: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.07 · *v7.07: CRM-14 — CPQ bundles, tiered discount authority & guided selling (docs/44, new RCM control **CRM-12**, migration `0399`, RCM 276) — maps UAT-O2C-391..397 to CRM-12: bundle master data (`cpq_bundles`/`cpq_bundle_items`) expands into ordinary instance-tagged `quote_lines` so the **unmodified** CPQ-01 margin-floor check governs a bundle's blended margin; a second, higher `exec_discount_pct` ceiling requires an `exec`-tier approver specifically (`quote_approvals.required_tier`, `403 TIER_APPROVAL_REQUIRED` on a tier mismatch); explainable co-purchase recommendations. Two tenant tables + 3 columns, RLS 0232. ToE `cpq-approval.ts` (13 CRM-14 checks, 30 total); narrative PN-18 §7.14 rev 0.17; manual 01-sales-and-pos.md v0.55.* · *v7.04: CRM-8 — sales sequences / cadences (docs/44, new RCM control **CRM-11**, migration `0392`, RCM 275) — maps UAT-O2C-384..390 to CRM-11: `crm_sequences`/`crm_sequence_steps` playbooks (channel email/line/sms/task + wait_days) + lead/deal `crm_sequence_enrollments`; advance sends via MessagingService + logs a `crm_activities` touch + schedules the next step + completes on the last; `run-due` sweep + BI report `crm_sequence_run` (module-owned BiReportSource, docs/46). New module `crm/sequences`, RLS 0232. ToE `pipeline.ts` (10 CRM-8 checks, 132 total); PN-18 §7.13 rev 0.16; manual 16 §16.4f rev 2.4.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.02: CRM-11 — persisted territory & quota management (docs/44, new RCM control **CRM-10**, migration `0385`, RCM 274) — maps UAT-O2C-378..383 to CRM-10: governed `crm_territories` (regions/segments/categories criteria + self-parent roll-up hierarchy + manager), rep membership (`crm_territory_members`), per-period owner/territory quotas (`crm_quotas`); `GET /api/crm/territory/attainment` reconciles won-in-period per owner vs owner quota + rolls a territory's SUBTREE up vs the territory quota. Three tenant tables, RLS 0232. New module `crm/territory`. ToE `pipeline.ts` (6 CRM-11 checks, 122 total); narrative PN-18 §7.12 rev 0.15; manual 16 §16.4e rev 2.3.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v7.00: PROJ-19 — earned schedule (docs/44 Wave P1, new RCM control **PROJ-19**, RCM 272, no migration) — maps UAT-O2C-378 to PROJ-19: `GET /api/projects/:code/earned-schedule` computes Lipke **ES** off the PV curve (plateau at exactly EV credited — no false alarm) → **SPI(t) = ES/AT**, **SV(t)**, **IEAC(t)** finish forecast (`NO_DATED_PLAN`/`PLAN_NOT_STARTED` when not computable), staying honest where the classic SPI converges to 1 on a late project; the PMO action center (PROJ-11) flags `schedule_slip_es` (medium) when SPI(t) < 0.9 on a not-already-red project; an earned-schedule card joins the classic EVM on the project overview. ToE `projects.ts` 259→264; narrative PN-16 §7 (30) rev 0.46; manual 14 rev 2.26.* · *v6.99: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.98: CRM-12 — sales forecasting depth (docs/44, new RCM control **CRM-09**, migration `0378`, RCM 271) — maps UAT-O2C-370..377 to CRM-09: a rep→manager OVERRIDE roll-up (`crm_forecast_submissions`, governed draft→submitted) reconciled against the system-weighted forecast with the VARIANCE (`GET /api/crm/forecast/depth`), a pipeline-COVERAGE ratio (open pipeline ÷ commit target, ≥3× healthy) + a commit→best-case→pipeline WATERFALL, and dated period SNAPSHOTS (`crm_forecast_snapshots`, idempotent per period/day, BI report `crm_forecast_snapshot`) capturing forecast + actual-won for the forecast-vs-actual ACCURACY trend. New module `crm/forecast`; BI constructor param appended at END (positional golden-master contract). ToE `pipeline.ts` (8 CRM-12 checks, 116 total); narrative PN-18 §7.11 rev 0.14; manual 16 §16.4d rev 2.2.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.06 · *v7.06: weather overlay for demand forecasting (docs/45 residual; no new control) — maps UAT-RPT-057: opt-in `weather` candidate in demand-ml (Open-Meteo, DEMAND_WEATHER_ENABLED), byte-identical to dow_seasonal when off.* · *v7.05: G3c consent-withdrawal removal sync (extends PDPA-05, migration `0386`) — maps UAT-LOY-084: manifest-driven REMOVE ops (Meta DELETE / Google remove job / webhook), `rows_removed` register evidence, idempotent.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v6.98: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.04 · *v7.04: CRM-8 — sales sequences / cadences (docs/44, new RCM control **CRM-11**, migration `0392`, RCM 275) — maps UAT-O2C-384..390 to CRM-11: `crm_sequences`/`crm_sequence_steps` playbooks (channel email/line/sms/task + wait_days) + lead/deal `crm_sequence_enrollments`; advance sends via MessagingService + logs a `crm_activities` touch + schedules the next step + completes on the last; `run-due` sweep + BI report `crm_sequence_run` (module-owned BiReportSource, docs/46). New module `crm/sequences`, RLS 0232. ToE `pipeline.ts` (10 CRM-8 checks, 132 total); PN-18 §7.13 rev 0.16; manual 16 §16.4f rev 2.4.* · *v7.03: G3b direct Meta/Google audience adapters (extends PDPA-05; no new control) — maps UAT-LOY-083: platform-exact hashed wire formats + per-recipient register rows.* · *v7.02: CRM-11 — persisted territory & quota management (docs/44, new RCM control **CRM-10**, migration `0385`, RCM 274) — maps UAT-O2C-378..383 to CRM-10: governed `crm_territories` (regions/segments/categories criteria + self-parent roll-up hierarchy + manager), rep membership (`crm_territory_members`), per-period owner/territory quotas (`crm_quotas`); `GET /api/crm/territory/attainment` reconciles won-in-period per owner vs owner quota + rolls a territory's SUBTREE up vs the territory quota. Three tenant tables, RLS 0232. New module `crm/territory`. ToE `pipeline.ts` (6 CRM-11 checks, 122 total); narrative PN-18 §7.12 rev 0.15; manual 16 §16.4e rev 2.3.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v7.00: PROJ-19 — earned schedule (docs/44 Wave P1, new RCM control **PROJ-19**, RCM 272, no migration) — maps UAT-O2C-378 to PROJ-19: `GET /api/projects/:code/earned-schedule` computes Lipke **ES** off the PV curve (plateau at exactly EV credited — no false alarm) → **SPI(t) = ES/AT**, **SV(t)**, **IEAC(t)** finish forecast (`NO_DATED_PLAN`/`PLAN_NOT_STARTED` when not computable), staying honest where the classic SPI converges to 1 on a late project; the PMO action center (PROJ-11) flags `schedule_slip_es` (medium) when SPI(t) < 0.9 on a not-already-red project; an earned-schedule card joins the classic EVM on the project overview. ToE `projects.ts` 259→264; narrative PN-16 §7 (30) rev 0.46; manual 14 rev 2.26.* · *v6.99: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.98: CRM-12 — sales forecasting depth (docs/44, new RCM control **CRM-09**, migration `0378`, RCM 271) — maps UAT-O2C-370..377 to CRM-09: a rep→manager OVERRIDE roll-up (`crm_forecast_submissions`, governed draft→submitted) reconciled against the system-weighted forecast with the VARIANCE (`GET /api/crm/forecast/depth`), a pipeline-COVERAGE ratio (open pipeline ÷ commit target, ≥3× healthy) + a commit→best-case→pipeline WATERFALL, and dated period SNAPSHOTS (`crm_forecast_snapshots`, idempotent per period/day, BI report `crm_forecast_snapshot`) capturing forecast + actual-won for the forecast-vs-actual ACCURACY trend. New module `crm/forecast`; BI constructor param appended at END (positional golden-master contract). ToE `pipeline.ts` (8 CRM-12 checks, 116 total); narrative PN-18 §7.11 rev 0.14; manual 16 §16.4d rev 2.2.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.02 · *v7.02: CRM-11 — persisted territory & quota management (docs/44, new RCM control **CRM-10**, migration `0385`, RCM 274) — maps UAT-O2C-378..383 to CRM-10: governed `crm_territories` (regions/segments/categories criteria + self-parent roll-up hierarchy + manager), rep membership (`crm_territory_members`), per-period owner/territory quotas (`crm_quotas`); `GET /api/crm/territory/attainment` reconciles won-in-period per owner vs owner quota + rolls a territory's SUBTREE up vs the territory quota. Three tenant tables, RLS 0232. New module `crm/territory`. ToE `pipeline.ts` (6 CRM-11 checks, 122 total); narrative PN-18 §7.12 rev 0.15; manual 16 §16.4e rev 2.3.* · *v7.01: PDPA-05 — consent-gated hashed audience export (G3 docs/45, new RCM control **PDPA-05**, migration `0379`, RCM 272) — maps UAT-LOY-081..082: live-consent-only filter (no flag fallback), sha256-only payload, fail-closed ROPA gate + append-only register, SSRF-gated push.* · *v7.00: PROJ-19 — earned schedule (docs/44 Wave P1, new RCM control **PROJ-19**, RCM 272, no migration) — maps UAT-O2C-378 to PROJ-19: `GET /api/projects/:code/earned-schedule` computes Lipke **ES** off the PV curve (plateau at exactly EV credited — no false alarm) → **SPI(t) = ES/AT**, **SV(t)**, **IEAC(t)** finish forecast (`NO_DATED_PLAN`/`PLAN_NOT_STARTED` when not computable), staying honest where the classic SPI converges to 1 on a late project; the PMO action center (PROJ-11) flags `schedule_slip_es` (medium) when SPI(t) < 0.9 on a not-already-red project; an earned-schedule card joins the classic EVM on the project overview. ToE `projects.ts` 259→264; narrative PN-16 §7 (30) rev 0.46; manual 14 rev 2.26.* · *v6.99: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.98: CRM-12 — sales forecasting depth (docs/44, new RCM control **CRM-09**, migration `0378`, RCM 271) — maps UAT-O2C-370..377 to CRM-09: a rep→manager OVERRIDE roll-up (`crm_forecast_submissions`, governed draft→submitted) reconciled against the system-weighted forecast with the VARIANCE (`GET /api/crm/forecast/depth`), a pipeline-COVERAGE ratio (open pipeline ÷ commit target, ≥3× healthy) + a commit→best-case→pipeline WATERFALL, and dated period SNAPSHOTS (`crm_forecast_snapshots`, idempotent per period/day, BI report `crm_forecast_snapshot`) capturing forecast + actual-won for the forecast-vs-actual ACCURACY trend. New module `crm/forecast`; BI constructor param appended at END (positional golden-master contract). ToE `pipeline.ts` (8 CRM-12 checks, 116 total); narrative PN-18 §7.11 rev 0.14; manual 16 §16.4d rev 2.2.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v7.00 · *v7.00: PROJ-19 — earned schedule (docs/44 Wave P1, new RCM control **PROJ-19**, RCM 272, no migration) — maps UAT-O2C-378 to PROJ-19: `GET /api/projects/:code/earned-schedule` computes Lipke **ES** off the PV curve (plateau at exactly EV credited — no false alarm) → **SPI(t) = ES/AT**, **SV(t)**, **IEAC(t)** finish forecast (`NO_DATED_PLAN`/`PLAN_NOT_STARTED` when not computable), staying honest where the classic SPI converges to 1 on a late project; the PMO action center (PROJ-11) flags `schedule_slip_es` (medium) when SPI(t) < 0.9 on a not-already-red project; an earned-schedule card joins the classic EVM on the project overview. ToE `projects.ts` 259→264; narrative PN-16 §7 (30) rev 0.46; manual 14 rev 2.26.*
**Status: DRAFT v6.99 · *v6.99: G2 menu affinity (docs/45; no new control — reporting only) — maps UAT-RPT-055..056: co-purchase support/confidence/lift per daypart + schedulable `menu_affinity` BI type.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*
**Status: DRAFT v6.98 · *v6.98: CRM-12 — sales forecasting depth (docs/44, new RCM control **CRM-09**, migration `0378`, RCM 272) — maps UAT-O2C-370..377 to CRM-09: a rep→manager OVERRIDE roll-up (`crm_forecast_submissions`, governed draft→submitted) reconciled against the system-weighted forecast with the VARIANCE (`GET /api/crm/forecast/depth`), a pipeline-COVERAGE ratio (open pipeline ÷ commit target, ≥3× healthy) + a commit→best-case→pipeline WATERFALL, and dated period SNAPSHOTS (`crm_forecast_snapshots`, idempotent per period/day, BI report `crm_forecast_snapshot`) capturing forecast + actual-won for the forecast-vs-actual ACCURACY trend. New module `crm/forecast`; BI constructor param appended at END (positional golden-master contract). ToE `pipeline.ts` (8 CRM-12 checks, 116 total); narrative PN-18 §7.11 rev 0.14; manual 16 §16.4d rev 2.2.* · *v6.97: REV-27 — Significant financing component + revenue disclosure pack (Track D Wave 4 FINAL, new RCM control **REV-27**, migration `0367`; TFRS 15 / IFRS 15 / ASC 606 §60-65 + §120) — maps TC-REV-27-01..06 to REV-27: a material financing component discounts the transaction price to its cash-selling-price PV and unwinds the interest by the effective-interest method (Σ interest = face − PV); the discount rate is a maker-checker judgement (setter ≠ approver → `SOD_SELF_APPROVAL`, an un-approved component cannot post → `FINANCING_NOT_APPROVED`, one per contract → `FINANCING_ALREADY_SET`); `run-financing` posts advance (customer prepays → entity borrows) Dr 5900 / Cr 2410 interest expense (contract liability accretes) or arrears (deferred → entity lends) Dr 1265 / Cr 4650 interest income (new COA 4650; contract asset accretes), idempotent; the §120 contract-liability rollforward reconciles to GL 2410/1265 and the RPO / backlog = Σ unrecognized allocated price banded by timing; tenant RLS. New module `revrec-disclosure` (`RevFinancingService`/`RevDisclosureService`/`Controller`) reusing the REV-19 engine + Wave 1 billing, table `rev_financing_schedules`; new COA 4650 + posting events `REVFIN.INCOME`/`REVFIN.EXPENSE`; BI report types `contract_liability_rollforward`/`rpo_backlog`. ToE `revrec-disclosure` (23); narrative PN-12 §5f rev 0.14; RCM 269.* · *v6.96: CRM-15 — B2B account health / churn + renewal-expansion pipeline (docs/44, new RCM control **CRM-08**, migration `0370`, RCM 269) — maps UAT-O2C-362..369 to CRM-08: an explainable per-account health score (engagement recency / open pipeline / open-escalated-SLA-breached `service_cases` / win-loss) banded healthy(≥70)/watch/at_risk(<40) drives a worst-first churn watchlist (`GET /api/crm/account-health`, band filter + counts); `crm_opportunities.deal_type` (new/renewal/expansion) forward pipeline + a renewal-GAP queue (won account, no open renewal); a schedulable daily snapshot (`crm_account_health_snapshots`, upsert per account/day; BI report `crm_account_health`) + trend history. New module `crm/account-health`; company AR/credit surfaced as context (not scored). Two new/ALTER schema (deal_type + snapshots table + service_cases index), RLS-scoped 0232. ToE `pipeline.ts` (12 CRM-15 checks, 108 total); narrative PN-18 §7.10 rev 0.13; manual 16 §16.4c rev 2.1.* · *v6.95: G4 `marketing_roi` schedulable BI report (docs/45; no new control — rides MKT-04) — maps UAT-LOY-080.* · *v6.94: COA-D2 — the account effective-date window and require_dimension flags are now ENFORCED by the postEntry guard (ACCOUNT_NOT_EFFECTIVE / REQUIRED_DIMENSION_MISSING — fail-closed only when declared, byte-identical otherwise), parent codes validated (PARENT_NOT_FOUND), both attributes maintainable on /chart-of-accounts (GL-27-staged). GL-21 activity extended (census unchanged 268). Adds UAT-GL-188 (basics.ts 414 + ledger-posting.test.ts). UAT 05 v0.53; manual 06 v0.18; PN-04 rev 2.32.* · *v6.93: MKT-13 — marketplace-to-member identity capture (G1 docs/45, new RCM control **MKT-13**, migration `0366`, RCM 268) — maps UAT-LOY-076..079: hash-only buyer-ref capture on aggregator ingest + member auto-attach, consent-gated QR/staff linking (`REF_ALREADY_LINKED` single-winner, `TENANT_MISMATCH`), consent recorded in the same tx.* · *v6.92: CRM-7 — B2B account/contact 360 depth (docs/44, new RCM control **CRM-07**, migration `0365`, RCM 267) — maps UAT-O2C-354..361 to CRM-07: account HIERARCHY (`crm_accounts.parent_account_id`; `PATCH …/accounts/:no/parent` rejects `SELF_PARENT`/`HIERARCHY_CYCLE`; `GET …/hierarchy` rolls the subtree's open weighted pipeline up), a per-deal buying COMMITTEE (`crm_opportunity_contacts` — role/influence, single `is_primary`, `CONTACT_ACCOUNT_MISMATCH`/`COMMITTEE_DUP` guards), and governed account PLANS (`crm_account_plans` draft → active → closed, owner+objective to activate, target categories validated vs `item_categories` → `UNKNOWN_CATEGORY`) + a product-WHITESPACE read. Three tables' worth of schema (2 new + 1 ALTER), all RLS-scoped (canonical 0232, tenant-leading indexes). ToE `pipeline.ts` (16 CRM-7 checks, 96 total); narrative PN-18 §7.9 rev 0.12; manual 16 §16.4b rev 2.0.* · *v6.91: COA-D1 — the GOV-01 pending-approvals center now aggregates the governance queues: GL-24 posting-rule overrides, GL-27 canonical CoA change requests, staged sensitive bulk imports (MDM-03) and staged sensitive master-field changes (MDM-01), with /approvals per-item + batch approve routed through each item's own maker-checker endpoint; /chart-of-accounts gains the GL-27 request-history register. No new control (GOV-01 evidence text updated). Adds UAT-GL-187 (compliance.ts 179). UAT 05 v0.52; manual 06 v0.17; PN-04 rev 2.31.* · *v6.90: GL-27 — canonical Chart-of-Accounts maker-checker (COA follow-up C, new RCM control **GL-27**, migration `0362`, RCM 266) — maps UAT-GL-186: canonical create/update/deactivate staged in coa_change_requests (fail-closed request-time validation; CHANGE_ALREADY_PENDING), creator self-approve → SOD_VIOLATION, distinct-Admin approve applies / reject leaves the chart untouched, and the SINGLE-ADMIN EXCEPTION (exactly one active Admin ⇒ AutoApplied immediately, trail kept). ToE compliance.ts 177 + basics.ts 411 + coa-change.test.ts (4). UAT 05 v0.51; manual 06 v0.16; PN-04 rev 2.30.* · *v6.89: REV-25 — Variable consideration + the constraint (Track D Wave 2, new RCM control **REV-25**, migration `0359`; TFRS 15 / IFRS 15 / ASC 606 §50-59) — maps TC-REV-25-01..06 to REV-25: estimate by expected value (Σ prob×amount) or most-likely amount; the constraint caps the recognizable price (only the `constrained_amount` ≤ gross drives the price, else `CONSTRAINT_EXCEEDS_ESTIMATE`); the estimate is a maker-checker management judgement (estimator ≠ approver → `SOD_SELF_APPROVAL`, approval mandatory before it drives revenue); re-estimate recomputes price (fixed base + constrained), re-allocates by SSP, rebuilds the unrecognized schedule and trues-up already-recognized revenue via a cumulative catch-up (Dr 2410/1265 ↔ Cr 4300), idempotent; up-front-billed catch-up releases 2410 (REV-19 back-compat); tenant RLS. New module `revrec-variable` (`RevVariableService`/`Controller`) reusing the REV-19 engine, table `rev_variable_estimates`; no new COA (2410/1265/4300 exist). ToE `revrec-variable` (25); narrative PN-12 §5d rev 0.12; RCM 264.* · *v6.89: REV-26 — Contract modifications (Track D Wave 3, new RCM control **REV-26**, migration `0360`; TFRS 15 / IFRS 15 / ASC 606 §18-21) — maps TC-REV-26-01..06 to REV-26: the classifier maps `distinct_flag`×`at_ssp_flag` to the three §18-21 outcomes — `separate_contract` (§20 distinct & at SSP → a NEW independent contract, original untouched), `prospective` (§21a distinct not at SSP → re-allocate the remaining transaction price over the remaining POs, NO catch-up), `cumulative_catchup` (§21b not distinct → catch-up on the satisfied portion, Dr 2410/1265 ↔ Cr 4300); the classification is the control and a maker-checker management judgement (recorder ≠ approver → `SOD_SELF_APPROVAL`, a Pending modification drives nothing, re-approval → `MODIFICATION_NOT_PENDING`); tenant RLS. New module `revrec-modifications` (`RevModificationService`/`Controller`) reusing the REV-19 engine, table `rev_contract_modifications`; no new COA (2410/1265/4300 exist). ToE `revrec-modifications` (28); narrative PN-12 §5e rev 0.13; RCM 265.* · *v6.88: REV-24 — Contract-asset / contract-liability split + independent billing schedule (Track D Wave 1, new RCM control **REV-24**, migration `0358`; TFRS 15 / IFRS 15 / ASC 606 §105-107) — maps TC-REV-24-01..06 to REV-24: recognize-ahead-of-billing builds a contract asset (Dr 1265 / Cr 4300), a maker defines invoice milestones and a DIFFERENT user bills them (`SOD_SELF_BILLING`) reclassing 1265 → 1100 AR + parking over-billing in 2410, the `/position` detective tie-out (contract_asset = max(0, Σrecognized − Σbilled)), over-contract billing blocked (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`), REV-19 up-front-billing back-compat (recognition still releases 2410), and tenant RLS. New module `revrec-billing` (`RevBillingService`/`Controller`), table `rev_billing_schedules` + `rev_contracts.billed_amount`; no new COA (1100/1265/2410/4300 exist). ToE `revrec-billing` (24); narrative PN-12 §5c rev 0.11; RCM 263.* · *v6.87: COA follow-up B — read-only account where-used report (GET /api/ledger/accounts/:code/where-used across the config masters) + the warn-list in the /chart-of-accounts deactivate dialog; deactivation stays balance-gated, no control change. Adds UAT-GL-185 (basics.ts 410). UAT 05 v0.47; manual 06 v0.15; PN-04 rev 2.29.* · *v6.86: TRE-05 — Cash pooling / in-house bank / intercompany-loan register (Track C Wave 4, FINAL; new RCM control **TRE-05**, migration `0357`) — maps UAT-TRE-051..055 to TRE-05: IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `SAME_PARTY`) posting the mirrored drawdown (Dr 1155 IC-Loan Receivable / Cr 1010 creditor + Dr 1010 / Cr 2155 IC-Loan Payable debtor), idempotent EIR interest (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor; `LOAN_NOT_APPROVED`), a notional interest allocation that MUST sum to zero (`ALLOCATION_NOT_ZERO`), and a physical sweep (Dr header / Cr member; `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL`). **Control core:** `consolidation.runConsolidation` extended so the intra-group 1155/2155 receivable/payable pair AND the 4700/5900 IC interest ELIMINATE per period (mirroring the trade-IC 1150/2150 pair) → group balances + finance cost/income net to zero (CON-03 balanced, REC-03 gated, `ic_loan_eliminations`). New module `treasury-pool` (`PoolService`/`Controller`), tables `cash_pools`/`cash_pool_members`/`ic_loans`/`ic_loan_accruals`; new COA 1155/2155 (CF_CLASSIFY 1155 investing / 2155 financing); ICLOAN.DRAWDOWN/INTEREST + POOL.SWEEP/INTEREST posting events; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-pool` (46); narrative PN-33 §7quater rev 0.4 + PN-11 rev 0.8; UAT 17 §9 v0.4.* · *v6.85: COA follow-up A (web-only) — the /chart-of-accounts EDIT dialog gains the cf_bucket + is_current fields (backfill an EXISTING balance-sheet account; อัตโนมัติ clears to the fallback chain) and the indirect cash-flow screen shows a warning banner listing `unclassified_accounts` with a link to the chart. Adds UAT-GL-184 (manual/e2e). UAT 05 v0.46; manual 06 v0.14; PN-04 rev 2.28. Header consolidated: the concurrently-stacked Status lines merged into one chain, duplicates dropped (TAX-11 retagged v6.72→v6.77 to match its own header; CLS-02 retagged v6.72→v6.74b — v6.72 is the historic docs/43 PR-6 note and v6.76..v6.82 were all claimed).* · *v6.84: TRE-04 — Hedge accounting register (Track C Wave 3, new RCM control **TRE-04**, migration `0356`; IFRS 9 / TFRS 9 · ASC 815) — maps UAT-TRE-041..047 to TRE-04: designation + documentation maker-checker (`SOD_SELF_APPROVAL`/SoD R23, `BAD_DOCUMENTATION`), effectiveness testing, and the two-part control gate (no hedge/OCI accounting until Approved AND the latest effectiveness test is effective=true) — CASH_FLOW effective portion → OCI reserve 3550 / ineffective → P&L 5450 (not-effective → OCI refused `HEDGE_NOT_EFFECTIVE`, whole change routes to P&L), accounting on an unapproved relationship → `HEDGE_NOT_DESIGNATED`, OCI→P&L reclassification (Dr 3550 / Cr 4000; `OCI_INSUFFICIENT`), FAIR_VALUE hedged-item basis adjustment (Cr 1200 / Dr 5450); derivative FV change Dr 1380 / Cr 2460. New module `treasury-hedge` (`HedgeService`/`Controller`), tables `hedge_relationships`/`hedge_derivatives`/`hedge_effectiveness_tests`/`hedge_oci_movements`; new COA 1380/2460/3550/5450; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-hedge` (38); narrative PN-33 §7ter rev 0.3; UAT 17 §8 v0.3.* · *v6.83: SVC-6 — Service Cloud: Knowledge Base + Case Deflection (new RCM control **SVC-06**, migration `0355`, RCM 260) — maps UAT-KB-001..010 to SVC-06: a per-tenant `kb_articles` register with a GOVERNED publish lifecycle (draft → published → archived), the **author ≠ publisher** maker-checker (`published_by ≠ author` → `SOD_SELF_PUBLISH`), draft-only edit (`ARTICLE_NOT_DRAFT`), published-only self-service `search` + view counter + helpful feedback, and the `kb_deflections` case-deflection log with a `deflection-stats` detective read (deflection rate + top articles). Two new RLS-scoped tables (canonical 0232, tenant-leading indexes); no new SoD rule. ToE `service.ts` (11 SVC-6 checks, 50 total); narrative PN-32 §10d rev 0.4; manual 16 §16.12 rev 1.9.* · *v6.82: TRE-03 — Investment & Securities register + the reusable OCI-reserve primitive (Track C Wave 2, new RCM control **TRE-03**, migration `0354`) — maps UAT-TRE-031..037 to TRE-03: buy maker-checker (`SOD_SELF_APPROVAL`/SoD R23) with classification-correct GL (Dr 1350\|1360\|1370 / Cr 1010), a maker-checker market-price register where MTM can only be driven by an Approved price (`NO_APPROVED_PRICE`), the FVOCI→OCI-reserve-3500 vs FVTPL→P&L-5430 remeasurement split (amortized cost `MTM_NOT_APPLICABLE`), idempotent EIR interest income (Dr 1350 / Cr 4700), and ECL impairment (Dr 5440 / Cr 1355). New module `treasury-invest` (`InvestmentService`/`Controller`), tables `investments`/`investment_prices`/`investment_valuations`; new COA 1350/1355/1360/1370/3500/4700/5430/5440; reuses the Wave-1 `treasury`/`treasury_approve` duties. ToE `treasury-invest` (40); narrative PN-33 §7bis rev 0.2; UAT 17 §7 v0.2.* · *v6.81: TRE-01/02 — Debt & Borrowings register + EIR amortized-cost engine (Track C Wave 1, new RCM controls **TRE-01**/**TRE-02**, migration `0353`) — maps UAT-TRE-011..016 to TRE-01 (facility create→approve maker-checker `SOD_SELF_APPROVAL`/SoD R23, drawdown Dr 1010/Cr 2500|2550 + `LIMIT_EXCEEDED`, idempotent EIR accrual Dr 5900/Cr 2450, repayment, maturity ladder) and UAT-TRE-021..022 to TRE-02 (covenant tracking + breach detection + worklist, tenant RLS). New module `treasury-debt` (`DebtService`/`Controller`), tables `debt_facilities`/`debt_drawdowns`/`debt_covenants`/`debt_covenant_tests`; new duties `treasury`/`treasury_approve` + roles TreasuryAnalyst/TreasuryManager. ToE `treasury-debt` (36); narrative PN-33; UAT 17.* · *v6.80: SVC-5 — Service Cloud: per-case Entitlements / SLA (new RCM control **SVC-05**, migration `0352`, RCM 256) — maps UAT-CASE-016..022 to SVC-05: the SVC-4 `service_cases` object gains an `sla_tier` (Standard/Bronze/Silver/Gold/Platinum) whose first-response + resolution targets are COMPUTED into `first_response_due_at`/`resolution_due_at` at open and on `POST …/cases/:id/entitlement` (recomputed off the original open time, breach flags re-evaluated); the first outbound reply stamps `first_responded_at` + `response_breached`, resolve stamps `resolution_breached`; and `GET …/cases/sla/breaches` is the detective worklist of open past-due cases tagged `breach_kind` response/resolution/both. Additive columns on the RLS-scoped `service_cases` (no new table/SoD rule); email-opened cases default to Standard. ToE `service.ts` (7 SVC-5 checks, 39 total); narrative PN-32 §10c rev 0.3; manual 16 §16.11 rev 1.8.* · *v6.79: TAX-12 — DTA valuation allowance + Uncertain Tax Positions (FIN 48) register (new RCM control **TAX-12**, migration `0351`) — maps UAT-TAX-051..056 to TAX-12: MLTN valuation-allowance run→post to 5950/1700 (maker-checker `SELF_POST`, gross DTA sourced from `deferred_tax_runs`), and the FIN 48 uncertain-tax-positions memo register (reserve = gross − recognized, maker-checker create→settle `SELF_SETTLE`, tenant RLS). New tables `dta_valuation_allowances`/`uncertain_tax_positions`; `TaxUtpService`/`Controller` in the tax module. ToE `tax-utp` (17); narrative PN-04 §3.2 rev 2.17; UAT 06 v0.11; manual 07.* · *v6.78: SVC-4 — Service Cloud: Support Cases + Email-to-Case (new RCM control **SVC-04**, migration `0350`, RCM 254) — maps UAT-CASE-001..015 to SVC-04: the `service_cases` object with a governed status lifecycle (new→open→pending→resolved→closed, reopen; priority P1–P4; assignee; optional CRM contact link) + the append-only `case_email_messages` trail (Message-ID dedupe), and a public HMAC-authenticated Email-to-Case webhook (`POST /api/service/email-to-case/inbound/:tenantCode`) that threads a customer reply onto its case (per-case thread token → sender's open case) or opens a NEW case so no inbound email is dropped (completeness), reopens a resolved/closed case on reply, and rejects `UNKNOWN_TENANT`/`BAD_INBOUND_SECRET`/`WEBHOOK_STALE`; illegal transitions → `CASE_NOT_ACTIVE`/`CASE_ALREADY_CLOSED`/`CASE_NOT_CLOSED`; RLS-isolated. No new SoD rule. ToE `service.ts` (13 SVC-4 checks); narrative PN-32 §10b rev 0.2; manual 16 §16.10 rev 1.7.* · *v6.77: TAX-11 — Current income-tax provision + ETR reconciliation (ASC 740 / IAS 12, new RCM control **TAX-11**, migration `0349`) — maps UAT-TAX-060..062 to TAX-11: pretax book income (from the income statement, EXCLUDING `DEFTAX`/`CITPROV` income-tax postings) → permanent M-1 adjustments + a temporary adjustment REUSED from the TAX-06 deferred-tax run's period delta → taxable income → current CIT @ statutory rate; the ETR schedule ties statutory→effective (permanent × rate, rate-change, valuation-allowance, prior deferred); maker-checker post Dr 5960 Corporate Income Tax Expense / Cr 2110 CIT Payable (poster ≠ runner → `SOD_SELF_APPROVAL`, `ALREADY_POSTED` on re-post) with RLS tenant isolation. New COA 5960/2110 (+ CF_CLASSIFY 2110 operating); posting-event `TAX.PROVISION`; new `income_tax_provisions` table (canonical 0232 RLS, tenant-leading index); web `/tax/provision` (server-shell + client island). RCM 250→251 (census 248/3/0). ToE `tax-provision.ts` (24); narrative PN-06 §9b rev 0.27; manual 07.* · *v6.76: CLS-01 / GL-25 — flux / variance analysis with forced explanation + independent sign-off (new RCM control **GL-25**, migration `0348`, RCM 251) — maps UAT-GL-179..183 to GL-25: generate a period movement analysis from `gl_period_balances` (P&L / BS; comparative prior_period / prior_year / vs approved-budget) with configurable abs+pct thresholds; a threshold-breaching line requires a written explanation before sign-off (`UNEXPLAINED_LINES`), a non-breached line can't be explained (`LINE_NOT_BREACHED`), the reviewer must differ from the preparer (`SOD_SELF_APPROVAL`) and certification locks it (`ALREADY_CERTIFIED`); prior_year/budget comparatives (`BUDGET_PL_ONLY` on BS) + RLS. Read-only — posts nothing to the GL; advisory `flux_review` close-checklist step + schedulable `flux_analysis` BI report. New tables `flux_analyses`/`flux_lines` (canonical 0232 RLS, tenant-leading indexes); `FluxService`/`Controller`. ToE `flux-analysis.ts` (18); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.75: docs/43 PR-9 (FINAL — SERIES COMPLETE) — /setup/posting-rules becomes the four-tab registry workspace (event registry grid with default/tier/override status over `GET …/posting-rules/registry`, cross-event GL-24 pending-approval queue, append-only audit viewer over `GET …/posting-rules/audit`, deactivate on approved rules); web-only, no control/API/schema change. Adds UAT-GL-174 (manual/e2e). UAT 05 v0.44; PN-04 rev 2.25; docs/43 rev 0.11 (PR-9 DELIVERED — all nine docs/43 PRs landed).* · *v6.74b: CLS-02 / GL-26 — governed disclosure / close-package checklist (new RCM control **GL-26**, migration `0347`, RCM 251) — maps UAT-GL-175..174 to GL-26: a per-period disclosure binder auto-seeds the standard TFRS/IFRS + SEC disclosure items (each with a standard ref, owner, Open/Complete/NA status + support-doc evidence pinned to `doc_attachments` docType DISC); review is a maker-checker gate blocked while any item is Open (`ITEMS_INCOMPLETE`) and reviewer ≠ preparer (`SOD_SELF_APPROVAL`) → Draft→Reviewed→Issued; an advisory `disclosure_review` step cross-links it into the GL-15/16 close run; tenant RLS. New tables `disclosure_checklists`/`disclosure_items` (canonical 0232 RLS, tenant-leading indexes); `DisclosureService`/`Controller` (`/api/close/disclosure`); web `/close/disclosure`. ToE `disclosure-checklist.ts` (13); PN-04 rev 2.24; UAT 05 v0.41; manual 06.* · *v6.74: docs/43 PR-8 — accounts.cf_bucket/is_current self-classification (0346, SCF + metrics fallback chains, COA dialog) + governed bulk IO (canonical accounts Admin-only staged; posting_rules per-row through GL-24 PendingApproval). Adds UAT-GL-173 (`basics.ts` 407, `ext.ts` 299). UAT 05 v0.43; PN-04 rev 2.24; docs/43 rev 0.10 (PR-8 DELIVERED).* · *v6.73: docs/43 PR-7 — tie-out widening: PAY-02 schedule/remit + PND3/53 + ภ.ธ.40 read the widened account sets {canonical ∪ approved override}; the payable roles flip Tier B→A; VAT sets/2410 stay widen; REC-04 pinned permanently. Adds UAT-GL-172 (`payroll.ts` 46). UAT 05 v0.42; PN-05 rev 0.7; PN-06 rev 0.27; docs/43 rev 0.9 (PR-7 DELIVERED).* · *v6.72: docs/43 PR-6 — POS hot-path override wiring (dine-in/portal/channel/CPQ/house-charge; batched postingOverridesMany + per-tenant cache keeps the hot path at zero extra steady-state queries; offline/hub replays share the builders). Adds UAT-GL-171 (`restaurant.ts` 186). UAT 05 v0.41; PN-01 rev 0.32; PN-20 rev 3.10; docs/43 rev 0.8 (PR-6 DELIVERED).* · *v6.71: docs/43 PR-5 — manufacturing & inventory override wiring (WO applied/variance, QA scrap incl. NCR, PPV incl. STDREV, costed COGS, inventory adjust, waste; the posting-rule layer sits between GL-21 determination and the control literal in the inventory account funnel). Adds UAT-GL-170 (`manufacturing.ts` 14). UAT 05 v0.40; PN-15 rev 0.9; PN-03 rev 1.5; docs/43 rev 0.7 (PR-5 DELIVERED).* · *v6.66: INV-3 — Governed cycle-count program with ABC classification + blind counts (new RCM control **INV-17**, migration `0341`) — maps UAT-INV-121..110 to INV-17: ABC recompute tiers items by annual consumption value (Pareto A/B/C) + seeds per-class count cadence (A=30/B=90/C=180), a cadence-driven due worklist, a BLIND count generation (book qty captured server-side, never returned to the counter), count entry then posting via the EXISTING INV-04 counter≠poster stocktake path (`SOD_SELF_APPROVAL`) with the valued GL adjustment (INV-06), and the freshly-counted item dropping off the worklist. New tables `item_abc_class`/`cycle_count_plans`/`cycle_count_tasks` (canonical 0232 RLS, tenant-leading indexes); `CycleCountService`/`Controller` in the existing stock-ops module. ToE `cycle-counts.ts` (15); narrative PN-03 §7 (13b) rev 1.4; manual 04.* · *v6.65: docs/43 PR-3 — assets & leases override wiring + the Q2 asset_categories account grain (category accounts drive acquisition/depreciation under posting_determination, GL-21 fail-closed at category save; dispose/revalue/lease-modify/lessor/prepaid legs consult GL-24-governed posting-rules; PAYROLL.REMIT stays literal until PR-7). Adds UAT-GL-168 (`basics.ts` 405). UAT 05 v0.38; PN-09 rev 1.7; PN-04 rev 2.23; docs/43 rev 0.5 (PR-3 DELIVERED).* · *v6.64: GRC-3 — Sensitive master-data single-record maker-checker (new RCM control **MDM-01**, migration `0340`, SoD R22) — maps UAT-P2P-144..147 to MDM-01: a sensitive vendor field (bank account / bank name / account-holder name / credit limit / payment terms) stages a `masterdata_change_requests` row (before/after encrypted at rest) and applies to the master only on a DISTINCT approval (`SOD_SELF_APPROVAL` on self-approval), reject/pending leave the master untouched, `payment_terms` removed from the direct profile edit, a non-sensitive field refused (`FIELD_NOT_SENSITIVE`), and RLS tenant isolation (`NOT_PENDING`). ToE `masterdata-change.ts` (17); narrative PN-02 rev 3.34; manual 03.* · *v6.63: GRC-4 — Control-Exception Disposition + KCI dashboard (new RCM control **GOV-02**, migration `0339`) — maps UAT-P2P-141..143 to GOV-02: the `/api/controls` monitor gains split-PO / weekend-manual-JE / dormant-vendor-reactivation detectors (each tagged with its `rcm_ref`, idempotent by fingerprint), a managed disposition workflow (owner + due_date + root_cause, open → investigating → remediated | accepted | false_positive, `remediated_by/at` on close), and a KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family), tenant-scoped by RLS. ToE `control-exceptions.ts` (24); narrative PN-02 rev 3.34; manual 10 §4d-bis.* · *v6.62: GRC-5 — SoD-Conflict Register + Compensating-Control governance (new RCM control **ITGC-AC-22**, migration `0338`) — maps UAT-ADM-148..152 to ITGC-AC-22 (+R07): the standing conflicts-by-rule dashboard (`GET /api/admin/sod/conflicts`), the accept-with-compensating-control gate (`422 COMPENSATING_CONTROL_REQUIRED` / `422 NO_SUCH_CONFLICT`), the acceptor-recorded acceptance + dashboard cross-reference, the expired/overdue detective worklist (`GET …/dispositions/expired`) + periodic re-review (`POST …/:id/review`), and the permission-gate + RLS isolation. Detective + accepted-risk governance ALONGSIDE the existing preventive SoD block (ITGC-AC-09, unchanged). RCM +1 (240→241). ToE `sod-register.ts` (11); narrative PN-08 rev 2.3; manual 11.* · *v6.61: GRC-2 — line-item Access Recertification Campaign with closed-loop revocation (new RCM control **ITGC-AC-21**, migration `0337`) — maps UAT-ADM-142..147 to ITGC-AC-21 (+ITGC-AC-03 RLS): open campaign snapshots the population as `pending`, certify blocked while any line pending (`ITEMS_PENDING`), keep/revoke records the reviewer, on certify a `revoke` line's `user_permissions` grants are ACTUALLY removed (`actioned:true`, the closed loop), a certified campaign is frozen (`CAMPAIGN_CERTIFIED`), and RLS isolates a campaign to its tenant (`CAMPAIGN_NOT_FOUND`). ToE `access-recert.ts` (17); narrative PN-08 §7 step 7 + rows 7/7a.* · *v6.60: docs/43 PR-2 — finance/treasury override wiring: 23 posting-event registry entries now consult the tenant posting-rule (Approved-only, GL-24-governed) before their registry default at the real posting sites (finance write-off/advance/reverse-charge/AP-WHT, AP payment-run discount, bank interest/fee, petty cash, FX unrealized+realized, returns, tax-invoice CN/DN, membership/loyalty, till variance+paid-in-out incl. the hub replay, deposits, card surcharge); pinned control legs stay literal; no new control/migration. Adds UAT-GL-167 (`basics.ts` 400, 8 PR-2 checks) and RESTORES the traceability rows UAT-GL-159..164 that the PR #676 merge race dropped (the 05-uat rows were also renumbered back to 159..164, resolving the duplicate ids vs SVC-3's 151..157; the PR-2 case itself moved 165→166 when #684's FX case took 165). UAT 05 v0.37; PN-04 rev 2.22; docs/43 rev 0.4 (PR-2 DELIVERED).* · *v6.59: Per-period intercompany reconciliation sign-off (REC-03) surfaced on the new `/consolidation → กระทบยอดระหว่างกัน` tab (UI over already-harnessed backend) — UAT-GL-166 maps prepare (`api/ic-reconciliation/groups/:id/prepare`), maker-checker approve (`…/approve`, `SOD_VIOLATION`/`IC_NOT_ELIMINATED`), and reject-with-reason (`…/reject`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (REC-03 checks); PN-11 rev 0.7.* · *v6.58: QMS-4 — Supplier Corrective Action Request (SCAR / 8D) closure control (new RCM control **QC-04**, migration `0335`) — maps UAT-P2P-137..140 to QC-04 (+R21): raise from a real gr_claim (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`), closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`), raiser self-close → `SOD_SELF_APPROVAL`, a distinct `quality_approve` reviewer closes effective (requalification gate), overdue detective read, reject `REASON_REQUIRED`, and RLS tenant isolation. ToE `supplier-scar.ts` (14); narrative PN-02 rev 3.33.* · *v6.57: QMS-3 — Certificate of Analysis capture + out-of-spec release approval (new RCM control **QC-03**, migration `0334`) — maps UAT-INV-098..105 to QC-03 (+R21 / ITGC-AC-18): quality-spec range catalogue, in-spec CoA → evaluate pass → recorder release, release-before-evaluate block (`COA_NOT_EVALUATED`), out-of-spec fail → recorder self-release block (`SOD_SELF_APPROVAL`), reason-required (`DEVIATION_REASON_REQUIRED`) + distinct-approver deviation release, the out-of-spec deviation register, and RLS tenant isolation. ToE `quality-coa.ts` (16); narrative PN-03 rev 1.3; manual 04.* · *v6.56: QMS-2 — CAPA (Corrective & Preventive Action) lifecycle with effectiveness sign-off (new RCM control **QC-02**, migration `0333`) — maps UAT-QMS-CAPA-001..008 to QC-02 (+R21 / ITGC-AC-03): open CAPA + child actions (open→in_progress, generic NCR/gr_claim source link), submit needs a plan (NO_ACTIONS), close blocked while actions pending (ACTIONS_INCOMPLETE), self-verify blocked (SOD_SELF_APPROVAL), distinct verifier effective→closed / ineffective→reopened, reject (REASON_REQUIRED), overdue detective + RLS isolation. RCM +1 (QC-02). ToE `quality-capa.ts` (20); PN-15 rev 0.6; manual 04 §CAPA.* · *v6.55: QMS-1 — Non-Conformance (NCR) register with maker-checker disposition (new RCM control **QC-01**, migration `0332`) — maps UAT-QC-001..008 to QC-01 (+SoD R21): defect-code lookup, raise NCR (open) / promote a failed inspection (pending), propose scrap → pending_disposition, raiser self-disposition → `SOD_SELF_APPROVAL`, a DISTINCT `quality_approve` user dispositions scrap → Dr 5810 / Cr 1250-1200-1210 write-off (entry_no, TB balanced) / reject → open, and RLS tenant isolation of the register. ToE `quality-ncr.ts` (11); narrative PN-15 rev 0.6; manual 20.* · *v6.54: Period-end FX revaluation (maker-checker run→post, GL-18) surfaced on the new `/fx → ปรับปรุงสิ้นงวด` tab (UI over already-harnessed backend) — UAT-GL-165 maps the run (`api/ledger/fx-reval/run`, Open), the per-doc detail drill-down (`…/:id`), and the post (`…/:id/post`, `SELF_POST`/`ALREADY_POSTED`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (TC-GL-18-01/02); PN-04 rev 2.21.* · *v6.53: docs/43 PR-1 — GL-24 posting-rule change governance (new control, migration `0331`, RCM 233): the posting-event REGISTRY (posting-events.ts — 74 events / 121 roles with side + real default + override tier free|widen|pinned, boot-asserted ⊆ canonical CoA) becomes the single source of truth; rule writes are validated fail-closed (UAT-GL-162) and land PendingApproval with a distinct-approver SoD + append-only audit (UAT-GL-163/164); the resolver + per-tenant bust-on-approve cache consume Approved rules only; /setup/posting-rules gains status badges + approve/reject. Also restores the docs/42 CoA-usability cases deferred from PR #665 (UAT-GL-159..161) and re-points UAT-PAY-047 at the maker-checked flow. ToE `compliance.ts` 173 (9 GL-24 checks) + `payroll.ts` 42; PN-04 rev 2.19, PN-05 rev 0.6, PN-09 rev 1.6, manual 06/99, UAT 05 v0.31 / 07 v0.9; docs/43 rev 0.3 (PR-1 DELIVERED).* · *v6.52: Statutory FS pack (FIN-4) surfaced on the new `/financial-statements → งบตามกฎหมาย & DBD` tab (UI over already-harnessed backend) — UAT-FIN4-08 maps the statement of changes in equity (`api/reports/fs/changes-in-equity`), the DBD e-Filing S-form XBRL export (`…/dbd-export`, downloadable), and the definition-driven statement/notes viewer (`…/definitions`, `…/render/:code`, `…/notes/:code`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (FIN-4 checks); PN-04 rev 2.19.* · *v6.51: SVC-1 — CPQ discount-approval & margin-floor control (new RCM control **CPQ-01**, migration `0328`) — maps UAT-O2C-349..353 to CPQ-01 (+R20 / AUD-ARC-01): within-floor send, margin & discount-ceiling breach → PendingApproval + accept blocked, author self-approve/reject → `SOD_SELF_APPROVAL`, distinct approver → Sent→Accepted+AR / reject→Draft, and RLS tenant isolation of quotes + `quote_approvals`. ToE `cpq-approval.ts` (17); narrative PN-18 rev 0.11.* · *v6.50: Lessor-side lease accounting (IFRS-16 lessor, LSE-02) surfaced on the new `/leases → ผู้ให้เช่า` tab (UI over already-harnessed backend) — UAT-GL-158 maps preview-classification (`api/lessor-leases/classify`), create-pending, maker-checker approve (`…/:leaseNo/approve`, `SOD_SELF_APPROVAL`), periodic run, and the GL-1610 net-investment reconciliation (`…/receivable-reconciliation`). No control/GL/RCM change; endpoints unchanged; ToE `basics.ts` (lessor checks); PN-04 rev 2.17.* · *v6.49: SVC-3 Service Contract Renewal & Expiry management — UAT-GL-151..157 map new control **SVC-02** (migration 0331): within-threshold renewal auto-approves + creates the successor contract; over-threshold / auto-renew-price-rise → pending maker-checker (SOD_SELF_APPROVAL); distinct approver creates the successor + marks the old contract renewed; reject leaves it (declined); expiry worklist + RLS isolation. RCM +1 (SVC-02). ToE `contract-renewal.ts` (19); PN-12 rev 0.10; manual 16 §16.8 · *v6.48: SVC-2 Warranty & Entitlement registry (net-new after-sales module `/service/warranty`) — new control **SVC-01** (coverage-authorization maker-checker). UAT-SVC-001..008 map term catalogue, installed-base registration (computed `warranty_end`), in-coverage auto-free vs out-of-coverage park-pending, the requester≠authorizer gate (`SOD_SELF_APPROVAL`), and the expiring / coverage-exceptions detective reads. Migration 0329; ToE `warranty.ts` (20); PN-32.* · *v6.47: Consolidation eliminations, IFRS-8 segments/report (CON-04) + post-elimination consolidated cash flow (CON-05) surfaced on `/consolidation → ตัดรายการ & ส่วนงาน` + the run cash-flow card (UI over already-harnessed backend) — UAT-GL-150 maps the elimination-rule form (`api/consolidation/rules`), segment definitions + segment report (`…/segments`, `…/segment-report`), and the indirect consolidated SCF (`…/runs/:id/cash-flow`). No control/GL/RCM change; endpoints unchanged; ToE `consolidation.ts` (42); PN-11 rev 0.6.* · *v6.46: MRP plan-to-PR + lot-sizing + rough-cut capacity (RCCP) surfaced on `/production → MRP` (UI over already-harnessed engine) — UAT-INV-097 maps the **Plan → PR** button (consolidated PR from the planned buy), the **ใช้ขนาดล็อต** toggle (lot-policy column), and the **RCCP** work-centre load/overload table. No control/GL/RCM change; endpoints (`api/mrp/run|plan-to-pr|capacity`) unchanged; ToE `mrp.ts` (16); PN-15 §5a rev 0.5.* · *v6.45: ATP / order-promising surfaced on `/costing` (UI over already-harnessed INV-09 engine) — UAT-INV-096 maps the `พร้อมส่งมอบ & จองสินค้า` tab: check available-to-promise, reserve against a doc, and a reservation register with release/fulfil. No control/GL/RCM change; endpoints (`api/costing/atp*`, `…/allocate`, `…/allocations/*`) unchanged; `INSUFFICIENT_ATP` guard unchanged; ToE `costing.ts` (19); PN-03 rev 1.2; manual 04 §11.* · *v6.44: TFRS 15 five-step contract engine surfaced on `/revenue` (UI over already-harnessed backend) — UAT-GL-149 maps the `สัญญา TFRS 15` tab: create contract + performance obligations, per-contract allocate-by-SSP / build-schedule / activate, then recognise on the deferred tab. No control/GL/RCM change; endpoints (`api/revenue/contracts/*`) unchanged; ToE `revrec.ts` (34); PN-12 rev 0.9; manual 06-general-ledger.md.* · *v6.43: Service workspace usability (`/service`) — subscription lifecycle + SLA resolve surfaced (UI over already-harnessed backend). UAT-LOY-074 maps the SLA-event **ปิดเคส/Resolve** (breach computed vs the tier's due times), UAT-LOY-075 the subscription lifecycle (create → run billing → pause skips → resume bills → cancel-terminal `SUB_CANCELLED`, invoices drill-down + mark-paid). New `POST /api/service/subscriptions/:id/resume` + cancel-terminal guard; ToE `service.ts` (18, was 15); manual 16 §16.7. No control/RCM change.* · *v6.42: Validated money-input dialog (UI hardening) — UAT-P2P-136 maps the shared `NumberPromptDialog` that replaces `window.prompt()`+`Number()` for petty-cash replenish/settle, subcontract valuation %, and real-estate deposit/contract figures so a non-numeric entry can never POST `NaN`; web-only, no control change (EXP-08 flow unchanged).* · *v6.41: Consolidation setup + post-to-GL wired into the `/consolidation` screen (UI-only surfacing of existing `api/consolidation/*`) — UAT-GL-148 maps create-group + add/remove-entity + the CON-03 maker-checker post-to-GL button (SELF_POST/ALREADY_POSTED unchanged); ToE `consolidation.ts` (42), PN-11 rev 0.5, manual 06-general-ledger.md. No control/RCM change.* · *v6.40: Governance / entity-level-controls web screen (`/governance`, UI-only surfacing of existing `api/governance/*`) — UAT-ADM-139..141 map the readiness overview + ethics/hotline/DoA/fraud-risk/oversight tabs to ELC-01..05; ToE `governance.ts`, ELC operating manual v1.1, manual 11-administration §15. No control/RCM change.* · *v6.39: Menu 86-toggle/edit + WMS confirm-pick — UAT-O2C-347/348 map the menu-screen `86 ปิดขาย/เปิดขาย` availability toggle + `แก้ไข` in-place edit (REST-08 / operational), and UAT-INV-095 the new wave→**pick**→pack UI (getPick + `?status=Open,Picking`); ToE `restaurant.ts`/`wms.ts` (34), narratives PN-20 rev 3.18 / PN-03 rev 0.8, manuals 01 v0.53 / 04.* · *v6.38: Hotfix — thread `tenant_id` through all six `stock_movements` posting sites (putaway/pick/GR/scan/replenish/WO), completing 0316's write path so a non-bypass tenant user no longer 500s under the fail-closed RLS; UAT-INV-094, narrative PN-03 rev 9.100, ToE `wms.ts` (30, restored to green).* · *v6.37: BRANCH-07 hub stocktake replay preserves SoD R11 (docs/41, migration `0318`, RCM 223) — UAT-O2C-346 maps the two-human-signed replay + self-posted-block to PN-24/PN-03 and control BRANCH-07.* · *v6.36: Tip-adjust-after-auth (POS-10, new control, migration `0317`, RCM 222) — UAT-O2C-343..345 map the authorize→adjust-tip-pre-capture→capture-bill+tip flow, the pre-capture-only + 25%-ceiling guards (`TIP_ADJUST_CLOSED` / `TIP_OVER_LIMIT`), the idempotent Dr 1000 / Cr 2300 tip post feeding the tip pool (TIP-01), and the append-only `pos_tip_adjustments` audit trail to POS-10; reuses the gateway auth/capture surface (Mock/Stripe/Opn — no new processor); ToE `tips.ts` 11→19; narrative PN-20 rev 3.17.* · *v6.35: seat-level ordering (POS-9, operational, no new control, migration `0312`) — UAT-O2C-340..342 map order/fire/reassign per seat and **split-by-seat** (a third split method `by_seat`; check number = seat; unseated line → `SEAT_UNASSIGNED`) to the seat-ordering feature + REST-06 (split-bill exact-coverage); ToE `restaurant.ts` (+5) + `splitbill.ts` (+5) + mobile e2e `dine-in-seat.mobile.spec.ts`; narrative PN-20 rev 3.17.* · *v6.34: FIN-9 dynamic / early-payment discounting on the AP payment run (docs/41, new control **EXP-14**, migration `0309`, RCM 217) — UAT-P2P-134/135 map the maker-checked `ap_discount_terms` policy (creditors drafts ≠ approvals/gl_close activates, `SOD_VIOLATION`; `INVALID_DISCOUNT_PCT`), the sliding-scale projection (2%×฿1000=฿20, net ฿980, capped so cash-out ≥ 0) and the execute capture (idempotent Dr 1000 / Cr 4600 Early-Payment Discount Income, run `total_discount`, TB balanced) + policy-off = ฿0 to EXP-14; ToE `compliance.ts` (+4 checks); new GL account 4600; narratives PN-02 rev 3.32 / PN-07 rev 1.9.* · *v6.33: AR/AP netting & contra settlement (docs/41 FIN-8, new control **REV-23**, migration `0309`, RCM 217) — UAT-O2C-333..339 map the counterparty netting agreement + threshold, the maker-checker contra settlement (propose no-GL → distinct-approver Dr 2000 / Cr 1100 clearing both sub-ledgers oldest-first, residual open), and the guards (`NETTING_NOT_AGREED`/`NETTING_DISABLED`/`NETTING_EXCEEDS_THRESHOLD`/`NOTHING_TO_NET`/`REASON_REQUIRED`/`SOD_VIOLATION`) to REV-23; ToE `basics.ts` (+13); narrative PN-01 rev 0.31.* · *v6.32: CRM-6 inbound email capture → CRM (2-way comms) (docs/41 CRM-4 note — the deferred inbound reply-capture side, migration `0309`, NO new RCM control, RCM still 216) — UAT-O2C-333..337 map the outbound reply-threading token, the signed inbound token/contact-email match → inbound timeline activity, Message-ID dedupe, the unmatched→review-queue + manual-link flow, and the bad-HMAC 401 rejection; mirrors the AP `email-capture` rail + webhook-auth L-2; ToE `pipeline.ts` 69→80; narrative PN-18 rev 0.10; manual 16-crm-workspace.md rev 1.3.* · *v6.31: FIN-10 lessor-side lease accounting (docs/41, new control **LSE-02**, migration `0309`, RCM 217) — IFRS 16 / TFRS 16 lessor: classify finance vs operating (boundary flip at 75% of economic life / 90% of fair value); classification + commencement maker-checker (creator self-approval → SOD_SELF_APPROVAL); finance lease derecognises the asset (Cr 1500) + books a net investment at PV (Dr 1610) with interest income (Cr 4600); operating lease keeps the asset with straight-line rental income (Cr 4610) + continued depreciation; net-investment reconciliation ties GL 1610 to the finance-lease receivable schedule; new COA 1610/4600/4610. New rows UAT-GL-141..147 map to LSE-02 (`basics.ts`, 12 lessor checks); narrative PN-04 rev 2.15.* · *v6.30: CRM-4 sales automation (docs/41, new detective control **REV-22**, migration `0308`, RCM 215) — UAT-O2C-328..332 map lead scoring (A–D), pipeline events into the automation engine, follow-up SLA-breach detection + round-robin + daily digest, and deal comms (merge fields → activity) to REV-22; ToE `pipeline.ts` 43→57; narrative PN-18 rev 0.9.* · *v6.29: FIN-7b GL allocation engine (new control **GL-23**, migration `0308`, RCM 212) — an allocation cycle distributes a source pool to targets by ratio / driver / statistical key; the scheduled `gl_allocation_run` posts ONE balanced Draft JE (Cr pool / Dr targets, remainder to last target) via GL-05 maker-checker, idempotent per period (`ALC-<id>-<date>`); zero-basis → NO_BASIS. New rows UAT-GL-137..140 map to GL-23 (`basics.ts`); narrative PN-04 rev 2.16.* · *v6.28: PromptPay store-level auto-reconciliation (POS-8, new control **POS-08**, migration `0301`, RCM 212) — UAT-O2C-317/318 map the inflow→tender match + unmatched→till-exception (mirrors the till-variance surface) + late-inflow auto-resolve + manager clear + `pos_sell`-denied to POS-08; reuses the shared bank auto-match engine (`modules/bank/match-engine.ts`); ToE `cash-banking.ts`.* · *v6.27: waste-ledger closing-the-loop (POS-5a, new control **INV-15**, migration `0305`, RCM 212) — reason × disposition taxonomy + `by_disposition`, void-fired-item capture (`POST /api/inventory/waste/void-fire` explodes a voided fired dish's recipe → per-ingredient waste + one aggregated Dr 5810/Cr 1200 JE), and theoretical-vs-actual usage variance (`GET /api/inventory/waste/variance`, recipe-COGS `Consume` vs actual depletion); new rows UAT-INV-090..093 (`waste.ts`); narrative PN-03 rev 1.1.* · *v6.26: offline dine-in + installable PWA register (POS-6, control **BRANCH-03** widened, migration `0305`) — the offline outbox now covers dine-in open/add/fire via `POST /api/restaurant/offline-sync/dinein` (idempotent on `(tenant, client_uuid)`, linked by `order_uuid`; settlement stays online); new rows UAT-O2C-320..322 → BRANCH-03; ToE `restaurant-offline.ts` (14); PN-24 rev 0.9.* · *v6.25: FIN-5 consolidation dual-rate FX translation + CTA/OCI reserve + consolidated statement of cash flows (new control **CON-05**, IAS 21/7, migration `0303`, RCM 212) — a foreign member entity's P&L translates at the period **average** rate and its balance sheet at the **closing** rate, the difference parked in a **CTA / OCI translation-reserve** line (account 3400, `line_type=FX_CTA`); each run line records `fx_rate`/`rate_type`; `GET /api/consolidation/runs/:id/cash-flow` gives a post-elimination consolidated SCF that reconciles to Δ cash. New rows TC-CON-05-01/02 (`consolidation.ts`).* · *v6.24: CRM-5 analytics that answer "why" (Module-Depth Uplift Wave 4; NO migration, NO new control) — UAT-O2C-317..320 map the funnel-conversion + time-in-stage velocity (`crm_funnel`), source-ROI (`crm_source_roi`) and forecast-categories + quota + activity-leaderboard (`crm_forecast`) read-only analytics, plus the now date-bounded `crm_win_loss`, to REV-17 (operational management reporting); ToE `pipeline.ts` 43→49; PN-18 rev 0.7.* · *v6.23: FIN-4 statutory FS pack — the audit pack riding the primary statements: a config-driven financial-report builder (`fs_report_definitions`, migration `0299`) with buyer-defined subtotals/row-groups + a comparative (YoY/budget) column, a statement of changes in equity (roll-forward that ties to the balance sheet), a note-schedule builder (per-note account mapping + comparative + policy text), and a DBD e-Filing export (Thai งบการเงิน — XBRL / S-form). No new RCM control (read-only presentation over the already-controlled GL via `LedgerService.perAccountNet`). New rows UAT-FIN4-01..07; ToE `basics.ts` (344); narrative PN-04 rev 2.0; manual 06-general-ledger.md.* · *v6.22: KDS depth (POS-4 — operational/reporting, no GL, no new control): prep-time SLA aging bands, bump/recall with per-line `recall_count` (migration `0302`), expo/order-ready pass (`GET /api/restaurant/kds/expo`), station-load view (`GET /api/restaurant/kds/load`). New rows UAT-O2C-316..318 (`restaurant.ts`); narrative PN-20 rev 3.14; manual 01-sales-and-pos.md §1.* · *v6.21: CRM-3 — Customer 360 that joins the business (docs/42 module-depth uplift Wave 4; NO migration, NO new RCM control) — a read-only aggregator `GET /api/crm/customer-360/:accountNo` folds the company AR/credit position (`CollectionsService.creditStatus` + `FinanceService.customerStatement` last payments), the account's open deals + weighted forecast + CPQ quotes, and the member-linked contact's loyalty (RFM/tier/NPS/recovery) into ONE payload; a Customer-360 panel on `/crm/accounts/[accountNo]`. New rows UAT-O2C-317..319; ToE `pipeline.ts` 43→49; narrative PN-18 rev 0.7; manual 16-crm-workspace.md §16.5.* · *v6.20: auto-86 out-of-stock sync to aggregators (POS-7, new control **INV-14**, migration `0300`, RCM 210) — UAT-O2C-316 maps deplete→86/idempotent/restock→un-86 to INV-14; ToE `channel-adapter.ts`.* · *v6.19: FIN-6 fixed-assets parallel TAX depreciation book + CIP/AUC settlement (new control **FA-13**, migration `0300`) — a memo-only tax book (Thai caps + first-year initial allowance, `POST /api/assets/tax-depreciation/run`, no GL) whose actual tax NBV feeds deferred tax (Dr 5950 / Cr 1700) instead of a manual GAAP adjustment (FIN-6a); CIP accumulates cost into new account **1520** (Dr 1520 / Cr 2000|1000, not depreciated) and settles to a fixed asset under a maker-checker gate (`…/cip/:cipNo/settle[/approve]`, mandatory reason, self-approve → `SOD_VIOLATION`, reclass Dr 1500 / Cr 1520; `CIP_NOT_OPEN`/`CIP_NO_COST` guards); tables `cip_assets`/`cip_cost_lines` + `fixed_assets` tax-book/`source_cip_no` columns; new rows UAT-GL-131..136 (`basics.ts`, RCM 210); narrative PN-09 rev 1.5.* · *v6.18: Batch approve/reject on Pending Approvals (`/approvals`) — UAT-GL-046b: an approver ticks several queued items (or Select all) and clears them in one action; each ticked item still fires its OWN maker-checker endpoint (`Promise.allSettled`), so its control + SoD (approver ≠ requester → `SOD_VIOLATION`) is enforced per item; summary reports N succeeded / M failed. UX over existing GOV-01 per-item routes — no new endpoint, no new control, no migration.* · *v6.16: AP payment run + Thai bank payment file (FIN-2) — batch disbursement maker-checker over the AP-PAY spine (propose by due-date cutoff with per-line 3-way-match gating, distinct approver/executor `SOD_VIOLATION`, idempotent execute through `requestApPayment`→`approveApPayment`, hash-pinned generic/scb/kbank/bbl CSV + pain.001 bank file fail-closed on `VENDOR_BANK_MISSING`, statement auto-match clearing); tables `ap_payment_runs`/`ap_payment_run_lines` (migration 0297); new control **EXP-13** (RCM 208); UAT-P2P-126..130; ToE compliance 147→159; narratives PN-02 rev 3.31 + PN-07 rev 1.7.* · *v6.15: FIN-3 budgetary control / encumbrance gate on PR/PO approval (new control **BUD-02**, migration `0296`) — policy off/advise/warn/block on `budget_control_settings`; availability = approved budget − GL actuals − open commitments; exec-only audited override; `budget_commitments` lifecycle (reserve/consume/release). New rows UAT-P2P-131..128 (`budget.ts`).* · *v6.14: CRM-2 — the modern CRM workspace (docs/41 UI phase): kanban board `/crm` (governed drag stage moves → `crm_stage_history`), deal page `/crm/deals/[oppNo]` (composed detail read), account page, `/pipeline` + `/projects/crm` redirects, lead capture (bulk import + public `web-to-lead` with strict rate bucket + honeypot); no new RCM control; UAT-O2C-306..299; ToE `pipeline.ts` 32→43; narrative PN-18 rev 0.6; manual 16-crm-workspace.md.* · *v6.13: AR cash application (docs/41 FIN-1, new control **REV-21**, migration `0295`) — UAT-O2C-298..293: one receipt across many invoices with an on-account remainder (`ar_receipts.unapplied_amount` ↔ GL 2220), apply-on-account later, fail-closed guards (`OVER_APPLIED`/`APPLY_EXCEEDS_RECEIPT`/`CUSTOMER_MISMATCH`/`INSUFFICIENT_UNAPPLIED`), ≥ THB 100k threshold maker-checker (self-approve → `SOD_VIOLATION`), reason-required audited reversal, and Issued AR-linked credit notes applied as credit lines (`CN_*` guards); ToE `basics.ts` (318); narrative PN-01 §7 8e rev 0.30.* · *v6.12: CRM-1 — CRM data-model unification (migration `0295`): one opportunity spine (`crm_opportunities`) behind both `/api/crm/pipeline` and the legacy `/api/pipeline` adapters (won/lost terminal everywhere), `crm_stage_history` transition audit, `crm_accounts`/`crm_contacts` with duplicate detection (409 `DUPLICATE_SUSPECT`) + maker-checked survivor merge, CPQ `crm_opportunity_id` link; no new RCM control (extends REV-17/REV-15); UAT-O2C-292..297; ToE `pipeline.ts` (32); narrative PN-18 rev 0.5.* · *v6.11: POS-3 standalone voucher/coupon codes redeemable at checkout — `voucher_campaigns`/`voucher_codes` (migration 0292, RLS), maker-checker activation (self-approve → `SOD_VIOLATION`), crypto-random bulk codes + CSV export, atomic single redemption inside the sale tx (`VOUCHER_ALREADY_REDEEMED` on a race), expired/min-spend/void gates, member wallet coupons redeem via the same surface; UAT-LOY-069..072; ToE pricing 23→40; new control REV-20 (RCM 203); narrative PN-19 §7 (34) rev 1.44.* · *v6.10: ABB → full tax-invoice conversion at the counter (`POST /api/tax-invoices/abbreviated/:docNo/convert-full`, ม.86/4 on buyer request) — buyer Tax ID required + checksum-validated, amounts/dates copied verbatim from the slip, ABB → Replaced (ภ.พ.30 single-count), idempotent one-full-per-ABB (partial unique index, migration 0291); new control **TAX-10** (RCM 203); UAT-TAX-050; ToE taxdocs 116→131; narrative PN-06 rev 0.26.* · *v6.9: FIN-7a multi-dimensional financial reports — additive `?project_id=&dept_id=&branch_id=` filters on `GET /api/ledger/trial-balance` / `account-ledger` / `income-statement` (dimension slices aggregate from journal lines; unfiltered snapshot path byte-identical — golden re-verified), `GET /api/ledger/dimensions` dropdown helper, dropdowns on `/accounting` + `/financial-statements`; UAT-GL-132..133; ToE basics.ts +7; no control change (GL-13 review surface); narrative PN-04 rev 2.14.* · *v6.8: LINE e-receipt to the sale's member (POS-2, docs/41) — `POST /api/pos/sales/:saleNo/receipt/send` channel `line` (member resolved from the sale via ledger `ref_doc`/dine-in `member_id`; flex card through the existing messaging LINE client; opaque public full-receipt link `GET /api/pos/receipt/public/:token`); UAT-O2C-286..287; ToE `receipts.ts` 11→18; no control change (REST-10 unchanged); narrative PN-20 rev 3.12.* · *v6.7: POS-5b menu-engineering depth — `branch_id` filter + qty-weighted average-CM threshold on `GET /api/analytics/menu-engineering`; UAT-RPT-034 updated + UAT-RPT-053; ToE in the CI-gated `restaurant` harness (167 checks); no control change; narrative PN-20 rev 3.12.* · *v6.6: bank-statement FILE import — `POST /api/bank/accounts/:id/statements/import-file` parses the bank's own CSV/XLSX export (fuzzy Thai/English headers covering KBank/SCB/BBL, Buddhist-Era→CE dates incl. 2-digit BE short years, parentheses negatives, derived opening/closing from running balance, skip-and-count summary rows, fail-closed NO_DATE_COLUMN/NO_AMOUNT_COLUMN/BAD_FILE) into the existing importStatement→auto-match→certify spine; `/bank` gains the upload button; UAT-GL-131; ToE bankrec 16→21; no control change (REC-02/BANK-02 unchanged); narrative PN-07 rev 1.6.* · *v6.5: RD e-Filing attachment files — ภ.ง.ด.3/53 ใบแนบ transfer .txt (`/api/tax-reports/pnd/efiling`, TIS-620, BE dates, อัตราร้อยละ, เงื่อนไข codes) + UTF-8-BOM CSV working papers for ภาษีขาย/ภาษีซื้อ/ภ.พ.30 (input-VAT CSV flags missing-tax-id/estimated rows); web `/tax/reports` download buttons; UAT-TAX-049; ToE taxdocs 108→116; no control change (strengthens TAX-03/04/05); narrative PN-06 rev 0.24.* · *v6.4: per-tenant AI-processing opt-out (PDPA มาตรา 27/30) — UAT-RPT-052 (`ai_external_processing` flag at Settings › Labs & AI; chat → `AI_TENANT_OPTED_OUT`, NL/doc-AI/insights/LINE-copilot/embedder degrade to deterministic paths; ToE ai-eval.ts layer 4, 22 checks); no RCM change; narrative PN-08 rev 0.5.* · *v6.3: doc-reference dropdowns round 2 (ERP + POS sweep) — UAT-O2C-283 (new `GET /api/payments/till/sessions` feeds the `/pos/close-of-day` Z-report dropdown; POS bill fields on `/returns`/`/pos-control`/`/pos-ops`/`/payments/terminals`/`/print` pick from `GET /api/pos/orders`; `cashreport.ts` 32), UAT-RE-05 (new `GET /api/realestate/developments` + `/contracts` pending lists; `projects.ts` 256), UAT-INV-073 (manufacturing WO/routing/BOM/quality-ref + goods-issue ref dropdowns over existing GETs; manual UI); read-only/UI only, no control change; narratives PN-01 rev 0.29 / PN-03 rev 0.8 / PN-06 rev 0.25 / PN-15 rev 0.2 / PN-31 rev 0.3.* · *v6.2: doc-reference dropdowns everywhere a screen referenced another document's number — UAT-P2P-125 (match / AP-intake / PO-attachments / supplier-portal / GR-claims pick from pending lists, `doc-select.tsx` shared island), UAT-O2C-282 (new read-only `GET /api/delivery/open-orders` feeds the `/delivery` SO dropdown; `gaps.ts`), UAT-INV-071..072 (new read-only `GET /api/wms/wave-candidates` + `GET /api/wms/picks?status=` + `GET /api/wms/shipments?status=` feed the `/wms` Wave/Pack/Ship dropdowns; `wms.ts` 27→30), UAT-TAX-048 (tax-invoice source ref + credit/debit original + `/einvoice` doc-no dropdowns); UI/read-only only, no control change; narratives PN-01 rev 0.28 / PN-02 rev 3.30 / PN-03 rev 0.7 / PN-06 rev 0.24.* · *v6.1: guest profile at service time — UAT-O2C-279 (guest-initiated consent request via LINE/SMS → /m self-service; transactional `consent_request`; already-granted short-circuits) + UAT-O2C-280 (read-time service flags on the floor board + KDS tickets; consent withdrawal wipes instantly) + UAT-O2C-281 (seat a buffet booking → offer to start the pre-picked tier); /m gains the "ความยินยอม (PDPA)" self-service card (LYL-10c); no control change; harness `reservations.ts` 23→27; narratives PN-20 rev 3.11 + PN-19 rev 1.36.* · *v6.0: blind-count goods receiving (new control **EXP-12**, migration `0290`) — UAT-P2P-120 (PO lines load on `/receiving`, counted qty never pre-filled) + UAT-P2P-121 (`OVER_RECEIPT` gate; weight-UoM 5% headroom, cumulative per item) + UAT-P2P-122 (ordered-vs-received summary + dock-photo claim inside the window) + UAT-P2P-123 (`CLAIM_WINDOW_CLOSED` — 24h window auto-closes) + UAT-P2P-124 (close-short binding `PO_LINE_CLOSED`; tolerance change restricted to procurement/exec); harness `compliance.ts`; narrative 02 §7 (5), §9, rev 3.29.* · *v5.9: guest-profile hardening — UAT-O2C-278 (profile PUT is merge-patch: omitted fields kept, explicit null clears — no silent wipe) + `extra` editor and companion allergies/preferences on the web card + `dine_in_orders (tenant_id, member_id)` index (migration `0289`); no control change; harness `reservations.ts` 21→23; narrative PN-20 rev 3.10.* · *v5.8: fine-casual reservations + PDPA guest dining profile — UAT-O2C-274 (buffet vs à-la-carte booking mode + pre-picked tier + occasion, covers split per mode) + UAT-O2C-275 (`BAD_PACKAGE` guards incl. cross-tenant) + UAT-O2C-276 (guest-profile consent gate: no `dining_profile` consent → no data + `CONSENT_REQUIRED`; save captures consent audited `source='pos'`; companion hard-delete) + UAT-O2C-277 (withdrawal hides all; DSAR export bundles + erasure hard-deletes the profile/companions — PDPA-01/02 wording updated in the RCM, census unchanged, no new control); tables `member_dining_profiles`/`member_companions` + reservation columns (migration `0288`, canonical RLS); harnesses `reservations.ts` (10→21) + `pdpa.ts` (+2); narrative PN-20 §7 step 14/14b rev 3.9.* · *v5.7: master-data audit Phase 12 (final) — date-effective (future-dated) master attributes: UAT-ADM-135 (schedule a non-sensitive item change + `UNSUPPORTED_FIELD` guard) + UAT-ADM-136 (a sensitive customer credit-limit change is staged `pending_approval` and released only by a distinct approver — self-approve → `SOD_VIOLATION`, strengthening G7/R09) + UAT-ADM-137 (the idempotent `apply_scheduled_master_changes` job / `run-due` applies only changes whose effective business date has arrived, future-dated ones skipped, second same-day run applies nothing); new tenant-scoped `scheduled_master_changes` table (migration `0278`, RLS + change-audited) + a BI-scheduler action job; web `/setup/items` per-item schedule/list/cancel section; **closes the date-effective attribute gap flagged in v5.4**; no new control; harness `basics.ts`; narrative PN-17 §7 step 13.* · *v5.6: master-data audit Phase 11 — item match-merge / DQM: UAT-ADM-131 (detection by barcode + fuzzy description) + UAT-ADM-132..134 (merge is god-only `ITEM_MERGE_HQ_ONLY` since `items` is a shared cross-tenant master; self-merge/already-merged guards; god merge repoints child rows by the TEXT `item_id` key via the new `md_merge_repoint_text` and soft-retires the duplicate `status='merged'`+`merged_into`); migration `0277` (item merge columns + text-key repoint function); web `/setup/items` review queue with a god-gated merge action; mirrors the Phase-5 customer/vendor match-merge; no new control; harness `basics.ts`; narrative PN-17 §7 step 12.* · *v5.5: master-data audit Phase 10 — item-master depth (item relationships + lifecycle status): UAT-ADM-128 (lifecycle status active/inactive/discontinued + successor SKU) + UAT-ADM-129..130 (typed `item_relationships` substitute/complement/supersedes/kit_component/accessory — self/dup/not-found guards, bidirectional listing, tenant-scoped delete-both-sides); new tenant-scoped `item_relationships` table + `items.status`/`items.superseded_by` (migration `0276`, RLS + `data_change_log`-audited); mirrors the Phase-8 party-relationship model; no new control; harness `basics.ts`; narrative PN-17 §7 step 11.* · *v5.4: master-data audit Phase 9 (final) — flexfields wired onto the master screens (Oracle DFF) + governed bank master: UAT-O2C-273 (customer custom-field round-trip + 19-bank Thai reference `GET /api/geo/banks` + normalize-bank) + UAT-P2P-119 (staged vendor bank name canonicalised to ธนาคารกสิกรไทย); custom-fields value perms widened to master stewards; no migration, no new control; harnesses `customers.ts`/`match.ts`. Full date-effective/future-dated attributes deferred as a standalone effort (Phase 6 change history covers the value-as-of-date audit need).* · *v5.3: master-data audit Phase 8 — typed party relationships (Oracle TCA): UAT-O2C-271..272 (customer bill_to/ship_to/sold_to/guarantor/related_party/subsidiary/franchisee, bidirectional visibility, self/dup guards, delete-both-sides) + UAT-P2P-118 (vendor equivalent); new `customer_relationships`/`vendor_relationships` (migration `0275`, RLS + change-audited); supports SOX related-party disclosure; no new control; harnesses `customers.ts`/`match.ts`.* · *v5.2: master-data audit Phase 7 — Thai address standardization: UAT-O2C-269..270 (customer province canonicalised to the 77-province reference + postal 5-digit validation + unknown-province-kept) + UAT-P2P-117 (vendor equivalent); new read-only `GET /api/geo/provinces` (`common/thai-address.ts`); no new control, no migration; harnesses `customers.ts`/`match.ts`.* · *v5.1: master-data audit Phase 6 — universal master-data change history: UAT-O2C-267..268 (customer create/onboarding + field-level old→new + sensitive-column masking) + UAT-P2P-116 (vendor equivalent); the DB-trigger field-level change log (`data_change_log`, ITGC-AC-14) extended to the master + child tables (migration `0274`), append-only, DB-enforced; strengthens ITGC-AC-14, no new control; harnesses `customers.ts`/`match.ts`.* · *v5.0: master-data audit Phase 5 — match-merge / duplicate resolution (DQM) for both customer_master and vendors: UAT-O2C-264..266 (detect probable duplicates, governed merge with child-row repoint + survivorship + soft-retire, self/already-merge guards) + UAT-P2P-115 (vendor equivalent); app-side fuzzy name matching (`pg_trgm` not enabled) + generic `md_merge_repoint` (migration `0273`); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.9: master-data audit Phase 4 — full relational Party-model depth for both customer_master and vendors: UAT-O2C-261..263 (customer multi-address/multi-contact/parent-company link) + UAT-P2P-114 (vendor multi-address/multi-contact/parent-vendor link); no new control; harnesses `customers.ts`/`match.ts`.* · *v4.8: master-data audit Phase 3 — UAT-O2C-260 (customer master gains credit_terms/sales_rep/category/language/external_ref + a direct-edit endpoint + its first web CRUD screen `/customers`, the fast-follow flagged in rev 0.10); no new control; harness `customers.ts`.* · *v4.7: master-data audit Phase 2 field-exposure cases — UAT-P2P-113 (vendor master direct-edit + supplier-list enrichment), UAT-GL-128..130 (item-master fields on `/setup/items`, warehouse master fields on `/setup/warehouses`, asset register location/department/serial/assigned-to), UAT-PAY-045 (employee-master fields on `/payroll`) — all "no new control", wiring existing schema columns to screens that previously omitted them; harnesses `match.ts`, `basics.ts`, `payroll.ts`.* · *v4.6: added UAT-P2P-112 (vendor bank-detail change maker-checker — new control EXP-11; a vendor's payee bank_name/bank_account is staged PendingApproval by `md_vendor` and applied only when a distinct `exec`/`approvals` user approves, self-approval → `SOD_VIOLATION`; migration `0270`); harness `match.ts`.* · *v4.5: added UAT-TAX-043 (issuing a full tax invoice upserts the buyer into customer_master — new buyer gets address/branch/tax-id, a repeat buyer's address is refreshed not duplicated; web buyer-name autocomplete searches it; migration 0269, no new control); harness `taxdocs.ts`.* · *v4.4: added UAT-TAX-042 (full tax invoice "ชำระเงินโดย" Paid By section — POS auto-derives from the sale's payment method, AR settable + a due date, presentation/data-adjacent, migration 0268, no new control); harness `taxdocs.ts`.* · *v4.3: Phase P3 detective-first maker-checker gaps + the cross-cutting readiness check (audit G14/G16/G10 + workflow-definition readiness) — UAT-O2C-258 (G14 POS void/refund exception report `GET /api/payments/exceptions/voids-refunds` for independent periodic review; voids + sub-threshold refunds single-user by design), UAT-O2C-259 (G10 bank-statement import single-user by design, imported lines folded into the reconciliation certifier's evidence — REC-02/R06; documentation only), UAT-TAX-041 (G16 voided-tax-invoice exception report `GET /api/tax-invoices/exceptions/voided`; void stays single-user per RD requirement), UAT-P2P-110 (cross-cutting workflow-definition readiness reporter `GET /api/workflow/readiness` detects engine-wired docTypes PR/PO/BUDGET/PMR/BQR that lack a definition and would auto-approve) — all detective/config, strengthening existing SoD (R08/R12/R06/EXP-03/R07) with NO new numbered control; harnesses `refund-approval.ts`/`bankrec.ts`/`taxdocs.ts`/`workflow.ts`.* · *v4.2: Phase P2 preventive maker-checker gaps (audit G9/G12/G13/G15) — UAT-O2C-253..254 (G12 CPQ: quote author cannot self-accept a billable quote → `SOD_VIOLATION`, no revenue; a distinct exec accepts → Dr 1100/Cr 4000; no migration), UAT-O2C-255..257 (G9 bank: a new account is PendingApproval + can't bank cash `BANK_NOT_APPROVED` until a distinct approver activates it; migration 0264), UAT-ADM-125..127 (G15 tenant PromptPay/Tax-ID change staged for a distinct approver; QR refused while pending; migration 0265), UAT-LOY-066..068 (G13 staff point transfers > 500 pts staged for a distinct approver; migration 0266) — all strengthening existing SoD (R02/R07/R10/R15/R16) + REC-05/CPQ-03/LYL-18 with NO new numbered control; harnesses `cpq-gl.ts`/`cash-banking.ts`/`promptpay.ts`/`loyalty.ts`.* · *v4.1: sensitive-field bulk-import maker-checker (audit gaps G5+G8) — UAT-ADM-121..124 added (a non-sensitive vendor import commits directly; an import that SETS a sensitive master field — customer/vendor credit limit, vendor payment terms, price-list price, promotion discount — is STAGED `PendingApproval` in `masterdata_import_batches` (migration `0263`) and writes nothing; requester self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver applies it → the rows + credit limit are written), strengthening SoD **R02/R09/R10/R13** + MDM-01/MDM-03 (no new numbered control); harness `ext.ts`; narrative PN-17 §7.3d.* · *v4.0: price/promo rule activation → two-person maker-checker (audit gap G6) — UAT-O2C-250..252 added (a rule change stages `PendingApproval`/`active=false` so it affects no quote or sale; author self-approve → `SOD_VIOLATION`; a distinct `exec`/`approvals` approver activates it → the discount then applies), strengthening SoD **R10** / **MKT-01** (no new control); migration `0262`; harness `pricing.ts`.* · *v3.9: SoD self-service override → two-person maker-checker (audit gap G11, part b) — UAT-ADM-003 updated + UAT-ADM-117..120 added (a justified SoD override is STAGED PendingApproval and no longer self-applied — user NOT created; requester self-approve → `SOD_VIOLATION`; a distinct admin approves → user created with the conflicting set + who/why/rules persisted in the hash-chained audit meta; no-reason still `SOD_CONFLICT`), strengthening ITGC-AC-09; migration `0253` (`access_grant_exceptions`); harness `compliance.ts`.* · *v3.8: gift-card issuance maker-checker (audit gap G1) — UAT-GL-065..068 added (a gift card is a cash-equivalent 2200 liability; face > 5,000 THB now issues `PendingApproval` → no GL, not redeemable (`GIFT_CARD_INACTIVE`); issuer self-approve → `SOD_VIOLATION`; a distinct `creditors`/`exec` approver posts Dr 1000/Cr 2200 + activates; ≤ 5,000 THB still auto-issues), strengthening GC-01/GL-01 (SoD R14); migration 0252; harness `giftcards.ts`.* · *v3.7: EXP-08 fund-funding maker-checker (audit gap G3) — UAT-P2P-050/056 updated + UAT-P2P-058/059 added (petty-cash **fund establishment & replenishment** now route through a PendingApproval funding request → distinct-user approval posts Dr 1015 / Cr 1000; self-approval → SOD_VIOLATION); harnesses `basics.ts` / `compliance.ts`.* · *v3.6: added UAT-GL-060..064 (GL dual-control gap closures — opening-balance batch maker-checker G4 + distinct-reverser on manual reversal G2, strengthening GL-05/GL-17; harnesses `opening-balances.ts`, `basics.ts`).* · *v3.5: added UAT-ADM-114..116 (ITGC-AC-02 — Admin-grant restricted to the platform owner) + UAT-SEC-054 (ITGC-AC-18 — public signup → request-access queue, god-only company creation); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v3.4: added UAT-GL-126..127 (FA-12 asset verification-exception + audit BI reports; harness `module-qr.ts`).* · *v3.3: added UAT-GL-121..125 (FA-11 asset custody-change maker-checker + audit-by-scan; harness `module-qr.ts`).* · *v3.2: added UAT-INV-069b/069c/070b (cross-browser scanner via @zxing fallback + 1D barcodes + continuous scan; `/q` page e2e `qr-resolver.spec.ts`).* · *v3.1: added UAT-INV-066..070 (QR camera scanning + `/q` deep-link resolver — capture-only, no control change; harness `module-qr.ts`).* · *v3.0: added UAT-P2P-104 (docs/34 Phase 4 — Email-to-Capture: verified send-from address + inbound bill webhook).* · *v2.9: added UAT-P2P-103 (docs/34 Phase 1 — Quick Capture over LINE `บิล` + photo).* · *v2.8: added UAT-P2P-101/102 (docs/34 Quick Capture lane — any `pr_raise` staffer snaps/uploads a bill → NeedsReview draft; SoD capturer ≠ poster).* · *v2.7: added UAT-O2C-234 (docs/32 FU4 — raise site cash from the project workspace; team walkthrough doc added).* · *v2.6: added UAT-P2P-100 (partial receive + short/damaged claim from chat).* · *v2.5: added UAT-P2P-099 (purchase spend insights — chat `spend` + `/spend-summary` + `purchase_spend` report).* · *v2.4: added UAT-P2P-098 (close-the-loop LINE notifications to the PR requester).* · *v2.3: added UAT-P2P-097 (proactive morning low-stock alert → one-tap reorder).* · *v2.2: added UAT-P2P-096 (reorder low stock → one-tap PR — web card + LINE `low`/`reorder`).* · *v2.1: reconciled the UAT-P2P-093 collision after merging main — 093 = vendor AP card (merged separately), PR→PO conversion renumbered to 094, one-tap goods receipt (web รับครบ + LINE `receive`) to 095.* · *v2.0: added the one-tap goods-receipt case (now 095).* · *v1.9: added the PR→PO conversion case (now 094).* · *v1.8: added UAT-RPT-049 (LP-3 — digest 2.0).* · *v1.7: added UAT-P2P-092, UAT-PAY-039, UAT-RPT-048 (LP-2 — copilot uplift).* · *v1.6: added UAT-SEC-049/050 (LP-1 — LINE OA production go-live pack).* · *v1.5: added UAT-P2P-086..091 (EXP-10 — AP invoice intake: scan → PO auto-map → matched-at-posting, duplicate refusal, cumulative guard, scheduled auto re-match release, direct image/PDF upload).* · *v1.4: added UAT-RPT-046 (LC-4 LINE delivery + digest).* · *v1.3: added UAT-SEC-048 + UAT-PAY-038 (LC-3 leave via chat + channel governance).* · *v1.2: added UAT-P2P-084 (LC-2 petty-cash chat raise + EXP-08 notifications).* · *v1.1: added UAT-P2P-083 (LC-1 one-tap chat approvals).* · *v1.0: added UAT-P2P-081..082 (PO attachment evidence — web + LINE chat photo attach).* · *v0.9: added UAT-P2P-076..080 (LINE chat phase 2 — workflow notifications, chat approve/reject with engine SoD, self-service commands).* · *v0.8: added UAT-P2P-070..075 (LINE chat → PR — link-code identity binding, chat-raised PR into the same approval workflow, entry-integrity negatives).* · *v0.7: added UAT-O2C-226..227 (PROJ-03 project period-end close review UI + PROJ-04 timesheet project allocation surfaced in `/hcm`).* · *v0.6: added UAT-SEC-036..045 (ITGC-AC-17 — POS-PIN quick-login restriction).* · *v0.5: added UAT-ADM-094..096 (SoD R12 — /returns nav perm for AR/pos_refund).*

Maps every UAT case → cycle → requirement/feature → RCM control (where applicable) → process-narrative section. RCM control IDs reference `compliance/Oshinei_ERP_SOX_RCM_v1.xlsx`; SoD rules (R01–R16) reference `packages/shared/src/permissions.ts`. Process-narrative files are in `docs/process-narratives/`.

Coverage check: every in-scope requirement/control should appear in ≥1 executed case (UAT exit criterion §7.4). Section numbers in the narrative column follow the common 14-section structure (§7 = Process narrative, §9 = Control matrix).

## 01 — Security & Access → `08-itgc.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-SEC-001 | JWT login returns token + role | ITGC-AC-01 | 08 §7, §9 |
| UAT-SEC-002 | Bad-credential rejection | ITGC-AC-01 | 08 §9, §13 |
| UAT-SEC-003 | Auth required on protected routes | ITGC-AC-02 | 08 §7 |
| UAT-SEC-004 | First login forces password change (hard API gate) | ITGC-AC-07 | 08 §7 |
| UAT-SEC-046 | Permission change revokes outstanding sessions immediately | ITGC-AC-15, ITGC-AC-02 | 08 §7 |
| UAT-SEC-047 | Employee DSAR access/erasure with statutory payroll retention | PDPA-02, ITGC-AC-19 | 08 §7 |
| UAT-SEC-005 | Password min-length policy | ITGC-AC-05 | 08 §9 |
| UAT-SEC-006 | Mandatory MFA for privileged role | ITGC-AC-06 | 08 §9 |
| UAT-SEC-007 | MFA not forced for low-privilege role | ITGC-AC-06 | 08 §9 |
| UAT-SEC-008 | TOTP enrolment | ITGC-AC-06 | 08 §7 |
| UAT-SEC-009 | MFA_REQUIRED enforcement | ITGC-AC-06 | 08 §9, §13 |
| UAT-SEC-010 | MFA_INVALID enforcement | ITGC-AC-06 | 08 §9, §13 |
| UAT-SEC-011 | Password + TOTP authn | ITGC-AC-06 | 08 §7 |
| UAT-SEC-012 | MFA disable | ITGC-AC-06 | 08 §7 |
| UAT-SEC-013 | Effective permissions exposed | ITGC-AC-07 | 08 §7 |
| UAT-SEC-014 | RBAC deny without permission | ITGC-AC-07 | 08 §9 |
| UAT-SEC-015 | RLS cross-tenant isolation | ITGC-AC (RLS) | 08 §7, §9 |
| UAT-SEC-016 | Edge security headers | ITGC-OP | 08 §9 |
| UAT-SEC-017 | Aggregator webhook requires secret | ITGC-AC-03 | 08 §7 |
| UAT-SEC-018 | PSP webhook tenant-scoped | ITGC-AC-03 | 08 §7 |
| UAT-SEC-019 | Input-validation hardening (qint/Zod) | ITGC-AC-02 | 08 §7 |
| UAT-SEC-020 | Username login case/whitespace-insensitive | ITGC-AC-01 | 08 §7 |
| UAT-SEC-021 | Password remains case-sensitive (not trimmed) | ITGC-AC-01 | 08 §7, §13 |
| UAT-SEC-022 | Logout revokes the token (jti denylist) | ITGC-AC-15 | 08 §7, §9 |
| UAT-SEC-023 | Deactivated account's existing token rejected live | ITGC-AC-15 | 08 §7, §9 |
| UAT-SEC-024 | Revoke-all-sessions invalidates pre-existing tokens | ITGC-AC-15 | 08 §7, §9 |
| UAT-SEC-025 | Audit hash chain verifies intact | ITGC-AC-16 | 08 §7, §9 |
| UAT-SEC-026 | Tampering a past audit row is detected (hash mismatch) | ITGC-AC-16 | 08 §7, §9 |
| UAT-SEC-027 | Member OTP login sets httpOnly cookie (no JS-readable token) | ITGC-AC-07 | 08 §7, 19 §7 |
| UAT-SEC-028 | Member cookie session authenticates self-scoped read | ITGC-AC-07 | 08 §7, 19 §7 |
| UAT-SEC-029 | Member cookie mutation requires CSRF double-submit | ITGC-AC-07 | 08 §7, 19 §7 |
| UAT-SEC-030 | Member logout clears + revokes the session | ITGC-AC-07 / ITGC-AC-15 | 08 §7, 19 §7 |
| UAT-SEC-031 | PDPA DSAR filed with statutory 30-day due date | PDPA-01 | 08 §7, §9 |
| UAT-SEC-032 | PDPA access/portability export | PDPA-01 | 08 §7, §9 |
| UAT-SEC-033 | PDPA erasure redacts PII + issues pseudonym | PDPA-02 | 08 §7, §9 |
| UAT-SEC-034 | PDPA erasure pseudonymises the audit trail at read-time (stored row immutable) | PDPA-02 / ITGC-AC-16 | 08 §7, §9 |
| UAT-SEC-035 | PDPA DSAR is tenant-isolated | PDPA-01 / ITGC-AC-03 | 08 §7, §9 |
| UAT-SEC-055 | RoPA — create a processing activity with a validated legal basis | PDPA-03 | 08 §7 (8b), §9 (19a) |
| UAT-SEC-056 | RoPA — update/retire + active filter + RLS isolation | PDPA-03 | 08 §7 (8b), §9 (19a) |
| UAT-SEC-057 | PII retention sweep — default-off, 12-month floor, dry-run non-destructive | PDPA-04 | 08 §7 (8c), §9 (19b) |
| UAT-SEC-058 | PII retention sweep — anonymizes aged members only, ledger row, idempotent, scheduled opt-in | PDPA-04 | 08 §7 (8c), §9 (19b) |
| UAT-SEC-059 | Encryption-key rotation — keyring inert/rotate/idempotent + fail-closed | ITGC-AC-12 | 08 (rev 2.5); docs/ops/secrets.md §4 |
| UAT-TAX-046 | SBT accrual at RE ownership transfer (opt-in; default-inert) | TAX-09 | 06 §7 (5b), §9 |
| UAT-TAX-047 | ภ.ธ.40 report + GL-2130 tie + filing register + calendar | TAX-09, TAX-05 | 06 §7 (5b), §9 |
| UAT-TAX-048 | Tax-doc source refs picked from dropdowns (full-invoice source, note original, e-invoice doc-no) | Usability over TAX-01/TAX-07 (no new control) | 06 §7, rev 0.24 (manual/UI) |
| UAT-TAX-050 | ABB → full tax-invoice conversion (verbatim amounts, output-VAT single-count, idempotent one-full-per-ABB; bad Tax ID / voided ABB / non-ABB rejected) | TAX-10 | 06 §7 (3a), §9 |
| UAT-TAX-051 | DTA valuation allowance — MLTN computes allowance = max(0, gross − MLTN) | TAX-12 | 06 §9, 04 §3.2 |
| UAT-TAX-052 | Valuation-allowance post maker-checker + idempotent (Dr 5950/Cr 1700) | TAX-12, R05 | 06 §9, 04 §3.2 |
| UAT-TAX-053 | Valuation allowance sources gross DTA from deferred_tax_runs (TAX-06) | TAX-12 | 06 §9, 04 §3.2 |
| UAT-TAX-054 | Uncertain Tax Position register — reserve = gross − recognized; over-recognition rejected | TAX-12 | 06 §9, 04 §3.2 |
| UAT-TAX-055 | UTP settle maker-checker; only an Open position can be settled | TAX-12, R05 | 06 §9, 04 §3.2 |
| UAT-TAX-056 | UTP register totals + tenant isolation (RLS) | TAX-12 | 06 §9, 04 §3.2 |
| UAT-TAX-060 | Current income-tax provision — book→taxable bridge + current CIT (reuses TAX-06 temp diff) | TAX-11 | 06 §9b, §9 |
| UAT-TAX-061 | ETR reconciliation ties statutory → effective | TAX-11 | 06 §9b, §9 |
| UAT-TAX-062 | Provision maker-checker (`SOD_SELF_APPROVAL`) + post Dr 5960 / Cr 2110 + isolation | TAX-11, R05 | 06 §9b, §9 |
| UAT-TRE-011 | Debt facility create → self-approve `SOD_SELF_APPROVAL` → distinct approver approves | TRE-01, R23 | 17 §5, PN-33 §7 |
| UAT-TRE-012 | Drawdown on an approved facility posts Dr 1010 / Cr 2550; over-limit → `LIMIT_EXCEEDED` | TRE-01 | 17 §5, PN-33 §7 |
| UAT-TRE-013 | Idempotent EIR accrual (carrying × EIR/12) Dr 5900 / Cr 2450; re-run same period posts 0 | TRE-01 | 17 §5, PN-33 §7 |
| UAT-TRE-014 | Repay principal + accrued interest Dr 2500/2550 + Dr 2450 / Cr 1010; over-repay guarded | TRE-01 | 17 §5, PN-33 §7 |
| UAT-TRE-015 | Maturity ladder buckets outstanding principal by time-to-maturity | TRE-01 | 17 §5, PN-33 §7 |
| UAT-TRE-016 | Permission gate — non-treasury role denied; TreasuryAnalyst maker cannot approve | TRE-01, R23 | 17 §5, PN-33 §7 |
| UAT-TRE-021 | Covenant test PASS then BREACH persists a breached reading; worklist surfaces it | TRE-02 | 17 §6, PN-33 §7 |
| UAT-TRE-022 | Debt register + covenants tenant-isolated (RLS) — sibling tenant sees none | TRE-01, TRE-02 | 17 §5, §6 |
| UAT-TRE-031 | Investment buy → self-approve `SOD_SELF_APPROVAL` → distinct approver → buy Dr 1350\|1360\|1370 / Cr 1010 | TRE-03, R23 | 17 §7, PN-33 §7bis |
| UAT-TRE-032 | Classification routes the buy GL (AMORTIZED_COST→1350, FVOCI→1360, FVTPL→1370) | TRE-03 | 17 §7, PN-33 §7bis |
| UAT-TRE-033 | Maker-checker market-price register; an unapproved price cannot drive MTM (`NO_APPROVED_PRICE`) | TRE-03, R23 | 17 §7, PN-33 §7bis |
| UAT-TRE-034 | MTM — FVOCI→OCI reserve 3500, FVTPL→P&L 5430; amortized cost `MTM_NOT_APPLICABLE` | TRE-03 | 17 §7, PN-33 §7bis |
| UAT-TRE-035 | Idempotent EIR interest income (Dr 1350 / Cr 4700); re-run same period posts 0 | TRE-03 | 17 §7, PN-33 §7bis |
| UAT-TRE-036 | ECL impairment Dr 5440 / Cr 1355 allowance (contra-asset) | TRE-03 | 17 §7, PN-33 §7bis |
| UAT-TRE-037 | Investment register tenant-isolated (RLS) + permission gate | TRE-03, R23 | 17 §7, PN-33 §7bis |
| UAT-TRE-041 | Hedge designation maker-checker (`SOD_SELF_APPROVAL`) + required documentation (`BAD_DOCUMENTATION`) | TRE-04, R23 | 17 §8, PN-33 §7ter |
| UAT-TRE-042 | Control gate — no accounting on an undesignated/unapproved relationship (`HEDGE_NOT_DESIGNATED`) | TRE-04 | 17 §8, PN-33 §7ter |
| UAT-TRE-043 | Effectiveness gate — not effective → OCI refused (`HEDGE_NOT_EFFECTIVE`), whole change to P&L 5450 | TRE-04 | 17 §8, PN-33 §7ter |
| UAT-TRE-044 | Effectiveness gate — effective → effective portion to OCI 3550, ineffective to P&L 5450 | TRE-04 | 17 §8, PN-33 §7ter |
| UAT-TRE-045 | Cash-flow hedge OCI→P&L reclassification (Dr 3550 / Cr 4000); over-release `OCI_INSUFFICIENT` | TRE-04 | 17 §8, PN-33 §7ter |
| UAT-TRE-046 | Fair-value hedge — basis-adjust the hedged item (Cr 1200 / Dr 5450) + derivative to P&L | TRE-04 | 17 §8, PN-33 §7ter |
| UAT-TRE-047 | Hedge register tenant-isolated (RLS) + permission gate (maker/checker split) | TRE-04, R23 | 17 §8, PN-33 §7ter |
| UAT-TRE-051 | IC-loan register→approve maker-checker (`SOD_SELF_APPROVAL`, `SAME_PARTY`) + mirrored drawdown (Dr 1155 / Cr 1010 creditor + Dr 1010 / Cr 2155 debtor) | TRE-05, R23 | 17 §9, PN-33 §7quater |
| UAT-TRE-052 | Idempotent EIR interest accrual — both sides (Dr 1155 / Cr 4700 creditor + Dr 5900 / Cr 2155 debtor); `LOAN_NOT_APPROVED` | TRE-05 | 17 §9, PN-33 §7quater |
| UAT-TRE-053 | **Consolidation elimination (control core)** — 1155/2155 + 4700/5900 IC interest net to zero (CON-03 balanced, `ic_loan_eliminations`) | TRE-05, CON-03, REC-03 | 17 §9, PN-33 §7quater, PN-11 §7 |
| UAT-TRE-054 | Notional interest allocation must sum to zero (`ALLOCATION_NOT_ZERO`); physical sweep Dr header / Cr member; wrong-mode `NOT_PHYSICAL_POOL`/`NOT_NOTIONAL_POOL` | TRE-05 | 17 §9, PN-33 §7quater |
| UAT-TRE-055 | Cash-pool + IC-loan register tenant-isolated (RLS) | TRE-05 | 17 §9, PN-33 §7quater |
| UAT-SEC-036 | Cashier PIN quick-login succeeds | ITGC-AC-17 | 08 §7, §9 |
| UAT-SEC-037 | PosSupervisor PIN login + open shift (no duplicate) | ITGC-AC-17 / R08 | 08 §7, §9 |
| UAT-SEC-038 | Self set/rotate own PIN (current-password step-up) | ITGC-AC-17 | 08 §7 |
| UAT-SEC-039 | Admin sets a staff PIN | ITGC-AC-17 | 08 §7 |
| UAT-SEC-040 | Wrong PIN rejected (generic UNAUTHORIZED) | ITGC-AC-17 | 08 §7, §9 |
| UAT-SEC-041 | Privileged account blocked from PIN (PIN_NOT_ALLOWED) | ITGC-AC-17 | 08 §7, §9 |
| UAT-SEC-042 | Malformed PIN rejected (DTO) | ITGC-AC-17 | 08 §7, §9 |
| UAT-SEC-043 | Repeated wrong PIN trips the lockout (LOGIN_LOCKED) | ITGC-AC-17 / ITGC-AC-07 | 08 §7, §9 |
| UAT-SEC-044 | Self set-PIN with wrong current password (BAD_CURRENT_PASSWORD) | ITGC-AC-17 | 08 §7, §9 |
| UAT-SEC-045 | Clear-PIN disables PIN login | ITGC-AC-17 | 08 §7, §9 |
| UAT-SEC-054 | Public signup files a request (no tenant); god approval provisions; prod self-serve always off | ITGC-AC-18 | 08 §7 (3b), 23 §7 (0) |

## 02 — Order-to-Cash → `01-order-to-cash.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-O2C-001 | Create sales order | REV-01 | 01 §7 |
| UAT-O2C-002 | Order within credit limit | REV-08 | 01 §9 |
| UAT-O2C-003 | Credit-limit block (CREDIT_LIMIT) | REV-08 | 01 §9, §13 |
| UAT-O2C-004 | Credit-hold block (CREDIT_HOLD) | REV-08 | 01 §9, §13 |
| UAT-O2C-005 | Order status transitions | REV-02 | 01 §7 |
| UAT-O2C-006 | AR invoice generation (INV-) | REV-04 | 01 §7 |
| UAT-O2C-007 | AR receipt (RCP-) | REV-05 | 01 §7 |
| UAT-O2C-008 | Portal sale VAT + loyalty | REV-03, GL-01 | 01 §7 |
| UAT-O2C-009 | Tender capture | REV-06 | 01 §7 |
| UAT-O2C-009a | Card tender real PSP charge | REV-03 | 07 §7 |
| UAT-O2C-009b | No-token/declined never books funds | REV-03 | 07 §7 |
| UAT-O2C-009c | Card tender idempotency = one charge | REV-02 | 07 §7 |
| UAT-O2C-010 | Full refund | REV-09 | 01 §7 |
| UAT-O2C-011 | Over-refund block | REV-09 | 01 §9, §13 |
| UAT-O2C-012 | Return + restock + GL reversal | REV-09, GL-01 | 01 §7, §9 |
| UAT-O2C-013 | Partial return pro-rata VAT | REV-09 | 01 §7 |
| UAT-O2C-014 | Over-return block (OVER_RETURN) | REV-09 | 01 §9, §13 |
| UAT-O2C-015 | No-captured-payment block | REV-09 | 01 §13 |
| UAT-O2C-016 | Till open/close Z-report variance | REV-11, REC-02 | 07 §7, §9 |
| UAT-O2C-017 | Blind drawer close | REV-11 | 07 §9 |
| UAT-O2C-018 | PromptPay async settlement | REV-06 | 07 §7 |
| UAT-O2C-019 | RLS return isolation | ITGC-AC (RLS) | 08 §9 |
| UAT-O2C-020 | POS home store overview | Feature (POS dashboard) | 01 §0 |
| UAT-O2C-021 | Cashier read-only shift KPIs | RBAC (least-privilege read) | 01 §0 |
| UAT-O2C-022 | ERP/POS workspace switcher | Feature (ERP/POS workspaces) | 00 §4 |
| UAT-O2C-023 | Pricing rules apply at dine-in checkout | REV-01, GL-01 | 20 §8 |
| UAT-O2C-024 | Service charge + satang rounding post & balance | GL-01 | 20 §8 |
| UAT-O2C-025 | Pricing rules NOT applied unless opted in | REV-01 | 20 §8 |
| UAT-O2C-250 | Price/promo rule staged PendingApproval — does NOT apply | SoD R10, MKT-01 | 19 §7, §9 (`pricing.ts`) |
| UAT-O2C-251 | Rule author cannot self-approve (SOD_VIOLATION) | SoD R10 | 19 §7, §9 (`pricing.ts`) |
| UAT-O2C-252 | Distinct approver activates rule → discount applies | SoD R10, MKT-01 | 19 §7, §9 (`pricing.ts`) |
| UAT-O2C-253 | CPQ — quote author cannot self-accept a billable quote (`SOD_VIOLATION`); no revenue posts (G12) | SoD R07/R10 (CPQ revenue distinct-actor; no new numbered control) | 18 §7, §9 (`cpq-gl.ts`) |
| UAT-O2C-254 | CPQ — a distinct exec accepts → Dr 1100 AR / Cr 4000 posts 50000, TB balanced (G12) | SoD R07/R10 | 18 §7, §9 (`cpq-gl.ts`) |
| UAT-O2C-255 | Bank — new account is PendingApproval + inactive; a deposit into it is rejected `BANK_NOT_APPROVED` (G9) | SoD R02 (bank-account maker-checker; no new numbered control) | 07 §7, §9 (`cash-banking.ts`) |
| UAT-O2C-256 | Bank — requester cannot self-approve the new bank account (`SOD_VIOLATION`) (G9) | SoD R02 | 07 §7, §9 (`cash-banking.ts`) |
| UAT-O2C-257 | Bank — a distinct approver activates the account → banking proceeds (Dr bank / Cr 1000) (G9) | SoD R02 | 07 §7, §9 (`cash-banking.ts`) |
| UAT-O2C-258 | POS — void/refund exception report surfaces voids + refunds for independent review (G14) | SoD R08/R12 (detective; no new numbered control) | 07 §7, §9 (`refund-approval.ts`) |
| UAT-O2C-259 | Bank — statement import single-user by design; imported lines reviewed at reconciliation certification (G10, documentation) | REC-02 (compensating detective; no new control) | 07 §7, §9 (`bankrec.ts`) |
| UAT-O2C-260 | Customer master direct-edit + Oracle/NetSuite-grade fields (credit_terms/sales_rep/category/language/external_ref) + first web CRUD screen `/customers` | REV-15 (no new control) | 01 §7 (8b), rev 0.21 (`customers.ts`) |
| UAT-O2C-261 | Customer — multiple addresses, newest primary demotes the old one | REV-15 (no new control) | 01 §7 (8b), rev 0.22 (`customers.ts`) |
| UAT-O2C-262 | Customer — multiple contacts | REV-15 (no new control) | 01 §7 (8b), rev 0.22 (`customers.ts`) |
| UAT-O2C-263 | Customer — parent-company link (self-parent blocked); 360° surfaces addresses/contacts/parent together | REV-15 (no new control) | 01 §7 (8b), rev 0.22 (`customers.ts`) |
| UAT-O2C-264 | Customer — duplicate detection (tax-id/email/phone + fuzzy name) surfaces a steward review queue | REV-15 / DQM (no new control) | 01 §7 (8b), rev 0.23 (`customers.ts`) |
| UAT-O2C-265 | Customer — governed merge: child-row repoint + survivorship back-fill + soft-retire (record preserved) | REV-15 / DQM (no new control) | 01 §7 (8b), rev 0.23 (`customers.ts`) |
| UAT-O2C-266 | Customer — merge guards (self-merge, already-merged, merged rows leave the queue; MERGE_CONFLICT on collision) | REV-15 / DQM (no new control) | 01 §7 (8b), rev 0.23 (`customers.ts`) |
| UAT-O2C-267 | Customer — change history: create/onboarding event + field-level old→new + child changes (append-only, DB-enforced) | ITGC-AC-14 (strengthened; no new control) | 01 §7 (8b), rev 0.24 (`customers.ts`) |
| UAT-O2C-268 | Customer — sensitive columns masked in the change history | ITGC-AC-14 (strengthened; no new control) | 01 §7 (8b), rev 0.24 (`customers.ts`) |
| UAT-O2C-269 | Customer — address province canonicalised (77-province ref) + postal 5-digit validation | Data quality (no new control) | 01 §7 (8b), rev 0.25 (`customers.ts`) |
| UAT-O2C-270 | Customer — unrecognised province kept as entered (migration-safe) | Data quality (no new control) | 01 §7 (8b), rev 0.25 (`customers.ts`) |
| UAT-O2C-271 | Customer — typed party relationship (add + bidirectional outgoing/incoming visibility + self/dup guards) | REV-15 / TCA (no new control) | 01 §7 (8b), rev 0.26 (`customers.ts`) |
| UAT-O2C-272 | Customer — relationship delete removes it from both sides | REV-15 / TCA (no new control) | 01 §7 (8b), rev 0.26 (`customers.ts`) |
| UAT-O2C-273 | Customer — flexfields (Oracle DFF) round-trip on the master + governed 19-bank Thai reference | Extensibility / reference data (no new control) | 01 §7 (8b), rev 0.27 (`customers.ts`) |
| UAT-O2C-274 | Fine-casual booking — buffet mode + pre-picked tier + occasion; covers split per mode | Feature (no new control) | 20 §7 (14), rev 3.9 (`reservations.ts`) |
| UAT-O2C-275 | Buffet-package guards (mode mismatch / unknown / cross-tenant → `BAD_PACKAGE`) | Feature · ITGC-AC RLS (no new control) | 20 §7 (14), rev 3.9 (`reservations.ts`) |
| UAT-O2C-276 | Guest dining profile PDPA consent gate — no consent → no data + `CONSENT_REQUIRED`; save captures consent (audited); companion hard-delete | PDPA-01/02 · LYL-10c (no new control) | 20 §7 (14b), rev 3.9 (`reservations.ts`) |
| UAT-O2C-277 | Consent withdrawal hides the profile; DSAR export bundles + erasure hard-deletes it | PDPA-01 / PDPA-02 | 20 §7 (14b), rev 3.9 (`reservations.ts` + `pdpa.ts`) |
| UAT-O2C-278 | Guest profile PUT is merge-patch — omitted fields kept, explicit null clears (no silent wipe) | Data integrity (no new control) | 20 §7 (14b), rev 3.10 (`reservations.ts`) |
| UAT-O2C-279 | Guest-initiated consent request — LINE/SMS deep-link to /m self-service; already-granted sends nothing | PDPA consent · LYL-10c (no new control) | 20 §7 (14b), rev 3.11 (`reservations.ts`) |
| UAT-O2C-280 | Service-time flags — allergies on floor board + KDS tickets, read-time consent gate (withdrawal wipes instantly) | PDPA-01/02 (read-time minimization) | 20 §7 (14b), rev 3.11 (`reservations.ts`) |
| UAT-O2C-281 | Seat a buffet booking → offer to start the pre-picked tier (UI chain to `POST /tables/:id/buffet`) | Feature (no new control) | 20 §7 (14b), rev 3.11 (manual UI) |
| UAT-O2C-282 | Delivery open-orders pending list → `/delivery` SO dropdown (`GET /api/delivery/open-orders`) | Read-only feed (no new control) | 01 §7, rev 0.28 (`gaps.ts`) |
| UAT-O2C-283 | Till-sessions pending list → `/pos/close-of-day` Z-report dropdown (`GET /api/payments/till/sessions`) | Read-only feed (no new control; POS-07 unchanged) | 01 §7, rev 0.29 (`cashreport.ts`) |
| UAT-O2C-284 | Register menu survives an offline reload (on-device snapshot) | BRANCH-03 (capture resilience) | 24 §7 step 6, rev 0.3 (e2e `register-offline.spec.ts`) |
| UAT-O2C-285 | Quick-sale checkout auto-queues on a network-level failure (false-online) | BRANCH-03 | 24 §7 step 6, rev 0.3 (e2e `register-offline.spec.ts`) |
| UAT-O2C-320 | Offline dine-in: open+fire replays, kitchen gets the order, settlement stays online | BRANCH-03 | 24 §7 step 6, rev 0.9 (`restaurant-offline.ts`) |
| UAT-O2C-321 | Offline dine-in replay idempotent (no double-fire); add resolves by `order_uuid` | BRANCH-03 | 24 §7 step 6, rev 0.9 (`restaurant-offline.ts`) |
| UAT-O2C-322 | Offline dine-in: orphan add → `OFFLINE_ORDER_NOT_SYNCED`; tenant-scoped resolution; seq-ordered replay | BRANCH-03 | 24 §7 step 6, rev 0.9 (`restaurant-offline.ts`) |
| UAT-O2C-286 | LINE e-receipt to the sale's member — flex card + opaque public full-receipt link (POS-2) | REST-10 unchanged (delivery-channel feature — no new control) | 20 §7 (11), rev 3.12 (`receipts.ts`) |
| UAT-O2C-287 | LINE e-receipt negatives — `LINE_NOT_LINKED` / forged link `BAD_TOKEN` / prod `LINE_NOT_CONFIGURED` | REST-10 unchanged (delivery-channel feature — no new control) | 20 §7 (11), rev 3.12 (`receipts.ts`) |
| UAT-O2C-288 | Hub snapshot export — fail-closed / signed / credential-gated / FoH-only / tenant-scoped | BRANCH-02 (extension) | 24 §7 step 6b, rev 0.4 (`hub-snapshot.ts`) |
| UAT-O2C-289 | Hub import round-trip — tamper-reject, id-stable, idempotent; FoH works on the hub | BRANCH-02 (extension) · ITGC-AC-17 | 24 §7 step 6b, rev 0.4 (`hub-snapshot.ts`) |
| UAT-O2C-290 | Hub→cloud sales replay — exactly-once, cloud GL, tamper-rejected, skips visible | BRANCH-04 | 24 §7 step 6c, rev 0.5 (`hub-snapshot.ts`) |
| UAT-O2C-291 | Hub ingest fail-closed + replay fidelity (discount/tip/service charge) | BRANCH-04 | 24 §7 step 6c, rev 0.5 (`hub-snapshot.ts`) |
| UAT-O2C-292 | CRM-1 unified pipeline: one deal list through both route families | REV-17 | 18 §7 (2), rev 0.5 (pipeline.ts) |
| UAT-O2C-293 | Stage-transition audit trail (crm_stage_history) | REV-17 | 18 §7 (2), rev 0.5 (pipeline.ts) |
| UAT-O2C-294 | Won/lost terminal on the legacy /api/pipeline route | REV-17 | 18 §7 (2), rev 0.5 (pipeline.ts) |
| UAT-O2C-295 | CRM account/contact duplicate detection + steward force | REV-17 (ext.) | 18 §7 (1b), rev 0.5 (pipeline.ts) |
| UAT-O2C-296 | Account merge: maker-checker + survivor repoint | REV-17 (ext.) | 18 §7 (1b), rev 0.5 (pipeline.ts) |
| UAT-O2C-297 | CPQ quote ↔ unified opportunity link (incl. 0294 data-migration) | REV-17, CPQ-01 | 18 §7 (2)/(5), rev 0.5 (pipeline.ts) |
| UAT-O2C-298 | Cash application — one receipt across many invoices, remainder parks on-account (GL 2220) | REV-21 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-299 | Apply-on-account later clears 2220 into 1100; `INSUFFICIENT_UNAPPLIED` when exhausted | REV-21 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-300 | Over-applying an invoice rejected (`OVER_APPLIED`, pending applications count) | REV-21 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-301 | Allocations beyond the receipt rejected (`APPLY_EXCEEDS_RECEIPT`) | REV-21 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-302 | Cross-customer application rejected (`CUSTOMER_MISMATCH`) | REV-21 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-303 | ≥ THB 100k application parks PendingApproval; self-approve `SOD_VIOLATION`; distinct approver applies | REV-21 (mirrors REV-16) | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-304 | Reversal reason-required + audited; cash returns on-account; `ALREADY_REVERSED` on repeat | REV-21 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-305 | Issued AR-linked credit note applies as a credit line (`CN_*` guards; statement shows it) | REV-21, TAX-07 | 01 §7 (8e), rev 0.30 (`basics.ts`) |
| UAT-O2C-333 | AR/AP netting: agreement + preview (open AR 1300 vs AP 900 → net 900) | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-334 | Propose contra settlement parks PendingApproval — no GL / sub-ledger movement; GOV-01 surfaces it | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-335 | Proposer self-approval blocked (`SOD_VIOLATION`, binds even Admin) | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-336 | Different user approves → contra JE Dr 2000 / Cr 1100, both sub-ledgers clear, residual open; netting statement | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-337 | Netting without an enabled agreement rejected (`NETTING_NOT_AGREED` / `NETTING_DISABLED`) | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-338 | Net above the per-counterparty threshold rejected (`NETTING_EXCEEDS_THRESHOLD`) | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-339 | No open AP → `NOTHING_TO_NET`; blank reason → `REASON_REQUIRED` | REV-23 | 01 §7 (8f), rev 0.31 (`basics.ts`) |
| UAT-O2C-306 | Buffet-tier hub sale replays, re-priced from the cloud package master | BRANCH-04 | 24 §7 step 6c, rev 0.6 (`hub-snapshot.ts`) |
| UAT-O2C-307 | Diner QR self-order end-to-end ON the hub; replay on reconnect | BRANCH-04 · REST-08 | 24 §7 step 6c, rev 0.6 (`hub-snapshot.ts`) |
| UAT-O2C-308 | Hub till blocked until all its sales have replayed (completeness gate) | BRANCH-05 | 24 §7 step 6d, rev 0.7 (`hub-snapshot.ts`) |
| UAT-O2C-309 | Cloud-recomputed expected cash + 5830 over/short (materiality preserved) | BRANCH-05 · REV-13 · GL-05 | 24 §7 step 6d, rev 0.7 (`hub-snapshot.ts`) |
| UAT-O2C-310 | Hub heartbeat + fleet view (stale/backlog, tenant-isolated) | BRANCH-05 (detective) | 24 §7 step 6e, rev 0.7 (`hub-snapshot.ts`) |
| UAT-O2C-316 | Cash tip counts as drawer cash; card tip excluded (till expected cash) | REV-13 | 24 §7 step 6d, rev 0.9 (`cashreport.ts`) |
| UAT-O2C-317 | Heartbeat version channel (behind / ahead / current / unknown) | BRANCH-05 (detective) | 24 §7 step 6e, rev 0.7 (`hub-snapshot.ts`) |
| UAT-O2C-312 | CRM workspace kanban: drag persists stage move + history row (Won/Lost reason dialog) | REV-17 | 18 §7 (2b), rev 0.6 (pipeline.ts + manual UI) |
| UAT-O2C-313 | Deal page: unified timeline (activities + stage audit + quotes) + next-step task | REV-17 | 18 §7 (2b), rev 0.6 (pipeline.ts) |
| UAT-O2C-314 | Web-to-lead: public capture, silent honeypot drop, TENANT_REQUIRED, strict rate bucket | REV-17 (intake) | 18 §7 (2c), rev 0.6 (pipeline.ts) |
| UAT-O2C-315 | Lead CSV import: dry-run report, invalid rows skipped, LEAD- numbering | REV-17 (intake) | 18 §7 (2c), rev 0.6 (pipeline.ts) |
| UAT-O2C-316 | Customer 360: account joined to the money (AR open balance + overdue + credit limit; last payment) | REV-08/REV-12 | 18 §7 (2d), rev 0.7 (pipeline.ts) |
| UAT-O2C-317 | Customer 360: open deals + weighted forecast + the account's CPQ quote | REV-17, CPQ-03 | 18 §7 (2d), rev 0.7 (pipeline.ts) |
| UAT-O2C-318 | Customer 360: loyalty joined through the member-linked contact (RFM/tier/NPS/recovery) | REV-17 | 18 §7 (2d), rev 0.7 (pipeline.ts) |
| UAT-O2C-320 | KDS prep-time SLA aging bands (ok/warn/late by elapsed vs prep target) | Feature (KDS depth) — operational | 20 §7 (5), rev 3.14 (restaurant.ts) |
| UAT-O2C-321 | KDS expo / order-ready pass aggregates active lines by order (all_ready) | Feature (KDS depth) — operational | 20 §7 (5), rev 3.14 (restaurant.ts) |
| UAT-O2C-322 | KDS bump/recall all-day counts + station-load view (overdue, all-day roll-up) | Feature (KDS depth) — operational | 20 §7 (5), rev 3.14 (restaurant.ts) |
| UAT-O2C-340 | Seat-level ordering — order/roll-up/fire per seat + reassign (POS-9) | Feature (seat-level ordering) — operational | 20 §7 (1/2/5), rev 3.17 (restaurant.ts) |
| UAT-O2C-341 | Split by seat — one check per guest, own sale/receipt/GL (POS-9) | REST-06 | 20 §7 (8), rev 3.17 (splitbill.ts) |
| UAT-O2C-342 | Split by seat blocked while a line is unseated → `SEAT_UNASSIGNED` (POS-9) | REST-06 | 20 §7 (8), rev 3.17 (splitbill.ts) |
| UAT-O2C-026 | Cashier-speed quick-tender & change (UI) | Feature (cashier speed) | 01 §0 |
| UAT-O2C-027 | AR receipt idempotency | REC-01 / GL-01 | 01 §7 |
| UAT-O2C-028 | Diner pulls QR menu | REST-08 | 20 §7 |
| UAT-O2C-029 | Diner self-order auto-fires to KDS | REST-08 | 20 §7, §8 |
| UAT-O2C-030 | Second submit appends to same order | REST-08 | 20 §7 |
| UAT-O2C-031 | Freeform/priced self-order rejected | REST-08 | 20 §7, §9 |
| UAT-O2C-032 | 86'd item self-order blocked | REST-08 | 20 §7, §9 |
| UAT-O2C-033 | Menu/order on ended session rejected | REST-08 | 20 §7, §13 |
| UAT-O2C-034 | Admin creates a buffet tier | REST-09 | 20 §7 |
| UAT-O2C-035 | Diner starts buffet (per-pax charge + window) | REST-09 | 20 §7, §8 |
| UAT-O2C-036 | Buffet food ฿0 but hits KDS | REST-09 | 20 §7, §9 |
| UAT-O2C-037 | Off-tier buffet item rejected | REST-09 | 20 §7, §9, §13 |
| UAT-O2C-038 | One mode per session (no mixing) | REST-09 | 20 §7, §9, §13 |
| UAT-O2C-039 | Ordering after buffet window blocked | REST-09 | 20 §7, §9, §13 |
| UAT-O2C-040 | Overtime surcharge billed past window | REST-09 | 20 §7, §9 |
| UAT-O2C-041 | KDS flags ticket origin (diner / buffet) | REST-08, REST-09 | 20 §5 |
| UAT-O2C-042 | Buffet behaviour analytics per tier | Feature (buffet analytics) | 20 §6, §12 |
| UAT-O2C-043 | Printed table-QR sticker | Feature (printed QR) | 20 §6 |
| UAT-O2C-044 | Scan printed QR opens/joins session | REST-04 | 20 §6 |
| UAT-O2C-045 | PromptPay pay returns scannable QR | REST-04 | 20 §6 |
| UAT-O2C-046 | PromptPay settlement webhook (auth + finalize) | REST-04 | 20 §6, §13 |
| UAT-O2C-047 | Webhook idempotent + payment-status poll | REST-04 | 20 §6 |
| UAT-O2C-048 | Staff starts buffet from the POS | REST-09 | 20 §6 |
| UAT-O2C-049 | Public diner endpoint rate-limited | Anti-abuse | 20 §6, §13 |
| UAT-O2C-050 | Diner self-order UI smoke (Playwright) | Feature (diner UI) | 20 §6 |
| UAT-O2C-051 | Move a live tab to a free table | Feature (table ops) | 20 §6 |
| UAT-O2C-052 | Move onto an occupied table blocked | Feature (table ops) | 20 §6, §13 |
| UAT-O2C-053 | Transfer line items between tables | Feature (table ops) | 20 §6 |
| UAT-O2C-054 | Merge two tabs into a combined bill | Feature (table ops) | 20 §6 |
| UAT-O2C-055 | Fire one course (hold the rest) | Feature (course firing) | 20 §5 |
| UAT-O2C-056 | Fire next course / empty course | Feature (course firing) | 20 §5, §13 |
| UAT-O2C-057 | Day-parting: menu flags availability | Feature (day-parting) | 20 §6 |
| UAT-O2C-058 | Day-parting: order outside window blocked | Feature (day-parting) | 20 §6, §13 |
| UAT-O2C-059 | CRM: enrol member + messaging send | MKT-04 | 19 §7 |
| UAT-O2C-060 | CRM: marketing-consent enforced | MKT-04 | 19 §7, §9 |
| UAT-O2C-061 | Food-cost: per-menu margin from recipe | Feature (food-cost) | 20 §12 |
| UAT-O2C-062 | Food-cost: ingredient cost contribution | Feature (food-cost) | 20 §12 |
| UAT-O2C-063 | Checkout auto-queues a receipt print job | REST-10 | 20 §7 |
| UAT-O2C-064 | Receipt ties out to the fiscal sale | REST-10 | 20 §7, §9 |
| UAT-O2C-065 | Receipt renders seller header + VAT + items | REST-10 | 20 §7 |
| UAT-O2C-066 | HTML receipt document served | REST-10 | 20 §7 |
| UAT-O2C-067 | Agent pulls + acks the next print job | REST-10 | 20 §7 |
| UAT-O2C-068 | Reprint flagged a COPY (สำเนา) | REST-10 | 20 §7, §9 |
| UAT-O2C-069 | Send receipt out-of-band (email) | REST-10 | 20 §7 |
| UAT-O2C-070 | Print jobs tenant-isolated (RLS) | REST-10 | 20 §7 |
| UAT-O2C-071 | Register a cash-drawer device | REST-11 | 20 §7 |
| UAT-O2C-072 | Cash checkout auto-pops the drawer | REST-11 | 20 §7, §9 |
| UAT-O2C-073 | No-sale open kicks + audits the drawer | REST-11 | 20 §7, §9 |
| UAT-O2C-074 | Drawer reconciliation by reason | REST-11 | 20 §7, §9 |
| UAT-O2C-075 | Set + poll customer display | Feature (display) | 20 §7 |
| UAT-O2C-076 | Mark an item sold-by-weight | REST-11 | 20 §7 |
| UAT-O2C-077 | Scale computes price server-side | REST-11 | 20 §7, §9 |
| UAT-O2C-078 | Scale read on non-weighed item rejected | REST-11 | 20 §7, §9 |
| UAT-O2C-079 | Peripheral registry tenant-isolated (RLS) | REST-11 | 20 §7 |
| UAT-O2C-080 | Take a customer deposit | REST-12 | 20 §7 |
| UAT-O2C-081 | Apply a deposit (recognise revenue) | REST-12 | 20 §7 |
| UAT-O2C-082 | Deposit over-apply rejected | REST-12 | 20 §7, §9 |
| UAT-O2C-083 | Refund the unused deposit | REST-12 | 20 §7 |
| UAT-O2C-084 | Open house account + charge within limit | REST-12 | 20 §7 |
| UAT-O2C-085 | Charge over credit limit rejected | REST-12 | 20 §7, §9 |
| UAT-O2C-086 | Settle a house account (THB) | REST-12 | 20 §7 |
| UAT-O2C-087 | FX settlement books realised FX gain | REST-12 | 20 §7, §9 |
| UAT-O2C-088 | Statement reconciles + over-settle rejected | REST-12 | 20 §7, §9 |
| UAT-O2C-089 | Card surcharge quote + charge | REST-12 | 20 §7 |
| UAT-O2C-090 | Payments-depth tenant isolation (RLS) | REST-12 | 20 §7 |
| UAT-O2C-091 | Receipt defaults to tenant language | Feature (i18n) | 20 §7 |
| UAT-O2C-092 | Receipt renders in English | Feature (i18n) | 20 §7 |
| UAT-O2C-093 | Bilingual (TH/EN) receipt | Feature (i18n) | 20 §7 |
| UAT-O2C-094 | Per-tenant default language persists | Feature (i18n) | 20 §7 |
| UAT-O2C-095 | Reposition a table on the floor plan | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-096 | Delete a free table (soft-delete) | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-097 | Delete a seated table blocked | Feature (floor-plan layout) | 20 §6, §13 |
| UAT-O2C-098 | Create a VIP room (zone) | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-099 | Move/resize/rename a room persists | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-100 | Assign / un-assign a table to a room | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-101 | Delete a room keeps its tables | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-102 | Rooms are tenant-isolated (RLS) | Feature (floor-plan layout) | 20 §6, 08 §RLS |
| UAT-O2C-103 | Set table shape / size / rotation / seats | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-104 | Invalid shape / rotation rejected | Feature (floor-plan layout) | 20 §6, §13 |
| UAT-O2C-105 | Optimistic-locked table update (rev) | Feature (floor-plan layout) | 20 §6, §13 |
| UAT-O2C-106 | Unconditional update / undo (no rev) | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-107 | Create with full initial appearance | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-108 | Floor-plan editor UI smoke (Playwright) | Feature (floor-plan UI) | 20 §6 |
| UAT-O2C-109 | Revenue attributed by room | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-110 | Room revenue is tenant-isolated (RLS) | Feature (floor-plan layout) | 20 §6, 08 §RLS |
| UAT-O2C-111 | Revenue snapshot survives a table move | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-112 | Deleted room keeps its past takings | Feature (floor-plan layout) | 20 §6 |
| UAT-O2C-113 | Service charge persisted + itemised on the receipt | REST-10 | 20 §7 |
| UAT-O2C-114 | Large-party receipt ties out incl. service charge | REST-10 | 20 §7, §9 |
| UAT-O2C-115 | Send receipt via LINE channel | REST-10 | 20 §7 |
| UAT-O2C-116 | Enrol/link member via LINE (idempotent) | Feature (LINE CRM) | 19 §7.8 |
| UAT-O2C-117 | One LINE account = one member | Feature (LINE CRM) | 19 §7.8 |
| UAT-O2C-118 | LINE push to userId, consent enforced | Feature (LINE CRM), MKT-04 | 19 §7.11 |
| UAT-O2C-119 | Aggregator menu push (real adapter) | Feature (aggregator adapter) | 20 §7.7 |
| UAT-O2C-120 | Accept/reject routes to KDS + notifies platform | Feature (aggregator adapter) | 20 §7.7 |
| UAT-O2C-121 | Status callback + mock fallback | Feature (aggregator adapter) | 20 §7.7 |
| UAT-O2C-316 | Auto-86 out-of-stock sync to aggregators (deplete→86, idempotent, restock→un-86) | INV-14 | 20 §7.7, rev 3.14 (channel-adapter.ts) |
| UAT-O2C-317 | PromptPay store-level auto-reconciliation — inflow→tender match, unmatched→till exception, `pos_sell` denied | POS-08 | 07 §7 step 12, rev 1.8 (cash-banking.ts) |
| UAT-O2C-318 | PromptPay recon — late-inflow auto-resolve + manager clear of a till exception (`NOT_OPEN` guard) | POS-08 | 07 §7 step 12, rev 1.8 (cash-banking.ts) |
| UAT-O2C-328 | CRM-4 lead scoring: explainable versioned grade A–D + per-factor breakdown | REV-22 | 18 §7 (2b), rev 0.9 (pipeline.ts) |
| UAT-O2C-329 | CRM-4 pipeline events (deal.won etc.) into the no-code automation engine | REV-22 | 18 §7 (2b), rev 0.9 (pipeline.ts) |
| UAT-O2C-330 | CRM-4 follow-up SLA breach detection + clear-on-touch (leads touched in time) | REV-22 | 18 §7 (2b), rev 0.9 (pipeline.ts) |
| UAT-O2C-331 | CRM-4 follow-up settings + round-robin assignment + daily digest (crm_followup_digest) | REV-22 | 18 §7 (2b), rev 0.9 (pipeline.ts) |
| UAT-O2C-332 | CRM-4 send email/LINE from a deal: merge fields resolved + logged as an activity | REV-22 | 18 §7 (2b), rev 0.9 (pipeline.ts) |
| UAT-O2C-333 | CRM-6 outbound comms stamps a reply-threading token (`[ref:crmt_…]`, thread_token) | Feature (CRM-6 threading), REV-17 | 18 §7 (3c), rev 0.10 (pipeline.ts) |
| UAT-O2C-334 | CRM-6 signed inbound reply with the thread token → matched to the deal + logged as an inbound activity | Feature (CRM-6 inbound capture), REV-17 | 18 §7 (3c), rev 0.10 (pipeline.ts) |
| UAT-O2C-335 | CRM-6 inbound from a known contact email (no token) → matched; Message-ID redelivery deduped | Feature (CRM-6 inbound capture), REV-17 | 18 §7 (3c), rev 0.10 (pipeline.ts) |
| UAT-O2C-336 | CRM-6 unmatched inbound → review queue; manual link logs the activity + resolves | Feature (CRM-6 review queue), REV-17 | 18 §7 (3c), rev 0.10 (pipeline.ts) |
| UAT-O2C-337 | CRM-6 inbound with a bad HMAC signature → 401 BAD_INBOUND_SECRET (fail-closed; nothing journaled) | Feature (CRM-6 webhook authenticity), L-2 | 18 §7 (3c), rev 0.10 (pipeline.ts) |
| UAT-O2C-343 | Tip-adjust-after-auth — authorize → adjust tip pre-capture → capture bill+tip → 2300 tip liability | POS-10 | 20 §7 (15b), rev 3.17 (tips.ts) |
| UAT-O2C-344 | Tip-adjust window controls — over-ceiling (`TIP_OVER_LIMIT`) + post-capture (`TIP_ADJUST_CLOSED`) rejected; re-capture idempotent | POS-10 | 20 §7 (15b), §13, rev 3.17 (tips.ts) |
| UAT-O2C-345 | Tip-adjust ceiling env-configurable + append-only audit trail (`pos_tip_adjustments`) | POS-10 | 20 §7 (15b), rev 3.17 (tips.ts) |
| UAT-O2C-122 | Multi-terminal realtime KDS event | Feature (multi-terminal SSE) | 20 §rev3.0 |
| UAT-O2C-123 | LINE marketing automation — closed loop | Feature (LINE automation), MKT-04 | 19 §7.12 |
| UAT-O2C-130 | Collections worklist (open overdue AR) | REV-12 | 01 §7, §9 |
| UAT-O2C-131 | Dunning stage recommended by aging | REV-12 | 01 §7 |
| UAT-O2C-132 | Record dunning action advances stage | REV-12 | 01 §7, §9 |
| UAT-O2C-133 | Dunning on paid invoice rejected (ALREADY_PAID) | REV-12 | 01 §9, §13 |
| UAT-O2C-134 | Credit status flags over-limit + serious overdue | REV-12, R09 | 01 §7, §9 |
| UAT-O2C-135 | Credit check denies further credit (held customer) | REV-12, R09 | 01 §9, §13 |
| UAT-O2C-136 | Order entry blocks 90+ defaulter (CREDIT_OVERDUE) | REV-12 | 01 §7, §9 |
| UAT-O2C-137 | Order entry — over-limit still blocked (parity) | REV-08 | 01 §9, §13 |
| UAT-O2C-138 | Order entry — good-standing customer can order | REV-12 | 01 §7 |
| UAT-O2C-139 | Automated dunning sweep advances overdue invoices | REV-12 | 01 §7 |
| UAT-O2C-140 | Dunning sweep is idempotent | REV-12 | 01 §7 |
| UAT-O2C-141 | Schedule daily automated dunning | REV-12 | 01 §7 |
| UAT-O2C-142 | Scheduler tick fires the dunning job | REV-12 | 01 §7 |
| UAT-O2C-143 | Dunning action dispatches a notice to the customer | REV-12 | 01 §7 |
| UAT-O2C-144 | Sweep dispatches notices, channel auto-picked | REV-12 | 01 §7 |
| UAT-O2C-145 | Credit Manager places a manual hold | REV-08, R09 | 01 §7, §9 |
| UAT-O2C-146 | Credit check denies a manually-held customer (CREDIT_HOLD) | REV-08 | 01 §9, §13 |
| UAT-O2C-147 | Self-release blocked, second person releases (SOD_SELF_RELEASE) | REV-08, R09 | 01 §7, §9, §13 |
| UAT-O2C-148 | Credit-limit change is STAGED for approval (ceiling not moved) | REV-08, R09 | 01 §7, §9 |
| UAT-O2C-247 | Credit-limit change — requester cannot self-approve (SOD_VIOLATION) | REV-08, R09 | 01 §7, §9, §13 |
| UAT-O2C-248 | Credit-limit change — a distinct approver applies it (→ 50000) | REV-08, R09 | 01 §7, §9 |
| UAT-O2C-249 | Credit-change audit shows the limit_change old→new | REV-08, R09 | 01 §7, §9 |
| UAT-O2C-149 | Customer statement of account (running balance) | REV-12 | 01 §7 |
| UAT-O2C-150 | Customer statement — multi-currency (base + filter) | REV-12 | 01 §7 |
| UAT-O2C-151 | Register quick sale (tap → cash → receipt + drawer) | Feature (POS register) | 01 §7 |
| UAT-O2C-152 | Register modifier line priced from the catalog | Feature (POS register) | 01 §7 |
| UAT-O2C-153 | Register PromptPay tender shows a scannable QR | Feature (POS register) | 01 §7 |
| UAT-O2C-154 | Register hold → recall round-trips the cart | Feature (POS register) | 01 §7 |
| UAT-O2C-155 | Register mirrors the cart to the customer display | REST-11 | 20 §7 |
| UAT-O2C-180 | Create a customer-of-record linking B2C + B2B | REV-15 | 01 §7 |
| UAT-O2C-181 | 360 view ties AR to the sub-ledger + shows loyalty | REV-15 | 01 §7, §9 |
| UAT-O2C-182 | Customer master register search | REV-15 | 01 §7 |
| UAT-O2C-183 | Small refund immediate; large refund needs approval | REV-16 | 07 §7 |
| UAT-O2C-184 | Refund SoD: requester ≠ approver | REV-16 | 07 §7, §9 |
| UAT-O2C-185 | Lead → convert → customer-of-record + opportunity | REV-17 | 01 §7, §9 |
| UAT-O2C-186 | Opportunity stage machine (won terminal) | REV-17 | 01 §7, §9 |
| UAT-O2C-187 | Marking lost requires a reason | REV-17 | 01 §7, §9 |
| UAT-O2C-188 | Weighted pipeline forecast + win-rate | REV-17 | 01 §7 |
| UAT-O2C-189 | Log + list a CRM activity | REV-17 | 01 §7 |
| UAT-O2C-202 | Won opportunity → project (won-only, traceable, idempotent) | CRM-WL | 16 §7 (1a), §9 |
| UAT-O2C-203 | Project WBS — planned-hours-weighted % complete roll-up | Feature (P1 WBS) | 16 §7 (7) |
| UAT-O2C-204 | Milestone completion raises the Fixed-price progress bill | PROJ-02 | 16 §7 (8), §9 |
| UAT-O2C-205 | Resource assignment snapshots the authorized rate card | PROJ-05 | 16 §7 (9), §9 |
| UAT-O2C-206 | Capacity utilization flags over-allocation | PROJ-05 | 16 §7 (9), §9 |
| UAT-O2C-207 | Timesheet → project labor maker-checker posts to WIP | PROJ-04 | 16 §7 (10), §9 |
| UAT-O2C-208 | Earned-value metrics + task dependency guard | PROJ-06 | 16 §7 (11), §9 |
| UAT-O2C-209 | Critical-path schedule (CPM) + EVM S-curve | PROJ-06 | 16 §7 (11) |
| UAT-O2C-210 | Win/loss analytics for the pipeline dashboard | REV-17 | 16 §7 (11) |
| UAT-O2C-211 | Schedulable BI report types (project_evm, crm_win_loss) | PROJ-06 / REV-17 | 16 §7 (11) |
| UAT-O2C-212 | Portfolio command center rollup | PROJ-06 | 16 §7 (11) |
| UAT-O2C-317 | CRM-5 funnel conversion + velocity from stage history | REV-17 (operational) | 18 §7 (3b) |
| UAT-O2C-318 | CRM-5 source ROI — lead source → won revenue | REV-17 (operational) | 18 §7 (3b) |
| UAT-O2C-319 | CRM-5 forecast categories + quota attainment + activity leaderboard | REV-17 (operational) | 18 §7 (3b) |
| UAT-O2C-320 | CRM-5 win/loss date-bounded + BI report types registered | REV-17 (operational) | 18 §7 (3b) |
| UAT-O2C-213 | Change-controlled baselines + variance | PROJ-07 | 16 §7 (12) |
| UAT-O2C-214 | Project templates: author + one-step apply | (operational) | 16 §7 (13) |
| UAT-O2C-215 | RACI accountability + "my tasks" | PROJ-04 (SoD note) | 16 §7 (14) |
| UAT-O2C-216 | Risk & issue register + portfolio top-risks | PROJ-08 | 16 §7 (15) |
| UAT-O2C-217 | POC over-time revenue recognition | PROJ-09 | 16 §7 (16) |
| UAT-O2C-218 | Change order — maker-checker contract variation | PROJ-10 | 16 §7 (17) |
| UAT-O2C-219 | Time-phased resource capacity calendar | PROJ-05 (operational) | 16 §7 (9) |
| UAT-O2C-220 | Project health history (EVM/RAG trend) | PROJ-06 (operational) | 16 §7 (18) |
| UAT-O2C-221 | PMO action center / exception inbox | PROJ-11 | 16 §7 (19) |
| UAT-O2C-378 | Earned schedule — SPI(t)/SV(t) catches the slip the classic SPI hides | PROJ-19 | 16 §7 (30) |
| UAT-O2C-222 | Pipeline-weighted forward resource & cash forecast | PROJ-05/PROJ-06 (operational) | 16 §7 (20) |
| UAT-O2C-223 | Period governance / status pack | PROJ-06 (operational) | 16 §7 (21) |
| UAT-O2C-224 | Program (cross-project) critical path | PROJ-06 (operational) | 16 §7 (22) |
| UAT-O2C-225 | Pipeline → FTE resourcing forecast | PROJ-05/PROJ-06 (operational) | 16 §7 (20) |
| UAT-O2C-226 | Project period-end close review — maker-checker (`/projects/close`) | PROJ-03 | 16 §7, §9 |
| UAT-O2C-227 | Timesheet project allocation + approval surfaced in list (`/hcm`) | PROJ-04 | 16 §7 (10), §9 |
| UAT-O2C-229 | Bill of Quantities (BoQ) + project-dimensioned procurement (M0) | maker-checker (BoQ approve); PROJ-12 (M1) | 16 §7 (23–24) |
| UAT-O2C-230 | Material-budget commitment enforcement (M1) | PROJ-12 | 16 §7 (25) |
| UAT-O2C-231 | Material requisition + over-budget LINE approval (M2) | PROJ-13 | 16 §7 (26) |
| UAT-O2C-232 | Stock reservation → issue-to-project (M3) | INV-13 | 16 §7 (27) |
| UAT-O2C-233 | Project-linked advances & reimbursements — site cash (M4) | PROJ-14 | 16 §7 (28) |
| UAT-O2C-234 | Raise site cash from the project workspace — web (FU4) | PROJ-14 | 16 §7 (28) |
| UAT-O2C-243 | Retention (เงินประกันผลงาน) shared sub-ledger — withhold / release / due (Phase 0) | Foundation for PROJ-16/PROJ-17 | 16 §14 (rev 0.35), 35 P0 |
| UAT-O2C-244 | Progress billing / งวดงาน — BoQ-line valuation, maker-checker certify, retention receivable (P1) | PROJ-16, SoD R17 | 16 §7 (29) / §14 (rev 0.36), 35 P1 |
| UAT-O2C-245 | Subcontractor management — subcontract vs BoQ budget, valuation certify, retention payable (P2) | PROJ-17, SoD R18 | 16 §7 (30) / §14 (rev 0.37), 35 P2 |
| UAT-O2C-246 | Tender / estimating → award — seed a project + draft BoQ from the winning bid (P3) | PROJ-18 | 16 §7 (31) / §14 (rev 0.38), 35 P3 |
| UAT-RE-01 | Real-estate unit inventory — no double allocation; availability grid ties out (P4) | RE-01 | 31 §3–4, 35 P4 (D1) |
| UAT-RE-02 | Real-estate sale contract — price/discount authority (maker-checker) (P4) | RE-02, SoD R19 | 31 §3–4, 35 P4 (D2) |
| UAT-RE-03 | Real-estate installment application — pay-once, exact amount (P4) | RE-03 | 31 §3–4, 35 P4 (D2) |
| UAT-RE-04 | Real-estate ownership transfer — settled-only, authorised revenue recognition (P5) | RE-04 | 31 §rev0.2, 35 P5 |
| UAT-RE-05 | RE developments + contracts pending lists → `/realestate` dropdowns | Read-only feed (no new control) | 31 §7, rev 0.3 (`projects.ts`) |
| UAT-O2C-194 | AR allowance: aging compute | REV-18 | 01 §7 (8d) |
| UAT-O2C-195 | AR allowance: computer cannot post own (SoD) | REV-18 | 01 §7 (8d) |
| UAT-O2C-196 | AR allowance: independent post books the delta (Dr 5720 / Cr 1190) | REV-18 | 01 §7 (8d) |
| UAT-O2C-197 | AR allowance: no double-post; register | REV-18 | 01 §7 (8d) |
| UAT-O2C-318 | Restaurant sale enters the fiscal hash chain; tampering detected | ITGC-AC-11 | 20 rev 3.8 (`restaurant.ts`) |
| UAT-O2C-319 | Hub kitchen waste replays exactly once (5810/1200, INV-07 guard preserved) | BRANCH-06 | 24 §7 step 6f, rev 0.9 (`hub-snapshot.ts`) |
| UAT-O2C-346 | Hub stocktake replay preserves SoD R11 (self-posted sheet blocked) | BRANCH-07 · R11 / INV-04 | 24 §7 step 6g, rev 0.10 (`hub-snapshot.ts`) |
| UAT-O2C-347 | Menu screen 86 ปิดขาย/เปิดขาย toggle — dish off sale everywhere (`ITEM_UNAVAILABLE`) + reversible | REST-08 | 20 §7 (restaurant.ts) |
| UAT-O2C-348 | Menu item แก้ไข reprices/renames the catalog in place; next order re-resolves new price | Feature (menu edit) — operational | 20 §7 (restaurant.ts) |
| UAT-O2C-349 | CPQ within-floor quote sends normally | CPQ-01 | 18 §7 (6), rev 0.11 (cpq-approval.ts) |
| UAT-O2C-350 | CPQ margin-floor breach → PendingApproval + accept blocked | CPQ-01 | 18 §7 (6), §13, rev 0.11 (cpq-approval.ts) |
| UAT-O2C-351 | CPQ discount approval — self-approve blocked; distinct approver → Sent→Accepted+AR | CPQ-01, R20 | 18 §7 (6b), §13, rev 0.11 (cpq-approval.ts) |
| UAT-O2C-352 | CPQ discount-ceiling breach → reject returns to Draft | CPQ-01, R20 | 18 §7 (6b), §13, rev 0.11 (cpq-approval.ts) |
| UAT-O2C-353 | CPQ approvals tenant-isolated (RLS) | CPQ-01, AUD-ARC-01 | 18 §7 (6), rev 0.11 (cpq-approval.ts) |
| UAT-O2C-354 | CRM-7 set an account parent → hierarchy link | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-355 | CRM-7 hierarchy cycle blocked (`HIERARCHY_CYCLE`) | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-356 | CRM-7 subtree pipeline roll-up (child deal rolls up to parent) | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-357 | CRM-7 buying-committee add + `COMMITTEE_DUP` / `CONTACT_ACCOUNT_MISMATCH` guards | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-358 | CRM-7 committee list (contact_name joined) + remove | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-359 | CRM-7 account-plan create + target-category validation (`UNKNOWN_CATEGORY`) | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-360 | CRM-7 account-plan lifecycle (`PLAN_NOT_DRAFT`/`PLAN_INCOMPLETE`/`PLAN_NOT_ACTIVE`) | CRM-07 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-361 | CRM-7 whitespace coverage + account-plan RLS isolation | CRM-07, AUD-ARC-01 | 18 §7.9, rev 0.12 (pipeline.ts) |
| UAT-O2C-362 | CRM-15 health: engaged account → healthy band + explainable breakdown | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-363 | CRM-15 health: escalated + SLA-breached cases, no pipeline → at_risk | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-364 | CRM-15 deal-type renewal + renewal/expansion pipeline (weighted) | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-365 | CRM-15 renewal gap (won account, no open renewal) flagged | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-366 | CRM-15 churn watchlist ranked worst-first + band filter/counts | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-367 | CRM-15 health snapshot + idempotent per-account/day trend history | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-368 | CRM-15 `crm_account_health` BI report type registered | CRM-08 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-369 | CRM-15 account-health RLS isolation (T1 can't read HQ health) | CRM-08, AUD-ARC-01 | 18 §7.10, rev 0.13 (pipeline.ts) |
| UAT-O2C-370 | CRM-12 rep forecast submission (draft→submitted) | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-371 | CRM-12 manager roll-up: system-weighted vs submitted + variance | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-372 | CRM-12 pipeline-coverage ratio (open pipeline ÷ commit target) | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-373 | CRM-12 category waterfall builds to the system forecast | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-374 | CRM-12 forecast snapshot captures forecast + period actual-won | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-375 | CRM-12 forecast-vs-actual accuracy trend (idempotent per period/day) | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-376 | CRM-12 `crm_forecast_snapshot` BI report type registered | CRM-09 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-377 | CRM-12 forecast submissions RLS isolation (T1 can't read HQ) | CRM-09, AUD-ARC-01 | 18 §7.11, rev 0.14 (pipeline.ts) |
| UAT-O2C-378 | CRM-11 create a territory hierarchy (parent + child) | CRM-10 | 18 §7.12, rev 0.15 (pipeline.ts) |
| UAT-O2C-379 | CRM-11 assign a rep to a territory | CRM-10 | 18 §7.12, rev 0.15 (pipeline.ts) |
| UAT-O2C-380 | CRM-11 owner attainment vs a persisted quota | CRM-10 | 18 §7.12, rev 0.15 (pipeline.ts) |
| UAT-O2C-381 | CRM-11 territory subtree roll-up vs the territory quota | CRM-10 | 18 §7.12, rev 0.15 (pipeline.ts) |
| UAT-O2C-382 | CRM-11 territory read exposes members | CRM-10 | 18 §7.12, rev 0.15 (pipeline.ts) |
| UAT-O2C-383 | CRM-11 territories RLS isolation (T1 can't see HQ) | CRM-10, AUD-ARC-01 | 18 §7.12, rev 0.15 (pipeline.ts) |
| UAT-O2C-384 | CRM-8 create a 2-step sequence (cadence) | CRM-11 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-385 | CRM-8 enroll a lead into a sequence | CRM-11 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-386 | CRM-8 advance step 1 (email) logs a sequence activity | CRM-11 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-387 | CRM-8 last step completes; re-advance rejected | CRM-11 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-388 | CRM-8 due-runner advances due enrolments | CRM-11 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-389 | CRM-8 stop an enrolment + `crm_sequence_run` registered | CRM-11 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-390 | CRM-8 sequences RLS isolation (T1 can't see HQ) | CRM-11, AUD-ARC-01 | 18 §7.13, rev 0.16 (pipeline.ts) |
| UAT-O2C-391 | CRM-14 create a bundle from existing configs | CRM-12 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-392 | CRM-14 expand a bundle into a Draft quote (instance-tagged lines) | CRM-12 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-393 | CRM-14 bundle's blended margin governed by the unmodified CPQ-01 floor | CRM-12, CPQ-01 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-394 | CRM-14 manager-tier approver clears a margin-only breach | CRM-12 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-395 | CRM-14 exec-ceiling breach blocks a manager-tier approver (`TIER_APPROVAL_REQUIRED`) | CRM-12 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-396 | CRM-14 exec-tier approver clears the exec-ceiling breach | CRM-12 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-397 | CRM-14 guided-selling recommendations + bundle RLS isolation | CRM-12, AUD-ARC-01 | 18 §7.14, rev 0.17 (cpq-approval.ts) |
| UAT-O2C-398 | PROJ-20 tag a named skill | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-399 | PROJ-20 calendar validation — bad month / out-of-range % | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-400 | PROJ-20 set a monthly availability override | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-401 | PROJ-20 calendar-aware over-allocation (60% vs a documented 50% ceiling) | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-402 | PROJ-20 a generic (untagged) booking reads named=false | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-403 | PROJ-20 role/skill supply-vs-demand — understaffed role | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-404 | PROJ-20 understaffed_role_count surfaces the gap | PROJ-20 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-405 | PROJ-20 existing capacity/utilization/portfolio/forecast callers unaffected (regression) | PROJ-20, PROJ-05 | 16 §31, rev 0.47 (projects.ts) |
| UAT-O2C-406 | PROJ-21 SS+lag dependency scheduling | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-407 | PROJ-21 FF+lag dependency scheduling | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-408 | PROJ-21 FS+lead dependency scheduling | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-409 | PROJ-21 critical path/slack correctness with mixed dependency types | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-410 | PROJ-21 self-dependency rejected | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-411 | PROJ-21 SNET floors the early start | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-412 | PROJ-21 FNLT caps the late finish vs an unconstrained twin | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-413 | PROJ-21 working calendar — disabled vs enabled duration | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-414 | PROJ-21 calendar holiday exception round-trip | PROJ-21 | 16 §32, rev 0.48 (projects.ts) |
| UAT-O2C-415 | PROJ-22 with no manual ETC entries, eac-scenarios reports the formulaic EAC only | PROJ-22 | 16 §33, rev 0.49 (projects.ts) |
| UAT-O2C-416 | PROJ-22 an ETC submitted against a task from another project | PROJ-22 | 16 §33, rev 0.49 (projects.ts) |
| UAT-O2C-417 | PROJ-22 a per-task ETC entry drives the bottom-up EAC + variance | PROJ-22 | 16 §33, rev 0.49 (projects.ts) |
| UAT-O2C-418 | PROJ-22 a second entry for the SAME task supersedes the first | PROJ-22 | 16 §33, rev 0.49 (projects.ts) |
| UAT-O2C-419 | PROJ-22 a project-level entry sums alongside a per-task entry | PROJ-22 | 16 §33, rev 0.49 (projects.ts) |
| UAT-O2C-420 | PROJ-22 existing evm()/schedule() callers are unaffected by ETC entries (regression) | PROJ-22, PROJ-06 | 16 §33, rev 0.49 (projects.ts) |
| UAT-O2C-421 | PROJ-23 over-allocation detected within a project's own assignments | PROJ-23 | 16 §34, rev 0.50 (projects.ts) |
| UAT-O2C-422 | PROJ-23 a positive-slack contributor is suggested for shifting | PROJ-23 | 16 §34, rev 0.50 (projects.ts) |
| UAT-O2C-423 | PROJ-23 the slack-0 (critical-path) contributor is never suggested | PROJ-23 | 16 §34, rev 0.50 (projects.ts) |
| UAT-O2C-424 | PROJ-23 an over-allocation with no slack-bearing contributor is flagged NO_SLACK | PROJ-23 | 16 §34, rev 0.50 (projects.ts) |
| UAT-O2C-425 | PROJ-23 over_allocated_count reflects every over-allocated resource | PROJ-23 | 16 §34, rev 0.50 (projects.ts) |
| UAT-O2C-426 | PROJ-23 a project with no over-allocated resource reports empty results (regression) | PROJ-23, PROJ-06, PROJ-20 | 16 §34, rev 0.50 (projects.ts) |
| UAT-O2C-427 | CRM-7 configure a stage playbook (WIP limit + required field) | CRM-13 | 18 §7.15, rev 0.18 (pipeline.ts) |
| UAT-O2C-428 | CRM-7 required-field exit criteria blocks an incomplete move | CRM-13 | 18 §7.15, rev 0.18 (pipeline.ts) |
| UAT-O2C-429 | CRM-7 a complete deal satisfies the exit criteria | CRM-13, REV-17 | 18 §7.15, rev 0.18 (pipeline.ts) |
| UAT-O2C-430 | CRM-7 WIP limit refuses a move past the cap | CRM-13 | 18 §7.15, rev 0.18 (pipeline.ts) |
| UAT-O2C-431 | CRM-7 bulk stage move reports per-item success/failure | CRM-13 | 18 §7.15, rev 0.18 (pipeline.ts) |
| UAT-O2C-432 | CRM-7 playbook config is supervisor-gated + tenant-scoped | CRM-13, AUD-ARC-01 | 18 §7.15, rev 0.18 (pipeline.ts) |
| UAT-O2C-433 | CRM-8 post an append-only team note with an @mention | CRM-14 | 18 §7.16, rev 0.19 (pipeline.ts) |
| UAT-O2C-434 | CRM-8 an unknown @handle is dropped, a valid one kept | CRM-14 | 18 §7.16, rev 0.19 (pipeline.ts) |
| UAT-O2C-435 | CRM-8 unified timeline merges every source, newest-first | CRM-14, REV-17 | 18 §7.16, rev 0.19 (pipeline.ts) |
| UAT-O2C-436 | CRM-8 @mention routes a directed notification | CRM-14 | 18 §7.16, rev 0.19 (pipeline.ts) |
| UAT-O2C-437 | CRM-8 BOLA — timeline/feed on an unknown entity | CRM-14, AUD-ARC-01 | 18 §7.16, rev 0.19 (pipeline.ts) |
| UAT-O2C-438 | CRM-8 account timeline rolls up its opportunities | CRM-14 | 18 §7.16, rev 0.19 (pipeline.ts) |
| UAT-O2C-439 | CRM-15 create a pricebook + entries (master-data gated) | CRM-15 | 18 §7.17, rev 0.20 (cpq-approval.ts) |
| UAT-O2C-440 | CRM-15 a quote prices from the pricebook; basis recorded | CRM-15 | 18 §7.17, rev 0.20 (cpq-approval.ts) |
| UAT-O2C-441 | CRM-15 config-path quote prices from the pricebook | CRM-15 | 18 §7.17, rev 0.20 (cpq-approval.ts) |
| UAT-O2C-442 | CRM-15 the CPQ-01 floor still governs a pricebook price | CRM-15, CPQ-01 | 18 §7.17, rev 0.20 (cpq-approval.ts) |
| UAT-O2C-443 | CRM-15 effective-window / active / existence guards | CRM-15 | 18 §7.17, rev 0.20 (cpq-approval.ts) |
| UAT-O2C-444 | CRM-15 pricebooks are tenant-isolated | CRM-15, AUD-ARC-01 | 18 §7.17, rev 0.20 (cpq-approval.ts) |
| UAT-O2C-445 | CRM-17 a complete account scores good | CRM-16 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-446 | CRM-17 a sparse account scores poor with missing fields | CRM-16 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-447 | CRM-17 validity — present ≠ valid | CRM-16 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-448 | CRM-17 worklist worst-first + band counts + snapshot | CRM-16 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-449 | CRM-17 duplicate surveillance surfaces a near-dup pair | CRM-16 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-450 | CRM-17 the maker-checked merge is audited | CRM-16, REV-17 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-451 | CRM-17 DQ worklist is tenant-isolated | CRM-16, AUD-ARC-01 | 18 §7.18, rev 0.21 (pipeline.ts) |
| UAT-O2C-452 | PROJ-24 simulate a pending change order's margin/EAC impact | PROJ-24, PROJ-10, PROJ-06 | 16 §35, rev 0.51 (projects.ts) |
| UAT-O2C-453 | PROJ-24 the simulation writes nothing (read-only) | PROJ-24 | 16 §35, rev 0.51 (projects.ts) |
| UAT-O2C-454 | PROJ-24 approval reproduces the simulated figures exactly | PROJ-24, PROJ-10, SoD R07 | 16 §35, rev 0.51 (projects.ts) |
| UAT-O2C-455 | PROJ-24 an already-decided change order can't be simulated | PROJ-24 | 16 §35, rev 0.51 (projects.ts) |
| UAT-O2C-456 | PROJ-24 a task-anchored project simulates on the tasks BAC basis | PROJ-24, PROJ-06 | 16 §35, rev 0.51 (projects.ts) |
| UAT-O2C-457 | PROJ-25 build a selection scenario and see the over-envelope roll-up | PROJ-25 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-458 | PROJ-25 an unknown candidate project is rejected | PROJ-25 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-459 | PROJ-25 the scenario author cannot commit their own scenario | PROJ-25, SoD R07 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-460 | PROJ-25 a different user cannot commit over the envelope without an override | PROJ-25 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-461 | PROJ-25 an exec override commits the over-envelope scenario and locks it | PROJ-25 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-462 | PROJ-25 a committed scenario is read-only | PROJ-25 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-463 | PROJ-25 a within-envelope scenario commits with no override | PROJ-25 | 16 §36, rev 0.52 (projects.ts) |
| UAT-O2C-464 | PROJ-26 a fresh project starts at the concept phase | PROJ-26 | 16 §37, rev 0.53 (projects.ts) |
| UAT-O2C-465 | PROJ-26 submit a phase gate for review | PROJ-26 | 16 §37, rev 0.53 (projects.ts) |
| UAT-O2C-466 | PROJ-26 only one gate may be pending at a time | PROJ-26 | 16 §37, rev 0.53 (projects.ts) |
| UAT-O2C-467 | PROJ-26 the gate submitter cannot decide their own gate | PROJ-26, SoD R07 | 16 §37, rev 0.53 (projects.ts) |
| UAT-O2C-468 | PROJ-26 an independent GO advances the project | PROJ-26 | 16 §37, rev 0.53 (projects.ts) |
| UAT-O2C-469 | PROJ-26 a decided gate cannot be re-decided; a backward gate is rejected | PROJ-26 | 16 §37, rev 0.53 (projects.ts) |
| UAT-O2C-470 | PROJ-26 a HOLD records the decision without advancing | PROJ-26 | 16 §37, rev 0.53 (projects.ts) |

## 03 — Procure-to-Pay → `02-procure-to-pay.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-P2P-001 | Create PO | EXP-01 | 02 §7 |
| UAT-P2P-002 | PO approval (maker≠checker) | EXP-01, R02 | 02 §9 |
| UAT-P2P-003 | Goods receipt | EXP-02 | 02 §7 |
| UAT-P2P-004 | 3-way match success | EXP-03 | 02 §7, §9 |
| UAT-P2P-005 | Matched pay (request+approve) + GL | EXP-06, GL-01 | 02 §7 |
| UAT-P2P-006 | Price variance block at AP-pay gate (MATCH_BLOCKED) | EXP-09, EXP-01 | 02 §8, §9, §13 |
| UAT-P2P-007 | Over-invoice block | EXP-03 | 02 §9, §13 |
| UAT-P2P-008 | Match tolerance | EXP-03 | 02 §9 |
| UAT-P2P-009 | Override-with-reason (independent overrider) | EXP-01, EXP-06, R04 | 02 §6, §9 |
| UAT-P2P-009b | Matcher cannot self-override (SoD) | EXP-01 | 02 §6 |
| UAT-P2P-010 | Override reset on re-match | EXP-03 | 02 §9, §13 |
| UAT-P2P-011 | Non-PO bill (AP-pay gate fails open, request+approve) | EXP-09, EXP-06 | 02 §7, §8 |
| UAT-P2P-012 | Blocklisted vendor (SUPPLIER_BLOCKED) | EXP-04, R13 | 02 §9, §13 |
| UAT-P2P-013 | Un-blocklist vendor | EXP-04 | 02 §7 |
| UAT-P2P-014 | RFQ→quote→award→PO | EXP-01 | 02 §7 |
| UAT-P2P-015 | Match idempotency | EXP-03 | 02 §7 |
| UAT-P2P-016 | RLS vendor isolation | ITGC-AC (RLS) | 08 §9 |
| UAT-P2P-017 | AP↔GL reconciliation | REC-01 | 04 §9 |
| UAT-P2P-018 | Supplier portal: vendor sees only own POs | Feature (supplier portal) | 02 §7 |
| UAT-P2P-019 | Supplier acknowledge + submit invoice | Feature (supplier portal) | 02 §7 |
| UAT-P2P-020 | Supplier cannot invoice another vendor's PO | Feature (supplier portal) | 02 §7 |
| UAT-P2P-021 | Supplier portal unlinked user refused | Feature (supplier portal) | 02 §7 |
| UAT-P2P-022 | AP bill idempotency | EXP-03 / GL-01 | 02 §7 |
| UAT-P2P-023 | AP payment-request idempotency | EXP-06 / GL-01 | 02 §7 |
| UAT-P2P-024 | Dimension routing — PO by vendor | EXP-03 (workflow) | 02 §7 |
| UAT-P2P-025 | PO approval routes through the engine | EXP-03 (workflow) | 02 §7 |
| UAT-P2P-026 | SLA escalation sweep flags + reminds | EXP-03 (workflow) | 02 §7 |
| UAT-P2P-027 | Escalation fallback approver can act | EXP-03 (workflow) | 02 §7 |
| UAT-P2P-028 | No-code builder replaces steps | Feature (workflow builder) | 02 §7 |
| UAT-P2P-029 | Vendor statement of account | EXP-06 | 02 §7 |
| UAT-P2P-030 | Petty cash — issue an advance | EXP-07 | 07 §7 |
| UAT-P2P-031 | Petty cash — settle reconciles or rejects | EXP-07 | 07 §7 |
| UAT-P2P-029 | Pre-paid bill creation blocked | EXP-06 | 02 §7, §9 |
| UAT-P2P-030 | Payment request creates no GL/paid effect | EXP-06 | 02 §7, §9 |
| UAT-P2P-031 | Requester self-approval blocked (SoD, incl. Admin) | EXP-06, R03/R07 | 02 §7, §9 |
| UAT-P2P-032 | Maker without approval authority blocked | EXP-06 | 02 §7, §9 |
| UAT-P2P-033 | Reject leaves bill unpaid | EXP-06 | 02 §7, §9 |
| UAT-P2P-060 | PR raised by any employee (company-wide `pr_raise`) | R03 (access design) | 02 §3, §6, §7 |
| UAT-P2P-061 | GR refused for a procurement-only user (R04 at permission layer) | R04 | 02 §7, §9 |
| UAT-P2P-062 | AP disbursement approved on finance Disbursements screen | EXP-06, R07 | 02 §3, §7 |
| UAT-P2P-066 | Vendor PII encrypted at rest; ghost-vendor detector still fires | ITGC-AC-19, EXP-02 | 02 §9 |
| UAT-P2P-070 | LINE chat: staff links LINE account with one-time code | EXP-03 (entry integrity) | 02 §3, §7 |
| UAT-P2P-071 | LINE chat: `pr` command raises a PR into the approval workflow | EXP-03 | 02 §7 |
| UAT-P2P-072 | LINE chat: unlinked / expired-code identities refused | EXP-03 (entry integrity) | 02 §7, §9 |
| UAT-P2P-073 | LINE chat: no `pr_raise` → refused; customer role gets no link code | EXP-03, R03 (access design) | 02 §7, §9 |
| UAT-P2P-074 | LINE chat: redelivery deduped; LINE account binds to one user | EXP-03 (entry integrity) | 02 §7, §9 |
| UAT-P2P-075 | LINE chat: free customer chat ignored; unlink stops the channel | EXP-03 (entry integrity) | 02 §7 |
| UAT-P2P-076 | LINE notify: approver queue-entry push; requester decision push | EXP-03 (workflow) | 02 §7 |
| UAT-P2P-077 | LINE chat: self-approve over chat refused (engine SoD) | EXP-03, R07 | 02 §3, §7, §9 |
| UAT-P2P-078 | LINE chat: approve without `procurement` refused; reject decides + notifies | EXP-03 | 02 §7, §9 |
| UAT-P2P-079 | LINE chat: `my prs` / `find` self-service lookups | Feature (chat self-service) | 02 §7 |
| UAT-P2P-080 | LINE chat: `cancel` own pending PR only; `stock` read-only | EXP-03 (entry integrity) | 02 §7 |
| UAT-P2P-081 | PO attachments: web upload/list/fetch; delete = uploader-or-Admin | EXP-01 (evidence) | 02 §7, §11 |
| UAT-P2P-082 | LINE chat: attach photo to PO; replay/stray/permission negatives | EXP-01 (evidence) | 02 §7 |
| UAT-P2P-083 | LINE chat: one-tap postback approve with confirm (replay-safe; SoD binds) | EXP-03, R07 | 02 §7 |
| UAT-P2P-084 | LINE chat: petty-cash raise + EXP-08 notifications (decision stays on web) | EXP-08, R07 | 07 §7 |
| UAT-SEC-048 | LINE chat governance: link registry, force-unlink, rate limit | ITGC-AC (chat channel) | 08 §7 |
| UAT-SEC-049 | LINE OA go-live: required Channel secret + webhook receipt health (fail-closed verify) | ITGC-AC (chat channel) | 08 rev 1.8 |
| UAT-SEC-050 | LINE OA go-live: test-push to the admin's own linked LINE (NOT_LINKED explained) | ITGC-AC (chat channel) | 08 rev 1.8 |
| UAT-SEC-051 | Platform-owner "god" sees all companies cross-org; per-tenant Admin stays org-scoped (env-gated, not a role) | ITGC-AC-18 | 01 §7 |
| UAT-PAY-038 | ESS leave raised from LINE chat + pushes (decision stays on web) | PAY (ESS leave) | 25 §7 |
| UAT-PAY-039 | LINE copilot AI-drafted leave — confirm-first, same ESS path | PAY (ESS leave; AI drafts) | 25 rev 1.0 |
| UAT-RPT-046 | LINE report delivery + daily digest + alert user-target (permission-at-subscribe) | Feature (LINE delivery) | 26 §7 |
| UAT-RPT-047 | LINE ask — governed NL analytics in chat (permission gate) | Feature (NL analytics) | 26 §7 |
| UAT-RPT-048 | LINE copilot LLM governance — schema validation, daily cap, DPA gate | PN-26 rev 1.8 | 26 rev 1.8 |
| UAT-RPT-049 | Digest 2.0 — KPI selection + per-recipient permission filter at send time | PN-26 rev 1.9 | 26 rev 1.9 |
| UAT-RPT-052 | Per-tenant AI opt-out — PDPA disclosure + right to object (chat blocks, other surfaces degrade) | PN-08 §8d (PDPA) | 08 rev 0.5 |
| UAT-TAX-049 | RD e-Filing attachments — ภ.ง.ด.3/53 ใบแนบ .txt (TIS-620/BE/ร้อยละ) + VAT CSV working papers | TAX-03/04/05 (filing artifacts) | 06 rev 0.24 |
| UAT-GL-131 | Bank-statement file import — Thai bank CSV/XLSX, BE→CE dates, fail-closed parse, auto-match on import | REC-02 / BANK-02 (import spine) | 07 rev 1.6 |
| UAT-GL-132 | Dimension-filtered TB / account-ledger / P&L — project/dept/branch slice shows only tagged lines, ties across reports | GL-13 (dimension review — no new control) | 04 rev 2.14 |
| UAT-GL-133 | Unfiltered TB byte-identical (snapshot path) + junk dimension id → 400 BAD_QUERY + `GET /api/ledger/dimensions` labels | GL-13 (no new control) | 04 rev 2.14 |
| UAT-P2P-085 | LINE copilot — AI-drafted PR is confirm-first (no action without ยืนยัน) | EXP-03 (entry integrity) | 02 §7 |
| UAT-P2P-092 | LINE copilot AI-drafted expense/advance — confirm-first, EXP-07/08 path | EXP-07/08 (AI drafts) | 07 rev 1.3 |
| UAT-P2P-093 | Vendor AP card — list → statement of account | EXP-01 / AP | 05-finance-ar-ap.md |
| UAT-P2P-094 | Convert approved PR → PO (item reconcile against master + open new code) | EXP (PR→PO link) | 02 rev 2.3 |
| UAT-P2P-111 | PR→PO auto-group by supplier (1 PR → many POs) + item ผู้ขายประจำ (preferred supplier) + item names | EXP-02, EXP-03, R02/R07 (unchanged); PR→PO link | 02 rev 3.20 |
| UAT-P2P-095 | One-tap รับครบ (receive-all) — web button + LINE `receive <PO no>`; EXP-03 approval gate + R04 hold | EXP-02, EXP-03, R04 | 02 rev 2.4 |
| UAT-P2P-096 | Reorder low stock → one-tap PR (web สินค้าใกล้หมด card + LINE `low`/`reorder`); createPr path unchanged | EXP-03 (entry integrity) | 02 rev 2.5 |
| UAT-P2P-097 | Proactive morning low-stock alert (`low_stock_reorder_alert`) → one-tap [สั่งเติมทั้งหมด] reorder | EXP-03 (entry integrity) | 02 rev 2.6 |
| UAT-P2P-098 | Close-the-loop LINE pushes to the PR requester (PR→PO / PO approved / GR received) | EXP (follow-through) | 02 rev 2.7 |
| UAT-P2P-099 | Purchase spend insights — month total / top vendors / most-bought items (chat `spend` + `/spend-summary` + `purchase_spend` BI report) | EXP (reporting) | 02 rev 2.8 |
| UAT-P2P-100 | Partial receive (`receive <PO> <item> <qty>`) + short/damaged claim (`claim <PO/GR> <qty>`) from chat | EXP-02, EXP-03 | 02 rev 2.9 |
| UAT-P2P-101 | Quick Capture (docs/34) — `pr_raise` staffer snaps/uploads a bill → NeedsReview draft + `/mine`; extract-only `/api/doc-ai/extract-document` | EXP-10 (entry extension) | 02 rev 3.1 |
| UAT-P2P-102 | Quick Capture SoD — capturer (maker) cannot post the bill nor read the full AP worklist (both 403); booking stays `creditors` | EXP-10, EXP-06 | 02 rev 3.1 |
| UAT-P2P-103 | Quick Capture over LINE — `บิล` + photo files a NeedsReview draft (content API); no-`pr_raise` refused; redelivery-safe | EXP-10, EXP-06 | 02 rev 3.2 |
| UAT-P2P-104 | Email-to-Capture — verify send-from address (mailed code), inbound bill files a draft attributed to the sender; unknown/unverified/no-`pr_raise`/redelivery all file no draft | EXP-10, EXP-06 | 02 rev 3.3 |
| UAT-P2P-086 | AP intake: scan with PO no. → auto-map + book + match in one flow | EXP-10, EXP-01 | 02 §3, §7, §9 |
| UAT-P2P-087 | AP intake: vendor+amount auto-map only when unambiguous; ties → NeedsReview | EXP-10 | 02 §7, §9 |
| UAT-P2P-088 | AP intake: duplicate invoice number never auto-booked; post refused | EXP-10 | 02 §7, §9, §13 |
| UAT-P2P-089 | AP intake: cumulative guard — one PO not billable twice | EXP-10, EXP-01, EXP-09 | 02 §7, §9 |
| UAT-P2P-090 | AP intake: blocked-ahead-of-goods released by scheduled auto re-match; non-PO fail-open | EXP-10, EXP-09 | 02 §7, §9 |
| UAT-P2P-091 | AP intake upload: direct image/PDF (PDF text layer auto-post; keyless image → review; type/size gates) | EXP-10 | 02 §3, §7, §13 |
| UAT-UI-P2P-ACC-01 | Procurement & AP screens split by user group | R03/R04/R07 | 02 §3 |
| UAT-UI-SUP-01 | Supplier portal screen (vendor self-service) — PO ack + invoice submit | Feature (supplier portal UI) | 02 §7 |
| UAT-P2P-040 | Capital PO line → GR eligible (not stocked) | FA-10 | 02 §7, 09 §7 |
| UAT-P2P-041 | Register asset from GR → PendingApproval, no GL | FA-10 | 09 §7 |
| UAT-P2P-042 | Capitalization self-approval blocked (SoD, incl. Admin) | FA-10, R07 | 09 §7 |
| UAT-P2P-043 | Independent approval creates asset + posts GL (Dr 1500/Cr 2000) | FA-10, GL-01 | 09 §7 |
| UAT-P2P-044 | GR line cannot be capitalised twice | FA-10 | 09 §7 |
| UAT-UI-CAP-01 | Capitalize-from-GR screen (eligible → request → approve) | FA-10 | 09 §7 |
| UAT-P2P-050 | Establish a fund with opening cash → funding request PendingApproval, no cash until approved (G3) | EXP-08, R07 | 07 §7 (11), §9 |
| UAT-P2P-051 | Expense request → PendingApproval, no GL | EXP-08 | 07 §7 |
| UAT-P2P-052 | Petty-cash disbursement self-approval blocked (SoD, incl. Admin) | EXP-08, R07 | 07 §7, §9 |
| UAT-P2P-053 | Independent approval posts GL + decrements fund | EXP-08, GL-01 | 07 §7, §9 |
| UAT-P2P-054 | Draw beyond the fund balance blocked | EXP-08 | 07 §7 |
| UAT-P2P-055 | Advance approve → disburse → settle to fund | EXP-08, GL-01 | 07 §7 |
| UAT-P2P-056 | Replenish raises a funding request approved by a distinct user; capped at the float | EXP-08, R07 | 07 §7 (11) |
| UAT-P2P-057 | Pending petty-cash requests in GOV-01 monitor | EXP-08, GOV-01 | 07 §7 |
| UAT-P2P-058 | Fund-funding self-approval blocked (SOD_VIOLATION) — basics.ts / compliance.ts | EXP-08, R07 | 07 §7 (11), §9 |
| UAT-P2P-059 | Independent approval funds the fund (Dr 1015 / Cr 1000, balance rises) — basics.ts / compliance.ts | EXP-08, GL-01 | 07 §7 (11), §9 |
| UAT-UI-PCX-01 | Petty-cash fund + expense screen (funds → request → approve) | EXP-08 | 07 §7 |
| UAT-P2P-110 | Workflow-definition readiness reporter detects engine-wired docTypes (PR/PO/BUDGET/PMR/BQR) that lack a definition and would auto-approve (cross-cutting) | EXP-03/R07 config integrity (detective/config; no new numbered control) | 02 §7 (3), §9 (`workflow.ts`) |
| UAT-P2P-112 | Vendor bank-detail change maker-checker — stage → self-approve blocked → distinct approver releases it; re-stage supersedes; reject leaves vendor unchanged | EXP-11 (new) | 02 §7 (1), §9, rev 3.21 (`match.ts`) |
| UAT-P2P-120 | Receiving screen loads PO lines for a blind count (qty never pre-filled) | EXP-12 (new) | 02 §7 (5), rev 3.29 (`compliance.ts`) |
| UAT-P2P-121 | Over-receipt blocked; weight (kg) line gets 5% headroom only (`OVER_RECEIPT`) | EXP-12 (new) | 02 §7 (5), §9, §13 |
| UAT-P2P-122 | Ordered-vs-received summary + dock-photo claim inside the window | EXP-12 (new) | 02 §7 (5), §9 |
| UAT-P2P-123 | Claim window auto-closes after 24h (`CLAIM_WINDOW_CLOSED`) | EXP-12 (new) | 02 §7 (5), §13 |
| UAT-P2P-124 | Close-short is binding (`PO_LINE_CLOSED`); tolerance change procurement/exec only | EXP-12 (new) | 02 §7 (5), §9 |
| UAT-P2P-126 | Payment run proposed by due-date cutoff; blocked/ineligible bills skipped (`MATCH_BLOCKED`/`NO_ELIGIBLE_AP`) | EXP-13 (new) | 02 §7 (8b), §13 |
| UAT-P2P-127 | Payment-run SoD — proposer can neither approve nor execute (`SOD_VIOLATION`); lines lock at submit (`NOT_DRAFT`) | EXP-13 (new) | 02 §7 (8b), §9, §13 |
| UAT-P2P-128 | Run execute rides the manual AP path (GL/WHT 2361 identical; TB balanced); double-execute idempotent | EXP-13 (new) | 02 §7 (8b), §9 |
| UAT-P2P-129 | Bank bulk-transfer file (generic/scb/kbank/bbl/pain.001) SHA-256-pinned; fail-closed `VENDOR_BANK_MISSING` | EXP-13 (new) | 02 §7 (8b), §13 |
| UAT-P2P-130 | Bank-statement auto-match clears run lines (per payment + bulk total) | EXP-13 (new) | 02 §7 (8b); 07 rev 1.7 |
| UAT-P2P-134 | Early-payment discount policy is maker-checked (`SOD_VIOLATION`/`INVALID_DISCOUNT_PCT`); the run projects the sliding-scale discount (2%×฿1000=฿20, net ฿980) | EXP-14 (new) | 02 §7 (8c), §9, §13 |
| UAT-P2P-135 | Execute captures the discount (Cr 4600, run total_discount, TB balanced, idempotent); policy-off vendor earns ฿0 | EXP-14 (new) | 02 §7 (8c); 07 rev 1.9 |
| UAT-P2P-136 | Petty-cash money inputs keyed in a validated dialog — non-numeric amount can't post (shared NumberPromptDialog; also subcontract %/real-estate deposit) | EXP-08 (UI hardening — no control change) | manual 16 §16.8 (web) |
| UAT-P2P-137 | SCAR raised from a real gr_claim; a fabricated claim reference is rejected (`CLAIM_NOT_FOUND`/`CLAIM_VENDOR_MISMATCH`) | QC-04 (new) | 02 §7 (11), §13 (`supplier-scar.ts`) |
| UAT-P2P-138 | SCAR closure blocked while the 8D response is incomplete (`SCAR_INCOMPLETE`); supplier response records 8D → pending_closure | QC-04 (new) | 02 §7 (11), §9, §13 (`supplier-scar.ts`) |
| UAT-P2P-139 | QC-04 closure maker-checker — raiser self-close → `SOD_SELF_APPROVAL`; distinct reviewer closes effective (requalification gate) | QC-04 (new), R21 | 02 §7 (11), §9, §13 (`supplier-scar.ts`) |
| UAT-P2P-140 | Overdue SCAR detective worklist (`GET …/scar/open`); reject needs a reason (`REASON_REQUIRED`); RLS tenant isolation | QC-04 (new) | 02 §7 (11), §13 (`supplier-scar.ts`) |
| UAT-P2P-141 | Continuous-monitoring scan raises split_po/weekend_je/dormant_vendor, each tagged with its RCM control (`rcm_ref`); re-scan idempotent by fingerprint | GOV-02 (new) | 02 §9, rev 3.34 (`control-exceptions.ts`) |
| UAT-P2P-142 | Exception disposition — owner + due_date + root_cause set; `INVALID_DISPOSITION` on a bad status; remediate stamps `remediated_by/at` and closes it | GOV-02 (new) | 02 §9, rev 3.34 (`control-exceptions.ts`) |
| UAT-P2P-143 | KCI roll-up (open/overdue/MTTR + by detector/severity/RCM family); RLS isolates a second tenant's monitor | GOV-02 (new) | 02 §9, rev 3.34 (`control-exceptions.ts`) |
| UAT-P2P-144 | Sensitive master-data edit stages a request — master unchanged while pending; self-approval → `SOD_SELF_APPROVAL` | MDM-01 (new), R22 | 02 §7 (1), §9, §13 (`masterdata-change.ts`) |
| UAT-P2P-145 | A distinct approver applies the staged change to the master; encrypted bank account round-trips | MDM-01 (new) | 02 §7 (1), §9 (`masterdata-change.ts`) |
| UAT-P2P-146 | Reject discards the change (master untouched); `payment_terms` changes only via the maker-checker (off the direct edit) | MDM-01 (new) | 02 §7 (1), §9, §13 (`masterdata-change.ts`) |
| UAT-P2P-147 | Non-sensitive field edits directly; non-sensitive field refused (`FIELD_NOT_SENSITIVE`); RLS tenant isolation (`NOT_PENDING`) | MDM-01 (new) | 02 §7 (1), §13 (`masterdata-change.ts`) |
| UAT-P2P-113 | Vendor master direct-edit (contact/address/terms/rating/category/currency/notes) + supplier-list field enrichment | (no new control) | 02 §7 (1), rev 3.22 (`match.ts`) |
| UAT-P2P-114 | Vendor — multiple addresses/contacts (newest primary demotes the old one) + parent-vendor link (self-parent blocked) | (no new control) | 02 §7 (1), rev 3.23 (`match.ts`) |
| UAT-P2P-115 | Vendor — match-merge / DQM: duplicate detection + governed merge (child-row repoint + survivorship + soft-retire) + guards | (no new control) | 02 §7 (1), rev 3.24 (`match.ts`) |
| UAT-P2P-116 | Vendor — change history: field-level profile update old→new with actor (append-only, DB-enforced; sensitive masked) | ITGC-AC-14 (strengthened; no new control) | 02 §7 (1), rev 3.25 (`match.ts`) |
| UAT-P2P-117 | Vendor — address province canonicalised (77-province ref) + postal 5-digit validation | Data quality (no new control) | 02 §7 (1), rev 3.26 (`match.ts`) |
| UAT-P2P-118 | Vendor — typed party relationship (add + bidirectional visibility + self/dup guards + delete) | TCA (no new control) | 02 §7 (1), rev 3.27 (`match.ts`) |
| UAT-P2P-119 | Vendor — governed bank master: staged bank name canonicalised to the official Thai name | Reference data (no new control) | 02 §7 (1), rev 3.28 (`match.ts`) |
| UAT-P2P-125 | Doc-reference dropdowns on the P2P screens (pending lists; manual escape) | Usability over EXP-01..03 (no new control) | 02 §7, rev 3.30 (manual/UI) |
| UAT-P2P-131 | Budget gate default OFF + advise policy — availability annotated, approval never blocked; policy change exec/gl_close only | BUD-02 (new) | 02 §7 (3), rev 3.31; 13 §7 (2bis) (`budget.ts`) |
| UAT-P2P-132 | Budget gate warn/block — `BUDGET_CONFIRM_REQUIRED` / `BUDGET_EXCEEDED`; exec-only override with audited reason | BUD-02 (new) | 02 §7 (3), §13; 13 §7 (2bis) (`budget.ts`) |
| UAT-P2P-133 | Encumbrance lifecycle — PR estimate reserved→released at conversion; PO reserved→consumed on full receipt | BUD-02 (new) | 02 §7 (3); 13 §7 (2bis) (`budget.ts`) |

## 04 — Inventory & WMS → `03-inventory-cogs.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-INV-001 | Stock + low-stock count | INV-01 | 03 §7 |
| UAT-INV-002 | Bin creation | INV-02 | 03 §7 |
| UAT-INV-003 | Putaway updates stock | INV-02 | 03 §7 |
| UAT-INV-004 | Putaway idempotency | INV-02 | 03 §9 |
| UAT-INV-005 | Wave → pick lists | INV-03 | 03 §7 |
| UAT-INV-006 | Re-wave idempotency | INV-03 | 03 §9 |
| UAT-INV-007 | Pick decrements stock | INV-03 | 03 §7 |
| UAT-INV-008 | Over-pick block (PICK_SHORT) | INV-03, R11 | 03 §9, §13 |
| UAT-INV-009 | Pack | INV-03 | 03 §7 |
| UAT-INV-010 | Ship + tracking | INV-03 | 03 §7 |
| UAT-INV-011 | WMS posts zero GL | INV-04 | 03 §9 |
| UAT-INV-012 | Replenishment suggestion | INV-01 | 03 §7 |
| UAT-INV-013 | Auto-PR | INV-01 | 03 §7 |
| UAT-INV-014 | RMA restock + credit | REV-07 | 03 §7 |
| UAT-INV-015 | RMA restock idempotency | REV-07 | 03 §9 |
| UAT-INV-016 | Cycle-count variance review | INV-01 | 03 §7, §9 |
| UAT-INV-017 | RLS bin/suggestion isolation | ITGC-AC (RLS) | 08 §9 |
| UAT-INV-018 | Trial balance balanced | REC-01 | 04 §9 |
| UAT-INV-019 | Multi-level MRP explosion + netting | Feature (MRP) | 15 §5a |
| UAT-INV-020 | MRP plan-to-PR creates a real PR | Feature (MRP→PR) | 15 §5a |
| UAT-INV-021 | Circular BOM rejected | Feature (MRP guard) | 15 §5a |
| UAT-INV-022 | MRP lot-sizing (min/multiple/EOQ) | Feature (MRP lot-sizing) | 15 §5a |
| UAT-INV-023 | Rough-cut capacity overload flag | Feature (RCCP) | 15 §5a |
| UAT-INV-024 | STD GR PPV balanced under rounding | MFG-03 / GL-01 | 15 §9 |
| UAT-MFG-01 | WO completion full-yield (no variance) | MFG-02, GL-01 | 15 §7 |
| UAT-MFG-02 | WO completion short-yield → variance to 5810 | MFG-02, GL-01 | 15 §7 |
| UAT-INV-025 | Food-cost variance valued at ingredient cost | INV-04 (analytics) | 03 §12 |
| UAT-INV-026 | Variance summary nets fav/unfav + tenant-isolated | INV-04 (analytics) | 03 §12 |
| UAT-INV-027 | Branch replenishment splits transfer-before-buy | INV-05 | 03 §7, §9 |
| UAT-INV-028 | Auto-transfer moves branch stock; auto-PR raises residual | INV-05, EXP-03 | 03 §7, §9 |
| UAT-INV-029 | SoD transfer-vs-buy + RLS branch isolation | INV-05, ITGC-AC (RLS) | 03 §7, §9 |
| UAT-INV-040 | Bin layout geometry + utilisation in warehouse map | INV-08 | 03 §7, §9 |
| UAT-INV-041 | Locate an item to its bin(s) | INV-08 | 03 §7 |
| UAT-INV-042 | Bin capacity over-fill blocked on putaway | INV-08 | 03 §7, §9 |
| UAT-INV-043 | 3D warehouse view screen (utilisation colour + locate) | INV-08 | 03 §7 |
| UAT-INV-044 | Reservation reduces ATP; idempotent re-allocate (no leak) | INV-09 | 03 §7, §9 |
| UAT-INV-045 | Reserving beyond ATP blocked (INSUFFICIENT_ATP) | INV-09 | 03 §7, §9 |
| UAT-INV-046 | Release frees a cancelled reservation | INV-09 | 03 §7 |
| UAT-INV-047 | Fulfil is ATP-neutral vs on-hand drop | INV-09 | 03 §7 |
| UAT-INV-060 | StockCounter cannot see /stock-adjustment in nav | R11, INV-04 | 03 §7, §11 |
| UAT-INV-061 | StockCounter saves count — no Post button visible | R11, INV-04 | 03 §7 |
| UAT-INV-062 | InventoryController posts a counted stocktake from /stock-adjustment | R11, INV-04 | 03 §7 |
| UAT-INV-063 | POST /api/stocktake/:id/post blocked for wh_count-only token | R11, INV-04 | 03 §9 |
| UAT-INV-064 | InventoryController approves write-off from /stock-adjustment | R11, INV-07 | 03 §9 |
| UAT-INV-065 | APS finite-capacity scheduling | (operational) | 15 §7 (5b) |
| UAT-INV-066 | QR deep-link URL resolves same as raw payload (scan-update) | FA-04 (capture) | 09 §7.4, 03 §7.6 |
| UAT-INV-067 | scanCodeId no longer drops asset/bare tag | INV-04 / FA-04 (capture) | 03 §7.6, 09 §7.4 |
| UAT-INV-068 | Resolve endpoint identifies item/asset/unknown | FA-04 / INV-04 (capture) | 09 §7.4, 03 §7.6 |
| UAT-INV-069 | In-app camera scanner (cross-browser) fills the scan box | INV-04 (capture) | 03 §7.6 |
| UAT-INV-069b | Continuous multi-scan auto-adds session lines | INV-04 (capture) | 03 §7.6 |
| UAT-INV-069c | Scanner reads a 1D product barcode | INV-04 (capture) | 03 §7.6 |
| UAT-INV-070 | Phone native camera opens /q resolver (deep-link) | FA-04 / INV-04 (capture) | 09 §7.4, 03 §7.6 |
| UAT-INV-070b | /q resolver page identifies item/asset/empty (e2e) | FA-04 / INV-04 (capture) | 09 §7.4, 03 §7.6 |
| UAT-INV-071 | WMS wave-candidates pending list (un-waved orders appear; waved drop out) | Read-only feed (no new control) | 03 §7, rev 0.7 (`wms.ts`) |
| UAT-INV-072 | WMS picks/shipments status lists → Pack (Picked) & Ship (Packed) dropdowns | Read-only feed (no new control) | 03 §7, rev 0.7 (`wms.ts`) |
| UAT-INV-073 | Manufacturing/goods-issue doc-reference dropdowns (WO/routing/BOM/quality-ref over existing GETs) | Usability (no new control) | 15 §7, rev 0.2 (manual UI) |
| UAT-INV-090 | Waste disposition taxonomy — costed waste + by_disposition roll-up + filter | INV-15 | 03 §7 (13a), §9 (`waste.ts`) |
| UAT-INV-091 | Invalid disposition rejected (BAD_DISPOSITION) | INV-15 | 03 §7 (13a) (`waste.ts`) |
| UAT-INV-092 | Void-fired-item capture — recipe explodes to ingredient waste + one Dr 5810/Cr 1200 JE (NO_RECIPE guard) | INV-15 | 03 §7 (13a), §9 (`waste.ts`) |
| UAT-INV-093 | Theoretical-vs-actual usage variance (recipe-COGS Consume vs actual depletion) | INV-15 | 03 §7 (13a), §12 (`waste.ts`) |
| UAT-INV-074 | Stocktake/stock-movement tenant isolation (cross-tenant read + post blocked) | ITGC-AC-18 · R11 | 03 rev 9.99 (`stock-ops.ts`) |
| UAT-INV-094 | stock_movements posting works for a non-bypass tenant user post-0316 (tenant_id threaded at all 6 sites) | ITGC-AC-18 · R11 | 03 rev 9.100 (`wms.ts`,`stock-ops.ts`,`manufacturing.ts`) |
| UAT-INV-095 | WMS confirm-pick screen — wave → pick → pack now reachable (getPick + multi-status list) | Read-only feed (no new control) | 03 §7, rev 0.8 (`wms.ts`) |
| UAT-INV-096 | ATP / order-promising surfaced on /costing (check ATP, reserve, release/fulfil register) | INV-09 | 03 §7 (12), rev 1.2 (`costing.ts`) |
| UAT-INV-097 | MRP plan-to-PR + lot-sizing + rough-cut capacity surfaced on /production → MRP | Feature (MRP→PR / RCCP) | 15 §5a, rev 0.5 (`mrp.ts`) |
| UAT-INV-098 | Create a quality spec (per-item acceptable range) | QC-03 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-099 | Spec with min>max rejected (`SPEC_RANGE_INVALID`) | QC-03 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-100 | In-spec CoA → evaluate pass → recorder releases | QC-03 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-101 | Release before evaluation blocked (`COA_NOT_EVALUATED`) | QC-03 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-102 | Out-of-spec → fail; recorder self-release blocked (`SOD_SELF_APPROVAL`) | QC-03 · R21 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-103 | Out-of-spec release without a reason blocked (`DEVIATION_REASON_REQUIRED`); distinct approver with a reason releases | QC-03 · R21 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-104 | Out-of-spec deviation register lists only released fails | QC-03 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-105 | CoA RLS isolation — a second tenant sees/acts on none (`COA_NOT_FOUND`) | QC-03 · ITGC-AC-18 | 03 §7 (14), rev 1.3 (`quality-coa.ts`) |
| UAT-INV-126 | Landed-cost voucher — create + allocation preview ties to 100% | COST-01 | 03 §7 (15), rev 1.4 (`landed-cost.ts`) |
| UAT-INV-127 | Landed-cost post is maker-checker — self-post `SOD_SELF_APPROVAL`; distinct approver posts balanced Dr1200/Cr2010 | COST-01 | 03 §7 (15), rev 1.4 (`landed-cost.ts`) |
| UAT-INV-128 | Future issue carries the loaded cost (avg 10→11, COGS 110) | COST-01 | 03 §7 (15), rev 1.4 (`landed-cost.ts`) |
| UAT-INV-129 | Already-issued residual expensed to variance 5500, not re-costed | COST-01 | 03 §7 (15), rev 1.4 (`landed-cost.ts`) |
| UAT-INV-130 | Landed cost RLS + reconcile — T2 sees none; sub-ledger ties to GL 1200 | COST-01 · ITGC-AC-18 | 03 §7 (15), rev 1.4 (`landed-cost.ts`) |
| UAT-INV-121 | ABC recompute tiers items by consumption value (A/B/C) + seeds per-class cadence | INV-17 | 03 §7 (13b), §9, rev 1.4 (`cycle-counts.ts`) |
| UAT-INV-122 | Cadence-driven cycle-count due worklist (never-counted first) | INV-17 | 03 §7 (13b), rev 1.4 (`cycle-counts.ts`) |
| UAT-INV-123 | Blind count — book qty never returned to the counter | INV-17 | 03 §7 (13b), §8, rev 1.4 (`cycle-counts.ts`) |
| UAT-INV-124 | Count entry → post via the existing INV-04 counter≠poster path + valued GL adjustment | INV-17 · INV-04 · INV-06 | 03 §7 (13b), rev 1.4 (`cycle-counts.ts`) |
| UAT-INV-125 | INV-04 self-post blocked on a cycle count (`SOD_SELF_APPROVAL`) | INV-17 · INV-04 · R11 | 03 §7 (13b), §8, rev 1.4 (`cycle-counts.ts`) |
| UAT-INV-116 | Create a two-step transfer order (Draft, document only); `from==to` → `SAME_LOCATION` | INV-16 | 03 §7 (15), rev 1.4 (`transfer-orders.ts`) |
| UAT-INV-117 | Ship → relieve source + `Dr 1255 / Cr 1200`; re-ship → `NOT_DRAFT` | INV-16 | 03 §7 (15), rev 1.4 (`transfer-orders.ts`) |
| UAT-INV-118 | In-transit aging / period-end cutoff report (days-in-transit + buckets) | INV-16 | 03 §7 (15), rev 1.4 (`transfer-orders.ts`) |
| UAT-INV-119 | Custody SoD — shipper cannot receive own transfer (`SOD_SELF_APPROVAL`) | INV-16 · R11 | 03 §7 (15), rev 1.4 (`transfer-orders.ts`) |
| UAT-INV-120 | Receive (≠ shipper) → land dest + `Dr 1200 / Cr 1255`; sub-ledger reconciles to GL 1200; `NOT_SHIPPED`/`NEG_STOCK` | INV-16 | 03 §7 (15), rev 1.4 (`transfer-orders.ts`) |
| UAT-INV-106 | Std-cost revise snapshots on-hand + computes revalue impact (posts nothing) | COST-02 | 15 §8a, rev 0.8 (`std-cost-roll.ts`) |
| UAT-INV-107 | Preparer cannot self-approve their own std-cost revision (`SOD_SELF_APPROVAL`) | COST-02 | 15 §8a, rev 0.8 (`std-cost-roll.ts`) |
| UAT-INV-108 | Distinct approver rolls the standard + posts the balanced revaluation JE (1200↔5500); subsequent issue at new std | COST-02 · GL-01 | 15 §8a, rev 0.8 (`std-cost-roll.ts`) |
| UAT-INV-109 | Std-cost guards — non-STD item (`STD_ITEM_REQUIRED`); re-approval (`NOT_DRAFT`) | COST-02 | 15 §8a, rev 0.8 (`std-cost-roll.ts`) |
| UAT-INV-110 | Std-cost revision RLS — a second tenant sees none | COST-02 · ITGC-AC-18 | 15 §8a, rev 0.8 (`std-cost-roll.ts`) |
| UAT-INV-111 | Backward trace — lot → goods receipt → supplier | INV-18 | 03 §7 (15), rev 1.4 (`lot-trace.ts`) |
| UAT-INV-112 | Forward trace — lot → issue/pick → sale/customer | INV-18 | 03 §7 (15), rev 1.4 (`lot-trace.ts`) |
| UAT-INV-113 | Lot hold quarantines — excluded from FEFO + WMS wave allocation | INV-18 | 03 §7 (15), §9 (15) (`lot-trace.ts`) |
| UAT-INV-114 | Release re-enables the lot; non-held release rejected (`LOT_NOT_HELD`) | INV-18 | 03 §7 (15) (`lot-trace.ts`) |
| UAT-INV-115 | Trace an unknown lot rejected (`LOT_NOT_FOUND`) | INV-18 | 03 §7 (15) (`lot-trace.ts`) |

## 05 — General Ledger & Close → `04-general-ledger-close.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-GL-001 | COA seeded | GL-01 | 04 §7 |
| UAT-GL-002 | Manual JE posts Draft | GL-05 | 04 §9 |
| UAT-GL-003 | Draft excluded from balances | GL-05 | 04 §9 |
| UAT-GL-004 | Pending-approval queue | GL-05 | 04 §7 |
| UAT-GL-005 | Self-approval block (SOD_VIOLATION) | GL-05, R05 | 04 §9, §13 |
| UAT-GL-006 | Independent approver posts | GL-05 | 04 §9 |
| UAT-GL-007 | Reject → Voided | GL-05 | 04 §9 |
| UAT-GL-008 | Maker-checker binds Admin | GL-05, R05 | 04 §9 |
| UAT-GL-060 | Opening-balance batch posts as Draft, excluded from TB (maker-checker, gap G4) | GL-05, R05 | 04 §7 (step 4), §9 |
| UAT-GL-061 | Opening batch self-approval blocked (SOD_VIOLATION) | GL-05, R05 | 04 §7 (step 4), §9, §13 |
| UAT-GL-062 | Distinct approver posts the opening batch → TB balances | GL-05 | 04 §7 (step 4), §9 |
| UAT-GL-063 | Preparer cannot reverse own posted entry (distinct reverser, gap G2) | GL-17, GL-05, R05 | 04 §2.2, §9, §13 |
| UAT-GL-064 | Distinct user reverses the posted entry successfully | GL-17, GL-05 | 04 §2.2, §9 |
| UAT-GL-065 | High-value gift-card issue (>5,000 THB) → PendingApproval, no GL, not redeemable (maker-checker gap G1) | GC-01, R14 | 22 §7 (steps 1/1a), §9, §13 |
| UAT-GL-066 | Gift-card issuer cannot self-approve the issuance (SOD_VIOLATION) | GC-01, R14 | 22 §7 (step 1a), §9, §13 |
| UAT-GL-067 | Distinct finance approver activates the card + posts Dr 1000/Cr 2200 | GC-01, GL-01, R14 | 22 §7 (step 1a), §9 |
| UAT-GL-068 | Small-value gift-card issue (≤5,000 THB) still auto-issues Active | GC-01, GL-01 | 22 §7 (step 1), §9 |
| UAT-GL-009 | Unbalanced JE block (UNBALANCED) | GL-02 | 04 §9, §13 |
| UAT-GL-010 | Trial balance ties | REC-01 | 04 §9 |
| UAT-GL-011 | Period close lock (PERIOD_CLOSED) | GL-04, R06 | 04 §9, §13 |
| UAT-GL-012 | Period re-open | GL-04 | 04 §7 |
| UAT-GL-013 | Year-end close to RE | GL-06 | 04 §7 |
| UAT-GL-014 | Balance sheet + RE | GL-06 | 04 §9 |
| UAT-GL-015 | Year-end close idempotency | GL-06 | 04 §9 |
| UAT-GL-016 | Sub-ledger reconciliation | REC-01 | 04 §9 |
| UAT-GL-045 | Control-account reconciliation pack (period-end) | REC-04 | 04 §7 |
| UAT-GL-046 | Pending-approvals monitor (maker-checker backlog) | GOV-01 | 04 §7 |
| UAT-GL-046b | Batch approve / reject (SoD still enforced per item) | GOV-01 | 04 §7 |
| UAT-GL-047 | Signup with industry provisions a curated chart | GL-10 | 04 §7 (step 14) |
| UAT-GL-048 | Overlay curates the picker but never gates postings | GL-10 | 04 §7 (step 14), §9 |
| UAT-GL-049 | Signup without industry defaults to the full chart | GL-10 | 04 §7 (step 14) |
| UAT-GL-050 | Industry template can't drift from the engine's codes | GL-10 | 04 §7 (step 14) |
| UAT-UI-COA-01 | Pick business type at signup (industry selector) | GL-10 | 04 §7 (step 14) |
| UAT-UI-COA-02 | View the industry chart of accounts (ผังบัญชี tab) | GL-10 | 04 §7 (step 14) |
| UAT-UI-COA-03 | Curate the chart in the UI — rename/group/active/re-order (overlay) | GL-11 | 04 §7 (step 14) |
| TC-GL-11-01 | Create a canonical account as platform Admin/HQ (+ duplicate → DUPLICATE_ACCOUNT) | GL-11 | 04 §7 (step 14) |
| TC-GL-11-02 | Deactivate an account with a non-zero balance → ACCOUNT_HAS_BALANCE | GL-11 | 04 §7 (step 14) |
| TC-GL-11-03 | Direct JE to an AR control account (1100) → CONTROL_ACCOUNT | GL-11 | 04 §7 (step 14) |
| TC-GL-11-04 | Tenant gl_coa holder blocked from a canonical CoA change → COA_ADMIN_ONLY | GL-11 | 04 §7 (step 14) |
| TC-GL-11-05 | Per-tenant overlay curation reflected + RLS-scoped (no cross-tenant leak) | GL-11 | 04 §7 (step 14) |
| TC-GL-11-06 | Overlay curation requires gl_coa (non-gl_coa → 403) | GL-11 | 04 §7 (step 14) |
| UAT-GL-017 | Reconciliation prepare→certify | REC-02/03 | 04 §9 |
| UAT-GL-018 | RLS GL isolation | ITGC-AC (RLS) | 08 §9 |
| UAT-GL-019 | Revenue recognition tenant-scoped | ITGC-AC-03 / REVREC-03 | 12 §7 |
| UAT-GL-020 | Bank reconciliation tenant-scoped | ITGC-AC-03 / REC-02 | 07 §7 |
| UAT-GL-048 | Bank adjustment maker-checker (request→Draft) | BANK-02 | 07 §7 |
| UAT-GL-049 | Bank adjustment self-approve blocked / approve closes diff | BANK-02 | 07 §7 |
| UAT-GL-050 | FX rate maker-checker — manual rate not usable until approved | FX-04 | 14 §7 |
| UAT-GL-051 | FX rate approved by different user → revaluation works | FX-04 | 14 §7 |
| UAT-GL-021 | Statement of Cash Flows reconstructed from GL | GL-07 | 04 §7, §9 |
| UAT-GL-022 | Cash flow reconciles to change in cash | GL-07 | 04 §9 |
| UAT-GL-023 | Year-end close excluded from cash flow | GL-07 | 04 §7, §9 |
| UAT-GL-024 | Direct-method cash flow by receipt/payment nature | GL-07 | 04 §7, §9 |
| UAT-GL-025 | Direct method ties to operating + Δcash | GL-07 | 04 §9 |
| UAT-GL-026 | Cash-flow forecast projects open AR/AP by due date | GL-07 | 04 §7 |
| UAT-EAM-001 | Raise a corrective maintenance work order | FA-06 | 09 §7 |
| UAT-EAM-002 | Complete WO → maintenance cost to AP (5710) | EXP-05, GL-01 | 09 §7, §9 |
| UAT-EAM-003 | Illegal WO transition rejected | FA-06 | 09 §7, §13 |
| UAT-EAM-004 | PM sweep raises due preventive WOs (time + meter) | FA-06 | 09 §7 |
| UAT-EAM-005 | PM generation is idempotent | FA-06 | 09 §7, §9 |
| UAT-EAM-006 | WO cost lines roll up to actual cost | FA-06 | 09 §7 |
| UAT-EAM-007 | Completion posts the rolled-up actual cost to AP | EXP-05, FA-06 | 09 §7, §9 |
| UAT-EAM-008 | Per-asset reliability & cost KPIs | FA-06 | 09 §7 |
| UAT-GL-027 | Unbalanced recurring template rejected (UNBALANCED) | GL-08 | 04 §7, §9 |
| UAT-GL-028 | Recurring run posts a Draft JE via maker-checker | GL-08, R05 | 04 §7, §9 |
| UAT-GL-029 | Recurring run idempotent; second person approves → hits GL | GL-08, GL-05, R05 | 04 §7, §9 |
| UAT-GL-030 | Register a prepaid schedule + capitalize | GL-09 | 04 §7 |
| UAT-GL-031 | Prepaid amortization run (straight-line) + idempotent | GL-09 | 04 §7 |
| UAT-GL-137 | Allocation cycle with zero total basis rejected (NO_BASIS) | GL-23 | 04 §7, §9 |
| UAT-GL-138 | Allocation run posts ONE balanced Draft JE via maker-checker | GL-23, R05 | 04 §7, §9 |
| UAT-GL-139 | Allocation run idempotent; second person approves → balanced split hits GL | GL-23, GL-05, R05 | 04 §7, §9 |
| UAT-GL-140 | Driver / statistical-key allocation splits the pool by the driver | GL-23 | 04 §7, §9 |
| UAT-GL-032 | Lease commencement recognises ROU + liability at PV | LSE-01 | 04 §7 |
| UAT-GL-033 | Lease periodic run (interest + payment + ROU depreciation) | LSE-01 | 04 §7 |
| UAT-GL-034 | Asset upward revaluation request → Draft, deferred | FA-07, FA-08 | 09 §7 |
| UAT-GL-035 | Asset impairment (after approval) + no-change guard + audit | FA-07, FA-08 | 09 §7 |
| UAT-GL-040 | Asset revaluation: preparer cannot self-approve (SoD) | FA-08, R07 | 09 §7 |
| UAT-GL-041 | Asset revaluation: independent approver makes it effective | FA-08, GL-01 | 09 §7 |
| UAT-GL-036 | Lease modification remeasures liability + ROU | LSE-01 | 04 §7 |
| UAT-GL-037 | Disposal (approved) recycles revaluation surplus to RE | FA-07, FA-09 | 09 §7 |
| UAT-GL-042 | Disposal request → Draft, pending (not yet disposed) | FA-09 | 09 §7 |
| UAT-GL-043 | Disposal: requester cannot self-approve (SoD) | FA-09, R07 | 09 §7 |
| UAT-GL-044 | Disposal: independent approver makes it effective | FA-09, GL-01 | 09 §7 |
| UAT-GL-038 | Working-capital health score | Feature (financial-health score) | 07 §7.10 |
| UAT-UI-LSE-01 | Lease screen reachable + create + run + modify (UI) | LSE-01 | 04 §7 |
| UAT-GL-141 | Lessor classification boundary flips finance↔operating at 75% | LSE-02 | 04 §7 |
| UAT-GL-142 | Lessor finance lease: create classifies FINANCE + net investment at PV, PENDING | LSE-02 | 04 §7 |
| UAT-GL-143 | Lessor: classifier cannot approve their own lease (SoD) | LSE-02, R05 | 04 §7 |
| UAT-GL-144 | Lessor finance: independent approver books commencement (Dr 1610 / Cr 1500) | LSE-02, GL-01 | 04 §7 |
| UAT-GL-145 | Lessor finance: periodic run recognises interest income + collects cash | LSE-02 | 04 §7 |
| UAT-GL-146 | Lessor operating: straight-line rental income + continued depreciation | LSE-02 | 04 §7 |
| UAT-GL-147 | Lessor net-investment reconciliation ties GL 1610 to the schedule | LSE-02 | 04 §7 |
| UAT-GL-148 | Consolidation group setup + post-to-GL from the /consolidation screen | CON-03 | 11 rev 0.5 (`consolidation.ts`) |
| UAT-GL-149 | TFRS 15 five-step contract (create/allocate/schedule/activate/recognise) from the /revenue screen | REV-19 | 12 rev 0.9 (`revrec.ts`) |
| TC-REV-24-01 | Recognize ahead of billing builds a contract asset (Dr 1265 / Cr 4300); position asset=recognized−billed (TFRS 15 §105-107) | REV-24 | 12 rev 0.11 (`revrec-billing.ts`) |
| TC-REV-24-02 | Bill reclasses the contract asset 1265 → 1100 AR; maker ≠ biller (`SOD_SELF_BILLING`) | REV-24 | 12 rev 0.11 (`revrec-billing.ts`) |
| TC-REV-24-03 | Over-bill (ahead of recognition) parks a contract liability 2410; position tie-out | REV-24 | 12 rev 0.11 (`revrec-billing.ts`) |
| TC-REV-24-04 | Billing beyond the contract price rejected (`SCHEDULE_EXCEEDS_CONTRACT` / `BILL_EXCEEDS_CONTRACT`) | REV-24 | 12 rev 0.11 (`revrec-billing.ts`) |
| TC-REV-24-05 | REV-19 back-compat: up-front billing still releases 2410 on recognition (1265 untouched) | REV-24, REV-19 | 12 rev 0.11 (`revrec-billing.ts`) |
| TC-REV-24-06 | Tenant RLS isolation on the billing schedule / contract position | REV-24, ITGC-AC-03 | 12 rev 0.11 (`revrec-billing.ts`) |
| TC-REV-25-01 | Estimate variable consideration — expected value (Σ prob×amount) vs most-likely amount (TFRS 15 §50-53) | REV-25 | 12 rev 0.12 (`revrec-variable.ts`) |
| TC-REV-25-02 | The constraint caps the recognizable price — constrained > estimate rejected (`CONSTRAINT_EXCEEDS_ESTIMATE`); probabilities must sum to 1 (`INVALID_PROBABILITIES`) | REV-25 | 12 rev 0.12 (`revrec-variable.ts`) |
| TC-REV-25-03 | Estimate maker-checker: estimator ≠ approver (`SOD_SELF_APPROVAL`); approval mandatory before it drives revenue | REV-25 | 12 rev 0.12 (`revrec-variable.ts`) |
| TC-REV-25-04 | Re-estimate trues-up already-recognized revenue (catch-up) building a contract asset (Dr 1265 / Cr 4300); constrained (not gross) drives the price | REV-25 | 12 rev 0.12 (`revrec-variable.ts`) |
| TC-REV-25-05 | Re-estimate idempotent (no duplicate post); up-front-billed catch-up releases 2410 (REV-19 back-compat) | REV-25, REV-19 | 12 rev 0.12 (`revrec-variable.ts`) |
| TC-REV-25-06 | Tenant RLS isolation on variable-consideration estimates | REV-25, ITGC-AC-03 | 12 rev 0.12 (`revrec-variable.ts`) |
| TC-REV-26-01 | Classify a contract modification into the three TFRS 15 §18-21 outcomes (separate_contract §20 / prospective §21a / cumulative_catchup §21b) from distinct_flag × at_ssp_flag | REV-26 | 12 rev 0.13 (`revrec-modifications.ts`) |
| TC-REV-26-02 | Cumulative catch-up (§21b): a not-distinct modification catches up the satisfied portion (Dr 1265/2410 ↔ Cr 4300) | REV-26 | 12 rev 0.13 (`revrec-modifications.ts`) |
| TC-REV-26-03 | Prospective (§21a): distinct-not-at-SSP re-allocates the remaining transaction price over the remaining POs with NO catch-up | REV-26 | 12 rev 0.13 (`revrec-modifications.ts`) |
| TC-REV-26-04 | Separate contract (§20): distinct-at-SSP creates a new independent contract and leaves the original untouched | REV-26 | 12 rev 0.13 (`revrec-modifications.ts`) |
| TC-REV-26-05 | Modification maker-checker: recorder ≠ approver (`SOD_SELF_APPROVAL`); a Pending modification drives nothing; re-approval → `MODIFICATION_NOT_PENDING` | REV-26 | 12 rev 0.13 (`revrec-modifications.ts`) |
| TC-REV-26-06 | Tenant RLS isolation on contract modifications | REV-26, ITGC-AC-03 | 12 rev 0.13 (`revrec-modifications.ts`) |
| TC-REV-27-01 | Significant financing component (§60-65): discount the transaction price to PV + EIR interest unwind so Σ interest = face − PV | REV-27 | 12 rev 0.14 (`revrec-disclosure.ts`) |
| TC-REV-27-02 | Financing-component discount-rate maker-checker: setter ≠ approver (`SOD_SELF_APPROVAL`); an un-approved component cannot post (`FINANCING_NOT_APPROVED`) | REV-27 | 12 rev 0.14 (`revrec-disclosure.ts`) |
| TC-REV-27-03 | Advance (prepay → entity borrows) run posts financing interest EXPENSE with the contract liability accreting (Dr 5900 / Cr 2410), idempotent | REV-27 | 12 rev 0.14 (`revrec-disclosure.ts`) |
| TC-REV-27-04 | Arrears (deferred payment → entity lends) run accretes the contract asset against interest INCOME (Dr 1265 / Cr 4650); one component per contract (`FINANCING_ALREADY_SET`) | REV-27 | 12 rev 0.14 (`revrec-disclosure.ts`) |
| TC-REV-27-05 | Contract-liability rollforward (§120(b)): opening + billings − recognized = closing = GL 2410/1265 (reconciled) | REV-27 | 12 rev 0.14 (`revrec-disclosure.ts`) |
| TC-REV-27-06 | RPO / backlog (§120(a)) = Σ unrecognized allocated price banded by timing + tenant RLS | REV-27, ITGC-AC-03 | 12 rev 0.14 (`revrec-disclosure.ts`) |
| UAT-GL-150 | Elimination rules, IFRS-8 segments/report + consolidated cash flow from the /consolidation screen | CON-04, CON-05 | 11 rev 0.6 (`consolidation.ts`) |
| UAT-GL-151 | SVC-3 within-threshold service-contract renewal auto-approves + creates the successor (new_value = base×(1+uplift)) | SVC-02 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-152 | SVC-3 over-threshold renewal routes to pending (no successor) | SVC-02 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-153 | SVC-3 renewal self-approval blocked (SOD_SELF_APPROVAL) | SVC-02 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-154 | SVC-3 distinct approver approves → successor created + old contract renewed | SVC-02 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-155 | SVC-3 reject leaves the old contract untouched (declined) | SVC-02 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-156 | SVC-3 auto-renew price rise is gated even within threshold | SVC-02 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-157 | SVC-3 expiry worklist + RLS tenant isolation on renewals | SVC-02, ITGC-AC-03 | 12 rev 0.10 (`contract-renewal.ts`) |
| UAT-GL-158 | Lessor-side lease accounting (classify/create/approve/run/reconcile) from the /leases → Lessor tab | LSE-02 | 04 rev 2.17 (`basics.ts`) |
| UAT-GL-159 | Account-universe guard: posting to a non-existent / non-postable account fails INVALID_POSTING_ACCOUNT (docs/42) | GL-21 | 05 rev 0.37 (`compliance.ts`) |
| UAT-GL-160 | /chart-of-accounts manage surface: canonical create/edit/deactivate + per-tenant overlay (docs/42) | GL-11 | 05 rev 0.37 (`basics.ts`) |
| UAT-GL-161 | posting_determination master switch on /setup/item-categories (docs/42) | GL-12 | 05 rev 0.37 (`basics.ts`) |
| UAT-GL-162 | GL-24 posting-rule validation fail-closed at save (pinned/unknown-role/side/account) | GL-24 | 05 rev 0.37 (`compliance.ts`) |
| UAT-GL-163 | GL-24 maker-checker: pending rule never posts; distinct approver activates (SOD_VIOLATION) | GL-24 | 05 rev 0.37 (`compliance.ts`, `payroll.ts`) |
| UAT-GL-164 | GL-24 audit trail (CREATE/APPROVE append-only) + deactivate falls back to the registry default | GL-24 | 05 rev 0.37 (`compliance.ts`) |
| UAT-GL-167 | docs/43 PR-2: approved override re-routes a wired finance posting; unapproved never applies; register keys on the debit leg | GL-24, REV-14 | 05 rev 0.37 (`basics.ts`) |
| UAT-GL-168 | docs/43 PR-3: category-grain asset accounts (Q2) + governed dispose/prepaid overrides | GL-24, GL-21, FA-02, FA-03 | 05 rev 0.38 (`basics.ts`) |
| UAT-GL-169 | docs/43 PR-4: approved PROJECT.REVENUE override re-routes a project bill's revenue leg (AR pinned) | GL-24, PROJ-02 | 05 rev 0.39 (`projects.ts`) |
| UAT-GL-170 | docs/43 PR-5: approved MFG.WO_COMPLETE override re-routes the yield-variance leg (FG/WIP pinned) | GL-24, MFG-01 | 05 rev 0.40 (`manufacturing.ts`) |
| UAT-GL-175 | CLS-02 open a disclosure checklist seeds the standard TFRS/SEC items (Draft; idempotent per period) | GL-26 | 05 rev 0.41 (`disclosure-checklist.ts`) |
| UAT-GL-176 | CLS-02 review blocked while any item is Open → ITEMS_INCOMPLETE | GL-26 | 05 rev 0.41 (`disclosure-checklist.ts`) |
| UAT-GL-177 | CLS-02 maker-checker: preparer self-review → SOD_SELF_APPROVAL; distinct reviewer reviews→issues; Issued immutable (NOT_DRAFT) | GL-26, R05 | 05 rev 0.41 (`disclosure-checklist.ts`) |
| UAT-GL-178 | CLS-02 DISC support evidence pins to doc_attachments (assertDocExists) + tenant RLS isolation | GL-26, ITGC-AC-03 | 05 rev 0.41 (`disclosure-checklist.ts`) |
| UAT-GL-171 | docs/43 PR-6: approved SALE.FOOD override re-routes a dine-in checkout's revenue (cash/VAT pinned) | GL-24, REV-01 | 05 rev 0.41 (`restaurant.ts`) |
| UAT-GL-172 | docs/43 PR-7: overridden PAY-02 payable still reconciles + remits via the widened set | GL-24, PAY-02, TAX-03, TAX-09 | 05 rev 0.42 (`payroll.ts`) |
| UAT-GL-173 | docs/43 PR-8: cf_bucket drives the SCF; bulk IO staged (accounts) / GL-24-pending (posting_rules); COA_ADMIN_ONLY | GL-07, GL-11, GL-24 | 05 rev 0.43 (`basics.ts`, `ext.ts`) |
| UAT-GL-174 | docs/43 PR-9: the /setup/posting-rules registry workspace — event grid (default/tier/override), cross-event pending queue, audit viewer, deactivate | GL-24 | 05 rev 0.44 (manual / e2e) |
| UAT-GL-179 | CLS-01 flux generate flags the lines breaching abs+pct thresholds (revenue/marketing breach; COGS below abs does not) | GL-25 | 05 rev 0.41 (`flux-analysis.ts`) |
| UAT-GL-180 | CLS-01 sign-off refused while a breached line is unexplained (UNEXPLAINED_LINES) | GL-25 | 05 rev 0.41 (`flux-analysis.ts`) |
| UAT-GL-181 | CLS-01 a non-breached line cannot be explained (LINE_NOT_BREACHED) | GL-25 | 05 rev 0.41 (`flux-analysis.ts`) |
| UAT-GL-182 | CLS-01 forced explanation → maker-checker sign-off (self-review SOD_SELF_APPROVAL; distinct reviewer certifies; ALREADY_CERTIFIED locks) | GL-25, R05 | 05 rev 0.41 (`flux-analysis.ts`) |
| UAT-GL-183 | CLS-01 prior_year/budget comparatives + BS-basis filter (BUDGET_PL_ONLY) + RLS isolation | GL-25, ITGC-AC-03 | 05 rev 0.41 (`flux-analysis.ts`) |
| UAT-GL-184 | COA follow-up A: /chart-of-accounts edit dialog backfills cf_bucket/is_current; indirect SCF shows the unclassified_accounts banner | GL-07, GL-11 | 05 rev 0.46 (manual / e2e) |
| UAT-GL-185 | COA follow-up B: where-used report (config masters referencing an account) + deactivate-dialog warn list | GL-11, GL-21 | 05 rev 0.47 (`basics.ts`) |
| UAT-GL-186 | GL-27: canonical CoA maker-checker — staged request, distinct-Admin approve, reject, single-Admin AutoApplied exception | GL-27, GL-11 | 05 rev 0.51 (`compliance.ts`, `basics.ts`, `coa-change.test.ts`) |
| UAT-GL-187 | COA-D1: GL-24/GL-27/MDM staged queues in the GOV-01 center + batch approve per-item SoD + GL-27 history register | GOV-01, GL-24, GL-27 | 05 rev 0.52 (`compliance.ts`) |
| UAT-GL-188 | COA-D2: effective window + required dimensions enforced at posting; PARENT_NOT_FOUND on create | GL-21, GL-27 | 05 rev 0.53 (`basics.ts`, `ledger-posting.test.ts`) |
| UAT-QMS-CAPA-001 | Open a CAPA + child actions (open→in_progress; generic source link) | QC-02 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-002 | Submit needs an action plan (NO_ACTIONS) | QC-02 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-003 | Close blocked while actions pending (ACTIONS_INCOMPLETE) | QC-02 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-004 | CAPA effectiveness self-verify blocked (SOD_SELF_APPROVAL) | QC-02, R21 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-005 | Independent verifier marks effective → closed; re-verify NOT_PENDING_VERIFICATION | QC-02 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-006 | Ineffective verification reopens (in_progress), not closed | QC-02 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-007 | Reject verification (REASON_REQUIRED + distinct-user SoD) | QC-02, R21 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-QMS-CAPA-008 | Overdue detective read + RLS tenant isolation of CAPAs | QC-02, ITGC-AC-03 | 15 rev 0.6 (`quality-capa.ts`) |
| UAT-UI-EAM-01 | EAM screen reachable + WO lifecycle / PM sweep / reliability (UI) | FA-06 | 09 §7 |
| TC-GL-18-01 | FX revaluation run computes unrealized gain/loss | GL-18 | 04 §3.2 |
| TC-GL-18-02 | FX revaluation post maker-checker + idempotent (5400/1100/2000) | GL-18, R05 | 04 §3.2 |
| UAT-GL-165 | Period-end FX revaluation run→post (maker-checker) from the /fx → Period-end tab | GL-18 | 04 §3.2, rev 2.20 (`basics.ts`) |
| TC-GL-19-01 | Pre-lock validation of a clean period → ready | GL-19 | 04 §2.1 |
| TC-GL-19-02 | Unposted Draft JE blocks pre-lock readiness | GL-19 | 04 §2.1 |
| TC-GL-13-04 | Cost-centre master — create & list (web `/cost-centers`) | GL-13 | 04 §1.3 |
| TC-GL-13-05 | Dimensional P&L view per cost centre (web `/cost-centers`) | GL-13 | 04 §1.3 |
| TC-CON-02-01 | Consolidation eliminates IC + keeps group TB balanced | CON-03 | 11 §7.8 |
| TC-CON-02-02 | Consolidation run→post maker-checker (SELF_POST/ALREADY_POSTED) | CON-03, R07 | 11 §7.9 |
| TC-CON-02-03 | Unbalanced eliminations → CONSOL_UNBALANCED (rolled back) | CON-03 | 11 §7.8 |
| TC-CON-03-01 | Segment report (IFRS 8) groups P&L by dimension | CON-04 | 11 §7.10 |
| TC-CON-05-01 | Dual-rate translation: P&L @average, BS @closing; CTA to OCI (3400) | CON-05 | 11 §7.11 |
| TC-CON-05-02 | Consolidated statement of cash flows reconciles (post-elimination) | CON-05 | 11 §7.12 |
| UAT-GL-101 | GlAccountant cannot see "รออนุมัติ (JE)" tab | R05, GL-05 | 04 §9 |
| UAT-GL-102 | FinancialController sees "รออนุมัติ (JE)" tab | R05, GL-05 | 04 §9 |
| UAT-GL-103 | GlAccountant can reach /accounting in nav (gl_post perm) | R05 | 04 §3 |
| UAT-GL-104 | GlAccountant cannot see /finance/period-close in nav | R05, GL-15 | 04 §7 |
| UAT-GL-105 | GlAccountant can reach /reconciliation in nav (recon_prep perm) | R06, REC-01 | 04 §7 |
| UAT-GL-106 | GlAccountant cannot see certify button on /reconciliation | R06, REC-03 | 04 §7 |
| UAT-GL-107 | FinancialController can certify a recon period | R06, REC-03 | 04 §7 |
| UAT-GL-108 | API certify blocked for recon_prep-only token | R06, REC-03 | 04 §9 |
| UAT-GL-166 | IC reconciliation prepare→approve/reject (maker-checker, REC-03) from the /consolidation → IC reconciliation tab | REC-03 | 11 rev 0.7 (`consolidation.ts`) |
| UAT-GL-113 | Snapshot reconciliation blocks a drifted close | GL-20 | 04 §9 |
| UAT-GL-114 | Trial balance reads the maintained snapshot | GL-01, GL-20 | 04 §7 |
| UAT-GL-115 | Item-posting determination off by default (parity) | GL-21 | 04 §9 |
| UAT-GL-116 | Determination on → COGS routes to item override; sub-ledger ties | GL-21 | 04 §9 |
| UAT-GL-117 | Determination fail-closed (INVALID_POSTING_ACCOUNT) | GL-21 | 04 §9 |
| UAT-GL-118 | Item-setup screens: category-level account drives posting | GL-21 | 04 §14 (rev 2.8) |
| UAT-GL-119 | Warehouse account default drives inventory posting (lowest tier) | GL-21 | 04 §14 (rev 2.9) |
| UAT-GL-120 | Item default_location_id + AR revenue account drive posting | GL-21 | 04 §14 (rev 2.10) |
| UAT-GL-121 | FA-11 custody change → maker-checker (register moves only on approval) | FA-11, R07 | 09 §7.4, §9 |
| UAT-GL-122 | Confirming current location = verification (no approval) | FA-11, FA-04 | 09 §7.4 |
| UAT-GL-123 | Custody reject leaves the asset in place | FA-11 | 09 §7.4 |
| UAT-GL-124 | Audit-by-scan reconciles + offline replay idempotent | FA-11 | 09 §7.4 |
| UAT-GL-125 | Audit misplaced → closing raises a custody request | FA-11 | 09 §7.4, §9 |
| UAT-GL-126 | FA-12 verification-exception report flags stale/never-verified assets | FA-12 | 09 §7.4, §9 |
| UAT-GL-127 | Audit results surfaced as a BI report | FA-12, FA-11 | 09 §7.4 |
| UAT-GL-128 | Item-master fields editable on `/setup/items` (barcode/UOM/stock thresholds/MRP lot-sizing/capital-asset flags) | GL-21 (no control change) | 04 §7 (16), rev 2.13 (`basics.ts`) |
| UAT-GL-129 | Warehouse master fields fully editable on `/setup/warehouses` (name/zone/type/capacity/temperature/notes) | GL-21 (no control change) | 04 §7 (16), rev 2.13 (`basics.ts`) |
| UAT-GL-130 | Asset register shows location/department/serial/assigned-to; capitalize form collects location/department/serial_no | FA-10 (no control change) | 09 §7.11, rev 1.4 (`basics.ts`) |
| UAT-GL-131 | FIN-6a parallel tax book: acquire seeds it, tax-depreciation run applies initial allowance + accelerated life (memo, no GL) | FIN-6a | 09 §7.12, rev 1.5 (`basics.ts`) |
| UAT-GL-132 | FIN-6a deferred tax consumes the actual tax NBV → DTL 10200, maker-checker post (Dr 5950 / Cr 1700) | FIN-6a, TAX-06 | 09 §7.12, rev 1.5 (`basics.ts`) |
| UAT-GL-133 | FIN-6a tax-depreciation run is idempotent per period | FIN-6a | 09 §7.12, rev 1.5 (`basics.ts`) |
| UAT-GL-134 | FA-13 CIP/AUC: accumulate cost into 1520 (not depreciated, not in 1500) | FA-13 | 09 §7.13, rev 1.5 (`basics.ts`) |
| UAT-GL-135 | FA-13 settlement maker-checker → reclass Dr 1500 / Cr 1520 only on independent approval | FA-13, R07 | 09 §7.13, rev 1.5 (`basics.ts`) |
| UAT-GL-136 | FA-13 CIP guards: `CIP_NOT_OPEN` after capitalize; `CIP_NO_COST` without cost | FA-13 | 09 §7.13, rev 1.5 (`basics.ts`) |
| UAT-FIN4-01 | Financial-report builder: custom P&L row-groups + computed subtotal tie to the income statement | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-02 | Comparative (prior-year) column on a rendered statement | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-03 | Custom balance-sheet layout ties to the balance sheet | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-04 | Statement of changes in equity — roll-forward ties to the balance sheet | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-05 | Note schedule — account mapping + comparative + policy text | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-06 | DBD e-Filing export — balanced S-form facts + XBRL instance | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-07 | Statutory-FS validation / control-negative cases (`FS_*` error codes) | FIN-4 (no control) | PN-04 §4bis, rev 2.0 (`basics.ts`) |
| UAT-FIN4-08 | Statutory pack (SOCE + DBD e-Filing XBRL + custom statement/notes viewer) from the /financial-statements → Statutory & DBD tab | FIN-4 (no control) | PN-04 §4bis, rev 2.19 (`basics.ts`) |

## 06 — Tax → `06-tax-compliance.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-TAX-001 | VAT 7% calc | TAX-01 | 06 §7 |
| UAT-TAX-002 | VAT on portal sale | TAX-01, REV-03 | 06 §7 |
| UAT-TAX-003 | Currency rounding | TAX-01 | 06 §9 |
| UAT-TAX-004 | Currency list | TAX-01 | 06 §7 |
| UAT-TAX-005 | e-Tax XML (UBL 2.1) | TAX-02 | 06 §7 |
| UAT-TAX-006 | e-Tax header fields | TAX-02 | 06 §7 |
| UAT-TAX-007 | e-Tax parties/tax IDs | TAX-02 | 06 §7 |
| UAT-TAX-008 | e-Tax tax/totals | TAX-02 | 06 §9 |
| UAT-TAX-009 | e-Tax escaping | TAX-02 | 06 §9 |
| UAT-TAX-010 | e-Tax submission | TAX-02 | 06 §7 |
| UAT-TAX-011 | e-Tax resubmit idempotency | TAX-02 | 06 §9 |
| UAT-TAX-012 | Tax-invoice numbering | TAX-03 | 06 §9 |
| UAT-TAX-013 | WHT on payroll | PAY-02 | 05 §7, 06 §7 |
| UAT-TAX-017 | Deferred tax run → DTA from AR allowance (×CIT) | TAX-06 | 06 §9a, 04 §3.2 |
| UAT-TAX-018 | Deferred tax post maker-checker + idempotent (Dr 1700/Cr 5950) | TAX-06, R05 | 06 §9a, 04 §3.2 |
| UAT-TAX-018a | Deferred tax web screen — run → maker-checker post (`/deferred-tax`) | TAX-06, R05 | 06 §9a, 04 §3.2 |
| UAT-TAX-028 | Scheduled WHT-cert batch auto-issues the 50-ทวิ from AP-payment WHT | TAX-03 | 06 §14 (rev 0.9) |
| UAT-TAX-029 | WHT-cert batch idempotent (no duplicate on re-run) | TAX-03 | 06 §14 (rev 0.9) |
| UAT-TAX-030 | Scheduled PP30 filing-draft job registers the period return | TAX-05 | 06 §14 (rev 0.9) |
| UAT-TAX-031 | AP bill tax_code routes input VAT to the code's account | GL-21 | 06 §14 (rev 0.10) |
| UAT-TAX-032 | AR output VAT routes to the item vat_code account | GL-21 | 06 §14 (rev 0.10) |
| UAT-TAX-033 | PP30↔GL reconciliation spans the VAT-account set | TAX-04 | 06 §14 (rev 0.10) |
| UAT-TAX-034 | WHT tax_code defaults the income type + rate on an AP payment | TAX-03 | 06 §14 (rev 0.11) |
| UAT-TAX-035 | Issue a credit note (ใบลดหนี้ ม.86/10) against a tax invoice | TAX-07 | 06 §14 (rev 0.12) |
| UAT-TAX-036 | Maker-checker on a credit/debit note (SoD) | TAX-07, GL-05 | 06 §14 (rev 0.12) |
| UAT-TAX-037 | An approved credit/debit note adjusts output VAT in its period | TAX-07, TAX-04 | 06 §14 (rev 0.12) |
| UAT-TAX-038 | Print a credit note as a ม.86/10 document | TAX-07 | 06 §14 (rev 0.12) |
| UAT-TAX-039 | Full tax invoice PDF applies the no-code document template (presentation only; ม.86/4 fiscal integrity) | TAX-01 | 06 §14 (rev 0.13), 27 §7.13 |
| UAT-TAX-040 | Abbreviated 80mm slip applies the no-code document template (presentation only; ม.86/6 fiscal integrity) | TAX-01 | 06 §14 (rev 0.14), 27 §7.13 |
| UAT-TAX-041 | Voided-tax-invoice exception report surfaces voided invoices for independent review (G16) | Detective (no new numbered control; void stays single-user by RD requirement) | 06 §7 step 3, §9, §14 (rev 0.15) (`taxdocs.ts`) |
| UAT-TAX-042 | Full tax invoice: "ชำระเงินโดย" (Paid By) auto-derives for POS, settable for AR, prints with a due date | TAX-01 (presentation/data-adjacent; no new control; migration 0268) | 06 §14 (rev 0.17) (`taxdocs.ts`) |
| UAT-TAX-043 | Issuing a full tax invoice upserts the buyer into customer_master (address/branch/tax-id); repeat buyer refreshes, not duplicates; web autocomplete finds it | No new control (data-quality aid; migration 0269) | 06 §14 (rev 0.18) (`taxdocs.ts`) |

## 07 — Payroll → `05-payroll.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-PAY-001 | Create employees | PAY-01 | 05 §7 |
| UAT-PAY-002 | List employees | PAY-01 | 05 §7 |
| UAT-PAY-045 | Employee-master fields exposed (department/hourly_rate/pf_rate/sso_no/allowances/bank_account/start_date) | (no new control) | 05 rev 0.8 |
| UAT-PAY-003 | Run payroll prepares Draft JE (PendingApproval) | PAY-01, PAY-03, GL-01 | 05 §7 |
| UAT-PAY-004 | SSO+WHT+net totals | PAY-01 | 05 §9 |
| UAT-PAY-005 | SSO cap 750 | PAY-01 | 05 §9 |
| UAT-PAY-006 | SSO 5% below cap | PAY-01 | 05 §7 |
| UAT-PAY-007 | GL expense/payables (after approval) | PAY-01, PAY-03, GL-01 | 05 §9 |
| UAT-PAY-008 | Payroll idempotency | PAY-03 | 05 §7 |
| UAT-PAY-009 | ภ.ง.ด.1 summary | PAY-02 | 05 §7 |
| UAT-PAY-010 | Payslips | PAY-01 | 05 §7 |
| UAT-PAY-011 | PIT/WHT withholding | PAY-02 | 05 §9 |
| UAT-PAY-012 | RLS payroll isolation | ITGC-AC (RLS) | 08 §9 |
| UAT-PAY-013 | RBAC non-HCM block | ITGC-AC-07 | 08 §9 |
| UAT-PAY-014 | ESS self-service own data | Feature (ESS), ITGC-AC | 25 §7 |
| UAT-PAY-015 | ESS expense → AP reimbursement on approve | Feature (ESS), EXP-05, GL-01 | 25 §7 |
| UAT-PAY-019 | Reimbursement is an AP payable, settled via AP | EXP-05, REC-01 | 25 §7, 02 §7 |
| UAT-PAY-016 | ESS expense self-approval blocked | ITGC-AC-09 | 25 §7 |
| UAT-PAY-017 | ESS unlinked user refused | Feature (ESS) | 25 §7 |
| UAT-PAY-018 | Payroll run tenant-scoped | ITGC-AC-03 | 05 §7 |
| UAT-PAY-020 | Run posts Draft JE excluded from balances | PAY-03 | 05 §7 |
| UAT-PAY-021 | Preparer cannot approve own run (SoD) | PAY-03, R07 | 05 §7 |
| UAT-PAY-022 | Independent approver posts the run | PAY-03, GL-01 | 05 §7 |
| UAT-PAY-023 | Reject a pending run, then re-run | PAY-03 | 05 §7 |
| UAT-PAY-035 | Async payroll run executes off-thread via the job queue | PAY-03, availability | 05 §7 |
| UAT-PAY-036 | Async run idempotent + job status tenant-isolated | ITGC-AC-03 | 05 §7 |
| UAT-PAY-037 | Employee PII encrypted at rest; forms still show the real ID | ITGC-AC-19 | 05 §9 |
| UAT-UI-ESS-01 | ESS self-service screen reachable + own data + submit-only (UI) | Feature (ESS UI) | 25 §7 |
| UAT-PAY-024 | Approver inbox lists pending expense claims (tenant-scoped) | Feature (ESS), ITGC-AC-03 | 25 §7 |
| UAT-UI-ESS-02 | Expense approval screen — approve/reject + self-block (UI) | Feature (ESS UI), ITGC-AC-09 | 25 §7 |

## 08 — Admin / SoD / Audit → `08-itgc.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-ADM-001 | SoD block on create (SOD_CONFLICT) | ITGC-AC-09, R03 | 08 §9 |
| UAT-ADM-002 | Block names rule | ITGC-AC-09, R03 | 08 §13 |
| UAT-ADM-003 | Justified override | ITGC-AC-09, R03 | 08 §9 |
| UAT-ADM-004 | Override without reason rejected | ITGC-AC-09 | 08 §9 |
| UAT-ADM-005 | Clean set accepted | ITGC-AC-09 | 08 §7 |
| UAT-ADM-006 | SoD block on update | ITGC-AC-09, R03 | 08 §9 |
| UAT-ADM-007 | Access-review report | ITGC-AC-08 | 08 §9 |
| UAT-ADM-008 | Access-review CSV export | ITGC-AC-08 | 08 §9 |
| UAT-ADM-009 | UAR certification | ITGC-AC-08 | 08 §9 |
| UAT-ADM-142 | Open recertification campaign (snapshot) | ITGC-AC-21 | 08 §9 |
| UAT-ADM-143 | Certify blocked while lines pending (ITEMS_PENDING) | ITGC-AC-21 | 08 §9 |
| UAT-ADM-144 | Keep/revoke disposition + reviewer | ITGC-AC-21 | 08 §9 |
| UAT-ADM-145 | Certify auto-removes revoked grants (closed loop) | ITGC-AC-21 | 08 §9 |
| UAT-ADM-146 | Certified campaign frozen (CAMPAIGN_CERTIFIED) | ITGC-AC-21 | 08 §9 |
| UAT-ADM-147 | Campaign tenant isolation (RLS) | ITGC-AC-21/AC-03 | 08 §9 |
| UAT-ADM-010 | Audit log capture | ITGC-AC-10 | 08 §9 |
| UAT-ADM-011 | Audit UPDATE blocked | ITGC-AC-10 | 08 §9 |
| UAT-ADM-012 | Audit DELETE blocked | ITGC-AC-10 | 08 §9 |
| UAT-ADM-013 | API key authn | ITGC-AC-07 | 08 §7 |
| UAT-ADM-014 | Self-serve signup | ITGC-AC-04 | 08 §7 |
| UAT-ADM-015 | Duplicate tenant 409 | ITGC-AC-04 | 08 §13 |
| UAT-ADM-016 | SoD conflict report | ITGC-AC-08/09 | 08 §9 |
| UAT-ADM-017 | AI-proposed action requires human approval | ITGC-AC-09, GL-01 | 08 §7 |
| UAT-ADM-018 | AI action — self-approval blocked (SoD) | ITGC-AC-09 | 08 §7 |
| UAT-ADM-019 | AI action — approver lacks kind permission | ITGC-AC-02/09 | 08 §7 |
| UAT-ADM-020 | AI action — tenant isolation (RLS) | ITGC-AC-03 | 08 §7 |
| UAT-ADM-021 | Custom field — define + slugged key | Feature (UDF) | 17 §7 |
| UAT-ADM-022 | Custom field — typed/required/option validation | Feature (UDF) | 17 §7 |
| UAT-ADM-023 | Custom field — typed read + bulk load | Feature (UDF) | 17 §7 |
| UAT-ADM-024 | Custom field — tenant isolation (RLS) | Feature (UDF) | 17 §7 |
| UAT-ADM-025 | Alert rule — create + reject unknown metric | Feature (alerts) | 17 §7 |
| UAT-ADM-026 | Alert sweep fires a breached rule | Feature (alerts) | 17 §7 |
| UAT-ADM-027 | Alert cooldown suppresses re-fire | Feature (alerts) | 17 §7 |
| UAT-ADM-028 | Alert rules tenant-isolated (RLS) | Feature (alerts) | 17 §7 |
| UAT-ADM-029 | Audit viewer — query, filter, paginate | ITGC-AC-10 | 08 §7.A.8 |
| UAT-ADM-030 | Audit viewer — CSV export + permission gate | ITGC-AC-10/02 | 08 §7.A.8 |
| UAT-ADM-031 | Audit viewer — tenant isolation (RLS) | ITGC-AC-10/03 | 08 §7.A.8 |
| UAT-ADM-032 | Bulk import — dry-run validates every row | Feature (bulk import), MDM-02 | 17 §7.3a |
| UAT-ADM-033 | Bulk import — checked commit (block vs skip) | Feature (bulk import) | 17 §7.3a |
| UAT-ADM-034 | Bulk import — tenant-scoped entity stamped | Feature (bulk import), MDM-01 | 17 §7.3a |
| UAT-ADM-037 | Bulk import — menu catalog (new-company load) | Feature (bulk import — menu_items), MDM-02 | 17 §7.3a |
| UAT-ADM-038 | Bulk import — direct `.xlsx` file round-trips | Feature (bulk import — xlsx), MDM-02 | 17 §7.3b |
| UAT-ADM-039 | Setup-page IO surface + SoD boundary (item-setup) | Feature (setup IO), R13 | 17 §7.3c |
| UAT-ADM-121 | Bulk import — non-sensitive vendor import commits directly | Feature (sensitive-import maker-checker), MDM-01, R02 | 17 §7.3d |
| UAT-ADM-122 | Bulk import — sensitive field (Credit_Limit/Payment_Terms) → STAGED PendingApproval, nothing written | Feature (sensitive-import maker-checker), MDM-03; R02, R09, R10 | 17 §7.3d, §9 |
| UAT-ADM-123 | Bulk import — requester cannot self-approve (SOD_VIOLATION) | Feature (sensitive-import maker-checker), MDM-03; R02/R09/R10 | 17 §7.3d, §9 |
| UAT-ADM-124 | Bulk import — distinct approver applies staged batch → rows + credit limit written | Feature (sensitive-import maker-checker), MDM-01; R02, R09, R10 | 17 §7.3d, §9 |
| UAT-ADM-125 | Tenant PromptPay/Tax-ID change STAGED PendingApproval, not applied; QR unaffected while pending (G15) | SoD R02 (tenant-profile maker-checker; no new numbered control) | 23 §7 step 6b (`promptpay.ts`) |
| UAT-ADM-126 | Requester cannot self-approve their own staged PromptPay/Tax-ID change (`SOD_VIOLATION`) (G15) | SoD R02 | 23 §7 step 6b (`promptpay.ts`) |
| UAT-ADM-127 | A distinct approver releases the staged change → tenant PromptPay id updates + QR generates (G15) | SoD R02 | 23 §7 step 6b (`promptpay.ts`) |
| UAT-ADM-128 | Item lifecycle — set status discontinued with a successor SKU | PN-17 §7 step 11 (master-data depth; no new control) | 17 §7 step 11 (`basics.ts`) |
| UAT-ADM-129 | Item relationships — add typed link; self-relation + duplicate rejected | PN-17 §7 step 11 (guards; no new control) | 17 §7 step 11 (`basics.ts`) |
| UAT-ADM-130 | Item relationships — bidirectional listing; tenant-scoped delete removes both sides | PN-17 §7 step 11 (RLS/tenant-scoped; no new control) | 17 §7 step 11 (`basics.ts`) |
| UAT-ADM-131 | Item DQM — detection groups probable duplicates (barcode + description) | PN-17 §7 step 12 (DQM; no new control) | 17 §7 step 12 (`basics.ts`) |
| UAT-ADM-132 | Item DQM — merge god-only (`ITEM_MERGE_HQ_ONLY`); self-merge rejected | PN-17 §7 step 12 (shared-master gate; no new control) | 17 §7 step 12 (`basics.ts`) |
| UAT-ADM-133 | Item DQM — god merges: child rows repoint, duplicate soft-retired | PN-17 §7 step 12 (repoint + soft-retire; no new control) | 17 §7 step 12 (`basics.ts`) |
| UAT-ADM-134 | Item DQM — an already-merged item cannot be merged again (`ALREADY_MERGED`) | PN-17 §7 step 12 (idempotence guard; no new control) | 17 §7 step 12 (`basics.ts`) |
| UAT-ADM-135 | Date-effective — schedule a non-sensitive change; unsupported field rejected | PN-17 §7 step 13 (date-effective; no new control) | 17 §7 step 13 (`basics.ts`) |
| UAT-ADM-136 | Date-effective — sensitive credit-limit change staged + maker≠checker (`SOD_VIOLATION`) | PN-17 §7 step 13 (strengthens G7/R09; no new control) | 17 §7 step 13 (`basics.ts`) |
| UAT-ADM-137 | Date-effective — run-due applies only due changes; idempotent | PN-17 §7 step 13 (idempotent apply; no new control) | 17 §7 step 13 (`basics.ts`) |
| UAT-ADM-139 | Governance screen — readiness overview + ethics ack/register | ELC-01 · ELC-02 | elc-operating-manual v1.1 (`governance.ts`) |
| UAT-ADM-140 | Governance screen — hotline file+advance + fraud-risk register+review | ELC-04 · ELC-05 | elc-operating-manual v1.1 (`governance.ts`) |
| UAT-ADM-141 | Governance screen — DoA matrix + oversight log | ELC-03 · ELC-02 | elc-operating-manual v1.1 (`governance.ts`) |
| UAT-ADM-148 | SoD-conflict register — standing dashboard, conflicts by rule | ITGC-AC-22, R07 | 08 §9 (`sod-register.ts`) |
| UAT-ADM-149 | Accept requires compensating control/owner/expiry; NO_SUCH_CONFLICT | ITGC-AC-22 | 08 §9 (`sod-register.ts`) |
| UAT-ADM-150 | Accept conflict → acceptor recorded; dashboard reflects | ITGC-AC-22, R07 | 08 §9 (`sod-register.ts`) |
| UAT-ADM-151 | Expired/overdue worklist + periodic re-review clears | ITGC-AC-22 | 08 §9 (`sod-register.ts`) |
| UAT-ADM-152 | Register permission-gated + RLS isolated | ITGC-AC-22 | 08 §9 (`sod-register.ts`) |
| UAT-ADM-153 | Unused-item purge — preview god-only + dry-run | PN-17 §7 step 14 (shared-master GC; no new control) | 17 §7 step 14 (`onboarding.ts`) |
| UAT-ADM-154 | Unused-item purge — typed confirm; deletes orphans, keeps cross-tenant-referenced item | PN-17 §7 step 14 (cross-tenant GC; no new control) | 17 §7 step 14 (`onboarding.ts`) |
| UAT-ADM-155 | Unused-item purge — idempotent | PN-17 §7 step 14 (idempotence; no new control) | 17 §7 step 14 (`onboarding.ts`) |
| UAT-ADM-156 | Force purge — blast-radius preview + strong confirm; deletes referenced item cross-tenant | PN-17 §7 step 14 (force purge; god-only; no new control) | 17 §7 step 14 (`onboarding.ts`) |
| UAT-ADM-157 | SME edition — enterprise self-approval stays hard-blocked (no regression) | SME-01, GL-05 | 23 §9 row 0b (`sme.ts`) |
| UAT-ADM-158 | SME edition — self-approval reason-gated; evidence row + audit marker | SME-01, GL-05 | 23 §9 row 0b (`sme.ts`) |
| UAT-ADM-159 | SME edition — god-only provisioning; upgrade-only transition (no downgrade) | SME-01, ITGC-AC-18 | 23 §9 row 0b (`sme.ts`) |
| UAT-ADM-160 | SME edition — SME-01 review report surfaces exceptions; cross-tenant isolated; public signup ignores the field | SME-01 | 23 §9 row 0b (`sme.ts`) |
| UAT-ADM-035 | Webhook — register + secret-once + isolation | ITGC-AC-04/03 | 08 §7.A.9 |
| UAT-ADM-036 | Webhook — event emits a signed delivery (logged) | Feature (webhooks), ITGC-AC-04 | 08 §7.A.9 |
| UAT-ADM-037 | Webhook — redeliver / dispatch / revoke | Feature (webhooks) | 08 §7.A.9 |
| UAT-ADM-038 | Branding — set + render on receipt + validation | Feature (branding) | 23 §7.6a |
| UAT-ADM-039 | Branding — tenant-isolated | Feature (branding), ITGC-AC-03 | 23 §7.6a |
| UAT-ADM-040 | Notification inbox — targeted note reaches the right (tenant, role) user | Feature (notification inbox) | 27 §7.10 |
| UAT-ADM-041 | Notification inbox — tenant isolation (no cross-tenant) | Feature (notification inbox), ITGC-AC-03 | 27 §7.10 |
| UAT-ADM-042 | Notification inbox — broadcast vs role-targeted visibility | Feature (notification inbox) | 27 §7.10 |
| UAT-ADM-043 | Notification inbox — per-user mark-read, guard & mark-all | Feature (notification inbox) | 27 §7.10 |
| UAT-ADM-044 | Public API — OpenAPI doc + discovery open | Feature (public API) | 27 §7.11 |
| UAT-ADM-045 | Public API — API-key-only, scope identity | Feature (public API), ITGC-AC-07 | 27 §7.11 |
| UAT-ADM-046 | Public API — tenant isolation (RLS) over reads | Feature (public API), ITGC-AC-03 | 27 §7.11 |
| UAT-ADM-047 | Public API — scope gate | Feature (public API), ITGC-AC-07 | 27 §7.11 |
| UAT-ADM-048 | Public API — per-key rate limit | Feature (public API), anti-abuse | 27 §7.11 |
| UAT-ADM-049 | SSO — configure OIDC + secret write-only | Feature (SSO), ITGC-AC-04 | 27 §7.12 |
| UAT-ADM-050 | SSO — authorize URL / not-configured | Feature (SSO) | 27 §7.12 |
| UAT-ADM-051 | SSO — callback JIT-provisions + mints session | Feature (SSO), ITGC-AC-01 | 27 §7.12 |
| UAT-ADM-052 | SSO — id_token rejected (sig/aud) | Feature (SSO), ITGC-AC-01 | 27 §7.12 |
| UAT-ADM-053 | SCIM — token + auth + provision (SoD) | Feature (SCIM), ITGC-AC-09 | 27 §7.12 |
| UAT-ADM-054 | SCIM — deprovision = deactivate (soft) | Feature (SCIM), ITGC-AC-10 | 27 §7.12 |
| UAT-ADM-055 | SCIM/identity — tenant isolation + deactivated login block | Feature (SSO/SCIM), ITGC-AC-02/03 | 27 §7.12 |
| UAT-ADM-056 | Document templates — create, default & live preview | Feature (document templates) | 27 §7.13 |
| UAT-ADM-057 | Document templates — core integrity, gate, isolation, no GL | Feature (document templates) | 27 §7.13, §9 |
| UAT-ADM-058 | Custom objects — define, fields & record CRUD | Feature (custom objects) | 27 §7.14 |
| UAT-ADM-059 | Custom objects — validation reuse, isolation, no GL | Feature (custom objects) | 27 §7.14, §9 |
| UAT-ADM-060 | Object layouts — design, resolve & live preview | Feature (object layouts) | 27 §7.15 |
| UAT-ADM-061 | Object layouts — dup name, isolation, no GL | Feature (object layouts) | 27 §7.15, §9 |
| UAT-ADM-062 | Automation — create rule + run-event match/skip | Feature (automation) | 27 §7.16 |
| UAT-ADM-063 | Automation — validation, isolation, no GL | Feature (automation) | 27 §7.16, §9 |
| UAT-ADM-064 | Analytics studio — model + governed aggregate + RLS | Feature (semantic layer) | 27 §7.17 |
| UAT-ADM-065 | Analytics studio — unknown dimension rejected | Feature (semantic layer) | 27 §7.17, §9 |
| UAT-ADM-066 | Copilot — KB-grounded, cite-or-refuse | Feature (copilot) | 27 §7.18 |
| UAT-ADM-067 | Document AI — extract an AP draft | Feature (document AI) | 27 §7.19 |
| UAT-ADM-068 | NL analytics — plain language → governed query | Feature (NL analytics) | 27 §7.20 |
| UAT-ADM-069 | AI config — suggest + reject unknown target | Feature (AI config) | 27 §7.21, §9 |
| UAT-ADM-070 | Controls monitoring — scan, review, isolation, no GL | Feature (controls monitoring) | 27 §7.22, §9 |
| UAT-ADM-071 | i18n — set own locale, resolution, bad code, per-user | Feature (i18n) | 28 §4 |
| UAT-ADM-072 | White-label theme — set tokens, validation, RLS | Feature (white-label) | 30 §4 |
| UAT-ADM-073 | Onboarding — checklist + idempotent pack apply | Feature (onboarding) | 30 §4 |
| UAT-ADM-074 | Developer portal — keys, tiers, RLS | Feature (developer portal) | 29 §4 |
| UAT-ADM-075 | Connectors — register, sync, idempotent, per-tenant RLS | Feature (connectors) | 29 §4 |
| UAT-ADM-076 | Migration — dry-run field-map + validation + RLS | Feature (migration) | 30 §4 |
| UAT-ADM-077 | Localization — packs, apply, bad-country, RLS | Feature (localization) | 28 §4 |
| UAT-ADM-078 | e-Invoicing — submit, idempotent, validation | Feature (e-invoicing) | 28 §4 |
| UAT-ADM-079 | Ops — metrics + cache round-trip | Feature (scale/ops) | 30 §4 |
| UAT-ADM-080 | PWA — installable + offline shell | Feature (PWA) | 30 §4 |
| UAT-ADM-084 | R08 — Cashier cannot access `/pos/refunds` or `/pos/till` | SoD R08, ITGC-AC-09 | 21 §6 |
| UAT-ADM-085 | R08/R12 — Cashier cannot see "บันทึกคืนสินค้า" button | SoD R08/R12, ITGC-AC-09 | 21 §6 |
| UAT-ADM-086 | R12 — POS Supervisor can authorize pending refunds | SoD R12, ITGC-AC-09 | 21 §7 |
| UAT-ADM-087 | R12 — Self-approve refund request blocked | SoD R12, REV-16 | 21 §7 |
| UAT-ADM-088 | R08 — POS Supervisor can manage till + approve variance | SoD R08, POS-01 | 20 §6 |
| UAT-ADM-089 | R10 — Cashier cannot see pricing screen in nav | SoD R10, ITGC-AC-09 | 19 §4 |
| UAT-ADM-090 | R10 — Sales role cannot reach /pricing via direct URL | SoD R10, ITGC-AC-09 | 19 §4 |
| UAT-ADM-091 | R10 — PricingManager can access /pricing and create rule | SoD R10, MKT-01 | 19 §3 |
| UAT-ADM-092 | R10 — API: pos-only token blocked from pricing mutations | SoD R10, MKT-01 | 19 §3 |
| UAT-ADM-093 | R10 — API: pos token can still call pricing quote | SoD R10 (positive) | 19 §3 |
| UAT-ADM-094 | R12 — AR Clerk can see /returns in nav | SoD R12, ITGC-AC-09 | 01 §7 |
| UAT-ADM-095 | R12 — AR Clerk sees "บันทึกคืนสินค้า" button on /returns | SoD R12, REV-16 | 01 §7 |
| UAT-ADM-096 | R12 — POS Supervisor (pos_refund only) can reach /returns | SoD R12, POS-01 | 21 §6 |
| UAT-ADM-114 | Non-platform Admin cannot CREATE an Admin (`ADMIN_GRANT_DENIED`) | ITGC-AC-02 | 08 §7 (3a), §9 |
| UAT-ADM-115 | Non-platform Admin cannot PROMOTE to Admin; CAN manage a non-Admin role | ITGC-AC-02 | 08 §7 (3a), §9 |
| UAT-ADM-116 | Platform owner CAN grant the Admin role | ITGC-AC-02 | 08 §7 (3a), §9 |
| UAT-ADM-117 | SoD override request STAGED PendingApproval, user NOT created (G11 part b) | ITGC-AC-09, R03 | 08 §7 (6), §9 |
| UAT-ADM-118 | Requester cannot self-approve a staged SoD exception (`SOD_VIOLATION`) | ITGC-AC-09 | 08 §7 (6), §9, §13 |
| UAT-ADM-119 | Distinct admin approves → user created + who/why/rules in hash-chained audit meta | ITGC-AC-09, R03 | 08 §7 (6), §9 |
| UAT-ADM-120 | Conflicting set with NO reason still blocked (`SOD_CONFLICT`), incl. UPDATE | ITGC-AC-09, R03 | 08 §7 (6), §9, §13 |
| UAT-ADM-142 | Control Console — RCM catalogue + census render (`GET /api/controls/rcm`) | ITGC-MON-01 | 08 §9 (20) |
| UAT-ADM-143 | Control Console — control detail drawer + record a ToE test-run; unknown id → `CONTROL_NOT_FOUND` | ITGC-MON-01 | 08 §9 (20) |
| UAT-ADM-144 | Control Console — `exec`/`users` gating + RLS isolation of `control_test_runs` (T1≠T2) | ITGC-MON-01 | 08 §9 (20) |

## 09 — Reports & Analytics → `01`/`04` narratives

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-RPT-001 | Dashboard KPIs | — (reporting) | 01 §12 |
| UAT-RPT-002 | Finance KPI MTD | — | 04 §12 |
| UAT-RPT-003 | Stock summary | — | 03 §12 |
| UAT-RPT-004 | Notifications | — | 03 §12 |
| UAT-RPT-005 | Replenishment analytics | — | 03 §12 |
| UAT-RPT-006 | AR aging | REC-01 | 01 §9 |
| UAT-RPT-007 | AP aging | REC-01 | 02 §9 |
| UAT-RPT-008 | P&L / income statement | GL-06 | 04 §9 |
| UAT-RPT-009 | Daily sales + export | — | 01 §12 |
| UAT-RPT-010 | Monthly P&L export | — | 04 §12 |
| UAT-RPT-011 | Stock summary export | — | 03 §12 |
| UAT-RPT-012 | AP aging export | REC-01 | 02 §12 |
| UAT-RPT-013 | Sales-cube / trend | — | 01 §12 |
| UAT-RPT-014 | Anomaly detection | — | 04 §12 |
| UAT-RPT-015 | Reconciliation dashboard | REC-01 | 04 §9 |
| UAT-RPT-016 | RLS report isolation | ITGC-AC (RLS) | 08 §9 |
| UAT-RPT-017 | Read-only role cannot mutate | ITGC-AC-07 | 08 §9 |
| UAT-RPT-018 | RAG ingest + retrieve a policy | Feature (RAG) | 26 §7 |
| UAT-RPT-019 | RAG cite-or-refuse (off-topic) | Feature (RAG safety) | 26 §7 |
| UAT-RPT-020 | RAG tenant isolation (RLS) | ITGC-AC-03 | 26 §7 |
| UAT-RPT-021 | Demand ML backtest beats naive | BI-01 (backtest accuracy) | 26 §8a |
| UAT-RPT-022 | Demand forecast auto-select + persist | BI-04 (advisory) | 26 §8a |
| UAT-RPT-023 | Demand forecast input guards | Feature (demand ML guard) | 26 §8a |
| UAT-RPT-024 | Demand forecasts tenant-isolated (RLS) | ITGC-AC (RLS) | 26 §8a |
| UAT-RPT-025 | Customer-360 detail + RLS + perm gate | Feature (CRM 360), ITGC-AC | 26 §7 |
| UAT-RPT-026 | Analytics HTTP layer (guard stack) | ITGC-AC-02 | 26 §7 |
| UAT-RPT-027 | Scheduled report subscription validation | Feature (scheduled reports) | 26 §5, §5a |
| UAT-RPT-028 | Scheduled report sweep runs due + records run | Feature (scheduled reports) | 26 §5a |
| UAT-RPT-029 | Scheduled report run-now + tenant isolation | Feature (scheduled reports), ITGC-AC-03 | 26 §5a |
| UAT-RPT-030 | Saved views visibility + owner-only delete | Feature (saved views), ITGC-AC-03 | 26 §5b |
| UAT-RPT-031 | Role dashboard catalog + layout validation | Feature (role dashboards) | 26 §3a |
| UAT-RPT-032 | Role dashboard resolution filtered to viewer perms | Feature (role dashboards), ITGC-AC-02 | 26 §3a |
| UAT-RPT-033 | Role dashboard default fallback + tenant isolation | Feature (role dashboards), ITGC-AC-03 | 26 §3a |
| UAT-RPT-034 | Menu-engineering matrix (Kasavana–Smith, qty-weighted average-CM threshold) | Feature (menu engineering) | 20 §rev2.7, rev 3.12 |
| UAT-RPT-053 | Menu-engineering matrix — per-branch view (`branch_id` filter re-derives thresholds) | Feature (menu engineering, multi-branch) | 20 rev 3.12 |
| UAT-RPT-035 | Daypart / hour demand on the business clock | Feature (daypart), TZ (Asia/Bangkok) | 20 §rev2.7 |
| UAT-RPT-036 | Void / discount shrinkage analytics | Feature (loss prevention) | 20 §rev2.7 |
| UAT-RPT-037 | Staff / cashier performance | Feature (staff analytics) | 20 §rev2.9 |
| UAT-RPT-038 | Sales trend vs prior window | Feature (sales trend) | 20 §rev2.9 |
| UAT-RPT-039 | BOM availability forecast (servings-remaining) | Feature (BOM availability) | 20 §rev2.9 |
| UAT-RPT-040 | Production plan — demand-ML forecast | Feature (production plan, demand-ML) | 20 §rev3.3 |
| UAT-RPT-041 | One-click draft PO + AI tools | Feature (production plan), AI | 20 §rev3.2 |
| UAT-RPT-042 | Residual-gap BI report types (exec / budget / supplier) | RG-1/2/3 (docs/21); ELC-06 | docs/21 §2 |
| UAT-RPT-043 | Streaming analytics — live KPI feed | (operational) | docs/22 Phase B |
| UAT-RPT-044 | Weekly-seasonal demand auto-selects the day-of-week model | demand forecasting | 26 §7 |
| UAT-RPT-045 | Thai-holiday demand model applies the learned uplift | demand forecasting | 26 §7 |
| UAT-RPT-055 | Menu affinity — support/confidence/lift pairs + daypart slices | — (reporting only) | 20 rev 3.19 (`menu-engineering.ts`) |
| UAT-RPT-056 | Menu affinity schedulable BI report type | — (reporting only) | 20 rev 3.19 (`menu-engineering.ts`) |
| UAT-RPT-057 | Weather demand model — rain-day dip when enabled, dow_seasonal parity when off | — (reporting only) | 26 rev 2.4 (`demand-ml.ts`) |
| UAT-UI-DEM-01 | Demand forecasting screen (forecast / backtest / accuracy) (UI) | Feature (demand ML UI) | 13 §7 |
| UAT-UI-ROI-01 | Marketing ROI dashboard tab (/marketing, live GET /api/bi/marketing-roi) | — (no new control) | 19 rev 1.51 |
| UAT-UI-AUD-01 | Audience export screen (/crm/audience-export — preview + register + ROPA banner) | PDPA-05 | 19 rev 1.51 |
| UAT-UI-CHREF-01 | Channel-to-member link screens (/channels ผูกสมาชิก tab + /m?clink= member self-link) | MKT-13 | 19 rev 1.51 |
| UAT-UI-REP-01 | Reputation & external analytics screen (/reputation — Google Maps reviews OAuth + GA4 sync + live dashboard) | MKT-14 (new) | 19 rev 1.52 |
| UAT-RPT-058 | Marketing Mix Modeling (MMM — ingest → run → audited reproducible channel ROI, tenant-isolated, BOLA-safe getRun) | MKT-15 (new, Partial) | 19 rev 1.53 |
| UAT-UI-BUD-01 | Budget vs Actual screen (set budget + variance) (UI) | Feature (budget-vs-actual UI) | 13 §7 |
| UAT-BUD-02 | Budget maker-checker — pending excluded until approved | BUD-01 | 13 §7 |
| UAT-BUD-05 | Budget-control settings tab — policy + availability lookup + commitment audit (UI) | BUD-02 (new) | 13 §7 (2bis), rev 0.5 (`budget.ts`) |
| UAT-UI-INS-01 | Insights screen — anomalies / replenishment / AI insight (UI) | Feature (insights UI), BI-04 | 26 §7 |

## 10 — Customer Portal → `01-order-to-cash.md` / `08-itgc.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-POR-001 | Customer login | ITGC-AC-01 | 08 §7 |
| UAT-POR-002 | Portal inventory RLS | ITGC-AC (RLS) | 08 §9 |
| UAT-POR-003 | Self-serve sale VAT 7% | REV-03, GL-01 | 01 §7 |
| UAT-POR-004 | Loyalty accrual | REV-03 | 01 §7 |
| UAT-POR-005 | Sale wires GL+payment | REV-06, GL-01 | 01 §7 |
| UAT-POR-006 | Sale links to till | REV-11 | 07 §9 |
| UAT-POR-007 | Portal sale return | REV-09 | 01 §7 |
| UAT-POR-008 | RMA credit | REV-07 | 03 §7 |
| UAT-POR-009 | Cross-tenant sale RLS | ITGC-AC (RLS) | 08 §9 |
| UAT-POR-010 | Over-return guard | REV-09 | 01 §9 |
| UAT-POR-011 | Billing plans public | — | 08 §7 |
| UAT-POR-012 | Credit hold/limit on portal | REV-08 | 01 §9 |

## 11 — Loyalty & CRM (Members & Points) → `19-marketing-pricing-loyalty.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-LOY-001 | Member directory list + search | Feature (member directory) | 19 §7 (8a) |
| UAT-LOY-002 | Directory RBAC gate | ITGC-AC-02/07 | 19 §6 |
| UAT-LOY-066 | Over-threshold staff transfer STAGED PendingApproval, moves NO points; sub-threshold/member-self still immediate (G13) | LYL-18 (SoD R15/R16; no new numbered control) | 19 §7 (29), §9 (`loyalty.ts`) |
| UAT-LOY-067 | Requesting staff member cannot self-approve their own staged over-threshold transfer (`SOD_VIOLATION`) (G13) | LYL-18 (SoD R15/R16) | 19 §7 (29), §9 (`loyalty.ts`) |
| UAT-LOY-068 | A distinct approver releases the staged transfer → points move + pending queue clears (G13) | LYL-18 (SoD R15/R16) | 19 §7 (29), §9 (`loyalty.ts`) |
| UAT-LOY-069 | Voucher campaign maker-checker — codes inert until a DISTINCT user approves; self-approve → `SOD_VIOLATION` (POS-3) | REV-20 | 19 §7 (34), §9 (`pricing.ts`) |
| UAT-LOY-070 | Voucher happy redeem at checkout + the same code again blocked `VOUCHER_ALREADY_REDEEMED` (atomic single use) (POS-3) | REV-20 | 19 §7 (34), §9 (`pricing.ts`) |
| UAT-LOY-071 | Voucher negative gates — `VOUCHER_EXPIRED` / `VOUCHER_MIN_SPEND` / audited void → `VOUCHER_VOID` (POS-3) | REV-20 | 19 §7 (34), §9 (`pricing.ts`) |
| UAT-LOY-072 | Loyalty wallet coupon redeems through the same checkout surface; reuse → `ALREADY_USED` (POS-3) | REV-20 / MKT-07 | 19 §7 (34), §9 (`pricing.ts`) |
| UAT-LOY-003 | Member 360 + points history | Feature (CRM 360) | 19 §7 |
| UAT-LOY-004 | Withdraw marketing consent stops sends | MKT-04, MKT-05 | 19 §7, §9 |
| UAT-LOY-005 | Consent register persists per purpose | MKT-05 | 19 §7 |
| UAT-LOY-006 | Points-liability tie-out (acct 2250) | MKT-06 | 19 §7, §9 |
| UAT-LOY-007 | Members tenant-isolated (RLS) | ITGC-AC (RLS) | 08 §9 |
| UAT-LOY-008 | Liability accrual posts & ties out to GL 2250 | MKT-06 | 19 §7, §9 |
| UAT-LOY-009 | Accrual run idempotent (no double-post) | MKT-06 | 19 §7 |
| UAT-LOY-010 | Tie-out tenant-scoped (Admin bypass) + all-member basis | MKT-06, ITGC-AC (RLS) | 19 §7 |
| UAT-LOY-011 | Aged points expire (breakage) & release liability | MKT-06 | 19 §7, §9 |
| UAT-LOY-012 | Period close auto-accrues the liability | MKT-06 | 19 §7, §9 |
| UAT-LOY-013 | Scheduled maintenance sweep (expire + accrue, per tenant) | MKT-06 | 19 §7, §9 |
| UAT-LOY-014 | Reward burn → single-use code → liability release → double-use blocked | MKT-07 | 19 §7, §9 |
| UAT-LOY-015 | Reward eligibility guards (points/stock/limit/tier) | MKT-07 | 19 §7 |
| UAT-LOY-016 | Tier auto-recompute + journey | MKT-08 | 19 §7, §9 |
| UAT-LOY-017 | Mission claim grants reward once (single-claim) | MKT-08 | 19 §7, §9 |
| UAT-LOY-018 | Referral rewards both once + anti-gaming + tenant-scoped | MKT-08, ITGC-AC (RLS) | 19 §7, §9 |
| UAT-LOY-019 | Member self-service app — phone-OTP login, self-scoped, staff routes blocked | LYL-10, ITGC-AC | 19 §7 (21), control 21 |
| UAT-LOY-019b | Member OTP brute-force cap holds (adversarial-review fix) | LYL-10b | 19 control 21 |
| UAT-LOY-020 | Spin-the-wheel — weighted draw, free→cost, per-prize stock cap | MKT-09 | 19 §7 (22), control 22 |
| UAT-LOY-021 | Campaign — segmented send respects opt-out, audited, idempotent | MKT-10 | 19 §7 (23), control 23 |
| UAT-LOY-022 | Partner privilege — tier-gated single-use claim, limit, partner redeem | MKT-11 | 19 §7 (24), control 24 |
| UAT-LOY-023 | Loyalty analytics — tenant-scoped liability + funnel + churn | LYL-15 | 19 §7 (25) |
| UAT-LOY-024 | LINE login — linked account mints a member token; unlinked rejected | ITGC-AC, LYL-16 | 19 §7 (26), control 26 |
| UAT-LOY-025 | Receipt upload — submit → staff approve → points granted via earnInTx | LYL-17 | 19 §7 (27), control 27 |
| UAT-LOY-026 | Receipt upload — reject leaves the balance untouched; re-review blocked | LYL-17 | 19 §7 (27), control 27 |
| UAT-LOY-027 | Receipt upload — duplicate member/date/amount claim blocked | LYL-17 | 19 §7 (27), control 27 |
| UAT-LOY-073 | Points ledger replay guard (same doc never earns/redeems twice) | LYL-22 | 19 rev 3.99 (`loyalty.ts`) |
| UAT-LOY-076 | Marketplace buyer-ref hash-only capture + member auto-attach on ingest | MKT-13 | 19 §7 (35), §9 (`channel.ts`) |
| UAT-LOY-077 | Member QR self-link: member token + required consent recorded in-tx | MKT-13 | 19 §7 (35), §9 (`channel.ts`) |
| UAT-LOY-078 | One platform account = one member (`REF_ALREADY_LINKED`) | MKT-13 | 19 §7 (35), §13 (`channel.ts`) |
| UAT-LOY-079 | Tenant-bound link token; staff link refuses implicit consent | MKT-13 | 19 §7 (35), §13 (`channel.ts`) |
| UAT-LOY-080 | marketing_roi schedulable report — spend/lift/margin, honest cost framing | — (rides MKT-04) | 19 §7 (36) (`bi.ts`) |
| UAT-LOY-081 | Audience export: consent-filtered, hash-only payload | PDPA-05 | 19 §7 (37) (`pdpa.ts`) |
| UAT-LOY-082 | Audience export: fail-closed ROPA gate + append-only register | PDPA-05 | 19 §7 (37) (`pdpa.ts`) |
| UAT-LOY-083 | Direct Meta/Google adapters: exact wire formats + per-recipient register rows | PDPA-05 | 19 §7 (37) rev 1.48 (`pdpa.ts`) |
| UAT-LOY-084 | Consent withdrawal removes the member from the external audiences (manifest, rows_removed, idempotent) | PDPA-05 | 19 §7 (37) rev 1.49 (`pdpa.ts`) |
| UAT-LOY-074 | Service SLA event Resolve computes breach vs the tier's due times (`/service`) | — (operational) | manual 16 §16.7 (`service.ts`) |
| UAT-LOY-075 | Service subscription lifecycle — create/bill/pause/resume/cancel-terminal + invoices/pay (`/service`) | — (operational) | manual 16 §16.7 (`service.ts`) |

## 12 — After-Sales Warranty & Entitlement → `32-after-sales-warranty.md`

| UAT ID | Requirement / Feature | RCM control / SoD | Narrative § |
|---|---|---|---|
| UAT-SVC-001 | Create a warranty term (coverage months + type); duplicate `term_code` → `TERM_EXISTS` | SVC-01 | 32 §7 (1), §9 (`warranty.ts`) |
| UAT-SVC-002 | Register a serialized unit — `warranty_end` computed = sold + coverage_months; duplicate serial → `SERIAL_EXISTS` | SVC-01 | 32 §7 (2), §9 (`warranty.ts`) |
| UAT-SVC-003 | In-coverage claim auto-authorizes FREE (charge 0, `is_in_coverage=true`) | SVC-01 | 32 §7 (3), §9 (`warranty.ts`) |
| UAT-SVC-004 | Out-of-coverage claim (expired / kind mismatch) parks `pending`, moves nothing | SVC-01 | 32 §7 (3), §9 (`warranty.ts`) |
| UAT-SVC-005 | Requester self-authorizing a claim → 403 `SOD_SELF_APPROVAL`; a DISTINCT user authorizes (charge or free override) | SVC-01 (SoD requester≠authorizer) | 32 §7 (4), §9 (`warranty.ts`) |
| UAT-SVC-006 | Re-deciding a non-pending claim → `CLAIM_NOT_PENDING`; reject requires a reason + distinct user | SVC-01 | 32 §7 (4–5), §9 (`warranty.ts`) |
| UAT-SVC-007 | Coverage-exceptions register lists authorized-free out-of-coverage overrides only (detective) | SVC-01 | 32 §7 (7), §9 (`warranty.ts`) |
| UAT-SVC-008 | Expiring worklist surfaces units within N days of `warranty_end`; RLS isolates a second tenant | SVC-01 | 32 §7 (6), §9 (`warranty.ts`) |
| UAT-CASE-001..006 | Support case governed lifecycle — open (`CASE-NNNNN`, thread token), assign→open, resolve, reopen, close; an illegal transition (resolve a closed case) → `CASE_NOT_ACTIVE` | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-007 | Email-to-Case: an unmatched inbound email OPENS a new case (`source='email'`) — completeness, no dropped email | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-008..009 | New email case carries a thread token + logged inbound message; a reply with `[case:<token>]` threads onto the SAME case (`matched_by='thread_token'`) | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-010..011 | Redelivered Message-ID is idempotent (`skipped='duplicate'`); a reply from the sender with no token threads onto their open case (`matched_by='contact_email'`) | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-012 | A reply onto a resolved/closed case reopens it (`status='open'`) | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-013..014 | Email-to-Case auth: unknown tenant code → `UNKNOWN_TENANT`; bad signature → `BAD_INBOUND_SECRET`; stale timestamp → `WEBHOOK_STALE` (forgery/replay defence) | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-015 | RLS tenant isolation — a second-tenant user sees 0 of the first tenant's cases | SVC-04 | 32 §10b (`service.ts`) |
| UAT-CASE-016..017 | Case SLA entitlement — a tier computes `first_response_due_at`/`resolution_due_at` from the open time (Gold 2h/8h); re-setting the entitlement recomputes off the original open time (Bronze 72h) | SVC-05 | 32 §10c (`service.ts`) |
| UAT-CASE-018 | The first outbound reply stamps `first_responded_at` + evaluates `response_breached` | SVC-05 | 32 §10c (`service.ts`) |
| UAT-CASE-019..021 | SLA-breach worklist (`GET …/cases/sla/breaches`) lists open past-due cases with `breach_kind`; resolving a past-due case sets `resolution_breached` and drops it from the worklist | SVC-05 | 32 §10c (`service.ts`) |
| UAT-CASE-022 | RLS on the breach worklist — a second-tenant user sees 0 of the first tenant's breaches | SVC-05 | 32 §10c (`service.ts`) |
| UAT-KB-001..005 | KB governed publish lifecycle — author a draft (`KB-*`), draft-only edit (`ARTICLE_NOT_DRAFT`), and the **author ≠ publisher** maker-checker: the author self-publishing → `SOD_SELF_PUBLISH`, a distinct user publishes → `published` | SVC-06 | 32 §10d (`service.ts`) |
| UAT-KB-006..007 | Self-service search returns **published-only** articles (drafts excluded) and increments `views`; a helpful vote increments the counter | SVC-06 | 32 §10d (`service.ts`) |
| UAT-KB-008 | Case-deflection log + `deflection-stats` detective read — deflected vs case-opened interactions → deflection rate (0.5 on 1-of-2) | SVC-06 | 32 §10d (`service.ts`) |
| UAT-KB-009..010 | Archive a published article; RLS isolation — a second-tenant user sees 0 of the first tenant's articles | SVC-06 | 32 §10d (`service.ts`) |
| UAT-QC-001 | Add a per-tenant defect code; list surfaces it | QC-01 | 15 §7 (13) (`quality-ncr.ts`) |
| UAT-QC-002 | Raise an NCR with no financial disposition → `status='open'` (`NCR-*`) | QC-01 | 15 §7 (13) (`quality-ncr.ts`) |
| UAT-QC-003 | Promote a failed `quality_inspection` into an NCR (ref to GR, `pending_disposition`) | QC-01 | 15 §7 (13) (`quality-ncr.ts`) |
| UAT-QC-004 | Propose scrap → parks `pending_disposition`; no GL posted yet | QC-01 | 15 §7 (13) (`quality-ncr.ts`) |
| UAT-QC-005 | Raiser self-dispositions → 403 `SOD_SELF_APPROVAL` (maker-checker) | QC-01 (SoD raiser≠approver, R21) | 15 §9 (`quality-ncr.ts`) |
| UAT-QC-006 | Distinct `quality_approve` user dispositions scrap → `dispositioned`, write-off 50, `entry_no` JE-*, Dr 5810 / Cr 1200, TB balanced | QC-01 / GL-01 | 15 §7 (13), §9 (`quality-ncr.ts`) |
| UAT-QC-007 | Reject a proposed disposition → NCR returns to `open`, no GL | QC-01 | 15 §9 (`quality-ncr.ts`) |
| UAT-QC-008 | RLS: a second tenant's NCR is not in this tenant's register | QC-01 / ITGC-AC-03 | 15 §9 (`quality-ncr.ts`) |

## Coverage summary

| Cycle | Cases | Control-type cases |
|---|---|---|
| 01 Security & Access | 45 | 33 |
| 02 Order-to-Cash | 57 | 27 |
| 03 Procure-to-Pay | 28 | 13 |
| 04 Inventory & WMS | 29 | 12 |
| 05 GL & Close (incl. fixed assets / EAM) | 55 | 37 |
| 06 Tax | 14 | 5 |
| 07 Payroll | 22 | 9 |
| 08 Admin / SoD / Audit | 80 | 47 |
| 09 Reports & Analytics | 29 | 6 |
| 10 Customer Portal | 12 | 5 |
| 11 Loyalty & CRM | 28 | 24 |
| **Total** | **399** | **217** |

> Note (2026-06-29): the "01 Security & Access" counts were reconciled to the actual case rows in `01-security-access-uat.md` (45 cases / 33 control, incl. the new UAT-SEC-036…045 for ITGC-AC-17). The other cycles' counts predate recent additions and may be understated — a separate full recount/reconciliation is pending and out of scope for this change.
