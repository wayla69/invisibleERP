# UAT — Cycle 01: Security & Access

**Status: DRAFT v1.13 · 2026-07-22** · *v1.13: **Audit completeness + egress reads** (UAT-SEC-075, migration `0464`, no new control) — the hash chain proves nothing was ALTERED but is structurally blind to OMISSION (`seq` comes from the last written row, so a dropped row leaves no gap). A sharded per-tenant expectation counter, bumped INSIDE the business transaction, makes `written >= expected` checkable and a shortfall provable — reported separately from a hash break so a loss is not misattributed to tampering. Plus `@AuditRead`: bulk exports (audit CSV, master data, item setup, UAR, SME registry, voucher codes, CRM CDP) now leave a row stamped `meta.audit_read`; ordinary reads stay unaudited.* · *v1.12: **Platform-surface safety rails** (UAT-SEC-074, no migration, no new control) — the read-only company view was keyed on act-as scoping, which `@PlatformAdmin` routes suppress by design, so it did NOT cover the very routes that suspend/factory-reset/delete/purge a company; now keyed on being a platform owner. And `force-purge` (deletes shared products even where a company still uses them; an omitted `item_ids` targets the whole catalogue) is fail-closed behind `ALLOW_ITEM_FORCE_PURGE` plus a mandatory `expected_ref_rows` echoed from force-preview and re-checked live — the blast-radius preview is now enforced, not advisory. ToE `pg-core` 15→17, `onboarding` 224→227.* · *v1.11: **God-surface audit coverage (reads + refusals) + MFA posture** (UAT-SEC-073, no migration, no new control) — refused attempts at god authority are audited too (`meta.platform_denied` + the refusing code): Nest runs guards BEFORE interceptors, so a `PlatformAdminGuard` rejection never reached the interceptor and all three refusal paths left no trace; the shared chain writer now lives in `common/audit-writer.ts`. Also: `AuditInterceptor` audited only POST/PATCH/PUT/DELETE, so **13 of the 38 `@PlatformAdmin` routes (every god READ) left no audit row**, including `GET /api/admin/tenants/:id/export` which streams every row of every tenant-scoped table for one company; it now audits **every method** on a platform route (`meta.platform_read`). Root-caused alongside it: the cross-tenant markers `meta.rls_bypass`/`rls_org_scope`/`god_act_as_tenant` were read before `TenantTxInterceptor` set them (audit is the OUTERMOST interceptor) and were never written — now read at tap-time. `PLATFORM_REQUIRE_MFA` documented as the recommended production posture + added to the API service env manifest. ToE: `onboarding` 216→221, vitest `platform-admin.test.ts` 12.* · *v1.10: **Slip-OCR pre-fill** (UAT-SEC-072, no migration, no new control) — client-side Slip-Verify mini-QR decode (`@ierp/shared` slip-qr) + `POST /api/doc-ai/slip-extract` honesty ladder (rules for text / vision when keyed / EMPTY for un-keyed images); /billing claim form อ่านจากสลิป upload; ToE `onboarding.ts` 216 + vitest `slip-extract.test.ts` + `billing-slip.spec.ts`.* · *v1.9: **Wave D console ops depth** (UAT-SEC-071, no migration, no new control) — D1 god-inbox → `PLATFORM_ALERT_EMAIL` push (`platform_alert` via the A1 outbox), D2 god-only full tenant export (schema-auto-discovered tenant tables, 403 for company admins), D3 `PLATFORM_REQUIRE_MFA` (live-DB fail-closed) + `PLATFORM_IP_ALLOWLIST` (IPv4/CIDR fail-closed) on `PlatformAdminGuard`, D4 `evaluateHealthAlerts` thresholds on ops-metrics + console breach banner; ToE `onboarding.ts` 214 + vitest `health-alerts.test.ts`.* · *v1.8: **Wave C Thai payment rails** (UAT-SEC-070, migration 0458, no new control) — `saas_payment_claims` slip-claim ledger: payment-info (platform PromptPay dynamic EMVCo QR + bank details, amount due = plan + add-ons), tenant slip claims (own-tenant only, DUPLICATE_SLIP fail-closed), god verify queue (approve → RCPT-S receipt source bank_transfer idempotent on claim:<id> + subscription Active + email; reject → reasoned payment_claim_rejected email; decided = CLAIM_NOT_PENDING); ToE `onboarding.ts` 207 + platform e2e payments tab.* · *v1.7: **Wave B enforcement rollout** (UAT-SEC-069, migration 0455, no new control) — B1 `entitlement_observations` ledger (would/did-block per day × tenant × code × mode, deduped; god-only read + Platform Console triage panel), B2 upsell dialog on plan-level deny codes (CTA → `/billing`), B3 per-tenant cohort `ENTITLEMENTS_ENFORCE_TENANTS` (full enforcement incl. Admin no-bypass; others keep the global mode); ToE `plan-gating.ts` 52 + `onboarding.ts` 196 + `platform-plans.spec.ts`.* · *v1.6: **A4 own-SaaS receipts** (UAT-SEC-068, migration 0454, no new control) — invoice.paid webhook → idempotent RCPT-S receipt + saas_receipt email + subscription Active; god manual bank-transfer receipts; tenant list/print own-only (foreign = 404); plain receipt without RECEIPT_ISSUER_TAX_ID; ToE `onboarding.ts` 194.* · *v1.5: **A3 add-on billing + self-serve purchase** (UAT-SEC-067, no new control, no migration) — `POST /api/billing/addons` (own tenant only) + checkout add-on line items (annual = 10× monthly, plan-included dropped, THB-only, UNKNOWN_ADDON fail-closed); ToE `onboarding.ts` 186.* · *v1.4: **A2 SaaS lifecycle automation** (UAT-SEC-066, migration 0453, no new control — automates the existing suspend authority with attribution + an idempotent event ledger): trial T-7/T-1 reminders, grace → auto-suspend, ฿0 activation, PastDue dunning ladder → suspend ≥21d, recovery clears; ToE `onboarding.ts` 178 + vitest `saas-lifecycle.test.ts`.* · *v1.3: **A1 transactional email for onboarding decisions** (UAT-SEC-065, migration 0452, no new control) — approve/reject/invite-with-email queue th/en emails into the god-only `platform_emails` outbox, delivered by the job worker or the deliver-pending sweep (mock provider without MAIL_PROVIDER); ToE `onboarding.ts` 165 + vitest `mailer-templates.test.ts`.* · *v1.2: **Platform Console plans/modules review** (UAT-SEC-064, UI over 0451, no new control) — a god-only แพ็กเกจ & โมดูล tab renders the per-plan module matrix from the live plan catalogue; the onboarding queue previews the requested pack's modules; the company drawer manages purchased add-ons (`POST /api/admin/tenants/:id/addons`) and shows effective modules (plan ∪ add-ons). ToE Playwright `platform-plans.spec.ts` (web-e2e gate).* · *v1.1: **/plans pack carry-through + add-on entitlements** (UAT-SEC-063, migration 0451, no new control) — the signup request stores the mapped plan/interval/add-ons, approve provisions them, and the four add-on suites gate their surfaces under `ENTITLEMENTS_ENFORCE` (à-la-carte via `subscriptions.addons`); ToE `onboarding.ts` 160 + `plan-gating.ts` 41.* · *v1.0: **public plans-&-pricing page** (UAT-SEC-060..062, no control — a prospect-facing marketing surface at `/plans`, static indicative prices aligned with the seeded plans, no session and no tenant data; ToE Playwright `pricing.spec.ts` + `pricing.mobile.spec.ts` in the `web-e2e` gate).* · *v0.9: **god-only company creation** (UAT-SEC-054, ITGC-AC-18) — public signup files a request-access entry (no tenant), god approval provisions it, and prod self-serve signup is always off (`PUBLIC_SIGNUP_ENABLED` no-op); harness `onboarding.ts` + `signup-gate.test.ts`.* · *v0.8: **Platform Console** (UAT-SEC-053, ITGC-AC-18) — enriched company directory (`GET /api/admin/tenants`) + platform-owner-only `/platform` nav.* · *v0.7: god **company-switcher** (UAT-SEC-052, ITGC-AC-18) — `X-Act-As-Tenant` narrows a god to one company (`GET /api/admin/tenants` directory + `is_platform_owner` on `/api/auth/me`); a non-god's header is ignored.* · *v0.6: platform-owner "god" cross-org visibility (UAT-SEC-051, ITGC-AC-18) — a `PLATFORM_ADMIN_USERNAMES` member gets a global RLS bypass on every route while a per-tenant Admin stays org-scoped; god is env-gated, not an assignable role.* · *v0.5: LP-1 LINE OA go-live pack (UAT-SEC-049/050 — required Channel secret, webhook receipt health, test-self push).* · *v0.4: LC-3 LINE chat governance (UAT-SEC-048).* · *v0.3 (2026-07-01): UAT-SEC-032/033 extended to cover `loyalty_receipt_submissions` (LYL-17) in the PDPA export/erasure — closes a gap where a new member-PII table wasn't wired into the existing DSAR machinery.* · *v0.2 (2026-06-29): added UAT-SEC-036..045 for ITGC-AC-17 (POS-PIN quick-login restriction + combined login/open-shift).* · Cross-ref: process narrative `08-itgc.md` (ITGC-AC-*), RCM `compliance/Invisible_ERP_SOX_RCM_v1.xlsx`, harness `tools/cutover/src/compliance.ts`, `pos-pin.ts` & `e2e.ts`.

Result legend: Pass / Fail / Blocked / N/A / Not Run (default). Error codes/amounts are exact — a different code is a Fail.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-SEC-001 | Valid login returns JWT + role | Admin | Admin account seeded | 1. `POST /api/login`. 2. Read response. | `{username: admin, password: admin123}` | 200; body has `token` and `role: "Admin"`. | High | Positive | ITGC-AC-01 | Not Run | |
| UAT-SEC-002 | Bad password rejected | Admin | Admin seeded | 1. `POST /api/login` with wrong password. | `{username: admin, password: x}` | 401; no token. | High | Control | ITGC-AC-01 | Not Run | e2e.ts |
| UAT-SEC-003 | No token blocked on protected route | (none) | — | 1. `GET /api/dashboard` with no Authorization header. | — | 401. | High | Control | ITGC-AC-02 | Not Run | |
| UAT-SEC-004 | First login forces password change | new user | User created, never logged in | 1. `POST /api/login` with temp password. 2. `GET /api/ledger/trial-balance` with that token. 3. `POST /api/auth/change-password`. 4. Retry step 2 with a fresh token. | `<<newuser/temp pw>>` | (1) 200 with `must_change_password: true`; (2) **403 `PASSWORD_CHANGE_REQUIRED`** — the flag is a hard API gate, not a UI hint; (3) 200; (4) allowed. | High | Control | ITGC-AC-07 | Not Run | onboarding.ts |
| UAT-SEC-005 | Change password enforces min length 8 | any | Logged in, change-password screen | 1. `POST /api/auth/change-password` new pw < 8 chars. 2. Retry ≥ 8 chars. | short=`pw123` / valid=`pw123456` | <8 rejected (400/422); ≥8 accepted; can log in with new pw. | High | Control | ITGC-AC-05 | Not Run | |
| UAT-SEC-006 | Privileged role flagged for mandatory MFA setup | Admin | Admin has NOT enrolled MFA | 1. `POST /api/login` (admin). | `{username: admin, password: admin123}` | 200 with `must_setup_mfa: true`. | High | Control | ITGC-AC-06 | Not Run | compliance.ts |
| UAT-SEC-007 | Non-privileged role NOT forced to MFA | Cashier | Cashier seeded (`pos_sell` only) | 1. `POST /api/login` (cashier1). | `{cashier1/pw1234}` | 200; `must_setup_mfa` not true. | Med | Control | ITGC-AC-06 | Not Run | compliance.ts |
| UAT-SEC-008 | TOTP enrolment activates 2nd factor | FinancialController | Logged in | 1. `POST /api/auth/mfa/setup` → secret. 2. `POST /api/auth/mfa/enable` with valid TOTP. | secret → `authenticator.generate(secret)` | setup returns `secret`; enable → 200 `enabled: true`. | High | Positive | ITGC-AC-06 | Not Run | compliance.ts |
| UAT-SEC-009 | MFA-enabled login without code blocked | FinancialController | MFA enabled (SEC-008) | 1. `POST /api/login` with password only. | `{fincon/pw}` | 401 `MFA_REQUIRED`. | High | Control | ITGC-AC-06 | Not Run | compliance.ts |
| UAT-SEC-010 | MFA-enabled login with wrong code blocked | FinancialController | MFA enabled | 1. `POST /api/login` with bad TOTP. | `{fincon/pw, totp: 000000}` | 401 `MFA_INVALID`. | High | Control | ITGC-AC-06 | Not Run | compliance.ts |
| UAT-SEC-011 | Password + valid TOTP authenticates | FinancialController | MFA enabled | 1. `POST /api/login` with current TOTP. | `{fincon/pw, totp: <valid>}` | 200 with `token`; `must_setup_mfa` not true. | High | Positive | ITGC-AC-06 | Not Run | compliance.ts |
| UAT-SEC-012 | MFA can be disabled | FinancialController | MFA enabled | 1. `POST /api/auth/mfa/disable`. 2. Login with password only. | — | disable 200; subsequent login no longer requires code. | Med | Positive | ITGC-AC-06 | Not Run | |
| UAT-SEC-013 | /auth/me returns effective permissions | Admin | Logged in | 1. `GET /api/auth/me`. | bearer admin | 200; `permissions` is a non-empty array. | Med | Positive | ITGC-AC-07 | Not Run | e2e.ts |
| UAT-SEC-014 | RBAC denies action without permission | Warehouse | Warehouse user seeded | 1. `POST /api/pos/returns` as Warehouse (no returns/pos perm). | `{sale_no, items}` | 403 Forbidden. | High | Control | ITGC-AC-07 / R-RBAC | Not Run | returns.ts |
| UAT-SEC-015 | RLS — tenant cannot see another tenant's data | Procurement (T2) | T1 has journal entries; T2 user authorized to read ledger | 1. `GET /api/ledger/journal?limit=100` as T2 user. 2. `GET /api/ledger/trial-balance?period=<cur>`. | bearer finT2 | T1 entries absent; T2 sees no T1 4000 credit. | High | Control | ITGC-AC (RLS) | Not Run | compliance.ts |
| UAT-SEC-016 | Security headers present (edge hardening) | Admin | — | 1. `GET /` and inspect response headers. | — | At least one of `x-frame-options` / `content-security-policy` / `x-content-type-options` present. | Low | Control | ITGC-OP | Not Run | worldclass.ts |
| UAT-SEC-017 | Aggregator webhook requires shared secret (W1) | (public) | `CHANNEL_WEBHOOK_SECRET` configured; grab adapter registered | 1. `POST /api/channels/grab/webhook` with no / wrong `x-webhook-secret`. 2. Retry with correct secret. | order payload | (1) 401 (`BAD_WEBHOOK_SIG`); (2) 200 `status: processed`, order written only to the resolved tenant. | High | Control | ITGC-AC-03 | Not Run | pos-p2.ts |
| UAT-SEC-018 | PSP webhook tenant-scoped (W1) | (public) | payment intent exists for a tenant | 1. `POST /api/payments/psp/webhook` (valid HMAC) flips the intent to Captured. | signed body | Only that tenant's intent updated; webhook runs RLS-scoped to the intent's tenant (`@NoTx` + `RealtimeScope.run`), not the anonymous bypass. | High | Control | ITGC-AC-03 | Not Run | pos-p0.ts |
| UAT-SEC-019 | Input-validation hardening (W5) | any | — | 1. `POST /api/branches` with a malformed body. 2. `GET` a list endpoint with `?limit=abc`. | `{code:'',name:123}` / `?limit=abc` | (1) 400 (Zod), not 500; (2) 400 `BAD_QUERY`, not a NaN LIMIT. | Med | Control | ITGC-AC-02 | Not Run | branch.ts |
| UAT-SEC-020 | Username login is case/whitespace-insensitive | Admin → new user | Admin logged in | 1. `POST /api/admin/users` with a mixed-case username. 2. `POST /api/login` typing the username in a *different* case and with a leading/trailing space. | create `{username:'JohnD', password:'pw123456', role:'Sales'}`; login `{username:' johnd ', password:'pw123456'}` | Create 201 (stored as `johnd`); login 200 with `token` — username matched despite casing/whitespace. | High | Positive | ITGC-AC-01 | Not Run | |
| UAT-SEC-021 | Password remains case-sensitive (not trimmed) | new user | User `johnd` created with `pw123456` | 1. `POST /api/login` with the password in wrong case / with a trailing space. | `{username:'johnd', password:'PW123456'}` and `{username:'johnd', password:'pw123456 '}` | 401 both times — the password is never lowercased or trimmed. | High | Control | ITGC-AC-01 | Not Run | |
| UAT-SEC-022 | Logout revokes the token (single session) | any user | logged in (token) | 1. `GET /api/auth/me` (works). 2. `POST /api/auth/logout` (same token). 3. `GET /api/auth/me` (same token). | — | Step 1 → 200; step 3 → 401 `TOKEN_REVOKED` (the jti is denylisted). | High | Control | ITGC-AC-15 | Not Run | compliance.ts |
| UAT-SEC-023 | Deactivated account's existing token is rejected live | Admin + target user | target logged in (token) | 1. deactivate the account (`is_active=false` / SCIM). 2. `GET /api/auth/me` with the target's existing token. | — | 401 `USER_DEACTIVATED` immediately — not valid until 8h expiry. | High | Control | ITGC-AC-15 | Not Run | compliance.ts |
| UAT-SEC-024 | Revoke-all-sessions (incident response) | Admin (`users`) + target | target logged in (token) | 1. `POST /api/auth/users/{username}/revoke-sessions`. 2. `GET /api/auth/me` with the pre-existing token. | — | revoke → 200 `revoked_all:true`; pre-existing token → 401 (issued before the `tokens_valid_from` watermark). | High | Control | ITGC-AC-15 | Not Run | compliance.ts |
| UAT-SEC-025 | Audit hash chain verifies intact | Admin (`users`) | several mutating requests logged | 1. `GET /api/admin/audit/verify`. | — | `ok:true`, `rows_checked` > 0 — every `audit_log` row's hash recomputes and chains. | High | Control | ITGC-AC-16 | Not Run | compliance.ts |
| UAT-SEC-026 | Tampering past audit row is detected | DBA (simulated superuser) | chained audit_log | 1. disable the append-only trigger, `UPDATE audit_log SET actor='tamper'` on a past row, re-enable. 2. `GET /api/admin/audit/verify`. | — | `ok:false`, `reason:'hash mismatch'`, `broken_at` = the tampered seq — the edit is detected even though it bypassed the immutability trigger. | High | Control | ITGC-AC-16 | Not Run | compliance.ts |
| UAT-SEC-027 | Member OTP login sets httpOnly cookie (no JS-readable token) | loyalty member | member enrolled (shop + phone) | 1. `POST /api/member/auth/request-otp`. 2. `POST /api/member/auth/verify-otp` with the code. | `{phone, tenant_code, code}` | 200/201; response sets `ierp_token` (**HttpOnly**) + readable `ierp_csrf`; the member JWT is not exposed to JS. | High | Control | ITGC-AC-07 | Not Run | cookie-auth.ts |
| UAT-SEC-028 | Member cookie session authenticates self-scoped read | loyalty member | verify-otp cookie | 1. `GET /api/member/me` with the cookie only (no Authorization header). | cookie `ierp_token`+`ierp_csrf` | 200; returns the caller's own `member_code` (derived from the token, no member-id input). | High | Positive | ITGC-AC-07 | Not Run | cookie-auth.ts |
| UAT-SEC-029 | Member cookie mutation requires CSRF double-submit | loyalty member | verify-otp cookie | 1. `POST /api/member/refer` with the cookie but **no** `X-CSRF-Token`. | cookie only | 403 `CSRF` — cookie-authenticated mutation rejected without the matching `X-CSRF-Token`. | High | Control | ITGC-AC-07 | Not Run | cookie-auth.ts |
| UAT-SEC-030 | Member logout clears + revokes the session | loyalty member | verify-otp cookie | 1. `POST /api/member/auth/logout`. | cookie | 200; `Set-Cookie` expires `ierp_token` (Max-Age=0) and the member jti is denylisted (cannot be replayed). | Med | Control | ITGC-AC-07 / AC-15 | Not Run | cookie-auth.ts |
| UAT-SEC-031 | PDPA DSAR filed with statutory 30-day due date | DPO (`users`) | member with PII | 1. `POST /api/pdpa/dsar` `{subject_type:member, subject_ref, request_type:access}`. | — | `received`; `due_date` ≈ today + 30 days. | High | Control | PDPA-01 | Not Run | pdpa.ts |
| UAT-SEC-032 | PDPA access/portability export | DPO | DSAR filed; member has ≥1 receipt-upload submission (LYL-17) | 1. `POST /api/pdpa/dsar/:id/export`. | — | `completed`; bundle returns the subject's profile + per-purpose consents + points ledger + `receipt_submissions` (incl. `receipt_image`/`store_name`/`note`). | High | Positive | PDPA-01 | Not Run | pdpa.ts |
| UAT-SEC-033 | PDPA erasure redacts PII + issues pseudonym | DPO | erasure DSAR for a member with a receipt-upload submission | 1. `POST /api/pdpa/dsar/:id/erase`. 2. re-export the subject. | — | `erased:true` + `PDPA-ERASED-*` pseudonym; member name=`[erased]`, phone/email null; consents withdrawn; the receipt submission's `receipt_image`=`[erased]`, `store_name`/`note`=null while `purchase_amount`/`status` are unchanged. | High | Control | PDPA-02 | Not Run | pdpa.ts |
| UAT-SEC-034 | PDPA erasure pseudonymises the audit trail at read-time (stored row immutable) | DPO + DBA | erased member with PII in an audit row | 1. `GET /api/admin/audit` for the row. 2. read the raw `audit_log` row from the DB. | — | The viewer shows the pseudonym (no PII); the raw stored row still contains the original PII unchanged (AC-10/AC-16 chain intact). | High | Control | PDPA-02 / ITGC-AC-16 | Not Run | pdpa.ts |
| UAT-SEC-035 | PDPA DSAR is tenant-isolated | DPO of another tenant | DSAR in tenant A | 1. `GET /api/pdpa/dsar/:id` as a DPO in tenant B. | — | 404 `NOT_FOUND` — RLS prevents cross-tenant access to data-subject requests. | High | Control | PDPA-01 / ITGC-AC-03 | Not Run | pdpa.ts |
| UAT-SEC-036 | Cashier PIN quick-login succeeds | Cashier | Cashier (`pos_sell`) has a PIN set | 1. `POST /api/login/pin`. | `{username: cashier1, pin: '1234'}` | 200; body has `token` + `permissions` array; sets `ierp_token` (HttpOnly) + readable `ierp_csrf`. | High | Positive | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-037 | PosSupervisor PIN login + open shift (R08) | PosSupervisor | Supervisor (`pos_till`) has a PIN; no open till | 1. `POST /api/login/pin` (open-shift flag + opening float). 2. `GET /api/payments/till/current`. | `{username: possup1, pin: '4321', open_shift: true, opening_float: 1000}` | 200 token; a till session is opened; `till.current.open` is non-null; re-issuing does not open a 2nd shift. | High | Positive | ITGC-AC-17 / R08 | Not Run | pos-pin.ts |
| UAT-SEC-038 | Self set/rotate own PIN (current-password step-up) | Cashier | Cashier logged in | 1. `POST /api/auth/me/pin` with correct current password. 2. `POST /api/login/pin` with the new PIN. | `{current_password: 'pw1234', pin: '5678'}` | set → 200; subsequent PIN login → 200 with `token`. | High | Positive | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-039 | Admin sets a staff PIN | AccessAdmin (`users`) + Cashier | Cashier with no PIN | 1. `POST /api/auth/users/cashier2/pin`. 2. Cashier `POST /api/login/pin`. | `{pin: '2468'}` | set → 200; cashier PIN login → 200 with `token`. | High | Positive | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-040 | Wrong PIN rejected (generic) | Cashier | Cashier has a PIN | 1. `POST /api/login/pin` with the wrong PIN. | `{username: cashier1, pin: '0000'}` | 401 `UNAUTHORIZED` (generic, no enumeration); no token. | High | Control | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-041 | Privileged account blocked from PIN (PIN_NOT_ALLOWED) | FinancialController / Admin | Privileged account (requires MFA) | 1. `POST /api/auth/me/pin` (set a PIN). 2. `POST /api/login/pin`. | `{current_password, pin: '1357'}` / `{username: fincon, pin: '1357'}` | both blocked with `PIN_NOT_ALLOWED` (set → 400; use → 401) — privileged/finance roles cannot set or use a PIN. | High | Control | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-042 | Malformed PIN rejected (DTO) | Cashier | — | 1. `POST /api/login/pin` with a non-4–6-digit PIN. | `{username: cashier1, pin: '12'}` and `{pin: 'abcd'}` | 400 (DTO validation), not 401/500. | Med | Control | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-043 | Repeated wrong PIN trips the account lockout (AC-07) | Cashier | Cashier has a PIN | 1. `POST /api/login/pin` with a wrong PIN N times until locked. | wrong `pin` × N | After the threshold → 429 `LOGIN_LOCKED` (the same per-account throttle as wrong-password login). | High | Control | ITGC-AC-17 / ITGC-AC-07 | Not Run | pos-pin.ts |
| UAT-SEC-044 | Self set-PIN with wrong current password blocked | Cashier | Cashier logged in | 1. `POST /api/auth/me/pin` with a wrong current password. | `{current_password: 'WRONG', pin: '9999'}` | 400 `BAD_CURRENT_PASSWORD`; the PIN is not changed. | High | Control | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-045 | Clear-PIN disables PIN login | AccessAdmin (`users`) + Cashier | Cashier has a working PIN | 1. `DELETE /api/auth/users/cashier1/pin`. 2. `POST /api/login/pin` with the old PIN. | `{username: cashier1, pin: '1234'}` | clear → 200; subsequent PIN login → 401 `UNAUTHORIZED` (no PIN on the account); password login still works. | High | Control | ITGC-AC-17 | Not Run | pos-pin.ts |
| UAT-SEC-046 | Permission change revokes outstanding sessions immediately | AccessAdmin + target user | target user logged in | 1. Target calls any permitted API (200). 2. Admin `PATCH /api/admin/users/<target>` with narrowed `permissions`. 3. Target retries with the SAME token. 4. Target re-logs-in and retries. | narrowed permission set | (2) 200 `sessions_revoked: true`; (3) **401 `TOKEN_REVOKED`** — no TTL lag; (4) allowed per the new (narrower) set. | High | Control | ITGC-AC-15, ITGC-AC-02 | Not Run | onboarding.ts |
| UAT-SEC-047 | Employee DSAR: access returns decrypted identifiers; erasure keeps statutory payroll | DPO (perm `users`) | employee with citizen ID/bank account + ≥1 payslip | 1. `POST /api/pdpa/dsar` (employee, access) → `/export`. 2. `POST /api/pdpa/dsar` (employee, erasure) → `/erase`. 3. Raw SQL on `employees` + `payslips`. | emp_code | (1) bundle carries the decrypted citizen ID/SSO/bank account + payslips + retention note; (2) completed with pseudonym `PDPA-ERASED-EMP-…`; (3) employee master redacted (`[erased]`, identifiers null, inactive) while the payslip row is intact incl. its ID snapshot (statutory carve-out, recorded in the DSAR result). | High | Control | PDPA-02, ITGC-AC-19 | Not Run | pdpa.ts |
| UAT-SEC-048 | LINE chat governance: link registry, force-unlink (offboarding), rate limit | AccessAdmin/Admin (`users`); linked staff | Several staff LINE-linked | 1. `GET /api/line/links` as `users` holder and as a non-`users` user. 2. `DELETE /api/line/links/<departed user>`. 3. The departed user's LINE sends a command. 4. A linked user sends commands past the per-user budget. | rate limit 3/window (env-tuned for test) | (1) registry lists linked users with **masked** LINE ids; non-`users` → 403. (2) unlinked + `[chat:admin-unlink]` audit row. (3) not-linked guidance — the channel is dead immediately. (4) first excess gets ONE throttle reply, further excess drops silently; `[chat:throttled]` audit row. | High | Control | ITGC-AC (chat channel) | Pass | line-crm.ts |
| UAT-SEC-049 | LINE OA go-live: Channel secret required + webhook receipt health | AccessAdmin (`users`/`exec`) | tenant with no LINE creds | 1. `PUT /api/messaging/providers/line` with `{token}` only. 2. Repeat with `{token, secret}`. 3. POST a webhook delivery with a **tampered** `X-Line-Signature`. 4. `GET /api/messaging/providers`. 5. POST a correctly signed delivery; re-read providers. | any token/secret pair | (1) 400 `MISSING_FIELD` — token-only creds refused; (2) saved, readiness shows `webhook_secret_set: true` + `webhook_path: /api/line/webhook/<code>` (secret never returned); (3) **401 `BAD_WEBHOOK_SIGNATURE`** (fail closed); (4) `last_webhook_status: bad_signature` with timestamp; (5) flips back to `verified`. | High | Control | ITGC-AC (chat channel) | Pass | line-crm.ts |
| UAT-SEC-050 | LINE OA go-live: test-push to the admin's own LINE | AccessAdmin (`users`/`exec`) | LINE channel configured | 1. `POST /api/messaging/providers/line/test-self` while the admin has NO linked LINE. 2. Link the admin (`link <code>` from their LINE). 3. Repeat the test-self call. | — | (1) 400 `NOT_LINKED` (explicit — silence explained, channel unaffected); (3) `status: sent` to the admin's own LINE userId, audit-logged with campaign `line_test`. | Medium | Positive+Control | ITGC-AC (chat channel) | Pass | line-crm.ts |
| UAT-SEC-051 | Platform-owner "god" sees all companies cross-org; per-tenant Admin stays org-scoped | Two org-scoped Admins (org-1, org-2) + a god (org-1 Admin whose username is in `PLATFORM_ADMIN_USERNAMES`) | `TENANCY_MODE=multi-company`; ≥1 data row per company across org-1 (2 tenants), org-2, and an org-null signup | 1. As the god, `GET /api/jobs?type=mc_seed`. 2. As the org-1 Admin (not a platform owner), same call. 3. Remove the god's username from `PLATFORM_ADMIN_USERNAMES`; repeat step 1. | — | (1) sees **ALL** companies (count = 4) — the global bypass; (2) sees only its own org's tenants (count = 2), never another company; (3) the ex-god now sees only its own org (count = 2) — god is env-gated, not a role, so it cannot be self-assigned in-app. | High | Control | ITGC-AC-18 | Not Run | pg-core.ts |
| UAT-SEC-053 | Platform Console: enriched company directory + platform-owner-only access | A god (in `PLATFORM_ADMIN_USERNAMES`) + a per-tenant Admin | ≥2 companies provisioned; each with a subscription | 1. As the god, `GET /api/admin/tenants`. 2. As the per-tenant Admin (not a platform owner), same call. 3. As the god in the web app, confirm the **ศูนย์ควบคุมแพลตฟอร์ม** nav entry is present; as the per-tenant Admin, confirm it is absent. | — | (1) 200 with **all** companies, each carrying `status`/`plan_code`/`users`/`trial_ends_at`/`created_at`; (2) **403 PLATFORM_ADMIN_REQUIRED**; (3) the `/platform` nav entry shows only for the god (gated on `is_platform_owner`), never for a per-tenant Admin. | High | Positive+Control | ITGC-AC-18 | Not Run | onboarding.ts |
| UAT-SEC-052 | God company-switcher: `X-Act-As-Tenant` narrows the god to ONE company; non-god header ignored | A god (in `PLATFORM_ADMIN_USERNAMES`) + a per-tenant org-2 Admin | `TENANCY_MODE=multi-company`; ≥1 data row per company across org-1 (2 tenants), org-2, org-null | 1. As the god, `GET /api/admin/tenants` (the switcher's directory). 2. As the god, `GET /api/jobs?type=mc_seed` **with header `X-Act-As-Tenant: <org-2 tenant id>`**. 3. Repeat step 2 with a different company id (an org-1 sibling). 4. As the org-2 Admin (not a god), send the same call with `X-Act-As-Tenant: <org-1 tenant id>`. 5. Confirm `GET /api/auth/me` exposes `is_platform_owner`. | — | (1) lists **all** companies (id/code/name/suspended). (2) sees **only** the chosen company (count = 1) — bypass dropped, scoped as that company's Admin would be. (3) sees **only** the newly chosen company (count = 1) — re-scopes per selection. (4) header **ignored** — the non-god still sees only its own company (count = 1), no widening. (5) `is_platform_owner: true` for the god, absent/false for others (drives the UI switcher). | High | Positive+Control | ITGC-AC-18 | Not Run | pg-core.ts |
| UAT-SEC-054 | Public signup files a request (no tenant created); god approval provisions it; prod self-serve always off | (public) + platform owner (god) | request-access enabled; god in `PLATFORM_ADMIN_USERNAMES` | 1. `POST /api/auth/signup-requests` with the new-company details. 2. Count tenants (none created for this request yet). 3. As the god, `GET /api/admin/signup-requests?status=pending` then `POST …/signup-requests/:id/approve`. 4. Log in as the newly-provisioned Admin. 5. (unit) production `POST /api/auth/signup` self-serve. | `{tenant_code: REQCO, admin_username: reqadmin, password: secret12}` | (1) request recorded **pending** — **no tenant provisioned**; (2) tenant count unchanged; (3) approve → the company is provisioned (tenant + Admin + trial + fiscal year); (4) the provisioned Admin logs in; (5) `signup-gate.test.ts` — production self-serve signup is **always disabled** (`isSignupAllowed()` false; `PUBLIC_SIGNUP_ENABLED` is a no-op). | High | Positive+Control | ITGC-AC-18 | Not Run | onboarding.ts, signup-gate.test.ts |
| UAT-SEC-055 | RoPA register — create a processing activity with a validated legal basis | DPO (`users`) | logged in as a DPO | 1. `POST /api/pdpa/ropa` `{name:'Loyalty membership', purpose:'…', legal_basis:'consent', sub_processors:['Anthropic','Stripe'], retention_period:'3 years', cross_border:'SCCs — Anthropic (US)'}`. 2. `POST /api/pdpa/ropa` with `legal_basis:'vibes'`. 3. `GET /api/pdpa/ropa`. | as left | (1) 201; the activity is created with its legal basis + sub-processors + cross-border basis captured. (2) **400** — an invalid legal basis is rejected (only the PDPA/GDPR set is accepted). (3) the register lists the activity. | High | Positive+Control | PDPA-03 | Pass | pdpa.ts |
| UAT-SEC-056 | RoPA — update/retire an activity + active filter + RLS isolation | DPO (`users`) + another tenant's DPO | a RoPA activity exists (UAT-SEC-055) | 1. `POST /api/pdpa/ropa/:id` `{retention_period:'5 years', active:false}`. 2. `GET /api/pdpa/ropa?active=1`. 3. As **another tenant's** DPO, `GET /api/pdpa/ropa/:id`. | as left | (1) 201; the retention period is updated and the activity is retired (`active:false`). (2) the active-only register **excludes** the retired activity. (3) **404** — RLS isolates the register per tenant (another company's DPO cannot read it). | High | Control | PDPA-03 | Pass | pdpa.ts |
| UAT-SEC-057 | PII retention sweep is opt-in + guarded (default-off, 12-month floor, dry-run) | DPO (`users`) | an aged member (last activity > window) + a recent member in the tenant | 1. `POST /api/pdpa/retention/sweep` with **no** policy set. 2. `PUT /api/pdpa/retention` `{subject_type:'member', retain_months:6, enabled:true}`. 3. `PUT` with `retain_months:36, enabled:true`. 4. `POST /api/pdpa/retention/sweep` `{dry_run:true}`. | as left | (1) no-op — `policies:0, swept_total:0` (**default-off**). (2) **400 `RETENTION_TOO_SHORT`** (floor 12 months). (3) policy saved + enabled. (4) dry-run lists the aged member as a candidate and **redacts nothing** (their name/phone intact). | High | Control | PDPA-04 | Pass | pdpa.ts |
| UAT-SEC-058 | Retention sweep anonymizes only aged members, records the ledger, and is idempotent | DPO (`users`) | the enabled 36-month policy from UAT-SEC-057 | 1. `POST /api/pdpa/retention/sweep`. 2. Read both members + `pdpa_erasures`. 3. Run the sweep again. 4. (scheduler) create + run a BI subscription `report_type:'pii_retention_sweep'` in a tenant with no policy. | as left | (1) `swept_total:1` — the aged member is anonymized (name `[erased]`, phone/email null, deactivated) via the same path as a DSAR erasure. (2) an append-only `pdpa_erasures` row exists with a pseudonym and **`dsar_id` NULL** (marks retention-swept); the recently-active member is untouched. (3) `swept_total:0` — idempotent. (4) job runs `success` and sweeps nothing (opt-in only). | High | Positive+Control | PDPA-04 | Pass | pdpa.ts, bi.ts |
| UAT-SEC-059 | Encryption-key rotation: keyring re-encrypts at-rest ciphertext, stays inert unconfigured, fails closed | DevOps/Security + DPO | an employee row with an encrypted `national_id` (`v1:` ciphertext); BI scheduler access | 1. Run the `key_rotation_sweep` job with **no** keyring env. 2. Set `APP_ENC_KEYRING='{"2":"<secret>"}'` + `APP_ENC_ACTIVE_KID=2`; run the job. 3. Read the raw column + the decrypted value; run the job again. 4. (unit) decrypt a `v2:` blob whose kid is missing from the ring; relabel a `v2:2:` blob to `v2:3:` and decrypt. | as left | (1) `success`, summary "re-encrypted **0**" — inert by default. (2) the `v1:` ciphertext becomes **`v2:2:`** and the application still decrypts the original value. (3) re-run rotates **0** (idempotent; the embedded key id is the discriminator). (4) missing kid → **throws** (fail closed, never returns ciphertext as plaintext); relabelled kid → GCM auth failure (per-kid HKDF label separation). | High | Control | ITGC-AC-12 | Pass | bi.ts, key-rotation.test.ts |
| UAT-SEC-060 | Public pricing page renders with no session and is linked from login/signup | Prospect (no account) | none — clean browser, no cookies | 1. Open `/plans` directly. 2. Open `/login` and `/signup`; click the **ดูแพ็กเกจและราคา / View plans & pricing** link. | as left | (1) The page renders **without any sign-in**: 5 starter packs (Essential ฿2,900 · Growth ฿4,900 "ยอดนิยม", preselected · Scale ฿9,900 · Franchise ฿14,900 · Enterprise เริ่มต้นที่ ฿19,900), the add-on list, and the summary showing **฿4,900/เดือน**. Thai by default; the ไทย/EN toggle switches every label. (2) Both auth pages link to `/plans`. | Medium | Positive | — (no control; marketing page) | Pass | pricing.spec.ts |
| UAT-SEC-061 | Pricing calculator math — annual = 10 × monthly, itemized add-ons, mobile bottom bar | Prospect (no account) | UAT-SEC-060 | 1. Toggle **รายปี/Annual**. 2. Toggle on the supply-chain (฿1,500) and webhook (฿990) add-ons; read the summary. 3. Toggle one back off. 4. (phone viewport) open `/plans`, toggle an add-on, expand the bottom-bar summary. | as left | (1) Growth shows **฿4,083/เดือน** (4,900×10÷12) with ฿4,900 struck and "ชำระ ฿49,000/ปี"; savings −฿9,800/ปี. (2) Summary itemizes each add-on and totals **฿6,158/เดือน** (7,390×10÷12), "ชำระรายปี ฿73,900/ปี". (3) The line disappears and the total drops accordingly. (4) The fixed bottom bar shows the live total, expands to the itemized summary, and the page has **no horizontal overflow** at 390px in every state. | Medium | Positive | — (no control; marketing page) | Pass | pricing.spec.ts, pricing.mobile.spec.ts |
| UAT-SEC-062 | Pricing page exposes no tenant data; CTAs route into the governed signup flow | Prospect (no account) | none | 1. Load `/plans` with the network panel open. 2. Click **สมัครใช้งาน / Start free trial** on a standard tier. 3. Select Enterprise; click **ติดต่อฝ่ายขาย / Contact sales**. | as left | (1) No authenticated API is called and no tenant/company data appears — the page is static content (the only dynamic state is client-side selection). (2)+(3) Both CTAs land on `/signup` — the god-approved request-access flow (UAT-SEC-054), so a prospect can never self-provision from the pricing page. Enterprise shows "เริ่มต้นที่ / starting at" and the custom-pricing note. | Medium | Negative | — (routes into ITGC-AC-18 signup gate) | Pass | pricing.spec.ts |
| UAT-SEC-063 | Pack selection carries from /plans through signup to provisioning; add-on suites gate under enforce | Prospect → Platform owner (god) | seeded plan catalogue (incl. `franchise` ฿14,900/เดือน) | 1. On `/plans` pick Growth · Annual · toggle 1 add-on; click สมัครใช้งาน and submit the request (junk add-on injected via URL too). 2. God opens the Onboarding queue. 3. God clicks อนุมัติ; read the company's subscription. 4. (enforce mode, `ENTITLEMENTS_ENFORCE=1`) a Standard-plan tenant calls an RFQ endpoint without the add-on, then with `addons:['scm_advanced']`; set an unknown add-on via `POST /api/admin/tenants/:id/addons`. | as left | (1) `/signup` shows **แพ็กเกจที่เลือกจากหน้าราคา**; the request stores `requested_plan='business'` (mapped), `requested_interval='annual'`, add-ons with the junk value **dropped**. (2) The queue row shows **แพ็กเกจที่ขอ** business · รายปี · +add-on. (3) The provisioned subscription is `plan_code='business'`, `billing_interval='annual'`, `addons=['cdp']` — not Free. (4) Without the add-on → **403 `SUITE_NOT_ENTITLED`**; with it → allowed; unknown key → **400 `UNKNOWN_ADDON`**; all gates inert with enforcement off (legacy default). | High | Positive+Control | — (strengthens ITGC-AC-18 fidelity; commercial gate) | Pass | onboarding.ts, plan-gating.ts |
| UAT-SEC-064 | Platform Console: review each plan's modules, a request's pack preview, and a company's add-ons | Platform owner (god) | ≥1 company on `business` with add-on `cdp`; a pending signup request carrying `business · annual · cdp` | 1. Open `/platform` → **แพ็กเกจ & โมดูล**. 2. Open **Onboarding**; read the request row. 3. Open **บริษัท**; read the plan column; click the company name; in the drawer tick add-on **sandbox** and press **บันทึกโมดูลเสริม**. | as left | (1) One card per sellable plan showing price (฿/เดือน + ฿/ปี), seats/branches caps (−1 → **ไม่จำกัด**; custom plan → **ราคาตามตกลง**), every included module as a chip (add-on suites amber), and which add-ons are included vs à-la-carte. (2) The request row shows `business · รายปี`, the requested add-on by its label (ไม่ใช่ key ดิบ), and a 4-chip module preview + `+n` for the pack approve will provision. (3) The plan column shows `+ โมดูลเสริม 1 รายการ`; the drawer lists the four add-ons with prices (an add-on already granted by the plan reads **รวมในแพ็กเกจแล้ว**), the **โมดูลที่เปิดใช้ทั้งหมด** chips update live as boxes are ticked, and saving calls `POST /api/admin/tenants/:id/addons` with the new set — entitlements apply immediately. | Medium | Positive | — (read/UI over ITGC-AC-18 + 0451 add-ons; no new control) | Pass | platform-plans.spec.ts |
| UAT-SEC-065 | Onboarding decisions email the requester; outbox is god-only with a deliver sweep | Platform owner (god) + prospect | a pending signup request with an email; an invite issued with an email; `MAIL_PROVIDER` unset (mock) | 1. Approve the request; reject a second request with a reason. 2. `GET /api/admin/emails` as god; same call as a company Admin. 3. `POST /api/admin/emails/deliver-pending`. 4. Re-read the outbox. | as left | (1) Three outbox rows appear **Queued**: `signup_invite` (to the invitee), `signup_approved` (to the requester, `about_tenant_id` = the provisioned company, subject carries the company name), `signup_rejected` (to the rejected requester). The decisions themselves succeed even if mail is misconfigured (fire-and-forget). (2) God sees the outbox; a company Admin gets **403** (platform-admin only). (3) The sweep reports `sent ≥ 3, failed 0`. (4) All three rows are **Sent** with `provider=mock` + a provider message id + `sent_at`. | Medium | Positive+Control | — (operational evidence for ITGC-AC-18; no new control) | Pass | onboarding.ts §3d-bis, mailer-templates.test.ts |
| UAT-SEC-066 | SaaS lifecycle sweep: reminders, grace auto-suspend, dunning ladder, ฿0 activation — idempotent | Platform owner (god) | a paid-plan Trialing company with a contact email; `MAIL_PROVIDER` unset (mock) | 1. Set the trial to end in ~12h; `POST /api/admin/saas-lifecycle/run` twice. 2. Backdate the trial end 10 days; run. 3. Reactivate; set the subscription PastDue; run; backdate the `dunning_1` event 22 days; run. 4. Reactivate; set Active; run. 5. Switch to the `free` plan with an expired trial; run. 6. `GET /api/admin/saas-lifecycle/events` as god and as a company Admin. | as left | (1) First run fires **both** reminders once (`trial_reminder_7` + `trial_reminder_1`; two `trial_reminder` outbox emails to the company contact); the immediate re-run does **nothing** (dedup). (2) The company is **auto-suspended** — `suspended_by = 'saas_lifecycle (auto)'`, reason mentions the expired trial, a `company_suspended` email queues — and later sweeps **skip** it. (3) PastDue starts the ladder (`dunning_1` + `payment_failed` email); with the anchor ≥21 days old the sweep **suspends** with the past-due reason. (4) Recovery closes the ladder (`dunning_cleared`). (5) The ฿0 plan **activates** (status Active — never suspended). (6) God sees ≥6 ledger rows for the company; a company Admin gets **403**. | High | Positive+Control | — (automates the existing ITGC-AC-18 suspend authority; evidence ledger) | Pass | onboarding.ts §3g-bis, saas-lifecycle.test.ts |
| UAT-SEC-067 | Tenant self-serve add-on purchase + checkout add-on line items (own tenant only) | Company admin (`users`) | a tenant on a paid plan; `STRIPE_SECRET_KEY` unset (mock) | 1. `POST /api/billing/addons` with an unknown key; then with `['cdp','sandbox']`. 2. `GET /api/billing/subscription`; read another tenant's `subscriptions.addons`. 3. `POST /api/billing/checkout` with an unknown add-on; with `currency USD` + an add-on; `plan business · annual · ['cdp']`; `plan franchise · annual · ['sandbox']`. | as left | (1) Unknown key → **400 `UNKNOWN_ADDON`**; the valid set saves (200, `billing.mock=true` without Stripe — entitlement-only) and applies **only to the caller's own tenant**. (2) The subscription echoes the purchased set; the other tenant's add-ons are untouched (BOLA-safe by construction). (3) Unknown key → 400; non-THB + add-ons → **400 `ADDON_CURRENCY_UNSUPPORTED`**; the annual checkout prices the add-on at **10× monthly** and totals plan+add-ons (49,000 + 12,900 = **61,900**); an add-on the plan already includes is **dropped** (franchise ⊇ sandbox → total 149,000, no add-on line). With Stripe live, add-ons ride the same subscription as separate recurring line items and mid-cycle changes prorate. | Medium | Positive+Control | — (no new control; pricing fail-closed) | Pass | onboarding.ts §3g-ter |
| UAT-SEC-068 | Own-SaaS receipts: webhook + manual recording, own-tenant reads, printable document | Platform owner (god) + company admin | a tenant with `stripe_customer_id` set; `MAIL_PROVIDER`/`STRIPE_*`/`RECEIPT_ISSUER_TAX_ID` unset | 1. POST the Stripe webhook `invoice.paid` (amount_paid 490000) twice. 2. Read the outbox. 3. God `POST /api/admin/tenants/:id/receipts` (฿14,900, งวด, note). 4. As the company admin: `GET /api/billing/receipts` + `GET /api/billing/receipts/<no>/pdf`; repeat the pdf call as ANOTHER tenant's admin. | as left | (1) Exactly **one** `RCPT-S-######` receipt (฿4,900, source `stripe_invoice`) — the duplicate delivery converges (source_ref unique) — and the subscription flips **Active** (the A2 dunning-recovery signal). Without `RECEIPT_ISSUER_TAX_ID` the receipt is **plain** (`vat_amount` NULL — no tax-invoice claim). (2) A `saas_receipt` email to the company address carries the receipt number. (3) 201 with a new RCPT-S number; the customer is emailed. (4) The tenant sees exactly its own receipts (newest first); the printable document renders (PDF, or HTML fallback in CI) with ใบเสร็จรับเงิน + the amount; another tenant's fetch of the same number → **404** (BOLA-safe, never 403). | Medium | Positive+Control | — (platform revenue paper trail; no new numbered control) | Pass | onboarding.ts §3g-quater |
| UAT-SEC-069 | Entitlement-enforcement rollout: observation ledger, per-tenant cohort, upsell dialog | Platform owner (god) + company user | a tenant (id T) on `starter` (no `procurement` suite); `ENTITLEMENTS_ENFORCE`/`ENTITLEMENTS_SHADOW` off | 1. `ENTITLEMENTS_SHADOW=true`: as a `Procurement` user of T call a `procurement` route twice; as god `GET /api/admin/entitlement-observations`; repeat the GET as a company Admin. 2. `ENTITLEMENTS_ENFORCE_TENANTS=T` (global flags **off**): repeat the route as the T user, as T's own Admin, and as a user of another tenant on the same plan; as god. 3. In the web app trigger the same 403 (or any plan-level deny). 4. Parse check: set the cohort to ` 1, 2,bogus,0,2 `. | as left | (1) Both requests are **allowed** (shadow) but exactly **one** observation row lands (day × tenant × code × mode dedup — in-process gate + `dedup_key` UNIQUE): mode `shadow`, code `SUITE_NOT_ENTITLED`; the god rollup shows T with the code and tenant name; a company Admin's read → **403** (god-only). An entitled route records **nothing**. (2) The cohort tenant is truly **blocked** (403 `SUITE_NOT_ENTITLED`, observation mode `enforce`) — including its own per-tenant **Admin** (no bypass; only god bypasses) — while the non-cohort tenant stays un-gated (legacy); an entitled plan in the cohort still passes. (3) The app shows the **สิทธิ์การใช้งานตามแพ็กเกจ** dialog with the server's localized reason and a **ดูแพ็กเกจ & ชำระเงิน** CTA that lands on `/billing` (internal only; repeated parallel 403s open ONE dialog). (4) The cohort parses to exactly {1, 2} (whitespace kept, bogus/zero dropped). The guard's observation write can never fail a request (fire-and-forget; DB error → debug log only). | High | Positive+Control | — (staged rollout of the existing 1.2 gating; no new numbered control) | Pass | plan-gating.ts (wave B), onboarding.ts §3g-quinquies, platform-plans.spec.ts |
| UAT-SEC-070 | Thai payment rails: payment-info QR, slip claims, god verify queue (approve/reject) | Platform owner (god) + company admin | a tenant on `business` (฿4,900/mo, monthly, no add-ons) with a contact email; `PLATFORM_PROMPTPAY_ID`/`PLATFORM_BANK_ACCOUNT` unset | 1. `GET /api/billing/payment-info`; set `PLATFORM_PROMPTPAY_ID=0812345678` + `PLATFORM_BANK_ACCOUNT`; repeat. 2. `POST /api/billing/payment-claims` (฿4,900, งวด, `slip_ref TXN-778899`, note); repeat the same slip_ref. 3. List claims as ANOTHER tenant's admin; `GET /api/admin/payment-claims` as a company admin, then as god. 4. Set the subscription PastDue; god `POST …/:id/approve`; approve again. 5. File a second claim; god `POST …/:id/reject` with a reason; list own claims. | as left | (1) Without the envs the rails hide (`qr_payload`/`bank_details` null) but `amount_due` still prices **฿4,900** (plan + add-ons via the ONE A3 rule); with them the QR is a genuine **dynamic EMVCo PromptPay payload** (`000201…`, THB `5303764`, amount tag `54074900.00`) + a scannable PNG + the bank text. (2) 201 **Pending** (a god-inbox notification fires); the same slip again → **400 `DUPLICATE_SLIP`**. (3) The other tenant sees **0** of these claims (data-leak test); the verify queue is god-only (**403** for a company admin) and shows the claim with the tenant name joined. (4) Approve → the A4 receipt (**RCPT-S**, source `bank_transfer`, idempotent on `claim:<id>`) + the claimed amount + a `saas_receipt` email, and the subscription flips **Active** (dunning-recovery); a second approve → **400 `CLAIM_NOT_PENDING`**. (5) Reject → a reasoned `payment_claim_rejected` email; the tenant's own list shows **Rejected** + the reason. | High | Positive+Control | — (platform revenue collection; the receipt stays god-verified — a claim is never money) | Pass | onboarding.ts §3g-sexies, platform-plans.spec.ts (payments tab) |
| UAT-SEC-071 | Console ops depth: god email push, full tenant export, MFA/IP hardening, health alert thresholds | Platform owner (god) + company admin | god `owner1` WITHOUT TOTP enrolled; a tenant with billing history; all wave-D envs unset | 1. Set `PLATFORM_ALERT_EMAIL`; trigger any god-inbox event (e.g. a tenant files a payment claim); read the outbox; unset. 2. God `GET /api/admin/tenants/:id/export`; repeat as a company admin. 3. Set `PLATFORM_IP_ALLOWLIST=203.0.113.0/24`; call any admin route as god; add `127.0.0.1`; retry; unset. 4. Set `PLATFORM_REQUIRE_MFA=true`; call an admin route as the TOTP-less god; unset; retry. 5. Unit: `evaluateHealthAlerts` + `ipv4InAllowlist` vectors. | as left | (1) A `platform_alert` email queues to the alert mailbox with the event title in its subject — the inbox row still lands (email is additive; failures never break the emitting action). (2) 200 with ONE JSON document: the tenant row + every tenant-scoped table auto-discovered from the schema (`subscriptions`, `saas_receipts` … present; >50k-row tables flagged `truncated`); a company admin gets **403** (god-only). (3) Outside the list → **403 `PLATFORM_IP_BLOCKED`**; with the caller's address listed → 200; unparsable/IPv6 peers fail CLOSED when a list is set. (4) **403 `PLATFORM_MFA_REQUIRED`** until TOTP is enrolled (live DB check — a stale token claim cannot pass); knob off → 200 (default behaviour byte-identical). (5) Defaults trip at pool ≥80% / any failed/stuck job / ≥500 queued / stale scheduler; env overrides move the trip points; a `never`-run scheduler does not alert. | High | Positive+Control | — (optional hardening + ops visibility; strengthens ITGC-AC-18 operationally, no new numbered control) | Pass | onboarding.ts §3g-septies, health-alerts.test.ts |
| UAT-SEC-072 | Slip pre-fill: mini-QR exact ref + doc-ai extraction honesty ladder | Company admin (`users`) | no `ANTHROPIC_API_KEY` (CI) | 1. `POST /api/doc-ai/slip-extract` with pasted Thai slip text (จำนวนเงิน 4,900.00 บาท, เลขที่รายการ 014000601034578, date). 2. Same endpoint with an image data URL. 3. Unit: `slipTransferRef` TLV vectors (top-level, nested container, digits-only vs mixed, bare ref, non-TLV). 4. Web: upload a slip image on the /billing claim form (stubbed extraction). | as left | (1) `source rules` with amount **4900**, transfer_ref **014000601034578**, date — deterministic, no AI. (2) `source none` with ALL-NULL fields (no key + image = honestly empty, never a guess). (3) The nested container contributes only sub-values; mixed alphanumeric outranks digits-only; a bare reference-shaped payload passes through; Thai prose → null. (4) The form pre-fills ref/amount/งวด from the extraction, shows อ่านสลิปแล้ว — ตรวจสอบข้อมูลก่อนกดแจ้งโอน, and submits ONLY on the user's click — the uploaded image is re-encoded to a bounded JPEG before leaving the browser. | Medium | Positive+Control | — (pre-fill only; the god verify queue against the bank statement stays the control) | Pass | onboarding.ts §3g-octies, slip-extract.test.ts, billing-slip.spec.ts |
| UAT-SEC-073 | Platform-owner surface is audited on READS and on REFUSALS, not only writes; ordinary tenant reads stay unaudited | Platform owner (god) + company admin | god `owner1`; ≥2 companies; audit viewer reachable (`users`) | 1. As god, `GET /api/admin/tenants` and `GET /api/admin/tenants/:id/export` (the whole-company export). 2. As god, `POST /api/admin/tenants/:id/tags` (any mutation). 3. As a company admin, `GET /api/tenant/profile` (an ordinary tenant read). 4. Open the audit viewer (`GET /api/admin/audit`) and filter by actor `owner1`. 5. As a company admin (NOT the platform owner), call `GET /api/admin/tenants`. 6. Set `PLATFORM_REQUIRE_MFA=true` and repeat as the god while its TOTP is off; unset. 7. `GET /api/admin/audit/verify`. | as left | (1) BOTH god reads appear in `audit_log` — actor `owner1`, action `GET /api/admin/tenants…`, status `success`, `meta.platform_read=true` **and** `meta.rls_bypass=true` (the export is the sensitive one: it streams every row of every tenant-scoped table for one company, and before this change it left no trace at all). (2) The god mutation appears with `meta.rls_bypass=true` and **no** `platform_read` — the scope marker now lands on writes too (it was previously read before `TenantTxInterceptor` set it and was silently never written). (3) **No** audit row is created for the ordinary tenant read — reads are RLS-confined and would drown the chain; only the god surface is audited on reads. (4) Every row is attributable to the named operator with its request id. (5) **403 `PLATFORM_ADMIN_REQUIRED`** — and the REFUSAL is itself audited: a `status=fail` row for that actor with `meta.platform_denied=true` and `meta.error=PLATFORM_ADMIN_REQUIRED` (an API-key principal additionally records `meta.api_key_prefix`). This needs a separate write path because Nest runs guards BEFORE interceptors, so a guard rejection never reaches the audit interceptor — denied fleet access previously left no trace at all. (6) **403 `PLATFORM_MFA_REQUIRED`**, likewise audited with `platform_denied` + that code; the same holds for `PLATFORM_IP_BLOCKED` when an allowlist is set. (7) `ok=true` — every added row (success, read and refusal) extends the hash chain cleanly (ITGC-AC-16 intact). |
| UAT-SEC-074 | Platform-surface safety rails: read-only view blocks fleet mutations; forced catalogue purge is off by default and preview-bound | Platform owner (god) | ≥2 companies; a shared item still referenced by company A (e.g. on a PO); `ALLOW_ITEM_FORCE_PURGE` unset | 1. Enter company B via the switcher in **read-only** mode; from there call `POST /api/admin/tenants/:id/suspend` (a platform-surface action) and `GET /api/admin/tenants`. 2. Leave read-only mode and repeat the suspend. 3. With `ALLOW_ITEM_FORCE_PURGE` unset, call `POST /api/admin/item-maintenance/force-purge` with the correct confirm phrase `FORCE-PURGE-ITEMS`. 4. Set `ALLOW_ITEM_FORCE_PURGE=1`; call force-purge with the WRONG confirm phrase; then with the right phrase but an `expected_ref_rows` that does not match. 5. Call `POST .../force-preview`, then force-purge echoing its `total_ref_rows`. 6. Unset the variable again. | as left | (1) The suspend is refused **403 `READONLY_IMPERSONATION`** — the look-only rail now covers the platform screens, not only the company's own; the GET still returns 200 (you can keep looking). (2) Served normally — the rail is opt-in, not a new block. (3) **403 `FORCE_PURGE_DISABLED`** however correct the confirm phrase: the route is inert on a normal deploy, so a leaked platform session cannot wipe the shared catalogue. (4) **400 `CONFIRM_MISMATCH`**, then **409 `BLAST_RADIUS_MISMATCH`** carrying the live `expected`/`items`/`by_tenant` figures. Verify after BOTH refusals that the target product is **still present** — a refusal must never partially destroy. (5) Only the fully-gated call deletes the product and its referencing rows in every company (nothing dangles). (6) The route is refused again — the maintenance window is closed. |
| UAT-SEC-075 | Audit completeness is provable, and bulk exports are audited events | Company admin (`users`) | a tenant with recent activity; DB access for the negative step | 1. Post a few ordinary mutations, then `GET /api/admin/audit/verify`. 2. Simulate rows that were never persisted: raise one tenant's `audit_expectations.expected` above the number of audit rows written; re-run verify; restore it. 3. `GET /api/admin/audit/export` (CSV) and `GET /api/masterdata/customers/export`. 4. `GET /api/admin/audit?limit=5` (an ordinary read of the same viewer). 5. Filter the audit viewer by actor for steps 3-4. | as left | (1) `ok=true` with `completeness.available=true`, `completeness.ok=true` and no shortfalls — every committed mutation is accounted for (`written >= expected`). (2) `completeness.ok=false` with a shortfall naming the tenant, `written`, `expected` and `missing`. This is the failure the hash chain is structurally blind to: a row that was never written leaves no sequence gap, so pre-0464 the trail reported a clean `ok=true` while rows were missing. Crucially the chain walk is still reported intact (`rows_checked>0`, no `reason`) — a LOSS must not be misattributed to TAMPERING. (3) Both exports appear in `audit_log` with `meta.audit_read` naming what left (`audit_log_csv`, `masterdata_export`) and the caller as actor — taking data out of the system is itself an event. (4) **No** audit row for the ordinary read: only egress is recorded, so the trail stays legible. (5) The exports are attributable to the named user with time, IP and request id. |
