# UAT — Cycle 09: Reports & Analytics

**Status: DRAFT v1.0 · 2026-07-05** · *v1.0: docs/35 Phase 1 — CFO KPI scorecard / ELC-07 (UAT-RPT-050).* · *v0.9: LP-3 digest 2.0 (UAT-RPT-049).* · *v0.8: LP-2 copilot LLM governance (UAT-RPT-048).* · *v0.7: LC-5 ask in chat (UAT-RPT-047).* · *v0.6: LC-4 LINE delivery + digest (UAT-RPT-046).* · Cross-ref: process narratives `01-order-to-cash.md`, `04-general-ledger-close.md`, `20-restaurant-operations.md` (management analytics), `26-reporting-bi-ai.md` §8a (demand ML), §5a–5b (scheduled reports / saved views), §3a (role dashboards); harness `tools/cutover/src/e2e.ts`, `worldclass.ts`, `rag.ts`, `demand-ml.ts`, `menu-engineering.ts`, `analytics-staff.ts`, `bom-availability.ts`, `production-plan.ts`, `ext.ts`. Endpoints under `/api/reports`, `/api/analytics`, `/api/menu/availability`, `/api/bi`, `/api/demand`, `/api/finance`, `/api/dashboard`, `/api/saved-views`.

Result legend: Pass / Fail / Blocked / N/A / Not Run. Amounts are exact (seed: today's sales = 107, low-stock items = 2).

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-RPT-001 | Dashboard summary KPIs | ExecutiveViewer/Admin | Seed sale 107, 2 low-stock | 1. `GET /api/dashboard`. | — | 200; `today.sales`=107; `low_stock_count`=2. | High | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-002 | Finance KPI MTD revenue | FinancialController | Seed sale 107 | 1. `GET /api/finance/kpi`. | — | `mtd_revenue`=107. | High | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-003 | Stock summary report | InventoryController | 3 items seeded | 1. `GET /api/inventory/stock?limit=50`. | — | `total`=3; `low_stock_count`=2. | Med | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-004 | Notifications counts | Admin | 2 low-stock items | 1. `GET /api/notifications`. | — | `counts.low_stock`=2. | Med | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-005 | Replenishment analytics | Planner | Items with reorder points | 1. `GET /api/analytics/replenishment`. | — | 200; replenishment suggestions returned. | Med | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-006 | AR aging report | ArClerk | AR invoices exist | 1. `GET /api/finance/ar/aging`. | — | 200; aging buckets reconcile to AR sub-ledger total. | High | Detective | REC-01 | Not Run | finance |
| UAT-RPT-007 | AP aging report | ApClerk | AP transactions exist | 1. `GET /api/finance/ap/aging`. | — | 200; aging buckets reconcile to AP sub-ledger total. | High | Detective | REC-01 | Not Run | finance |
| UAT-RPT-008 | P&L / income statement | FinancialController | Postings exist | 1. `GET /api/finance/pl` (or `/api/ledger/income-statement`). | `<<period>>` | 200; revenue−expense = net income; ties to trial balance. | High | Detective | GL-06 | Not Run | worldclass.ts |
| UAT-RPT-009 | Daily sales report + export | Sales/Admin | Sales exist | 1. `GET /api/reports/daily-sales`. 2. `GET /api/reports/daily-sales/export`. | `<<date range>>` | 200; export downloads (CSV/file) with matching totals. | Med | Positive | RPT | Not Run | reports |
| UAT-RPT-010 | Monthly P&L export | FinancialController | — | 1. `GET /api/reports/monthly-pl/export`. | `<<month>>` | 200; file downloads. | Med | Positive | RPT | Not Run | reports |
| UAT-RPT-011 | Stock summary export | InventoryController | — | 1. `GET /api/reports/stock-summary/export`. | — | 200; file downloads with all items. | Low | Positive | RPT | Not Run | reports |
| UAT-RPT-012 | AP aging export | ApClerk | — | 1. `GET /api/reports/ap-aging/export`. | — | 200; file downloads. | Low | Positive | RPT | Not Run | reports |
| UAT-RPT-013 | Sales-cube / trend (BI) | ExecutiveViewer | Sales data | 1. `GET /api/bi/sales-cube`. 2. `GET /api/dashboard/sales-trend`. | — | 200; aggregated dimensions/series returned. | Med | Positive | RPT | Not Run | analytics |
| UAT-RPT-014 | Anomaly detection (corrected z-score math) | Admin | one item with a genuine daily spike + one steadily-active item | 1. `GET /api/analytics/anomalies`. | — | 200; the spike item is flagged (critical when z>3.5, peak recent DAY vs pre-window per-day baseline); the steadily-active item is **not** flagged (the legacy sum-vs-day unit mismatch is preserved only behind `ANOMALY_PARITY_MODE=legacy`). | Med | Detective | RPT | Not Run | parity analytics |
| UAT-RPT-015 | Reconciliation dashboard | FinancialController | AR/AP activity | 1. `GET /api/finance/reconciliation`. | — | `ar.reconciled: true`; AP balanced. | High | Detective | REC-01 | Not Run | worldclass.ts |
| UAT-RPT-016 | RLS — reports scoped to own tenant | Sales (T2) | T1 + T2 sales | 1. `GET /api/dashboard` as T2 user. | bearer T2 | Only T2 figures; no T1 sales co-mingled. | High | Control | ITGC-AC (RLS) | Not Run | worldclass.ts |
| UAT-RPT-017 | Permission — viewer cannot mutate | ExecutiveViewer | read-only role | 1. Attempt a write (e.g. `POST /api/ledger/journal`). | — | 403 Forbidden (read-only). | Med | Control | ITGC-AC-07 | Not Run | — |
| UAT-RPT-018 | RAG ingest + retrieve a policy (D2) | masterdata/ai_chat | — | 1. `POST /api/ai/kb/documents` (refund policy). 2. `GET /api/ai/kb/search?q=refund within 14 days`. | policy text | Doc chunked; top hit is the policy with score > 0.15. | High | Positive | Feature (RAG) | Not Run | rag.ts |
| UAT-RPT-019 | RAG cite-or-refuse — off-topic refused | ai_chat | Policy ingested | 1. `GET /api/ai/kb/ask?q=<unrelated question>`. | — | `refused: true`, no citations (no hallucinated answer). | High | Control | Feature (RAG safety) | Not Run | rag.ts |
| UAT-RPT-020 | RAG tenant isolation (RLS) | ai_chat (T2) | T1 has KB docs | 1. `GET /api/ai/kb/search`/`ask` as T2. | bearer T2 | 0 results; ask refuses — no T1 leakage. | High | Control | ITGC-AC-03 | Not Run | rag.ts |
| UAT-RPT-021 | Demand ML backtest — models beat naive (D4) | Planner | ≥14d POS history; trend + intermittent items | 1. `POST /api/demand/backtest` (trend item). 2. (intermittent item). | `{item_id:DM-TREND}` / `{item_id:DM-INTER}` | 200; 5 candidates scored with WAPE/MASE; Holt WAPE < SMA on trend; Croston WAPE < SMA on intermittent. | High | Positive | Feature (demand ML) | Not Run | demand-ml.ts |
| UAT-RPT-022 | Demand forecast auto-selects best model + persists (D4) | Planner | trend item history | 1. `POST /api/demand/forecast` horizon 10. 2. `GET /api/demand/accuracy`. | `{item_id:DM-TREND,horizon:10}` | `selected_by`=lowest_wape; forecast length 10, all ≥ 0, trends upward; run persisted; accuracy KPI returns avg WAPE/MASE. | High | Positive | Feature (demand ML) | Not Run | demand-ml.ts |
| UAT-RPT-023 | Demand forecast — input guards | Planner | item with no history | 1. `POST /api/demand/forecast` unknown item. 2. pin `algorithm:wizardry`. | — | 400 `INSUFFICIENT_HISTORY`; 400 `UNKNOWN_ALGORITHM`. | Med | Control | Feature (demand ML guard) | Not Run | demand-ml.ts |
| UAT-RPT-024 | Demand forecasts tenant-isolated (RLS) | Planner (T2) | HQ has a persisted forecast | 1. `GET /api/demand/forecasts` as each tenant. | bearer per tenant | Each tenant sees only its own runs — no cross-tenant forecast leakage. | High | Control | ITGC-AC (RLS) | Not Run | demand-ml.ts |
| UAT-RPT-025 | Customer-360 detail + RLS + permission gate (W6) | crm/dashboard/ar | tenant with sales + open AR | 1. `GET /api/customers/:code`. 2. as a user w/o crm/dashboard/ar. 3. as another tenant's user. | — | (1) stats exclude Voided, AR outstanding correct; (2) 403; (3) RLS → 0 of the other tenant's sales/AR. | High | Control | Feature (CRM 360), ITGC-AC | Not Run | customers.ts |
| UAT-RPT-026 | Analytics HTTP layer through guard stack (W6) | planner/dashboard/warehouse | — | 1. `GET /api/analytics/replenishment` + `/dashboard-summary`. 2. `?days=abc`. 3. as a user w/o the perm. | — | (1) 200; (2) 400 `BAD_QUERY`; (3) 403. | Med | Control | ITGC-AC-02 | Not Run | customers.ts |
| UAT-RPT-027 | Scheduled report — subscription validation | exec/Planner | report-type catalog | 1. `POST /api/bi/subscriptions` type `nope`. 2. frequency `hourly`. 3. valid `{kpi_board, daily}`. | as left | (1) 400 `BAD_REPORT_TYPE`; (2) 400 `BAD_FREQUENCY`; (3) 201 with id. | Med | Control | Feature (scheduled reports) | Not Run | ext.ts |
| UAT-RPT-028 | Scheduled report — sweep runs due + records a run | exec/Planner | an active subscription | 1. `POST /api/bi/subscriptions/run`. 2. `GET /api/bi/runs`. 3. run again. | as left | (1) `ran_count`≥1, run `status:success`; (2) history has the kpi_board run; (3) `ran_count`=0 (schedule advanced). | High | Positive | Feature (scheduled reports) | Not Run | ext.ts |
| UAT-RPT-029 | Scheduled report — run-now + tenant isolation | exec/Planner (T2) | T2 subscription; T1 exec user | 1. `POST /api/bi/subscriptions/:id/run`. 2. `GET /api/bi/runs` as the other tenant. | bearer per tenant | (1) `status:success`; (2) the other tenant sees none of T2's runs (RLS). | High | Control | Feature (scheduled reports), ITGC-AC-03 | Not Run | ext.ts |
| UAT-RPT-030 | Saved views — personal/shared visibility + owner-only delete | any list-screen role | two users in one tenant + another tenant | 1. Create a personal + a shared view. 2. List as owner, as same-tenant peer, as other tenant. 3. Delete as non-owner then owner. | as left | Owner sees both; peer sees only the shared one; other tenant sees 0; non-owner delete → 404 `VIEW_NOT_FOUND`; owner delete → 200. | High | Control | Feature (saved views), ITGC-AC-03 | Not Run | ext.ts |
| UAT-RPT-031 | Role dashboard — catalog + layout validation | users/exec | widget catalog | 1. `GET /api/dashboard/widgets/catalog`. 2. `PUT /api/dashboard/layouts/NotARole`. 3. `PUT .../ExecutiveViewer` widgets `['nope']`. 4. valid 5-widget set. | as left | Catalog lists widgets + roles; (2) 400 `BAD_ROLE`; (3) 400 `BAD_WIDGET`; (4) 200 saved + reads back. | Med | Control | Feature (role dashboards) | Not Run | ext.ts |
| UAT-RPT-032 | Role dashboard — resolution filtered to viewer permissions | ExecutiveViewer | ExecutiveViewer layout set incl AR/AP widgets | 1. `GET /api/dashboard/layout/me` as an ExecutiveViewer. | as left | Returns today_sales/low_stock/open_pipeline with numeric values; **omits** open_ar/outstanding_ap (viewer lacks `ar`/`creditors`/`exec`). | High | Control | Feature (role dashboards), ITGC-AC-02 | Not Run | ext.ts |
| UAT-RPT-033 | Role dashboard — default fallback + tenant isolation | exec/Planner; other tenant | HQ configured a layout | 1. `GET /api/dashboard/layout/me` for an unconfigured role. 2. `GET /api/dashboard/layouts/ExecutiveViewer` as another tenant. | bearer per tenant | (1) `configured:false`, default widgets returned; (2) other tenant sees `configured:false`, 0 widgets (RLS). | High | Control | Feature (role dashboards), ITGC-AC-03 | Not Run | ext.ts |
| UAT-RPT-034 | Menu-engineering matrix (Kasavana–Smith) | exec/Planner | 4 costed items sold (qty 40/40/5/5; margin 160/20/240/20) | 1. `GET /api/analytics/menu-engineering?from&to`. | window covering the sales | 4 items → exactly one **Star / Plowhorse / Puzzle / Dog** each; popularity threshold 0.175 (70% rule); total contribution 8500; each item carries an action. | High | Positive | Feature (menu engineering) | Not Run | menu-engineering.ts |
| UAT-RPT-035 | Daypart / hour demand on the business clock | exec/Planner | 5 captured tenders timed across the day (Asia/Bangkok) | 1. `GET /api/analytics/daypart?from&to`. | window | by_daypart breakfast 100/lunch 200/dinner 800(×2)/late 50; **hour buckets on Bangkok clock** (08:00,12:00,19:00,01:00 — no UTC drift); peak = dinner / 19:00; totals 1150 / 5 txns. | High | Control | Feature (daypart), TZ (Asia/Bangkok) | Not Run | menu-engineering.ts |
| UAT-RPT-036 | Void / discount shrinkage analytics | exec | 2 voids (฿240) + 1 discount (฿50) overrides; 5 sales | 1. `GET /api/analytics/voids-discounts?from&to`. | window | void_count 2, void amount 240, discount 50, sales_count 5, **void_rate 40%**; breakdowns by action / reason / actor (cashier1 = 2 events ฿240). | High | Control | Feature (loss prevention) | Not Run | menu-engineering.ts |
| UAT-RPT-037 | Staff / cashier performance | exec | anna 2 sales ฿500 + 1 void ฿50; ben 1 sale ฿100 | 1. `GET /api/analytics/staff-performance?from&to`. | window | anna: 2 sales, ฿500, avg 250, 1 void ฿50; ben: 1 sale ฿100; summary 2 staff / ฿600 / 3 sales; ranked by revenue. | Med | Positive | Feature (staff analytics) | Not Run | analytics-staff.ts |
| UAT-RPT-038 | Sales trend vs prior window | exec/Planner | today ฿600/3 sales; yesterday ฿400/1 | 1. `GET /api/analytics/sales-trend?from&to`. | 1-day window | current ฿600/3, previous ฿400/1 (auto prior equal window); revenue_delta +฿200 (+50%), txn_delta +2. | Med | Positive | Feature (sales trend) | Not Run | analytics-staff.ts |
| UAT-RPT-039 | BOM availability forecast (servings-remaining) | order_mgt | RICE (ample), PADTHAI (prawn-limited 4), SOLDOUT (egg 0); prawn≤reorder | 1. `GET /api/menu/availability/forecast?low=5`. 2. `?low=1`. | recipes + stock | (1) 3 dishes → 1 out / 1 low / 1 ok; PADTHAI 4 left **limited by prawn**; SOLDOUT 0 (out); low-ingredients = prawn+egg. (2) low=1 reclasses PADTHAI ok. | High | Positive | Feature (BOM availability) | Not Run | bom-availability.ts |
| UAT-RPT-040 | Production plan — demand-ML forecast | planner/order_mgt | STEADY dish: 40 days × qty 10 (constant); THIN dish: only 3 days; beef stock 5 (reorder 4, pack 10) | 1. `GET /api/menu/production-plan?days=1&lookback=28`. | sales + recipes + stock | `forecast_method` mentions **demand-ML**; STEADY forecast **10** via an **auto-selected model** (one of sma/ses/holt/seasonal_naive/croston) with a numeric `forecast_wape`; THIN (thin history) → **day-of-week** fallback model; BOM → beef required 2×10=20, **order 20** (pack-rounded), `unit_price` 50 on the line. | High | Positive | Feature (production plan, demand-ML) | Not Run | production-plan.ts |
| UAT-RPT-041 | One-click draft PO + AI tools | procurement | A buy list from the production plan | 1. `POST /api/procurement/pos` with the buy-list items (omit null uom). 2. AppModule boot. | as left | (1) a real PO created — `po_no` PO-…, status `Pending`, total = Σ qty×unit_price (125×8=1000); (2) the AI agent registers the restaurant tools (`get_production_plan`…) and the app boots (DI/no cycle). | High | Positive | Feature (production plan), AI | Not Run | production-plan.ts |
| UAT-RPT-042 | Residual-gap BI report types (exec / budget / supplier) | exec/Planner | report-type catalog | 1. `GET /api/bi/report-types`. 2. subscribe + run `exec_scorecard`. 3. subscribe + run `budget_variance`. 4. subscribe + run `supplier_scorecard`. | as left | (1) catalog includes `exec_scorecard`+`budget_variance`+`supplier_scorecard`; (2) run `status:success`, summary composes finance/CRM/projects/supply-chain (incl. win rate); (3) `status:success`, summary shows net variance + review count; (4) `status:success`, summary shows avg score + underperformer count. | Med | Positive | RG-1/2/3 (docs/21); ELC-06 (budget) | Not Run | bi.ts |
| UAT-RPT-043 | Streaming analytics — live KPI feed | exec (tenant T1) | a refreshable KPI snapshot; T1 + HQ users | 1. refresh the snapshot for T1. 2. `GET /api/bi/live/recent` as T1. 3. read the live feed as HQ. 4. (UI) open `/dashboard`. | as left | (1) a `kpi_refresh` event is published with the KPI deltas; (2) T1's recent feed contains it (`available:true`); (3) HQ does **not** see T1's event (tenant-filtered); (4) the dashboard shows a **สด (Live)** badge and refreshes its figures on the event. | Med | Positive | docs/22 Phase B (operational; no control) | Not Run | bi.ts |
| UAT-RPT-044 | Weekly-seasonal demand auto-selects the day-of-week model | Planner | item with weekend-heavy sales history (≥28 days) | 1. `POST /api/demand/backtest` for the item. 2. `POST /api/demand/forecast` horizon 7. | — | (1) `dow_seasonal` scores the lowest WAPE among the 7 candidates; (2) the forecast auto-selects it and the 7-day shape peaks on the weekend positions. | Med | Positive | RPT / demand | Not Run | demand-ml.ts |
| UAT-RPT-045 | Thai-holiday demand model applies the learned uplift on future holidays | Planner | item with holiday-spiked history (≥2 fixed holidays in window) | 1. Backtest the item. 2. Forecast a horizon that crosses a fixed Thai holiday (e.g. Songkran). | — | `th_holiday` is among the 8 candidates; when selected, the forecast days landing on fixed holidays carry the learned uplift while ordinary days follow the weekday pattern. | Med | Positive | RPT / demand | Not Run | demand-ml.ts |
| UAT-RPT-046 | LINE report delivery + daily digest + alert user-target (LC-4) | linked staff w/ `dashboard`; Admin | Staff LINE-linked; pending approvals/PRs exist | 1. Chat `subscribe digest` as a user WITHOUT dashboard/fin_report/exec, then as one WITH. 2. `POST /api/bi/subscriptions/run`. 3. Chat `unsubscribe digest`. 4. Alert rule `channel:line, target_to:'user:<name>'` → `POST /api/alerts/run`. | as left | (1) refused, then ✔ — a `line_daily_digest` subscription carries `{line_user}`; permission-at-subscribe enforced. (2) digest pushed to the linked LINE (รออนุมัติ · PR ค้าง · แจ้งเตือน 24 ชม.) — summary only, never a dataset. (3) recipient removed. (4) the fired alert lands on the **linked** LINE (registry-resolved at send time; force-unlink silences it). | High | Control | Feature (LINE delivery) + permission gate | Pass | line-crm.ts |
| UAT-RPT-047 | LINE `ask` — governed NL analytics in chat (LC-5) | linked staff w/ `dashboard`; linked staff without | — | 1. Chat `ask ยอดขายตามสาขา` as a user WITHOUT dashboard/exec/masterdata. 2. Same as a user WITH. | as left | (1) refused — same gate as `POST /api/nl/ask`. (2) answer via the whitelist-dimension, RLS-scoped query engine (top-5 summary + `/query` pointer; honest ไม่มีข้อมูล when empty; NL never produces raw SQL). | High | Control | Feature (NL analytics) + permission gate | Pass | line-crm.ts |
| UAT-RPT-048 | LINE copilot LLM governance: schema validation, daily cap, DPA gate (LP-2) | linked staff; ops (env) | LLM key configured (or scripted client in CI) | 1. Free Thai the rules can't parse → LLM drafts → confirm. 2. Force a malformed LLM answer. 3. Set `LINE_COPILOT_DAILY_CAP=1` and exceed it. 4. `NODE_ENV=production` + key + no `AI_DPA_ACKNOWLEDGED`, send an LLM-needed text. | as left | (1) schema-valid draft → confirm → document via the ordinary path (model routed via `chat_copilot`). (2) honest "ยังไม่เข้าใจ" — nothing parked or created. (3) model NOT called past the cap; `[chat:ai-cap]` audit row; deterministic rules still answer. (4) model never called (DPA fail-closed); deterministic refusal. | High | Control | ITGC-SEC-AI / PN-26 rev 1.8 | Pass | line-crm.ts |
| UAT-RPT-049 | Digest 2.0: KPI selection + per-recipient permission filter at send time (LP-3) | two linked subscribers (Admin; dashboard-only) | AR/GL/stock data seeded | 1. `digest kpis` as a no-perm user and as Admin. 2. `subscribe digest nonsense_kpi`; `subscribe digest cash_position` as dashboard-only. 3. Admin `subscribe digest sales_yesterday,cash_position,ar_overdue,low_stock`; dashboard-only bare `subscribe digest`. 4. Run the digest once. | as left | (1) refused / permission-aware menu (defaults marked). (2) `ไม่รู้จัก KPI` / `ไม่มีสิทธิ์เห็น`. (3) selection stored on the recipient (jsonb). (4) ONE run, DIFFERENT payloads: Admin gets a flex card with the financial KPIs (formatted THB); the dashboard-only subscriber gets the trio and **never** sees cash/AR; empty data renders `—`. | High | Control | Permission-at-send (PN-26 rev 1.9) | Pass | line-crm.ts |
| UAT-RPT-050 | CFO KPI scorecard: values reconcile, RAG thresholds, drill-through, permission gate (docs/35 Phase 1) | exec / fin_report (positive); warehouse-only (negative) | a known ledger seeded (cash 100k, AR 50k, inventory 30k, AP 40k, lease 60k; rev 100k, COGS 40k, opex 20k, dep 10k, interest 5k); one 90+ overdue AR invoice; an approved monthly budget | 1. `GET /api/finance/metrics/pack`. 2. Inspect `current_ratio`, `gross_margin_pct`, `overdue_ar_pct` values + `rag`. 3. `GET /api/finance/metrics/current_ratio/drill`. 4. `GET /api/finance/metrics/gross_margin_pct/trend?periods=3`. 5. `GET /api/finance/metrics/nope/drill`. 6. Call the pack as a warehouse-only user. | as left | (1) ~31 KPIs, each with `prior_period`/`prior_year`/`budget`/`rag`/`drill`. (2) `current_ratio=4.5` (🟢 green), `gross_margin_pct=60`, `overdue_ar_pct=40` (🔴 red past threshold); `pack.budget.revenue.budget=90000`. (3) balance-sheet rows incl. cash 1000 = 100000 (KPI ties to the ledger). (4) 3-point monthly series, current month = 60. (5) **400 UNKNOWN_METRIC**. (6) **403** — the scorecard is permission-gated. | High | Control | ELC-07 (PN-26 rev 2.0) | Pass | finance-kpi.ts |
| UAT-UI-DEM-01 | Demand forecasting screen | Planner | item with ≥14 days history | 1. Sidebar → **วางแผน & BI → พยากรณ์ความต้องการ (Demand ML)** (`/demand`). 2. **พยากรณ์** tab → item + horizon 14 → พยากรณ์. 3. **เทียบโมเดล** tab → backtest. 4. **ประวัติ & ความแม่น** tab. | item with <14d for the negative path | Forecast shows the auto-selected model, WAPE and a per-day table; backtest lists all candidate models with the best flagged; history + accuracy populate. An item with <14 days surfaces `INSUFFICIENT_HISTORY` as an error toast. | Med | UI | Feature (demand ML) | Not Run | manual / e2e |
| UAT-UI-BUD-01 | Budget vs Actual screen | exec / planner | posted GL activity in the year | 1. Sidebar → **วางแผน & BI → งบประมาณเทียบจริง (Budget vs Actual)** (`/budget`). 2. **ตั้งงบประมาณ** tab → account + annual amount → save. 3. **งบเทียบจริง** tab → choose year. | account 5100, annual 120000 | Budget saved (toast: lines=12, total); BvA table shows budget/actual/variance/% with a **Favorable/Unfavorable** flag and revenue/expense/net roll-ups; actuals are sourced only from **Posted** journal lines. | Med | UI | Feature (budget vs actual) | Not Run | manual / e2e |
| UAT-BUD-02 | Budget maker-checker — pending excluded until approved | Planner then a different approver (`approvals`/`gl_close`) | — | 1. `POST /api/ledger/budgets`. 2. `GET …/budget-vs-actual`. 3. self-approve `POST …/budgets/approve`. 4. approve as a different user. 5. re-read budget-vs-actual. | account 5105, 2030-01, 300 | (1) `status:'PendingApproval'`; (2) the budget is EXCLUDED from variance; (3) `403 SOD_VIOLATION` (binds Admin); (4) `status:'Approved'`; (5) the budget now appears in budget-vs-actual. | High | Control | BUD-01 | Pass | budget.ts |
| UAT-BUD-03 | Material budget variance flagged for review | Controller / FP&A | budget 5000 + actual 7000 for a period | 1. `GET /api/ledger/budget-vs-actual?fiscal_year=&period=`. | budget 5000, actual 7000 | The 5100 line shows `material:true`, `requires_review:true`; `review.material_count`≥1, `review.requires_review_count`≥1, `review.last_signoff:null`; threshold ≥10% & ≥THB 1,000 reported. | High | Detective | ELC-06 | Not Run | budget.ts |
| UAT-BUD-04 | Management variance-review sign-off recorded | Controller (`exec`/`approvals`/`gl_close`) | material variance present | 1. `POST /api/ledger/budget-review/sign-off` with no note. 2. With a note. 3. Re-read budget-vs-actual. 4. `GET /api/ledger/budget-reviews`. | `{fiscal_year, period, notes}` | (1) **400** (note required); (2) **200**, records `material_count`, `unfavorable_total`=2000, `reviewed_by`; (3) report `review.last_signoff` shows the reviewer + material count; (4) review history lists the sign-off. | High | Control | ELC-06 | Not Run | budget.ts |
| UAT-UI-BUD-02 | Variance-review banner + sign-off | Controller | on `/budget` → งบเทียบจริง | 1. Open the BvA tab for a period with a material variance. 2. Click **บันทึกการสอบทาน**, enter a follow-up note. | — | Banner shows the material count + **ต้องสอบทาน** count + ⚠ on material rows; after sign-off the banner shows **สอบทานล่าสุดโดย …** with the note. | Med | UI | ELC-06 | Not Run | manual / e2e |
| UAT-UI-INS-01 | Insights screen (anomalies · replenishment · AI) | exec / planner / warehouse | sales + movement + stocktake history | 1. Sidebar → **วางแผน & BI → ข้อมูลเชิงลึก (Insights)** (`/insights`). 2. **ภาพรวม** tab. 3. **ความผิดปกติ** tab → toggle 7/30/90 → press **คำแนะนำ (AI)** on a movement row. 4. **เติมสต๊อก** tab → click an item row. | — | Overview shows reorder/anomaly counts + an AI summary + top-3 reorder items; Anomalies lists movement (Z-score) + stocktake-variance rows and the AI button returns a per-anomaly narrative (`POST /api/analytics/insight`); Replenishment lists predicted stock-outs and the row drill shows the per-item AI recommendation. All advisory (no GL, no auto-order). | Med | UI | Feature (insights UI), BI-04 | Not Run | manual / e2e |
