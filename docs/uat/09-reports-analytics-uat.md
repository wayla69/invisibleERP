# UAT — Cycle 09: Reports & Analytics

**Status: DRAFT v3.2 · 2026-07-23** · *v3.2: docs/61 Phase 2 — Segment × Channel ROI (NEW preventive control MKT-25; no migration) — extends the MKT-17 Budget Optimizer from channel to **segment × channel**: `GET /api/marketing-activation/segment-channel-roi?budget=` ranks cells by incremental ROI (channel ROI × measured-lift multiplier) × value (reach × CLV) and splits the budget toward the best channels, `POST /segment-channel-roi/stage` stages the split as a **Pending mi_budget_plans** row reusing the MKT-17 **maker-checker** (approver ≠ requester → `SOD_SELF_APPROVAL`); combines pushed MMM ROI + a customer_profiles segment aggregation + MKT-19 measured lift (no cross-domain join); advisory (read never posts spend); tenant-scoped (403); pure cell scorer unit-tested (`test/segment-channel-scoring.test.ts`, 9); case UAT-RPT-066; ToE `ext.ts` +6 (373).* · *v3.1: docs/61 Phase 1 — Propensity & Cross-Sell (NEW preventive control MKT-23; no migration) — the first Fact-Layer tool: `GET /api/marketing-activation/propensity/customer/:code` ranks the **next product to offer** (excluding what they already buy, scored confidence × lift × margin) and `GET /propensity/item/:itemId` ranks the **best audiences** (reach × CLV), combining menu-affinity lift + Fact-Layer favourites/CLV + food-cost margin (no cross-domain join); **advisory only** (read-only, contact stays the consent-gated draft); tenant-scoped (404) + permission-gated (403); pure deterministic scorer unit-tested (`test/propensity-scoring.test.ts`, 11); case UAT-RPT-065; ToE `ext.ts` +6 (367).* · *v3.0: docs/61 Phase 0 — Marketing Fact Layer (no new control; read aggregator reusing tenant isolation) — `modules/marketing-activation` exposes read-only `GET /api/marketing-activation/facts/customer/:code` + `.../facts/segment/:segment` (`marketing`/`exec`) composing `customer_profiles` (`mi_*` CLV/churn/NBA) + `pos_members` (identity, tier, `marketing_opt_in`) in SEPARATE queries (no cross-domain join) + the pushed MMM channel-ROI; tenant-scoped (cross-tenant read → 404, non-marketing → 403); the shared foundation the docs/61 five tools read from; case UAT-RPT-064; ToE `ext.ts` +5 (361).* · *v2.9: docs/60 Phase 4 — Model Governance (new preventive control MKT-20, migration `0469`) — opt-in per-tenant **maker-checker** on the pushed analytics that drive spend/contact (a run lands Pending, approver ≠ pusher → `SOD_SELF_APPROVAL`; consumers gated Approved-only / `ANALYTICS_NOT_APPROVED`), **model cards**, **drift** flagged into GOV-01 + blocked without a reason (`DRIFT_REASON_REQUIRED`), and a run → plan → experiment **audit trail**; case UAT-RPT-063; ToE `ext.ts` +11 (356).* · *(prior)* · *v2.8: docs/60 Phase 3 — Closed-loop Measurement (new preventive control MKT-19, migration `0468`) — an activated `mi_segment` splits ONCE into a treatment arm + a randomised **immutable holdout control** (never contacted; treatment-only `audience:'members'` campaign), and after the window `POST /experiments/measure` computes **lift** = treatment-vs-control per-head on **real POS revenue** via `CrmService.revenueByMembers` (no cross-domain join); measured outcomes pull-back via `GET /api/v1/marketing/experiment-outcomes` (`analytics:read`); case UAT-RPT-062; ToE `ext.ts` +9 (345).* · *(prior)* · *v2.7: docs/60 Phase 2 — Customer Intelligence (new preventive control MKT-18, migration `0467`) — per-customer **CLV / churn probability / next-best-action** pushed onto `customer_profiles.mi_clv`/`mi_churn_risk`/`mi_nba` (SEPARATE from the ERP's own churn/LTV; a segment-only push leaves them intact), a **read-only** drill-down `GET /api/marketing-intel/segment/:segment/customers?sort=clv|churn`, advisory (sole contact path = the consent-gated campaign draft); case UAT-RPT-061; ToE `ext.ts` +6 (336).* · *(prior)* · *v2.6: docs/60 Phase 1 — Budget Optimizer (new preventive control MKT-17, migration `0466`) — prescriptive MMM allocation on `/marketing-intel` (`GET response-curves` + `POST simulate`/`optimize`, a pure/deterministic Hill-saturation optimiser) + a **staged advisory** `mi_budget_plans` (never posts spend) under **maker-checker** (`POST budget-plan` → `POST budget-plan/approve`, approver ≠ requester → `SOD_SELF_APPROVAL`); case UAT-RPT-060; ToE `ext.ts` +9 (330).* · *(prior)* · *v2.5: docs/47 next-level — review-response SLA governance (new detective control MKT-16, migration `0416`) — per-tenant policy `reputation_response_settings` (`GET`/`PUT /api/reputation/response-settings`) + the breach worklist `GET /api/reputation/response-sla` (≤threshold unreplied reviews split breached/open by age) + a schedulable `reputation_response_sla` digest + a Response-SLA tab on `/reputation`; case UAT-RPT-059; ToE `reputation.ts` 26/26.* · *(prior)* · *v2.4: docs/48 MMM phase 2 (MKT-15 Partial→Implemented) — delivered the `/mmm` web workspace (Signals/Model/Recommendation tabs, Planning & BI nav) and the `cutover/mmm.ts` end-to-end ToE harness (16 checks, CI `analytics` shard); UAT-RPT-058 updated to reference the harness re-performance. No backend change.* · *(prior)* · *v2.3: docs/48 — Marketing Mix Modeling / MMM (new control MKT-15, Partial) — staging→core→analytics pipeline (`POST /api/mmm/ingest/*` → `POST /api/mmm/run` → audited `mmm_model_runs`/`mmm_channel_results`), pure/deterministic `computeMmm` attribution, BI report types `mmm_run`/`mmm_summary` + live read `GET /api/bi/mmm-summary`; case UAT-RPT-058.* · *(prior)* · *v2.2: docs/47 — reputation & external analytics ingestion (new control MKT-14) — Google Maps reviews (Business Profile OAuth2 + `reviews.list`/`updateReply`) and Google Analytics (GA4 Data API `runReport`) polled on a schedule (`reputation_review_sync`/`reputation_ga4_sync`; no webhook exists for either platform), dedicated screen `/reputation`, live dashboard read `GET /api/bi/reputation-summary`; case UAT-UI-REP-01.* · *(prior)* · *v2.1: docs/45 UI gap closure (no new control) — marketing ROI dashboard tab (`GET /api/bi/marketing-roi`), audience-export screen (`/crm/audience-export`), channel-link staff/member screens (`/channels` ผูกสมาชิก tab + `/m?clink=`, fixing a dead `/portal/loyalty` URL), and the stale demand-model dropdown; cases UAT-UI-ROI-01, UAT-UI-AUD-01, UAT-UI-CHREF-01, UAT-UI-DEM-01 updated.* · *(prior)* · *v2.0: weather overlay for demand forecasting (docs/45 residual; no new control) — opt-in `weather` candidate in `demand-ml` (Open-Meteo, `DEMAND_WEATHER_ENABLED`), byte-identical to `dow_seasonal` when off; case UAT-RPT-057; ToE `demand-ml.ts` 24/24.* · *(prior)* · *v1.9: G2 menu affinity (docs/45; no new control) — `GET /api/analytics/menu-affinity` support/confidence/lift + daypart slices + BI type `menu_affinity`; cases UAT-RPT-055..056; ToE `menu-engineering.ts` 13/13.* · *(prior)* · *v1.8: FIN-3 budgetary control — `/budget` gains the **ควบคุมงบ** tab (policy off/advise/warn/block on `budget_control_settings`, availability lookup, commitment audit list; new control **BUD-02**, migration `0296`; approval-gate cases live in UAT-P2P-131..128). New case UAT-BUD-05.* · *v1.7: POS-5b menu-engineering depth — branch_id filter + qty-weighted average-CM threshold (UAT-RPT-034 updated, UAT-RPT-053).* · *v1.6: per-tenant AI opt-out / PDPA (UAT-RPT-052).* · *v1.5: docs/35 Phase 6 — MD&A narrative + scheduled KPI packs (UAT-RPT-051).* · *v1.4: docs/35 Phase 5 — Segment Profitability (UAT-UI-PROF-01).* · *v1.3: docs/35 Phase 4 — Treasury / Cash Command / TR-01 (UAT-UI-TR-01).* · *v1.2: docs/35 Phase 3 — Close Cockpit / GL-22 (UAT-UI-CC-01).* · *v1.1: docs/35 Phase 2 — CFO Command Center dashboard (UAT-UI-CFO-01).* · *v1.0: docs/35 Phase 1 — CFO KPI scorecard / ELC-07 (UAT-RPT-050).* · *v0.9: LP-3 digest 2.0 (UAT-RPT-049).* · *v0.8: LP-2 copilot LLM governance (UAT-RPT-048).* · *v0.7: LC-5 ask in chat (UAT-RPT-047).* · *v0.6: LC-4 LINE delivery + digest (UAT-RPT-046).* · Cross-ref: process narratives `01-order-to-cash.md`, `04-general-ledger-close.md`, `20-restaurant-operations.md` (management analytics), `26-reporting-bi-ai.md` §8a (demand ML), §5a–5b (scheduled reports / saved views), §3a (role dashboards); harness `tools/cutover/src/e2e.ts`, `worldclass.ts`, `rag.ts`, `demand-ml.ts`, `menu-engineering.ts`, `analytics-staff.ts`, `bom-availability.ts`, `production-plan.ts`, `ext.ts`. Endpoints under `/api/reports`, `/api/analytics`, `/api/menu/availability`, `/api/bi`, `/api/demand`, `/api/finance`, `/api/dashboard`, `/api/saved-views`.

Result legend: Pass / Fail / Blocked / N/A / Not Run. Amounts are exact (seed: today's sales = 107, low-stock items = 2).

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-RPT-001 | Dashboard summary KPIs | ExecutiveViewer/Admin | Seed sale 107, 2 low-stock | 1. `GET /api/dashboard`. | — | 200; `today.sales`=107; `low_stock_count`=2. | High | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-002 | Finance KPI MTD revenue | FinancialController | Seed sale 107 | 1. `GET /api/finance/kpi`. | — | `mtd_revenue`=107. | High | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-003 | Stock summary report | InventoryController | 3 items seeded | 1. `GET /api/inventory/stock?limit=50`. | — | `total`=3; `low_stock_count`=2. | Med | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-004 | Notifications counts | Admin | 2 low-stock items | 1. `GET /api/notifications`. | — | `counts.low_stock`=2. | Med | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-005 | Replenishment analytics | Planner | Items with reorder points | 1. `GET /api/analytics/replenishment`. | — | 200; replenishment suggestions returned. | Med | Positive | RPT | Not Run | e2e.ts |
| UAT-RPT-006 | AR aging report | ArClerk | AR invoices exist | 1. `GET /api/finance/ar/aging`. | — | 200; aging buckets reconcile to AR sub-ledger total. | High | Detective | REC-01 | Not Run | finance |
| UAT-RPT-007 | AP aging report | ApClerk | AP transactions exist | 1. `GET /api/finance/ap/aging`. | — | 200; aging buckets reconcile to AP sub-ledger total. | High | Detective | REC-01 | Not Run | finance |
| UAT-RPT-008 | P&L / income statement | FinancialController | Postings exist | 1. `GET /api/finance/pl` (or `/api/ledger/income-statement`). | `<<period>>` | 200; revenue−expense = net income; ties to trial balance. | High | Detective | GL-06 | Not Run | worldclass.ts |
| UAT-RPT-009 | Daily sales report + export | Sales/Admin | Sales exist | 1. `GET /api/reports/daily-sales`. 2. `GET /api/reports/daily-sales/export`. | `<<date range>>` | 200; export downloads (CSV/file) with matching totals. | Med | Positive | RPT | Not Run | reports |
| UAT-RPT-010 | Monthly P&L export | FinancialController | — | 1. `GET /api/reports/monthly-pl/export`. | `<<month>>` | 200; file downloads. | Med | Positive | RPT | Not Run | reports |
| UAT-RPT-011 | Stock summary export | InventoryController | — | 1. `GET /api/reports/stock-summary/export`. | — | 200; file downloads with all items. | Low | Positive | RPT | Not Run | reports |
| UAT-RPT-012 | AP aging export | ApClerk | — | 1. `GET /api/reports/ap-aging/export`. | — | 200; file downloads. | Low | Positive | RPT | Not Run | reports |
| UAT-RPT-013 | Sales-cube / trend (BI) | ExecutiveViewer | Sales data | 1. `GET /api/bi/sales-cube`. 2. `GET /api/dashboard/sales-trend`. | — | 200; aggregated dimensions/series returned. | Med | Positive | RPT | Not Run | analytics |
| UAT-RPT-014 | Anomaly detection (corrected z-score math) | Admin | one item with a genuine daily spike + one steadily-active item | 1. `GET /api/analytics/anomalies`. | — | 200; the spike item is flagged (critical when z>3.5, peak recent DAY vs pre-window per-day baseline); the steadily-active item is **not** flagged (the legacy sum-vs-day unit mismatch is preserved only behind `ANOMALY_PARITY_MODE=legacy`). | Med | Detective | RPT | Not Run | parity analytics |
| UAT-RPT-015 | Reconciliation dashboard | FinancialController | AR/AP activity | 1. `GET /api/finance/reconciliation`. | — | `ar.reconciled: true`; AP balanced. | High | Detective | REC-01 | Not Run | worldclass.ts |
| UAT-RPT-016 | RLS — reports scoped to own tenant | Sales (T2) | T1 + T2 sales | 1. `GET /api/dashboard` as T2 user. | bearer T2 | Only T2 figures; no T1 sales co-mingled. | High | Control | ITGC-AC (RLS) | Not Run | worldclass.ts |
| UAT-RPT-017 | Permission — viewer cannot mutate | ExecutiveViewer | read-only role | 1. Attempt a write (e.g. `POST /api/ledger/journal`). | — | 403 Forbidden (read-only). | Med | Control | ITGC-AC-07 | Not Run | — |
| UAT-RPT-018 | RAG ingest + retrieve a policy (D2) | masterdata/ai_chat | — | 1. `POST /api/ai/kb/documents` (refund policy). 2. `GET /api/ai/kb/search?q=refund within 14 days`. | policy text | Doc chunked; top hit is the policy with score > 0.15. | High | Positive | Feature (RAG) | Not Run | rag.ts |
| UAT-RPT-019 | RAG cite-or-refuse — off-topic refused | ai_chat | Policy ingested | 1. `GET /api/ai/kb/ask?q=<unrelated question>`. | — | `refused: true`, no citations (no hallucinated answer). | High | Control | Feature (RAG safety) | Not Run | rag.ts |
| UAT-RPT-020 | RAG tenant isolation (RLS) | ai_chat (T2) | T1 has KB docs | 1. `GET /api/ai/kb/search`/`ask` as T2. | bearer T2 | 0 results; ask refuses — no T1 leakage. | High | Control | ITGC-AC-03 | Not Run | rag.ts |
| UAT-RPT-021 | Demand ML backtest — models beat naive (D4) | Planner | ≥14d POS history; trend + intermittent items | 1. `POST /api/demand/backtest` (trend item). 2. (intermittent item). | `{item_id:DM-TREND}` / `{item_id:DM-INTER}` | 200; 5 candidates scored with WAPE/MASE; Holt WAPE < SMA on trend; Croston WAPE < SMA on intermittent. | High | Positive | Feature (demand ML) | Not Run | demand-ml.ts |
| UAT-RPT-022 | Demand forecast auto-selects best model + persists (D4) | Planner | trend item history | 1. `POST /api/demand/forecast` horizon 10. 2. `GET /api/demand/accuracy`. | `{item_id:DM-TREND,horizon:10}` | `selected_by`=lowest_wape; forecast length 10, all ≥ 0, trends upward; run persisted; accuracy KPI returns avg WAPE/MASE. | High | Positive | Feature (demand ML) | Not Run | demand-ml.ts |
| UAT-RPT-023 | Demand forecast — input guards | Planner | item with no history | 1. `POST /api/demand/forecast` unknown item. 2. pin `algorithm:wizardry`. | — | 400 `INSUFFICIENT_HISTORY`; 400 `UNKNOWN_ALGORITHM`. | Med | Control | Feature (demand ML guard) | Not Run | demand-ml.ts |
| UAT-RPT-024 | Demand forecasts tenant-isolated (RLS) | Planner (T2) | HQ has a persisted forecast | 1. `GET /api/demand/forecasts` as each tenant. | bearer per tenant | Each tenant sees only its own runs — no cross-tenant forecast leakage. | High | Control | ITGC-AC (RLS) | Not Run | demand-ml.ts |
| UAT-RPT-025 | Customer-360 detail + RLS + permission gate (W6) | crm/dashboard/ar | tenant with sales + open AR | 1. `GET /api/customers/:code`. 2. as a user w/o crm/dashboard/ar. 3. as another tenant's user. | — | (1) stats exclude Voided, AR outstanding correct; (2) 403; (3) RLS → 0 of the other tenant's sales/AR. | High | Control | Feature (CRM 360), ITGC-AC | Not Run | customers.ts |
| UAT-RPT-026 | Analytics HTTP layer through guard stack (W6) | planner/dashboard/warehouse | — | 1. `GET /api/analytics/replenishment` + `/dashboard-summary`. 2. `?days=abc`. 3. as a user w/o the perm. | — | (1) 200; (2) 400 `BAD_QUERY`; (3) 403. | Med | Control | ITGC-AC-02 | Not Run | customers.ts |
| UAT-RPT-027 | Scheduled report — subscription validation | exec/Planner | report-type catalog | 1. `POST /api/bi/subscriptions` type `nope`. 2. frequency `hourly`. 3. valid `{kpi_board, daily}`. | as left | (1) 400 `BAD_REPORT_TYPE`; (2) 400 `BAD_FREQUENCY`; (3) 201 with id. | Med | Control | Feature (scheduled reports) | Not Run | ext.ts |
| UAT-RPT-028 | Scheduled report — sweep runs due + records a run | exec/Planner | an active subscription | 1. `POST /api/bi/subscriptions/run`. 2. `GET /api/bi/runs`. 3. run again. | as left | (1) `ran_count`≥1, run `status:success`; (2) history has the kpi_board run; (3) `ran_count`=0 (schedule advanced). | High | Positive | Feature (scheduled reports) | Not Run | ext.ts |
| UAT-RPT-029 | Scheduled report — run-now + tenant isolation | exec/Planner (T2) | T2 subscription; T1 exec user | 1. `POST /api/bi/subscriptions/:id/run`. 2. `GET /api/bi/runs` as the other tenant. | bearer per tenant | (1) `status:success`; (2) the other tenant sees none of T2's runs (RLS). | High | Control | Feature (scheduled reports), ITGC-AC-03 | Not Run | ext.ts |
| UAT-RPT-030 | Saved views — personal/shared visibility + owner-only delete | any list-screen role | two users in one tenant + another tenant | 1. Create a personal + a shared view. 2. List as owner, as same-tenant peer, as other tenant. 3. Delete as non-owner then owner. | as left | Owner sees both; peer sees only the shared one; other tenant sees 0; non-owner delete → 404 `VIEW_NOT_FOUND`; owner delete → 200. | High | Control | Feature (saved views), ITGC-AC-03 | Not Run | ext.ts |
| UAT-RPT-031 | Role dashboard — catalog + layout validation | users/exec | widget catalog | 1. `GET /api/dashboard/widgets/catalog`. 2. `PUT /api/dashboard/layouts/NotARole`. 3. `PUT .../ExecutiveViewer` widgets `['nope']`. 4. valid 5-widget set. | as left | Catalog lists widgets + roles; (2) 400 `BAD_ROLE`; (3) 400 `BAD_WIDGET`; (4) 200 saved + reads back. | Med | Control | Feature (role dashboards) | Not Run | ext.ts |
| UAT-RPT-032 | Role dashboard — resolution filtered to viewer permissions | ExecutiveViewer | ExecutiveViewer layout set incl AR/AP widgets | 1. `GET /api/dashboard/layout/me` as an ExecutiveViewer. | as left | Returns today_sales/low_stock/open_pipeline with numeric values; **omits** open_ar/outstanding_ap (viewer lacks `ar`/`creditors`/`exec`). | High | Control | Feature (role dashboards), ITGC-AC-02 | Not Run | ext.ts |
| UAT-RPT-033 | Role dashboard — default fallback + tenant isolation | exec/Planner; other tenant | HQ configured a layout | 1. `GET /api/dashboard/layout/me` for an unconfigured role. 2. `GET /api/dashboard/layouts/ExecutiveViewer` as another tenant. | bearer per tenant | (1) `configured:false`, default widgets returned; (2) other tenant sees `configured:false`, 0 widgets (RLS). | High | Control | Feature (role dashboards), ITGC-AC-03 | Not Run | ext.ts |
| UAT-RPT-034 | Menu-engineering matrix (Kasavana–Smith) | exec/Planner | 4 costed items sold (qty 40/40/5/5; margin 160/20/240/20) + 1 uncosted item (no recipe/cost) sold | 1. `GET /api/analytics/menu-engineering?from&to`. | window covering the sales | 4 items → exactly one **Star / Plowhorse / Puzzle / Dog** each; popularity threshold 0.175 (70% rule: 0.7 × 1/4); profitability bar = **qty-weighted average CM 94.44** (8500/90 — not the unweighted mean 110); total contribution 8500; thresholds echoed in the response; each item carries an action; the uncosted item is listed under `uncosted_items` and **not** classified. | High | Positive | Feature (menu engineering) | Not Run | restaurant.ts, menu-engineering.ts |
| UAT-RPT-035 | Daypart / hour demand on the business clock | exec/Planner | 5 captured tenders timed across the day (Asia/Bangkok) | 1. `GET /api/analytics/daypart?from&to`. | window | by_daypart breakfast 100/lunch 200/dinner 800(×2)/late 50; **hour buckets on Bangkok clock** (08:00,12:00,19:00,01:00 — no UTC drift); peak = dinner / 19:00; totals 1150 / 5 txns. | High | Control | Feature (daypart), TZ (Asia/Bangkok) | Not Run | menu-engineering.ts |
| UAT-RPT-036 | Void / discount shrinkage analytics | exec | 2 voids (฿240) + 1 discount (฿50) overrides; 5 sales | 1. `GET /api/analytics/voids-discounts?from&to`. | window | void_count 2, void amount 240, discount 50, sales_count 5, **void_rate 40%**; breakdowns by action / reason / actor (cashier1 = 2 events ฿240). | High | Control | Feature (loss prevention) | Not Run | menu-engineering.ts |
| UAT-RPT-037 | Staff / cashier performance | exec | anna 2 sales ฿500 + 1 void ฿50; ben 1 sale ฿100 | 1. `GET /api/analytics/staff-performance?from&to`. | window | anna: 2 sales, ฿500, avg 250, 1 void ฿50; ben: 1 sale ฿100; summary 2 staff / ฿600 / 3 sales; ranked by revenue. | Med | Positive | Feature (staff analytics) | Not Run | analytics-staff.ts |
| UAT-RPT-038 | Sales trend vs prior window | exec/Planner | today ฿600/3 sales; yesterday ฿400/1 | 1. `GET /api/analytics/sales-trend?from&to`. | 1-day window | current ฿600/3, previous ฿400/1 (auto prior equal window); revenue_delta +฿200 (+50%), txn_delta +2. | Med | Positive | Feature (sales trend) | Not Run | analytics-staff.ts |
| UAT-RPT-039 | BOM availability forecast (servings-remaining) | order_mgt | RICE (ample), PADTHAI (prawn-limited 4), SOLDOUT (egg 0); prawn≤reorder | 1. `GET /api/menu/availability/forecast?low=5`. 2. `?low=1`. | recipes + stock | (1) 3 dishes → 1 out / 1 low / 1 ok; PADTHAI 4 left **limited by prawn**; SOLDOUT 0 (out); low-ingredients = prawn+egg. (2) low=1 reclasses PADTHAI ok. | High | Positive | Feature (BOM availability) | Not Run | bom-availability.ts |
| UAT-RPT-040 | Production plan — demand-ML forecast | planner/order_mgt | STEADY dish: 40 days × qty 10 (constant); THIN dish: only 3 days; beef stock 5 (reorder 4, pack 10) | 1. `GET /api/menu/production-plan?days=1&lookback=28`. | sales + recipes + stock | `forecast_method` mentions **demand-ML**; STEADY forecast **10** via an **auto-selected model** (one of sma/ses/holt/seasonal_naive/croston) with a numeric `forecast_wape`; THIN (thin history) → **day-of-week** fallback model; BOM → beef required 2×10=20, **order 20** (pack-rounded), `unit_price` 50 on the line. | High | Positive | Feature (production plan, demand-ML) | Not Run | production-plan.ts |
| UAT-RPT-041 | One-click draft PO + AI tools | procurement | A buy list from the production plan | 1. `POST /api/procurement/pos` with the buy-list items (omit null uom). 2. AppModule boot. | as left | (1) a real PO created — `po_no` PO-…, status `Pending`, total = Σ qty×unit_price (125×8=1000); (2) the AI agent registers the restaurant tools (`get_production_plan`…) and the app boots (DI/no cycle). | High | Positive | Feature (production plan), AI | Not Run | production-plan.ts |
| UAT-RPT-042 | Residual-gap BI report types (exec / budget / supplier) | exec/Planner | report-type catalog | 1. `GET /api/bi/report-types`. 2. subscribe + run `exec_scorecard`. 3. subscribe + run `budget_variance`. 4. subscribe + run `supplier_scorecard`. | as left | (1) catalog includes `exec_scorecard`+`budget_variance`+`supplier_scorecard`; (2) run `status:success`, summary composes finance/CRM/projects/supply-chain (incl. win rate); (3) `status:success`, summary shows net variance + review count; (4) `status:success`, summary shows avg score + underperformer count. | Med | Positive | RG-1/2/3 (docs/21); ELC-06 (budget) | Not Run | bi.ts |
| UAT-RPT-043 | Streaming analytics — live KPI feed | exec (tenant T1) | a refreshable KPI snapshot; T1 + HQ users | 1. refresh the snapshot for T1. 2. `GET /api/bi/live/recent` as T1. 3. read the live feed as HQ. 4. (UI) open `/dashboard`. | as left | (1) a `kpi_refresh` event is published with the KPI deltas; (2) T1's recent feed contains it (`available:true`); (3) HQ does **not** see T1's event (tenant-filtered); (4) the dashboard shows a **สด (Live)** badge and refreshes its figures on the event. | Med | Positive | docs/22 Phase B (operational; no control) | Not Run | bi.ts |
| UAT-RPT-044 | Weekly-seasonal demand auto-selects the day-of-week model | Planner | item with weekend-heavy sales history (≥28 days) | 1. `POST /api/demand/backtest` for the item. 2. `POST /api/demand/forecast` horizon 7. | — | (1) `dow_seasonal` scores the lowest WAPE among the 7 candidates; (2) the forecast auto-selects it and the 7-day shape peaks on the weekend positions. | Med | Positive | RPT / demand | Not Run | demand-ml.ts |
| UAT-RPT-045 | Thai-holiday demand model applies the learned uplift on future holidays | Planner | item with holiday-spiked history (≥2 fixed holidays in window) | 1. Backtest the item. 2. Forecast a horizon that crosses a fixed Thai holiday (e.g. Songkran). | — | `th_holiday` is among the 8 candidates; when selected, the forecast days landing on fixed holidays carry the learned uplift while ordinary days follow the weekday pattern. | Med | Positive | RPT / demand | Not Run | demand-ml.ts |
| UAT-RPT-046 | LINE report delivery + daily digest + alert user-target (LC-4) | linked staff w/ `dashboard`; Admin | Staff LINE-linked; pending approvals/PRs exist | 1. Chat `subscribe digest` as a user WITHOUT dashboard/fin_report/exec, then as one WITH. 2. `POST /api/bi/subscriptions/run`. 3. Chat `unsubscribe digest`. 4. Alert rule `channel:line, target_to:'user:<name>'` → `POST /api/alerts/run`. | as left | (1) refused, then ✔ — a `line_daily_digest` subscription carries `{line_user}`; permission-at-subscribe enforced. (2) digest pushed to the linked LINE (รออนุมัติ · PR ค้าง · แจ้งเตือน 24 ชม.) — summary only, never a dataset. (3) recipient removed. (4) the fired alert lands on the **linked** LINE (registry-resolved at send time; force-unlink silences it). | High | Control | Feature (LINE delivery) + permission gate | Pass | line-crm.ts |
| UAT-RPT-047 | LINE `ask` — governed NL analytics in chat (LC-5) | linked staff w/ `dashboard`; linked staff without | — | 1. Chat `ask ยอดขายตามสาขา` as a user WITHOUT dashboard/exec/masterdata. 2. Same as a user WITH. | as left | (1) refused — same gate as `POST /api/nl/ask`. (2) answer via the whitelist-dimension, RLS-scoped query engine (top-5 summary + `/query` pointer; honest ไม่มีข้อมูล when empty; NL never produces raw SQL). | High | Control | Feature (NL analytics) + permission gate | Pass | line-crm.ts |
| UAT-RPT-048 | LINE copilot LLM governance: schema validation, daily cap, DPA gate (LP-2) | linked staff; ops (env) | LLM key configured (or scripted client in CI) | 1. Free Thai the rules can't parse → LLM drafts → confirm. 2. Force a malformed LLM answer. 3. Set `LINE_COPILOT_DAILY_CAP=1` and exceed it. 4. `NODE_ENV=production` + key + no `AI_DPA_ACKNOWLEDGED`, send an LLM-needed text. | as left | (1) schema-valid draft → confirm → document via the ordinary path (model routed via `chat_copilot`). (2) honest "ยังไม่เข้าใจ" — nothing parked or created. (3) model NOT called past the cap; `[chat:ai-cap]` audit row; deterministic rules still answer. (4) model never called (DPA fail-closed); deterministic refusal. | High | Control | ITGC-SEC-AI / PN-26 rev 1.8 | Pass | line-crm.ts |
| UAT-RPT-049 | Digest 2.0: KPI selection + per-recipient permission filter at send time (LP-3) | two linked subscribers (Admin; dashboard-only) | AR/GL/stock data seeded | 1. `digest kpis` as a no-perm user and as Admin. 2. `subscribe digest nonsense_kpi`; `subscribe digest cash_position` as dashboard-only. 3. Admin `subscribe digest sales_yesterday,cash_position,ar_overdue,low_stock`; dashboard-only bare `subscribe digest`. 4. Run the digest once. | as left | (1) refused / permission-aware menu (defaults marked). (2) `ไม่รู้จัก KPI` / `ไม่มีสิทธิ์เห็น`. (3) selection stored on the recipient (jsonb). (4) ONE run, DIFFERENT payloads: Admin gets a flex card with the financial KPIs (formatted THB); the dashboard-only subscriber gets the trio and **never** sees cash/AR; empty data renders `—`. | High | Control | Permission-at-send (PN-26 rev 1.9) | Pass | line-crm.ts |
| UAT-RPT-050 | CFO KPI scorecard: values reconcile, RAG thresholds, drill-through, permission gate (docs/35 Phase 1) | exec / fin_report (positive); warehouse-only (negative) | a known ledger seeded (cash 100k, AR 50k, inventory 30k, AP 40k, lease 60k; rev 100k, COGS 40k, opex 20k, dep 10k, interest 5k); one 90+ overdue AR invoice; an approved monthly budget | 1. `GET /api/finance/metrics/pack`. 2. Inspect `current_ratio`, `gross_margin_pct`, `overdue_ar_pct` values + `rag`. 3. `GET /api/finance/metrics/current_ratio/drill`. 4. `GET /api/finance/metrics/gross_margin_pct/trend?periods=3`. 5. `GET /api/finance/metrics/nope/drill`. 6. Call the pack as a warehouse-only user. | as left | (1) ~31 KPIs, each with `prior_period`/`prior_year`/`budget`/`rag`/`drill`. (2) `current_ratio=4.5` (🟢 green), `gross_margin_pct=60`, `overdue_ar_pct=40` (🔴 red past threshold); `pack.budget.revenue.budget=90000`. (3) balance-sheet rows incl. cash 1000 = 100000 (KPI ties to the ledger). (4) 3-point monthly series, current month = 60. (5) **400 UNKNOWN_METRIC**. (6) **403** — the scorecard is permission-gated. | High | Control | ELC-07 (PN-26 rev 2.0) | Pass | finance-kpi.ts |
| UAT-UI-CFO-01 | CFO Command Center dashboard (docs/35 Phase 2) | exec / fin_report | KPI data present (see UAT-RPT-050 seed) | 1. Sidebar → **การเงิน → ศูนย์บัญชาการ CFO** (`/finance/command-center`). 2. Read the summary strip + a red-flagged tile (e.g. Overdue AR %). 3. Click **12-month trend** on a tile. 4. Click **View detail →**. 5. Trigger an ops-dashboard refresh in another tab. | — | Scorecard renders grouped by family; the summary strip counts needs-action / watch / healthy; a breached KPI shows a red RAG chip (icon + label) + a red left border; the trend expands a sparkline; **View detail** navigates to the statement page; the **Live** badge shows connected and tiles re-pull on `fin_kpi_refresh`. Renders correctly in light and dark themes. | Med | UI | ELC-07 (docs/35 Phase 2) | Not Run | manual / e2e |
| UAT-UI-CC-01 | Close Cockpit — period-close readiness board (docs/35 Phase 3) | exec / fin_report / gl_close (positive); warehouse-only (negative) | a period with GL activity; an AP or inventory sub-ledger that does NOT tie to its GL control account | 1. Sidebar → **การเงิน → ศูนย์ปิดงวดบัญชี** (`/finance/close-cockpit`). 2. Read the overall banner + days-to-close. 3. Inspect the tie-out pillar. 4. Post the matching sub-ledger so it reconciles; reload. 5. Open the cockpit as a warehouse-only user. | as left | (1) board renders four pillars, each with a RAG chip (icon+label). (2) overall banner **RED** with the tie-out break; days-to-close numeric. (3) the out-of-balance control account shows its variance in red; readiness pillar green (no drafts/balanced/snapshot ties). (4) after reconciling, tie-out + overall flip **GREEN**, 0 exceptions. (5) **403** — the cockpit is permission-gated. Renders in light and dark themes. | High | Detective | GL-22 (PN-04 §9) | Pass | finance-kpi.ts (aggregate) + manual (UI) |
| UAT-UI-TR-01 | Treasury / Cash Command (docs/35 Phase 4) | exec / fin_report / ar (positive); warehouse-only (negative) | GL cash + open AR/AP; optionally a non-THB payable | 1. Sidebar → **การเงิน → ศูนย์บริหารเงินสด** (`/finance/treasury`). 2. Read the headline (total cash, projected closing, trough). 3. Inspect the 13-week forecast curve + table. 4. Check the cash/bank, liquidity and FX panels. 5. Open as a warehouse-only user. | as left | (1) board renders headline + forecast + cash/bank + liquidity + FX. (2) total cash ties to the GL cash accounts; projected closing = opening + net AR/AP; the trough week is marked. (3) the forecast has 14 weekly buckets (0..13) with inflow/outflow/projected balance; the trough row is highlighted. (4) liquidity shows current/quick/cash ratios; a non-THB payable appears under FX exposure with a negative net. (5) **403** — the board is permission-gated. Renders in light and dark themes. | High | Detective | TR-01 (PN-07 §9) | Pass | finance-kpi.ts (aggregate) + manual (UI) |
| UAT-UI-PROF-01 | Segment Profitability — P&L by dimension (docs/35 Phase 5) | exec / fin_report (positive); warehouse-only (negative) | posted GL with some postings tagged to a branch/cost-centre/project and some untagged | 1. Sidebar → **การเงิน → กำไร-ขาดทุนตามมิติ** (`/finance/profitability`). 2. Read the totals + reconcile badge. 3. Switch the dimension (branch → cost centre → project). 4. Inspect the contribution bars + P&L matrix. 5. Open as a warehouse-only user. | as left | (1) board renders totals + a **✓ ties to the consolidated P&L** badge. (2) Σ segment revenue/net equals the consolidated P&L; untagged postings appear under **Unassigned**. (3) the view recomputes per dimension; a period with no activity shows an empty state (no error). (4) each segment shows revenue/COGS/gross/opex/net + net margin + contribution %; the bars rank segments by net (red if negative). (5) **403** — the board is permission-gated. Renders in light and dark themes. | High | Detective | GL-13 / ELC-07 (PN-26 §3d) | Pass | finance-kpi.ts (aggregate) + manual (UI) |
| UAT-RPT-051 | Finance MD&A narrative + schedulable KPI packs (docs/35 Phase 6) | exec / fin_report | KPI data seeded (see UAT-RPT-050); a report subscription can be created | 1. `GET /api/finance/metrics/pack` → read `narrative`. 2. `GET /api/bi/report-types`. 3. Create + run a subscription of `cfo_kpi_pack`; likewise `cash_position_pack`, `close_status_pack`. 4. On `/finance/command-center` read the **What changed** panel. | — | (1) `narrative.headline_en` names net margin + how many KPIs need action; `narrative.bullets` includes a red bullet for each breached KPI. (2) catalog exposes `cfo_kpi_pack` / `cash_position_pack` / `close_status_pack`. (3) each run **success**; the CFO summary carries the MD&A headline + red count, the cash summary the cash / projected-close / trough, the close summary the overall RAG / tie-out / days-to-close; a `report_runs` row is logged (a failed run is captured, never silent — ITGC-OP-04). (4) the What-changed panel shows the headline + bullets. | High | Detective | ELC-07 / ITGC-OP-04 (PN-26 rev 2.3) | Pass | finance-kpi.ts + bi.ts |
| UAT-RPT-052 | Per-tenant AI opt-out (PDPA มาตรา 27/30) — disclosure + right to object | md_config/exec (toggle); any AI user (effect) | AI enabled at platform level (key + DPA acknowledged) | 1. Settings › Labs & AI (`/settings/labs`) → read the disclosure on **AI ภายนอก: อนุญาตส่งข้อมูลให้ผู้ให้บริการ AI** → turn OFF (`PUT /api/feature-flags/ai_external_processing {enabled:false}`). 2. Ask the AI assistant anything (`POST /api/agent/chat`). 3. `POST /api/nl-analytics/ask`, doc-AI extract, insights. 4. Turn the flag back ON and repeat step 2. | — | (1) toggle saved; the flag description carries the PDPA disclosure. (2) `403 AI_TENANT_OPTED_OUT` — no LLM call is made. (3) each answers from its deterministic path (`source: keyword/rules/template`); nothing is transmitted to the provider. (4) the assistant answers normally again. | High | Control (negative + positive) | PN-08 §8d (PDPA opt-out) | Pass | ai-eval.ts (layer 4) |
| UAT-RPT-053 | Menu-engineering matrix — per-branch view (POS-5b) | exec/Planner | Same seed as UAT-RPT-034 with the high-volume dishes (qty 40/40) rung on branch 1 and the slow dishes (qty 5/5) + the uncosted item on branch 2 | 1. `GET /api/analytics/menu-engineering?from&to&branch_id=2`. 2. On `/restaurant-analytics` → Menu engineering, pick the branch in the **สาขา** picker. | window covering the sales | Response echoes `branch_id: 2`; the matrix is re-derived from branch-2 sales only — 2 costed items / 10 units; popularity threshold recomputes to 0.35 (0.7 × 1/2) and the weighted-average CM to 130 (1300/10); the premium dish **flips Puzzle→Star** and the low-margin drink classifies **Plowhorse**; items sold only on branch 1 are absent. UI shows the same grouping + the thresholds caption; the picker is hidden for users without the `branch`/`exec` duty. | High | Positive | Feature (menu engineering, multi-branch) | Not Run | restaurant.ts |
| UAT-RPT-054 | Workforce-analytics BI report types (HR-9) | exec | employees + org assignments + pay grades + an offboarding lifecycle + a leave balance seeded (see UAT-HR-900..907) | 1. `GET /api/bi/report-types`. 2. subscribe + run `hr_headcount_trend`. 3. `hr_turnover`. 4. `hr_tenure_distribution`. 5. `hr_comp_ratio`. 6. `hr_leave_liability` `{working_days:20}`. 7. run `hr_headcount_trend` as a 2nd tenant. | — | (1) catalog includes the five `hr_*` types; (2) `status:success`, `total_active`=4, Engineering headcount=2; (3) `turnover_pct`=20% (1 sep / 5 avg); (4) `<1y` bucket=1, `unknown`=1; (5) `count_rated`=3, `employees_out_of_band`=1 (salary above band max); (6) `total_liability`=8800 THB over 8 untaken days; (7) T2 sees `total_active`=1 (RLS). Each run persists to `report_runs`. | High | Detective | HR-09 (PN-29 HR-9) | Pass | hcm-analytics.ts |
| UAT-RPT-055 | Menu affinity computes support/confidence/lift per pair and slices by daypart | Manager | Sales seeded: A+B together on 3 of 5 bills (A on 4, B on 3); 2 lunch + 2 dinner tenders, 1 bill tenderless | 1. `GET /api/analytics/menu-affinity?from=D&to=D`. 2. Repeat with `min_pair_count=1`. | 5 bills, 3 menu items | (1) summary `{baskets:5, multi_item_baskets:3, pairs_returned:1}`; top pair A+B `{pair_count:3, support_pct:60, confidence 75/100, lift:1.25}`; `by_daypart` shows lunch (2 baskets, A+B ×2) and dinner (2 baskets, no pair over min) only — the tenderless bill counts overall-only. (2) 3 pairs (long tail). | Medium | Positive | 20 rev 3.19 (`menu-engineering.ts`) | Not Run | lift > 1 = better than chance |
| UAT-RPT-056 | Menu affinity is schedulable as a BI report | Exec | Same seeding | 1. `POST /api/bi/subscriptions` `{report_type:'menu_affinity', frequency:'weekly', filters:{from:D,to:D}}`. 2. Run it. | — | `status='success'`; summary reads `Menu affinity (D..D): 5 basket(s), 1 pair(s); top …(lift 1.25)`; catalog `GET /api/bi/report-types` lists `menu_affinity`. | Low | Positive | 20 rev 3.19 (`menu-engineering.ts`) | Not Run | trailing 30d default when no filters |
| UAT-RPT-057 | Weather demand model applies the learned rain-day dip when enabled; stays inert (dow_seasonal parity) when off | Planner | item with a rain-day dip in its history; `DEMAND_WEATHER_ENABLED` toggled per step | 1. With the flag OFF (default), backtest the item — confirm `weather` scores identically to `dow_seasonal`. 2. Turn the flag ON with a stubbed weather provider (geocode + archive + forecast) and backtest/forecast again. | — | (1) `weather` is among the 9 candidates but its forecast is byte-identical to `dow_seasonal`'s — no fabricated signal without a resolved rain-date context. (2) with the flag on and a resolved rain signal, `weather`'s forecast carries the learned rain-day dip on days the provider flags rainy, while ordinary days follow the day-of-week pattern; a provider failure (bad geocode, network error) degrades gracefully back to (1)'s parity, never an error. | Med | Positive | PN-26 rev 2.4 (`demand-ml`) | Not Run | demand-ml.ts; vendor Open-Meteo, opt-in, keyless |
| UAT-UI-DEM-01 | Demand forecasting screen | Planner | item with ≥14 days history | 1. Sidebar → **วางแผน & BI → พยากรณ์ความต้องการ (Demand ML)** (`/demand`). 2. **พยากรณ์** tab → item + horizon 14 → พยากรณ์. 3. **เทียบโมเดล** tab → backtest. 4. **ประวัติ & ความแม่น** tab. | item with <14d for the negative path | Forecast shows the auto-selected model, WAPE and a per-day table; backtest lists all candidate models with the best flagged; history + accuracy populate. An item with <14 days surfaces `INSUFFICIENT_HISTORY` as an error toast. The manual **algorithm** dropdown lists the real 9 backend keys (sma/ses/holt/seasonal_naive/croston/croston_sba/dow_seasonal/th_holiday/weather) — was stale (naive/moving_average, missing 5 real models). | Med | UI | Feature (demand ML) | Not Run | manual / e2e |
| UAT-UI-ROI-01 | Marketing ROI dashboard tab | marketing / exec | campaign ROI data present (see UAT-RPT-050-adjacent seeding, or an automation campaign with redeemed coupons) | 1. Sidebar → **CRM → การตลาด** (`/marketing`) → **ROI การตลาด** tab. 2. Change the window selector (30/90/180/365 days). 3. Repeat with no campaign activity in the window. | — | Tiles render spend (labelled "cost", not revenue), attributed revenue, net margin after discount, and ROI% (green when ≥0, red when negative); the honest-framing note renders below the tiles; changing the window re-queries `GET /api/bi/marketing-roi?days=`; an empty window renders zero-safe tiles, not an error. A `crm`-only user (no `marketing`/`exec`) cannot reach the tab (403). | Med | UI | PN-19 rev 1.51 (no new control — live read of the existing `marketing_roi` composition) | Not Run | manual / e2e |
| UAT-UI-AUD-01 | Audience export screen | marketing / exec | PDPA-05 seeding (consented + non-consented members; optionally the `audience_export` ROPA activity) | 1. `/crm/audience-export`. 2. With no ROPA activity recorded, read the banner. 3. Record the ROPA activity (`POST /api/pdpa/ropa`) and reload. 4. As a `marketing`-only user without the `users` duty, reload. 5. Inspect the preview + register sections. | — | (2) banner reads "blocked — no ROPA activity" (red); (3) banner turns green ("ROPA recorded, export can run"); (4) banner degrades to "can't verify — needs the users permission" (amber) rather than erroring the page; (5) preview shows only consented members as sha256 hashes (no raw PII anywhere in the DOM/network tab), register lists past runs with target/status/rows pushed/removed; **Go to Scheduled Reports** link navigates to `/scheduled-reports`. | Med | UI | PDPA-05 (no new control — dedicated view of existing preview/register/ROPA endpoints) | Not Run | manual / e2e |
| UAT-UI-CHREF-01 | Channel-to-member link screens (staff + member) | POS/order_mgt staff (mint); crm_member/loyalty staff (link); the customer (self-link) | an aggregator order with a captured `channel_customer_refs` row (see UAT-LOY-076..079 seeding) | 1. Staff: `/channels` → **ออเดอร์เดลิเวอรี** → **สร้าง QR** on the order → copy the link from the dialog. 2. Open the copied link as an unauthenticated browser session — confirm it shows platform + order count pre-login, then complete OTP login and see the consent checkbox (unchecked by default). 3. Confirm the link with the checkbox left unchecked, then reload the same link. 4. Staff: `/channels` → **ผูกสมาชิก** tab, filter **ยังไม่ผูก**, link a different ref to a member without ticking consent, then with it ticked. | — | (1) dialog shows a `/m?clink=…` URL (not `/portal/…`) with a copy button. (2) the pre-login screen shows platform + order count, no PII; after login the consent checkbox starts **unchecked** (never pre-selected). (3) link succeeds with `marketing_opt_in:false` recorded; reloading the same link shows "already linked" (idempotent, no re-link prompt). (4) the staff-link dialog requires an explicit member ID + checkbox state each time; the ref moves from **ยังไม่ผูก** to **ผูกแล้ว** and shows the member code/name + `link_source` badge. | Med | UI | MKT-13 (PN-19 rev 1.51) | Not Run | manual / e2e; channel.ts covers the API layer |
| UAT-UI-REP-01 | Reputation & external analytics screen (Google Maps reviews, GA4) | marketing / exec | none (mocked/stubbed OAuth in the harness; a real Google Cloud OAuth client is required in production) | 1. `/reputation` → **Connections** tab → **Connect** on Google Maps → complete the OAuth consent flow (`GET /api/reputation/oauth/start`, then the callback at `/reputation/callback`). 2. **Manage targets** → select a location → save. 3. **Sync now**. 4. **Reviews** tab → filter **ต้องตอบกลับ** → reply to a low-rated review. 5. **Analytics** tab after connecting + syncing GA4. 6. Repeat step 1 with `GOOGLE_OAUTH_CLIENT_ID` unset. 7. As a T2 tenant, confirm no T1 connections/reviews/analytics are visible. | — | (1) the callback page shows "กำลังเชื่อมต่อ…" then redirects to `/reputation` with the connection now **active** and the connected Google account email shown; (2)-(3) selected targets persist and Sync now populates reviews/analytics; (4) the needs-attention filter shows only rating ≤3 with no reply; replying posts via the Business Profile API and the review shows a **Replied** badge; (5) tiles show avg rating, needs-attention count, GA4 sessions and revenue, plus the daily table; (6) `oauth/start` 503s `OAUTH_NOT_CONFIGURED` rather than crashing; (7) T2 sees 0 T1 rows across all three tables (RLS + explicit tenant filter). No OAuth token value is ever present in any API response (only `has_refresh_token: boolean`). | Med | UI | MKT-14 (PN-19 rev 1.52, docs/47) | Not Run | manual / e2e; reputation.ts covers the API layer |
| UAT-RPT-059 | Review-response SLA — policy + breach worklist, tenant-isolated | marketing / exec | two tenants T1/T2; T1 has an OLD unreplied 2★ review, a RECENT unreplied 2★, a 5★ review; T2 has its own old 1★ | 1. As T1, `GET /api/reputation/response-settings`. 2. `GET /api/reputation/response-sla`. 3. `PUT /api/reputation/response-settings` `{slaRatingThreshold:1, slaHours:24}` then re-read the worklist. 4. Restore ≤3★/48h, reply to the old 2★, re-read. 5. As T2, `GET /api/reputation/response-sla`. 6. As a `crm`-only user (no marketing/exec), `GET /api/reputation/response-sla`. | policy defaults ≤3★/48h | (1) defaults ≤3★ within 48h, `is_default:true`; (2) the old 2★ is **breached**, the recent 2★ is **open**, the 5★ is excluded; (3) tightening to ≤1★ persists (`updated_by` set) and drops both 2★ reviews from the worklist; (4) after a reply the old review no longer appears in breaches; (5) T2 sees only its OWN breach — none of T1's reviews; (6) 403. | High | Positive + Negative (control) | MKT-16 (PN-19 rev 1.55, docs/47) | Not Run | API/functional; re-performed by `cutover/reputation.ts` (MKT-16 checks, 26/26) |
| UAT-RPT-060 | Budget Optimizer — prescriptive MMM allocation + what-if + staged plan under maker-checker, tenant-isolated | marketing / exec (plan) · exec/approvals (approve) · pr_raise (stage) | two tenants T1/T2; T1 has a pushed MMM snapshot (channels with spend/ROI); a Planner (`pr_raise`) and a separate approver (`exec`/`approvals`) | 1. As T1 marketing, `GET /api/marketing-intel/response-curves`. 2. `POST /api/marketing-intel/optimize` `{budget:100000}`. 3. `POST /api/marketing-intel/simulate` `{allocation:<the optimal split>}`. 4. `POST /api/marketing-intel/simulate` `{allocation:{}}`. 5. As the Planner, `POST /api/marketing-intel/budget-plan` `{total_budget:100000, allocation:<optimal>}`. 6. As the separate approver, `POST /api/marketing-intel/budget-plan/approve` `{plan_no}`. 7. As the SAME user who staged a plan, approve that own plan. 8. Approve a non-existent `plan_no`. 9. As T2, list budget plans. | T1's MMM snapshot pushed | (1) per-channel curves (`beta>0`, `kappa>0`); with no saturation params pushed the curves are `derived:true`; (2) the allocation sums to ~the budget and `predictedSales>0` (spend concentrates on the higher-return channel); (3) simulate reproduces the optimize prediction **identically** (deterministic); (4) empty allocation → **400** (validation); (5) plan staged **Pending** with a `BP-…` id — **no GL posting, no spend**; (6) a **different** user's approval → **Approved**; (7) the requester approving their OWN plan → **400 SOD_SELF_APPROVAL** (maker-checker, binds even Admin); (8) **404 PLAN_NOT_FOUND**; (9) T2 sees **zero** of T1's plans (tenant-isolated). | High | Positive + Negative (control) | MKT-17 (PN-19 rev 1.59, docs/60) | Not Run | API/functional; re-performed by `cutover/ext.ts` (MKT-17 checks, +9 / 330); `test/mmm-optimizer.test.ts` covers the optimiser math |
| UAT-RPT-061 | Customer Intelligence — per-customer CLV / churn / next-best-action drill-down, advisory + tenant-isolated | marketing / exec | two tenants T1/T2; T1 has members with `customer_profiles` rows and its OWN churn_risk/predicted_ltv set (Growth Engine G3); an analytics:write API key | 1. As T1, push an rfm snapshot carrying per-customer `members` with `{segment, clv, churn_risk, nba}` for a T1 member. 2. Read that member's `customer_profiles` row. 3. As T1 marketing, `GET /api/marketing-intel/segment/<segment>/customers`. 4. `GET …/customers?sort=churn`. 5. Push an rfm snapshot for the same member with **segment only** (no score fields); re-read the row. 6. As T2, `GET /api/marketing-intel/segment/<segment>/customers`. 7. `POST /api/marketing-intel/segments/activate` `{segment}` then confirm the campaign is a **draft**. | T1's members + own scores seeded | (1) push returns `scores_applied ≥ 1`; (2) `mi_clv`/`mi_churn_risk`/`mi_nba` are set on the member **without** touching the ERP's own `churn_risk`/`predicted_ltv` (separate columns); (3) the drill-down returns the member with its CLV/churn/NBA + ERP context, sorted by CLV (default); (4) `sort=churn` orders highest churn first; (5) the segment-only push **leaves the previously-pushed scores intact** (back-compat); (6) T2 sees **none** of T1's members (tenant-isolated); (7) the only contact path is a **draft** campaign (audience `mi_segment`) a human edits + sends — nothing auto-sends. | High | Positive + Negative (control) | MKT-18 (PN-19 rev 1.61, docs/60) | Not Run | API/functional; re-performed by `cutover/ext.ts` (MKT-18 checks, +6 / 336); platform `rfm_model.customer_intelligence` covered by engine unit tests |
| UAT-RPT-062 | Closed-loop measurement — holdout A/B experiment, immutable arms, real-revenue lift, tenant-isolated | marketing / exec · crm_campaign (start) | a tenant with ≥8 members in a pushed `mi_segment` | 1. `POST /api/marketing-intel/experiments` `{segment, control_pct:0.5, window_days:0, activate:true}`. 2. Inspect the stored arms + the treatment-only campaign recipients. 3. Seed POS revenue: treatment ฿1000/head, control ฿100/head in the window. 4. `POST /api/marketing-intel/experiments/measure` `{experiment_no}`. 5. Measure the same experiment again. 6. Start a new experiment with `window_days:30` and measure immediately. 7. Start on a segment with no members. 8. `GET /api/v1/marketing/experiment-outcomes` with an `analytics:read` key; and as a second tenant. | members seeded in the segment | (1) the segment is split into a treatment arm + a randomised holdout control (both non-empty, sum = members); (2) the arms are **immutable** and partition the segment (no member in both), and the treatment-only campaign (`audience:'members'`) contains **no** control member; (3–4) measure returns **positive lift** (฿1000 vs ฿100/head → ~900%) with incremental revenue > 0, computed from real POS revenue via the read API (no cross-domain join); (5) re-measuring → **400 ALREADY_MEASURED**; (6) measuring before the window elapses → **400 WINDOW_NOT_ELAPSED**; (7) empty segment → **400 EMPTY_SEGMENT**; (8) the measured outcomes are pullable read-only (`analytics:read`) and the second tenant sees **none** of the first's experiments. | High | Positive + Negative (control) | MKT-19 (PN-19 rev 1.62, docs/60) | Not Run | API/functional; re-performed by `cutover/ext.ts` (MKT-19 checks, +9 / 345) |
| UAT-RPT-063 | Model governance — two-person approval on spend/contact-driving analytics, model cards, drift gate, audit trail | marketing / exec · exec/approvals (settings + approve) | a governed tenant with a prior approved MMM run + a pushed-via-API-key run | 1. `GET /governance/settings` (default). 2. `PUT /governance/settings` `{require_approval:true}`. 3. Push a new MMM run with a much lower R² than the prior approved + a `model_card`. 4. `GET /governance/runs`. 5. `GET /api/finance/approvals/pending`. 6. `POST /governance/runs/approve` `{id}` (no reason) for the drifted run. 7. Same, with a reason. 8. Push a new RFM run (Pending), then `POST /segments/activate` for its segment; then approve the RFM run and retry activate. 9. `GET /governance/audit-trail`. 10. As a second tenant, `GET /governance/runs`. | governance seeded per steps | (1) defaults `require_approval:false` (back-compat); (2) enabling persists; (3) the push is accepted but lands **Pending**; (4) the run is Pending, carries its **model card**, and is flagged **drift**/blocked; (5) it appears in the **GOV-01** center as `mi_analytics_run` (control MKT-20); (6) approving a drifted run with **no reason** → **400 DRIFT_REASON_REQUIRED**; (7) a **different** user (≠ the pusher) approving **with a reason** → **Approved** (maker-checker); (8) activating off an **unapproved** latest RFM run → **400 ANALYTICS_NOT_APPROVED**, and after the RFM run is approved the activation succeeds (draft campaign); (9) the audit trail links **runs → budget plans → experiments**; (10) the second tenant sees **none** of the first's runs. | High | Positive + Negative (control) | MKT-20 (PN-19 rev 1.63, docs/60) | Not Run | API/functional; re-performed by `cutover/ext.ts` (MKT-20 checks, +11 / 356) |
| UAT-RPT-064 | Marketing Fact Layer — shared customer + segment fact sheet, tenant-isolated, permission-gated (docs/61 Phase 0) | marketing / exec | two tenants T1/T2; T1 has a member `M1` with a `customer_profiles` row carrying pushed `mi_clv`/`mi_churn_risk`/`mi_nba` + `mi_rfm_segment=S` and `marketing_opt_in=true`; a pushed MMM snapshot (channels with ROI); a `warehouse`-only user (no `marketing`/`exec`) | 1. As T1 marketing, `GET /api/marketing-activation/facts/customer/M1`. 2. `GET /api/marketing-activation/facts/customer/M-NOPE`. 3. `GET /api/marketing-activation/facts/segment/S`. 4. As T1 marketing, `GET …/facts/customer/<a T2 member code>`. 5. As the warehouse-only user, `GET …/facts/customer/M1`. | T1's member + scores + MMM snapshot seeded | (1) the fact sheet composes **CRM × MI** — `value.clv_platform` = the pushed CLV **and** `value.predicted_ltv_own` from the ERP (separate), `risk.churn_risk_platform` + `risk.churn_risk_own`, `next_best_action` = the pushed NBA, `rfm.segment` + `rfm.mi_segment`, and `marketing_opt_in=true` (the consent fact every tool must honour); (2) an unknown code → **404 CUSTOMER_NOT_FOUND**; (3) the segment sheet rolls up `count`, `value.avg_clv_platform`/`total_spend`, `risk.avg_churn_risk_platform`, `next_best_action.dominant`, and `best_channel` (the tenant's highest-ROI MMM channel); (4) a member in **another tenant** → **404** (RLS tenant-scoped, no cross-tenant read); (5) a non-`marketing`/`exec` principal → **403**. No GL posting, no contact, no spend — a pure read model. | High | Positive + Negative (control) | docs/61 Phase 0 — PN-19 rev 1.64 (no new control; read aggregator reusing tenant isolation) | Not Run | API/functional; re-performed by `cutover/ext.ts` (Fact Layer checks, +5 / 361) |
| UAT-RPT-065 | Propensity & Cross-Sell — fact-ranked next-best-offer + best-audiences, advisory-only, tenant-isolated (docs/61 Phase 1, MKT-23) | marketing / exec | two tenants T1/T2; T1 has co-purchase history where item **A ↔ B** co-occur (lift > 1) and **C ↔ D** as noise, menu items A/B/C/D priced+costed (so a margin exists), a member **M1** whose `favorite_item_ids=['A']` and a pushed `mi_segment`; a `warehouse`-only user (no `marketing`/`exec`) | 1. As T1 marketing, `GET /api/marketing-activation/propensity/customer/M1`. 2. Inspect the ranked `offers`. 3. `GET /api/marketing-activation/propensity/item/B`. 4. As the warehouse-only user, `GET …/propensity/customer/M1`. 5. As T1, `GET …/propensity/customer/<a T2 member code>`. | T1's baskets + member + margins seeded | (1) a ranked **next-best-offer** list; (2) it offers **B** (the un-owned cross-sell) driven by the owned **A** (`driver_item_id=A`, `lift>1`), **excludes A** (never re-offers what they already buy), carries the `marketing_opt_in` consent fact + a **note** stating scoring is advisory (MKT-23) and contact stays consent-gated — **no contact is sent, no spend posted**; (3) `driver_item_ids` includes **A** and the ranked **audiences** name the member's segment (`candidate_members ≥ 1`); (4) a non-`marketing`/`exec` principal → **403**; (5) a member in another tenant → **404** (RLS tenant-scoped). A pure read/scoring model — no GL posting, no migration. | High | Positive + Negative (control) | MKT-23 (PN-19 rev 1.65, docs/61) | Not Run | API/functional; re-performed by `cutover/ext.ts` (MKT-23 checks, +6 / 367); `test/propensity-scoring.test.ts` covers the ranking math (11) |
| UAT-RPT-066 | Segment × Channel ROI — cell ranking + budget split, staged under maker-checker, tenant-isolated (docs/61 Phase 2, MKT-25) | marketing / exec (rank) · pr_raise (stage) · exec/approvals (approve) | a tenant with a pushed MMM run (channels with ROI), ≥1 pushed `mi_segment` with members carrying CLV, optionally a Measured experiment on a segment; a requester (`pr_raise`), a separate approver (`exec`/`approvals`), and a `warehouse`-only user | 1. As marketing, `GET /api/marketing-activation/segment-channel-roi?budget=100000`. 2. Inspect `cells` + `channel_allocation`. 3. As the requester, `POST /api/marketing-activation/segment-channel-roi/stage` `{total_budget:100000}`. 4. As the separate approver, `POST /api/marketing-intel/budget-plan/approve` `{plan_no}`. 5. Stage a second plan as the requester, then approve it as the SAME requester. 6. As the warehouse-only user, `GET …/segment-channel-roi`. | MMM + segments seeded | (1) a ranked list of **segment × channel cells**, each scored **incremental ROI × value** (a measured-lift segment multiplies that segment's cells; the recommendation basis is `measured+mmm` or `mmm`); (2) `channel_allocation` sums to ~the budget and concentrates on the higher-ROI channels — the response carries a **note** that it is advisory (MKT-25) and never posts spend; (3) staging creates a **Pending** `mi_budget_plans` row (a `BP-…` id) — **no GL posting, no spend**; (4) a **different** user's approval → **Approved**; (5) the requester approving their OWN plan → **400 SOD_SELF_APPROVAL** (the reused MKT-17 maker-checker); (6) a non-`marketing`/`exec` principal → **403**. A read/scoring model — no new spend path, no migration (reuses `mi_budget_plans`). | High | Positive + Negative (control) | MKT-25 (PN-19 rev 1.66, docs/61) | Not Run | API/functional; re-performed by `cutover/ext.ts` (MKT-25 checks, +6 / 373); `test/segment-channel-scoring.test.ts` covers the cell-ranking math (9) |
| UAT-RPT-058 | Marketing Mix Modeling (MMM) — ingest → run → audited, reproducible channel ROI, tenant-isolated | marketing / exec | two tenants T1/T2 | 1. As T1, ingest signals: `POST /api/mmm/ingest/sales-daily` (revenue tagged to `utm_source` tiktok/facebook/'') and `POST /api/mmm/ingest/sentiment` (mention_count + sentiment_score per platform). 2. `POST /api/mmm/run` `{windowDays:30, spendByChannel:{tiktok:50000, facebook:30000}}`. 3. `GET /api/mmm/runs/:runNo`. 4. Re-run step 2 with the SAME inputs. 5. `POST /api/mmm/run` `{spendByChannel:{}}` (zero spend). 6. As T2, `GET /api/mmm/summary` and `GET /api/mmm/runs/:T1runNo`. 7. `GET /api/bi/mmm-summary`. 8. As a `crm`-only user (no `marketing`/`exec`), any `/api/mmm/*`. | ingested rows per step 1 | (2) returns a `run_no` (`MMM-YYYYMMDD-NNN`) + per-channel results; the persisted `mmm_model_runs` header records `window_days`, `total_spend=80000`, `spend_by_channel`, and `created_by` = the caller; (3) results carry `roi = contribution ÷ spend`, `sales_lift_contribution` summing to 100, and `optimal_budget_allocation` summing to exactly 80000 — a higher-ROI channel gets a larger allocation; (4) identical inputs yield identical numbers (deterministic); (5) every `roi` is **null** (not Infinity/error) and allocations are 0; (6) T2 sees `has_run:false`/0 T1 rows and the T1 run_no returns **404 MMM_RUN_NOT_FOUND** (BOLA-safe, tenant+run_no filter); (7) the live BI read returns the latest run's channels; (8) 403. | High | Positive + Negative (control) | MKT-15 (PN-19 rev 1.54, docs/48) | Not Run | API/functional; re-performed end-to-end by `cutover/mmm.ts` ToE harness (16 checks — steps 1–8, incl. the append-only `audit_log` run row + the `/mmm` web workspace); `test/mmm-model.test.ts` covers the attribution math; RLS for all six tables also hard-asserted by `pg-core` |
| UAT-UI-BUD-01 | Budget vs Actual screen | exec / planner | posted GL activity in the year | 1. Sidebar → **วางแผน & BI → งบประมาณเทียบจริง (Budget vs Actual)** (`/budget`). 2. **ตั้งงบประมาณ** tab → account + annual amount → save. 3. **งบเทียบจริง** tab → choose year. | account 5100, annual 120000 | Budget saved (toast: lines=12, total); BvA table shows budget/actual/variance/% with a **Favorable/Unfavorable** flag and revenue/expense/net roll-ups; actuals are sourced only from **Posted** journal lines. | Med | UI | Feature (budget vs actual) | Not Run | manual / e2e |
| UAT-BUD-02 | Budget maker-checker — pending excluded until approved | Planner then a different approver (`approvals`/`gl_close`) | — | 1. `POST /api/ledger/budgets`. 2. `GET …/budget-vs-actual`. 3. self-approve `POST …/budgets/approve`. 4. approve as a different user. 5. re-read budget-vs-actual. | account 5105, 2030-01, 300 | (1) `status:'PendingApproval'`; (2) the budget is EXCLUDED from variance; (3) `403 SOD_VIOLATION` (binds Admin); (4) `status:'Approved'`; (5) the budget now appears in budget-vs-actual. | High | Control | BUD-01 | Pass | budget.ts |
| UAT-BUD-03 | Material budget variance flagged for review | Controller / FP&A | budget 5000 + actual 7000 for a period | 1. `GET /api/ledger/budget-vs-actual?fiscal_year=&period=`. | budget 5000, actual 7000 | The 5100 line shows `material:true`, `requires_review:true`; `review.material_count`≥1, `review.requires_review_count`≥1, `review.last_signoff:null`; threshold ≥10% & ≥THB 1,000 reported. | High | Detective | ELC-06 | Not Run | budget.ts |
| UAT-BUD-04 | Management variance-review sign-off recorded | Controller (`exec`/`approvals`/`gl_close`) | material variance present | 1. `POST /api/ledger/budget-review/sign-off` with no note. 2. With a note. 3. Re-read budget-vs-actual. 4. `GET /api/ledger/budget-reviews`. | `{fiscal_year, period, notes}` | (1) **400** (note required); (2) **200**, records `material_count`, `unfavorable_total`=2000, `reviewed_by`; (3) report `review.last_signoff` shows the reviewer + material count; (4) review history lists the sign-off. | High | Control | ELC-06 | Not Run | budget.ts |
| UAT-BUD-05 | Budget-control settings tab — policy + availability + commitment audit (UI over BUD-02) | Exec (`exec`/`gl_close` to change) | approved budget for an account in the current month; budget-control gate cases in UAT-P2P-131..128 | 1. `/budget` → **ควบคุมงบ (Budget control)** tab. 2. Set policy `advise|warn|block` + default budget account → save. 3. **ตรวจงบคงเหลือ**: enter the account (+period) → check. 4. Review **ภาระผูกพันงบประมาณ** after approving a PR/PO. | account 5100 | (2) saved (`PUT /api/budget/control-settings`, exec/gl_close only — a viewer role gets 403); last-updated-by shown. (3) budget YTD / actuals YTD / open commitments / **available** tiles; an unbudgeted account states it is not enforced. (4) the commitments table lists each approved doc's reservation with status open/consumed/released and, for an override, who + why (red). | Med | UI+Control | BUD-02 (new) | Not Run | budget.ts / manual |
| UAT-UI-BUD-02 | Variance-review banner + sign-off | Controller | on `/budget` → งบเทียบจริง | 1. Open the BvA tab for a period with a material variance. 2. Click **บันทึกการสอบทาน**, enter a follow-up note. | — | Banner shows the material count + **ต้องสอบทาน** count + ⚠ on material rows; after sign-off the banner shows **สอบทานล่าสุดโดย …** with the note. | Med | UI | ELC-06 | Not Run | manual / e2e |
| UAT-UI-INS-01 | Insights screen (anomalies · replenishment · AI) | exec / planner / warehouse | sales + movement + stocktake history | 1. Sidebar → **วางแผน & BI → ข้อมูลเชิงลึก (Insights)** (`/insights`). 2. **ภาพรวม** tab. 3. **ความผิดปกติ** tab → toggle 7/30/90 → press **คำแนะนำ (AI)** on a movement row. 4. **เติมสต๊อก** tab → click an item row. | — | Overview shows reorder/anomaly counts + an AI summary + top-3 reorder items; Anomalies lists movement (Z-score) + stocktake-variance rows and the AI button returns a per-anomaly narrative (`POST /api/analytics/insight`); Replenishment lists predicted stock-outs and the row drill shows the per-item AI recommendation. All advisory (no GL, no auto-order). | Med | UI | Feature (insights UI), BI-04 | Not Run | manual / e2e |
