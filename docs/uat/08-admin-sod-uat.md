# UAT — Cycle 08: Admin / Segregation of Duties / Access Governance / Audit

**Status: DRAFT v0.7 · 2026-06-28** · Cross-ref: process narrative `08-itgc.md` (ITGC-AC-01/08/09/10, SoD R01–R16) + `27-platform-customization.md` §7 (platform features, incl. public API + SSO/SCIM), harness `tools/cutover/src/compliance.ts` + `ext.ts`.

Result legend: Pass / Fail / Blocked / N/A / Not Run. Error codes/amounts are exact.

| Test ID | Scenario/Title | Role | Preconditions | Test steps | Test data | Expected result | Priority | Type | Traceability | Result | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UAT-ADM-001 | Conflicting permission set blocked on create | AccessAdmin/Admin | — | 1. `POST /api/admin/users` with both `procurement`+`creditors`. 2. `GET /api/admin/users`. | `{username: sod_blocked, permissions:[procurement, creditors]}` | 422 `SOD_CONFLICT`; user NOT created. | High | Control | ITGC-AC-09, R03 | Not Run | compliance.ts |
| UAT-ADM-002 | Block message names the violated rule | AccessAdmin | SoD conflict attempt | 1. Inspect error message from ADM-001. | — | Message includes `R03` (procurement ✗ creditors). | Med | Control | ITGC-AC-09, R03 | Not Run | compliance.ts |
| UAT-ADM-003 | Justified override (with reason) honoured | AccessAdmin | — | 1. `POST /api/admin/users` conflict + `allow_sod_override` + `sod_reason`. | `{..., allow_sod_override: true, sod_reason: compensating CFO review}` | 200/201; user created; override+reason logged. | High | Control | ITGC-AC-09, R03 | Not Run | compliance.ts |
| UAT-ADM-004 | Override without reason rejected | AccessAdmin | — | 1. `POST /api/admin/users` conflict + `allow_sod_override` but no reason. | `{..., allow_sod_override: true}` | 422 `SOD_CONFLICT` (reason mandatory). | High | Control | ITGC-AC-09 | Not Run | compliance.ts |
| UAT-ADM-005 | Clean single-duty set accepted | AccessAdmin | — | 1. `POST /api/admin/users` with `['ar']` only. | `{username: sod_clean, permissions:[ar]}` | 200/201; created without friction. | Med | Positive | ITGC-AC-09 | Not Run | compliance.ts |
| UAT-ADM-006 | SoD block enforced on UPDATE too | AccessAdmin | sod_clean exists | 1. `PATCH /api/admin/users/sod_clean` set conflicting perms. | `{permissions:[procurement, creditors]}` | 422 `SOD_CONFLICT`. | High | Control | ITGC-AC-09, R03 | Not Run | compliance.ts |
| UAT-ADM-007 | Access-review report lists effective perms + conflicts | Admin | Users with overrides exist | 1. `GET /api/admin/users/access-review`. | — | Per-user effective permissions + `sod_conflict_count`; `summary.total_users` ≥1; override user shows conflict ≥1. | High | Detective | ITGC-AC-08 | Not Run | compliance.ts |
| UAT-ADM-008 | Access-review CSV export with decision column | Admin | — | 1. `GET /api/admin/users/access-review/export`. | — | CSV with `decision_keep_revoke` column; >1 line. | Med | Detective | ITGC-AC-08 | Not Run | compliance.ts |
| UAT-ADM-009 | UAR period certification recorded + retrievable | Admin/AccessAdmin | — | 1. `POST /api/admin/users/access-review/certify`. 2. `GET .../certifications`. | `{period: 2026-Q2, notes: quarterly UAR}` | certify `certified: true`; certifications list contains 2026-Q2. | High | Control | ITGC-AC-08 | Not Run | compliance.ts |
| UAT-ADM-010 | Audit log captures mutating requests | Admin | After user/JE mutations | 1. Inspect `audit_log` row count. | — | Rows > 0; tenant_id populated for scoped mutations. | High | Detective | ITGC-AC-10 | Not Run | compliance.ts |
| UAT-ADM-011 | Audit log UPDATE blocked (append-only) | DBA/Admin | audit_log has rows | 1. Attempt `UPDATE audit_log SET actor='tamper'`. | — | Rejected by DB trigger (append-only). | High | Control | ITGC-AC-10 | Not Run | compliance.ts |
| UAT-ADM-012 | Audit log DELETE blocked (append-only) | DBA/Admin | audit_log has rows | 1. Attempt `DELETE FROM audit_log`. | — | Rejected; row count unchanged. | High | Control | ITGC-AC-10 | Not Run | compliance.ts |
| UAT-ADM-013 | API key issuance + authentication | Admin | Platform enabled | 1. `POST /api/platform/api-keys`. 2. `GET /api/auth/me` with `Bearer ierp_...`. | `{name: ci, scopes:[read]}` | key starts `ierp_`; authenticates; username `apikey:*`. | Med | Positive | ITGC-AC-07 | Not Run | worldclass.ts |
| UAT-ADM-014 | Self-serve signup provisions tenant | (public) | — | 1. `POST /api/auth/signup`. 2. login provisioned admin. | `{tenant_code: NEWCO, admin_username: newadmin, admin_password: secret12}` | tenant provisioned; admin can log in. | Med | Positive | ITGC-AC-04 | Not Run | worldclass.ts |
| UAT-ADM-015 | Duplicate tenant code → clean 409 | (public) | NEWCO exists | 1. `POST /api/auth/signup` same tenant_code. | `{tenant_code: NEWCO, ...}` | 409 (not 500). | Low | Control | ITGC-AC-04 | Not Run | worldclass.ts |
| UAT-ADM-016 | SoD conflict report available | Admin | Conflicts exist | 1. View SoD conflict report (access-review summary). | — | Report enumerates users holding conflicting duties with rule IDs. | Med | Detective | ITGC-AC-08/09 | Not Run | compliance.ts |
| UAT-ADM-017 | AI-proposed action requires human approval (D1) | proposer + approver | AI write-ops enabled | 1. `POST /api/ai/actions` (kind journal_entry). 2. Approve as a DIFFERENT user holding `gl_post`. | balanced JE | Step 1 → `pending`; step 2 → `executed`, `result_ref` `JE-…`, GL balanced. | High | Positive | ITGC-AC-09, GL-01 | Not Run | ai-actions.ts |
| UAT-ADM-018 | AI action — self-approval blocked (SoD) | proposer | Pending AI action by self | 1. Approve own proposed action. | — | 400 `SOD_SELF_APPROVAL`; stays pending. | High | Control | ITGC-AC-09 | Not Run | ai-actions.ts |
| UAT-ADM-019 | AI action — approver lacks kind permission | approver w/ `approvals` only | Pending JE action | 1. Approve a journal_entry without `gl_post`. | — | 403 `FORBIDDEN`; stays pending. | High | Control | ITGC-AC-02/09 | Not Run | ai-actions.ts |
| UAT-ADM-020 | AI action — tenant isolation (RLS) | T2 user | T1 has a pending action | 1. List/approve the T1 action as T2. | bearer t2 | List shows 0; approve → 404 NOT_FOUND. | High | Control | ITGC-AC-03 | Not Run | ai-actions.ts |
| UAT-ADM-097 | AI token ceiling + metered overage (no unlimited tier) | Tenant Admin (Pro) | AI enabled; daily usage near the included cap | 1. Use AI past the included daily cap. 2. `GET /api/billing/ai-usage`. 3. Continue to the hard ceiling. 4. `GET /api/billing/ai-overage?month=YYYY-MM`. | — | (2) `overage_tokens`>0 with `projected_overage_thb` priced at the plan's `ai_overage_rate_thb_per_1k`, `over_budget:false` below the ceiling; (3) at `ai_tokens_daily_max` the next AI call → 403 `AI_BUDGET_EXCEEDED`; (4) month overage line `amount` = overage_tokens/1000 × rate. | High | Control | AIG-03 | Not Run | manual (checkBudget/recordUsage) |
| UAT-ADM-021 | Custom field — define + slugged key | MasterDataAdmin | — | 1. `POST /api/custom-fields/defs` `{entity:customer, label:"Sales rep", data_type:text}`. | as left | 201; `field_key`=`sales_rep`; listed for `customer`. | Med | Positive | Feature (UDF) | Not Run | ext.ts |
| UAT-ADM-022 | Custom field — typed/required/option validation | MasterDataAdmin | customer has text + required select (A/B/C) + date fields | 1. `PUT /values` missing required. 2. select value `Z`. 3. unknown key. 4. valid set. | as left | `REQUIRED_FIELD`; `BAD_OPTION`; `UNKNOWN_FIELD`; then 200 with typed values returned. | High | Control | Feature (UDF) | Not Run | ext.ts |
| UAT-ADM-023 | Custom field — values typed on read + bulk load | MasterDataAdmin | CUST-1 has values | 1. `GET /values?entity=customer&record_id=CUST-1`. 2. `POST /values/bulk`. | as left | date returns `2026-06-01` typed; bulk keys by record (`CUST-1` present, `CUST-2` absent). | Med | Positive | Feature (UDF) | Not Run | ext.ts |
| UAT-ADM-024 | Custom field — tenant isolation (RLS) | T2 user (non-admin) | T1 defined customer fields | 1. `GET /api/custom-fields/defs?entity=customer` as T2. | bearer t2 | 0 definitions (T1's not visible). | High | Control | Feature (UDF) | Not Run | ext.ts |
| UAT-ADM-025 | Alert rule — create + reject unknown metric | MasterDataAdmin | metric catalog | 1. `POST /api/alerts/rules {metric:low_stock_count,operator:gte,threshold:1}`. 2. metric `nope`. | as left | Rule created; unknown metric → 400 `BAD_METRIC`. | Med | Control | Feature (alerts) | Not Run | ext.ts |
| UAT-ADM-026 | Alert sweep fires a breached rule | MasterDataAdmin | inventory below reorder point | 1. `POST /api/alerts/run`. | as left | `fired_count`≥1 for `low_stock_count`; a notification + `alert_events` row written. | High | Positive | Feature (alerts) | Not Run | ext.ts |
| UAT-ADM-027 | Alert cooldown suppresses re-fire | MasterDataAdmin | rule just fired | 1. `POST /api/alerts/run` again. | as left | `fired_count`=0, `suppressed`≥1. | Med | Control | Feature (alerts) | Not Run | ext.ts |
| UAT-ADM-028 | Alert rules tenant-isolated (RLS) | other-tenant user | a tenant created a rule | 1. `GET /api/alerts/rules` as another tenant. | as left | The rule isn't visible. | High | Control | Feature (alerts) | Not Run | ext.ts |
| UAT-ADM-029 | Audit viewer — query, filter, paginate | AccessAdmin/Admin | mutations recorded | 1. `GET /api/admin/audit?limit=10`. 2. `?action=saved-views`. 3. `?status=success`. 4. `?limit=abc`. | as left | (1) 200, `rows`≤10 + numeric `total`>0; (2) all rows' `action` contains `saved-views`; (3) all `status=success`; (4) 400 `BAD_QUERY`. | High | Detective | ITGC-AC-10 | Not Run | ext.ts |
| UAT-ADM-030 | Audit viewer — CSV export + permission gate | AccessAdmin; non-users role | — | 1. `GET /api/admin/audit/export`. 2. as a user without `users`. | as left | (1) 200, CSV starting `id,ts,actor,tenant_id,action…`; (2) 403. | Med | Control | ITGC-AC-10/02 | Not Run | ext.ts |
| UAT-ADM-031 | Audit viewer — tenant isolation (RLS) | AccessAdmin (T2) + AccessAdmin (HQ) | both tenants have events | 1. `GET /api/admin/audit` as a T2 admin. 2. as the HQ admin filtered to T2's action. | bearer per tenant | (1) every row `tenant_id`=T2 (≥1); (2) HQ admin sees none of T2's rows (no leakage). | High | Control | ITGC-AC-10/03 | Not Run | ext.ts |
| UAT-ADM-032 | Bulk import — dry-run validates every row | MasterDataAdmin | items master | 1. `POST /api/admin/master-data/items/import/validate` with a 5-row CSV (1 good, 1 no-key, 1 no-desc, 1 bad price, 1 dup key). | as left | `total`=5, `valid`=1, `invalid`=4; errors include `REQUIRED_EMPTY`,`BAD_NUMBER`,`DUP_IN_FILE`; **no rows written**. | High | Control | Feature (bulk import), MDM-02 | Not Run | ext.ts |
| UAT-ADM-033 | Bulk import — checked commit (block vs skip) | MasterDataAdmin | same CSV | 1. `…/import/checked` (no skip). 2. with `skip_errors:true`. 3. clean CSV. 4. re-run clean CSV. | as left | (1) `status:invalid`, imported 0; (2) `status:partial`, imported 1 (valid only); (3) `status:success`, imported N; (4) imported 0, all `EXISTS`. | High | Positive | Feature (bulk import) | Not Run | ext.ts |
| UAT-ADM-034 | Bulk import — tenant-scoped entity stamped | MasterDataAdmin/Warehouse | vendors (tenant-scoped) | 1. `…/vendors/import/checked` as a tenant admin. | as left | `status:success`; the vendor row's `tenant_id` = the importer's tenant (RLS scoping). | High | Control | Feature (bulk import), MDM-01, R13 | Not Run | ext.ts |
| UAT-ADM-035 | Webhook — register + secret-once + isolation | AccessAdmin (2 tenants); non-users role | platform enabled | 1. `POST /api/platform/webhooks`. 2. `GET …/webhooks` as owner, as other tenant, as a non-`users` user. | `{url, events:[]}` | Register returns id + 48-hex `secret`; list shows it **without** the secret; other tenant sees none; non-`users` → 403. | High | Control | ITGC-AC-04/03 | Not Run | ext.ts |
| UAT-ADM-036 | Webhook — event emits a signed delivery (logged) | AccessAdmin | endpoint registered; a breaching alert | 1. `POST /api/alerts/run` (fires). 2. `GET …/webhooks/deliveries`. | as left | A `alert.fired` delivery is recorded; status `failed` to the unreachable test URL, `attempts`=1 (HMAC-signed POST attempted, bounded 10s). | High | Positive | Feature (webhooks), ITGC-AC-04 | Not Run | ext.ts |
| UAT-ADM-037 | Webhook — redeliver / dispatch / revoke | AccessAdmin | a failed delivery exists | 1. `POST …/deliveries/:id/redeliver`. 2. `POST …/webhooks/dispatch`. 3. `DELETE …/webhooks/:id`. | as left | Redeliver re-attempts (attempts advance, capped at 3); dispatch re-runs failed-but-not-exhausted; delete revokes the endpoint (`deleted:true`). | Med | Control | Feature (webhooks) | Not Run | ext.ts |
| UAT-ADM-038 | Branding — set + render on receipt + validation | AccessAdmin | a sale exists for the tenant | 1. `PATCH /api/tenant/profile` `{logo_url(https), tagline, branding_prefs}`. 2. `GET /api/print/receipt/:saleNo`. 3. `PATCH` a non-https logo. 4. set `show_logo_on_receipt:false`. | as left | (1) 200 round-trips; (2) receipt HTML contains the logo `<img>` + tagline; (3) 400; (4) logo suppressed, tagline stays. | High | Positive | Feature (branding) | Not Run | ext.ts |
| UAT-ADM-039 | Branding — tenant-isolated | AccessAdmin (2 tenants) | both tenants set branding | 1. Each tenant `PATCH`es a distinct tagline. 2. `GET /api/tenant/profile` as each. | bearer per tenant | Each admin sees only its own org branding (RLS); no cross-tenant bleed. | High | Control | Feature (branding), ITGC-AC-03 | Not Run | ext.ts |
| UAT-ADM-040 | Inbox — targeted notification reaches the right (tenant, role) user | Warehouse@T2 | an alert sweep fired a `target_role:Warehouse` notification for T2 | 1. `GET /api/notifications/inbox`. 2. `GET …/unread-count`. | bearer T2-Warehouse | The fired notification appears as an **unread** item; `unread_count` ≥ 1. | High | Positive | Feature (notification inbox) | Not Run | ext.ts |
| UAT-ADM-041 | Inbox — tenant isolation (no cross-tenant notifications) | Warehouse@HQ | T2 has the notification above | 1. `GET /api/notifications/inbox` as HQ-Warehouse. | bearer HQ-Warehouse | 0 items; `unread_count` = 0 — the `notifications` table is filtered by `target_tenant_id` (not RLS). | High | Control | Feature (notification inbox), ITGC-AC-03 | Not Run | ext.ts |
| UAT-ADM-042 | Inbox — broadcast visible to all; another role's note hidden | Warehouse@T2 | a `target_role:NULL` broadcast and a `target_role:Planner` note both exist for T2 | 1. `GET /api/notifications/inbox`. | bearer T2-Warehouse | The broadcast IS listed; the Planner-only note is NOT (role filter). | High | Control | Feature (notification inbox) | Not Run | ext.ts |
| UAT-ADM-043 | Inbox — per-user mark-read, guard & mark-all | Warehouse@T2 | unread items exist; the Planner-only note exists | 1. `POST /api/notifications/:id/read` (a visible one). 2. `GET …/inbox?unread_only=1`. 3. `POST …/:id/read` on the Planner-only note. 4. `POST …/mark-all-read`. | bearer T2-Warehouse | (1) `ok:true`, `unread_count` decremented; (2) the just-read item is excluded; (3) `ok:false` (not visible → cannot mark); (4) `unread_count` = 0. | High | Control | Feature (notification inbox) | Not Run | ext.ts |
| UAT-ADM-044 | Public API — OpenAPI doc + discovery open | (none) | API running | 1. `GET /api/v1/openapi.json` (no key). 2. `GET /api/v1` (no key). | — | (1) 200, `openapi:"3.1.0"`, paths include `/items` + `/invoices`; (2) 200, `version:"v1"` + endpoints list. | Med | Positive | Feature (public API) | Not Run | ext.ts |
| UAT-ADM-045 | Public API — API-key-only, scope identity | AccessAdmin (issuer); api-key | a key with `scopes:['*']` issued | 1. `GET /api/v1/me` with `Bearer ierp_…`. 2. `GET /api/v1/items` with a **human JWT**. 3. `GET /api/v1/me` with no token. | bearer ierp_… | (1) 200, `principal:apikey:*`, correct `tenant_id`, scopes; (2) 403 `API_KEY_REQUIRED`; (3) 401. | High | Control | Feature (public API), ITGC-AC-07 | Not Run | ext.ts |
| UAT-ADM-046 | Public API — tenant isolation (RLS) over reads | api-key (2 tenants) | HQ & T2 keys; T2 has a seeded order/invoice + stock | 1. `GET /api/v1/inventory`, `/orders`, `/invoices` with each key. | bearer per key | Each key sees only its tenant's rows (HQ stock item A vs T2 LOW1; T2's `SO-PUB-CF2`/`INV-PUB-CF2` invisible to HQ); invoice `outstanding` = amount − paid. | High | Control | Feature (public API), ITGC-AC-03 | Not Run | ext.ts |
| UAT-ADM-047 | Public API — scope gate | api-key | a key with `scopes:['catalog:read']` | 1. `GET /api/v1/items`. 2. `GET /api/v1/orders`. | bearer ierp_… | (1) 200; (2) 403 `INSUFFICIENT_SCOPE` (missing `orders:read`). | High | Control | Feature (public API), ITGC-AC-07 | Not Run | ext.ts |
| UAT-ADM-048 | Public API — per-key rate limit | api-key | rate cap set low (test) | 1. Call `GET /api/v1/me` with one key past the cap. 2. Call with a different key. | bearer ierp_… | (1) a `429 RATE_LIMITED` is returned once the window cap is exceeded; (2) the other key still returns 200 (limit is per-key). | Med | Control | Feature (public API), anti-abuse | Not Run | ext.ts |
| UAT-ADM-049 | SSO — configure OIDC + secret write-only | AccessAdmin@T2 | — | 1. `PUT /api/platform/identity` `{sso_enabled, oidc_issuer, oidc_client_id, oidc_client_secret, default_role}`. 2. `GET /api/platform/identity`. 3. `PUT` as a non-`users` role. | bearer | (1)/(2) 200, `has_client_secret:true`, the secret is **never** in the response; (3) 403. | High | Control | Feature (SSO), ITGC-AC-04 | Not Run | ext.ts |
| UAT-ADM-050 | SSO — authorize URL / not-configured | (public) | T2 has SSO on; HQ off | 1. `GET /api/auth/sso/authorize?tenant=T2`. 2. `…?tenant=HQ`. | — | (1) 200, URL starts with the issuer + has `client_id` + `state`; (2) 503 `SSO_NOT_CONFIGURED`. | High | Positive | Feature (SSO) | Not Run | ext.ts |
| UAT-ADM-051 | SSO — callback JIT-provisions + mints session | (public) | T2 SSO on | 1. `POST /api/auth/sso/callback` body `{state:"T2.x", id_token:<HS256, signed w/ client secret>}`. 2. `GET /api/auth/me` with the returned token. 3. Repeat (1) with the same `sub`. | id_token (in body) | (1) 200 `{token, role:default}`; (2) 200, username = the JIT user; (3) **same** user (idempotent by `sso_subject`). | High | Positive | Feature (SSO), ITGC-AC-01 | Not Run | ext.ts |
| UAT-ADM-052 | SSO — id_token rejected (sig/aud) | (public) | T2 SSO on | 1. Callback with a **tampered** id_token. 2. Callback with a **wrong-audience** id_token. | id_token | (1) 401 `BAD_ID_TOKEN`; (2) 401 `BAD_AUDIENCE`. | High | Control | Feature (SSO), ITGC-AC-01 | Not Run | ext.ts |
| UAT-ADM-053 | SCIM — token + auth + provision (SoD) | AccessAdmin@T2; SCIM token | SCIM enabled | 1. `POST /api/platform/identity/scim-token`. 2. `GET /scim/v2/Users` with no token. 3. `POST /scim/v2/Users` `{userName, externalId, active}`. 4. `POST` the same userName again. 5. `GET /scim/v2/Users?filter=userName eq "…"`. | bearer scim_… | (1) `scim_…` once; (2) 401 `SCIM_UNAUTHORIZED`; (3) 201, active, `externalId` linked; (4) 409 (reuses the SoD-checked admin create); (5) `ListResponse` totalResults 1. | High | Control | Feature (SCIM), ITGC-AC-09 | Not Run | ext.ts |
| UAT-ADM-054 | SCIM — deprovision = deactivate (soft) | SCIM token | a provisioned user | 1. `PATCH /scim/v2/Users/:id` `active=false`. 2. `DELETE /scim/v2/Users/:id`. 3. `GET /scim/v2/Users/:id`. | bearer scim_… | (1) 200, `active:false`; (2) 204; (3) the row **survives** with `active:false` (no hard delete — audit trail kept). | High | Control | Feature (SCIM), ITGC-AC-10 | Not Run | ext.ts |
| UAT-ADM-055 | SCIM/identity — tenant isolation + deactivated login block | SCIM tokens (2 tenants) | T2 has a user; a deactivated seeded user | 1. `GET /scim/v2/Users?filter=…T2 user` with **HQ's** SCIM token. 2. `POST /api/login` as a `is_active=false` user. | bearer scim_… | (1) totalResults 0 (RLS isolation); (2) 401 `USER_DEACTIVATED`. | High | Control | Feature (SSO/SCIM), ITGC-AC-02/03 | Not Run | ext.ts |
| UAT-ADM-056 | Document templates — create, default & live preview | AccessAdmin/Exec | tenant profile set | 1. `GET /api/document-templates/doc-types`. 2. `POST /api/document-templates {doc_type:receipt, name, config}`. 3. `GET …/active?doc_type=receipt`. 4. `POST …/preview {doc_type:receipt, config}`. | as left | catalog lists `receipt` as `live`; first template → `is_default:true`; active config echoes the saved knobs; preview returns HTML reflecting the config (e.g. the custom thank-you text). | High | Positive | Feature (document templates) | Not Run | ext.ts |
| UAT-ADM-057 | Document templates — core integrity, gate, isolation, no GL | AccessAdmin (2 tenants); Warehouse | a receipt template exists | 1. `POST …/preview` with empty `{}` config. 2. `POST …` unknown `doc_type`. 3. `POST …` duplicate name. 4. `GET …` as Warehouse (no `users`/`exec`). 5. `GET …` as the other tenant. 6. inspect `journal_lines` before/after authoring. | as left | (1) the total + seller name still render (core never blanked, amounts unchanged); (2) 400 `BAD_DOC_TYPE`; (3) 400 `NAME_EXISTS`; (4) 403; (5) 0 templates (RLS); (6) journal-line count unchanged (no GL post). | High | Control | Feature (document templates) | Not Run | ext.ts |
| UAT-ADM-058 | Custom objects — define, fields & record CRUD | MasterDataAdmin/Exec | — | 1. `POST /api/custom-objects {label}`. 2. define fields via `POST /api/custom-fields/defs` (entity = object_key). 3. `GET /api/custom-objects/:key`. 4. `POST …/:key/records {values}`. 5. `GET …/:key/records`. 6. `PUT …/:key/records/:id`. 7. `DELETE …/:key/records/:id`. | as left | object created with a slugged key; the object carries its field defs; record created with a `record_id` + `display_name`; list returns typed values; update recomputes the display; delete drops it from the list. | High | Positive | Feature (custom objects) | Not Run | ext.ts |
| UAT-ADM-059 | Custom objects — validation reuse, isolation, no GL | MasterDataAdmin (2 tenants) | an object with a required text + a select field | 1. `POST /api/custom-objects` duplicate. 2. create a record with an out-of-list select value. 3. create a record missing the required field. 4. `GET /api/custom-objects` as the other tenant. 5. inspect `journal_lines` before/after. | as left | (1) 400 `OBJECT_EXISTS`; (2) 400 `BAD_OPTION`; (3) 400 `REQUIRED_FIELD` (reused custom-fields validation); (4) the object isn't visible (RLS); (5) journal-line count unchanged (no GL post). | High | Control | Feature (custom objects) | Not Run | ext.ts |
| UAT-ADM-060 | Object layouts — design, resolve & live preview | MasterDataAdmin/Exec | a custom object with ≥3 fields | 1. `GET /api/object-layouts/resolve?object_key=…` (no layout). 2. `POST /api/object-layouts` with sections + a hidden field. 3. `GET …/resolve`. 4. add a new field via `/api/custom-fields/defs`. 5. `GET …/resolve` again. 6. `POST …/preview` with a different config. | as left | (1) built-in layout = one section with all fields; (2) created, `is_default`; (3) sections applied, the field hidden; (4–5) the newly-added field auto-surfaces (never lost); (6) preview reflects the posted config without saving. | High | Positive | Feature (object layouts) | Not Run | ext.ts |
| UAT-ADM-061 | Object layouts — dup name, isolation, no GL | MasterDataAdmin (2 tenants) | a layout exists | 1. `POST /api/object-layouts` duplicate name. 2. `GET /api/object-layouts?object_key=…` as the other tenant. 3. inspect `journal_lines` before/after. | as left | (1) 400 `NAME_EXISTS`; (2) 0 layouts (RLS); (3) journal-line count unchanged (no GL post). | High | Control | Feature (object layouts) | Not Run | ext.ts |
| UAT-ADM-062 | Automation — create rule + run-event match/skip | MasterDataAdmin/Exec | event catalog | 1. `GET /api/automation/events`. 2. `POST /api/automation/rules` (event `alert.fired`, condition `severity eq critical`, action notification). 3. `POST …/run-event` payload `severity:critical`. 4. `POST …/run-event` payload `severity:warning`. 5. `GET …/executions`. | as left | catalog lists `alert.fired`; rule created; (3) matched ≥1, executed ≥1 + a notification written; (4) executed 0 (skipped); (5) executed + skipped both logged. | High | Positive | Feature (automation) | Not Run | ext.ts |
| UAT-ADM-063 | Automation — validation, isolation, no GL | MasterDataAdmin (2 tenants) | a rule exists | 1. `POST …/rules` unknown event. 2. `POST …/rules` unknown action type. 3. `GET …/rules` as the other tenant. 4. inspect `journal_lines` before/after. | as left | (1) 400 `BAD_EVENT`; (2) 400 `BAD_ACTION`; (3) 0 rules (RLS); (4) journal-line count unchanged (no GL post). | High | Control | Feature (automation) | Not Run | ext.ts |
| UAT-ADM-064 | Analytics studio — model + governed aggregate + RLS | Warehouse/Exec (2 tenants) | tenant has POS sales | 1. `GET /api/query/model`. 2. `POST /api/query/run {dimension:payment_method}`. 3. `POST …/run` as another tenant. | as left | model lists measures × dimensions; run returns rows grouped with summed totals for the tenant; another tenant's run totals 0 (RLS). | High | Positive | Feature (semantic layer) | Not Run | ext.ts |
| UAT-ADM-065 | Analytics studio — unknown dimension rejected | Exec | — | 1. `POST /api/query/run {dimension:nope}`. | as left | 400 `BAD_DIMENSION` (only whitelisted dimensions allowed; no ad-hoc SQL). | Med | Control | Feature (semantic layer) | Not Run | ext.ts |
| UAT-ADM-066 | Copilot — KB-grounded, cite-or-refuse | Admin/Exec | a KB doc exists | 1. ingest a policy doc. 2. ask a related question via `/api/copilot/ask`. | as left | the answer is grounded with ≥1 citation; with no API key the source is the KB (deterministic) — no fabrication; read-only. | Med | Positive | Feature (copilot) | Not Run | ext.ts |
| UAT-ADM-067 | Document AI — extract an AP draft | Procurement/Creditors | invoice text | 1. `POST /api/doc-ai/extract` with invoice text. | as left | returns vendor / invoice-no / date / amount as a draft (regex fallback with no key); nothing is posted to AP or the GL. | Med | Positive | Feature (document AI) | Not Run | ext.ts |
| UAT-ADM-068 | NL analytics — plain language → governed query | Warehouse/Exec | tenant has POS sales | 1. `POST /api/nl-analytics/ask {question:"sales by payment method"}`. | as left | resolves to dimension `payment_method` and returns grouped totals via the governed engine (no raw SQL); RLS-scoped. | Med | Positive | Feature (NL analytics) | Not Run | ext.ts |
| UAT-ADM-069 | AI config — suggest + reject unknown target | MasterDataAdmin | — | 1. `POST /api/ai-config/suggest {target:custom_object,…}`. 2. `…{target:nope}`. | as left | (1) returns a proposed config (template with no key) for review — not applied; (2) 400 `BAD_TARGET`. | Med | Control | Feature (AI config) | Not Run | ext.ts |
| UAT-ADM-070 | Controls monitoring — scan, review, isolation, no GL | Exec/AccessAdmin (2 tenants) | seeded duplicate invoices + shared-tax-id vendors | 1. `POST /api/controls/scan`. 2. `GET …/findings`. 3. review a finding. 4. scan as the other tenant. 5. inspect `journal_lines`. | as left | duplicate-invoice + ghost-vendor findings raised; a finding marked reviewed; the other tenant sees none of them (RLS); journal-line count unchanged (no GL). | High | Control | Feature (controls monitoring) | Not Run | ext.ts |
| UAT-ADM-071 | i18n — set own locale, resolution, bad code, per-user | Two users (HQ / cf2) | — | 1. `GET /api/i18n/locales`. 2. `PUT /api/i18n/me {locale:en}` then `GET …/me`. 3. `PUT …/me {locale:xx}`. 4. `GET …/me` as the other user. | as left | catalog ≥5 locales; my locale resolves to `en` (source `user`); `xx` → 400 `BAD_LOCALE`; the other user is unaffected (per-user). | Med | Positive | Feature (i18n) | Not Run | ext.ts |
| UAT-ADM-072 | White-label theme — set tokens, validation, RLS | AccessAdmin (2 tenants) | — | 1. `GET /api/tenant/theme`. 2. `PUT …/theme {primary_hue:200, radius:lg}`. 3. `PUT …{primary_hue:400}` / `{radius:huge}`. 4. `GET …/theme` as the other tenant. | as left | default gives an oklch primary; set returns hue 200; 400 `BAD_HUE` / `BAD_RADIUS`; the other tenant keeps its own theme (RLS); no GL. | Med | Control | Feature (white-label) | Not Run | ext.ts |
| UAT-ADM-073 | Onboarding — checklist + idempotent pack apply | Admin/Exec (2 tenants) | — | 1. `GET /api/onboarding`. 2. complete a step. 3. `POST …/apply-pack {restaurant}` ×2. 4. bad step / bad pack. 5. check another tenant's custom objects. | as left | steps + percent; step toggles; first apply seeds objects, re-apply 0 (idempotent); 400 `BAD_STEP` / `BAD_PACK`; seeded objects don't leak (RLS). | Med | Positive | Feature (onboarding) | Not Run | ext.ts |
| UAT-ADM-074 | Developer portal — keys, tiers, RLS | AccessAdmin (2 tenants) | a key exists | 1. `GET /api/developer/portal`. 2. `PUT …/keys/:id/tier {partner}`. 3. `…{gold}`. 4. portal as the other tenant. | as left | scopes/endpoints/tiers listed; tier set to partner; 400 `BAD_TIER`; another tenant doesn't see the key (RLS). | Med | Control | Feature (developer portal) | Not Run | ext.ts |
| UAT-ADM-075 | Connectors — register, sync, idempotent, per-tenant RLS | AccessAdmin (2 tenants) | — | 1. `GET …/catalog`. 2. register shopee + `POST …/:id/sync` ×2. 3. bad type. 4. bank_csv sync with CSV. 5. cf2 syncs same fixtures. | as left | catalog lists line/shopee/bank_csv; first sync new=2, re-sync new=0 (idempotent); 400 `BAD_CONNECTOR`; bank-CSV parses lines; cf2 dedupe is independent (per-tenant RLS); no GL. | Med | Positive | Feature (connectors) | Not Run | ext.ts |
| UAT-ADM-076 | Migration — dry-run field-map + validation + RLS | MasterDataAdmin (2 tenants) | — | 1. `GET …/sources`. 2. `POST …/dry-run {loyverse, products, [valid, invalid]}`. 3. bad source / bad entity. 4. jobs as another tenant. | as left | sources + entities listed; dry-run maps fields, total 2 / valid 1 / 1 error; 400 `BAD_SOURCE` / `BAD_ENTITY`; another tenant doesn't see the job (RLS); no writes/GL. | Med | Control | Feature (migration) | Not Run | ext.ts |
| UAT-ADM-077 | Localization — packs, apply, bad-country, RLS | Exec/AccessAdmin (2 tenants) | — | 1. `GET …/packs`. 2. `POST …/apply {TH}` + `GET …`. 3. `…{ZZ}`. 4. localization as the other tenant. | as left | TH certified + a draft listed; apply sets active TH + locale th; 400 `BAD_COUNTRY`; the other tenant has its own (no leak); no GL. | Med | Positive | Feature (localization) | Not Run | ext.ts |
| UAT-ADM-078 | e-Invoicing — submit, idempotent, validation | Exec/Creditors | — | 1. `GET …/providers`. 2. `POST …/submit {valid doc}` ×2. 3. submit a doc with no total. | as left | providers incl. stub + country adapters; submit returns accepted + ref; re-submit idempotent (same ref); 400 `BAD_DOC`; no GL. | Med | Control | Feature (e-invoicing) | Not Run | ext.ts |
| UAT-ADM-079 | Ops — metrics + cache round-trip | Exec/AccessAdmin | — | 1. `GET /api/ops/metrics`. 2. `GET …/cache-selftest` ×2. | as left | metrics expose uptime + cache (provider memory) + scale posture; first self-test cached:false, second cached:true (CacheService works); no GL. | Low | Positive | Feature (scale/ops) | Not Run | ext.ts |
| UAT-ADM-080 | PWA — installable + offline shell | Any device | — | 1. load the app, use the browser "Install". 2. inspect `/manifest.webmanifest` + SW registration. | as left | manifest served (name / icons / standalone); the app-shell SW registers (GET-only, API-skipping); installable; offline POS replays via the idempotent outbox. | Low | Positive | Feature (PWA) | Not Run | web build |
| UAT-ADM-081 | SoD R14 — reward config ✗ POS redemption blocked | AccessAdmin/Admin | — | 1. `POST /api/admin/users` with both `crm_reward`+`pos_sell`. 2. `POST /api/admin/users` with `['crm_reward']` only. | `{username: sod_r14, permissions:[crm_reward, pos_sell]}` | (1) 422 `SOD_CONFLICT` naming `R14`; user NOT created. (2) 200/201; single-duty `crm_reward` user accepted. | High | Control | ITGC-AC-09, R14 | Not Run | compliance.ts |
| UAT-ADM-082 | Feature flags — LABS modules off by default | Admin | new tenant | 1. `GET /api/feature-flags`. | — | `consolidation` (and other thin modules) `tier:LABS`, `enabled:false`; `labs_visible` `enabled:false` (`source:default`). | High | Positive | Platform (Labs) | Not Run | onboarding.ts |
| UAT-ADM-083 | Enable a Labs flag; unknown flag rejected; tenant-isolated | Admin (`md_config`) | as above | 1. `PUT /api/feature-flags/labs_visible {enabled:true}`. 2. `PUT /api/feature-flags/nonsense`. 3. `GET /api/feature-flags` as a different tenant. | — | (1) `labs_visible` `enabled:true` (`source:override`). (2) **400 `UNKNOWN_FLAG`**. (3) the other tenant still sees `labs_visible:false` (RLS — override does not leak). | High | Control | Platform (Labs) | Not Run | onboarding.ts |
| UAT-ADM-084 | R08 — Cashier (`pos_sell`) cannot access refund auth screen | Cashier (`pos_sell` only) | Cashier user created | 1. Login as Cashier. 2. Navigate to `/pos/refunds`. 3. Navigate to `/pos/till`. | — | (2) `/pos/refunds` is not in the nav and returns 403 (no `pos_refund`). (3) `/pos/till` is not in the nav and returns 403 (no `pos_till`). | High | Control | R08, SoD | Not Run | UI manual |
| UAT-ADM-085 | R08 — Cashier cannot see "บันทึกคืนสินค้า" button | Cashier (`pos_sell` only) | Cashier logged in | 1. Open `/returns`. 2. Inspect top-right area. | — | "บันทึกคืนสินค้า" button is hidden (hidden by `canRefund` check — `pos_sell` does not imply `pos_refund`). | High | Control | R08/R12, SoD | Not Run | UI manual |
| UAT-ADM-086 | R12 — POS Supervisor can access `/pos/refunds` and authorize | PosSupervisor (`pos_refund`) | Pending refund request exists | 1. `GET /api/payments/refund-requests?status=Pending`. 2. `POST /api/payments/refund-requests/:id/approve` as POS Supervisor. | valid refund request id | (1) returns requests list. (2) 200; request status becomes Approved. | High | Control | R12, SoD | Not Run | payments.controller |
| UAT-ADM-087 | R12 — Self-approve refund request blocked | PosSupervisor | Self-submitted refund request | 1. `POST /api/payments/refund-requests/:id/approve` as the same user who submitted it. | own request id | 403 `SOD_VIOLATION` — maker cannot approve their own refund. | High | Control | R12, SoD | Not Run | payments.controller |
| UAT-ADM-088 | R08 — POS Supervisor can access `/pos/till` and approve variance | PosSupervisor (`pos_till`) | Pending variance session exists | 1. Login as POS Supervisor. 2. Navigate to `/pos/till`. 3. `POST /api/payments/till/variance/:sessionNo/approve`. | — | (2) Till management screen shows, with Variance sessions marked. (3) 200; variance approved (by a different user than who closed). | High | Control | R08, SoD | Not Run | payments.controller |
| UAT-ADM-089 | R10 — Cashier cannot see pricing screen in nav | Cashier (`pos_sell` only) | Cashier logged in | 1. Login as Cashier. 2. Check ERP/POS nav. | — | "กฎราคา & โปรโมชั่น" nav item absent; direct `/pricing` returns 403. | High | Control | R10, SoD | Not Run | UI manual |
| UAT-ADM-090 | R10 — Sales role cannot reach /pricing | Sales (`pos`, `order_mgt`) | Sales user | 1. Direct navigation to `/pricing`. | — | 403 FORBIDDEN — `pricelist` or `exec` required. | High | Control | R10, SoD | Not Run | UI manual |
| UAT-ADM-091 | R10 — PricingManager can access /pricing and create a rule | PricingManager (`pricelist`, `promos`) | PricingManager user | 1. Login as PricingManager. 2. Navigate to `/pricing`. 3. `POST /api/pricing/rules` with rule body. | — | (2) Screen loads. (3) 201; rule created. | High | Control | R10, SoD | Not Run | UI manual |
| UAT-ADM-092 | R10 — API: pos-only token blocked from creating pricing rules | Sales (`pos`, `order_mgt`) | — | 1. `POST /api/pricing/rules` with `pos`-only JWT. | valid rule body | 403 FORBIDDEN — `pricelist` required. | High | Control | R10, SoD | Not Run | pricing.controller.ts |
| UAT-ADM-093 | R10 — API: pos token can still call /pricing/quote | Sales (`pos`) | — | 1. `POST /api/pricing/quote` with `pos` JWT. | valid quote body | 200 — quote returned normally. | High | Positive | R10, SoD | Not Run | pricing.controller.ts |
| UAT-ADM-094 | R12 — AR Clerk can see /returns in nav | AR Clerk (`ar`) | AR user | 1. Login as AR Clerk. 2. Check ERP/POS nav under POS workspace. | — | "คืนสินค้า & คืนเงิน" item is present in nav; `/returns` loads. | High | Control | R12, SoD | Not Run | UI manual |
| UAT-ADM-095 | R12 — AR Clerk sees "บันทึกคืนสินค้า" button | AR Clerk (`ar`) | AR user on /returns | 1. Navigate to `/returns`. 2. Inspect action area. | — | "บันทึกคืนสินค้า" button is visible (`ar` passes `canRefund`). | High | Control | R12, SoD | Not Run | UI manual |
| UAT-ADM-096 | R12 — POS Supervisor can reach /returns | PosSupervisor (`pos_refund`, no `pos`) | Supervisor without broad `pos` | 1. Login as POS Supervisor (`pos_refund` only). 2. Navigate to `/returns`. | — | Page loads; nav item visible. | High | Control | R12, SoD | Not Run | UI manual |
