{
  "name": "@ierp/etl",
  "version": "0.1.0",
  "private": true,
  "description": "SQLite → PostgreSQL migration (see docs/04-data-migration.md)",
  "scripts": {
    "start": "tsx src/index.ts",
    "validate": "tsx src/index.ts --pglite --limit 5000",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint placeholder)\"",
    "build": "echo \"(etl: run via tsx)\""
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.13",
    "@ierp/shared": "workspace:*",
    "drizzle-orm": "^0.36.4",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
